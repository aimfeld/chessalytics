---
status: complete
phase: 194-engine-main-thread-cache-hygiene
source: [194-VERIFICATION.md]
started: 2026-07-30T16:07:40Z
updated: "2026-07-30T16:42:37Z"
---

## Current Test

[testing complete]

## Tests

### 1. Resign / New opponent during a persona bot's think stops Stockfish immediately

expected: Stockfish worker CPU drops immediately, not after up to 2.5s.
why_human: Real browser CPU observation; no headless proxy exists.
requirement: ABORT-02 (Plan 02 backstop truth)
result: pass

### 2. A 400-node analysis-board search does not evict its own working set

expected: |
  Run a full 400-node analysis-board search and confirm the grade cache evicts
  none of that search's own entries mid-search. The 1024-entry cap sits against
  a documented ~386-FEN measured ceiling, so there should be ~2.6x headroom.
why_human: |
  No live eviction-count instrumentation exists in the codebase. The claim is
  currently capacity math against a PRE-change measurement, not a measured
  post-change result. Surfaced by verification, not by the plans themselves.
requirement: CACHE-01 (Plan 03 backstop truth)
result: skipped
reason: "User can't verify — no live eviction-count instrumentation exists to observe cache evictions during a search. Accepted as an acknowledged gap; see 194-VERIFICATION.md '## Acknowledged Gaps'."

### 3. Chart-to-engine navigation costs no second Maia inference

expected: |
  On /analysis, navigate to a position, let the Moves-by-Rating chart settle,
  then let the FlawChess engine panel run. The engine's first result appears
  with no second visible Maia inference delay (~130 ms/position) for that
  position — the chart's write-through should have already populated the
  shared fen|elo cache.
why_human: |
  Perceived latency in a real browser session. Unit coverage only proves zero
  analyze() calls on a pre-seeded cache hit, not the absence of a perceptible
  stall in the live flow.
requirement: CACHE-05 (Plan 03 Task 3 human-check)
result: pass

### 4. Bot play and analysis search stay responsive

expected: |
  Play several persona bot moves and run a 400-node analysis-board search.
  The board and controls stay responsive throughout — no visible input lag
  while the bot is thinking.
why_human: Perceived UI responsiveness; not measurable from unit tests.
requirement: JANK-03 (Plan 04 backstop truth)
result: pass

## Summary

total: 4
passed: 3
issues: 0
pending: 0
skipped: 1
blocked: 0

## Gaps
