---
phase: 198-mctssearch-continuous-dispatch
plan: 04
type: execute
wave: 4
depends_on: [198-03]
files_modified:
  - reports/continuous-dispatch/report.md
autonomous: false
requirements: [DISPATCH-02]

must_haves:
  truths:
    - "`reports/continuous-dispatch/report.md` states the measured per-expansion policy cost P and grade cost G at BOTH budgets, each traced to a named column in a named committed TSV."
    - "The modelled wall-clock reduction is computed at both budgets by the formula the accept rule pinned, with every input shown so a reader can re-derive the percentage by hand."
    - "The report states the modelled reduction as a function of c and the Little's-law saturation point c-star = (P + G) / P, and states explicitly that this is reported but not acted on in this phase (D-10)."
    - "SEED-126's quoted 123.5 ms per Maia inference and Phase 197's implied 86 ms upper bound are reconciled against the newly measured maia_cpu_ms per inference figure, with the discrepancy named rather than averaged away."
    - "The verdict is derived by applying `reports/continuous-dispatch/accept-rule.md` mechanically: the band at each budget is stated, and when the two budgets land in different bands the LOWER band governs, per the rule."
    - "The exit-or-continue decision is taken by the operator at an explicit checkpoint against the pre-declared bands — never inferred, never narrowed silently, and never satisfied by retreating to the prefetch-only variant."
    - "U-04's counter-intuitive direction is stated out loud: a faster policy narrows the gap between P and G/c and makes the overlap win BIGGER, so the WASM harness is the pessimistic environment and its number is a lower bound for WebGPU users."
    - "The report states plainly that the shipped grading ladder from commit 02fe44f2 cut ply-2 grades again after Phase 197 closed, shrinking this phase's own headroom — sequencing, not failure."
  artifacts:
    - path: "reports/continuous-dispatch/report.md"
      provides: "Re-baseline numbers, the U-04 ceiling model, the c-sweep, the SEED-126 reconciliation, and the mechanically-derived band verdict"
      min_lines: 80
  key_links:
    - from: "reports/continuous-dispatch/report.md"
      to: "reports/data/engine-grading-depth-ab-*.tsv"
      via: "a Provenance section naming each TSV by repository-relative path plus the verbatim command that produced it"
      pattern: "reports/data/engine-grading-depth-ab-"
    - from: "reports/continuous-dispatch/report.md"
      to: "reports/continuous-dispatch/accept-rule.md"
      via: "the verdict section applies the rule's bands to the computed percentages"
      pattern: "accept-rule"
  prohibitions:
    - statement: "MUST NOT present the U-04 modelled ceiling (or any rescaled estimate) as a measured throughput result."
      requirement_id: DISPATCH-02
      category: transparency
    - statement: "MUST NOT amend the accept rule's bands, budgets, or comparison rule after any re-baseline TSV has been committed — an override is a separate dated override document, never an edit to the rule."
      requirement_id: DISPATCH-02
      category: transparency
    - statement: "MUST NOT substitute the rejected prefetch-only variant for continuous dispatch without a recorded operator override."
      requirement_id: DISPATCH-02
      category: transparency
---

<objective>
D-15 steps 3b and 4 — the ceiling model, the mechanically-derived verdict, and the
exit-or-continue checkpoint.

This is where the phase either earns its rewrite or ends. The honest prior recorded in U-04 is
that it lands near its own exit band: a floor-10 batched grade around 82 ms against a policy
somewhere in the 86-123 ms range at concurrency 4 puts the per-expansion grade term around 20 ms
against a policy term around 86 ms, i.e. roughly 19 percent — squarely inside the 15-25 percent
checkpoint band. The bands were written knowing that, which is precisely what makes them a rule
rather than a rationalisation.

Purpose: convert two committed TSVs into a defensible go/no-go, taken by the operator against a
contract written before the numbers existed.
Output: `reports/continuous-dispatch/report.md` through its verdict section, plus a recorded
operator disposition.

**Exit is a first-class outcome.** If the governing band is below 15 percent, the phase still
ships the instrumentation, the accept rule, the design doc (198-05 runs on BOTH branches), and
the report, and 198-08 marks DISPATCH-03 through DISPATCH-10 `Rejected` in
`.planning/REQUIREMENTS.md` exactly the way Phase 197 marked LEAF-01. Plans 198-06 and 198-07 do
not run on that branch.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/198-mctssearch-continuous-dispatch/198-CONTEXT.md
@.planning/phases/198-mctssearch-continuous-dispatch/198-RESEARCH.md
@reports/continuous-dispatch/accept-rule.md
@reports/grading-ladder/report.md
@reports/grading-ladder/override-2026-07-31.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: compute the ceiling model, the c-sweep, and the SEED-126 reconciliation into report.md</name>
  <read_first>
    - Both `reports/data/engine-grading-depth-ab-*.tsv` files committed by 198-03 — the actual columns and values. Identify the exact column names carrying wall time, grade CPU, `maia_cpu_ms`, `maia_inferences`, and the grade-call count.
    - `reports/continuous-dispatch/accept-rule.md` section 2 — the formula and the paired-column discipline this section must obey. Do not restate the formula differently from how the rule pins it.
    - `.planning/phases/198-mctssearch-continuous-dispatch/198-CONTEXT.md` U-01 through U-05 and D-10 — the five premise corrections, and the mandate to report the ceiling as a function of concurrency including the saturation point.
    - `reports/grading-ladder/report.md` lines 1-45 — the headline-then-table-at-both-budgets shape and the Provenance table plus verbatim-command discipline to copy exactly.
    - `.planning/ROADMAP.md` section "Phase 198" Success Criterion 2 — the SEED-126 123.5 ms versus Phase 197 86 ms reconciliation the roadmap requires, and its note that the margin is thin rather than comfortable.
    - `reports/grading-ladder/override-2026-07-31.md` — U-02's record: the two-rung ladder override, its derived-not-measured cost basis, and the per-ply grade-CPU differencing. This pass is the first real measurement of that ladder and the report must say so.
  </read_first>
  <files>reports/continuous-dispatch/report.md</files>
  <action>
Create `reports/continuous-dispatch/report.md` following `reports/grading-ladder/report.md`'s
narration structure: a title, a `**Phase:** 198` / `**Date:**` / `**Contract:**` header naming
`reports/continuous-dispatch/accept-rule.md`, then `## Headline`, then `## Provenance`, then the
model sections.

`## Provenance` must be a table listing every TSV this report cites by repository-relative path
with a one-line description, followed by a fenced block containing the verbatim
`node --import ./scripts/lib/frontend-alias-hook.mjs scripts/...` command that produced each one.
Copy that discipline exactly — a number whose producing command is not written down is not
evidence.

`## 1. Measured split` — a table at BOTH budgets side by side, giving per-expansion policy cost P
(`maia_cpu_ms` divided by `maia_inferences`) and per-expansion grade cost G, each with the column
it came from and the position count. State the `maia_peak_inflight` value observed for the run.
Note explicitly that this is the first measurement of the shipped two-rung ladder plus floor 10,
whose own 1.4x wall-clock figure was a prediction rescaled from Stage A per-call costs and never
a measured Stage B candidate (U-02).

`## 2. The ceiling model` — state U-04's algebra with the inputs substituted so a reader can
re-derive the percentage by hand: a round of c expansions costs cP + G, i.e. P + G/c per
expansion; perfect overlap costs the max of P and G/c; the modelled reduction is
one minus max(P, G/c) divided by (P + G/c), capped at 50 percent and reached only when P equals
G/c. Give the resulting percentage at each budget. Label this section unambiguously as a MODEL
derived from measured P and G — not a measured throughput result. That labelling is a prohibition
in this plan's `must_haves`, not a stylistic preference.

Say U-04's correction out loud rather than burying it: SEED-127 and SEED-126 both read the
WASM-only caveat as the policy share being inflated and therefore the win being inflated. For this
change the algebra runs the other way — a faster policy narrows the gap between P and G/c and
increases the overlap win, so the WASM harness is the PESSIMISTIC environment and this number is a
lower bound for WebGPU desktop users. This should not be discovered by a reviewer.

`## 3. The c-sweep` — a table of modelled reduction against concurrency over a range spanning the
shipped values, plus the Little's-law saturation point (P + G) / P for a serial Maia. State
explicitly that this is reported and NOT acted on: `FLAWCHESS_BOT_CONCURRENCY` stays pinned and
the analysis board keeps device-adaptive `computePoolSize()` (D-10), because retuning concurrency
changes the tree and therefore strength, and Phase 199's sweep is already absorbing a combined
change. If concurrency 4 under-saturates Maia post-ladder, record that as a finding worth its own
future unit.

`## 4. SEED-126 reconciliation` — put the newly measured per-inference Maia cost next to
SEED-126 Appendix's quoted 123.5 ms and next to the 86 ms upper bound implied by Phase 197's
depth-1 differencing arm. Name the discrepancy and which figure the measurement supports; do not
average the three into a single comfortable number. State whether the measured figure still clears
the roughly 82 ms grade being overlapped, and whether the margin is thin or comfortable.

`## 5. Sequencing note` — state plainly, in the register `reports/grading-ladder/report.md` uses
for its own missed ambition, that the ladder shipped in commit `02fe44f2` cut ply-2 grades again
after Phase 197 closed and thereby shrank this phase's headroom. Phase 197 said this about itself
(P-02); it is now true twice over. Sequencing, not failure.

Do not write the verdict in this task and do not consult the accept rule's band section yet.
  </action>
  <verify>
    <automated>test -f reports/continuous-dispatch/report.md && grep -q 'Provenance' reports/continuous-dispatch/report.md && grep -q '123.5' reports/continuous-dispatch/report.md && grep -q 'reports/data/engine-grading-depth-ab-' reports/continuous-dispatch/report.md && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `reports/continuous-dispatch/report.md` exists with sections `## Headline`, `## Provenance`, `## 1. Measured split`, `## 2. The ceiling model`, `## 3. The c-sweep`, `## 4. SEED-126 reconciliation`, `## 5. Sequencing note`.
    - Every TSV path cited in `## Provenance` resolves under `test -f`.
    - Each cited TSV in `## Provenance` is accompanied by a fenced `node --import ./scripts/lib/frontend-alias-hook.mjs` command line.
    - `## 1. Measured split` reports P and G at both budgets, each with its source column name and the position count.
    - `## 2. The ceiling model` shows every input substituted, and its modelled percentage is re-derivable by hand from the stated P, G, and concurrency.
    - `## 2` labels its output as modelled rather than measured: the word `model` appears in that section's prose.
    - `## 3. The c-sweep` contains a saturation-point value and an explicit statement that shipped concurrency is not retuned in this phase.
    - `## 4` names all three figures — the newly measured per-inference cost, SEED-126's 123.5, and the 86 upper bound — without collapsing them into one.
    - No band verdict has been written yet: `grep -c '## 6' reports/continuous-dispatch/report.md` is 0.
  </acceptance_criteria>
  <done>The re-baseline is modelled, the c-sweep and saturation point are reported, the SEED-126 discrepancy is named, and every number traces to a committed TSV and a verbatim command.</done>
</task>

<task type="auto">
  <name>Task 2: apply the accept rule mechanically and record the band verdict</name>
  <read_first>
    - `reports/continuous-dispatch/accept-rule.md` in full — in particular its band section's exact inequalities, which boundary is inclusive, the no-rounding statement, and the split-band tie-break where the LOWER band governs.
    - `reports/continuous-dispatch/report.md` section 2 as written in Task 1 — the two computed percentages.
    - `reports/grading-ladder/report.md` — how a rule-derived selection is narrated as mechanical rather than as a judgement call.
    - `reports/leaf-wdl/report.md` — the precedent for narrating an evidence-based rejection without framing it as a failure.
  </read_first>
  <files>reports/continuous-dispatch/report.md</files>
  <action>
Append `## 6. Verdict` to `reports/continuous-dispatch/report.md`.

Quote the accept rule's bands verbatim. State the computed modelled reduction at each budget,
apply the stated inequality at each boundary with no rounding, and name the resulting band per
budget. If the two budgets land in different bands, apply the rule's tie-break — the LOWER band
governs — and say so explicitly rather than choosing.

State the governing band as exactly one of the three outcomes, using the rule's own words: build
it; raise a checkpoint and let the operator decide; or exit the phase.

Then state, in one paragraph, what that band means operationally for the remainder of this phase:
which plans run, which do not, and what `.planning/REQUIREMENTS.md` will record. On the exit
branch, say plainly that measured-and-not-worth-shipping is the same kind of success Phase 197
was — that phase's verification explicitly refused to score an evidence-based rejection as
partial, on the grounds that doing so would penalise honest negative results relative to a phase
that fabricated a marginal accept. Phase 198 inherits that standard.

Do NOT adjust the rule, the position set, the budgets, or the formula to move the number across a
boundary. If the rule as written cannot be applied to the data as measured, that is a checkpoint
to raise in Task 3, and the resolution is a separate dated override document in the shape of
`reports/grading-ladder/override-2026-07-31.md` — never an edit to the rule file.

Commit this section separately from Task 1's commit, so the git history shows the model and the
verdict as two distinct acts.
  </action>
  <verify>
    <automated>grep -q '## 6. Verdict' reports/continuous-dispatch/report.md && A=$(git log --diff-filter=A --format=%ct -- reports/continuous-dispatch/accept-rule.md | tail -1) && B=$(git log --diff-filter=A --format=%ct -- 'reports/data/engine-grading-depth-ab-*.tsv' | tail -1) && test "$A" -le "$B" && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `## 6. Verdict` exists in `reports/continuous-dispatch/report.md` and quotes all three bands verbatim from the accept rule.
    - It names one band per budget and one governing band, and where the two differ it states the LOWER-band tie-break was applied.
    - The governing band's operational consequence is stated: which of 198-05 through 198-08 run, and what `.planning/REQUIREMENTS.md` will record for DISPATCH-03 through DISPATCH-10.
    - `reports/continuous-dispatch/accept-rule.md` is unchanged since its add-commit: `git log --oneline -- reports/continuous-dispatch/accept-rule.md | wc -l` is 1.
    - The accept rule's add-commit timestamp is less than or equal to the earliest re-baseline TSV's add-commit timestamp.
    - The verdict section is its own commit, separate from Task 1's report commit.
  </acceptance_criteria>
  <done>The band verdict is derived mechanically from an unamended pre-declared rule, and the rule file has exactly one commit in its history.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: D-15 step 4 — the exit-or-continue checkpoint</name>
  <decision>Given the measured re-baseline and the mechanically-derived band, does Phase 198 proceed to the design doc plus rewrite, or exit with the measurement shipped?</decision>
  <context>
This is one of the phase's three named honesty mechanisms (alongside the pre-declared bands and
D-04's determinism derivation). It exists so the go/no-go is an operator decision taken out loud
against a contract written before the numbers existed, rather than a silent narrowing discovered
later in a diff.

Present, in this order:

1. The governing band and the two per-budget percentages from `## 6. Verdict`, with the position
   count, the budget, and the `maia_fifo` value each was measured at.
2. The two per-budget modelled reductions and the inputs P and G they were derived from.
3. The saturation point from the c-sweep, and whether concurrency 4 under-saturates Maia
   post-ladder.
4. The SEED-126 reconciliation outcome and whether the measured Maia per-inference cost still
   comfortably clears the grade being overlapped.
5. An explicit statement that the accept rule has one commit in its history and was not amended.

D-04's undo cost is recorded as `costly`: the design is the phase, and undoing it after Phase 199
means re-running whatever that sweep calibrated against. D-11's in-place-rewrite landing shape has
the same rating. That weight belongs in this decision, not discovered afterwards.

Steps 5 through 7 (design doc, rewrite, parity gate) do not start until this clears — with one
carve-out that is not optional: the design doc (198-05) ships on BOTH branches, because D-02
commits the phase to shipping the instrumentation, the accept rule, the design doc, and the report
even on exit.
  </context>
  <options>
    <option id="build">
      <name>Build it — the governing band is at or above 25 percent</name>
      <pros>The rule already decided this; no judgement is being added. 198-05 through 198-08 all run as planned.</pros>
      <cons>D-04 and D-11 are both rated costly: a later revert means re-running Phase 199's calibration sweep.</cons>
    </option>
    <option id="continue-on-checkpoint">
      <name>Continue — the governing band is 15 to 25 percent and the operator elects to build</name>
      <pros>The band the honest prior predicted. The win is real and, per U-04, is a lower bound for WebGPU users where a faster policy widens it.</pros>
      <cons>A marginal win against the riskiest change in the engine codebase. The decision and its reasoning must be recorded in the report as an operator call, not presented as if the rule mandated it.</cons>
    </option>
    <option id="exit-on-checkpoint">
      <name>Exit — the governing band is 15 to 25 percent and the operator elects to stop</name>
      <pros>Spends no risk on a marginal win. Everything measured still ships.</pros>
      <cons>Forfeits a real, if modest, throughput improvement, and Phase 199 then calibrates without it.</cons>
    </option>
    <option id="exit">
      <name>Exit the phase — the governing band is below 15 percent</name>
      <pros>The rule already decided this. Instrumentation, accept rule, design doc, and report still ship; DISPATCH-03 through DISPATCH-10 are marked Rejected the way Phase 197 marked LEAF-01.</pros>
      <cons>None beyond the forfeited throughput; an evidence-based rejection is a first-class outcome by this phase's own declared standard.</cons>
    </option>
    <option id="override">
      <name>Recorded operator override — deviate from the rule, or reconsider the rejected prefetch-only variant</name>
      <pros>Keeps a genuinely unforeseen situation resolvable without pretending the rule anticipated it.</pros>
      <cons>Requires a separate dated override document in the shape of `reports/grading-ladder/override-2026-07-31.md`, stating what is being overridden and why. Never an edit to the accept rule, and never a silent retreat — the prefetch-only variant was explicitly rejected on 2026-07-30 and is reachable only this way.</cons>
    </option>
  </options>
  <verify>
    <automated>grep -qE 'Operator disposition' reports/continuous-dispatch/report.md &amp;&amp; echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `reports/continuous-dispatch/report.md` gains an `## 7. Operator disposition` section naming the chosen option id, the date, and a one-paragraph rationale in the operator's own terms.
    - If the chosen option is `override`, a new file exists under `reports/continuous-dispatch/` named `override-<date>.md`, and `reports/continuous-dispatch/accept-rule.md` still has exactly one commit in its history.
    - If the chosen option is `exit` or `exit-on-checkpoint`, the disposition section explicitly lists DISPATCH-03 through DISPATCH-10 as the requirements 198-08 will mark `Rejected`, and states that 198-05 still runs.
    - The disposition is committed before any commit that touches `frontend/src/lib/engine/mctsSearch.ts`.
  </acceptance_criteria>
  <resume-signal>Select: build, continue-on-checkpoint, exit-on-checkpoint, exit, or override — and state the one-paragraph rationale to record.</resume-signal>
</task>

</tasks>

<flagged_assumptions>
The spec-less edge probe classified DISPATCH-02 as `unclassified`, so its probe row stays
`unresolved` rather than being auto-backstopped. Surfaced here as an explicit assumption rather
than silently dropped:

- **DISPATCH-02 (unclassified).** The probe could not assign an edge category to "a post-ladder
  re-baseline measures the policy/grade wall split and the `policy peak in-flight` telltale, and
  models the achievable ceiling before any code is written." The assumption this plan makes on its
  behalf is that DISPATCH-02's edge surface is entirely captured by the accept rule's own
  invalidation conditions (`maia_fifo` true, `maia_peak_inflight` exactly 1, `maia_cpu_ms` strictly
  inside `(0, wall_ms)`, at least 16 positions, identical position sets across budgets), which are
  asserted per row in 198-03's acceptance criteria. If a reviewer believes DISPATCH-02 carries an
  edge those five conditions miss, that is a real gap in this plan.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-declared rule → verdict | the accept rule is the integrity mechanism the verdict rests on |
| measured TSV → narrated report | numbers cross from machine output into prose a human decides on |
| operator decision → phase branch | the checkpoint disposition determines whether production code is touched at all |

ASVS V2/V3/V4/V5/V6 are **N/A** for this plan (no auth, session, access-control, external input,
or cryptography surface) — reason recorded in `198-RESEARCH.md` section Security Domain. This plan
writes only Markdown.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-198-01 | Tampering | `reports/continuous-dispatch/accept-rule.md` amended after the data is in hand | high | mitigate | Task 2's acceptance criteria assert the rule file has exactly one commit in its history and that its add-commit predates the earliest TSV add-commit. A deviation must be a separate dated override document, enforced by Task 3's `override` option criteria. |
| T-198-09 | Spoofing | a modelled ceiling presented as a measured throughput result | high | mitigate | Section 2 must carry the word `model` in its prose and show every substituted input so the reader re-derives it; a matching prohibition rides in `must_haves.prohibitions`. |
| T-198-10 | Repudiation | an exit-or-continue decision taken silently | high | mitigate | Task 3 is a blocking `checkpoint:decision` whose acceptance criteria require a committed `## 7. Operator disposition` section naming the chosen option id, the date, and the rationale, committed before any `mctsSearch.ts` edit. |
| T-198-11 | Elevation of Privilege | a silent retreat to the rejected prefetch-only variant | medium | mitigate | Reachable only through the `override` option, which requires a separate dated override document; a matching prohibition rides in `must_haves.prohibitions`. |
| T-198-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase (`198-RESEARCH.md` section Package Legitimacy Audit: not applicable). |
</threat_model>

## Artifacts this phase produces (scoped to this plan)

New file:
- `reports/continuous-dispatch/report.md` — sections `## Headline`, `## Provenance`,
  `## 1. Measured split`, `## 2. The ceiling model`, `## 3. The c-sweep`,
  `## 4. SEED-126 reconciliation`, `## 5. Sequencing note`, `## 6. Verdict`,
  `## 7. Operator disposition`
- Conditionally: `reports/continuous-dispatch/override-<date>.md` (only on the `override` option)

No new code symbols, flags, or TSV columns.

<verification>
- `reports/continuous-dispatch/report.md` carries sections 1 through 7.
- `reports/continuous-dispatch/accept-rule.md` has exactly one commit in its history.
- The disposition names one option id and is committed before any `mctsSearch.ts` edit.
</verification>

<success_criteria>
- The exit-or-continue decision was taken by the operator against an unamended pre-declared rule.
- Every number in the report traces to a committed TSV and a verbatim producing command.
- The modelled ceiling is labelled as a model, and U-04's counter-intuitive direction is stated rather than left for a reviewer to find.
- On the exit branch, the phase's remaining shape is written down before any code is skipped.
</success_criteria>

<output>
Create `.planning/phases/198-mctssearch-continuous-dispatch/198-04-SUMMARY.md` when done.
Record the two per-budget percentages, the governing band, the chosen option id, and the operator
rationale verbatim — 198-05 through 198-08 branch on it.
</output>
