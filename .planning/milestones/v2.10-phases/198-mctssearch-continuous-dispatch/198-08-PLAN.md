---
phase: 198-mctssearch-continuous-dispatch
plan: 08
type: execute
wave: 8
depends_on: [198-07]
files_modified:
  - reports/data/engine-dispatch-stop-rule-continuous-<stamp>.tsv
  - reports/continuous-dispatch/report.md
  - .planning/REQUIREMENTS.md
  - CHANGELOG.md
autonomous: true
requirements: [DISPATCH-02, DISPATCH-07, DISPATCH-08]

must_haves:
  truths:
    - "`scripts/lib/calibration-determinism.check.mjs` passes at the shipped bot budget with real engines and concurrency 4 — the app and the calibration harness agree bit-for-bit on the played move sequence."
    - "A `continuous`-mode stop-rule TSV is committed after the rewrite, and the report carries a before/after table comparing committed-nodes-at-stop and stop reason per position across the two committed TSVs."
    - "The before/after comparison is git-history-anchored across two commits rather than produced by a single script holding both code paths, because the round loop was not retained."
    - "The report states plainly whether the stop rule now fires at different node counts and by how much, framed as a Phase 199 calibration input rather than as something retuned here."
    - "`.planning/REQUIREMENTS.md`'s Traceability rows for DISPATCH-01 through DISPATCH-11 reflect the actual outcome — Complete on the continue branch, and Rejected for DISPATCH-03 through DISPATCH-10 on the exit branch, in the same shape and with the same explanatory note style Phase 197 used for LEAF-01."
    - "No stop-rule threshold, grading-ladder rung, or shipped concurrency value was retuned in this phase."
  artifacts:
    - path: "reports/continuous-dispatch/report.md"
      provides: "The completed narrated report: headline, provenance, model, verdict, disposition, stop-rule distribution, parity-gate result, and outcome"
      min_lines: 140
    - path: ".planning/REQUIREMENTS.md"
      provides: "DISPATCH-01..11 Traceability rows updated to the measured outcome"
      contains: "DISPATCH-11"
  key_links:
    - from: "reports/continuous-dispatch/report.md"
      to: "reports/data/engine-dispatch-stop-rule-round-*.tsv"
      via: "the before/after table cites both committed TSVs by repository-relative path and names the commit each was captured at"
      pattern: "engine-dispatch-stop-rule-"
  prohibitions:
    - statement: "MUST NOT quote the modelled wall-clock reduction at only one budget, or without naming the position count, the budget, and whether the Maia FIFO was enabled."
      requirement_id: DISPATCH-02
      category: transparency
    - statement: "MUST NOT retune the stop-rule thresholds, the grading ladder, or shipped concurrency in response to what the stop-rule distribution shows — measured here, retuned in its own unit."
      requirement_id: DISPATCH-07
      category: transparency
---

<objective>
D-15 step 7 and the phase close-out — the real-engine parity gate, the `continuous` half of D-08's
two-point stop-rule comparison, the completed report, and honest requirement statuses.

`scripts/lib/calibration-determinism.check.mjs` is confirmed NOT wired into CI or `package.json`;
it is a manual real-engine script the plan has to schedule explicitly, because it will not run
itself. It plays full games at the shipped bot budget with a real Stockfish pool and a real Maia
ONNX session and asserts byte-identical move sequences. It is DISPATCH-08's actual gate.

Purpose: prove the rewrite did not break app-versus-harness bit-identity, publish the calibration
input Phase 199 consumes, and record what actually happened.
Output: a passing parity gate, the `continuous` TSV, the finished report, and updated Traceability
rows.

**Both branches.** On the continue branch all three tasks run. On the exit branch Tasks 1 and 2 do
not apply — no rewrite exists to gate or to re-measure — and Task 3 closes the phase by marking
DISPATCH-03 through DISPATCH-10 `Rejected` exactly the way Phase 197 marked LEAF-01, with
DISPATCH-01, DISPATCH-02 and DISPATCH-11 Complete.

**Execution constraint.** Tasks 1 and 2 are real-engine runs measured in tens of minutes. Run both
in the FOREGROUND, inline. Do not background either and do not dispatch either to a subagent that
returns while the child is still running.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/198-mctssearch-continuous-dispatch/198-CONTEXT.md
@.planning/phases/198-mctssearch-continuous-dispatch/198-RESEARCH.md
@reports/continuous-dispatch/report.md
@reports/continuous-dispatch/accept-rule.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: run the real-engine determinism parity gate at concurrency 4</name>
  <precondition>Plan 198-04's operator disposition selected a continue outcome (`build` or `continue-on-checkpoint`) and the rewrite has landed — `git log --oneline $(git merge-base main HEAD)..HEAD -- frontend/src/lib/engine/mctsSearch.ts` is non-empty. On the exit branch, skip this task and record the skip in the summary.</precondition>
  <read_first>
    - `scripts/lib/calibration-determinism.check.mjs` — the whole 237-line script: what it plays, how many runs, what it asserts, and in particular its Plan 03 note on load-dependent divergence. A recurrence there would be a regression, not an expected flake, and must be investigated rather than re-run until green.
    - `frontend/src/lib/engine/botBudget.ts` — the shipped budget the check runs at, and the median per-move cost comment that sets the expected wall time.
    - `.planning/phases/198-mctssearch-continuous-dispatch/198-RESEARCH.md` Assumptions Log row A5 — the 10-to-15-minute estimate is extrapolated, not timed. Record the real duration so the estimate stops being an assumption.
    - `.planning/phases/198-mctssearch-continuous-dispatch/198-VALIDATION.md` section "Manual-Only Verifications" — this gate's row.
  </read_first>
  <files>reports/continuous-dispatch/report.md</files>
  <action>
Run the parity gate in the FOREGROUND, inline:
`node --import ./scripts/lib/frontend-alias-hook.mjs scripts/lib/calibration-determinism.check.mjs`

It must exit 0. Record the actual wall-clock duration, which replaces the extrapolated A5 estimate.

If it FAILS, do not re-run it until it happens to pass, and do not weaken the check. Attribute the
failure first: check out the pre-rewrite `mctsSearch.ts` commit recorded in 198-06's summary into a
scratch worktree and run the same gate there. If the pre-rewrite run also fails, the divergence
predates this phase and is a separate finding to raise, not this phase's regression. If the
pre-rewrite run passes and the post-rewrite run fails, that is a real determinism regression in the
rewrite and it is a blocking defect — the design doc's section 2 derivation says bit-identity at
fixed concurrency is guaranteed by construction, so a failure means the implementation departed
from the reviewed mechanism. Raise it with the divergence point (the first differing move index and
both move sequences) rather than patching around it.

Record the outcome in the report: append `## 8. Parity gate` naming the exact command, the exit
status, the measured duration, and — if it failed and was attributed — the pre-rewrite control
result.
  </action>
  <verify>
    <automated>node --import ./scripts/lib/frontend-alias-hook.mjs scripts/lib/calibration-determinism.check.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --import ./scripts/lib/frontend-alias-hook.mjs scripts/lib/calibration-determinism.check.mjs` exits 0.
    - `reports/continuous-dispatch/report.md` gains `## 8. Parity gate` naming the verbatim command, the exit status, and the measured wall-clock duration.
    - The script itself is unchanged: `git diff $(git merge-base main HEAD)..HEAD -- scripts/lib/calibration-determinism.check.mjs` is empty.
    - If the gate failed at any point, the report records the pre-rewrite control run's result and the first differing move index.
  </acceptance_criteria>
  <done>The app and the calibration harness agree bit-for-bit at concurrency 4 with real engines, and the gate's real cost is recorded rather than estimated.</done>
</task>

<task type="auto">
  <name>Task 2: capture the `continuous` stop-rule TSV and publish the before/after distribution</name>
  <precondition>The pre-rewrite `round` TSV from Plan 198-02 exists and is committed — `ls reports/data/engine-dispatch-stop-rule-round-*.tsv` resolves. On the exit branch, skip this task and record the skip.</precondition>
  <read_first>
    - `.planning/phases/198-mctssearch-continuous-dispatch/198-02-SUMMARY.md` — the resolved `round` TSV filename and the verbatim command line used to produce it. The `continuous` run must use the IDENTICAL flags apart from the mode label, or the two halves are not comparable.
    - `scripts/engine-dispatch-stop-rule.mjs` — the flags as implemented.
    - `.planning/phases/198-mctssearch-continuous-dispatch/198-RESEARCH.md` Pitfall 3 — why this is a two-point git-history-anchored comparison.
    - `reports/grading-ladder/report.md` — the Provenance discipline and the table-at-both-budgets narration shape.
    - `.planning/phases/198-mctssearch-continuous-dispatch/198-CONTEXT.md` D-08 and the Deferred Ideas entry on stop-rule thresholds — this is measured here and retuned in its own future unit.
  </read_first>
  <files>reports/data/engine-dispatch-stop-rule-continuous-<stamp>.tsv, reports/continuous-dispatch/report.md</files>
  <action>
Re-run the stop-rule harness against the rewritten `mctsSearch` with the IDENTICAL flags 198-02
used, changing only `--dispatch-mode` to `continuous`. Run it in the FOREGROUND, inline. Commit the
emitted TSV alone, with the verbatim command line in the commit message.

Then append `## 9. Stop-rule distribution (D-08)` to the report:

- A Provenance sub-entry naming both TSVs by repository-relative path, the commit each was captured
  at, and the verbatim command that produced each. State explicitly that this is a two-point
  comparison across two commits, not a dual-code-path script, because D-11 forbade retaining the
  round loop.
- A per-position table with committed-nodes-at-stop and stop reason on both sides, plus the delta.
- Summary statistics: how many positions changed stop reason, the median and range of the
  node-count shift, and whether the shift is systematically in one direction.
- One paragraph of interpretation, framed correctly: the stop rule is structurally unchanged and
  still fires once per applied expansion in a strictly ordered sequence, but WHICH nodes get
  selected changed, so the rolling stable-check counter fires at different node counts. That number
  is the artifact Phase 199 consumes.

Do not retune anything in response to what the table shows. Not the stop-rule thresholds, not the
grading ladder, not `budget.concurrency`. Those are explicitly deferred to their own units, and
retuning here would put a third unattributed variable into Phase 199's already-combined sweep.
  </action>
  <verify>
    <automated>ls reports/data/engine-dispatch-stop-rule-continuous-*.tsv >/dev/null 2>&amp;1 &amp;&amp; grep -q '## 9. Stop-rule distribution' reports/continuous-dispatch/report.md &amp;&amp; echo ok</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one `reports/data/engine-dispatch-stop-rule-continuous-*.tsv` exists, with the same position set as the `round` TSV: `comm -3 <(cut -f1 <round.tsv> | tail -n +2 | sort -u) <(cut -f1 <continuous.tsv> | tail -n +2 | sort -u)` produces no output.
    - Every row's `dispatch_mode` reads `continuous`, `maia_fifo` reads `true`, and `concurrency` and `max_nodes` match the `round` TSV's values.
    - The `round` TSV's add-commit predates the first in-phase `mctsSearch.ts` edit commit, and the `continuous` TSV's add-commit postdates it — assert both with `git log --diff-filter=A --format=%ct` comparisons.
    - `## 9. Stop-rule distribution (D-08)` exists with a per-position before/after table, summary statistics, and both TSVs named by repository-relative path with their verbatim producing commands.
    - Nothing was retuned: `git diff $(git merge-base main HEAD)..HEAD -- frontend/src/lib/engine/botBudget.ts frontend/src/lib/engine/gradingLadder.ts` is empty.
  </acceptance_criteria>
  <done>The stop-rule shift is a committed distribution and a published table, captured either side of the rewrite in git history, with nothing retuned in response.</done>
</task>

<task type="auto">
  <name>Task 3: finish the report and set honest requirement statuses on whichever branch ran</name>
  <read_first>
    - `reports/continuous-dispatch/report.md` as it stands — sections 1 through 9 on the continue branch, 1 through 7 on the exit branch.
    - `.planning/REQUIREMENTS.md` lines 76-88 (the DISPATCH requirement list) and lines 143-176 (the Traceability table, including the LEAF-01 `Rejected` row and the explanatory block beneath it at lines 151-159). That block is the exact shape and register to copy on the exit branch.
    - `reports/leaf-wdl/report.md` — the precedent for narrating an evidence-based rejection without framing it as a failure.
    - `CLAUDE.md` section "Changelog & Releases" — one or more terse user-facing bullets under `## [Unreleased]`, grouped into the right subsection, referencing the phase number.
    - `.planning/phases/198-mctssearch-continuous-dispatch/198-CONTEXT.md` Specific Ideas — the honest-framing register: the ladder ate this phase's headroom too, and measured-and-not-worth-shipping is the same kind of success Phase 197 was.
  </read_first>
  <files>reports/continuous-dispatch/report.md, .planning/REQUIREMENTS.md, CHANGELOG.md</files>
  <action>
Write the report's `## Headline` for real — it was drafted in 198-04 before the outcome was known —
following `reports/grading-ladder/report.md`'s shape: the result stated in bold prose first, then a
table at BOTH budgets. On the continue branch the table carries the modelled reduction per budget
plus the parity-gate result and the stop-rule shift. On the exit branch it carries the modelled
reduction per budget and the governing band, and the prose states plainly that the phase measured
the change and declined to build it.

Add a closing `## 10. Outcome` section stating which requirements are Complete, which are Rejected,
what shipped, and what is deferred to a later unit (the saturation-point finding from the c-sweep,
and any stop-rule retune the distribution suggests). Keep the honest-framing register: state that
the ladder shipped in `02fe44f2` cut ply-2 grades again after Phase 197 closed and shrank this
phase's headroom — sequencing, not failure — and, on the exit branch, that an evidence-based
rejection is a first-class outcome by this phase's own declared standard, not a partial result.

Update `.planning/REQUIREMENTS.md`:
- Continue branch: mark DISPATCH-01 through DISPATCH-11 `Complete` in the Traceability table and
  tick their checkboxes in the requirement list, with a short note under the table pointing at
  `reports/continuous-dispatch/report.md` as the evidence.
- Exit branch: mark DISPATCH-01, DISPATCH-02 and DISPATCH-11 `Complete` and DISPATCH-03 through
  DISPATCH-10 `Rejected`, and add an explanatory block beneath the table in the same shape and
  register as the existing LEAF-01 block: what was built, what was measured, which pre-declared
  gate rejected it, what the surviving requirements genuinely delivered, and the evidence path.
  Do not silently leave rows `Pending`; an unmarked row reads as unfinished work rather than as a
  decision.

Do not touch the file's top-of-file checkbox drift beyond this phase's own DISPATCH rows — that
general drift is a standing non-blocking warning and is explicitly not this phase's to fix.

Append the CHANGELOG bullets under `## [Unreleased]`, terse and user-facing, referencing Phase 198,
in the right subsection. On the exit branch the honest bullet is about the measurement and the
instrumentation, not about a speedup that did not ship.
  </action>
  <verify>
    <automated>grep -q '## 10. Outcome' reports/continuous-dispatch/report.md &amp;&amp; ! grep -E '^\| DISPATCH-(0[1-9]|1[01]) \| Phase 198 \| Pending \|' .planning/REQUIREMENTS.md &amp;&amp; echo ok</automated>
  </verify>
  <acceptance_criteria>
    - No DISPATCH row remains `Pending`: `grep -cE '^\| DISPATCH-[0-9]+ \| Phase 198 \| Pending \|' .planning/REQUIREMENTS.md` is 0.
    - All eleven DISPATCH rows are present and each reads `Complete` or `Rejected`: `grep -cE '^\| DISPATCH-[0-9]+ \| Phase 198 \| (Complete|Rejected) \|' .planning/REQUIREMENTS.md` is 11.
    - On the exit branch, an explanatory block beneath the Traceability table names the pre-declared gate that rejected the change and cites `reports/continuous-dispatch/report.md`, in the same shape as the existing LEAF-01 block.
    - `reports/continuous-dispatch/report.md` has a `## Headline` whose table covers BOTH budgets, and a `## 10. Outcome`.
    - `CHANGELOG.md` gains at least one bullet under `## [Unreleased]` referencing Phase 198, in an existing subsection heading.
    - The existing LEAF Traceability ROWS are untouched (an explanatory block may still cite LEAF-01 as precedent — only table rows are gated): `git diff $(git merge-base main HEAD)..HEAD -- .planning/REQUIREMENTS.md | grep -cE '^[-+]\| LEAF-'` is 0.
    - `cd frontend && npm run build && npm test -- --run && npm run lint && npm run knip` all exit 0 (continue branch), or the frontend is byte-unchanged against the phase branch point (exit branch).
  </acceptance_criteria>
  <done>The report tells the whole story at both budgets, every DISPATCH row carries a decided status, and the CHANGELOG says what actually changed for a user.</done>
</task>

</tasks>

<flagged_assumptions>
The spec-less edge probe classified DISPATCH-08 as `unclassified`, so its probe row stays
`unresolved` rather than being auto-backstopped. Surfaced explicitly:

- **DISPATCH-08 (unclassified).** The probe could not assign an edge category to
  "`scripts/lib/calibration-determinism.check.mjs` passes — the app and `calibration-harness.mjs`
  agree bit-for-bit at `FLAWCHESS_BOT_CONCURRENCY = 4`." The assumption this plan makes on its
  behalf is that a single green run of the unmodified script is sufficient witness. What that does
  NOT establish is flake resistance: the script's own Plan 03 note records a past load-dependent
  divergence, and one passing run cannot distinguish "deterministic" from "happened to agree under
  this machine's load". Task 1 mitigates the wrong-conclusion half by forbidding re-running until
  green and requiring pre-rewrite attribution on failure, but it does not add repeat runs. If a
  reviewer believes DISPATCH-08 requires N passing runs under varied load, that is a real gap.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| real-engine gate → phase completion claim | a green gate is the evidence DISPATCH-08 rests on |
| committed TSVs → published distribution table | machine output crosses into prose Phase 199 will calibrate against |
| measured outcome → `.planning/REQUIREMENTS.md` status | the durable record of what this phase actually delivered |

ASVS V2/V3/V4/V5/V6 are **N/A** for this plan (no auth, session, access-control, external runtime
input, or cryptography surface) — reason recorded in `198-RESEARCH.md` section Security Domain.
This plan runs two committed scripts and writes Markdown.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-198-21 | Tampering | a parity gate re-run until it happens to pass, or weakened to pass | high | mitigate | Task 1's action forbids both explicitly and requires attribution against the pre-rewrite commit on failure; the acceptance criteria assert an empty diff on the check script itself. |
| T-198-22 | Spoofing | a requirement row left `Pending` reading as unfinished work rather than as a recorded decision | high | mitigate | Task 3's automated verify and acceptance criteria assert zero `Pending` DISPATCH rows and exactly eleven rows reading `Complete` or `Rejected`. |
| T-198-23 | Repudiation | a before/after comparison whose two halves used different flags or position sets | medium | mitigate | Acceptance criteria assert identical position sets and matching concurrency and node-budget columns across the two TSVs, and assert the add-commit ordering either side of the rewrite. |
| T-198-24 | Tampering | a threshold retuned in response to the stop-rule distribution, adding a third unattributed variable to Phase 199's combined sweep | medium | mitigate | Acceptance criteria assert empty diffs on `botBudget.ts` and `gradingLadder.ts`; a matching prohibition rides in `must_haves.prohibitions`. |
| T-198-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase (`198-RESEARCH.md` section Package Legitimacy Audit: not applicable). |
</threat_model>

## Artifacts this phase produces (scoped to this plan)

New file:
- `reports/data/engine-dispatch-stop-rule-continuous-<stamp>.tsv` (continue branch only)

Extended file:
- `reports/continuous-dispatch/report.md` — adds `## 8. Parity gate`,
  `## 9. Stop-rule distribution (D-08)`, `## 10. Outcome`, and the real `## Headline`

Modified planning artifacts:
- `.planning/REQUIREMENTS.md` — DISPATCH-01 through DISPATCH-11 Traceability rows and checkboxes
- `CHANGELOG.md` — Phase 198 bullets under `## [Unreleased]`

No new code symbols, CLI flags, or TSV columns.

<verification>
- `node --import ./scripts/lib/frontend-alias-hook.mjs scripts/lib/calibration-determinism.check.mjs` exits 0 (continue branch).
- Both stop-rule TSVs exist with identical position sets and matching budgets, captured either side of the rewrite (continue branch).
- Zero DISPATCH rows read `Pending`; all eleven read `Complete` or `Rejected`.
- `cd frontend && npm run build && npm test -- --run && npm run lint && npm run knip` all exit 0 (continue branch).
</verification>

<success_criteria>
- App-versus-harness bit-identity is proven with real engines at the shipped bot concurrency, or the phase exited before there was anything to prove.
- Phase 199 receives a committed stop-rule distribution rather than a sentence.
- Every DISPATCH requirement carries a decided status, and an evidence-based rejection is recorded as the first-class outcome this phase declared it would be.
- Nothing was retuned in response to what was measured.
</success_criteria>

<output>
Create `.planning/phases/198-mctssearch-continuous-dispatch/198-08-SUMMARY.md` when done.
Record the parity gate's real duration (replacing the extrapolated estimate), the stop-rule shift
summary statistics, the final requirement statuses, and which branch ran.
</output>
