---
phase: 199-bot-re-calibration-sweep-strength-curve-refit
plan: 04
type: execute
wave: 2
depends_on: ["199-01", "199-02", "199-03"]
files_modified:
  - reports/bot-parity-199/runbook.md
autonomous: false
requirements: [RECAL-01, RECAL-04]
user_setup:
  - service: local-compute
    why: "The 5-cell parity sweep is a multi-hour supervised run on the operator's own 16-core box; only the operator can decide when to occupy it and can observe the streaming ledger."
    dashboard_config:
      - task: "Launch the 5 supervised cell runs and keep the machine available until -cells.tsv appears in all five out-dirs"
        location: "local shell, repo root"
must_haves:
  truths:
    - "The pre-registered thresholds are in git BEFORE the first sweep game is played — the launch is gated on it by a blocking decision checkpoint (D-03)."
    - "Each of the 5 cells is launched with exactly its own historic 4-anchor bracket from reports/data/bot-cells-sweep.tsv, so selectMeasureBracket has nothing else to choose and cannot silently re-bracket onto a different anchor set (D-02)."
    - "Every cell runs under bin/preset-supervisor.sh, never the bare harness driver, so a wasm out-of-bounds crash resumes from the ledger instead of restarting from zero (RECAL-04)."
    - "The 5 cells are launched with --seed 1 and 24 games per (cell, anchor), matching the 2026-07-21 run's discipline; only the engine version differs (D-02)."
    - "A committed runbook records the exact command line used for each cell, so the run is reproducible and auditable rather than reconstructed from shell history."
    - statement: "Launching all 5 cells concurrently stays inside the box's core budget under the corrected ~1-busy-core-per-harness model, and the operator confirms nproc before launching."
      verification: backstop
    - statement: "No calibration-harness process from an unrelated run is already live when the sweep launches; a second writer would corrupt the measurement."
      verification: backstop
  artifacts:
    - reports/bot-parity-199/runbook.md
  key_links:
    - "reports/data/bot-cells-sweep.tsv (git_sha 562bdd84) is the source of the pinned brackets; each cell's bracket is the set of anchors with 24 games in that file."
    - "PRESET_SUPERVISOR_ANCHORS (plan 03) -> preset-supervisor.sh launch() -> calibration-harness.mjs --anchors -> selectMeasureBracket, the chain that makes the bracket pinned rather than re-derived."
  prohibitions:
    - "Do NOT launch any cell with the default 10-anchor pool — that silently re-invokes the locate-and-bracket schedule and can re-bracket onto different anchors precisely when strength has shifted, corrupting the comparison D-02 exists to protect."
    - "Do NOT re-bracket, and do NOT run the two-pass locate-then-measure schedule."
    - "Do NOT launch the bare harness driver directly for the real cells; the crash supervisor is mandatory."
    - "Do NOT run the persona spot-check concurrently with the curve cells (P-07)."
    - "Do NOT edit the threshold constants or reports/bot-parity-199/accept-rule.md after this plan's checkpoint clears."
---

<objective>
Get the measurement running. Commit the exact runbook, lock the pre-registration behind a blocking
decision checkpoint, and launch the 5 pinned-bracket cells under the crash supervisor.

Purpose: the operator's stated working style for this phase is "start it and observe early results"
rather than gate on a long paper plan. Everything that genuinely cannot be fixed after launch — the
ledger columns, the threshold, the pinned brackets — is done by the time this plan starts. Nothing
else should stand between here and a running sweep.

Output: `reports/bot-parity-199/runbook.md` and five supervised harness processes streaming ledger
rows.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/199-bot-re-calibration-sweep-strength-curve-refit/199-CONTEXT.md
@.planning/phases/199-bot-re-calibration-sweep-strength-curve-refit/199-RESEARCH.md
@.planning/phases/199-bot-re-calibration-sweep-strength-curve-refit/199-01-SUMMARY.md
@.planning/phases/199-bot-re-calibration-sweep-strength-curve-refit/199-02-SUMMARY.md
@.planning/phases/199-bot-re-calibration-sweep-strength-curve-refit/199-03-SUMMARY.md
</context>

<pinned_brackets>
## The 5 cells and their pinned brackets (D-02, D-05)

Verified this session against `reports/data/bot-cells-sweep.tsv` (`git_sha 562bdd84`) — each bracket
is exactly the set of anchors that received 24 games in the 2026-07-21 run for that cell. Anchors
that received 0 or 8 games there were locate-pass or out-of-window spend and are deliberately
excluded.

| # | Cell | Role | Pinned anchors | Out-dir | Games |
|---|---|---|---|---|---|
| 1 | elo 1100, blend 0 | **null control** — validity gate | `maia700,maia1100,sf0,sf3` | `reports/data/sweep-199-human1100` | 96 |
| 2 | elo 1300, blend 0.05 | the measured non-monotone dip | `maia1100,maia1500,sf3,sf5` | `reports/data/sweep-199-light1300` | 96 |
| 3 | elo 1900, blend 0.05 | light's top end; twins `attacker-1600` | `maia1100,maia1500,sf3,sf5` | `reports/data/sweep-199-light1900` | 96 |
| 4 | elo 1500, blend 0.5 | deep's low end | `maia1500,maia1900,sf3,sf5` | `reports/data/sweep-199-deep1500` | 96 |
| 5 | elo 2300, blend 0.5 | the shared rung-1800 cell; twins `wall-1800` | `maia1500,maia1900,sf3,sf5` | `reports/data/sweep-199-deep2300` | 96 |

480 games total, matching CONTEXT.md's estimate. Cell 1 carried `beyond_ladder=true` in the original
run and has the widest committed CI of the five — which is why its role is a validity gate rather
than a pooled contributor.
</pinned_brackets>

<tasks>

<task type="auto">
  <name>Task 1: Commit the runbook with all five exact command lines</name>
  <files>reports/bot-parity-199/runbook.md</files>
  <read_first>
    - This plan's `<pinned_brackets>` table — the settled cells, anchors and out-dirs.
    - `bin/preset-supervisor.sh` as modified by plan 03 — the positional arguments (name, blend, elo)
      and the three environment overrides.
    - `.planning/phases/199-bot-re-calibration-sweep-strength-curve-refit/199-RESEARCH.md` section
      "CPU model and wall-clock estimate" — the old-engine per-cell wall-clock table and the explicit
      instruction not to present a single-point ETA.
    - `bin/run_persona_calibration_sweep.sh` lines 95-105 — the corrected CPU model (roughly one busy
      core per concurrent harness) that supersedes `run_bot_curves_sweep.sh`'s older `1 + procs`
      model and justifies launching all five cells at once.
    - `.planning/milestones/v2.7-phases/184-persona-calibration-strength-honesty/184-CONTEXT.md` D-09
      — the operator-run-sweep-with-a-committed-runbook shape this phase reuses at roughly one fifth
      the size.
  </read_first>
  <action>
    Create `reports/bot-parity-199/runbook.md` as the committed, copy-pasteable operator procedure.

    Open with a preflight section: confirm `nproc`, confirm no `calibration-harness.mjs` process is
    already live (a second writer corrupts the measurement), confirm
    `reports/bot-parity-199/accept-rule.md` and `scripts/calibration_parity_verdict.py` are committed,
    and confirm `node --import ./scripts/lib/frontend-alias-hook.mjs scripts/lib/calibration-ledger-schema.check.mjs`
    is green.

    Then give one launch block per cell, five in total, each exporting `PRESET_SUPERVISOR_DIR` to that
    cell's out-dir from the `<pinned_brackets>` table and `PRESET_SUPERVISOR_ANCHORS` to that cell's
    anchor list, then invoking `bin/preset-supervisor.sh` with a short cell name, the blend and the
    single elo value, backgrounded under `nohup` so the run survives the session ending. Set the games
    per cell to 24 (the supervisor's default; state it rather than relying on it). Record for each
    cell which of the five roles it plays.

    State the parallelism decision and its basis: launch all five concurrently. Under the corrected
    CPU model each concurrent harness averages roughly one busy core with transient bursts during an
    anchor search, so five concurrent runs sit well inside a 16-core box, and the wall clock collapses
    from the sum of the cells to roughly the longest cell. Give the fallback: if `nproc` is materially
    below 16, launch in two groups, deep cells first, since they are the long pole.

    Give an expectations section rather than an ETA. Record the old-engine per-cell wall clock from
    RESEARCH.md's table as the upper bound, note that the new numbers should come in lower because
    D-02 removes the locate-pass spend and the ladder measured a 1.35 to 1.4x wall-clock speedup on
    the 12-position fixture, and state plainly that the exact new number is not predictable and the
    ledger's own rows are the answer. Do not print a single-point estimate.

    Give an observation section for the "start it and observe early results" style: how to tail each
    cell's supervisor log, how to count completed games from the raw ledger while the run is in
    flight, and the standing warning that the per-cell aggregate is written once at the very end from
    memory, so its absence mid-run is expected and is not a fault. Name the aggregate's appearance as
    the single completion signal, matching the supervisor's own completion check.

    Give a crash-recovery section: the supervisor relaunches with resume automatically on the wasm
    out-of-bounds fault, losing at most the one in-flight game; the fast-crash guard aborts loudly
    after three sub-threshold crashes because that means a real defect rather than the slow wasm leak;
    if that abort fires, read the run log rather than relaunching blindly.

    Close with a "what must NOT happen" list drawn from this plan's prohibitions, so the operator has
    them in the same file as the commands.
  </action>
  <verify>
    <automated>test -f reports/bot-parity-199/runbook.md && grep -c PRESET_SUPERVISOR_ANCHORS reports/bot-parity-199/runbook.md</automated>
  </verify>
  <acceptance_criteria>
    - `reports/bot-parity-199/runbook.md` exists and is committed.
    - It contains exactly five launch blocks, and each names one of the five out-dirs from the
      `<pinned_brackets>` table.
    - Each launch block sets `PRESET_SUPERVISOR_ANCHORS`, and the five anchor lists match the
      `<pinned_brackets>` table exactly. Cross-check each against
      `reports/data/bot-cells-sweep.tsv` by confirming every listed anchor has 24 games for that
      `(bot_elo, bot_blend)` pair, and that no anchor with 0 or 8 games appears.
    - No launch block omits the anchors variable — a cell launched without it would silently use the
      default 10-anchor pool.
    - The runbook states the completion signal as the appearance of the per-cell aggregate file, and
      states that its absence mid-run is expected.
    - The runbook contains no single-point ETA; it presents a range plus the reason a point estimate
      is not available.
    - The preflight section names all four checks (cores, no live harness, pre-registration committed,
      ledger schema check green).
    - `bash -n` on any shell block extracted from the runbook exits 0.
  </acceptance_criteria>
  <done>
    A committed, copy-pasteable runbook whose command lines are verified against the historic bracket
    data.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Lock the pre-registration — the one-way door before launch</name>
  <read_first>
    - `reports/bot-parity-199/accept-rule.md` (plan 02) — the pre-registered contract.
    - `scripts/calibration_parity_verdict.py` — the five threshold constants.
    - `.planning/phases/199-bot-re-calibration-sweep-strength-curve-refit/199-CONTEXT.md` D-03 and
      amendment A-04.
  </read_first>
  <decision>
    Lock the parity thresholds and launch, or revise them first. Once the sweep starts producing
    ledger rows the threshold can no longer be honestly set, so this is the last moment at which
    revising it is legitimate.
  </decision>
  <context>
    D-03 makes the pre-registration explicitly non-editable after data is visible. A-04 already
    revised D-03's original flat plus-or-minus 50 into a per-family split, because the committed Maia
    CIs are roughly twice as wide as the SF ones and a flat 50 would have produced a false-fail rate
    the run cannot support. That revision was made from the committed CIs alone, with no new data, so
    it is legitimate; any further revision after launch would not be.

    The adopted values are: Maia pooled threshold 85.0, SF pooled threshold 50.0, null-control
    tolerance 165.0 Maia and 149.0 SF, all derived in 199-02-PLAN.md's pre-registration block and
    re-checkable against `reports/data/bot-curves-internal-scale.json`.

    Also being locked implicitly: D-04's exit branch. If parity fails, this phase stops and reports.
    It does not revert the ladder and does not escalate to a refit. Note that per D-10 the named
    revert target does not restore the calibrated configuration anyway — the committed curves were
    measured against flat depth 14 plus the 2500 ms movetime safety cap, a third configuration
    distinct from both the live ladder and the named revert target — so "just revert" is not the safe
    undo it appears to be, and is not an option this phase exercises.
  </context>
  <options>
    <option id="lock-and-launch">
      <name>Lock the adopted thresholds and proceed to launch</name>
      <pros>Honest pre-registration; the arithmetic is derived from committed data and independently checkable; matches D-03 and A-04</pros>
      <cons>Commits to a resolution of roughly plus-or-minus 85 Maia and plus-or-minus 50 SF; a smaller real shift will not be detectable and the report must say so</cons>
    </option>
    <option id="revise-first">
      <name>Revise a threshold before launching</name>
      <pros>Last legitimate opportunity to change it; still uses no new data</pros>
      <cons>Requires re-deriving from the committed CIs and re-committing accept-rule.md and the constants before any cell starts; delays the run</cons>
    </option>
    <option id="widen-cells">
      <name>Add cells or games to buy finer resolution before launching</name>
      <pros>Would tighten the pooled resolution below the adopted thresholds</pros>
      <cons>Contradicts D-01 and D-05; the full 15-cell sweep is explicitly out of scope and is what finer resolution actually requires</cons>
    </option>
  </options>
  <acceptance_criteria>
    - `git log --oneline -- reports/bot-parity-199/accept-rule.md scripts/calibration_parity_verdict.py`
      returns at least one commit for each path, and `git status --porcelain` is clean for both.
    - No path matching `reports/data/sweep-199-*` exists in the working tree at the moment this
      checkpoint is answered — the pre-registration must precede the first game, not accompany it.
    - `uv run python scripts/calibration_parity_verdict.py --self-test` exits 0.
    - `node --import ./scripts/lib/frontend-alias-hook.mjs scripts/lib/calibration-ledger-schema.check.mjs`
      exits 0.
    - The operator's answer is recorded in the plan summary, including which option was chosen.
  </acceptance_criteria>
  <resume-signal>Select: lock-and-launch, revise-first, or widen-cells</resume-signal>
  <done>
    The threshold is fixed in git, the instrument checks are green, and no sweep data exists yet.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator launches the 5 pinned-bracket cells</name>
  <precondition>
    The operator's own 16-core box is free for a multi-hour run, no other `calibration-harness.mjs`
    process is live, and task 2's checkpoint returned lock-and-launch.
  </precondition>
  <read_first>
    - `reports/bot-parity-199/runbook.md` — the five launch blocks, verbatim.
    - `bin/preset-supervisor.sh` — what the supervisor does on crash, and its completion check.
  </read_first>
  <what-built>
    Plans 01 through 03 built the instrument: a ledger that records per-game wall clock, a schema
    check that pins it, an anchor-pinning seam on the crash supervisor, and a pre-registered verdict
    machine. Task 1 committed the runbook. Nothing further is automatable here — the sweep is a
    multi-hour occupation of the operator's machine and only the operator can decide to start it.
  </what-built>
  <how-to-verify>
    Run the runbook's preflight section. Then launch all five cells from
    `reports/bot-parity-199/runbook.md`, each backgrounded under `nohup` so the run survives the
    session ending.

    Within a few minutes of launch, confirm for every one of the five out-dirs that: a run log exists
    and is growing; a raw ledger file exists whose header has 21 tab-separated fields ending in the
    two timing columns; at least one completed game row has been written; and the distinct values in
    that ledger's anchor column are a subset of that cell's pinned four. Record the five supervisor
    PIDs.

    Then leave it running. Do NOT wait for completion inside this task — plan 06 gates on completion,
    and plan 05 is designed to proceed while this runs.
  </how-to-verify>
  <acceptance_criteria>
    - Five supervisor processes are running, one per cell, and their PIDs are recorded in the plan
      summary.
    - `ls -d reports/data/sweep-199-*` lists exactly the five out-dirs from the `<pinned_brackets>`
      table.
    - Each out-dir holds a raw ledger whose header line has 21 tab-separated fields with `elapsed_ms`
      and `mean_move_ms` last, in that order.
    - Each out-dir's ledger has at least one data row, and every row's `elapsed_ms` is a positive
      integer and `mean_move_ms` parses as a positive float.
    - For each out-dir, the distinct values of the ledger's `anchor` column are a subset of that
      cell's pinned four anchors from the `<pinned_brackets>` table. Any fifth anchor value means the
      anchors variable did not reach the harness and the run must be killed and relaunched — a
      re-bracket would corrupt the comparison.
    - Every ledger row's `seed` column is 1.
    - Cell 1's `mean_move_ms` values are visibly smaller than the blend>0 cells' — the null control
      does no search, so a search-sized value there would contradict the blend dispatch.
    - No per-cell aggregate file exists yet in any out-dir. This is expected: the aggregate is written
      once at the end.
  </acceptance_criteria>
  <verify>
    <human-check>Five supervised cells are streaming ledger rows with pinned anchors and populated timing columns.</human-check>
  </verify>
  <resume-signal>Type "launched" once all five cells are streaming correctly-anchored, timed ledger rows, or describe the failure</resume-signal>
  <reversibility rating="costly">
    Killing and relaunching costs the hours already spent. The run is restartable via the ledger
    resume path, so nothing is destroyed, but operator wall clock is not recoverable — which is why
    every unfixable-after-launch item was front-loaded into waves 1 and 2.
  </reversibility>
  <done>
    Five supervised cells are running against their pinned brackets, writing timed ledger rows.
  </done>
</task>

</tasks>

<threat_model>
Assessed for the whole phase in 199-01-PLAN.md's `<threat_model>`: offline measurement tooling only,
no web tier, no user data, no packages introduced. T-199-02 (tampering via a mismatched anchor pool
on resume) is the live one during this plan, mitigated by plan 03's threading of the anchors through
the shared launch path and by this plan's acceptance criterion that every ledger row's anchor is a
member of the cell's pinned four.
</threat_model>

<verification>
- `reports/bot-parity-199/runbook.md` is committed with five launch blocks whose anchor lists match
  `reports/data/bot-cells-sweep.tsv`.
- The pre-registration is committed and no sweep data existed when the checkpoint cleared.
- Five supervisors are live; five out-dirs hold growing, 21-column, correctly-anchored ledgers.
- `git status --porcelain frontend/` is empty.
- `git status --porcelain reports/data/bot-strength-lookup.json frontend/src/generated/botStrengthCurves.ts reports/data/persona-calibration.json frontend/src/generated/personaCalibration.ts`
  is empty.
</verification>

<success_criteria>
The sweep is running under crash supervision against pinned brackets, its threshold was fixed in git
beforehand, and the operator can observe streaming results — which is the working style this phase
was scoped around.
</success_criteria>

<output>
Create `.planning/phases/199-bot-re-calibration-sweep-strength-curve-refit/199-04-SUMMARY.md` when done.
</output>
