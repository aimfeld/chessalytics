---
phase: 199-bot-re-calibration-sweep-strength-curve-refit
plan: 06
type: execute
wave: 3
depends_on: ["199-04"]
files_modified:
  - reports/data/sweep-199-human1100/
  - reports/data/sweep-199-light1300/
  - reports/data/sweep-199-light1900/
  - reports/data/sweep-199-deep1500/
  - reports/data/sweep-199-deep2300/
  - reports/data/sweep-199-personas/
autonomous: false
requirements: [RECAL-01, RECAL-03, RECAL-04]
user_setup:
  - service: local-compute
    why: "Waiting out the 5-cell run and then running the 2 persona spot-check cells is multi-hour operator-supervised work on the operator's own box."
    dashboard_config:
      - task: "Keep the machine available until all five per-cell aggregates appear, then run the persona spot-check pass"
        location: "local shell, repo root"
must_haves:
  truths:
    - "All five curve cells reach clean completion, evidenced by a per-cell aggregate file in each of the five out-dirs — the supervisor's own completion signal."
    - "The two persona spot-checks are attacker-1600 (Light, blend 0.05, botElo 1900) and wall-1800 (Deep, blend 0.5, botElo 2300) — one per search preset, two styles, both blend>0 (D-06 as amended by A-01)."
    - "The persona pass runs with the fit step suppressed, so reports/data/persona-calibration.json and frontend/src/generated/personaCalibration.ts are never rewritten (RECAL-03 stays conditional and untriggered)."
    - "The persona pass writes to a fresh data tree, so it plays real games instead of skipping against the 2026-07-22/23 aggregates already present under reports/data (P-06)."
    - "The persona pass is launched only after every curve-cell harness process has exited, because the persona runbook refuses to launch while one is live (P-07)."
    - "The raw ledgers and per-cell aggregates are force-added to git, because .gitignore excludes the out-dir pattern and the measurement evidence would otherwise never be committed (P-05)."
    - statement: "If the wasm out-of-bounds fault fires during the run, the supervisor resumes from the ledger and at most the one in-flight game is lost — RECAL-04 observed rather than assumed."
      verification: backstop
    - statement: "Each persona row can be paired within-run against the plain curve cell at the identical botElo and blend, so the style-times-ladder question is a paired comparison rather than new ELO coverage (A-01)."
      verification: backstop
  artifacts:
    - reports/data/sweep-199-human1100
    - reports/data/sweep-199-light1300
    - reports/data/sweep-199-light1900
    - reports/data/sweep-199-deep1500
    - reports/data/sweep-199-deep2300
    - reports/data/sweep-199-personas
  key_links:
    - "attacker-1600 twins curve cell 3 at (1900, 0.05); wall-1800 twins curve cell 5 at (2300, 0.5). Every rung-1600/1800 persona collides with a curve cell by construction, because persona botElo values are retargeted onto the same calibration grid."
    - "preset-supervisor.sh cells_present() is the completion signal; the aggregate is written once at the end from memory, so its absence mid-run is expected and is not a fault."
  prohibitions:
    - "Do NOT run the persona pass with the fit step enabled — it would overwrite reports/data/persona-calibration.json and regenerate frontend/src/generated/personaCalibration.ts, both explicitly out of scope."
    - "Do NOT sweep all 24 personas; exactly two spot-checks."
    - "Do NOT report the persona rows as additional ELO coverage — they add none (A-01)."
    - "Do NOT force-add any run.log or supervisor.log; only the ledger and aggregate TSVs."
    - "Do NOT delete or overwrite the pre-195 directories reports/data/persona-sweep-attacker-1600 and reports/data/persona-sweep-wall-1800."
    - "Do NOT refit reports/data/bot-strength-lookup.json or regenerate frontend/src/generated/botStrengthCurves.ts."
---

<objective>
See the measurement through to durable, committed evidence: wait out the 5 curve cells, run the 2
persona spot-checks against a fresh tree with the fit step suppressed, and force-add the resulting
ledgers and aggregates past the gitignore rule that would otherwise silently drop them.

Purpose: without the force-add, this phase's entire evidentiary basis stays untracked on one machine
— the same failure mode A-02 already had to work around for the pre-195 side.

Output: five completed curve-cell out-dirs, one persona out-dir tree, all committed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/199-bot-re-calibration-sweep-strength-curve-refit/199-CONTEXT.md
@.planning/phases/199-bot-re-calibration-sweep-strength-curve-refit/199-RESEARCH.md
@.planning/phases/199-bot-re-calibration-sweep-strength-curve-refit/199-03-SUMMARY.md
@.planning/phases/199-bot-re-calibration-sweep-strength-curve-refit/199-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Curve sweep reaches clean completion in all five cells</name>
  <precondition>
    The five supervisors launched in 199-04 task 3 are still running or have exited, and their PIDs
    were recorded in that plan's summary.
  </precondition>
  <read_first>
    - `reports/bot-parity-199/runbook.md` — the observation and crash-recovery sections.
    - `bin/preset-supervisor.sh` lines 44-91 — the completion check, the fast-crash guard and the
      watch loop, so a genuine abort is distinguishable from a normal wasm-fault resume.
    - This plan's `<must_haves>` — the completion signal and the expected-absence-of-aggregate note.
  </read_first>
  <what-built>
    Plan 04 launched five supervised, pinned-bracket cells. This checkpoint is the wait: the run is
    multi-hour and the supervisor handles crashes on its own. Plan 05 is designed to proceed in
    parallel with this wait, so the executor should not idle here.
  </what-built>
  <how-to-verify>
    Periodically check each of the five out-dirs for the appearance of a per-cell aggregate file. That
    file is written once, at the very end, from memory — so it is the completion signal and its
    absence mid-run means nothing is wrong. The supervisor logs its own completion line when it sees
    it and exits zero.

    While waiting, confirm the run is healthy rather than merely alive: each raw ledger's row count is
    growing, and each supervisor log shows either no crash lines or crash-then-resume pairs rather
    than a fast-crash abort.

    If a supervisor logs its abort message after repeated sub-threshold crashes, that is a real defect
    and not the slow wasm leak — read that cell's run log and diagnose before relaunching. Do not
    relaunch blindly.

    When all five aggregates exist, record for each cell: the total games in its raw ledger, the
    distinct anchors, the wall clock from first to last row, and whether any crash-resume occurred.
  </how-to-verify>
  <acceptance_criteria>
    - Exactly five per-cell aggregate files exist, one in each of the five out-dirs, and all five
      supervisor processes have exited zero.
    - Each cell's raw ledger holds 96 completed game rows.
    - For each cell, the distinct values of the ledger's `anchor` column are exactly that cell's
      pinned four anchors from 199-04-PLAN.md's `<pinned_brackets>` table — no more, no fewer. A fifth
      anchor means the run re-bracketed and the comparison is invalid.
    - Every ledger row's `seed` column is 1, and every row's `git_sha` is the same value across all
      five cells.
    - Every ledger row has a positive integer `elapsed_ms` and a `mean_move_ms` that parses as a
      positive float.
    - If any crash-resume occurred, the affected cell's ledger has no duplicate `(pass, bot_elo,
      bot_blend, anchor, game_index)` tuple — resume replayed rather than re-played. Record the crash
      count and the observed loss in the plan summary as the RECAL-04 evidence.
    - The measured per-cell wall clock is recorded for each cell, for the D-08 comparison in plan 07.
  </acceptance_criteria>
  <verify>
    <human-check>Five per-cell aggregates exist, each ledger holds 96 rows on exactly its four pinned anchors, and any crash-resume lost at most the in-flight game.</human-check>
  </verify>
  <resume-signal>Type "complete" once all five aggregates exist, or describe the failure</resume-signal>
  <done>
    The curve sweep is finished and its per-cell aggregates exist.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: The two persona spot-checks, fit suppressed, fresh tree</name>
  <precondition>
    No `calibration-harness.mjs` process is live. The persona runbook refuses to launch while one is,
    so task 1 must have fully completed first.
  </precondition>
  <read_first>
    - `bin/run_persona_calibration_sweep.sh` — the personas-subset flag, the no-fit flag, the
      duplicate-run guard at lines 87-94, and the `PERSONA_SWEEP_DATA_DIR` override added in plan 03.
    - `reports/data/persona-calibration.json` — confirms `attacker-1600` is Light with blend 0.05 and
      botElo 1900, and `wall-1800` is Deep with blend 0.5 and botElo 2300. This is the per-persona
      source of truth; do not derive a persona's blend from `RUNG_BLEND`, which is a canonical default
      that rung-1600 personas override inline (A-03).
    - `frontend/src/lib/personas/personaRegistry.ts` lines 107-121 and line 358 — the inline-override
      mechanism and `grinder-1600`'s explicit Deep setting, which is why D-06's original pair was
      replaced by A-01's.
    - 199-03-SUMMARY.md — the exact name and semantics of the data-directory override.
  </read_first>
  <what-built>
    Plan 03 added the data-directory override that lets this pass write somewhere fresh. Without it
    the supervisor would find the 2026-07-22/23 aggregates already sitting in
    `reports/data/persona-sweep-attacker-1600` and `reports/data/persona-sweep-wall-1800`, treat both
    personas as already done, and exit zero having played zero games.
  </what-built>
  <how-to-verify>
    Run `bin/run_persona_calibration_sweep.sh` with the personas subset restricted to exactly
    `attacker-1600` and `wall-1800`, the fit step suppressed via the script's existing no-fit flag,
    and the data directory pointed at `reports/data/sweep-199-personas`. Use the script rather than
    hand-rolling the invocation: it dumps the persona tuples from the JS source of truth, threads the
    style bundle through the environment, gives each persona its own out-dir, and runs each under the
    crash supervisor.

    Unlike the curve cells these are deliberately NOT bracket-pinned. The persona baseline in
    `reports/data/persona-calibration.json` was itself produced by the auto-locate schedule, so
    running them the same way now is more consistent with how the comparison target was produced than
    pinning would be. This asymmetry is intentional and must be stated in the report.

    Confirm before launching that no harness process is live, and confirm after launching that the two
    out-dirs appear under the fresh tree and not under the pre-195 paths.
  </how-to-verify>
  <acceptance_criteria>
    - Two persona out-dirs exist under `reports/data/sweep-199-personas`, one for `attacker-1600` and
      one for `wall-1800`, each holding a raw ledger and a per-cell aggregate.
    - Each persona's ledger has a positive game count — a zero-game "completed" run means the skip
      path fired and the data-directory override did not take effect.
    - `attacker-1600`'s ledger rows carry `bot_elo` 1900 and `bot_blend` 0.05; `wall-1800`'s carry
      `bot_elo` 2300 and `bot_blend` 0.5. Both are blend>0, satisfying D-06's hard constraint.
    - Every persona ledger row has a positive integer `elapsed_ms` and a positive `mean_move_ms`.
    - `git status --porcelain reports/data/persona-calibration.json frontend/src/generated/personaCalibration.ts`
      is empty — the fit step did not run.
    - The pre-195 directories are untouched: the file lists, byte sizes and mtimes under
      `reports/data/persona-sweep-attacker-1600` and `reports/data/persona-sweep-wall-1800` match
      their pre-task values.
    - The plan summary records that these two rows twin curve cells 3 and 5 at identical botElo and
      blend, so they are a within-run paired style comparison and add no new ELO coverage.
  </acceptance_criteria>
  <verify>
    <human-check>Two persona out-dirs under the fresh tree hold real games at the expected botElo and blend, and no persona calibration artifact changed.</human-check>
  </verify>
  <resume-signal>Type "complete" once both persona aggregates exist, or describe the failure</resume-signal>
  <reversibility rating="costly">
    Re-running the persona pass costs hours. It is not destructive — the pre-195 directories are
    untouched and the fresh tree is separate — but the operator time is not recoverable.
  </reversibility>
  <done>
    Both persona spot-checks are measured, with the persona calibration artifacts untouched.
  </done>
</task>

<task type="auto">
  <name>Task 3: Force-add and commit the measurement evidence</name>
  <files>reports/data/sweep-199-human1100/, reports/data/sweep-199-light1300/, reports/data/sweep-199-light1900/, reports/data/sweep-199-deep1500/, reports/data/sweep-199-deep2300/, reports/data/sweep-199-personas/</files>
  <read_first>
    - `.gitignore` lines 237-238 — the two patterns that exclude every one of this phase's out-dirs.
    - `git ls-files reports/data/sweep-human/` — the Phase-180 precedent: exactly three TSVs per
      out-dir were force-added, and the run log and supervisor log were not.
    - This plan's prohibitions.
  </read_first>
  <action>
    The out-dirs created by plans 04 and 06 all match the gitignore patterns, so a plain add is a
    no-op and the evidence would stay untracked. Force-add exactly the TSV artifacts from each of the
    five curve-cell out-dirs and both persona out-dirs: the raw per-game ledger, the per-cell
    aggregate, and the summary TSV if the harness wrote one. This mirrors what Phase 180 force-added
    under the earlier sweep directories.

    Do not force-add the run logs or supervisor logs. They are large, they are stdout scratch by
    convention, and the durable per-game timing they used to be the only source of now lives in the
    ledger's own columns — which is precisely what D-08 bought.

    Add a short provenance note alongside the commit message recording the git sha the sweep ran
    under (read it from any ledger row's `git_sha` column, which is uniform across all five cells),
    the total games, and whether any crash-resume occurred.

    Verify before committing that no shipping calibration artifact is in the staged set.
  </action>
  <verify>
    <automated>git ls-files reports/data/sweep-199-\* | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files 'reports/data/sweep-199-*'` lists the raw ledger and the per-cell aggregate for
      each of the five curve cells and both personas — at least 14 tracked TSV paths.
    - `git ls-files 'reports/data/sweep-199-*'` includes no path ending in `.log` and no `current.pid`.
    - `git status --porcelain` is clean for the phase's out-dirs after the commit.
    - The staged diff contains none of `reports/data/bot-strength-lookup.json`,
      `frontend/src/generated/botStrengthCurves.ts`, `reports/data/persona-calibration.json`,
      `frontend/src/generated/personaCalibration.ts`.
    - `git status --porcelain frontend/` is empty.
    - The commit message records the sweep's `git_sha`, the total game count, and the crash-resume
      count.
    - Sanity check on the force-add itself: `git check-ignore -v` still reports the out-dir pattern as
      matching, confirming the files are tracked despite the ignore rule rather than because the rule
      was weakened. `.gitignore` must be unmodified.
  </acceptance_criteria>
  <reversibility rating="reversible">
    Tracked data files; removable with a follow-up commit if the layout is later reconsidered.
  </reversibility>
  <done>
    The phase's measurement evidence is committed and auditable rather than living untracked on one
    machine.
  </done>
</task>

</tasks>

<threat_model>
Assessed for the whole phase in 199-01-PLAN.md's `<threat_model>`. Live in this plan: T-199-02
(tampering via a mismatched anchor pool on resume) — mitigated and observed by task 1's criterion
that each cell's ledger carries exactly its four pinned anchors and no duplicate game tuples after a
resume; and T-199-03 (repository hygiene on committed artifacts) — mitigated by force-adding only the
TSVs and never the logs.
</threat_model>

<verification>
- Five curve-cell aggregates and two persona aggregates exist.
- Each curve cell holds 96 rows on exactly its four pinned anchors, seed 1, uniform git sha.
- Timing columns are populated on every row of every ledger.
- The persona pass ran with the fit step suppressed and against a fresh tree; the two pre-195 persona
  directories are untouched.
- All ledgers and aggregates are tracked by git; no log file is.
- `.gitignore` is unmodified and the four shipping calibration artifacts are unchanged.
</verification>

<success_criteria>
The measurement is complete, crash-resilient behaviour was observed rather than assumed, the persona
spot-checks add a paired style comparison without touching persona labels, and every artifact the
verdict rests on is committed.
</success_criteria>

<output>
Create `.planning/phases/199-bot-re-calibration-sweep-strength-curve-refit/199-06-SUMMARY.md` when done.
</output>
