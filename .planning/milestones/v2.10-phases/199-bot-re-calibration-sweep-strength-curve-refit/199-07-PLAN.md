---
phase: 199-bot-re-calibration-sweep-strength-curve-refit
plan: 07
type: execute
wave: 4
depends_on: ["199-05", "199-06"]
files_modified:
  - reports/data/bot-parity-199-verdict.json
  - reports/bot-parity-199/report.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - CHANGELOG.md
autonomous: false
requirements: [RECAL-01, RECAL-02, RECAL-03, RECAL-04, RECAL-05]
must_haves:
  truths:
    - "The verdict is computed by scripts/calibration_parity_verdict.py using the thresholds committed before the run, with no edit to those constants after data existed (D-03)."
    - "The null-control validity gate is reported first; if it fails the verdict is VOID and no parity claim is made about the exposed cells."
    - "The Maia-family and SF-family pooled shifts are reported as two separate numbers against two separate thresholds and are never merged (D-03)."
    - "On parity failure the phase STOPS AND REPORTS: it delivers the measurement plus a written recommendation, and does not revert the ladder or escalate to a refit (D-04)."
    - "The report attributes the measured delta, or its absence, to Phase 195's grading ladder alone, with the reason each other v2.10 phase is excluded (D-09, RECAL-05)."
    - "The report carries the three stated fidelity limits: SEED-130 browser/harness Stockfish-hash divergence, the pooled resolution limit, and blend-0 immunity leaving 16 of 24 personas unmeasured (D-09)."
    - "The report records that the named revert target restores neither the calibration nor the measured configuration, so it is not a safe undo (D-10)."
    - "The report records the 8-exposed / 16-immune persona split as derived per-persona from persona-calibration.json, never via RUNG_BLEND (A-03)."
    - "RECAL-01..05 are rewritten in .planning/REQUIREMENTS.md to the re-scoped boundary, and the ROADMAP's Phase 199 block is corrected, so the stale three-change premise stops propagating."
    - "The four shipping calibration artifacts are byte-identical to their pre-phase state, which is how RECAL-02's CI drift criterion is satisfied when parity holds."
    - statement: "If the achieved per-cell CI widths come back materially wider than the old run's, the report states that the achieved resolution was worse than the pre-registered one, rather than silently reporting against the pre-registered number."
      verification: backstop
    - statement: "The before/after timing comparison accounts for the locate-pass games present in the pre-195 baseline but removed from the new run by D-02, so the ladder's contribution is not overstated."
      verification: backstop
  artifacts:
    - reports/data/bot-parity-199-verdict.json
    - reports/bot-parity-199/report.md
  key_links:
    - "scripts/calibration_parity_verdict.py consumes the five per-cell aggregates plus reports/data/bot-curves-internal-scale.json and emits the verdict JSON the report narrates."
    - "reports/data/bot-parity-199-timing-baseline.json (plan 05) is the before side; the new ledgers' elapsed_ms and mean_move_ms columns are the after side."
    - "reports/grading-ladder/report.md forward-references this phase as the strength gate; that loop must be closed and its stale combined-sweep framing corrected."
  prohibitions:
    - "Do NOT edit the threshold constants in scripts/calibration_parity_verdict.py or reports/bot-parity-199/accept-rule.md — they were pre-registered before the run and are not editable now (D-03)."
    - "Do NOT refit or regenerate reports/data/bot-strength-lookup.json or frontend/src/generated/botStrengthCurves.ts, whatever the verdict."
    - "Do NOT relabel personas or regenerate reports/data/persona-calibration.json or frontend/src/generated/personaCalibration.ts."
    - "Do NOT revert the ladder: frontend/src/lib/engine/gradingLadder.ts must be unmodified, with no restoration of the flat-14 table and no reinstatement of the movetime safety cap."
    - "Do NOT open any work on SEED-129, SEED-130 or SEED-114; all three stay dormant."
    - "Do NOT re-run or re-bracket any cell to chase a better number after seeing the verdict."
---

<objective>
Turn the measurement into a decision-grade written record: compute the verdict against the
pre-registered threshold, state what it does and does not license, attribute it to the one strength
change that actually shipped, and stop the stale three-change premise from propagating any further.

Purpose: this phase is a single-change gate, which is what makes attribution possible at all. That is
worth saying explicitly, because the ROADMAP's framing — a combined sweep that forfeits per-change
attribution — is exactly backwards for what actually shipped.

Output: `reports/data/bot-parity-199-verdict.json`, `reports/bot-parity-199/report.md`, corrected
requirement and roadmap text, and a changelog entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/199-bot-re-calibration-sweep-strength-curve-refit/199-CONTEXT.md
@.planning/phases/199-bot-re-calibration-sweep-strength-curve-refit/199-RESEARCH.md
@.planning/phases/199-bot-re-calibration-sweep-strength-curve-refit/199-PATTERNS.md
@.planning/phases/199-bot-re-calibration-sweep-strength-curve-refit/199-02-SUMMARY.md
@.planning/phases/199-bot-re-calibration-sweep-strength-curve-refit/199-05-SUMMARY.md
@.planning/phases/199-bot-re-calibration-sweep-strength-curve-refit/199-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compute the parity verdict against the pre-registered threshold</name>
  <files>reports/data/bot-parity-199-verdict.json</files>
  <read_first>
    - `scripts/calibration_parity_verdict.py` — the five pre-registered constants and the CLI it
      exposes. Read it to invoke it correctly; do not modify it.
    - `reports/bot-parity-199/accept-rule.md` — the contract being applied, in evaluation order.
    - `reports/data/bot-curves-internal-scale.json` — the committed comparison target.
    - The five per-cell aggregates committed by 199-06 task 3, and 199-06-SUMMARY.md for the
      per-cell game counts, anchors and crash-resume record.
  </read_first>
  <action>
    Run `scripts/calibration_parity_verdict.py` over the five new per-cell aggregates against the
    committed internal-scale JSON, writing `reports/data/bot-parity-199-verdict.json`. Do not touch
    the threshold constants; they were pre-registered before the run and are not editable now.

    Read the emitted verdict and record, in the plan summary, in this order: the null-control gate
    outcome per family; the pooled shift, pooled standard error and threshold comparison per family
    over the four exposed cells; the per-cell shape-guard outcome; and the single overall verdict —
    void, holds, or fails.

    Independently sanity-check two things rather than trusting the script blindly. First, recompute
    one cell's standard error by hand from its emitted confidence interval using the same
    width-over-twice-the-z formula and confirm it matches. Second, confirm the Maia and SF numbers
    differ from each other — an identical pair would suggest the families were merged somewhere, which
    the never-merge discipline forbids.

    Also compute the achieved resolution from the NEW run's own bootstrap confidence intervals, using
    the same pooling formula, and compare it to the pre-registered resolution. If the achieved
    resolution is materially worse, that fact goes in the report as a stated limitation — the
    threshold still stands, but the claim it supports is weaker than planned.

    Then compute the D-08 timing comparison: per cell, the new mean per-game elapsed and mean
    per-bot-move from the committed ledgers' timing columns, against the corresponding entries in
    `reports/data/bot-parity-199-timing-baseline.json`. Account explicitly for the locate-pass games
    present in the baseline but removed from the new run by D-02 — either by restricting the baseline
    to its measure-pass rows or by stating the inflation and its direction. Compare the resulting
    per-move ratio to the fixture-level claim of 1.35x wall clock and a 61.4% cut in grade CPU, which
    is the first game-level test that claim has ever had.

    If the overall verdict is fails or void, stop here and do not proceed to any remediation: D-04
    makes this a stop-and-report phase, and the revert-versus-refit call is a separate operator
    decision taken with this data in hand. Record the recommendation in the report, not in code.
  </action>
  <verify>
    <automated>uv run python scripts/calibration_parity_verdict.py --self-test</automated>
  </verify>
  <acceptance_criteria>
    - `reports/data/bot-parity-199-verdict.json` exists, is valid JSON, and is committed.
    - It carries separate `maia` and `sf` blocks whose pooled shift values are not equal to each other.
    - It carries the null-control gate outcome, and if that gate failed the overall verdict is void
      and no holds-or-fails claim about the exposed cells appears anywhere in the output.
    - It carries a per-cell shape-guard result for each of the four exposed cells, and the guard is
      recorded as fired only where the excursion is outside the committed interval in BOTH families.
    - `git diff HEAD -- scripts/calibration_parity_verdict.py reports/bot-parity-199/accept-rule.md`
      is empty — the pre-registration was not edited after seeing data. Confirm the same by checking
      that both files' most recent commits predate the first `reports/data/sweep-199-*` commit.
    - The hand-recomputed standard error for one cell matches the script's emitted value to within
      floating-point tolerance.
    - The achieved-resolution figure is computed and recorded alongside the pre-registered one.
    - The timing comparison is computed per cell with the locate-pass adjustment applied or its
      direction stated.
    - `git status --porcelain reports/data/bot-strength-lookup.json frontend/src/generated/botStrengthCurves.ts reports/data/persona-calibration.json frontend/src/generated/personaCalibration.ts frontend/src/lib/engine/gradingLadder.ts`
      is empty.
  </acceptance_criteria>
  <done>
    The verdict exists as a committed JSON artifact, computed against an unedited pre-registration,
    with the two families separate and the timing comparison done.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the report — verdict, timing, attribution, limits</name>
  <files>reports/bot-parity-199/report.md</files>
  <read_first>
    - `reports/grading-ladder/report.md` in full — the sibling report to mirror: header block naming
      the phase, date and contract; a headline that leads with the number and puts a table immediately
      after rather than prose; an explicit "this is not X" framing directly under the headline; a
      provenance table of every artifact feeding the report; middle sections keyed by decision ID; and
      a closing limits section of specific named limits. Note also its own forward-reference to this
      phase, and its stale "combined recalibration sweep" phrasing, both of which this report must
      close and correct.
    - `reports/data/bot-parity-199-verdict.json` and `reports/data/bot-parity-199-timing-baseline.json`.
    - `.planning/phases/199-bot-re-calibration-sweep-strength-curve-refit/199-CONTEXT.md` decisions
      D-04, D-07, D-09, D-10 and amendments A-01, A-02, A-03 — the source of the attribution and
      limits sections, several of which are near-pre-written there.
    - `.planning/seeds/SEED-130-browser-grade-nondeterminism-uncleared-stockfish-hash.md` — the first
      fidelity limit.
    - `frontend/src/lib/engine/gradingLadder.ts` lines 95-120 — the revert-target language D-10
      corrects.
  </read_first>
  <action>
    Create `reports/bot-parity-199/report.md` following `reports/grading-ladder/report.md`'s structure.

    Header block: phase, date, and the contract pointer to `reports/bot-parity-199/accept-rule.md`.

    Headline: the overall verdict in one or two sentences with the actual numbers, then immediately a
    table with one row per cell carrying family, committed rating, new rating, shift, and the
    per-family pooled row with its threshold and outcome. Directly under it, the explicit framing:
    this is a parity check, not a refit, and the supportable claim is bounded by the pre-registered
    resolution rather than being a claim of no change.

    Provenance section: a table of every artifact feeding the report with repository-relative paths —
    the five per-cell aggregates and their raw ledgers, the two persona aggregates,
    `reports/data/bot-curves-internal-scale.json`, `reports/data/bot-cells-sweep.tsv` as the source of
    the pinned brackets, `reports/data/bot-parity-199-timing-baseline.json`, and the verdict JSON.
    Include the exact command line used for each cell, copied from
    `reports/bot-parity-199/runbook.md`, plus the sweep's git sha.

    A parity verdict section keyed to D-03: the validity gate first, then the per-family pooled
    comparison, then the shape guard, then the achieved-versus-pre-registered resolution.

    A timing section keyed to D-08: the per-cell before and after, the locate-pass adjustment, and
    the comparison against the fixture-level 1.35x wall and 61.4% grade-CPU claim. State plainly that
    this is the first game-level evidence for that claim and whether it holds up at game scale.

    A persona section: the two spot-checks paired against their twin curve cells at identical botElo
    and blend, framed as a within-run style-times-ladder probe. State explicitly that they add no new
    ELO coverage, since every rung-1600/1800 persona collides with a curve cell by construction, and
    that the persona pass deliberately auto-located its bracket rather than pinning — matching how the
    persona baseline was produced, unlike the curve cells whose comparison target came from a known
    bracket. That asymmetry is a legitimate methodological difference, not an oversight, provided it
    is stated.

    An attribution section keyed to D-09 and RECAL-05: the measured delta, or its absence, is
    attributable to Phase 195's grading ladder alone. Give the reason for each exclusion — Phase 194
    is bit-identical by requirement, Phase 196 touches only the analysis board and never bot play,
    Phase 197's leaf-value requirement was rejected and its mechanism stripped, Phase 198 was never
    built. State that this makes the phase a single-change gate, which is what makes attribution
    possible, and note that this is the opposite of the ROADMAP's combined-sweep framing.

    A closing limits section, one specific named limit per bullet, no vague caveats. Include: the
    SEED-130 divergence, since the harness clears the Stockfish hash before every grading call and the
    browser deliberately never does, so harness-measured strength is not bit-identical to shipped
    browser strength regardless of anything this sweep found; the resolution limit, both
    pre-registered and achieved, per family; the blend-0 immunity, stating that 16 of the 24 personas
    do no search at all and are structurally unreachable by the ladder, so this measurement says
    nothing about them — and stating that the 8-exposed split was derived per-persona from
    `reports/data/persona-calibration.json`, never from the canonical `RUNG_BLEND` default that
    rung-1600 personas override inline; the A-02 limit that the pre-195 timing side rests on
    gitignored local logs, so if those are lost the committed derived baseline is the only surviving
    record; and the P-02 limit that the new cells preserve the seed and opening-book discipline but do
    not replay the original run's exact game transcripts, because game indices restart per invocation,
    which is acceptable for a WDL-rate comparison but is not a byte-identical replay.

    Add a D-10 note: the ladder's own doc comment names a flat three-rung table as the revert target
    if this sweep regresses, but the committed curves and persona labels were measured against a flat
    depth-14 configuration plus a 2500 ms movetime safety cap — a third configuration distinct from
    both the live ladder and the named revert target. So that revert restores neither the calibration
    nor the measured configuration, and is not the safe undo the comment implies. If the verdict is
    fails, state the two honest options and record, per D-04, that the choice between them is a
    separate operator decision and not made here.

    Finally, close the loop `reports/grading-ladder/report.md` opened: it forward-references this
    phase as the strength gate and describes it as a combined recalibration sweep. Amend that phrase
    in place so a future reader following the cross-reference is not misled about what this phase
    actually was.
  </action>
  <verify>
    <automated>test -f reports/bot-parity-199/report.md && grep -q 'SEED-130' reports/bot-parity-199/report.md && grep -q 'accept-rule.md' reports/bot-parity-199/report.md</automated>
  </verify>
  <acceptance_criteria>
    - `reports/bot-parity-199/report.md` exists and is committed.
    - The headline states the overall verdict with numbers and is followed immediately by a table, not
      by prose.
    - The report contains a provenance table listing at least: the five per-cell aggregates, the two
      persona aggregates, `reports/data/bot-curves-internal-scale.json`,
      `reports/data/bot-cells-sweep.tsv`, `reports/data/bot-parity-199-timing-baseline.json`, and
      `reports/data/bot-parity-199-verdict.json`, all as repository-relative paths, plus the five
      exact cell command lines and the sweep's git sha.
    - The Maia and SF pooled results appear as two separate rows or columns with two separate
      thresholds; no single merged parity number appears anywhere.
    - The limits section has at least five bullets covering SEED-130, the resolution limit, the
      blend-0 sixteen-persona immunity, the A-02 local-logs limit, and the P-02 non-replay limit.
    - The attribution section names all four excluded phases with a reason each.
    - The D-10 note is present and states that the named revert target restores neither the
      calibration nor the measured configuration.
    - The persona section states that the spot-checks add no new ELO coverage and explains the
      pinned-versus-auto-located asymmetry.
    - `reports/grading-ladder/report.md`'s forward-reference is amended, and
      `git diff HEAD -- reports/grading-ladder/report.md` shows a change confined to that phrase and
      its immediate sentence — the Phase 195 findings themselves are untouched.
    - No file under `frontend/` is modified.
  </acceptance_criteria>
  <done>
    A decision-grade report exists whose verdict, timing result, attribution and limits are all
    explicit, with the Phase 195 cross-reference corrected.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write the re-scoped requirements back and correct the roadmap</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, CHANGELOG.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` around lines 93-97 — the stale RECAL-01..05 block under the "Bot
      Re-Calibration (combined, final)" heading.
    - `.planning/ROADMAP.md` — the Phase 199 block, its goal paragraph, its depends-on line and its
      five success criteria, plus the milestone-intro paragraph whose closing clause describes Phase
      199 as one combined calibration sweep covering three phases' strength changes.
    - `199-01-PLAN.md`'s `<requirements_rescope>` section — the authoritative rewritten text for all
      five IDs, already settled. Transcribe it; do not re-derive it.
    - `CLAUDE.md` "Changelog & Releases" — the per-phase changelog rule and its carve-out for tooling
      work with no user-facing behaviour change.
  </read_first>
  <action>
    In `.planning/REQUIREMENTS.md`, replace the five stale RECAL bullets with the re-scoped text from
    `199-01-PLAN.md`'s `<requirements_rescope>` section, keeping the same five IDs and the same
    checkbox format the surrounding requirements use. Rename the section heading so it no longer
    claims to be a combined sweep. Add a one-line note above the block recording that the text was
    re-scoped during Phase 199 planning on the grounds that only one of the three anticipated strength
    changes shipped, and pointing at the phase CONTEXT for the full reasoning. Use a scoped edit that
    touches only this block — do not rewrite the file.

    In `.planning/ROADMAP.md`, correct the Phase 199 block: replace the goal paragraph with the parity
    check plus timing measurement it actually was, replace the depends-on line so it names only the
    phase that actually changed strength, and replace the five success criteria with ones matching the
    re-scoped requirements, marking the two conditional ones as conditional and untriggered if parity
    held. Update the plans list to the seven plans of this phase with their one-line objectives, and
    set the plan count. Also amend the milestone-intro paragraph's closing clause, which still
    describes Phase 199 as absorbing three phases' strength changes and forfeiting per-change
    attribution — the opposite of what happened. Use scoped edits throughout; do not rewrite the
    ROADMAP, which would destroy phase entries outside the edit window.

    In `CHANGELOG.md`, add a bullet under the unreleased section. Per the project's changelog rule
    this phase produces no user-facing behaviour change — it is a measurement plus a written verdict —
    so a Tests-flavoured entry is the honest classification. Record that bot strength was re-measured
    against the shipped grading ladder and the outcome, and reference the phase. Keep it terse and
    avoid em-dashes.

    Do not run any generator and do not touch any of the four shipping calibration artifacts.
  </action>
  <verify>
    <automated>grep -c 'RECAL-0' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - All five IDs RECAL-01 through RECAL-05 are still present in `.planning/REQUIREMENTS.md`; none
      was dropped or renumbered.
    - None of the five rewritten bullets describes a combined sweep of three strength changes, a full
      15-cell sweep, or a 24-persona relabel as in-scope work.
    - RECAL-02 and RECAL-03 are each explicitly marked conditional on parity failing and on a separate
      authorized decision.
    - RECAL-03's exposed surface is stated as 8 personas with the per-persona derivation basis, not 24.
    - RECAL-05 is stated as an attribution statement naming Phase 195's ladder alone, with the three
      fidelity limits.
    - `git diff HEAD -- .planning/REQUIREMENTS.md` shows changes confined to the RECAL block and its
      heading; no other requirement group is touched.
    - `git diff HEAD -- .planning/ROADMAP.md` shows changes confined to the Phase 199 block and the
      milestone-intro clause; the diff contains no deletion of any other phase heading. Verify by
      confirming the count of lines matching the phase-heading pattern is unchanged before and after.
    - The ROADMAP's Phase 199 plans list names seven plans with their objectives and the plan count
      matches.
    - `CHANGELOG.md` has exactly one new bullet under the unreleased section referencing this phase.
    - `git status --porcelain reports/data/bot-strength-lookup.json frontend/src/generated/botStrengthCurves.ts reports/data/persona-calibration.json frontend/src/generated/personaCalibration.ts frontend/src/lib/engine/gradingLadder.ts`
      is empty.
  </acceptance_criteria>
  <reversibility rating="reversible">
    Planning-document edits, revertable by a follow-up commit; no code or generated artifact is
    affected.
  </reversibility>
  <done>
    The dead three-change premise no longer exists in any planning document, and all five requirement
    IDs carry text matching what the phase actually delivered.
  </done>
</task>

</tasks>

<threat_model>
Assessed for the whole phase in 199-01-PLAN.md's `<threat_model>`: offline research and measurement
tooling only, no web tier, no user input surface, no auth or crypto concern, no packages introduced.
This plan writes only documents and a derived JSON, so no register row is live in it. The applicable
ASVS category remains V5 Input Validation, already covered by the harness's pre-existing fail-loud
CLI parsing and unchanged by this phase.
</threat_model>

<verification>
- `reports/data/bot-parity-199-verdict.json` and `reports/bot-parity-199/report.md` are committed.
- `git diff` against the pre-run commit shows no change to
  `scripts/calibration_parity_verdict.py`'s constants or to
  `reports/bot-parity-199/accept-rule.md`.
- All five RECAL IDs are present in `.planning/REQUIREMENTS.md` with re-scoped text.
- `git status --porcelain frontend/` is empty; in particular
  `frontend/src/lib/engine/gradingLadder.ts` is unmodified.
- The four shipping calibration artifacts are byte-identical to their pre-phase state — which is
  exactly how RECAL-02's drift criterion is satisfied when parity holds. Confirm with
  `git diff <pre-phase-sha> -- reports/data/bot-strength-lookup.json frontend/src/generated/botStrengthCurves.ts reports/data/persona-calibration.json frontend/src/generated/personaCalibration.ts`
  returning empty.
- No seed file under `.planning/seeds/` was modified — SEED-129, SEED-130 and SEED-114 stay dormant.
</verification>

<success_criteria>
A parity verdict computed against an unedited pre-registration, a report that states what the numbers
do and do not license and attributes them to the single strength change that shipped, and planning
documents that no longer carry the dead three-change premise.
</success_criteria>

<output>
Create `.planning/phases/199-bot-re-calibration-sweep-strength-curve-refit/199-07-SUMMARY.md` when done.
</output>
