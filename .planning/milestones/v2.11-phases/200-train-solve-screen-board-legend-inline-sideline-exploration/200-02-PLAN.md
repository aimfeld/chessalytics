---
phase: 200-train-solve-screen-board-legend-inline-sideline-exploration
plan: 02
type: execute
wave: 2
depends_on: [200-01]
files_modified:
  - frontend/src/lib/trainArrows.ts
  - frontend/src/lib/__tests__/trainArrows.test.ts
  - frontend/src/components/train/TrainReveal.tsx
  - frontend/src/components/train/TrainSolveScreen.tsx
  - frontend/src/components/train/__tests__/TrainReveal.test.tsx
  - frontend/src/components/train/__tests__/TrainSolveScreen.test.tsx
autonomous: true
requirements: [LEGEND-01, LEGEND-03, LEGEND-04, LEGEND-05, LEGEND-06]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "No arrow, badge, step highlight, or header glyph anywhere on the Train reveal surface renders the inaccuracy tier's yellow — a played inaccuracy shows the green arrow, the good thumbs-up badge, and the green step highlight."
    - "toDisplayQuality maps 'inaccuracy' to 'good' and is the identity for 'best', 'good', 'mistake' and 'blunder'."
    - "The CardHeader's MoveQualityIcon is fed the display quality, so a played inaccuracy shows the good glyph and never the severity glyph."
    - "TrainRevealOverlay.alsoFineMoves contains exactly the fine moves the same builder call actually pushed as green arrows — never a move skipped as the best or played move, and never an entry beyond the puzzle-type arrow cap."
    - "On a sharp puzzle (arrow cap 1) alsoFineMoves is empty and the Also fine row does not render at all."
    - "The Also fine row is a compact SAN-token row with one green glyph, no stepper and no eval badge, and every label in it is text-sm or larger."
    - "Hovering, focusing, or tapping the Also fine glyph spotlights ALL of that row's drawn green arrows together, and the row carries the same active-entry highlight the line-box Cards do."
    - "applyTrainSpotlight carries alsoFineMoves through unchanged — filtering the board never empties the sidebar row."
    - "The line eval badge still discloses the eval drop for a move whose quality was collapsed from inaccuracy to good."
  artifacts:
    - "toDisplayQuality exported from frontend/src/lib/trainArrows.ts"
    - "alsoFineMoves field on the TrainRevealOverlay interface"
    - "data-testid train-reveal-also-fine and train-reveal-glyph-also-fine in TrainReveal.tsx"
  key_links:
    - "buildTrainRevealOverlay's cappedFineMoves loop pushes to arrows and alsoFineMoves in the SAME iteration, so the sidebar row and the board arrows cannot drift"
    - "toDisplayQuality is consumed by markerForQuality inside the pure builder AND by TrainReveal's CardHeader MoveQualityIcon — one collapse rule, two surfaces"
    - "TrainSolveScreen passes the UNFILTERED revealOverlay.alsoFineMoves to TrainReveal; the spotlight filters the board only"
  prohibitions:
    - "The inaccuracy-to-good collapse must not leak outside the Train reveal surface: MOVE_QUALITY_INACCURACY stays defined and in use in theme.ts, SEVERITY_GLYPH's inaccuracy entry stays unchanged, and the Analysis board, Move Stats table, and eval chart keep rendering inaccuracy as its own tier."
    - "The collapse is inaccuracy-to-good ONLY. Mistake and blunder keep their own arrow color, badge glyph, and step highlight on the reveal board, and the eval badge remains the disclosure channel for a collapsed inaccuracy — the surface must never flatter the user beyond the one accepted collapse."
---

<objective>
Remove yellow from the Train reveal surface and give the drawn green alternatives a
sidebar entry, so the legend is strictly 1:1 with the board.

Purpose: LEGEND-03 (inaccuracy renders as good everywhere on the reveal, including the
user's own played move per D-04, and including the CardHeader glyph the research found as
a fifth site CONTEXT.md does not name), LEGEND-04 (the compact "Also fine" row), LEGEND-05
(both changes land in the pure builder and are unit-tested).
Output: `toDisplayQuality`, a recolored `trainArrows.ts`, an `alsoFineMoves` field on
`TrainRevealOverlay`, and a spotlight-participating compact row in the reveal sidebar.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/200-train-solve-screen-board-legend-inline-sideline-exploration/200-CONTEXT.md
@.planning/phases/200-train-solve-screen-board-legend-inline-sideline-exploration/200-RESEARCH.md
@.planning/phases/200-train-solve-screen-board-legend-inline-sideline-exploration/200-PATTERNS.md
@.planning/phases/200-train-solve-screen-board-legend-inline-sideline-exploration/200-01-SUMMARY.md
</context>

## Artifacts this plan produces

- `toDisplayQuality(quality: TrainMoveQuality): TrainMoveQuality` — new export from `frontend/src/lib/trainArrows.ts`
- `TrainRevealOverlay.alsoFineMoves: TrainFineMove[]` — new interface field
- `TrainRevealProps.alsoFineMoves: TrainFineMove[]` — new prop
- `data-testid="train-reveal-also-fine"` — the compact row container
- `data-testid="train-reveal-glyph-also-fine"` — the row's green glyph button

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Collapse the inaccuracy tier into good across all five reveal sites</name>
  <files>frontend/src/lib/trainArrows.ts, frontend/src/lib/__tests__/trainArrows.test.ts, frontend/src/components/train/TrainReveal.tsx</files>
  <read_first>
    - `frontend/src/lib/trainArrows.ts` — the four sites D-05 names: `QUALITY_ARROW_COLOR` (L87-93), the fine-move arrow loop's color ternary (L269-279), `markerForQuality`'s severity branch (L185-189), and `TRAIN_STEP_HIGHLIGHT` (L100-106). Also the module docstring's colour-language paragraph (L6-13), which currently documents yellow and must be rewritten.
    - `frontend/src/lib/theme.ts` — `MOVE_QUALITY_GOOD` (L478), `MOVE_QUALITY_INACCURACY` (L479), `MOVE_HIGHLIGHT_GOOD` (L24), `MOVE_HIGHLIGHT_SQUARE` (L16).
    - `frontend/src/lib/arrowColor.ts` — `DARK_GREEN` (L30).
    - `frontend/src/components/icons/MoveQualityIcon.tsx` — `isSeverityQuality` routes `'inaccuracy'` to the yellow `SeverityQualityIcon`; this file itself must NOT change (it is shared with Move Stats and the rating chart).
    - `frontend/src/lib/severityGlyph.ts` — `SEVERITY_GLYPH`; must NOT change.
    - `frontend/src/components/train/TrainReveal.tsx` — the `CardHeader` `MoveQualityIcon` call added by plan 200-01 task 2, which is the fifth site.
    - `frontend/src/lib/__tests__/trainArrows.test.ts` — the existing inaccuracy expectations that must be inverted: L86-113 (yellow fine-move arrow + severity badge) and L296-300 (`TRAIN_STEP_HIGHLIGHT` yellow), plus the `MOVE_QUALITY_INACCURACY` / `MOVE_HIGHLIGHT_SQUARE` imports at L14-22.
  </read_first>
  <behavior>
    - `toDisplayQuality('inaccuracy')` returns `'good'`; `toDisplayQuality(q)` returns `q` for `'best'`, `'good'`, `'mistake'`, `'blunder'`.
    - A soft puzzle whose second fine move has `quality: 'inaccuracy'` produces that alternative's arrow in `DARK_GREEN`, identical to a clean alternative's arrow.
    - A played move classified `'inaccuracy'` produces an arrow in `MOVE_QUALITY_GOOD` and a marker of `{ square, good: true }` — no `severity` marker anywhere in the overlay.
    - A played move classified `'mistake'` still produces `MOVE_QUALITY_MISTAKE` and a `{ square, severity: 'mistake' }` marker; `'blunder'` likewise.
    - `TRAIN_STEP_HIGHLIGHT.inaccuracy` equals `TRAIN_STEP_HIGHLIGHT.good`; `TRAIN_STEP_HIGHLIGHT.mistake` and `.blunder` keep their own distinct values.
  </behavior>
  <action>
Apply D-05's presentation collapse at all five sites, with `toDisplayQuality` as the single rule.

In `frontend/src/lib/trainArrows.ts`, add `export function toDisplayQuality(quality: TrainMoveQuality): TrainMoveQuality` immediately after `classifyTrainMoveQuality` — it returns `'good'` for `'inaccuracy'` and the input unchanged otherwise. Document in its docstring that this is a PRESENTATION mapping only and that `classifyTrainMoveQuality` / `classifyLiveSeverity` are untouched, so the verdict and the eval numbers still know the difference.

Then change the four builder sites:
1. `QUALITY_ARROW_COLOR.inaccuracy` resolves to `MOVE_QUALITY_GOOD` (same value as the `good` entry).
2. In the `cappedFineMoves.forEach` loop, the pushed arrow's `color` is unconditionally `DARK_GREEN` — drop the `fine.quality === 'inaccuracy' ? ... : ...` ternary entirely.
3. `markerForQuality` collapses its argument through `toDisplayQuality` before branching, so an inaccuracy yields `{ square, good: true }` rather than `{ square, severity: 'inaccuracy' }`.
4. `TRAIN_STEP_HIGHLIGHT.inaccuracy` resolves to `MOVE_HIGHLIGHT_GOOD`.

Remove the now-unused `MOVE_QUALITY_INACCURACY` and `MOVE_HIGHLIGHT_SQUARE` imports from this module (lint and knip both fail on dead imports). Do NOT touch `frontend/src/lib/theme.ts`, `frontend/src/lib/severityGlyph.ts`, or `frontend/src/components/icons/MoveQualityIcon.tsx` — those constants and that icon are shared with the Analysis board, the Move Stats table, and the Moves-by-Rating chart, all of which must keep rendering the inaccuracy tier as its own distinct tier. Rewrite the module docstring's colour-language paragraph so it states the collapse and its accepted cost (the reveal board can no longer distinguish good from inaccuracy, by design; the line eval still discloses the drop).

The fifth site is in `frontend/src/components/train/TrainReveal.tsx`: feed the `CardHeader`'s `MoveQualityIcon` (and the `data-quality` attribute on its wrapping span) `toDisplayQuality(box.quality)` rather than the raw `box.quality`, guarding for `box.quality === null`. Without this, a played inaccuracy would show a green arrow beside a yellow severity glyph inside the same header — the exact mixed signal the recolor exists to remove.

In `frontend/src/lib/__tests__/trainArrows.test.ts`: invert the two existing yellow expectations (the fine-move-arrow case around L86-113 and the `TRAIN_STEP_HIGHLIGHT` case around L296-300) to assert the green treatment, drop the now-unused `MOVE_QUALITY_INACCURACY` / `MOVE_HIGHLIGHT_SQUARE` test imports, and add a `describe('toDisplayQuality')` block covering all five tiers. Add one case proving a played `'mistake'` and a played `'blunder'` still emit their own colors and `severity` markers, so a future over-collapse fails CI.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/__tests__/trainArrows.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/lib/__tests__/trainArrows.test.ts` exits 0.
    - `cd frontend && npx vitest run src/lib/__tests__/trainArrows.test.ts -t "toDisplayQuality"` reports at least 3 passing tests.
    - `cd frontend && grep -v '^\s*\*' src/lib/trainArrows.ts | grep -v '^\s*//' | grep -v '^/\*' | grep -c 'MOVE_QUALITY_INACCURACY\|MOVE_HIGHLIGHT_SQUARE' | tr -d '\n'` outputs `0` (comment lines are excluded, so only real code counts; the module references no yellow constant).
    - `cd frontend && grep -c "MOVE_QUALITY_INACCURACY" src/lib/theme.ts` outputs at least `1` — the constant survives for the rest of the app (prohibition 1).
    - `cd frontend && grep -rln "MOVE_QUALITY_INACCURACY" src | grep -v "src/lib/theme.ts" | grep -v "src/lib/trainArrows.ts" | wc -l` outputs at least `1` — at least one non-Train consumer still renders the yellow tier (prohibition 1).
    - `cd frontend && grep -c "isSeverityQuality" src/components/icons/MoveQualityIcon.tsx` outputs `2` and `cd frontend && grep -c "inaccuracy" src/lib/severityGlyph.ts` outputs at least `1` — the shared icon still routes inaccuracy to the severity glyph for its non-Train consumers (prohibition 1).
    - A test asserts a played mistake and a played blunder still emit their own arrow color and a `severity` marker (prohibition 2).
    - `cd frontend && npm run lint && npm run build` exits 0.
  </acceptance_criteria>
  <done>Yellow is gone from the reveal board, badges, step highlights, and the CardHeader glyph; mistake and blunder are untouched; the collapse has not leaked into any other page.</done>
  <reversibility rating="reversible">A mapping change inside one pure module plus one call-site wrap; `MOVE_QUALITY_INACCURACY` remains in `theme.ts` for the rest of the app (rating carried from CONTEXT.md D-05).</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derive alsoFineMoves in the builder and render the compact Also fine row</name>
  <files>frontend/src/lib/trainArrows.ts, frontend/src/lib/__tests__/trainArrows.test.ts, frontend/src/components/train/TrainReveal.tsx, frontend/src/components/train/TrainSolveScreen.tsx, frontend/src/components/train/__tests__/TrainReveal.test.tsx, frontend/src/components/train/__tests__/TrainSolveScreen.test.tsx</files>
  <read_first>
    - `frontend/src/lib/trainArrows.ts` — the `TrainRevealOverlay` interface (L152-155), the `!verdictLanded` early return (L218), the `goodMoveArrowCap` / `cappedFineMoves` slice (L172-174, L236-238), the `cappedFineMoves.forEach` push loop, and `applyTrainSpotlight` as plan 200-01 left it (it must now preserve the new field).
    - `frontend/src/components/train/TrainReveal.tsx` — `sanFromPlayedUci(fen, uci)` (L287-300) for the SAN tokens, the `TrainRevealProps` interface, the Card wrappers and glyph buttons from plan 200-01, and the panel root at L489.
    - `frontend/src/components/train/TrainSolveScreen.tsx` — the `revealOverlay` memo (L470-486) and the `<TrainReveal .../>` prop list (L777-797).
    - `frontend/src/lib/arrowColor.ts` — `DARK_GREEN` (L30), the Also-fine glyph's only legal color post-collapse.
    - `frontend/src/lib/__tests__/trainArrows.test.ts` — the soft/sharp/herring cap cases at L43-125 to extend with `alsoFineMoves` assertions.
    - CLAUDE.md Frontend section — `text-sm` is the floor; this row is primary content, not a tooltip body.
  </read_first>
  <behavior>
    - `buildTrainRevealOverlay(..., verdictLanded: false)` returns `alsoFineMoves: []` alongside the empty arrays.
    - A soft puzzle with fine moves `[best, altA, altB, altC]` yields exactly `[altA, altB]` in `alsoFineMoves` — the best move is skipped and the 4th entry is beyond the cap.
    - A fine move equal to the played move is skipped from `alsoFineMoves` as it is from the arrows.
    - A sharp puzzle yields `alsoFineMoves: []`.
    - `alsoFineMoves.length` always equals the number of arrows whose `layerKey` starts with `good-` in the same returned overlay.
    - `applyTrainSpotlight(overlay, ['e2e4'])` returns the same `alsoFineMoves` array the input overlay carried.
    - Component: with two drawn alternatives the sidebar renders one `train-reveal-also-fine` row listing both SANs; hovering its glyph reduces the board to exactly those two arrows.
  </behavior>
  <action>
Make the "Also fine" row a derivation of the arrow-selection logic rather than a re-implementation of it (D-03's 1:1 invariant).

In `frontend/src/lib/trainArrows.ts`: add `alsoFineMoves: TrainFineMove[]` to the `TrainRevealOverlay` interface. Initialise a local `alsoFineMoves: TrainFineMove[] = []` next to `arrows`/`markers`, and inside the SAME `cappedFineMoves.forEach` iteration that pushes a green arrow — after the `bestMoveUci` / `playedMove` skip and after the `squaresFromUci` null guard — push the `fine` object itself. Return it from both exit points: the `!verdictLanded` early return gets `alsoFineMoves: []`, and the normal return carries the array. Update `applyTrainSpotlight` to spread the input overlay (`return { ...overlay, arrows: ..., markers: ... }`) so the field survives filtering — the board filter must never empty the sidebar row.

In `frontend/src/components/train/TrainSolveScreen.tsx`: pass `alsoFineMoves={revealOverlay.alsoFineMoves}` to `<TrainReveal>`. Pass the UNFILTERED `revealOverlay`, not `spotlitOverlay` — the row is the legend and always renders in full; only the board is filtered.

In `frontend/src/components/train/TrainReveal.tsx`: add `alsoFineMoves?: TrainFineMove[]` (default `[]`) to `TrainRevealProps`, importing the `TrainFineMove` type from `@/lib/trainArrows`. Render the row immediately after the `lineBoxes.map(...)` block and before the SAN-only fallback, only when `alsoFineMoves.length > 0`. It is a compact row, NOT a Card (D-02): a single flex container with `data-testid="train-reveal-also-fine"`, `text-sm` type, holding the green glyph button (`<ArrowGlyphIcon color={DARK_GREEN} />` inside a `<button type="button" data-testid="train-reveal-glyph-also-fine" aria-label="Also fine moves">`), the literal label text, and the SAN tokens produced by mapping each entry's `uci` through `sanFromPlayedUci(puzzle.fen, uci)`, dropping nulls, and joining with a comma and a space. No stepper, no eval badge, no per-token click target.

Wire the row into the spotlight exactly like a line box: the entry is `{ key: 'train-reveal-also-fine', ucis: alsoFineMoves.map((f) => f.uci) }` — one glyph spotlights ALL of that row's drawn green arrows together, because the row has no per-token granularity. Attach `onPointerEnter`/`onPointerLeave` only when `isDesktop`, `onFocus`/`onBlur` + `tabIndex={0}` unconditionally, the glyph-button toggle with `stopPropagation`, and the same `data-spotlight` attribute + ring treatment the Cards use when `spotlightKey === 'train-reveal-also-fine'`.

Extend `trainArrows.test.ts` with `alsoFineMoves` assertions on the existing sharp / soft / herring / played-equals-alternative cases, plus one invariant case asserting `overlay.alsoFineMoves.length` equals the count of arrows whose `layerKey` starts with `good-`, and one asserting `applyTrainSpotlight` preserves the field. Extend `TrainSolveScreen.test.tsx` with a case that renders a soft puzzle with two drawn alternatives, asserts the row lists both SANs, and asserts hovering its glyph drops `data-arrows-count` to exactly 2.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/__tests__/trainArrows.test.ts src/components/train/__tests__/TrainReveal.test.tsx src/components/train/__tests__/TrainSolveScreen.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/lib/__tests__/trainArrows.test.ts src/components/train/__tests__/TrainReveal.test.tsx src/components/train/__tests__/TrainSolveScreen.test.tsx` exits 0.
    - A unit test asserts `overlay.alsoFineMoves.length === overlay.arrows.filter(a => a.layerKey?.startsWith('good-')).length` for a soft puzzle with an over-cap fine-move list (the 1:1 invariant).
    - A unit test asserts a sharp puzzle returns `alsoFineMoves: []`.
    - A unit test asserts `applyTrainSpotlight` returns the input overlay's `alsoFineMoves` array.
    - A component test asserts hovering `train-reveal-glyph-also-fine` reduces the mocked board's `data-arrows-count` to the number of drawn alternatives.
    - The Also fine row's container and its SAN tokens use `text-sm` or larger (source assertion; the row is primary content, not a tooltip body).
    - `cd frontend && npm run build` exits 0 (`TrainRevealOverlay` is a shared interface — tsc is the gate that proves every consumer was updated).
    - `cd frontend && npm run lint && npm run knip` exits 0.
  </acceptance_criteria>
  <done>The sidebar carries a compact green "Also fine" row listing exactly the alternatives drawn on the board, participating in the spotlight as one entry; the row's contents are derived from the overlay builder, not recomputed.</done>
  <reversibility rating="reversible">One additive interface field populated in an existing loop, plus one conditional render block.</reversibility>
</task>

</tasks>

## Flagged Planner Assumptions

- **A-05 (unclassified edge, LEGEND-03):** the probe returned "review manually". Reviewed:
  the substantive risk LEGEND-03 carries is the collapse leaking beyond the Train reveal
  (Analysis board, Move Stats, eval chart) or over-collapsing past inaccuracy. Both are
  authored as prohibitions in this plan's `must_haves.prohibitions` and gated in task 1's
  acceptance criteria. No residual sub-behavior identified.
- **A-06 (unclassified edge, LEGEND-04):** reviewed. LEGEND-04's substance is fully covered
  by the `alsoFineMoves` 1:1 invariant test and the sharp-puzzle empty case. One judgement
  call is recorded rather than hidden: RESEARCH assumption A1 — the row's single glyph
  spotlights ALL its drawn alternatives together, not one SAN token at a time. This follows
  D-02 ("no steppable lines") and LEGEND-04's singular "the green arrow glyph". If per-token
  granularity is later wanted, `applyTrainSpotlight`'s array signature already supports it.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine-derived SAN → DOM | The Also fine row renders SAN strings produced by `chess.js` from engine UCIs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-200-04 | Tampering (XSS) | The Also fine row's SAN token list in `TrainReveal.tsx` | low | mitigate | SAN strings are rendered as React children (auto-escaped); no `dangerouslySetInnerHTML` is introduced. Gated by grep below. |
| T-200-05 | Information Disclosure | The row surfacing answer-key moves | low | accept | The row renders only inside `TrainReveal`, which mounts behind the existing `showResultRow` / `verdict !== null` gate, and `alsoFineMoves` is empty until `verdictLanded` — no new pre-verdict surface. |
| T-200-06 | Repudiation | Collapsing inaccuracy to good misrepresenting the user's own result | low | mitigate | The collapse is presentation-only: `classifyTrainMoveQuality`, `classifyLiveSeverity`, and the `move_quality` value POSTed to `solvePuzzle` are untouched, and the eval badge still discloses the drop. Encoded as prohibition 2. |

Canon ASVS L1 items with no phase-specific surface: V2, V3, V4, V6 — no endpoint, no auth
path, no persisted state, no new network call.

**Additional gate:** `cd frontend && grep -rn "dangerouslySetInnerHTML" src/components/train | wc -l` outputs `0`.
</threat_model>

<verification>
- `cd frontend && npx vitest run src/lib/__tests__/trainArrows.test.ts src/components/train/__tests__/` exits 0.
- `cd frontend && npm run lint && npm run knip && npm run build` all exit 0.
- `cd frontend && npm test -- --run` exits 0 — the recolor touches a module read by
  `TrainSolveScreen`, so the whole frontend suite must stay green.
</verification>

<success_criteria>
- LEGEND-03: no yellow anywhere on the reveal board, badges, step highlights, or header
  glyphs, including for the user's own played inaccuracy; the eval badge still discloses
  the drop.
- LEGEND-04: a compact "Also fine" row lists exactly the drawn green alternatives, with one
  glyph that participates in the spotlight, no stepper and no eval.
- LEGEND-05: both the collapse and the row's derivation live in `trainArrows.ts` and fail
  CI on regression.
</success_criteria>

<output>
Create `.planning/phases/200-train-solve-screen-board-legend-inline-sideline-exploration/200-02-SUMMARY.md` when done
</output>
