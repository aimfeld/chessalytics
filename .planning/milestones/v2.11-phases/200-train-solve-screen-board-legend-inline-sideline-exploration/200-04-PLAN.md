---
phase: 200-train-solve-screen-board-legend-inline-sideline-exploration
plan: 04
type: execute
wave: 4
depends_on: [200-03]
files_modified:
  - frontend/src/components/train/TrainExplorationLine.tsx
  - frontend/src/components/train/__tests__/TrainExplorationLine.test.tsx
  - frontend/src/components/train/TrainReveal.tsx
  - frontend/src/components/train/TrainSolveScreen.tsx
  - frontend/src/components/train/__tests__/TrainReveal.test.tsx
  - frontend/src/components/train/__tests__/TrainSolveScreen.test.tsx
autonomous: true
requirements: [EXPLORE-03, EXPLORE-07, LEGEND-06]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The moment exploration starts, the reveal line boxes and the Also fine row give way to a Stockfish engine-lines card plus a move list of the explored line — no Maia card and no FlawChess engine card anywhere on the Train screen."
    - "The reveal panel's header block (guess verdict, outcome copy, flaw-fixed banner) and the game footer stay pinned on screen throughout exploration; only the boxes and the Also fine row are replaced."
    - "The exploration move list is a single clickable chain: SAN tokens plus prev/next, with the current ply highlighted; there is no branching and no tree."
    - "Clicking a Stockfish PV move plays that line's prefix up to and including the clicked move into the exploration chain, truncating any tail first."
    - "The exploration move list is fully controlled by its index prop and holds no index state, so appending a move can never snap it back to the start of the sideline."
    - "PV lines render in the engine's own multipv order, best line first, unchanged from the Analysis page."
    - "Exploring into a position with no legal continuation renders the engine card's empty/loading state rather than crashing, and the move list still allows stepping back."
    - "Every interactive element added by this plan carries a kebab-case data-testid, and every icon-only control carries an aria-label."
    - statement: "Two PV lines that transiently share a first move render as two separate rows and each remains independently clickable, with no dedupe introduced."
      verification: backstop
    - statement: "The legend spotlight, the Also fine row, and the exploration swap all render and remain operable in the mobile below-board layout at 375px."
      verification: backstop
  artifacts:
    - frontend/src/components/train/TrainExplorationLine.tsx
    - frontend/src/components/train/__tests__/TrainExplorationLine.test.tsx
    - "data-testid train-reveal-exploration, train-exploration-engine-card, train-exploration-line, train-exploration-token-{i}, btn-train-exploration-prev, btn-train-exploration-next"
  key_links:
    - "EngineLines.onMoveClick -> exploration.playLine -> exploration.fen -> TrainSolveScreen displayFen -> ChessBoard position"
    - "TrainExplorationLine.onJumpTo -> exploration.jumpTo -> exploration.fen -> the same board, with no truncation"
    - "TrainReveal's isExploring ternary wraps ONLY the line boxes + Also fine row; the header block and game footer sit outside it (D-10)"
---

<objective>
Render the exploration surface: the instant a sideline starts, the reveal boxes swap for a
Stockfish engine-lines card and a clickable move list of the explored line, with the
just-earned verdict still pinned above it — and confirm the whole phase in a real browser
at 375px.

Purpose: EXPLORE-03 (the swap-in card + move list), EXPLORE-07 (mobile below-board layout at
375px), and the pixel half of LEGEND-06 that jsdom structurally cannot verify.
Output: `TrainExplorationLine`, the `isExploring` swap inside `TrainReveal`, PV click-to-play
wiring, and an operator-confirmed browser pass.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/200-train-solve-screen-board-legend-inline-sideline-exploration/200-CONTEXT.md
@.planning/phases/200-train-solve-screen-board-legend-inline-sideline-exploration/200-RESEARCH.md
@.planning/phases/200-train-solve-screen-board-legend-inline-sideline-exploration/200-PATTERNS.md
@.planning/phases/200-train-solve-screen-board-legend-inline-sideline-exploration/200-03-SUMMARY.md
</context>

## Artifacts this plan produces

- `frontend/src/components/train/TrainExplorationLine.tsx` — new component exporting `TrainExplorationLine` and `TrainExplorationLineProps`
- `frontend/src/components/train/__tests__/TrainExplorationLine.test.tsx` — new test file
- New `TrainRevealProps` fields: `isExploring: boolean`, `exploration: TrainExplorationState`, `explorationPvLines: PvLine[]`, `explorationIsAnalyzing: boolean`, `explorationFen: string | null`
- New `data-testid` values: `train-reveal-exploration`, `train-exploration-engine-card`, `train-exploration-line`, `train-exploration-token-{i}`, `btn-train-exploration-prev`, `btn-train-exploration-next`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TrainExplorationLine — a fully controlled single-chain move list</name>
  <files>frontend/src/components/train/TrainExplorationLine.tsx, frontend/src/components/train/__tests__/TrainExplorationLine.test.tsx</files>
  <read_first>
    - `frontend/src/components/train/TrainLineStepper.tsx` — copy the INTERACTION shape only: the prev/next `Button variant="ghost" size="icon"` pair with `ChevronLeft`/`ChevronRight` and `aria-label` (L277-331), the horizontally scrolling token row's class list (L291-296), the active-token treatment `bg-brand-brown text-white` vs `text-muted-foreground hover:bg-muted` (L308-313), and the `scrollIntoView` effect that keeps the active token visible on mobile (L214-225). Do NOT copy the `useEffect(() => setIndex(0), [movesKey, startFen, resetNonce])` reset at L182-184, and do NOT copy the internal `index` state at L177 — this component is controlled.
    - `frontend/src/components/train/TrainLineStepper.tsx` L130-150 — `replayLine`'s `new Chess(startFen)` + per-move `try/catch` + break idiom, and the `movesKey = moves.join(' ')` content-identity memo key lesson at L163-174.
    - `frontend/src/hooks/useTrainExploration.ts` (from plan 200-03) — `TrainExplorationState`'s `moves`, `index`, `jumpTo` shape.
    - CLAUDE.md Frontend — `text-sm` floor for this component's own labels, `data-testid` on every interactive element, `aria-label` on icon-only buttons, `noUncheckedIndexedAccess` narrowing.
  </read_first>
  <behavior>
    - Given `moves: ['e2e4','e7e5','g1f3']`, `startFen` = the standard start position, and `index: 2`, the component renders three SAN tokens (`e4`, `e5`, `Nf3`) and marks the second one active.
    - `index: 0` renders all tokens with none active, and disables the prev control.
    - `index === moves.length` disables the next control.
    - Clicking `train-exploration-token-2` calls `onJumpTo(3)` — a token at array index i maps to chain index i+1, because index 0 is the position before any move.
    - Clicking prev at index 2 calls `onJumpTo(1)`; clicking next at index 1 calls `onJumpTo(2)`.
    - Appending a move to the `moves` prop and bumping `index` re-renders with the new token active — the component never calls `onJumpTo` on its own and never resets to 0.
    - A malformed or illegal UCI stops SAN derivation at that point without throwing; already-derived tokens still render.
  </behavior>
  <action>
Build the D-13 move list as a controlled component, so Pitfall 2 is structurally impossible.

Create `frontend/src/components/train/TrainExplorationLine.tsx` exporting
`TrainExplorationLineProps { moves: string[]; startFen: string; index: number; onJumpTo: (i: number) => void }`
and `TrainExplorationLine`. The component holds NO index state — the index arrives as a prop
and every navigation is reported through `onJumpTo`. That is the whole reason this is a new
component rather than a `TrainLineStepper` reuse: `TrainLineStepper` owns its index and
resets it to 0 whenever its `moves` content changes, which is exactly the append operation
exploration performs on every move.

Derive SAN labels in a `useMemo` keyed on `moves.join(' ')` and `startFen` (content identity,
never array identity — the parent rebuilds the array on each render). Replay with
`new Chess(startFen)` and a per-move `try { chess.move({ from, to, promotion }) } catch { break }`,
pushing `move.san` per successful ply and stopping at the first failure.

Render, in one row: a prev `Button variant="ghost" size="icon"` with `aria-label="Previous move"`
and `data-testid="btn-train-exploration-prev"`, disabled when `index === 0`, calling
`onJumpTo(index - 1)`; a scrolling token container with `data-testid="train-exploration-line"`
carrying the same overflow/wrap class list `TrainLineStepper` uses for its token row; one
`<button type="button">` per SAN with `data-testid={`train-exploration-token-${i}`}`,
`data-active={i + 1 === index ? 'true' : undefined}`, the `bg-brand-brown text-white` active
treatment (otherwise `text-muted-foreground hover:bg-muted`), `text-sm` type, and
`onClick={() => onJumpTo(i + 1)}`; and a next `Button` with `aria-label="Next move"` and
`data-testid="btn-train-exploration-next"`, disabled when `index === moves.length`, calling
`onJumpTo(index + 1)`. Copy `TrainLineStepper`'s `scrollIntoView` effect keyed on `index` so
a long sideline keeps the active token in view on mobile; call it optionally
(`active?.scrollIntoView?.(...)`) because jsdom does not implement it.

Do NOT play move sounds here. `TrainLineStepper` plays them because stepping a solution line
is part of the reveal's study choreography; nothing in CONTEXT.md asks for sound on a
free-play sideline, and adding it would be unrequested scope.

Create `frontend/src/components/train/__tests__/TrainExplorationLine.test.tsx` covering every
behavior bullet above with `@testing-library/react`, including the append case: render with
two moves at index 2, rerender with three moves at index 3, and assert the third token is the
active one and `onJumpTo` was never called by the component itself.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/train/__tests__/TrainExplorationLine.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/train/__tests__/TrainExplorationLine.test.tsx` exits 0 with at least 6 passing tests.
    - A test rerenders with an appended move and a bumped index and asserts the new token is active and `onJumpTo` was not called by the component (the Pitfall 2 regression guard).
    - `cd frontend && grep -c "useState" src/components/train/TrainExplorationLine.tsx | tr -d '\n'` outputs `0` — the component is fully controlled.
    - Both chevron buttons carry a non-empty `aria-label` and a `btn-train-exploration-` prefixed `data-testid` (source assertion).
    - Every label rendered by this component is `text-sm` or larger (source assertion).
    - `cd frontend && npm run lint && npm run build` exits 0.
  </acceptance_criteria>
  <done>A clickable, truncation-free, fully controlled single-chain move list exists with prev/next and an active-ply highlight, matching TrainLineStepper's look without inheriting its reset behavior.</done>
  <reversibility rating="reversible">A new leaf component with a four-prop interface and no state.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Swap the reveal body for the engine card + move list, wire PV click-to-play</name>
  <files>frontend/src/components/train/TrainReveal.tsx, frontend/src/components/train/TrainSolveScreen.tsx, frontend/src/components/train/__tests__/TrainReveal.test.tsx, frontend/src/components/train/__tests__/TrainSolveScreen.test.tsx</files>
  <read_first>
    - `frontend/src/components/train/TrainReveal.tsx` — the render tree's numbered sections: 1 guess verdict (L490-502), 2 outcome copy (L504-510), 3 flaw-fixed banner (L512-515), the reveal-error line (L517-521), 4 the `lineBoxes.map(...)` block, 4a the SAN-only fallback, the Also fine row added by plan 200-02, and 5 the game footer (L588-624). D-10 replaces only 4, 4a and the Also fine row.
    - `frontend/src/components/analysis/EngineLines.tsx` — the `EngineLinesProps` contract (L195-224), especially `onMoveClick`'s documented semantics ("the UCI moves from the start of the line up to and including the clicked move"); the exported `EngineLinesSkeleton` (L129) and `MAX_LINES` (L38); and L88-104, where `text-xs` is recorded as a user-approved exception scoped to this component.
    - `frontend/src/pages/Analysis.tsx` around L3550-3587 — the `Card`/`CardHeader`/`CardBody` + conditional-render shell already used to host `EngineLines`; reuse that shape rather than inventing a new one.
    - `frontend/src/components/train/TrainSolveScreen.tsx` — `explorationPvLines`, `explorationFen`, `explorationEngine` and the `exploration` object from plan 200-03, and the `<TrainReveal .../>` prop list.
    - `frontend/src/hooks/useTrainExploration.ts` — `playLine` and `jumpTo` signatures.
    - `frontend/src/components/train/__tests__/TrainReveal.test.tsx` — the existing harness that renders `TrainReveal` directly; the new props need defaults there.
  </read_first>
  <action>
Render the exploration surface in place of the reveal boxes, keeping the earned result on screen.

Add five props to `TrainRevealProps`: `isExploring?: boolean` (default false),
`exploration?: TrainExplorationState`, `explorationPvLines?: PvLine[]` (default `[]`),
`explorationIsAnalyzing?: boolean` (default false), and `explorationFen?: string | null`
(default null). Import the `PvLine` type from wherever `useStockfishEngine` exposes it and
`TrainExplorationState` from `@/hooks/useTrainExploration`.

Restructure the render so the swap is a single ternary wrapping ONLY sections 4, 4a and the
Also fine row. Sections 1, 2, 3, the reveal-error line and section 5 (the game footer) stay
outside it and therefore stay pinned on screen throughout exploration — keeping the
just-earned verdict visible while branching is the entire point of not sending the user to
the Analysis page (D-10).

The exploring arm renders a container with `data-testid="train-reveal-exploration"` holding,
in order:
1. A `Card` with `data-testid="train-exploration-engine-card"`, a `CardHeader size="compact"`
   labelled for Stockfish, and a `CardBody` containing `<EngineLinesSkeleton rows={MAX_LINES} />`
   when `explorationPvLines.length === 0` (the discretion call: reuse whatever `EngineLines`
   already ships for a search in flight, add no new loading treatment) and otherwise
   `<EngineLines pvLines={explorationPvLines} isAnalyzing={explorationIsAnalyzing} baseFen={explorationFen ?? undefined} flipped={puzzle.side_to_move === 'black'} onMoveClick={(uciMoves) => exploration?.playLine(uciMoves)} />`.
   Reuse `EngineLines` verbatim — do not fork a Train-specific copy, and do not edit
   `EngineLines.tsx` itself. Inherit its type scale unchanged: add no wrapper class, no
   arbitrary variant, and no scoped CSS that touches font size. Its `text-xs` PV chips, eval
   badge and move-number labels are a user-approved exception recorded at
   `EngineLines.tsx:88-91`, re-confirmed by the user at plan time for this new Train surface
   (A-11). Keeping the inherited scale is what makes the Train exploration card and the
   Analysis engine card visually identical, which is the intent.

   `onMoveClick` already hands over the prefix up to and including the clicked chip, which is
   exactly D-14's semantics; `playLine` truncates at the current index before appending.
2. `<TrainExplorationLine moves={exploration.moves} startFen={puzzle.fen} index={exploration.index} onJumpTo={exploration.jumpTo} />`.

Render no Maia card and no FlawChess engine card — EXPLORE-03 excludes both, and this phase
never touches the ONNX runtime.

In `frontend/src/components/train/TrainSolveScreen.tsx`, pass the five new props through from
the state plan 200-03 established, and confirm `displayFen` (not `boardFen`) is what reaches
`<ChessBoard position=...>` so a PV click or a token jump moves the board with no extra wiring.

Extend `TrainReveal.test.tsx` with: a case asserting that with `isExploring` true the
`train-line-box-*` testids and `train-reveal-also-fine` are absent while
`train-reveal-exploration`, `train-exploration-engine-card` and `train-exploration-line` are
present; a case asserting `train-verdict-guess` and the game footer remain present in the same
render (D-10); a case asserting the skeleton shows when `explorationPvLines` is empty and
`EngineLines` shows when it is not; and a case asserting PV lines render in the supplied
multipv order. Extend `TrainSolveScreen.test.tsx` with an end-to-end case: solve, drop a piece
to start exploration, assert the swap happened and `data-arrows-count` is 0, click an engine
PV move chip (`engine-line-0-move-0`), and assert the board's `data-position` advanced and a
token appeared in `train-exploration-line`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/train/__tests__/TrainReveal.test.tsx src/components/train/__tests__/TrainSolveScreen.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/train/__tests__/TrainReveal.test.tsx src/components/train/__tests__/TrainSolveScreen.test.tsx` exits 0.
    - A test asserts that while exploring, `train-reveal-exploration` is present and every `train-line-box-*` testid plus `train-reveal-also-fine` are absent (EXPLORE-03).
    - A test asserts `train-verdict-guess` is still rendered in that same exploring render (D-10).
    - A test asserts clicking an `engine-line-0-move-` chip advances the board's `data-position` and appends to `train-exploration-line` (D-14).
    - `cd frontend && grep -c "MaiaCard\|FlawChessEngineLines" src/components/train/TrainReveal.tsx | tr -d '\n'` outputs `0` (EXPLORE-03 excludes both engines).
    - `cd frontend && grep -c "from '@/components/analysis/EngineLines'" src/components/train/TrainReveal.tsx` outputs `1` — `EngineLines` is imported, not reimplemented.
    - `cd frontend && git diff --quiet $(git merge-base HEAD main) HEAD -- src/components/analysis/EngineLines.tsx && echo unchanged` outputs `unchanged` — `EngineLines` is reused, not forked and not edited, so the Analysis engine surface is bit-identical after this phase.
    - `cd frontend && npm run build` exits 0 (`TrainRevealProps` is a shared interface; tsc is the gate).
    - `cd frontend && npm run lint && npm run knip` exits 0.
  </acceptance_criteria>
  <done>Starting a sideline swaps the reveal boxes for a Stockfish engine card and a clickable move list while the verdict and game footer stay pinned; clicking a PV move plays it into the line and moves the shared board.</done>
  <reversibility rating="reversible">One ternary in one render tree plus five optional props with defaults.</reversibility>
</task>

</tasks>

## Human Verification Required (non-blocking, end of phase)

**This does NOT gate execution.** Both automated tasks above stand on their own — the phase
executes to completion without pausing here. Run this browser pass after the phase wraps,
before the squash-merge to `main`. Record the outcome in the phase SUMMARY / VERIFICATION.

It covers only what jsdom structurally cannot verify: real pixel layout at 375px, hover feel,
and tap-target behavior on a touch viewport. Everything else in the phase is automated-test
green already.

**What was built:** reveal line boxes as Card/CardHeader with arrow glyphs in their
board-arrow colors, a two-sided spotlight (desktop hover on the card, mobile tap on the
glyph), yellow removed from the entire reveal surface, a compact "Also fine" row, and
post-solve inline sideline exploration on the shared board with a Stockfish engine card and a
clickable move list.

**Why human (all items):** jsdom has no layout engine — it reports zero-sized boxes, cannot
detect overflow, and `fireEvent.click` passes regardless of a 4px tap target. Rendered font
size and composited color after the Tailwind cascade are likewise not observable there.

<details>
<summary>Browser pass — 15 steps (desktop, then 375px)</summary>

    Setup:
    1. `docker compose -f docker-compose.dev.yml -p flawchess-dev up -d`
    2. `uv run uvicorn app.main:app --reload` and, in another shell, `cd frontend && npm run dev`
    3. Open `/train` and start a session. If no puzzles are due, use the dev time-travel strip
       on the Train page, and `uv run python scripts/reset_train_state.py --user-id <N>`
       afterwards to get back to a clean slate.

    Desktop (default window):
    4. Solve a puzzle. Each reveal box is a Card with a small arrow glyph before its title, in
       the same color as that move's arrow on the board (blue best, quality-colored played,
       thin white game move).
    5. Hover a whole card: it gains its own highlight AND every other arrow and quality badge
       disappears from the board, leaving only that card's move. Move away: everything returns.
       Tab to a card with the keyboard: the same thing happens.
    6. On a soft or herring puzzle, an "Also fine: …" row appears listing only the moves that
       are actually drawn as green arrows. Hover its glyph: only those green arrows remain.
    7. Find or force a puzzle where your played move grades as an inaccuracy. Confirm there is
       no yellow anywhere — arrow, corner badge, header glyph, or step highlight — and that the
       eval badge still shows the small drop.
    8. Step into a line two or three moves, then drag any piece on the board. The reveal boxes
       give way to a Stockfish card plus a move list that already contains the moves you had
       stepped through, followed by your new move. The board's solution arrows are gone.
    9. Click a move inside a Stockfish PV line: it plays into the move list and the board follows.
       Click an earlier token in the move list, then play a different move: the tail is discarded.
    10. The Solution button is only on screen once the board has left the pristine reveal.
        Press it: exploration ends and the full reveal (boxes + arrows) comes back.
    11. Press Next: the new puzzle shows no leftover engine lines and no leftover arrows.
    12. Click Analyze, then use the browser back button: the pristine reveal returns (not the
        exploration state), and the Analysis page itself looks unchanged.

    Mobile (Chrome DevTools device toolbar, width exactly 375px):
    13. Repeat steps 4-11 in the below-board layout. Specifically confirm: the glyphs are big
        enough to tap accurately; tapping a glyph spotlights and tapping it again clears;
        tapping a different glyph switches; tapping the board or empty space clears; hovering
        is not required for anything.
    14. The exploration engine card and move list fit the 375px column without horizontal page
        scroll, and the move list scrolls horizontally on its own once the line gets long.
    15. The exploration card's Stockfish PV rows are comfortably readable at 375px: chips,
        eval badges and move numbers are legible without zooming, nothing is clipped, and no
        row overflows its container. They render at the same scale as the Analysis page's
        engine lines by design (A-11) — the two engine cards should look identical.

</details>

**Outcome:** record "approved" or the issues found (screenshots welcome) in the phase
VERIFICATION.md. Issues found here become follow-up work, not a retroactive execution block.

## Flagged Planner Assumptions

- **A-10 (unclassified edge, EXPLORE-07):** probe returned "review manually". Reviewed:
  EXPLORE-07's substance splits into a DOM half (the swap renders inside the same
  `flex-col` / `lg:flex-row` shell, no separate mobile markup exists in `TrainReveal` —
  asserted by task 2's component tests) and a pixel half (actual 375px layout), which jsdom
  cannot verify. The pixel half is carried explicitly by the end-of-phase "Human Verification
  Required" section below, not dropped. That section is non-blocking by the user's plan-time
  decision: it runs before the squash-merge rather than pausing execution, so the pixel half
  is confirmed by an operator but is not an execution gate.
- **A-11 (settled — no executor decision required):** the Train exploration card reuses
  `EngineLines` verbatim and inherits its `text-xs` PV chips, eval badge and move-number
  labels. This is **not** a CLAUDE.md `text-sm`-floor violation: the exception is already
  granted and documented in the component itself at `EngineLines.tsx:88-91` — the user
  approved it personally in the Quick 260628-r5v UAT ("shrink the desktop engine lines to
  match the already-compact mobile lines"), scoped to this dense engine surface — and the
  user re-confirmed it at plan time for this new Train surface. `EngineLines.tsx:98-101`
  records the follow-through: desktop was deliberately brought down to `text-xs` to match
  mobile, so the compact/desktop split is about row layout, not font size.
  Mobile readability is met either way: below 640px the unlayered typography bump in
  `frontend/src/index.css` (L363-366) renders `.text-xs` at 0.875rem regardless — exactly the
  desktop `text-sm` size — and unlayered rules beat every layered utility. Keeping the
  inherited scale is what makes the Train exploration card and the Analysis engine card
  visually identical, which is the intent. No type-scale override, wrapper class or scoped
  CSS is to be added, `EngineLines.tsx` is not to be edited, and it is not to be forked.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine PV strings → DOM | SAN chips and eval badges produced by the exploration Stockfish instance are rendered by `EngineLines` and by `TrainExplorationLine` |
| engine PV moves → board state | A PV chip click feeds engine-supplied UCIs into the exploration chain and thus into the shared board |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-200-11 | Tampering (XSS) | `EngineLines` chips and `TrainExplorationLine` SAN tokens | low | mitigate | Both render strings as React children. `EngineLines` already documents this mitigation in its own module docstring (T-137-03); `TrainExplorationLine` follows the same pattern and introduces no raw HTML injection point. Gated by the grep below. |
| T-200-12 | Tampering | Engine-supplied UCIs replayed onto the board via `onMoveClick` | low | mitigate | `playLine` replays each UCI through `chess.js` inside a `try/catch` that breaks on the first illegal move, so a malformed PV truncates the chain rather than corrupting board state. |
| T-200-13 | Denial of Service (self-inflicted) | An unbounded exploration chain growing the move list without limit | low | accept | The chain grows only by explicit user action, one ply per interaction, and the token row scrolls rather than growing the panel; the search itself is bounded by `useStockfishEngine`'s existing movetime and node caps. |

Canon ASVS L1 items with no phase-specific surface: V2, V3, V4, V6 — no endpoint, no auth
path, no persisted state, no new network call.

**Additional gate:** `cd frontend && grep -rn "dangerouslySetInnerHTML" src/components/train | wc -l` outputs `0`.
</threat_model>

<verification>
Full phase gate, run before this phase is squash-merged (CLAUDE.md pre-merge gate, frontend half):
- `cd frontend && npm run lint` exits 0.
- `cd frontend && npm run knip` exits 0.
- `cd frontend && npm run build` exits 0 — mandatory here, not optional: this phase changes
  the shared `TrainRevealOverlay`, `TrainLineStep`, `TrainRevealStep` and `TrainRevealProps`
  types, and neither lint nor vitest type-checks.
- `cd frontend && npm test -- --run` exits 0.
- The end-of-phase "Human Verification Required" browser pass has been run and its outcome
  recorded in VERIFICATION.md. It is non-blocking for execution, but it is a pre-merge item:
  do not squash-merge the phase with it unrun.
</verification>

<success_criteria>
- EXPLORE-03: the reveal boxes and Also fine row give way to a Stockfish engine-lines card
  plus a move list of the explored line, with no Maia card and no FlawChess engine card.
- EXPLORE-07: the swap renders and stays operable in the mobile below-board layout at 375px
  (operator-confirmed).
- LEGEND-06: the legend glyphs and the tap spotlight are operable at 375px
  (operator-confirmed).
- D-10: the guess verdict, outcome copy, flaw-fixed banner and game footer stay pinned
  throughout exploration.
- D-13/D-14: the move list is a single truncating chain and PV moves are clickable into it.
</success_criteria>

<output>
Create `.planning/phases/200-train-solve-screen-board-legend-inline-sideline-exploration/200-04-SUMMARY.md` when done
</output>
