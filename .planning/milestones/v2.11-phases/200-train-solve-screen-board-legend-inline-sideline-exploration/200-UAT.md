---
status: complete
phase: 200-train-solve-screen-board-legend-inline-sideline-exploration
source: [200-VERIFICATION.md, 200-04-PLAN.md]
started: 2026-08-01
updated: 2026-08-01
completed: 2026-08-01
---

## Tests

### 1. Desktop — legend glyphs, card hover spotlight, no yellow, Also fine row

covers: LEGEND-01, LEGEND-02, LEGEND-03, LEGEND-04
steps: |
  Setup: dev DB up, backend on :8000, frontend on :5173. Open /train and start a
  session. If nothing is due, use the Train page's dev time-travel strip, then
  `uv run python scripts/reset_train_state.py --user-id <N>` afterwards.

  4. Solve a puzzle. Confirm each reveal box is a Card with an arrow glyph before
     its title, colored to match its board arrow (blue best, quality-colored played,
     thin white game move).

  5. Hover a whole card: it gains its own highlight AND every other arrow and badge
     leaves the board. Move away: everything returns. Tab to a card: same behavior.

  6. On a soft or herring puzzle, confirm the "Also fine: …" row lists only moves
     actually drawn as green arrows. Hover the row: only those green arrows remain.
  6b. (UAT round 2, item 1) While hovering a card, CLICK its arrow glyph: the card
     stays selected. The glyph has no handler of its own any more.
  6c. (UAT round 2, item 2) Step into one card's line, then step into another card's:
     only the card you are currently stepping shows a brown move-cursor badge.
  6d. (UAT round 3, items 1+2) The green check / red cross marks are gone. The guess
     row carries a rounded score chip (+1 green when right, +0 red when wrong), and
     the Your-move card header carries the MOVE points it earned: +2 green for a good
     move, +1 green for an inaccuracy, +0 red for a wrong one. Guess chip + move chip
     always add up to the "Points: +N" flash that popped over the board.
  6e. (UAT round 3, item 4) The "Also fine: …" row is a card with a header and no
     body, sitting flush with the line-box cards above it.
  6f. (UAT round 3, item 5) A long engine line steps through up to 12 plies (was 10).

  7. On a puzzle where your played move grades as an inaccuracy, confirm there is no
     yellow anywhere (arrow, corner badge, header glyph, step highlight) and that the
     eval badge still shows the small drop.
expected: All four steps behave as described, with no yellow on the reveal surface.
result: passed — operator-confirmed at ship time after 10 UAT fix rounds (commits 88e18b55e..016084be3)

### 2. Desktop — inline sideline exploration, PV click-to-play, Solution/Next/Analyze

covers: EXPLORE-01, EXPLORE-02, EXPLORE-03, EXPLORE-04, EXPLORE-05, EXPLORE-06
steps: |

  8. Step into a line two or three moves, then drag any piece. The reveal boxes give
     way to a Stockfish card plus the ANALYSIS PAGE'S move list (VariationTree),
     already containing the moves you stepped through, followed by your new move. The
     board's solution arrows are gone.

  9. Click a move inside a PV line: it plays into the move list and the board follows.
     Click an earlier move in the move list, then play a DIFFERENT move: the original
     continuation SURVIVES as its own sideline block (UAT round 2, item 3 — it used to
     be truncated), with its own × to close it, exactly as on the analysis board.
  9b. (UAT round 2, items 4+5) Every move you play free-hand carries a quality badge on
     the board AND in the move list, and its last-move square is tinted by that quality.
     Confirm the badges are only best / good / mistake / blunder — never a gem or great
     badge, because Train runs no Maia.
  9c. (UAT round 2, item 6) The Stockfish card header has an × on its right. Press it:
     same effect as the Solution button (back to the pristine reveal).

  10. Solution appears only once the board has left the pristine reveal. Press it:
      exploration ends and the full reveal (boxes + arrows) comes back.
  10b. (UAT round 3, item 3) Step a card's line forward, then step BACK to its start
      with the ◀ control, without moving the pointer off the card. The Solution button
      disappears and the board shows the FULL solution again — both the Your-move and
      the Best-move arrow, not just the card you were stepping. Repeat on a card whose
      line is exactly ONE move (click the token, then ◀): it used to strand you there
      with both controls dead.
  10c. (UAT round 9) Step a card's line two or three moves forward, then click (desktop)
      or tap (mobile) a DIFFERENT card's body. The board snaps back to the puzzle
      position and shows that card's move alone (its arrow + badge), with the card
      itself highlighted. Clicking the stepper's ◀ ▶ or a SAN token inside a card
      still just steps that line.

  11. Press Next: the new puzzle shows no leftover engine lines and no leftover arrows.
  12. Click Analyze, then browser-back: the pristine reveal returns (not the exploration
      state), and the Analysis page itself looks unchanged.
expected: |
  Exploration starts from any legal post-verdict move with the correct seeded prefix,
  PV clicks play in, Solution exits cleanly, Next leaves no residue, and Analyze plus
  back restores the pristine reveal.
result: passed — operator-confirmed at ship time after 10 UAT fix rounds (commits 88e18b55e..016084be3)

### 3. Mobile at exactly 375px — tap spotlight and exploration layout

covers: LEGEND-06 (pixel half), EXPLORE-07 (pixel half)
why_human: |
  jsdom has no layout engine — it reports zero-sized boxes, cannot detect overflow, and
  `fireEvent.click` passes regardless of a 4px tap target. Rendered font size and
  composited color after the Tailwind cascade are likewise unobservable there. This is
  the only part of the phase automation structurally cannot cover.
steps: |
  Chrome DevTools device toolbar, width exactly 375px.

  13. Repeat steps 4-11 in the below-board layout. Specifically: tapping ANYWHERE on a
      card (glyph included) spotlights it, tapping again clears, tapping a different
      card switches, tapping the board or empty space clears; hover is never required.
      NOTE: the first tap on each card is the exact case code review found broken
      (WR-01) and commit 6926d0dc fixed — please confirm it now works on a real touch
      viewport, since that fix is the one thing here not proven in a real browser.

  14. The exploration engine card and move list fit the 375px column with no page-level
      horizontal scroll; the move list is the analysis vertical paired list inside a
      fixed-height box and scrolls VERTICALLY on its own once the line gets long.

  15. The PV rows are comfortably readable at 375px: chips, eval badges and move numbers
      legible without zooming, nothing clipped, no row overflowing its container. They
      intentionally render at the same scale as the Analysis page's engine lines (A-11) —
      the two engine cards should look identical.
expected: |
  All steps pass with no layout breakage, no clipped or illegible text, and comfortably
  hittable tap targets. In particular the first tap on any glyph registers.
result: passed — operator-confirmed at ship time after 10 UAT fix rounds (commits 88e18b55e..016084be3)

## Summary

total: 3
passed: 3
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps
