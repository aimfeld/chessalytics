---
phase: 208-analysis-fen-pgn-paste
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/App.tsx
  - frontend/src/lib/pastedGame.ts
  - frontend/src/lib/pastedGame.test.ts
  - frontend/src/components/analysis/PasteModal.tsx
  - frontend/src/components/analysis/__tests__/PasteModal.test.tsx
  - frontend/src/pages/Analysis.tsx
  - frontend/src/App.test.tsx
autonomous: true
requirements: [PASTE-01, PASTE-02, PASTE-03, PASTE-08, PASTE-09]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

assumption_delta_decision:
  noun: "platform (app/schemas/normalization.py Platform Literal + games.platform)"
  decision: no-change
  rationale: >-
    DEFAULT_EXCLUDED_PLATFORMS already IS the analytics-eligibility seam — a derived set
    over platform values, not a second source of truth. Promoting eligibility to its own
    column needs a migration (explicit phase non-goal) and the seed already rejected the
    per-game opt-in boolean. Recorded in full, with the invariant test, in 208-02-PLAN.md.

must_haves:
  truths:
    # --- Goal-backward (SC-1 / PASTE-01) ---
    - "Pasting a bare FEN into the modal and pressing Load puts that position on the /analysis board as a free-play root"
    - "A bare-FEN paste issues zero network requests and leaves window.location.search byte-identical to what it was before the paste"
    # --- Goal-backward (SC-2 / PASTE-02) ---
    - "Pasting a PGN loads its full mainline into the move list; the number of loaded plies equals chess.js history().length for that PGN"
    - "A PGN carrying [SetUp \"1\"] + [FEN ...] loads with the board adopting the header FEN as its root, including a Black-to-move root"
    - "Headerless movetext (no [Event]/[White]/[Black] tags) loads its mainline without error"
    - "A ChessBase-style PGN carrying RAVs, NAGs and {comments} loads its mainline with those constructs dropped and no error surfaced"
    - "Parsed White/Black names, Elo ratings, Result and Date from the PGN headers are visible in the board's player-info area for an ephemeral (unsaved) pasted PGN"
    # --- Goal-backward (SC-3 / PASTE-03) ---
    - "One textarea accepts both formats; the modal renders no format toggle control"
    - "Malformed input renders the inline error 'Couldn't read that as a FEN or PGN.' and leaves the board exactly as it was — no partial mainline, no blank board, no thrown exception"
    - "A PGN whose Nth move is illegal in the position loads ZERO moves, not the legal prefix — the chess.js instance is discarded on throw and never read after a caught error"
    # --- Goal-backward (SC-8 / PASTE-08) ---
    - "/analysis appears in the desktop header nav and in the mobile More drawer"
    - "BOTTOM_NAV_ITEMS still has exactly 5 entries and none of them is /analysis"
    - "/analysis is clickable (not opacity-40 / aria-disabled) for an account with zero imported games"
    - "The /analysis nav item renders its active treatment when the pathname is /analysis"
    # --- Goal-backward (SC-9 / PASTE-09) ---
    - "Every interactive element the modal introduces carries a data-testid, and the modal body is a <form> element carrying its own data-testid distinct from the DialogContent testid"
    - "No element this plan introduces uses a Tailwind font-size utility below the 14px floor"
    # --- Edge probe: PASTE-02 empty ---
    - "An empty textarea, a whitespace-only paste, and a PGN with headers but zero movetext all resolve to a state that loads nothing: empty leaves Load disabled with no error, headers-with-zero-moves renders the parse error"
    # --- Edge probe: PASTE-02 encoding ---
    - "Input is normalized before parsing: a leading BOM is stripped, CRLF is folded to LF, non-breaking spaces are folded to ordinary spaces, and typographic quotes are folded to ASCII quotes — a PGN copied from a PDF or web page with those characters parses identically to its plain-ASCII equivalent"
    - "Non-ASCII player names (accented Latin and CJK) round-trip from the PGN header to the rendered player-info area as the same code points, with no mojibake and no truncation of the header value before display"
    # --- Edge probe: PASTE-03 concurrency ---
    - "The sniff is a pure function of its input string: calling it twice on the same text returns equal results, and it holds no module-level mutable chess.js instance between calls"
    # --- Edge probe: PASTE-08 adjacency / ordering / empty ---
    - "/analysis is appended as the LAST entry of NAV_ITEMS, so the superuser ADMIN_NAV_ITEM spread still renders rightmost"
    - "isActive('/analysis', pathname) uses startsWith('/analysis') and returns false for every other NAV_ITEMS route, and no other route's clause returns true for the pathname /analysis"
    - "Both NAV_ITEMS and BOTTOM_NAV_ITEMS carry a comment recording that their divergence is intentional and naming the WR-07 lesson"
    # --- Edge probe: PASTE-09 adjacency / ordering ---
    - "Every data-testid this plan introduces is unique across the rendered /analysis DOM — no value collides with an existing testid"
    - "The modal footer's DOM order is [secondary 'Analyze full game', primary 'Load'], so DialogFooter's flex-col-reverse puts Load topmost on mobile and rightmost on desktop"
    # --- UI-SPEC lift: E1 long-text ---
    - "The trigger label is the fixed literal 'Paste'; at a 375px viewport the compact movelist CardHeader renders the icon plus label alongside the 'Moves' title without wrapping or truncating either"
    # --- UI-SPEC lift: E2 empty / loading / error / partial / long-text ---
    - "An empty (whitespace-only) textarea leaves 'Load' disabled, renders no error, and hides the side selector — no copy narrates the state"
    - "A client parse failure renders the locked D-22 copy inline at data-testid=\"paste-error\" with role=\"alert\""
    - "A PGN missing WhiteElo/BlackElo/Date, or carrying headerless movetext, parses and displays with the absent fields omitted rather than rendered blank or as the literal undefined"
    - "The textarea carries an explicit max-height with internal vertical scrolling, so a several-hundred-ply PGN can never grow DialogContent past the viewport"
    # --- UI-SPEC lift: E3 long-text ---
    - "Parsed player names in the side-selector meta line truncate individually (each name has its own truncating element), and the rating parenthetical is omitted when the Elo header is absent"
    # --- UI-SPEC lift: E4 long-text ---
    - "Footer button labels are fixed literals ('Load', 'Analyze full game', 'Analyzing…'); at 375px DialogFooter's flex-col-reverse gives each a full-width row so no label truncates"
    # --- UI-SPEC lift: E5 overflow / long-text ---
    - "Both error strings are fixed literals that wrap within the modal width at 375px — never truncated, never horizontally scrolled"
    - "Raw chess.js error text is never surfaced to the user; the only parse-error string the modal can render is the single locked literal"
    # --- UI-SPEC lift: E6 long-text ---
    - "Player names sourced from PGN headers truncate rather than wrap or push the rating and result/date content out of the PlayerBar row"
    # --- UI-SPEC lift: E9 error ---
    - "/analysis is reachable and clickable with zero imported games via IMPORT_EXEMPT_ROUTES — it never renders the locked/disabled nav treatment"
    # --- UI-SPEC backstop (verification: backstop) ---
    - statement: "The side-selector meta line fits at a 375px viewport with a realistically long parsed header name, with per-name truncation applied and the toggle's 50/50 item widths unchanged"
      verification: backstop
    - statement: "The result + date slot (reusing the empty clock slot) has room on the same PlayerBar row alongside a long truncating player name at 375px"
      verification: backstop

  prohibitions:
    - statement: "MUST NOT let an ephemeral paste become persistent: no pasted FEN, PGN, parsed mainline, header set or side selection may be written to localStorage, sessionStorage, IndexedDB, a cookie, or the URL — 'a refresh returns to the start position' is the promise, and a cache that survives reload silently breaks it"
      category: transparency
      status: resolved
      verification: judgment
    - statement: "MUST NOT present a partially-parsed game as a complete one: if any move in the movetext fails to parse, nothing loads at all. Loading the legal prefix produces a game that looks real, ends at the wrong move, and yields real-looking but wrong analysis — the user has no way to tell it happened"
      category: transparency
      status: resolved
      verification: judgment

  artifacts:
    - path: "frontend/src/lib/pastedGame.ts"
      provides: "Format sniffing, input normalization, and PGN header extraction for pasted input"
      exports: ["sniffPastedInput", "MAX_PASTED_INPUT_LENGTH", "PasteParseResult", "PastedGameHeaders"]
    - path: "frontend/src/components/analysis/PasteModal.tsx"
      provides: "The paste dialog: textarea, live-sniffed state machine, side selector, footer"
      exports: ["PasteModal"]
    - path: "frontend/src/lib/pastedGame.test.ts"
      provides: "Sniff unit tests covering every row of the measured chess.js behavior table"
    - path: "frontend/src/components/analysis/__tests__/PasteModal.test.tsx"
      provides: "Modal state-machine, testid, and a11y tests"

  key_links:
    - from: "frontend/src/components/analysis/PasteModal.tsx"
      to: "frontend/src/lib/pastedGame.ts"
      via: "sniffPastedInput drives the four-state footer/side-selector machine"
      pattern: "sniffPastedInput"
    - from: "frontend/src/pages/Analysis.tsx"
      to: "frontend/src/hooks/useAnalysisBoard.ts"
      via: "loadMainLine(sans, rootFen) applies a sniffed PGN result to the board"
      pattern: "loadMainLine\\("
    - from: "frontend/src/pages/Analysis.tsx"
      to: "frontend/src/lib/analysisUrl.ts"
      via: "parseAnalysisFenParam validates a sniffed bare FEN before seeding the free-play root"
      pattern: "parseAnalysisFenParam"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/App.tsx"
      via: "NAV_ITEMS entry, IMPORT_EXEMPT_ROUTES member, and isActive clause must all name /analysis"
      pattern: "'/analysis'"
---

<objective>
Open the door into `/analysis` from outside, end to end, with zero backend work: a nav item
that makes the page reachable, a trigger in the move-list header, a modal with one textarea
whose format is sniffed, and a board that loads either a bare FEN as a free-play root or a
PGN's full mainline plus headers. Nothing is persisted by this plan.

Purpose: this is the phase tracer. It proves the whole user path — nav → page → modal →
parse → board — before any persistence, exclusion, or Library work is built on top of it.
If the sniff-based single-textarea design (D-01) does not hold, it fails here, after one
plan, not after four.

Output: `/analysis` in the main nav, `PasteModal`, a tested `pastedGame.ts` sniff helper,
and ephemeral player info on the board for a pasted PGN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/208-analysis-fen-pgn-paste/208-CONTEXT.md
@.planning/phases/208-analysis-fen-pgn-paste/208-UI-SPEC.md
@.planning/phases/208-analysis-fen-pgn-paste/208-PATTERNS.md
@.planning/seeds/SEED-144-analysis-fen-pgn-paste.md
@CLAUDE.md
</context>

## Requirements

Requirement IDs are minted at planning time, one per ROADMAP Phase 208 Success Criterion, in
order. This phase predates its milestone's `REQUIREMENTS.md`, so this table is the
traceability record.

| ID | Success Criterion (ROADMAP Phase 208) | Plan(s) | Task(s) |
|----|----------------------------------------|---------|---------|
| PASTE-01 | Pasting a bare FEN loads that position on `/analysis` as a free-play root, writes nothing to the DB, and does not touch the URL. | 01 | 1 |
| PASTE-02 | Pasting a PGN loads its mainline and headers (names, ratings, result, date reach the PlayerBar); a `[SetUp]`/`[FEN]`-rooted PGN adopts the header FEN, headerless movetext parses, and ChessBase-style RAVs/NAGs/comments are dropped without an error. | 01 | 2 |
| PASTE-03 | One textarea handles both formats with no toggle, and malformed input produces an inline error rather than a blank board, a crash, or a silently wrong position. | 01 | 1, 2, 3 |
| PASTE-04 | "Analyze full game" persists exactly one `platform='pgn'` game with `user_color` from the side selector, and enqueues it via `POST /imports/eval/tier1` — for guests as well as authenticated users. Paste-and-look and bare-FEN paste persist nothing. | 01 (persists-nothing half), 03 | 01/1-2, 03/1-2 |
| PASTE-05 | Pasted games never appear in Openings WDL, Endgames, Insights, or any other `apply_game_filters()` consumer, proven by a test that goes red if `'pgn'` is removed from `DEFAULT_EXCLUDED_PLATFORMS`. | 02, 04 | 02/1, 04/1-2 |
| PASTE-06 | Re-pasting and re-analyzing the same game reuses the existing row instead of violating or dodging `uq_games_user_platform_game_id`. | 03 | 1, 3 |
| PASTE-07 | A pasted game that is saved and then never analyzed is marked eval-ineligible rather than left pending — `ix_games_user_evals_pending` stays near-zero at steady state and the `users_with_zero_pending` gate is unaffected. | 03 | 1, 3 |
| PASTE-08 | `/analysis` is reachable from the desktop header and the mobile More drawer, is clickable with zero imported games, highlights when active, and the mobile bottom bar is unchanged — with a comment on both nav arrays recording that the divergence is intentional. | 01 | 1 |
| PASTE-09 | Every interactive element in the modal carries a `data-testid`, the modal is a `<form>` with its own container testid, no type is below `text-sm`, and the whole flow works at 375px — including landing on `/analysis` from the More drawer and getting sanely back out. | 01, 03, 04 | 01/1-3, 03/2, 04/2-3 |

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / Requirement | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Bring an arbitrary position or game into `/analysis` from outside via one button, one modal, one sniffed textarea | 01 | COVERED |
| GOAL | — | Explicit "Analyze full game" saves `platform='pgn'` (always excluded) and enqueues tier-1 | 02, 03 | COVERED |
| GOAL | — | `/analysis` gains the main nav item it has never had | 01 | COVERED |
| REQ | PASTE-01 | Bare FEN → free-play root, no DB write, no URL change | 01 | COVERED |
| REQ | PASTE-02 | PGN mainline + headers, SetUp/FEN root, headerless, RAV/NAG dropped | 01 | COVERED |
| REQ | PASTE-03 | One textarea, sniffed, malformed → inline error | 01 | COVERED |
| REQ | PASTE-04 | Save + tier-1 enqueue; paste-and-look persists nothing | 01, 03 | COVERED |
| REQ | PASTE-05 | Never in any `apply_game_filters()` consumer, red-if-removed test | 02, 04 | COVERED |
| REQ | PASTE-06 | Re-paste reuses the existing row | 03 | COVERED |
| REQ | PASTE-07 | Saved-but-unanalyzed pasted game is not left pending | 03 | COVERED |
| REQ | PASTE-08 | Nav item on two surfaces, bottom bar unchanged, comments on both arrays | 01 | COVERED |
| REQ | PASTE-09 | testids, `<form>`, 14px type floor, 375px | 01, 03, 04 | COVERED |
| RESEARCH | — | *(no RESEARCH.md — intentionally skipped; CONTEXT.md carries the measured chess.js table)* | — | N/A |
| CONTEXT | D-01 | Button + modal, one textarea, format sniffed | 01 | COVERED |
| CONTEXT | D-02 | Mainline + headers only; comments and variations dropped | 01 | COVERED |
| CONTEXT | D-03 | Ephemeral — no `?fen=`/`?line=` write-back | 01 | COVERED |
| CONTEXT | D-04 | Persist only on explicit "Analyze full game" | 01, 03 | COVERED |
| CONTEXT | D-05 | `platform='pgn'` added to `DEFAULT_EXCLUDED_PLATFORMS` | 02 | COVERED |
| CONTEXT | D-06 | Side selector sets `user_color`, defaults to White | 01, 03 | COVERED |
| CONTEXT | D-07 | Untimed: no clocks, no TC bucket | 02 | COVERED |
| CONTEXT | D-08 | Enqueue via the existing tier-1 path, guests included | 03 | COVERED |
| CONTEXT | D-09 | `/analysis` added to `NAV_ITEMS` only | 01 | COVERED |
| CONTEXT | D-10 | `/analysis` added to `IMPORT_EXEMPT_ROUTES` | 01 | COVERED |
| CONTEXT | D-11 | Pasted games hidden from BOTH Library tabs by default, opt-in chip | 04 | COVERED |
| CONTEXT | D-12 | Hiding needs no backend change; the opt-in is the work | 04 | COVERED |
| CONTEXT | D-13 | "Pasted" text badge top-right of the Library game card | 04 | COVERED |
| CONTEXT | D-14 | The chip is Library-scoped; shared `Platform`/`PLATFORMS` must not gain `pgn` | 04 | COVERED |
| CONTEXT | D-15 | After save, navigate to `?game_id=N` | 03 | COVERED |
| CONTEXT | D-16 | `platform_game_id` = deterministic hash of normalized mainline SAN + root FEN | 02 | COVERED |
| CONTEXT | D-17 | Hash hit → silently reuse the existing row and open it | 03 | COVERED |
| CONTEXT | D-18 | `user_color` not in the hash; re-paste updates it in place | 03 | COVERED |
| CONTEXT | D-19 | Trigger lives in the move-list card header | 01 | COVERED |
| CONTEXT | D-20 | Trigger visible in every mode, including game mode | 01 | COVERED |
| CONTEXT | D-21 | Any parse failure rejects the whole paste; discard the chess.js instance on throw | 01 | COVERED |
| CONTEXT | D-22 | One generic error message | 01 | COVERED |

All rows COVERED. No `⚠ MISSING`.

## Artifacts this phase produces

Newly created symbols, files and identifiers across all four plans of Phase 208. Source-grounding
passes should treat everything below as created-by-this-phase, not as drift against existing code.

**Frontend files (new)**
- `frontend/src/lib/pastedGame.ts` — `sniffPastedInput()`, `MAX_PASTED_INPUT_LENGTH`, types `PasteParseResult`, `PastedGameHeaders`, `PastedPgnResult`, `PastedFenResult`
- `frontend/src/lib/pastedGame.test.ts`
- `frontend/src/components/analysis/PasteModal.tsx` — `PasteModal` component, `PasteModalProps`
- `frontend/src/components/analysis/__tests__/PasteModal.test.tsx`
- `frontend/src/hooks/usePasteGame.ts` — `useSavePastedGame()` (Plan 03)
- `frontend/src/hooks/__tests__/usePasteGame.test.tsx` (Plan 03)

**Frontend symbols added to existing files**
- `frontend/src/pages/Analysis.tsx` — `pastedHeaders` state, `handlePasteLoad`, `PASTE_MODAL_*` local constants
- `frontend/src/types/api.ts` — `SavePastedGameRequest`, `SavePastedGameResponse` (Plan 03); `FilterState.includePasted` is added in `FilterPanel.tsx` (Plan 04)
- `frontend/src/components/filters/FilterPanel.tsx` — `showPastedChip` prop, `FilterState.includePasted` field (Plan 04)
- `frontend/src/hooks/useLibrary.ts` — `include_pasted` wire param in `buildLibraryParams` (Plan 04)

**Backend files (new)**
- `app/services/store_paste_game_service.py` — `store_pasted_game()`, `_PASTE_PLATFORM` (Plan 03)
- `tests/services/test_normalize_pasted_game.py`, `tests/services/test_store_paste_game_service.py`, `tests/routers/test_imports_paste.py`, `tests/repositories/test_pasted_platform_exclusion.py`

**Backend symbols added to existing files**
- `app/schemas/normalization.py` — `"pgn"` added to the `Platform` Literal (Plan 02)
- `app/repositories/query_utils.py` — `"pgn"` added to `DEFAULT_EXCLUDED_PLATFORMS`; new keyword-only `include_pasted` parameter on `apply_game_filters()`; new `ANALYTICS_INCLUDED_PLATFORMS` constant (Plan 02)
- `app/services/normalization.py` — `normalize_pasted_game()`, `pasted_game_identity_hash()`, `canonical_root_fen()`, `parse_pgn_played_at()`, `MAX_PASTED_PGN_PLIES` (Plan 02)
- `app/schemas/imports.py` — `SavePastedGameRequest`, `SavePastedGameResponse`, `MAX_PASTED_PGN_LENGTH` (Plan 03)
- `app/routers/imports.py` — `POST /imports/paste` route handler `save_pasted_game` (Plan 03)
- `app/services/library_service.py` / `app/routers/library.py` — `include_pasted` parameter (Plan 04)

**New endpoint paths**
- `POST /imports/paste`

**New platform literal value**
- `'pgn'` (backend `Platform` Literal, `games.platform` column values, `DEFAULT_EXCLUDED_PLATFORMS`)

**New `data-testid` values**
- `analysis-btn-paste`, `paste-modal`, `paste-form`, `paste-textarea`, `paste-error`, `paste-save-error`, `paste-side-selector`, `paste-side-white`, `paste-side-black`, `btn-paste-load`, `btn-paste-analyze`, `paste-header-meta`
- `nav-analysis`, `drawer-nav-analysis` (derived automatically from the `NAV_ITEMS` entry)
- `filter-platform-pasted` (Plan 04), `library-pasted-badge-${gameId}` (Plan 04)

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "paste a FEN on /analysis" — one path only</name>
  <reversibility rating="reversible">The nav entry, the trigger site and the modal shell are each a local edit behind a stable component boundary; reverting is deleting one array entry and one component.</reversibility>
  <files>frontend/src/App.tsx, frontend/src/lib/pastedGame.ts, frontend/src/components/analysis/PasteModal.tsx, frontend/src/pages/Analysis.tsx, frontend/src/lib/pastedGame.test.ts</files>
  <read_first>
    - `frontend/src/App.tsx` lines 60-215 — `NAV_ITEMS`, `BOTTOM_NAV_ITEMS`, `ADMIN_NAV_ITEM`, `ROUTE_TITLES`, the WR-07 comment block on `IMPORT_EXEMPT_ROUTES`, `isNavLocked`, `isActive`, and `NavHeader`'s `data-testid={`nav-${to.slice(1)}`}` derivation
    - `frontend/src/App.tsx` — the `MobileMoreDrawer` component (its `drawer-nav-${to.slice(1)}` derivation) and the `isAnalysisRoute` mobile-shell takeover around lines 330-345 and 525-545
    - `frontend/src/components/feedback/FeedbackModal.tsx` (all 191 lines) — the Dialog composition, `onInteractOutside` draft guard, `handleOpenChange` draft-clear, inline `role="alert"` error, `DialogFooter` two-button shape
    - `frontend/src/lib/analysisUrl.ts` — `parseAnalysisFenParam` (the FEN validator to reuse) and the module doc comment recording the `fen > line` precedence D-03 protects
    - `frontend/src/pages/Analysis.tsx` lines 3595-3615 — the `movesCard` / `analysis-movelist-header` `CardHeader size="compact"` trigger site
    - `frontend/src/pages/Analysis.tsx` lines 560-575 — `isGameMode = gameId != null`
    - `frontend/src/components/ui/dialog.tsx`, `frontend/src/components/ui/textarea.tsx`, `frontend/src/components/ui/button.tsx`, `frontend/src/components/ui/card.tsx` — the primitives being composed; confirm `DialogFooter`'s `flex-col-reverse ... sm:flex-row sm:justify-end` and `Textarea`'s `field-sizing-content`
    - `frontend/src/lib/pgn.ts` — read it only to confirm `pgnToSanArray` is a naive regex splitter that MUST NOT be used here; the sniff uses chess.js `loadPgn`
    - `.planning/phases/208-analysis-fen-pgn-paste/208-UI-SPEC.md` § Interaction Contract 1-4 and § Copywriting Contract
  </read_first>
  <action>
    Wire the single thinnest path end to end: main nav → `/analysis` → trigger → modal → bare FEN → board.

    NAVIGATION (D-09/D-10, SC-8). In `frontend/src/App.tsx`: append exactly one entry
    `{ to: '/analysis', label: 'Analysis', Icon: Microscope }` to `NAV_ITEMS` as its LAST element, importing
    `Microscope` from `lucide-react`. Do NOT add it to `BOTTOM_NAV_ITEMS`. Add `'/analysis'` to the
    `IMPORT_EXEMPT_ROUTES` Set and extend that Set's existing JSDoc with a bullet for `/analysis` matching the
    `/bots` bullet's shape (the route is already ungated and guest-friendly; a locked nav entry pointing at an
    open route is incoherent). Add `if (to === '/analysis') return pathname.startsWith('/analysis');` to
    `isActive`. Place a short comment directly above BOTH `NAV_ITEMS` and `BOTTOM_NAV_ITEMS` stating that the
    two arrays are now intentionally different, that `NAV_ITEMS` feeds `NavHeader` plus `MobileMoreDrawer`
    while `BOTTOM_NAV_ITEMS` feeds `MobileBottomBar`, that Analysis is deliberately absent from the bottom bar,
    and that this divergence must not be "fixed" — cite WR-07 (the shipped bug from nav surfaces silently
    disagreeing, already documented on `IMPORT_EXEMPT_ROUTES`). `ROUTE_TITLES` already carries `'/analysis'` —
    do not touch it. `data-testid` values `nav-analysis` and `drawer-nav-analysis` derive automatically from
    `to.slice(1)`; verify both surfaces derive them and add nothing manually.

    SNIFF HELPER. Create `frontend/src/lib/pastedGame.ts` exporting:
    `MAX_PASTED_INPUT_LENGTH = 100_000` (mirrors the backend bound this phase adds, `MAX_PASTED_PGN_LENGTH`);
    `interface PastedGameHeaders { white: string | null; black: string | null; whiteElo: number | null;
    blackElo: number | null; result: string | null; date: string | null }`;
    a discriminated union `type PasteParseResult =
    { kind: 'empty' } | { kind: 'fen'; fen: string } | { kind: 'pgn'; sans: string[]; rootFen: string;
    headers: PastedGameHeaders; pgn: string } | { kind: 'error' }`;
    and `sniffPastedInput(raw: string): PasteParseResult`.
    Normalize the input first, in this order: strip a leading U+FEFF byte-order mark, replace CRLF and CR
    with LF, replace U+00A0 (non-breaking space) and U+2007/U+202F with an ordinary space, fold the
    typographic quote code points U+2018/U+2019 to an ASCII apostrophe and U+201C/U+201D to an ASCII
    double quote, then trim. Return `{ kind: 'empty' }` when the normalized string is empty, and
    `{ kind: 'error' }` when it exceeds `MAX_PASTED_INPUT_LENGTH`.
    Sniffing order, from CONTEXT.md's measured chess.js 1.4 table: construct a NEW `Chess()` and call
    `loadPgn(normalized)` inside a try. On success read `history()`; if length is 0 return `{ kind: 'error' }`
    (empty movetext and headers-only both parse successfully with zero moves, so the throw path never fires
    for them and they must be rejected explicitly). On success with moves, build the result from a SECOND,
    freshly constructed `Chess()` instance — never from the instance that could have thrown — reading
    `getHeaders()` for `White`/`Black`/`WhiteElo`/`BlackElo`/`Result`/`Date` (falling back to `UTCDate` for
    date), coercing absent or `?`-filled header values to `null`, and coercing Elo values to a number only
    when `Number.isFinite`. Derive `rootFen` from the `SetUp`/`FEN` header pair when present (validated
    through `parseAnalysisFenParam`), else the standard start FEN.
    On any throw from `loadPgn`, DISCARD that instance entirely — never read `history()` or any other state
    from it (D-21 landmine: chess.js retains the moves that parsed before throwing, so reading a thrown-on
    instance silently loads a truncated game, which is exactly the failure SC-3 forbids). Fall through to
    `parseAnalysisFenParam(normalized)`; a non-null result yields `{ kind: 'fen', fen }`, otherwise
    `{ kind: 'error' }`.
    Keep `sniffPastedInput` a pure function with no module-level mutable state.

    MODAL SHELL. Create `frontend/src/components/analysis/PasteModal.tsx` exporting `PasteModal`, props
    `{ open: boolean; onOpenChange: (open: boolean) => void; onLoad: (result: PasteParseResult) => void }`.
    Compose `Dialog`/`DialogContent`/`DialogHeader`/`DialogTitle`/`DialogDescription`/`DialogFooter` exactly as
    `FeedbackModal` does, with `data-testid="paste-modal"` and `className="sm:max-w-md"` on `DialogContent`.
    Nest a `<form onSubmit=...>` carrying `data-testid="paste-form"`. Copy `FeedbackModal`'s `onInteractOutside`
    draft guard verbatim, gating on the textarea being non-empty, and copy its `handleOpenChange` draft-clear
    (reset text, side selection and error on close). Title "Paste a FEN or PGN"; description "Paste a position
    (FEN) or a full game (PGN). We'll figure out which."; visible `<label htmlFor="paste-input"
    className="text-sm font-medium">` reading "Position or game". Use the shadcn `Textarea` primitive with
    `id="paste-input"`, `data-testid="paste-textarea"`, `aria-label="FEN or PGN"`, `rows={6}`,
    `maxLength={MAX_PASTED_INPUT_LENGTH}`, and an explicit max-height plus `overflow-y-auto` so
    `field-sizing-content` cannot grow `DialogContent` past the viewport.
    Run `sniffPastedInput` on every `onChange` and derive the footer from the result. For THIS task only the
    `empty` / `fen` / `error` branches need behavior: `empty` → the primary Button (`variant="default"`,
    `data-testid="btn-paste-load"`, label "Load") is `disabled` and no error renders; `fen` → Load enabled;
    `error` → Load disabled and a `<p className="text-sm text-destructive" role="alert"
    data-testid="paste-error">` renders the exact literal `Couldn't read that as a FEN or PGN.`. Submitting
    the form calls `onLoad(result)` then closes the modal. Task 2 adds the `pgn` branch. There is no format
    toggle control anywhere in this component (D-01).

    TRIGGER + BOARD WIRE. The trigger lives in the move-list card header, not the board toolbar (D-19).
    In `frontend/src/pages/Analysis.tsx`, inside the `movesCard` `CardHeader
    size="compact" data-testid="analysis-movelist-header"`, add
    `<Button variant="ghost" size="default" className="ml-auto gap-1" data-testid="analysis-btn-paste">`
    with a `ClipboardPaste` lucide icon and the label "Paste", opening a `PasteModal` held in local
    `useState`. Render the trigger unconditionally in every mode including `?game_id=` game mode (D-20) —
    no gating on `isGameMode`. Implement `handlePasteLoad(result)`: for `kind === 'fen'`, seed the board's
    free-play root with that FEN through the same board API the existing `?fen=` seeding path uses. Do NOT
    write `?fen=` or `?line=` back to the URL and do not call any navigation helper (D-03) — the
    `fen > line` precedence documented in `analysisUrl.ts` must stay untouched.
  </action>
  <verify>
    <automated>(cd frontend && npm test -- --run src/lib/pastedGame.test.ts && npm run build && npm run lint)</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/lib/pastedGame.ts` contains `export function sniffPastedInput(`
    - `frontend/src/lib/pastedGame.ts` contains `export const MAX_PASTED_INPUT_LENGTH = 100_000`
    - `grep -c "'/analysis'" frontend/src/App.tsx` is at least 3 (NAV_ITEMS entry, IMPORT_EXEMPT_ROUTES member, isActive clause)
    - `node -e "const s=require('fs').readFileSync('frontend/src/App.tsx','utf8');const b=s.slice(s.indexOf('const BOTTOM_NAV_ITEMS'), s.indexOf('const ADMIN_NAV_ITEM'));process.exit(b.includes('/analysis')?1:0)"` exits 0 — `/analysis` is absent from the `BOTTOM_NAV_ITEMS` literal
    - `frontend/src/App.tsx` `BOTTOM_NAV_ITEMS` still declares exactly 5 entries
    - `frontend/src/components/analysis/PasteModal.tsx` contains `data-testid="paste-modal"`, `data-testid="paste-form"`, `data-testid="paste-textarea"`, `data-testid="paste-error"`, `data-testid="btn-paste-load"`
    - `frontend/src/components/analysis/PasteModal.tsx` contains the exact literal `Couldn't read that as a FEN or PGN.`
    - `grep -c 'text-xs' frontend/src/components/analysis/PasteModal.tsx frontend/src/lib/pastedGame.ts` reports 0 for both files
    - `grep -c 'dangerouslySetInnerHTML' frontend/src/components/analysis/PasteModal.tsx` is 0
    - `frontend/src/pages/Analysis.tsx` contains `data-testid="analysis-btn-paste"`
    - `pastedGame.test.ts` asserts, with one test per row: bare FEN → `kind: 'fen'`; valid movetext → `kind: 'pgn'`; `1. e4 e5 2. Nf3 Nc6 3. Nh6 d6` → `kind: 'error'` AND `sans` is not present on the result (the truncation landmine); `3. Qh9` → `kind: 'error'`; `hello world this is not chess` → `kind: 'error'`; empty string → `kind: 'empty'`; whitespace-only → `kind: 'empty'`; headers-only with no movetext → `kind: 'error'`; a `[SetUp "1"]`+`[FEN ...]` PGN → `rootFen` equals the header FEN; a `[SetUp]` PGN with a Black-to-move root → parses with correct SAN; a BOM-prefixed, CRLF-lined PGN containing non-breaking spaces and typographic quotes → parses identically to its plain-ASCII equivalent; a 100_001-character input → `kind: 'error'`
    - `pastedGame.test.ts` asserts `sniffPastedInput(x)` called twice on the same input returns deep-equal results
    - `npm run build` (tsc) exits 0 and `npm run lint` exits 0
  </acceptance_criteria>
  <done>From the desktop header or the mobile More drawer a user reaches `/analysis`, clicks Paste in the Moves card header, pastes a bare FEN, presses Load, and that position appears on the board. No network request fires and the URL is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the slice to PGN — mainline, headers, side selector, ephemeral player info</name>
  <files>frontend/src/lib/pastedGame.ts, frontend/src/components/analysis/PasteModal.tsx, frontend/src/pages/Analysis.tsx, frontend/src/lib/pastedGame.test.ts</files>
  <read_first>
    - `frontend/src/lib/pastedGame.ts` and `frontend/src/components/analysis/PasteModal.tsx` as written by Task 1
    - `frontend/src/hooks/useAnalysisBoard.ts` — the `loadMainLine(sans: string[], newRootFen: string): void` signature and its surrounding doc comments, plus the `STARTING_FEN` constant
    - `frontend/src/pages/Analysis.tsx` lines 3168-3205 — the `isGameMode && gameData` PlayerBar gating in the board column (both the top `boardHeaderRow` and the bottom `boardFooterRow` call sites)
    - `frontend/src/components/board/PlayerBar.tsx` — its full prop contract (`isWhite`, `name`, `rating`, `clockSeconds`, `testId`) and the `clockSeconds === null` clock-hiding branch
    - `frontend/src/components/filters/FilterPanel.tsx` lines 283-291 — the White/Black "Played as" `ToggleGroup`, the direct structural analog for the side selector including its `min-h-11 sm:min-h-0 flex-1 text-sm` item override
    - `.planning/phases/208-analysis-fen-pgn-paste/208-UI-SPEC.md` § Interaction Contract 4, 5 and 6
  </read_first>
  <behavior>
    - Given valid movetext with >= 1 move, `sniffPastedInput` returns `kind: 'pgn'` with `sans.length === history().length` and headers populated from `getHeaders()`.
    - Given a ChessBase-style PGN with RAVs, NAGs and brace comments, the result's `sans` contains only mainline moves and no error is produced.
    - Given a PGN with no `WhiteElo`, the header object's `whiteElo` is `null` (not `NaN`, not `undefined`, not the string "?").
    - Given a PGN whose `Date` header is `????.??.??`, `headers.date` is `null`.
    - The modal in the `pgn` state renders the side selector defaulting to `white` and both footer buttons; in the `fen` state it renders neither the side selector nor the secondary button.
    - Loading a pasted PGN renders the player-info area with both names, both ratings when present, and the result plus date on the top row only.
  </behavior>
  <action>
    Extend `sniffPastedInput`'s `pgn` branch to fully populate `sans` (from `history()` on the fresh instance)
    and `headers`. Coerce a header value that is absent, empty, `?`, or entirely `?`/`.` characters (the
    conventional PGN unknown markers, including `????.??.??` for Date) to `null`. Parse `WhiteElo`/`BlackElo`
    with `Number.parseInt` and keep the value only when `Number.isFinite`. Prefer `UTCDate` over `Date` when
    both are present. RAVs, NAGs and brace comments need no special handling — chess.js drops them from
    `history()` already (D-02); assert that in a test rather than writing parsing code for them.

    In `PasteModal.tsx`, complete the four-state machine from UI-SPEC § Interaction Contract 4. On the `pgn`
    state: render the side selector — a `ToggleGroup type="single" variant="outline" size="sm"
    data-testid="paste-side-selector" className="w-full"` with two `ToggleGroupItem`s
    (`value="white"` / `data-testid="paste-side-white"` / label "White", and the black equivalent), each
    carrying `className="min-h-11 sm:min-h-0 flex-1 text-sm"`. That size override is mandatory, not
    decorative: `size="sm"` on `ToggleGroupItem` otherwise renders below the 14px floor. Default the
    selection to `white` (D-06). Above it place a `<p className="text-sm font-medium">` label reading
    "Which side is yours?". Directly below the toggle render one meta line,
    `data-testid="paste-header-meta"`, `text-sm text-muted-foreground`, of the form
    `White: {name} ({elo}) · Black: {name} ({elo})`, omitting each rating parenthetical when that Elo is
    null and wrapping EACH name in its own truncating element (per-name truncation, not line truncation).
    On the `pgn` state also render the secondary Button `variant="brand-outline"`,
    `data-testid="btn-paste-analyze"`, label "Analyze full game", placed BEFORE the primary Load button in
    DOM order so `DialogFooter`'s `flex-col-reverse` puts Load topmost on mobile and rightmost on desktop.
    In this plan the secondary button's click handler is not yet wired to a request — Plan 03 owns that; give
    it a no-op handler and leave it enabled. Do not use `variant="ghost"` for it and do not hand-roll its
    colors. The `fen` state renders neither the side selector nor the secondary button (a bare FEN has no
    moves, so nothing can be persisted).

    In `Analysis.tsx`, extend `handlePasteLoad` for `kind === 'pgn'`: call
    `useAnalysisBoard`'s `loadMainLine(result.sans, result.rootFen)`, and store the parsed headers plus the
    chosen side in a `pastedHeaders` state object, cleared whenever the board is reset or a real
    `?game_id=` game loads. Render the ephemeral player info by widening the existing
    `isGameMode && gameData` PlayerBar conditions to also fire when `pastedHeaders` is non-null, sourcing
    `name` and `rating` from the parsed headers instead of `gameData`. Do NOT synthesize a fake `gameData`
    object. `clockSeconds` is always `null` for a pasted game (D-07, untimed) — `PlayerBar` already hides the
    clock for null, and that freed right-aligned slot is where the top row renders
    `"{Result} · {Date}"` as `text-sm text-muted-foreground`, on the TOP player row only, omitting either
    part when its header was null. Player names must truncate rather than wrap. Board orientation follows
    the selected side. When `kind === 'fen'` no player info renders at all, matching today's free-play
    behavior.
  </action>
  <verify>
    <automated>(cd frontend && npm test -- --run src/lib/pastedGame.test.ts && npm run build && npm run lint)</automated>
  </verify>
  <acceptance_criteria>
    - `pastedGame.test.ts` asserts a ChessBase-style PGN containing at least one RAV, one NAG and one brace comment returns `kind: 'pgn'` with `sans` equal to the mainline-only SAN list
    - `pastedGame.test.ts` asserts a PGN with no `WhiteElo` header yields `whiteElo === null`, and a PGN with `Date "????.??.??"` yields `date === null`
    - `pastedGame.test.ts` asserts headerless movetext yields `kind: 'pgn'` with `white === null` and `black === null`
    - `pastedGame.test.ts` asserts an accented Latin name and a CJK name in the `White`/`Black` headers round-trip to identical strings on the result
    - `PasteModal.tsx` contains `data-testid="paste-side-selector"`, `data-testid="paste-side-white"`, `data-testid="paste-side-black"`, `data-testid="btn-paste-analyze"`, `data-testid="paste-header-meta"`
    - `PasteModal.tsx` contains the literals `min-h-11 sm:min-h-0 flex-1 text-sm`, `variant="brand-outline"` and `Analyze full game`
    - In `PasteModal.tsx` the index of `btn-paste-analyze` is LESS than the index of `btn-paste-load` (secondary-then-primary DOM order)
    - `grep -c 'text-xs' frontend/src/components/analysis/PasteModal.tsx` is 0
    - `grep -c 'pgnToSanArray' frontend/src/lib/pastedGame.ts frontend/src/components/analysis/PasteModal.tsx` is 0 for both — the naive regex splitter in `lib/pgn.ts` is not used
    - `Analysis.tsx` calls `loadMainLine(` with the sniffed `sans` and `rootFen`
    - `grep -Ec 'buildAnalysisFenUrl|buildAnalysisLineUrl|setSearchParams' frontend/src/pages/Analysis.tsx frontend/src/components/analysis/PasteModal.tsx` reports 0 for both files — no `?fen=`/`?line=` write-back exists on any paste path (D-03). Verified safe as a file-wide gate: none of those three symbols appears in `Analysis.tsx` today, and Plan 03 adds only `buildGameAnalysisUrl`, which is deliberately not in this pattern.
    - `npm run build` exits 0 and `npm run lint` exits 0
  </acceptance_criteria>
  <done>Pasting a PGN loads its full mainline with the correct root (including a `[SetUp]` Black-to-move root), the side selector appears defaulting to White, and the board's player-info area shows both names, both ratings, the result and the date without any saved game row existing.</done>
</task>

<task type="auto">
  <name>Task 3: Modal + nav test coverage, and the 375px / testid / type-floor sweep</name>
  <files>frontend/src/components/analysis/__tests__/PasteModal.test.tsx, frontend/src/App.test.tsx, frontend/src/components/analysis/PasteModal.tsx, frontend/src/pages/Analysis.tsx</files>
  <read_first>
    - `frontend/src/components/analysis/PasteModal.tsx` and `frontend/src/pages/Analysis.tsx` as written by Tasks 1-2
    - `frontend/src/App.test.tsx` — in particular the existing nav-surface assertions around `mobile-nav-more`, to learn whether any test enumerates the nav item lists and therefore needs updating for the new entry
    - `frontend/src/components/train/__tests__/TrainScheduleSettings.test.tsx` — the project's component-test conventions (render helpers, `@testing-library` usage, provider wrapping)
    - `CLAUDE.md` § Frontend § Browser Automation Rules — the `data-testid` naming convention and the ARIA/semantic-HTML requirements
  </read_first>
  <action>
    Write `frontend/src/components/analysis/__tests__/PasteModal.test.tsx` covering the four states from
    UI-SPEC § Interaction Contract 4 through the rendered DOM, not through the helper: empty textarea leaves
    Load disabled with no error node and no side selector; a valid FEN enables Load and still renders no side
    selector and no secondary button; a valid PGN renders the side selector defaulting to White plus both
    buttons; malformed input disables Load and renders the exact locked error literal in a node with
    `role="alert"`. Add a test that the modal body is a `<form>` element and that its testid differs from the
    `DialogContent` testid. Add a test that closing and reopening the modal clears the textarea, the side
    selection and any error. Add a test that an accented and a CJK player name reach the meta line unmangled,
    and one that a PGN missing `WhiteElo` renders no empty parentheses and no literal "undefined".

    Update `frontend/src/App.test.tsx`: assert that `NavHeader` renders `nav-analysis`, that the mobile More
    drawer renders `drawer-nav-analysis`, that the mobile bottom bar renders no analysis entry, and that
    `nav-analysis` is NOT rendered with the locked treatment for a profile with zero imported games. If an
    existing test enumerates the nav item list by count or exact contents, update it rather than working
    around it.

    Then sweep the two components this plan touched: confirm every interactive element introduced carries a
    `data-testid` in the project's kebab-case component-prefixed form, that the icon-only trigger is not
    icon-only (it carries the visible label "Paste", so no `aria-label` is required — if the label is ever
    dropped, an `aria-label` becomes mandatory), that no introduced element uses a Tailwind font-size utility
    below the 14px floor, and that no button hand-rolls its colors with a `bg-` class instead of a `Button`
    variant. Fix anything the sweep finds in `PasteModal.tsx` / `Analysis.tsx` rather than recording it.
  </action>
  <verify>
    <automated>(cd frontend && npm test -- --run src/components/analysis/__tests__/PasteModal.test.tsx src/App.test.tsx src/lib/pastedGame.test.ts && npm run lint && npm run build && npm run knip)</automated>
  </verify>
  <acceptance_criteria>
    - `(cd frontend && npm test -- --run src/components/analysis/__tests__/PasteModal.test.tsx)` exits 0 with at least 8 passing tests
    - `(cd frontend && npm test -- --run src/App.test.tsx)` exits 0
    - `App.test.tsx` contains assertions on all three of `nav-analysis`, `drawer-nav-analysis`, and the absence of an analysis entry in the bottom-bar surface
    - `grep -rc 'text-xs' frontend/src/components/analysis/` reports 0 for every file in that directory
    - `grep -REc 'className="[^"]*bg-(brand|primary|blue|green|red)' frontend/src/components/analysis/PasteModal.tsx` is 0 — button color comes from `Button` variants only
    - `(cd frontend && npm run knip)` exits 0 — no dead exports introduced
    - `(cd frontend && npm run build)` exits 0
  </acceptance_criteria>
  <done>The modal's four states, the draft-clear behavior, the non-ASCII header path and the three nav surfaces are all covered by passing tests; lint, tsc, and knip are clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| clipboard → PasteModal textarea | Fully untrusted text of arbitrary origin (a book, a broadcast site, a stranger's message) enters the app here. |
| parsed PGN headers → rendered DOM | Attacker-influenced strings (player names, event, site) reach the PlayerBar and the meta line. |
| PasteModal → chess.js | Untrusted text is handed to a third-party parser inside the render loop. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-208-01 | Information Disclosure | `PasteModal` meta line + `PlayerBar` header rendering | medium | mitigate | Header values are rendered as React text children only, which React escapes. No raw-HTML injection prop is used anywhere in `frontend/src/components/analysis/`; asserted by a zero-match grep in Task 1's acceptance criteria. |
| T-208-02 | Denial of Service | client-side `loadPgn` on pathological input | medium | mitigate | `MAX_PASTED_INPUT_LENGTH = 100_000` is enforced twice — as the textarea's `maxLength` and as an explicit length check inside `sniffPastedInput` before any parse — so a paste larger than the bound short-circuits to `error` without reaching chess.js. |
| T-208-03 | Tampering | truncated-game load via a thrown-on chess.js instance | high | mitigate | The D-21 landmine: a caught `loadPgn` throw leaves the parsed prefix readable on the instance. The instance is discarded on throw and the success path builds from a second, freshly constructed instance. Task 1's acceptance criteria include the `3. Nh6` fixture asserting zero moves survive an error. |
| T-208-04 | Spoofing | untrusted headers presented as authoritative game metadata | low | accept | A pasted game is by design frequently not the user's own; the "Pasted" badge (Plan 04) and the always-excluded platform value are the product-level answer. No cryptographic provenance is in scope. |
| T-208-SC | Tampering | npm/pip/cargo installs | high | accept | The package-legitimacy gate does not fire for this phase: no package-manager install task exists. `chess.js`, `lucide-react` and every shadcn primitive used (`dialog`, `button`, `textarea`, `toggle-group`, `card`, `badge`) are already present — UI-SPEC § Registry Safety records "no new component installs" and no new Python dependency is added (the D-16 hash uses stdlib `hashlib`). Recorded, not skipped. |
</threat_model>

<verification>
- `(cd frontend && npm run lint && npm test -- --run && npm run build && npm run knip)` all exit 0.
- Manual 375px pass at a mobile viewport: land on `/analysis` from the More drawer, open the modal, paste a long PGN, confirm the modal never exceeds the viewport height and the back button returns sanely to the drawer's origin route.
- The two `verification: backstop` truths above are viewport measurements and cannot be asserted from the spec; at verify time an unproven statement routes to `insufficient_spec → human_needed` rather than passing silently.
</verification>

<success_criteria>
- PASTE-01, PASTE-02, PASTE-03, PASTE-08 fully satisfied; PASTE-09 satisfied for every element this plan introduces.
- `/analysis` reachable from the desktop header and the mobile More drawer, clickable at zero games, highlighting when active, with the mobile bottom bar byte-identically 5 entries.
- A malformed paste never loads a partial mainline.
- Zero DB writes and zero URL mutations from any path in this plan.
</success_criteria>

<output>
Create `.planning/phases/208-analysis-fen-pgn-paste/208-01-SUMMARY.md` when done.
</output>
