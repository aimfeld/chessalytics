---
status: testing
phase: 208-analysis-fen-pgn-paste
source: [208-VERIFICATION.md]
started: 2026-08-08T19:20:00Z
updated: 2026-08-08T19:20:00Z
---

## Current Test

number: 1
name: CR-01 fix confirmation — same-page game switch reseeds the board
expected: |
  Board/move-list update to the new game, matching the URL and the already-correct
  PlayerBar/eval chart/flaw panel. No stale prior-game position lingers.
awaiting: user response

## Tests

### 1. CR-01 fix confirmation — same-page game switch reseeds the board
expected: View `/analysis?game_id=OLD`, paste a different PGN, click "Analyze full game" directly (without first clicking "Load"). After the URL becomes `/analysis?game_id=NEW`, the board and move list show the NEW game. Also try browser back/forward between two already-saved games while staying on `/analysis`.
result: [pending]
note: Covered by 2 automated regression tests, independently confirmed red-if-removed by the verifier. This is a live-browser sanity check on an already-proven fix, not an open bug.

### 2. Library cross-surface containment (208-04 D4)
expected: On Library, enable the "Pasted" chip — pasted games appear on BOTH the Games and Flaws tabs. Then navigate to Openings, Endgames, and GlobalStats without resetting filters: no "Pasted" chip renders on any of the three, and no pasted game influences any number shown there.
result: [pending]
note: The suite proves the same containment at code level (D-14 zero-match greps, backend route assertions), but no autonomous run exercised it against real pasted-game data.

### 3. UI-SPEC backstop E1/E3 — trigger label and side-selector meta line at 375px
expected: The movelist header renders the `ClipboardPaste` icon + "PGN/FEN" alongside the "Moves" title at 375px without wrapping or truncating either, on BOTH the desktop moves card and the mobile Moves tab. (The label was widened from "Paste" to "PGN/FEN" after execution, so this fit is no longer the one the executor measured.)
result: [pending]

### 3b. UI-SPEC backstop E3 — side-selector meta line at 375px
expected: With the modal open on a Valid-PGN paste whose White/Black header names are realistically long, the meta line ("White: {name} ({elo}) · Black: {name} ({elo})") truncates each name individually at a 375px viewport. The White/Black ToggleGroupItems stay equal-width; nothing overflows or wraps the modal.
result: [pending]
note: Viewport-rendering measurement — UI-SPEC records this as verification:backstop, not assertable from unit tests.

### 4. UI-SPEC backstop E6 — ephemeral player-info row at 375px
expected: With an ephemeral pasted-PGN player-info row showing a long truncating player name plus the "{Result} · {Date}" content in the freed clock slot, both fit on the same PlayerBar row at 375px with no overflow, wrap, or clipping.
result: [pending]
note: Same class as test 3 — a viewport measurement UI-SPEC explicitly routes to human verification.

## Summary

total: 5
passed: 0
issues: 0
pending: 5
skipped: 0
blocked: 0

## Gaps
