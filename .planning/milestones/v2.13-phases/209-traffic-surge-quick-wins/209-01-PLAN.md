---
phase: 209-traffic-surge-quick-wins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/guest_service.py
  - app/services/user_benchmark_percentiles_service.py
  - tests/test_guest_auth.py
  - tests/services/test_percentile_compute_gate.py
autonomous: true
requirements: [SURGE-02, SURGE-05, SURGE-06, SURGE-07]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "POST /auth/guest/promote/email still returns a promoted user + JWT, and the argon2 hash for that promotion is computed on a worker thread rather than the event-loop thread (SURGE-02, D-02)"
    - "Calling promote_guest_with_password a second time for the same user still raises ValueError('Not a guest user') — moving the hash off the loop does not change the idempotency contract (SURGE-02, idempotency edge)"
    - "Two concurrent promote_guest_with_password calls for two DIFFERENT guests each produce a distinct, verifying argon2 hash — the module-level _password_helper carries no mutable state (SURGE-02, concurrency edge)"
    - "POST /auth/guest/create still performs zero password hashing: create_guest_user writes an empty hashed_password and never reaches _password_helper (D-02, seed correction)"
    - "The argon2 algorithm and cost parameters are unchanged by this phase — only the thread the unchanged call runs on changes (D-01 locked constraint 6)"
    - "No more than PERCENTILE_COMPUTE_LIMIT (2) compute_stage_a/compute_stage_b bodies execute concurrently, counted across BOTH trigger sites (import_service.py completion path and eval_drain.py cold-drain path) (SURGE-05, D-06)"
    - "percentile_compute_registry.is_computing stays true for a user whose compute is waiting on the semaphore, so the Tier-2 readiness gate does not unlock early (SURGE-05, D-06)"
    - "app/core/rate_limiters.py is byte-identical to its pre-phase content — md5 b1b39c57ec38c9819639297cac04d392 (SURGE-06)"
    - "Neither new semaphore constant lives in app/core/rate_limiters.py; PERCENTILE_COMPUTE_LIMIT lives in app/services/user_benchmark_percentiles_service.py and IMPORT_CONCURRENCY_LIMIT in app/services/import_service.py, so byte-identity cannot be defeated by an additive edit (SURGE-06, precision edge)"
    - "Each production change in this plan is proven by reverting it and observing a NAMED test go red, never by symbol presence, grep, or a green type-check (SURGE-07, D-07)"
    - "Two production changes that live in the same file are each reverted SEPARATELY and each revert turns a distinct, named test red; a single test covering both does not satisfy SURGE-07 (SURGE-07, adjacency edge, D-07)"
    - "A production change with no named revert-proof is reported as a phase gap, not a pass (SURGE-07, empty edge, D-07)"
    - "The phase mutation-test ledger lists one row per production change in a fixed order (poll backoff, to_thread wrap, import cap + queued + started_at re-stamp, percentile gate); two rows proven by the same test file must name distinct test functions (SURGE-07, ordering edge, D-07)"
    - statement: "Two simultaneous promote_guest_with_password calls for the SAME guest never leave a half-promoted row: each call performs one UPDATE plus commit, so the row settles is_guest=false carrying one of the two submitted emails, and the to_thread wrap adds no new interleaving window because _password_helper holds no mutable state"
      verification: backstop
  prohibitions:
    - statement: "MUST NOT lower argon2 cost parameters or swap the hashing algorithm to make promotion faster — this phase changes only where the unchanged hash call runs (D-01 locked constraint 6)"
      status: unverified
    - statement: "MUST NOT accept symbol presence, a grep hit, or a green type-check as proof that a production change in this phase works (D-07)"
      status: unverified
    - statement: "MUST NOT implement register/login async hashing (a UserManager.create/authenticate override) — explicitly deferred (D-01)"
      status: unverified
    - statement: "MUST NOT edit app/core/rate_limiters.py, and MUST NOT remove or loosen CHESSCOM_SEMAPHORE_LIMIT / LICHESS_SEMAPHORE_LIMIT (D-01, D-03)"
      status: unverified
    - statement: "MUST NOT tune Postgres, add a load-test rig, or touch eval/analysis throughput as part of this phase (D-01)"
      status: unverified
    - statement: "MUST NOT introduce Web Push as a readiness signal (D-01 locked constraint 2)"
      status: unverified
    - statement: "MUST NOT implement any of seed item 6 (to_thread PGN parse, uvicorn --workers, API rate limiting) (D-01)"
      status: unverified
  assumptions:
    - "FLAGGED — unresolved probe (SURGE-05, category `unclassified`): the edge probe returned `unclassified — review manually` for SURGE-05 and no acceptance criterion was auto-derived. Manual review at planning: the gate's only parameter is its limit, so its observable contract is the concurrency-bound truth above plus the is_computing ordering truth; no additional boundary / ordering / precision edge was identified. Recorded as a flagged assumption rather than silently resolved."
    - "FLAGGED — assumption-delta detector reported detected=true, but all three signals are lexical false positives ('It also carries…', 'reserved as fallback only', 'second fetch of maia3_simplified.onnx'). No singular→plural, required→optional, or derived→chosen identity transition exists in this phase. Visible skip recorded; no identity question raised."
    - "FLAGGED — research assumption A1: pwdlib / argon2-cffi thread-safety was verified only at the Python wrapper level, not exhaustively against the C-extension internals. The two-different-guests concurrency truth is the practical check."
  artifacts:
    - path: "app/services/guest_service.py"
      provides: "promote_guest_with_password hashing the password on a worker thread"
      contains: "asyncio.to_thread"
    - path: "app/services/user_benchmark_percentiles_service.py"
      provides: "PERCENTILE_COMPUTE_LIMIT + get_percentile_semaphore(), acquired inside compute_stage_a and compute_stage_b"
      contains: "get_percentile_semaphore"
    - path: "tests/test_guest_auth.py"
      provides: "thread-identity, idempotency and two-guest concurrency tests for the promotion hash"
      contains: "threading.get_ident"
    - path: "tests/services/test_percentile_compute_gate.py"
      provides: "peak-concurrency assertion for compute_stage_a / compute_stage_b"
      contains: "PERCENTILE_COMPUTE_LIMIT"
  key_links:
    - from: "app/services/user_benchmark_percentiles_service.py"
      to: "app/services/eval_drain.py"
      via: "the semaphore lives INSIDE compute_stage_b, so the cold-drain trigger site is gated without editing eval_drain.py"
      pattern: "get_percentile_semaphore"
    - from: "app/services/user_benchmark_percentiles_service.py"
      to: "app/services/import_service.py"
      via: "the import-completion trigger sites for compute_stage_a / compute_stage_b are gated by the same in-function semaphore"
      pattern: "compute_stage_[ab]"
---

<objective>
Land the two smallest, fully independent backend quick wins of Phase 209: move the one
argon2 hash in our own code off the event loop (D-02), and put a concurrency gate around
the 1,111.7 ms percentile computes that fire on import completion (D-06). Both are
proven by reverting them and watching a named test go red (D-07).

Purpose: a traffic spike must not be able to freeze the single event loop on a guest
promotion, and a burst of import completions must not be able to hold more than 2 of the
20 pooled DB connections in 1.1 s percentile queries.
Output: two production changes, three new backend tests, and the phase-level requirement
traceability table + source coverage audit that the rest of the phase is measured against.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/209-traffic-surge-quick-wins/209-CONTEXT.md
@.planning/phases/209-traffic-surge-quick-wins/209-RESEARCH.md
@.planning/phases/209-traffic-surge-quick-wins/209-PATTERNS.md
@CLAUDE.md
</context>

## Requirements

Phase 209 predates its milestone's REQUIREMENTS.md. The seven SURGE IDs are minted here,
one per ROADMAP Phase 209 Success Criterion, in order.

| ID | Success Criterion (ROADMAP Phase 209) | Plan / Task |
|----|----------------------------------------|-------------|
| SURGE-01 | Readiness poll's emitted interval sequence backs off once tier1 is true and tier2 outstanding, stays 3 s while tier1 is false, and stops after a total-duration cap; the test asserts the sequence and goes red on revert | 209-03 / T1, T2 |
| SURGE-02 | `POST /auth/guest/promote/email` hashes off the event loop; a test goes red when the wrapper is removed; register/login and `POST /auth/guest/create` untouched | 209-01 / T1 |
| SURGE-03 | `/maia/*` and `/engine/*` served from CDN cache with correct headers; `/maia/maia-worker.js` still `no-cache` | 209-04 / T1, T2 |
| SURGE-04 | No more than the configured cap of imports runs concurrently; the cap+1th job is visibly "queued", starts when a slot frees, and is never reaped while waiting | 209-02 / T1, T2, T3 |
| SURGE-05 | A burst of import completions never runs more than the semaphore's worth of concurrent `compute_stage_a`/`b` | 209-01 / T2 |
| SURGE-06 | The outbound chess.com/lichess rate-limiter semaphores are byte-identical to before the phase | 209-01 / T2 gate, 209-02 / T1 gate |
| SURGE-07 | Each production change is mutation-tested (revert → red) rather than accepted on symbol presence | all code plans; ledger below |

### Mutation-test ledger (SURGE-07, fixed order — one row per production change)

| # | Production change | Revert this | Named test that must go red | Plan |
|---|-------------------|-------------|------------------------------|------|
| 1 | Readiness poll backoff ladder + duration cap | the `refetchInterval` backoff branch in `useReadiness.ts` (return the flat 3 s interval) | `useReadiness > emits the backoff interval sequence once tier1 is true` | 209-03 |
| 2 | Guest-promotion hash moved to a worker thread | the `asyncio.to_thread` wrapper in `promote_guest_with_password` | `TestPromoteGuestWithPassword::test_promotion_hashes_off_the_event_loop_thread` | 209-01 |
| 3a | Global import concurrency cap + QUEUED state | the `async with get_import_semaphore():` wrapper in `run_import` | `TestImportConcurrencyCap::test_cap_plus_one_job_is_queued_until_a_slot_frees` | 209-02 |
| 3b | `started_at` re-stamp at slot acquisition | the `update_import_job(..., started_at=...)` call inside the semaphore block | `TestQueuedJobReaperExemption::test_restamped_job_survives_the_orphan_reaper` | 209-02 |
| 4 | Percentile compute concurrency gate | the `async with get_percentile_semaphore():` wrapper inside `compute_stage_a`/`compute_stage_b` | `test_stage_a_never_exceeds_the_configured_concurrency` | 209-01 |

Rows 3a and 3b live in the same file and MUST be reverted separately, each turning its own
named test red (SURGE-07 adjacency edge). A change with no row here is a phase gap.

## Source Coverage Audit

| SOURCE | ID | Feature / Requirement | Plan | Status |
|--------|----|----------------------|------|--------|
| GOAL | — | Readiness poll stops being unbounded standing load per open tab | 209-03 | COVERED |
| GOAL | — | The one password-hash call in our own code moves off the event loop | 209-01 | COVERED |
| GOAL | — | The vendored ML runtime moves off the origin NIC to a CDN | 209-04 | COVERED |
| GOAL | — | Post-import percentile computes are concurrency-gated | 209-01 | COVERED |
| GOAL | — | Imports beyond a global cap wait in a visible "queued" state | 209-02 | COVERED |
| REQ | SURGE-01 | Poll interval sequence + duration cap, revert-red test | 209-03 | COVERED |
| REQ | SURGE-02 | Promotion hash off the loop, revert-red test | 209-01 | COVERED |
| REQ | SURGE-03 | CDN cache-hit headers, worker file still no-cache | 209-04 | COVERED |
| REQ | SURGE-04 | Import cap + visible queued + reaper exemption | 209-02 | COVERED |
| REQ | SURGE-05 | Percentile compute concurrency bound | 209-01 | COVERED |
| REQ | SURGE-06 | Rate-limiter semaphores byte-identical | 209-01, 209-02 | COVERED |
| REQ | SURGE-07 | Every production change mutation-tested | all code plans (ledger above) | COVERED |
| RESEARCH | Pattern 1 | Lazy module-level semaphore mirrored, NOT added to rate_limiters.py | 209-01 T2, 209-02 T1 | COVERED |
| RESEARCH | Pattern 3 | Queued state in-memory only, no migration, three active-job call sites updated | 209-02 T1 | COVERED |
| RESEARCH | Pattern 4 | `started_at` re-stamp at slot acquisition closes the reaper gap | 209-02 T1, T2 | COVERED |
| RESEARCH | Pattern 5 | Semaphore inside `compute_stage_a`/`b`, covering BOTH trigger sites | 209-01 T2 | COVERED |
| RESEARCH | Pitfall 1 | `create_guest_user` never hashes — do not wrap it | 209-01 T1 | COVERED |
| RESEARCH | Pitfall 2 | Do not hard-stop the whole hook at tier1; three live tier2 consumers | 209-03 T1 | COVERED |
| RESEARCH | Pitfall 5 | `useEvalCoverage.ts` is out of scope | 209-03 (prohibition) | COVERED |
| RESEARCH | Pitfall 6 | `count_pending_evals` already indexed — no backend endpoint change | 209-03 (prohibition) | COVERED |
| RESEARCH | Pitfall 7 | `ImportStatusResponse.status` / `ImportStartedResponse.status` typed `str` | 209-02 T3 | COVERED |
| RESEARCH | Wave 0 gap | No existing bounded-concurrency assertion pattern | 209-01 T2, 209-02 T2 (see note) | COVERED |
| CONTEXT | D-01 | Quick-win cut only; deferred items stay deferred | all plans (prohibitions) | COVERED |
| CONTEXT | D-02 | Argon2 correction — wrap only `guest_service.py` promotion hash | 209-01 T1 | COVERED |
| CONTEXT | D-03 | Item 4 thin slice: semaphore + bare "queued", reaper exemption load-bearing | 209-02 | COVERED |
| CONTEXT | D-04 | Cloudflare free plan, mail records survive, Full (strict), respect-origin cache rule | 209-04 | COVERED |
| CONTEXT | D-05 | Poll backoff frontend-first; interval sequence is the tested artifact | 209-03 | COVERED |
| CONTEXT | D-06 | `Semaphore(2–3)` on the percentile computes | 209-01 T2 | COVERED |
| CONTEXT | D-07 | Mutation-test discipline; CDN exempt | all plans + ledger | COVERED |

All rows COVERED. Deferred Ideas in 209-CONTEXT.md are excluded by definition and are
carried as prohibitions rather than gaps.

**Documented deviation from RESEARCH Wave 0 gap:** research recommends one reusable
peak-concurrency test helper shared by SURGE-04a and SURGE-05. The two tests observe
different instruments — SURGE-05 counts concurrent bodies through an injected
`session_maker`, SURGE-04 counts concurrent `JobStatus.IN_PROGRESS` entries in the
in-memory `_jobs` registry — so a shared helper would need two call shapes for ~8 lines of
counter. Per CLAUDE.md ("don't split just to fit a signature") each plan keeps its own
local counter, and both plans stay in wave 1 with no cross-plan dependency.

## Probe Fallback Ledger (this plan's share: 6 of 16 items)

| Req | Category | Disposition |
|-----|----------|-------------|
| SURGE-02 | idempotency | resolved-explicit → `must_haves.truths` (second promotion raises ValueError) |
| SURGE-02 | concurrency | resolved-explicit (two different guests) + resolved-backstop (same guest, structured marker) |
| SURGE-05 | unclassified | UNRESOLVED → flagged assumption in `must_haves.assumptions` |
| SURGE-07 | adjacency | resolved-explicit → same-file changes reverted separately, distinct named tests |
| SURGE-07 | empty | resolved-explicit → a change with no revert-proof is a gap, not a pass |
| SURGE-07 | ordering | resolved-explicit → fixed ledger order, one row per change, distinct test functions |

Canon-referral breadcrumbs (recalled, NOT minted as prohibitions): password-storage
strength in general is canon — covered by `/gsd-secure-phase`; the queued-endpoint
access-control concern is canon — carried in 209-02's `<threat_model>` instead.

## Artifacts this phase produces (this plan's share)

- `PERCENTILE_COMPUTE_LIMIT` — new module constant in `app/services/user_benchmark_percentiles_service.py`, value `2`
- `_percentile_semaphore` — new module-level `asyncio.Semaphore | None`
- `get_percentile_semaphore()` — new lazy getter returning `asyncio.Semaphore`
- `tests/services/test_percentile_compute_gate.py` — new test module
- `TestPromoteGuestWithPassword::test_promotion_hashes_off_the_event_loop_thread` — new test
- `TestPromoteGuestWithPassword::test_second_promotion_of_the_same_user_raises` — new test
- `TestPromoteGuestWithPassword::test_concurrent_promotions_of_two_guests_produce_distinct_hashes` — new test

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move the guest-promotion argon2 hash onto a worker thread (SURGE-02, D-02)</name>
  <reversibility rating="reversible">One call site behind an unchanged function signature; undoing it is a one-line edit with no data or contract impact.</reversibility>
  <read_first>
    - app/services/guest_service.py (the full file — the target call is inside `promote_guest_with_password`; `create_guest_user` above it is the function that must stay untouched)
    - tests/test_guest_auth.py (class `TestPromoteGuestWithPassword` around line 294 — reuse its `db_session` fixture and construction style)
    - .planning/phases/209-traffic-surge-quick-wins/209-RESEARCH.md (Pattern 2 and Pitfall 1, plus the deterministic thread-identity test example under Code Examples)
    - .planning/phases/209-traffic-surge-quick-wins/209-CONTEXT.md (D-02 — the seed's funnel claim was corrected; only the promotion path hashes)
  </read_first>
  <behavior>
    - Test 1 (`test_promotion_hashes_off_the_event_loop_thread`): monkeypatch `guest_service._password_helper.hash` with a spy that records `threading.get_ident()`, run `promote_guest_with_password`, assert the recorded id differs from the test's own `threading.get_ident()`. Reverting the wrapper makes the ids equal → red.
    - Test 2 (`test_second_promotion_of_the_same_user_raises`): promote once, then call `promote_guest_with_password` again with the same user object and assert `ValueError` is raised (the row is no longer `is_guest`).
    - Test 3 (`test_concurrent_promotions_of_two_guests_produce_distinct_hashes`): create two guests, promote both, assert both stored hashes are non-empty, differ from each other, and each verifies against its own password via `PasswordHelper().verify_and_update`. Uses the real hasher (no monkeypatch) so it also proves the algorithm was not swapped.
  </behavior>
  <files>app/services/guest_service.py, tests/test_guest_auth.py</files>
  <action>
    Per D-02, wrap exactly one call: inside `promote_guest_with_password` in
    `app/services/guest_service.py`, the synchronous `_password_helper.hash(password)`
    assignment becomes an awaited `asyncio.to_thread` call passing the bound
    `_password_helper.hash` method and `password` as its argument. Add the `asyncio`
    import to the module's stdlib import block (the module currently imports only `uuid4`
    from the stdlib). Everything else in the function — the `is_guest` guard, the
    `select(User).where(User.email == email)` uniqueness check, the `sa_update(User)`
    statement, the commit, the re-fetch, and the `auth_backend.get_strategy()` token issue
    — stays exactly as it is, and the function's signature and return type are unchanged.

    Do NOT touch `create_guest_user`: it constructs `User(..., hashed_password="", ...)`
    and never reaches `_password_helper`, so there is nothing to move off the loop there
    (D-02 / RESEARCH Pitfall 1). Do NOT touch `app/users.py`, `UserManager.create`, or
    `authenticate` — register/login async hashing is deferred by D-01. Do NOT change the
    argon2 algorithm or its cost parameters; `PasswordHelper()` is constructed at module
    scope exactly as today.

    Add a short comment at the wrap site explaining what it fixes: the measured 65.9 ms
    argon2id hash previously ran on the single event loop, freezing every other request
    for its duration.

    Then extend `tests/test_guest_auth.py`'s existing `TestPromoteGuestWithPassword` class
    with the three tests named in the behavior block above, following the class's existing
    fixture style (`db_session`, `pytest.mark.asyncio`, in-function imports of
    `create_guest_user` / `promote_guest_with_password`). Use `threading.get_ident()` for
    the thread-identity assertion — do NOT assert on wall-clock timing, which is the
    project's known flake source.
  </action>
  <verify>
    <automated>uv run pytest tests/test_guest_auth.py::TestPromoteGuestWithPassword -x -q</automated>
    <automated>uv run ty check app/ tests/</automated>
    <automated>test "$(grep -c 'to_thread' app/services/guest_service.py)" -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_guest_auth.py::TestPromoteGuestWithPassword -x -q` passes with all three new tests collected.
    - `grep -c 'to_thread' app/services/guest_service.py` returns exactly `1` — the wrap exists once and only once.
    - `grep -n 'hashed_password=""' app/services/guest_service.py` still matches inside `create_guest_user` (the guest-creation path is unmodified).
    - `git diff app/users.py` is empty (register/login paths untouched, D-01).
    - MUTATION PROOF (SURGE-07 row 2): replacing the awaited `asyncio.to_thread(...)` call with a direct synchronous `_password_helper.hash(password)` call makes `test_promotion_hashes_off_the_event_loop_thread` FAIL (recorded thread id equals the test thread id). Record the observed failure output in the SUMMARY, then restore the wrapper.
    - `uv run ty check app/ tests/` reports zero errors.
  </acceptance_criteria>
  <done>The promotion path hashes on a worker thread, three named tests cover thread identity / idempotency / two-guest concurrency, and the revert-red proof is recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate compute_stage_a / compute_stage_b behind a concurrency semaphore (SURGE-05, D-06)</name>
  <reversibility rating="reversible">A module-level semaphore acquired inside two function bodies; removing it restores the previous behaviour with no schema, wire-format, or contract change.</reversibility>
  <read_first>
    - app/services/user_benchmark_percentiles_service.py (lines 390-561 — `compute_stage_a` and `compute_stage_b`, their `session_maker` DI parameter, and the `finally: percentile_compute_registry.clear(user_id)` block at the end of Stage B)
    - app/core/rate_limiters.py (READ ONLY — the lazy module-level semaphore shape to mirror; this file must not be edited)
    - app/services/import_service.py lines 700-750 (the two import-completion trigger sites and the `percentile_compute_registry.mark(...)`-before-`create_task` ordering)
    - app/services/eval_drain.py around line 384 (the SECOND, independent `compute_stage_b` trigger site — the reason the semaphore goes inside the function, not at a call site)
    - .planning/phases/209-traffic-surge-quick-wins/209-RESEARCH.md (Pattern 5 and Pitfall 4)
  </read_first>
  <behavior>
    - Test 1 (`test_stage_a_never_exceeds_the_configured_concurrency`): inject a fake `session_maker` whose `__aenter__` blocks on an `asyncio.Event` and increments/decrements a shared counter, launch `PERCENTILE_COMPUTE_LIMIT + 2` concurrent `compute_stage_a` calls, release the event, and assert the observed peak counter never exceeded `PERCENTILE_COMPUTE_LIMIT`.
    - Test 2 (`test_stage_b_shares_the_same_semaphore_as_stage_a`): launch a mix of `compute_stage_a` and `compute_stage_b` calls through the same blocking `session_maker` and assert the combined peak never exceeded `PERCENTILE_COMPUTE_LIMIT` — this is what proves the eval-drain trigger site is covered too.
    - Test 3 (`test_semaphore_getter_is_lazy_and_returns_one_shared_instance`): two calls to `get_percentile_semaphore()` return the same object, and the limit equals `PERCENTILE_COMPUTE_LIMIT`.
  </behavior>
  <files>app/services/user_benchmark_percentiles_service.py, tests/services/test_percentile_compute_gate.py</files>
  <action>
    In `app/services/user_benchmark_percentiles_service.py`, add a named constant
    `PERCENTILE_COMPUTE_LIMIT = 2` (D-06 permits 2–3; pick 2 so the 1,111.7 ms query can
    hold at most 2 of the 20 pooled connections), a module-level
    `_percentile_semaphore: asyncio.Semaphore | None = None`, and a
    `get_percentile_semaphore() -> asyncio.Semaphore` lazy getter that constructs the
    semaphore on first call. Mirror `app/core/rate_limiters.py`'s shape exactly, including
    the lazy-init rationale in the docstring (a Semaphore must not be constructed before
    the event loop starts). Add the `asyncio` import if the module does not already have
    it. Give the constant a comment recording the measured 1,111.7 ms mean and the 10+10
    pool size that motivate the value — no magic numbers (CLAUDE.md).

    Per RESEARCH Pattern 5, acquire the semaphore INSIDE both `compute_stage_a` and
    `compute_stage_b` — as an `async with get_percentile_semaphore():` block wrapping each
    function's existing body, placed after the `maker = session_maker if session_maker is
    not None else _default_session_maker` line and OUTSIDE the existing top-level
    `try/except asyncio.CancelledError/except Exception` so the existing Sentry capture
    semantics are unchanged. Do NOT wrap the `asyncio.create_task(...)` call sites in
    `app/services/import_service.py` or `app/services/eval_drain.py`: `compute_stage_b` has
    TWO independent trigger sites (import completion and the cold-drain zero-pending
    crossing) and only an in-function acquisition covers both by construction (Pitfall 4).
    Leave `import_service.py` and `eval_drain.py` unmodified in this plan.

    Leave the `percentile_compute_registry.mark(user_id)` / `clear(user_id)` ordering
    alone. `mark` is called at the trigger site before `create_task`, so `is_computing`
    correctly stays true for a user whose compute is waiting on the semaphore — that is
    the intended behaviour, not a bug to fix.

    Do NOT add either new constant to `app/core/rate_limiters.py`; SURGE-06 requires that
    file byte-identical and an additive edit would break it.

    Create `tests/services/test_percentile_compute_gate.py` with the three tests named in
    the behavior block. Follow the existing `tests/services/` module conventions
    (`pytest.mark.asyncio`, plain module-level test functions or a single class). Keep the
    peak counter local to this module — see the plan's documented deviation note; the
    import-cap test in 209-02 observes a different instrument and keeps its own counter.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_percentile_compute_gate.py -x -q</automated>
    <automated>uv run ty check app/ tests/</automated>
    <automated>md5sum app/core/rate_limiters.py | grep -q b1b39c57ec38c9819639297cac04d392</automated>
    <automated>test "$(grep -c 'get_percentile_semaphore()' app/services/user_benchmark_percentiles_service.py)" -eq 3</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/services/test_percentile_compute_gate.py -x -q` passes with all three tests collected.
    - `md5sum app/core/rate_limiters.py` equals `b1b39c57ec38c9819639297cac04d392` (SURGE-06 byte-identity, measured at planning time).
    - `grep -c 'get_percentile_semaphore()' app/services/user_benchmark_percentiles_service.py` returns `3` — one definition-site return plus one acquisition in each of the two compute functions.
    - `git diff app/services/eval_drain.py` and `git diff app/services/import_service.py` are both empty for this task (the gate is inside the compute functions, not at their call sites).
    - `grep -n 'PERCENTILE_COMPUTE_LIMIT = 2' app/services/user_benchmark_percentiles_service.py` matches exactly once.
    - MUTATION PROOF (SURGE-07 row 4): removing the `async with get_percentile_semaphore():` wrapper from `compute_stage_a` makes `test_stage_a_never_exceeds_the_configured_concurrency` FAIL with an observed peak above `PERCENTILE_COMPUTE_LIMIT`. Record the failure output in the SUMMARY, then restore the wrapper.
    - `uv run ty check app/ tests/` reports zero errors.
  </acceptance_criteria>
  <done>Both percentile compute entry points acquire one shared semaphore of limit 2, both trigger sites are covered by construction, rate_limiters.py is provably untouched, and the revert-red proof is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `POST /auth/guest/promote/email` | Untrusted email + password cross here into the promotion path this plan modifies. |
| event loop → worker thread | The argon2 hash computation now crosses a thread boundary; the password value crosses with it. |
| background task → DB connection pool | `compute_stage_a`/`compute_stage_b` cross here; the semaphore bounds how many do so at once. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-209-01-01 | Tampering | argon2 parameters in `promote_guest_with_password` | high | mitigate | Algorithm and cost parameters are unchanged; only the executing thread changes. `test_concurrent_promotions_of_two_guests_produce_distinct_hashes` uses the real hasher and verifies each hash, so a swapped or weakened hasher fails the test. Prohibition P2 records the constraint. |
| T-209-01-02 | Information Disclosure | plaintext password crossing into a worker thread | medium | mitigate | The password is passed as a call argument to `asyncio.to_thread`; it is never logged, never placed in a Sentry context, and never widened in scope beyond the existing function-local variable. The spy used in the thread-identity test records only `threading.get_ident()`, never the password. |
| T-209-01-03 | Denial of Service | default `ThreadPoolExecutor` behind `asyncio.to_thread` | medium | accept | The default executor is bounded at `min(32, cpu_count + 4)`; a promotion burst queues on the executor instead of blocking the loop, which is strictly better than today. No unbounded thread creation is introduced and no new endpoint is added. Server-side request rate limiting is out of scope by D-01. |
| T-209-01-04 | Denial of Service | percentile-compute semaphore starvation | low | accept | With a limit of 2, a long-running compute delays other users' percentile refreshes but cannot block request-serving paths (both computes are background tasks). No priority or fairness logic is in scope for the thin slice. |
| T-209-01-05 | Repudiation | Sentry capture semantics around the new `async with` block | low | mitigate | The semaphore acquisition sits outside the existing top-level `try/except`, so every existing `capture_exception` path is preserved verbatim; the acceptance criteria assert both compute functions still pass `ty` and their tests. |
| T-209-01-SC | Tampering | npm/pip/cargo installs | n/a | accept | This phase installs no packages in any ecosystem (RESEARCH: "Package Legitimacy Audit — not applicable"). The legitimacy gate has no subject; no `[ASSUMED]`/`[SUS]` checkpoint is required. |
</threat_model>

<verification>
- `uv run pytest tests/test_guest_auth.py tests/services/test_percentile_compute_gate.py -x -q` passes.
- `uv run ruff format app/ tests/` and `uv run ruff check app/ tests/ --fix` leave no further changes.
- `uv run ty check app/ tests/` reports zero errors.
- `md5sum app/core/rate_limiters.py` still equals `b1b39c57ec38c9819639297cac04d392`.
- Both mutation proofs (ledger rows 2 and 4) are recorded in the SUMMARY with the observed
  failure output, and the production code is restored afterwards.
</verification>

<success_criteria>
- SURGE-02: the promotion hash runs off the event-loop thread, proven by revert-red.
- SURGE-05: concurrent `compute_stage_a`/`compute_stage_b` bodies never exceed 2, across
  both trigger sites, proven by revert-red.
- SURGE-06: `app/core/rate_limiters.py` is byte-identical.
- SURGE-07: ledger rows 2 and 4 have named tests and recorded revert-red evidence.
- `POST /auth/guest/create` and `app/users.py` are untouched.
</success_criteria>

<output>
Create `.planning/phases/209-traffic-surge-quick-wins/209-01-SUMMARY.md` when done.
</output>
