---
phase: 212-benchmark-full-game-analysis-lane
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/benchmark_selection.py
  - app/core/config.py
  - app/services/eval_queue_service.py
  - scripts/benchmark_lane.py
  - alembic/env.py
  - tests/test_benchmark_lane.py
  - tests/services/test_eval_queue.py
  - tests/test_alembic_autogen_filter.py
autonomous: true
requirements: [BENCHLANE-01, BENCHLANE-02, BENCHLANE-04]

estimate:
  tokens: 132000
  raw_tokens: 66000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Re-running `benchmark_lane.py select --tranche classical` against an unchanged eligible set inserts zero additional rows: the `(game_id, tc_tranche)` compound unique key makes the second run a no-op, and a game eligible in two different tranches gets one row per tranche rather than being merged into one."
    - "`select` against a tranche with zero eligible games exits 0, creates `benchmark_selection` if absent, inserts 0 rows and logs a `selected 0` line; a user with exactly one eligible game contributes that one game (the 100/user/TC cap is a ceiling, never a floor)."
    - "Selection order is reproducible: the per-user draw ranks games by `md5(g.id::text || :seed)` with a fixed `SELECTION_SEED`, and users are iterated in ascending `user_id`, so two runs over the same eligible set choose the identical game set."
    - "With `BENCHMARK_SELECTION_GATE_ENABLED=false` the SQL fragment passed to `_es_weighted_user_pick` / `_es_weighted_game_pick` from `_claim_tier3_derived` Step 1 and Step 2 is character-for-character identical to the pre-change baseline: the gate helper returns the empty string, not whitespace and not `AND TRUE`."
    - "`benchmark_selection` exists in the benchmark DB with a `(game_id, tc_tranche)` unique constraint, `game_id` FK to `games.id ON DELETE CASCADE`, `user_id` FK to `users.id ON DELETE CASCADE`, and a TEXT + CHECK `tc_tranche` column."
    - "A future `alembic revision --autogenerate` emits no `op.create_table` for any benchmark-only table: `_include_object` filters tables named in `_AUTOGEN_TABLE_IGNORELIST` the same way it already filters indexes."
    - "For one selected game processed by the local backend on :8001, `game_positions.best_move` and `.pv` are non-NULL where the pipeline writes them, `game_flaws` rows exist for the game, and `game_best_moves` rows exist."
  prohibitions:
    - statement: "Production's `.env` must never set `BENCHMARK_SELECTION_GATE_ENABLED` or `BENCHMARK_HOMOGENIZE_EVAL_SOURCE` to true. Unlike `EVAL_AUTO_DRAIN_ENABLED`, these two flags exist only for the benchmark instance; enabling either in prod silently changes what prod's lottery claims or what eval values prod stores."
      status: resolved
      verification: judgment
    - statement: "`app/models/benchmark_selection.py` and `app/models/benchmark_lichess_eval_snapshot.py` must never be added to `alembic/env.py`'s model import list. Importing them puts benchmark-only tables into the canonical chain, which is exactly the prod schema mutation INFRA-02 exists to prevent."
      status: resolved
      verification: test
  artifacts:
    - app/models/benchmark_selection.py
    - scripts/benchmark_lane.py
    - tests/test_benchmark_lane.py
    - tests/test_alembic_autogen_filter.py
  key_links:
    - "`_claim_tier3_derived` Step 1 + Step 2 predicate strings -> `_selection_gate_clause()` -> `settings.BENCHMARK_SELECTION_GATE_ENABLED`"
    - "`scripts/benchmark_lane.py select` -> `BenchmarkSelection.metadata.create_all(tables=[...], checkfirst=True)` -> benchmark Postgres :5433"
    - "`alembic/env.py:_include_object` -> `_AUTOGEN_TABLE_IGNORELIST` -> prod migration chain stays free of benchmark tables"
---

<objective>
Build and prove the thin end-to-end spine of the benchmark analysis lane: a materialized
`benchmark_selection` table, a config-gated narrowing of the tier-3 lottery, and one real game
carried all the way through the FlawChess pipeline by a worker pointed at a local backend on
:8001 against the benchmark Postgres on :5433.

Purpose: prove the architecture end-to-end on one game before building out the other three
lottery lanes, the dual-URL worker, and the homogenization override. If the spine is wrong,
it is wrong after one commit instead of after five.

Output: `BenchmarkSelection` ORM model, `scripts/benchmark_lane.py select`, the
`BENCHMARK_SELECTION_GATE_ENABLED` flag wired into tier-3, the Alembic autogenerate table
guard, and a human-verified pipeline run on a small tranche.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/212-benchmark-full-game-analysis-lane/212-CONTEXT.md
@.planning/phases/212-benchmark-full-game-analysis-lane/212-RESEARCH.md
@.planning/phases/212-benchmark-full-game-analysis-lane/212-PATTERNS.md
</context>

## Requirement traceability

| ID | ROADMAP Success Criterion (abridged) | Covered by |
|----|--------------------------------------|------------|
| BENCHLANE-01 | SC1 — `benchmark_selection` materializes the capped (100/user/TC), randomly-selected, equal-footing set as `(game_id, tc_tranche)`, populated per TC tranche; the table IS the reproducibility record | 212-01 T1, 212-05 T1 |
| BENCHLANE-02 | SC2 — a single config-gated `WHERE EXISTS (... benchmark_selection ...)` narrows the tier-3 candidate query, off by default and verifiably inert in dev, CI and prod | 212-01 T1, 212-02 T1/T2 |
| BENCHLANE-03 | SC3 — `remote_eval_worker.py` accepts an ordered URL list and claims from the fallback only when the primary returns no work | 212-03 T1/T2 |
| BENCHLANE-04 | SC4 — a second backend on :8001 runs against the benchmark DB on 5433 with the gate on, Alembic head verified, producing full pipeline output on a small tranche | 212-01 T3, 212-05 T3 |
| BENCHLANE-05 | SC5 — the eval-source homogeneity question is decided and implemented before classical starts, with the consequence written down | 212-04 T1/T2, 212-06 T1 |
| BENCHLANE-06 | SC6 — the classical tranche completes or stops at a TC boundary, with row counts recorded and a post-run vacuum | 212-05 T1, 212-06 T2/T3 |

## Artifacts this phase produces

New symbols created by Phase 212 (all plans). Newly-created names are not pre-existing
codebase symbols and must not be treated as drift.

**New modules / files**
- `app/models/benchmark_selection.py`
- `app/models/benchmark_lichess_eval_snapshot.py`
- `scripts/benchmark_lane.py`
- `tests/test_benchmark_lane.py`
- `tests/test_alembic_autogen_filter.py`
- `docs/benchmark-lane-runbook.md`
- `reports/benchmark-lane/benchmark-lane-{tranche}-YYYY-MM-DD.md` (generated)
- `.planning/phases/212-benchmark-full-game-analysis-lane/COVERAGE.md`

**New ORM classes / tables / columns**
- `BenchmarkSelection` -> table `benchmark_selection`, columns `id`, `game_id`, `user_id`,
  `tc_tranche`, `lichess_arm`, `selected_at`; constraint
  `uq_benchmark_selection_game_tranche`; check constraint `ck_benchmark_selection_tc_tranche`
- `BenchmarkLichessEvalSnapshot` -> table `benchmark_lichess_eval_snapshot`, columns `id`,
  `game_id`, `ply`, `eval_cp`, `eval_mate`, `captured_at`; constraint
  `uq_benchmark_lichess_eval_snapshot_game_ply`

**New Settings fields / env vars**
- `BENCHMARK_SELECTION_GATE_ENABLED: bool = False`
- `BENCHMARK_HOMOGENIZE_EVAL_SOURCE: bool = False`

**New functions / constants**
- `app/services/eval_queue_service.py`: `_BENCHMARK_SELECTION_GATE_SQL`,
  `_selection_gate_clause()`, `assert_benchmark_selection_gate_ready()`
- `alembic/env.py`: `_AUTOGEN_TABLE_IGNORELIST`
- `scripts/benchmark_lane.py`: `SELECTION_SEED`, `GAMES_PER_USER_PER_TC_CAP`,
  `EQUAL_FOOTING_MAX_RATING_GAP`, `TC_TRANCHES`, `TcTranche`, `persist_selection`,
  `snapshot_lichess_evals`, `tranche_status`, `write_record_report`
- `scripts/remote_eval_worker.py`: `_CycleOutcome`, `_BackendTarget`,
  `_build_backend_targets`; CLI flags `--fallback-url`, `--fallback-token`

**New CLI subcommands**
- `scripts/benchmark_lane.py select | snapshot | status | record`

<assumption_delta_decision>
Detector: `gsd_run query assumption-delta scan 212 --json` returned `detected: true` with two
`pluralization` signals (`second`, `fallback`).

- **Noun now primary:** a **backend target** — the `(base_url, operator_token, label)` triple a
  worker cycle runs its whole ladder against. The old primary noun was "the base URL"
  (a single string fixed at `httpx.AsyncClient` construction).
- **Decision: `promote`.** `scripts/remote_eval_worker.py` models an ordered
  `list[_BackendTarget]` as its primary identity, with the prod target as element 0. The
  existing `--base-url` / `--token` flags stay as the construction surface (the fleet's
  systemd units and shell wrappers must keep working unchanged), but internally they are one
  way of building the list, not the model. `_run_loop` iterates targets in order; it does not
  hold a distinguished `client` plus an optional `fallback_client`.
- **Why not add-alongside:** keeping a required `base_url` with an optional `fallback_url`
  bolted on means a third target (the deferred prod-host sibling-DB backend in CONTEXT.md's
  Deferred Ideas) cannot be expressed without another special case. The ordered list absorbs
  it for free.
- **Suggested invariant test (accepted, implemented in 212-03 T2):**
  `test_ladder_never_interleaves_targets` — for every supported target-list length, a single
  cycle issues all of one target's rungs before any of the next target's rungs.

Also detected as a `_run_cycle` return-shape change (`bool` -> `_CycleOutcome`). That is a
richer return on an existing seam, not an identity change: `no-change` to the identity model.
</assumption_delta_decision>

<flagged_assumptions>
**Spec-less edge probe — unresolved item 1 of 2**
(`.planning/phases/212-benchmark-full-game-analysis-lane/212-EDGE-COVERAGE.json`,
`BENCHLANE-04`, category `unclassified`). The probe could not classify SC4 into a boundary /
adjacency / empty / ordering / precision shape, and it is not auto-resolved with a backstop.
Planner assumption, flagged for the developer: **"produces the full pipeline output end to end
on a small tranche" is interpreted as at least one selected game reaching all three of
`game_positions.best_move`+`pv`, `game_flaws` rows, and `game_best_moves` rows — not a
percentage-of-tranche threshold.** If a coverage threshold (e.g. "100% of the smoke tranche")
is wanted instead, say so before T3 runs; the checkpoint prompt is where it would be set.

## Phase-level no-silent-drop accounting

Source of truth: `212-EDGE-COVERAGE.json` in this phase directory (a permanent, non-hidden
artifact — spot-check the tally against it). It lists **15** applicable items, all
`status: unresolved` at probe time. Each is dispositioned below, so the count is auditable
per item rather than only in aggregate.

| # | Requirement | Probe category | Disposition | Where it lives now |
|---|-------------|----------------|-------------|--------------------|
| 1 | BENCHLANE-01 | adjacency | authored | 212-01 truth 1 (two tranches -> one row per tranche, never merged) |
| 2 | BENCHLANE-01 | empty | authored | 212-01 truth 2 (zero-eligible tranche; one-game user) |
| 3 | BENCHLANE-01 | ordering | authored | 212-01 truth 3 (md5-seeded rank, ascending `user_id`) |
| 4 | BENCHLANE-02 | adjacency | authored | 212-01 truth 4 (gate-off fragment is the empty string, not whitespace, not `AND TRUE`) |
| 5 | BENCHLANE-02 | empty | authored | 212-02 truth 2 (gate on + table empty -> 204, no ungated fallthrough) |
| 6 | BENCHLANE-02 | ordering | authored | 212-02 truth 1 (predicate carries no tranche filter; sequencing is by population order) |
| 7 | BENCHLANE-03 | boundary | authored | 212-03 truth 1 (fallback only after all five rungs 204) |
| 8 | BENCHLANE-03 | empty | authored | 212-03 truth 3 (optional `--fallback-token`; empty-string URL rejected) |
| 9 | BENCHLANE-03 | adjacency | authored | 212-03 truth 5 (both targets idle -> `idle_sleep` awaited once, not once per target) |
| 10 | BENCHLANE-03 | ordering | authored | 212-03 truth 4 (rungs never interleaved across targets) |
| 11 | BENCHLANE-03 | precision | authored | 212-03 truth 6 (`TransportError` as no-work; streak escalation fires exactly once) |
| 12 | BENCHLANE-04 | unclassified | **flagged** | this block, item 1 of 2 |
| 13 | BENCHLANE-05 | unclassified | **flagged** | `212-04-PLAN.md` `<flagged_assumptions>`, item 2 of 2 |
| 14 | BENCHLANE-06 | boundary | authored | 212-05 truth 1 (zero-completed tranche writes explicit zero counts) |
| 15 | BENCHLANE-06 | precision | authored | 212-05 truth 2 (exact `COUNT(*)`, never `reltuples`) — the one backstop-verified entry |

Totals: **13 authored** into `must_haves.truths` (4 in this plan, 2 in 212-02, 5 in 212-03,
2 in 212-05; 12 explicit + 1 backstop) + **2 flagged** = **15**. Matches
`212-EDGE-COVERAGE.json`'s `coverage.applicable`. No item was dropped and none was
auto-dismissed. Plans 212-01/212-03/212-05 each carry additional truths beyond their probe
items; only the probe-derived ones are tabulated here.
</flagged_assumptions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end selection spine — table, select subcommand, tier-3 gate</name>
  <files>app/models/benchmark_selection.py, scripts/benchmark_lane.py, app/core/config.py, app/services/eval_queue_service.py, tests/test_benchmark_lane.py, tests/services/test_eval_queue.py</files>
  <read_first>
    - app/models/benchmark_selected_user.py (structure, docstring, UniqueConstraint idempotency — copy verbatim shape)
    - app/models/benchmark_ingest_checkpoint.py (the FK-with-explicit-ondelete precedent; benchmark_selected_user.py has NO FK, do not copy it for this)
    - scripts/select_benchmark_users.py lines 340-470 (persist_selection: targeted create_all, SELECT-existing -> skip-dupe -> add -> commit; argparse + main shape)
    - app/core/config.py lines 77-105 (the EVAL_AUTO_DRAIN_ENABLED / BEST_MOVE_BACKFILL_ENABLED comment-and-default shape) and lines 155-178 (db_url_for_target)
    - app/services/eval_queue_service.py lines 290-450 (_es_weighted_user_pick / _es_weighted_game_pick and their trusted-SQL docstring) and lines 560-660 (_claim_tier3_derived Step 1 and Step 2)
    - tests/test_benchmark_ingest.py (mock-session idempotency test shape, notably test_persist_selection_compound_dedup)
    - tests/services/test_eval_queue.py (module docstring and the existing byte-identity tests)
    - .claude/skills/benchmarks/SKILL.md "Standard CTE — selected_users" section (the mandatory bsu -> bic status='completed' -> users join and the ::text cast on time_control_bucket)
  </read_first>
  <behavior>
    - persist_selection with an empty eligible set: creates the table, inserts 0 rows, returns (0, 0), does not raise.
    - persist_selection run twice over the same eligible rows: second run inserts 0, skips N.
    - persist_selection with one game eligible under two tranches: two rows, one per tranche.
    - _selection_gate_clause() returns "" when the flag is False and the EXISTS fragment when True.
    - _claim_tier3_derived Step 1 candidate_where_sql with the flag False == frozen baseline string, byte for byte.
    - _claim_tier3_derived Step 2 game_where_sql with the flag False == frozen baseline string, byte for byte.
    - With the flag True and one benchmark_selection row present, the tier-3 claim returns only that game.
  </behavior>
  <action>
Create `app/models/benchmark_selection.py` defining `class BenchmarkSelection(Base)` with
`__tablename__ = "benchmark_selection"`. Columns: `id` int primary key; `game_id`
`Mapped[int]` via `mapped_column(ForeignKey("games.id", ondelete="CASCADE"), nullable=False)`;
`user_id` `Mapped[int]` via `mapped_column(ForeignKey("users.id", ondelete="CASCADE"),
nullable=False)` (denormalized so `status` can report per-user progress without a join to
`games`); `tc_tranche` `Mapped[str]` as `String(20)`, nullable False; `lichess_arm`
`Mapped[bool]` nullable False (true when `games.lichess_evals_at IS NOT NULL` at selection
time, so the two §6 arms are separable without re-deriving them after homogenization runs);
`selected_at` with `server_default=func.now()`, nullable False. `__table_args__` carries
`UniqueConstraint("game_id", "tc_tranche", name="uq_benchmark_selection_game_tranche")` and
`CheckConstraint("tc_tranche IN ('bullet','blitz','rapid','classical')",
name="ck_benchmark_selection_tc_tranche")` — TEXT + CHECK, never a native PostgreSQL ENUM, per
the CLAUDE.md enum-column policy for low-cardinality domain columns. Write the module
docstring in the `benchmark_selected_user.py` style, stating verbatim that the table is created
via `Base.metadata.create_all()` against the benchmark engine and is NOT in the canonical
Alembic chain (Phase 69 INFRA-02, per D-07), and stating that this table IS the reproducibility
record a data story cites.

Add `BENCHMARK_SELECTION_GATE_ENABLED: bool = False` to `Settings` in `app/core/config.py`,
placed directly after `BEST_MOVE_BACKFILL_ENABLED`. Its comment must state: the flag narrows
every eval-lottery lane to games present in `benchmark_selection`; it exists only for the
benchmark backend instance on :8001; and unlike `EVAL_AUTO_DRAIN_ENABLED`, prod's `.env` must
never set it true.

In `app/services/eval_queue_service.py`, add a module-level constant
`_BENCHMARK_SELECTION_GATE_SQL` holding the literal
`AND EXISTS (SELECT 1 FROM benchmark_selection bs WHERE bs.game_id = g.id)` and a function
`_selection_gate_clause() -> str` returning that constant when
`settings.BENCHMARK_SELECTION_GATE_ENABLED` is true and `""` otherwise. It must be a function,
not a module-level evaluated string, so the flag is read per call and tests can monkeypatch
`settings`. Both must sit next to the `_es_weighted_user_pick` docstring's QUEUE-08 note and
carry a comment saying the fragment is a hardcoded literal that never receives request or user
input. The gate keys on `bs.game_id = g.id` only — it deliberately carries no `tc_tranche`
filter, because tranche sequencing is achieved by populating `benchmark_selection` one tranche
at a time, and a tranche filter would need a second config value to stay in sync.

Wire the clause into `_claim_tier3_derived` at both steps: Step 1's `candidate_where_sql`
(around line 600) gets the clause appended inside each of its two `EXISTS` bodies — once after
`AND g.lichess_evals_at IS NULL` and once after `AND g.lichess_evals_at IS NOT NULL` — and
Step 2's `_es_weighted_game_pick(game_where_sql=...)` call (around line 627) gets it appended
to its predicate. Convert the affected string literals to f-strings interpolating
`{_selection_gate_clause()}`. Critical: when the flag is off the rendered string must be
byte-identical to today's, so the interpolation site must be positioned such that an empty
clause leaves no extra whitespace, blank line, or trailing space. Verify by capturing the
strings before and after the edit and diffing them.

Create `scripts/benchmark_lane.py` as an argparse tool with a `select` subcommand (the other
subcommands land in 212-04 and 212-05; leave the subparser structure ready for them).
Module-level constants: `GAMES_PER_USER_PER_TC_CAP = 100`, `EQUAL_FOOTING_MAX_RATING_GAP =
100`, `SELECTION_SEED = "212-benchmark-lane"`, `TC_TRANCHES = ("classical", "rapid", "blitz",
"bullet")` and `TcTranche = Literal["classical", "rapid", "blitz", "bullet"]`. `select` takes
`--tranche` (choices from `TC_TRANCHES`, required), `--db` (choices dev/test/prod/benchmark,
default `benchmark`, resolved through `db_url_for_target`) and `--limit` (optional int, caps
total rows inserted — this is what makes the T3 smoke tranche possible). Implement
`persist_selection(db_url, tranche, limit) -> tuple[int, int]` returning
`(inserted, skipped_dupes)`, following `select_benchmark_users.persist_selection` exactly:
`create_async_engine`, then
`BenchmarkSelection.metadata.create_all(sync_conn, tables=[cast(Table,
BenchmarkSelection.__table__)], checkfirst=True)` inside `engine.begin()`, then an
`async_sessionmaker` session that reads existing `(game_id, tc_tranche)` pairs, skips dupes,
`session.add()`s the rest and commits once.

The eligible-set query is raw `sa.text` SQL built from the benchmarks skill's canonical
`selected_users` CTE (join `benchmark_selected_users` to `benchmark_ingest_checkpoints` on
username+tc_bucket with `bic.status = 'completed'`, then to `users` on `lichess_username`),
joined to `games g` on `g.user_id = su.user_id` with:
`g.rated`, `NOT g.is_computer_game`, `g.time_control_bucket::text = su.tc_bucket`,
`su.tc_bucket = :tranche`, both ratings NOT NULL, and
`abs(g.white_rating - g.black_rating) <= :max_gap`. The `::text` cast is mandatory —
`games.time_control_bucket` is the `timecontrolbucket` enum and `tc_bucket` is varchar.
Apply the cap with
`ROW_NUMBER() OVER (PARTITION BY g.user_id ORDER BY md5(g.id::text || :seed)) <= :cap` — an
md5-of-id ordering, not `random()`, so the draw is uniform within a user AND reproducible
across runs (which is what makes the table a real reproducibility record rather than a
snapshot of an unrepeatable roll). Per D-02 this is ONE random draw per user across the whole
eligible set: no stratification by arm, no minimum-per-arm floor, and no recency weighting, so
the analyzed/unanalyzed split falls out at that user's natural ratio — stratifying would
distort the exact quantity the §6 check measures, and recency-ordering would systematically
shift users toward their peak rating and away from the `median_elo` their cell was selected on.
Users with zero games in one arm simply drop out of the paired analysis. Select `g.id`, `g.user_id`, and
`(g.lichess_evals_at IS NOT NULL) AS lichess_arm`, ordered by `g.user_id, g.id` so iteration
order is stable. Log per-tranche counts in the `_log(...)` style of the sibling scripts.
Print a `select`-mode summary line carrying inserted / skipped / total.

Add tests. In `tests/test_benchmark_lane.py` (new), mirror
`tests/test_benchmark_ingest.py::test_persist_selection_compound_dedup`'s mock-session shape:
patch `create_async_engine` and `async_sessionmaker`, feed a fake eligible-row result, and
assert (a) empty eligible set -> `(0, 0)` and no raise, (b) a second run over the same rows ->
`(0, N)`, (c) one game under two tranches -> two `BenchmarkSelection` rows added, (d) the
compiled SQL string returned by the query builder contains `md5(` and the `SELECTION_SEED` bind
name, and does not contain a non-deterministic ordering function. The (d) assertion inspects
the compiled statement text at runtime, not the source file, so an explanatory comment in
`benchmark_lane.py` cannot invalidate it.
<!-- planner-discipline-allow: random() -->
<!-- planner-discipline-allow: md5( -->
In `tests/services/test_eval_queue.py`, add `test_benchmark_selection_gate_off_byte_identical`:
freeze today's Step 1 `candidate_where_sql` and Step 2 `game_where_sql` as string constants in
the test module, patch `_es_weighted_user_pick` / `_es_weighted_game_pick` with recorders, call
`_claim_tier3_derived` with the flag off, and assert `==` on the captured strings (direct
string equality, not normalized-whitespace equality). Add
`test_benchmark_selection_gate_on_narrows_tier3`: create `benchmark_selection` in the per-run
test DB via the same targeted `create_all`, insert one row for a known game, set the flag via
monkeypatch on `settings`, and assert the tier-3 claim returns only that game while a second
otherwise-eligible game is not returned.

All new backend code carries explicit return type annotations and uses `Literal[...]` for the
tranche parameter, per CLAUDE.md.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_benchmark_lane.py tests/services/test_eval_queue.py -x` exits 0.
    - Source: `app/models/benchmark_selection.py` contains `class BenchmarkSelection(Base)`, `__tablename__ = "benchmark_selection"`, `ForeignKey("games.id", ondelete="CASCADE")`, `ForeignKey("users.id", ondelete="CASCADE")`, `UniqueConstraint("game_id", "tc_tranche", name="uq_benchmark_selection_game_tranche")` and a `CheckConstraint` naming the four tranche values.
    - Source: `app/core/config.py` contains `BENCHMARK_SELECTION_GATE_ENABLED: bool = False`.
    - Source: `app/services/eval_queue_service.py` defines both `_BENCHMARK_SELECTION_GATE_SQL` and `def _selection_gate_clause() -> str`, and `_claim_tier3_derived` interpolates `_selection_gate_clause()` at three positions (Step 1's two EXISTS bodies plus Step 2's game predicate).
    - Behavior: `test_benchmark_selection_gate_off_byte_identical` compares with `==` against frozen literals and passes; changing `_selection_gate_clause()` to return a single space makes it fail.
    - Behavior: `test_benchmark_selection_gate_on_narrows_tier3` returns exactly the one selected game.
    - CLI: `uv run python scripts/benchmark_lane.py select --help` exits 0 and lists `--tranche`, `--db`, `--limit`.
    - CLI: `uv run ruff check app/ scripts/ tests/` and `uv run ty check app/ tests/` both exit 0 with zero errors.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_benchmark_lane.py tests/services/test_eval_queue.py -x</automated>
  </verify>
  <reversibility rating="reversible">New table plus an off-by-default flag; reverting is a code revert plus a DROP TABLE in a research DB.</reversibility>
  <done>`benchmark_selection` can be materialized per tranche idempotently, and with the gate on the tier-3 lottery sees only selected games while the gate-off SQL is provably unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close the Alembic autogenerate table gap (D-08)</name>
  <files>alembic/env.py, tests/test_alembic_autogen_filter.py</files>
  <read_first>
    - alembic/env.py in full (the model import list at lines 10-30, `_AUTOGEN_INDEX_IGNORELIST` at lines ~87-109 including the 2026-07-02 code-review note, and `_include_object` at lines 112-115)
    - app/models/benchmark_selected_user.py and app/models/benchmark_ingest_checkpoint.py (the two already-exposed tables this retroactively protects)
    - app/models/benchmark_selection.py (created in Task 1)
  </read_first>
  <behavior>
    - `_include_object(obj, "benchmark_selection", "table", False, None)` returns False.
    - `_include_object(obj, "benchmark_selected_users", "table", False, None)` returns False.
    - `_include_object(obj, "benchmark_ingest_checkpoints", "table", False, None)` returns False.
    - `_include_object(obj, "benchmark_lichess_eval_snapshot", "table", False, None)` returns False.
    - `_include_object(obj, "games", "table", False, None)` returns True.
    - `_include_object(obj, "benchmark_cohort_cdf", "table", False, None)` returns True (canonical, imported at env.py's top).
    - `_include_object(obj, "ix_games_evals_pending", "index", True, None)` still returns False (existing behavior unchanged).
  </behavior>
  <action>
Extend `alembic/env.py` with a module-level `_AUTOGEN_TABLE_IGNORELIST` set, declared directly
below `_AUTOGEN_INDEX_IGNORELIST` and mirroring its shape and comment style. Populate it with
the four benchmark-only table names: `benchmark_selected_users`,
`benchmark_ingest_checkpoints`, `benchmark_selection`, `benchmark_lichess_eval_snapshot`.
Do NOT add `benchmark_cohort_cdf` — that table IS canonical and is deliberately imported at
`alembic/env.py`'s top.

Write the ignorelist's comment to state the failure mode concretely: these tables live on the
shared declarative `Base` (so `target_metadata` sees them) but are created only by targeted
`Base.metadata.create_all(tables=[...])` from `scripts/` against the benchmark engine, per
Phase 69 INFRA-02. `_include_object` filtered indexes only, so the next unrelated
`alembic revision --autogenerate` would emit `op.create_table` for each of them against prod's
migration chain. Reference the same class of latent defect as the 2026-07-02 code-review note
already in the index ignorelist.

Extend `_include_object` with a second branch: return False when `type_ == "table"` and `name`
is in `_AUTOGEN_TABLE_IGNORELIST`, mirroring the existing index branch's structure exactly.
Leave the trailing `return True` as the default.

Create `tests/test_alembic_autogen_filter.py` importing `_include_object` and
`_AUTOGEN_TABLE_IGNORELIST` from `alembic.env`, and parametrize over the behaviors listed
above. Add one further test asserting that every name in `_AUTOGEN_TABLE_IGNORELIST` is
absent from the set of table names imported at the top of `alembic/env.py` — read the module
source and assert no `from app.models.benchmark_selection import` or
`from app.models.benchmark_lichess_eval_snapshot import` line exists, so a future executor
cannot "helpfully" add the model to the canonical chain and still pass. That assertion is the
test-verified half of this plan's second prohibition.

Note for the executor: these two tables are deliberately NOT in the canonical Alembic chain.
Do not run `alembic revision --autogenerate` for them and do not add a migration file. Their
entire lifecycle is `create_all(tables=[...], checkfirst=True)` from
`scripts/benchmark_lane.py`.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_alembic_autogen_filter.py -x` exits 0.
    - Source: `alembic/env.py` defines `_AUTOGEN_TABLE_IGNORELIST` containing exactly the four benchmark-only table names, and `_include_object` has a branch keyed on `type_ == "table"`.
    - Behavior: reverting only the `_include_object` table branch makes `tests/test_alembic_autogen_filter.py` fail.
    - CLI: `ls alembic/versions/ | wc -l` is unchanged from before this task (no migration file was created).
    - CLI: `uv run alembic upgrade head` against the dev DB still exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_alembic_autogen_filter.py -x</automated>
  </verify>
  <reversibility rating="reversible">Pure filter addition on a shared file; narrow and fully test-pinned.</reversibility>
  <done>An `alembic revision --autogenerate` run can no longer emit `op.create_table` for any benchmark-only table, and the two pre-existing ones are retroactively protected.</done>
</task>

<task type="auto">
  <name>Task 3: Benchmark DB preflight and smoke tranche, then the :8001 pipeline proof</name>
  <files>none — this task runs commands and verifies database state; it writes no source file</files>
  <read_first>
    - .planning/phases/212-benchmark-full-game-analysis-lane/212-RESEARCH.md "Pitfall 5" and "Pitfall 6" (DATABASE_URL vs DATABASE_URL_BENCHMARK; the write role vs the _ro role)
    - bin/benchmark_db.sh in full (start/stop/reset, the `alembic upgrade head` against 5433, the RO grants)
    - app/services/eval_apply.py around line 810 (the `maia_available` guardrail docstring — why a Maia-absent backend fails silently)
    - scripts/benchmark_lane.py (the `select` subcommand written in Task 1)
  </read_first>
  <precondition>Docker is running and the dev Postgres on :5432 is up, so the benchmark container on :5433 can be started and its Alembic head compared against the dev head.</precondition>
  <action>
Run the automatable half of BENCHLANE-04's small-tranche criterion, then record the rest as a
human check for end-of-phase harvest.

Automatable, run now:

1. `bin/benchmark_db.sh start`. This brings :5433 up, runs `alembic upgrade head` against it,
   and re-applies the read-only grants.
2. Compare the Alembic head on :5433 against the dev DB's head. Run `uv run alembic current`
   once with `DATABASE_URL` set to the benchmark URL and once with the dev URL, and assert the
   two revision ids are equal. This is BENCHLANE-04's "Alembic head verified to match".
3. Materialize a smoke tranche:
   `uv run python scripts/benchmark_lane.py select --tranche classical --db benchmark --limit 20`.
   Assert the reported inserted count is 20.
4. Re-run the identical command and assert it reports inserted 0 / skipped 20 — the live
   idempotency proof that complements Task 1's mock-session unit test.

Not automatable by an executor, and deliberately left as a human check (a uvicorn server plus a
real Stockfish worker cannot be reliably kept alive across an agent task boundary — see the
project's recorded experience with backgrounded executor runs). Record these steps verbatim in
the task's `<human-check>` so the end-of-phase UAT harvest carries them:

- Launch the second backend with these values on the command line, never in `.env`: plain
  `DATABASE_URL` pointed at the benchmark database on :5433 using the write-capable
  `flawchess_benchmark` role (NOT the `_ro` role the MCP tool uses, and NOT
  `DATABASE_URL_BENCHMARK`, which the running app never reads), plus
  `EVAL_AUTO_DRAIN_ENABLED=true`, `BEST_MOVE_BACKFILL_ENABLED=true`,
  `BENCHMARK_SELECTION_GATE_ENABLED=true`, `STOCKFISH_POOL_SIZE=1` (per D-15 the local
  instance is a submit and Maia service, not a Stockfish contributor — the throughput comes
  from the fleet, where it is already proven, and this keeps Adrian's cores for the workers
  already on that box), and an `EVAL_OPERATOR_TOKEN` generated for this instance and distinct
  from prod's. Then `uv run uvicorn app.main:app --port 8001 --host 0.0.0.0`.
- Confirm from the startup log that Maia loaded in this process (D-12: treat "Maia loaded" as
  an explicit precondition the operator checks, never an assumption). A Maia-absent backend
  produces PV and never stamps `best_moves_completed_at`, and does so silently.
- Point one worker at the local instance with `--base-url http://localhost:8001` and the same
  instance token, using `--once`, repeated until at least one full game has been leased and
  submitted. No fallback flag yet — that lands in 212-03.
- For one game id present in `benchmark_selection`, confirm all three outputs: `game_positions`
  rows with non-NULL `best_move` and non-NULL `pv` where the pipeline writes them, `game_flaws`
  rows, and `game_best_moves` rows.
- Confirm the gate held: no game absent from `benchmark_selection` was touched.
- If PV lands but `game_best_moves` stays empty, that is the Maia-absent signature, not a
  pipeline bug. Stop rather than continuing to the expansion plans.

Leave the benchmark container running when the task ends; later plans depend on it.
  </action>
  <acceptance_criteria>
    - CLI: `bin/benchmark_db.sh start` exits 0.
    - CLI: `uv run alembic current` against :5433 and against the dev DB print the same revision id.
    - CLI: `uv run python scripts/benchmark_lane.py select --tranche classical --db benchmark --limit 20` reports 20 inserted on the first run.
    - CLI: the identical second invocation reports 0 inserted and 20 skipped.
    - Behavior: `SELECT count(*) FROM benchmark_selection WHERE tc_tranche = 'classical'` returns 20 against the benchmark DB.
  </acceptance_criteria>
  <verify>
    <automated>uv run python scripts/benchmark_lane.py select --tranche classical --db benchmark --limit 20 &amp;&amp; uv run python scripts/benchmark_lane.py select --tranche classical --db benchmark --limit 20</automated>
    <human-check>With the local backend running on :8001 against the benchmark DB and one worker pointed at it, a selected game has `game_positions.best_move` and `.pv` populated, `game_flaws` rows, and `game_best_moves` rows; Maia is confirmed loaded in the :8001 startup log; and no unselected game was touched.</human-check>
  </verify>
  <reversibility rating="reversible">Preflight and a 20-row smoke tranche in a research DB.</reversibility>
  <done>The benchmark DB's Alembic head matches, a 20-game smoke tranche is materialized idempotently, and the operator-side pipeline proof is recorded for end-of-phase verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell -> benchmark Postgres :5433 | write-capable `flawchess_benchmark` role credentials cross here; the wrong URL writes into the dev DB on :5432 instead |
| LAN worker box -> local backend `0.0.0.0:8001` | unauthenticated HTTP arrives here and is gated only by `X-Operator-Token` |
| benchmark-only ORM models -> canonical Alembic chain | a schema object can leak from the research DB into prod's migration chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-212-01 | Spoofing / Elevation of Privilege | local backend bound to `0.0.0.0:8001` | medium | mitigate | The instance sets its own `EVAL_OPERATOR_TOKEN`, distinct from prod's. `app/routers/eval_remote.py`'s existing fail-closed check applies unchanged: 403 when unconfigured, `hmac.compare_digest` 401 on mismatch. Task 3 step 3 requires a per-instance token on the command line, never in `.env`. |
| T-212-02 | Denial of Service (of the tranche's controlled scope) | `BENCHMARK_SELECTION_GATE_ENABLED` with `benchmark_selection` absent | high | mitigate | 212-02 T2's boot assertion refuses startup. Within this plan, `_selection_gate_clause()` is a pure predicate that raises `UndefinedTable` rather than silently widening, so the failure is loud even before the boot assertion lands. |
| T-212-03 | Tampering | the gate SQL fragment | low | accept | `_BENCHMARK_SELECTION_GATE_SQL` is a module-level literal, never composed from request or user input, matching the QUEUE-08 convention already enforced at every call site in the module. |
| T-212-05 | Tampering | `DATABASE_URL` misconfigured to :5432 (Pitfall 5) | high | mitigate | Task 3 spells out plain `DATABASE_URL` with the :5433 write role and names the two wrong values explicitly. Structural backstop: the dev DB has no `benchmark_selection` table, so 212-02's gate-on boot assertion fails closed on exactly this misconfiguration. |
| T-212-SC | Tampering | package-manager installs | n/a | accept | This phase installs no npm/pip/cargo packages; `212-RESEARCH.md` records the Package Legitimacy Audit as N/A under its own trigger condition. No install task exists in any Phase 212 plan. |
</threat_model>

<verification>
- `uv run pytest tests/test_benchmark_lane.py tests/services/test_eval_queue.py tests/test_alembic_autogen_filter.py -x` green.
- `uv run ruff check app/ scripts/ tests/` and `uv run ty check app/ tests/` both clean.
- No new file under `alembic/versions/`.
- Operator confirmation recorded for Task 3.
</verification>

<success_criteria>
BENCHLANE-01's table exists and is idempotently populatable per tranche; BENCHLANE-02's gate
is wired into tier-3 with a byte-identity proof when off; BENCHLANE-04's small-tranche
end-to-end run has produced `best_move` + `pv`, `game_flaws` and `game_best_moves` for at least
one selected game.
</success_criteria>

<output>
Create `.planning/phases/212-benchmark-full-game-analysis-lane/212-01-SUMMARY.md` when done
</output>
