---
phase: 212-benchmark-full-game-analysis-lane
plan: 02
type: execute
wave: 2
depends_on: ["212-01"]
files_modified:
  - app/services/eval_queue_service.py
  - app/main.py
  - tests/services/test_eval_queue.py
  - tests/test_main_lifespan.py
autonomous: true
requirements: [BENCHLANE-02]

estimate:
  tokens: 84000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The gate predicate keys on `bs.game_id = g.id` alone and carries no `tc_tranche` filter: a game whose only `benchmark_selection` row belongs to a tranche other than the one currently running is still claimable. Tranche sequencing is achieved by populating the table one tranche at a time, not by the claim predicate."
    - "With the gate on and `benchmark_selection` present but empty, every lottery lane returns no candidate (the worker sees 204) rather than falling through to the ungated global candidate set."
    - "With `BENCHMARK_SELECTION_GATE_ENABLED=false`, the SQL fragments passed to `_es_weighted_user_pick` / `_es_weighted_game_pick` from all four gated sites (`_claim_tier3_derived` Step 1 and Step 2, `_claim_tier4_blob`, `_claim_tier4_bestmove`) are byte-identical to the pre-change baselines."
    - "Starting the app with `BENCHMARK_SELECTION_GATE_ENABLED=true` while `benchmark_selection` does not exist in the connected database raises at startup and aborts uvicorn; it never boots into a state where the gate silently no-ops."
  artifacts:
    - app/services/eval_queue_service.py
    - app/main.py
  key_links:
    - "`_claim_tier4_blob` and `_claim_tier4_bestmove` user-pick and game-pick predicates -> `_selection_gate_clause()`"
    - "`app/main.py` lifespan -> `assert_benchmark_selection_gate_ready()` -> `to_regclass('public.benchmark_selection')`"
---

<objective>
Extend the selection gate from the tier-3 spine to every lottery lane the worker fleet can
reach, and make a misconfigured gate fail closed at startup instead of degrading silently.

Purpose: with `EVAL_AUTO_DRAIN_ENABLED=true` mandatory on the benchmark instance (D-11), a
tier-3-only gate would spend the tranche's capacity on the 477,829 tier-4-blob-eligible games
nobody selected, and "stoppable at a TC boundary" would stop meaning what it says.

Output: the gate applied at all four predicate sites (D-09), byte-identity tests for each, and
a boot assertion (D-10 point 3).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/212-benchmark-full-game-analysis-lane/212-CONTEXT.md
@.planning/phases/212-benchmark-full-game-analysis-lane/212-RESEARCH.md
@.planning/phases/212-benchmark-full-game-analysis-lane/212-PATTERNS.md
@.planning/phases/212-benchmark-full-game-analysis-lane/212-01-SUMMARY.md
</context>

## Artifacts this phase produces

See the "Artifacts this phase produces" section in `212-01-PLAN.md` for the full phase-wide
list. New in this plan: `assert_benchmark_selection_gate_ready()` in
`app/services/eval_queue_service.py`, and its call site in `app/main.py`'s lifespan.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply the gate to tier-4 blob and tier-4b best-move lanes (D-09)</name>
  <files>app/services/eval_queue_service.py, tests/services/test_eval_queue.py</files>
  <read_first>
    - app/services/eval_queue_service.py lines 290-450 (`_es_weighted_user_pick` / `_es_weighted_game_pick` and the QUEUE-08 trusted-SQL docstring), 560-660 (`_claim_tier3_derived`, already gated in 212-01), 659-790 (`_claim_tier4_blob`, both stages), 784-880 (`_claim_tier4_bestmove`, both stages)
    - tests/services/test_eval_queue.py (module docstring pins tier-4 blob / bestmove byte-identity; and the gate tests added in 212-01)
    - .planning/phases/212-benchmark-full-game-analysis-lane/212-CONTEXT.md D-09 (why tier-4b is only incidentally safe today)
  </read_first>
  <behavior>
    - Flag off: `_claim_tier4_blob` Stage 1 `candidate_exists_sql` == frozen baseline, byte for byte.
    - Flag off: `_claim_tier4_blob` Stage 2 `game_where_sql` == frozen baseline, byte for byte.
    - Flag off: `_claim_tier4_bestmove` Stage 1 `candidate_exists_sql` == frozen baseline, byte for byte.
    - Flag off: `_claim_tier4_bestmove` Stage 2 `game_where_sql` == frozen baseline, byte for byte.
    - Flag on, `benchmark_selection` holding one game: the tier-4 blob claim returns that game and not an otherwise-eligible unselected game.
    - Flag on, `benchmark_selection` holding one game: the tier-4b best-move claim returns that game and not an otherwise-eligible unselected game.
    - Flag on, `benchmark_selection` present but empty: all four lanes return None.
    - Flag on, a game selected under `tc_tranche='rapid'` while a classical run is in flight: the game IS claimable (the predicate has no tranche filter).
  </behavior>
  <action>
Apply the `_selection_gate_clause()` helper introduced in 212-01 to the four remaining
predicate sites, so the flag's semantics become "this backend only ever works on selected
games, full stop" across every lane the fleet can reach via `/flaw-blob-lease` and
`/bestmove-lease`.

Site A — `_claim_tier4_blob` Stage 1 (around line 747): its `candidate_exists_sql` body ends
with `AND g.blobs_completed_at IS NULL`. Convert the literal to an f-string and interpolate
`{_selection_gate_clause()}` immediately after that condition.

Site B — `_claim_tier4_blob` Stage 2 (around line 765): the `_es_weighted_game_pick(
game_where_sql=...)` predicate. Interpolate the clause at the end of its game-level
conditions. Note this predicate already aliases the games table as `g`, which is what
`_BENCHMARK_SELECTION_GATE_SQL` expects — confirm the alias while reading, and if any site
uses a different alias, fix the site to use `g` rather than parameterizing the constant.

Site C — `_claim_tier4_bestmove` Stage 1 (around line 831): `candidate_exists_sql` ends with
`AND g.best_moves_completed_at IS NULL`. Same treatment.

Site D — `_claim_tier4_bestmove` Stage 2 (around line 849): same treatment as Site B.

At every site the empty-clause rendering must leave no extra whitespace, blank line, or
trailing space, so the flag-off string is byte-identical to today's. Capture each predicate
string before the edit, keep it as a frozen literal in the test module, and diff after.

Add to `tests/services/test_eval_queue.py`, alongside the tier-3 tests from 212-01:
`test_benchmark_selection_gate_off_byte_identical_tier4_blob`,
`test_benchmark_selection_gate_off_byte_identical_tier4_bestmove` (both direct `==` string
comparisons against frozen literals, with `_es_weighted_user_pick` / `_es_weighted_game_pick`
patched as recorders), `test_benchmark_selection_gate_on_narrows_tier4_blob`,
`test_benchmark_selection_gate_on_narrows_tier4_bestmove`,
`test_benchmark_selection_gate_on_empty_table_returns_no_candidate` (all four lanes, table
created but zero rows, each claim returns None), and
`test_benchmark_selection_gate_ignores_tc_tranche` (a row with `tc_tranche='rapid'` still makes
its game claimable — this pins the deliberate no-tranche-filter decision so a future reader
does not "fix" it into a tranche-scoped predicate and quietly break cross-tranche resumes).

Update the module docstring of `tests/services/test_eval_queue.py` to record that the
byte-identity pins now cover four sites rather than two, and why (a refactor that reformats a
predicate is exactly the failure SC2 is worried about).
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/services/test_eval_queue.py -k benchmark_selection_gate -x` exits 0 and collects at least 8 tests.
    - `uv run pytest tests/services/test_eval_queue.py -x` exits 0 (no regression in the 52 pre-existing tests).
    - Source: `_claim_tier4_blob` and `_claim_tier4_bestmove` each interpolate `_selection_gate_clause()` at exactly two positions.
    - Behavior: making `_selection_gate_clause()` return a single space when the flag is off causes all four byte-identity tests to fail.
    - Behavior: with the flag on and one row in `benchmark_selection`, `_claim_tier4_blob` and `_claim_tier4_bestmove` each return that game and not an unselected sibling.
    - CLI: `uv run ty check app/ tests/` exits 0 with zero errors.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/services/test_eval_queue.py -x</automated>
  </verify>
  <reversibility rating="reversible">Predicate additions behind an off-by-default flag, each pinned by a byte-identity test.</reversibility>
  <done>All four lottery predicate sites honour the gate; the flag means "this backend only works on selected games", and prod's rendered SQL is provably unchanged with the flag off.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fail-closed boot assertion when the gate is on and the table is missing</name>
  <files>app/services/eval_queue_service.py, app/main.py, tests/test_main_lifespan.py</files>
  <read_first>
    - app/core/config.py lines 133-155 (`assert_secret_key_configured` — the fail-closed deploy-blocker pattern and its docstring style, including why it runs from lifespan and not at import time)
    - app/main.py lines 88-115 (the lifespan preamble: `assert_secret_key_configured()`, `get_insights_agent()`, `cleanup_orphaned_jobs()`, `start_engine()`, `start_maia()`)
    - app/core/database.py lines 15-30 (`engine`, `async_session_maker`, `get_async_session`)
    - tests/test_main_lifespan.py (the monkeypatched-lifespan smoke-test shape and `EXPECTED_TASKS`)
    - app/services/eval_queue_service.py (where `_selection_gate_clause()` lives, from 212-01)
  </read_first>
  <behavior>
    - Gate flag False, table absent: the assertion is a no-op and does not touch the database at all.
    - Gate flag True, table present: the assertion returns without raising.
    - Gate flag True, table absent: the assertion raises `RuntimeError` whose message names `benchmark_selection` and the flag, and does not name any credential.
    - The lifespan calls the assertion before `start_engine()`, so a misconfigured instance never pays the Stockfish/Maia startup cost before failing.
  </behavior>
  <action>
Add `async def assert_benchmark_selection_gate_ready() -> None` to
`app/services/eval_queue_service.py`, immediately below `_selection_gate_clause()` so the
invariant sits next to the predicate it protects. Behavior: return immediately when
`settings.BENCHMARK_SELECTION_GATE_ENABLED` is False (no session is opened, so dev, CI and prod
pay nothing). When True, open a session from `async_session_maker` and execute
`SELECT to_regclass('public.benchmark_selection')`; if the scalar result is None, raise
`RuntimeError` with a message that names the flag and the table and instructs the operator to
run `scripts/benchmark_lane.py select` first. Follow the CLAUDE.md Sentry rule: no variable
data interpolated into the message string (nothing variable is needed here — the flag name and
table name are constants). Write the docstring in `assert_secret_key_configured`'s style,
stating that this is called from the lifespan rather than at import so `scripts/` and Alembic,
which import `settings` but never claim work, are unaffected, and that failing closed is the
point: a gate-on instance with no table would otherwise raise `UndefinedTable` on every claim
for hours while looking merely idle from the outside.

Wire it into `app/main.py`'s lifespan, awaited directly after `cleanup_orphaned_jobs()` and
before `start_engine()`. Add a comment at the call site stating the two failure modes it
catches: a gate-on instance whose benchmark tables were never created, and the far worse
Pitfall-5 case where plain `DATABASE_URL` was left pointing at the dev database on :5432 (which
has no `benchmark_selection` table, so the assertion converts a silent
wrong-database-corruption into a startup abort).

Add tests to `tests/test_main_lifespan.py` reusing that module's existing monkeypatched-
lifespan harness: `test_benchmark_gate_assertion_noop_when_flag_off` (flag off, the DB is never
touched — assert the session maker is not called), `test_benchmark_gate_assertion_passes_when_table_present`,
and `test_benchmark_gate_assertion_aborts_startup_when_table_missing` (flag on, `to_regclass`
returns None, entering the lifespan raises `RuntimeError` and the background tasks in
`EXPECTED_TASKS` are never spawned). Keep the existing `EXPECTED_TASKS` assertions green.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_main_lifespan.py -x` exits 0.
    - `uv run pytest tests/test_main_lifespan.py -k benchmark_gate -x` collects 3 tests and exits 0.
    - Source: `app/services/eval_queue_service.py` defines `async def assert_benchmark_selection_gate_ready() -> None` and its body queries `to_regclass`.
    - Source: `app/main.py`'s lifespan awaits `assert_benchmark_selection_gate_ready()` at a line index lower than its `await start_engine()` line.
    - Behavior: removing the `raise` from the assertion makes `test_benchmark_gate_assertion_aborts_startup_when_table_missing` fail.
    - CLI: `uv run ty check app/ tests/` exits 0 with zero errors.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_main_lifespan.py -x</automated>
  </verify>
  <reversibility rating="reversible">A guarded startup check that is a no-op whenever the flag is off.</reversibility>
  <done>A gate-on instance with no `benchmark_selection` table refuses to start, and the same check incidentally catches a `DATABASE_URL` still pointing at the dev database.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config flag -> lottery claim scope | a boolean decides whether the backend works on 20 games or 2.1M |
| process env -> connected database | plain `DATABASE_URL` decides which database receives every write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-212-02 | Denial of Service (of the tranche's controlled scope) | `BENCHMARK_SELECTION_GATE_ENABLED=true` with `benchmark_selection` absent | high | mitigate | Task 2's `assert_benchmark_selection_gate_ready()` aborts uvicorn startup before the engine pool is built. This is the security-relevant reason D-10 point 3 exists, not merely a correctness nicety. |
| T-212-05 | Tampering | plain `DATABASE_URL` left pointing at the dev DB on :5432 | high | mitigate | Same assertion: the dev database has no `benchmark_selection` table, so a gate-on instance aimed at the wrong database fails closed at startup instead of writing benchmark-lane output into dev. |
| T-212-03 | Tampering | the gate SQL fragment at four sites | low | accept | `_BENCHMARK_SELECTION_GATE_SQL` remains a single module-level literal, reused at all four sites, never composed from request or user input (QUEUE-08). |
| T-212-06 | Information disclosure | the boot assertion's error message | low | mitigate | The message names only the flag and the table — no connection string, host, or credential — so a startup failure in a shared log does not leak the benchmark role's credentials. |
</threat_model>

<verification>
- `uv run pytest tests/services/test_eval_queue.py tests/test_main_lifespan.py -x` green.
- `uv run ruff check app/ tests/` and `uv run ty check app/ tests/` clean.
- All four gated predicate sites carry a frozen-literal byte-identity test.
</verification>

<success_criteria>
BENCHLANE-02 is fully satisfied: the gate is off by default, byte-identity-proven inert at all
four sites when off, actually narrows every lane when on, and refuses to start when on without
its table.
</success_criteria>

<output>
Create `.planning/phases/212-benchmark-full-game-analysis-lane/212-02-SUMMARY.md` when done
</output>
