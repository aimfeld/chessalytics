---
phase: 212-benchmark-full-game-analysis-lane
plan: 10
type: execute
wave: 9
depends_on: ["212-09"]
gap_closure: true
files_modified:
  - reports/benchmark-lane/benchmark-lane-classical-YYYY-MM-DD.md
  - docs/benchmark-lane-runbook.md
autonomous: false
requirements: [BENCHLANE-05, BENCHLANE-06]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The classical tranche either completes or is stopped at the classical TC boundary by an explicit operator choice; it is never left in an ambiguous half-stopped state, and a partial result is labelled as partial in prose so a later reader cannot mistake it for a complete one."
    - "A record report exists under `reports/benchmark-lane/` carrying the classical tranche's selected count, the four completion counts, and the downstream `game_positions`, `game_flaws` and `game_best_moves` row counts, with the counts internally consistent: best moves done never exceeds PV done, which never exceeds selected."
    - "A vacuum with analyze has been run against the tables the tranche churned, and the before size, after size and reclaimed delta are recorded alongside the row counts."
    - "The eval-source homogeneity consequence is on the record before the classical drain starts, not after: after this tranche, flaw rows in the benchmark database span two eval sources, and the lichess analysis marker no longer implies the stored eval came from lichess, with `benchmark_selection` remaining the split key."
    - "The paired same-position check the user asked to remain possible works: for one selected lichess-arm game, at least one ply's stored eval now differs from its preserved snapshot value while that game's lichess analysis marker is still set."
    - "The gate held across a multi-day run at full fleet capacity: the corpus-wide count of games with a non-null `evals_completed_at` is unchanged from the tranche-start baseline frozen in 212-09."
  prohibitions:
    - statement: "The benchmark database reset subcommand and the dev database reset script must never be run while a tranche is in flight. Reset on the benchmark database destroys the entire corpus and every completed tranche, not just the current one, and there is no backup path for it in this project."
      status: resolved
      verification: judgment
    - statement: "No later TC tranche may be selected or snapshotted while classical is in flight. The locked TC ordering is what makes the program stoppable at a clean boundary, and materializing the next tranche mid-run dissolves that boundary."
      status: resolved
      verification: judgment
  artifacts:
    - reports/benchmark-lane/benchmark-lane-classical-YYYY-MM-DD.md
  key_links:
    - "the readiness file's coverage difference -> the blocking decision -> permission to start the homogenized drain"
    - "record output plus vacuum -> the BENCHLANE-06 artifact"
    - "tranche-start completion baseline -> the post-run leak check"
---

<objective>
Run the classical tranche the phase exists to produce, then record it and reclaim the space it
consumed.

Purpose: everything in plans 01 through 09 exists to make this run safe, scoped, stoppable and
reproducible. This plan supersedes 212-06 Tasks 2 and 3, which were authorized on a 20-game
smoke tranche, attempted, and aborted 80 seconds in on a defect 212-07 has since fixed and
212-08 has since proven fixed. Do not re-run 212-06; run this.

Output: a populated classical tranche in the benchmark database, a timestamped record report,
a vacuumed database, and a filled-in operator record of what was actually done.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/212-benchmark-full-game-analysis-lane/212-CONTEXT.md
@.planning/phases/212-benchmark-full-game-analysis-lane/212-09-TRANCHE-READINESS.md
@.planning/phases/212-benchmark-full-game-analysis-lane/212-08-SUMMARY.md
@docs/benchmark-lane-runbook.md
</context>

## What this plan closes

The remaining three `missing:` items of BENCHLANE-06 in `212-VERIFICATION.md`: a completed or
deliberately-stopped-at-boundary fleet run, the record report artifact, and the post-run vacuum.
It also carries BENCHLANE-05's written-down half at the decision checkpoint, the same way 212-06
Task 1 did.

## Artifacts this plan produces

No new source symbols. Its output is the generated report under `reports/benchmark-lane/`, the
tranche data itself, and the filled-in record section of the operator runbook.

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Authorize the one-way overwrite on current, measured numbers</name>
  <reversibility rating="one-way">Starting the homogenized drain overwrites the stored eval and mate values in place for every selected classical lichess-arm game. For any game not covered by the snapshot table, that loss is permanent: lichess's stored values for those positions exist nowhere else in this project and re-fetching them is out of scope. Snapshot coverage is what downgrades this from one-way to costly, so it is verified by count rather than assumed. The blocking gate is the point — auto-approving this defeats it entirely, and the automatic-advance setting does not apply.</reversibility>
  <decision>Start the classical tranche now with eval-source homogenization on, plan to stop at the classical boundary rather than continuing to rapid, or defer the tranche entirely.</decision>
  <context>
Read first: `212-09-TRANCHE-READINESS.md` in full, which 212-09 wrote specifically so this
checkpoint could be presented on current numbers rather than assembled live; runbook sections 1
through 3; and CONTEXT decisions D-01, D-03, D-04 and D-05 with the reversibility ratings
recorded there.

Present the five numbers from the readiness file, and re-query each one live before presenting
it. The readiness file exists so the checkpoint is never blocked on assembling them, not so they
can be quoted stale — 212-06's first presentation could not offer the start option at all
because its snapshot coverage gap was nine of nine, and the numbers moved once that was fixed.
The five are: the classical selection count split by arm; the count of distinct selected
lichess-arm games covered by the snapshot; the difference between those two; confirmation that
neither the gate flag nor the homogenization flag appears in any environment dotfile, so both
stay command-line-only for the port 8001 instance; and free disk against the readiness file's
computed verdict.

Also present two numbers 212-06 did not have, because they are what makes a retry different from
a repeat: the corpus-wide completion count frozen as the tranche-start baseline, which the
post-run task compares against; and 212-08's result, naming the specific game id proven end to
end on the gated build.

If the coverage difference is anything other than zero, do not offer the start option. Re-run the
snapshot subcommand for the classical tranche, re-check, and re-present with fresh numbers.

What is being started: the classical tranche, both arms per D-01, on the order of 54,390 capped
games at roughly 3.4 days of budgeted fleet throughput.

What becomes irreversible: stored eval and mate values for the classical lichess arm are
overwritten in place with our own engine's values. Recovery is a restore from the snapshot table
and nothing else.

What stays reversible: the games' lichess analysis marker is untouched per D-04, so the arm split
survives regardless, and every code change involved is flag-gated and reverts cleanly.

The consequence being accepted, for the record — this is BENCHLANE-05's written-down half:
after this tranche, flaw rows in the benchmark database span two eval sources, and the lichess
analysis marker no longer implies the stored eval came from lichess. The split stays recoverable
via the selection table's arm flag, and the disclosure already lives in the benchmarks skill
alongside CONTEXT decisions D-03 through D-06.

Record the operator's choice and every number presented in the plan summary.
  </context>
  <options>
    <option id="start-now">
      <name>Start the classical tranche with homogenization on, running to completion</name>
      <pros>Delivers the phase's actual product: both arms present and comparable in-population, so the selection-bias check stops having to borrow production accounts and concede that the direction of the effect need not transfer. Coverage verified at a zero gap means the overwrite is recoverable, and 212-08 has proven the gated pipeline end to end on a real game.</pros>
      <cons>Roughly 3.4 days of fleet time committed. The classical lichess arm's stored evals are overwritten in place; recovery depends entirely on the snapshot table.</cons>
    </option>
    <option id="start-and-stop-at-boundary">
      <name>Start, but plan to stop at the classical boundary rather than continuing to rapid</name>
      <pros>The same product for the tranche that matters most, with the fleet released afterwards. This is a first-class outcome under the phase's success criteria, not a compromise. It is the right choice when the readiness file's disk verdict was narrow or when the fleet is needed elsewhere.</pros>
      <cons>Later tranches stay unstarted, so any cross-time-control comparison waits for a future run.</cons>
    </option>
    <option id="defer-tranche">
      <name>Defer the tranche entirely</name>
      <pros>Nothing is overwritten. All code from plans 01 through 09 is merged and inert, so the tranche can start any time later with no rework, and the selection and snapshot already materialized stay valid.</pros>
      <cons>BENCHLANE-06 stays unmet — it cannot be satisfied without a run — and the phase's goal remains a description of plumbing rather than of data.</cons>
    </option>
  </options>
  <resume-signal>Select: start-now, start-and-stop-at-boundary, or defer-tranche</resume-signal>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Run the classical tranche to completion or to the TC boundary</name>
  <files>none</files>
  <read_first>
    - docs/benchmark-lane-runbook.md sections 3 through 6, including the rung-by-rung scoping table and the entry-lane troubleshooting entry that 212-07 added
    - .planning/phases/212-benchmark-full-game-analysis-lane/212-03-SUMMARY.md (the fallback URL and fallback token surface)
    - .planning/phases/212-benchmark-full-game-analysis-lane/212-08-SUMMARY.md (what a healthy first two minutes looked like on the gated build)
  </read_first>
  <precondition>Task 1's decision resolved to start-now or start-and-stop-at-boundary, with the snapshot coverage difference confirmed at zero.</precondition>
  <action>Run the classical tranche on the fleet, from launching the port 8001 backend through to the tranche completing or being stopped at the classical boundary.</action>
  <instructions>
This is unattended fleet time measured in days, driven by long-lived server and worker processes
on the operator's own box. It cannot be carried out inside an agent task, and the launch requires
two secrets an agent session cannot read. Everything automatable was built in plans 01 through 09.

Steps, all detailed in the runbook:

1. Launch the second backend on port 8001 per runbook section 3, with all five flags on the
   command line and none of them in a dotfile.
2. Confirm Maia loaded from the startup log per runbook section 4. Do not proceed past a
   Maia-absent start — the tranche would produce PV with no gem tiers and would have to be
   partially re-run, and the failure is silent.
3. Watch the first two minutes against 212-08's recorded shape. Two things should be true: the
   entry-ply lane reports nothing to do, which is expected on this instance and covered in the
   troubleshooting section, and the tranche status starts moving on selected games. If the
   corpus-wide completion count climbs instead, stop immediately — that is 212-06's failure
   recurring despite 212-07, and it is a code problem, not an operational one.
4. Add the fallback URL and fallback token to the fleet's worker invocations per runbook section
   5. Production keeps strict per-claim priority; the benchmark backend receives only the cycles
   where production's whole ladder had nothing to do. Repeat across every worker box that should
   help drain the lane.
5. Monitor with the status subcommand per runbook section 6. Watch specifically for the
   Maia-absent warning line, and for best moves done tracking PV done rather than lagging at
   zero.
6. Watch free disk on the benchmark volume through the run against the readiness file's transient
   figure. These are updates, so dead row versions roughly double the footprint until vacuumed.

Stopping is a first-class outcome, not a failure. The TC tranches exist precisely so the program
is stoppable at a boundary. If you stop after classical rather than continuing to rapid, that
satisfies the success criterion as written — say so on resume, and Task 3 records it as a
deliberate stop.

Do not run the benchmark database reset subcommand or the dev database reset script at any point
during this task, and do not select or snapshot a later tranche while this one is in flight.
  </instructions>
  <verification>`uv run python scripts/benchmark_lane.py status --tranche classical --db benchmark` reports best moves done equal to selected, or the counts the operator chose to stop at.</verification>
  <resume-signal>Type "complete" when the tranche finished, "stopped at boundary" if you chose to stop after classical, or "leak recurred" if the corpus-wide completion count moved</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the row counts, vacuum, and prove the invariants held</name>
  <files>reports/benchmark-lane/benchmark-lane-classical-YYYY-MM-DD.md, docs/benchmark-lane-runbook.md</files>
  <read_first>
    - docs/benchmark-lane-runbook.md section 7 and section 9 (the record and vacuum procedure, and the disk budget)
    - .claude/skills/db-report/SKILL.md (the storage and bloat metrics worth quoting alongside the vacuum result)
    - scripts/benchmark_lane.py, the record subcommand written in 212-05
    - .planning/phases/212-benchmark-full-game-analysis-lane/212-09-TRANCHE-READINESS.md (the tranche-start completion baseline this task compares against)
  </read_first>
  <precondition>Task 2 has ended, either by tranche completion or by an explicit stop at the classical boundary, and the benchmark Postgres on port 5433 is still running.</precondition>
  <action>
Produce the phase's closing artifacts, and confirm in the same pass that the three invariants the
run was supposed to preserve actually did.

Run the record subcommand for the classical tranche against the benchmark database. Confirm the
file lands under `reports/benchmark-lane/` with the tranche and date in its name, and that its
counts are non-zero and internally consistent: best moves done must not exceed PV done, which
must not exceed selected.

Note the benchmark database's on-disk size, then run a vacuum with analyze. Prefer a targeted
pass over the four tables the tranche actually churned — positions, flaws, best moves and games
— before any database-wide pass, since those carry essentially all of the dead row versions and
a whole-database vacuum is far slower for no additional benefit here. Run it with the
write-capable role. Note the size after, and append the before size, the after size and the
reclaimed delta to the record report under a post-run vacuum heading.

Then confirm the three invariants, each as a stated result in the report rather than an
assumption:

First, the gate held. Compare the corpus-wide count of games with a non-null completion stamp
against the tranche-start baseline in the readiness file. It must be unchanged. This is the
multi-day, full-fleet version of the check 212-08 ran over minutes and one worker, and it is the
real proof that 212-07 closed the lane. A non-zero delta is a finding to report prominently, not
a footnote.

Second, the homogenized overwrite happened and D-04 held. Pick a selection row whose arm flag is
true, and confirm at least one ply where the stored eval in positions now differs from the
matching preserved snapshot value, while that game's lichess analysis marker is still set. This
is the paired same-position comparison the user asked to remain possible, run once as a smoke
check.

Third, the stop state is unambiguous. If the operator stopped at the boundary rather than
completing, say so explicitly in the report's prose so a later reader cannot mistake a partial
tranche for a complete one, and state the selected-versus-completed gap in words as well as in
the count table.

Finally, fill in the runbook's record-of-what-was-done section for this run: the tranche, the
date range, the number of worker boxes, any deviation from the documented procedure, and the
final status and record output. That section exists to be filled in and has been empty since
212-05 created it.
  </action>
  <acceptance_criteria>
    - CLI: the record subcommand for the classical tranche exits 0 against the benchmark database.
    - Source: a report file exists under `reports/benchmark-lane/` for the classical tranche and is non-empty.
    - Source: that report contains a post-run vacuum heading with a before size, an after size and the reclaimed delta.
    - Behavior: in the report's count table, best moves done is less than or equal to PV done, which is less than or equal to selected.
    - Behavior: the corpus-wide completion count equals the tranche-start baseline from the readiness file, and the report states both numbers side by side.
    - Behavior: for one sampled lichess-arm game, at least one ply's stored eval differs from the matching preserved snapshot value, and that game's lichess analysis marker is still set.
    - Source: when the tranche was stopped rather than completed, the report says so in prose and quantifies the gap.
    - Source: the runbook's record-of-what-was-done section is filled in for this run.
  </acceptance_criteria>
  <verify>
    <automated>uv run python scripts/benchmark_lane.py record --tranche classical --db benchmark &amp;&amp; ls reports/benchmark-lane/benchmark-lane-classical-*.md</automated>
    <human-check>The report's counts are internally consistent, the vacuum section quantifies the reclaim, the completion-count comparison shows a zero delta, and the paired check confirms both that the eval was overwritten and that the lichess analysis marker survived.</human-check>
  </verify>
  <reversibility rating="reversible">Report generation, a vacuum and read-only checks; none of it changes logical data.</reversibility>
  <done>BENCHLANE-06's deliverables exist — row counts recorded in a durable timestamped artifact and a quantified post-run vacuum — and the run's three invariants are confirmed in writing rather than assumed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator decision -> irreversible in-place overwrite | approval at Task 1 authorizes overwriting stored lichess evals for the classical lichess arm |
| operator shell -> benchmark Postgres (write role) | destructive statements and the vacuum run here |
| LAN worker fleet -> local backend on port 8001 | days of unattended claim traffic cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-212-31 | Tampering / data loss | in-place overwrite of the classical lichess arm's stored evals | high | mitigate | Task 1 is a blocking decision checkpoint that re-queries snapshot coverage live and refuses to offer the start option on a non-zero gap; 212-09 already proved coverage complete before the checkpoint is reached. |
| T-212-32 | Denial of Service | the database reset scripts run mid-tranche | high | mitigate | Explicit prohibition in this plan's must-haves, in Task 2's instructions, and in the runbook's warning; the blast radius (the whole corpus, no backup path) is stated each time. |
| T-212-33 | Denial of Service | the benchmark volume filling from dead row versions during a multi-day run | medium | mitigate | 212-09 computed the headroom verdict against both the net and transient budgets before the decision; Task 2 step 6 makes disk part of the monitoring loop; Task 3 vacuums and quantifies the reclaim. |
| T-212-34 | Repudiation | a partial tranche later read as a complete one | medium | mitigate | Task 3 requires the stop-at-boundary case to be stated in prose and quantified, and the record subcommand carries selected-versus-completed counts side by side so the gap is visible without prose. |
| T-212-35 | Denial of Service (of the tranche's controlled scope) | a leak recurring at full fleet capacity over days rather than minutes | high | mitigate | Task 2 step 3 makes the first two minutes an explicit watch against 212-08's recorded shape with an immediate stop condition; Task 3 compares the corpus-wide completion count against the tranche-start baseline as a hard pass or fail. |
| T-212-SC | Tampering | package-manager installs | n/a | accept | This plan installs no npm, pip or cargo packages. |
</threat_model>

<verification>
- The status subcommand reflects either completion or an explicit boundary stop for the classical tranche.
- A record report exists under `reports/benchmark-lane/` with counts plus a post-run vacuum section.
- The corpus-wide completion count equals 212-09's tranche-start baseline.
- The paired check confirms an overwritten eval and a surviving lichess analysis marker.
- Full pre-merge gate green before integrating the phase: `uv run ruff format app/ tests/`, `uv run ruff check app/ tests/ --fix`, `uv run ty check app/ tests/`, `uv run pytest -n auto -x`, and the frontend lint and test pair.
</verification>

<success_criteria>
BENCHLANE-06 is satisfied: the classical tranche completed or stopped at a TC boundary by
choice, row counts are recorded in a timestamped artifact, and a post-run vacuum has been
performed and quantified. BENCHLANE-05's written-down half is confirmed at Task 1's decision
checkpoint. The phase goal is finally true of the data, not only of the plumbing.
</success_criteria>

<output>
Create `.planning/phases/212-benchmark-full-game-analysis-lane/212-10-SUMMARY.md` when done
</output>
