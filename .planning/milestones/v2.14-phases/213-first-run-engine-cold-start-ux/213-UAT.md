---
status: complete
phase: 213-first-run-engine-cold-start-ux
source: 213-01-SUMMARY.md, 213-02-SUMMARY.md, 213-03-SUMMARY.md, 213-04-SUMMARY.md, 213-05-SUMMARY.md, 213-06-SUMMARY.md, 213-07-SUMMARY.md, 213-VERIFICATION.md
started: 2026-08-28T00:00:00Z
updated: 2026-08-29T12:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. supportsWasmSimd() — synchronous WASM-SIMD capability probe that never throws

expected: supportsWasmSimd() returns a boolean and never throws
result: pass
source: automated
coverage_id: 213-01/D1

### 2. engineAssetProgress.ts — byte-weighted N-asset store (coercion/clamping/monotonicity, D-04 cache-miss gate, D-13 unsupported/failed transitions)

expected: store coerces, clamps and stays monotonic; gate and terminal transitions behave
result: pass
source: automated
coverage_id: 213-01/D2

### 3. useEngineAssets — D-11 byte-weighted aggregate read model

expected: aggregate is byte-weighted, proven load-bearing against a per-asset-average mutation
result: pass
source: automated
coverage_id: 213-01/D3

### 4. Cold-start download progress is real (bytes, not a fake spinner)

expected: |
  On the Bots page with a cleared cache and DevTools throttled to Slow 4G, pick a
  blend-0 persona and start a game. A non-dismissible gate Dialog appears and its
  single progress bar advances smoothly in step with the real 45.7 MB Maia model
  download (watch the Network tab) — it is not a fake/indeterminate animation, does
  not jump straight to 100%, and does not stall at 0% while bytes are clearly flowing.
result: pass
note: "Behavior confirmed. User requested two readout additions - logged as gap G-213-4."

### 5. maiaWorkerHost.ts — progress/ready forwarding + D-13 choke point in ensureSpawned()

expected: progress/ready messages reach the store; zero Workers constructed on an unsupported device
result: pass
source: automated
coverage_id: 213-01/D5

### 6. MaiaQueue.whenReady() — pure forwarding to the lease's whenReady()

expected: whenReady() stays pending until the lease reports ready, then resolves with the backend
result: pass
source: automated
coverage_id: 213-01/D6

### 7. components/ui/progress.tsx — Radix Progress wrapper, no hand-rolled ARIA

expected: gate renders real byte progress through the Radix primitive
result: pass
source: automated
coverage_id: 213-01/D7

### 8. Engine readiness gate looks right (desktop + mobile)

expected: |
  The gate Dialog is visually correct on desktop and on a narrow/mobile viewport:
  sized and centred sensibly, title/subtext readable and not clipped, one progress
  bar (never one row per asset), Start clearly disabled while downloading and
  enabled once ready, and no way to dismiss it (no X, no click-outside, no Esc).
  Copy reads as finished product, not placeholder text.
result: pass

### 9. useBotGame.ts — D-05 live-gating initializer + D-03 pool.warm() blend-0 skip

expected: gating initializer and blend-0 warm skip behave inside the []-deps bring-up effect
result: pass
source: automated
coverage_id: 213-01/D9

### 10. The originating bug is gone: bot no longer burns its clock before move 1

expected: |
  Same Slow-4G cold-cache run as test 4, with a 3+0 preset as a guest: the clock does
  NOT start while the download is in flight. It starts only after you tap Start, and
  the bot has its full time. (Before this phase, the bot flagged or lost most of its
  clock before its first move.)
result: pass

### 11. Persona avatar bundle shrinks ~836 KB → ~137 KB (128px WebP variants, masters preserved)

expected: bundled avatar payload drops to ~137 KB; masters byte-identical in a non-bundled dir
result: pass
source: automated
coverage_id: 213-02/D1

### 12. gen_persona_avatars.py --rebuild-variants works offline (no API key, no network)

expected: rebuild regenerates all 24 variants from kept masters with no GOOGLE_API_KEY
result: pass
source: automated
coverage_id: 213-02/D2

### 13. All three persona avatar render sites lazy-load their <img>

expected: PersonaCard, PersonaDetailSurface and ClockDisplay each use loading="lazy"
result: pass
source: automated
coverage_id: 213-02/D3

### 14. WorkerPool.whenReady() — resolves on the first slot's readyok, re-pends after respawn

expected: resolves on first slot ready, immediately once ready, re-pends after terminate()+respawn
result: pass
source: automated
coverage_id: 213-03/D1

### 15. createSlot() progressPort wiring — port before handshake, missing/zero total stays finite

expected: every fresh worker gets a MessageChannel port before the uci handshake; no MessageChannel degrades gracefully
result: pass
source: automated
coverage_id: 213-03/D2

### 16. engineGateRequired() blend>0 branch — gated on clean cache, ungated once BOTH assets seen

expected: gate required until both assets are seen; still gated when only one is seen
result: pass
source: automated
coverage_id: 213-03/D3

### 17. EngineReadyGate for blend>0 — exactly ONE aggregate bar, subtext names the in-flight asset

expected: one progress element (never per-asset rows), subtext switches asset, Start disabled until both done
result: pass
source: automated
coverage_id: 213-03/D4

### 18. useBotGame live initializer for blend>0 — false on clean cache, true when both seen

expected: initializer reads the real seen flags at mount
result: pass
source: automated
coverage_id: 213-03/D5

### 19. Blend>0 persona (1600/1800 rung) waits behind the gate, no double Stockfish fetch

expected: |
  Cold cache, Slow 4G, pick a persona on the 1600 or 1800 rung (blend > 0) and start a
  game. The gate waits for BOTH the Maia model and the Stockfish .wasm, showing one
  combined bar. The game starts only when you tap Start. In the Network tab the
  Stockfish .wasm (~7.3 MB) is fetched exactly once — not twice.
result: pass
note: "Original response was a design change request (always download both models), logged as gap G-213-19b, LOCKED by the user and implemented by 213-06. The blend-conditional split this test targeted no longer exists; user has since observed the unified 66.5 MB bundle across the 213-08..12 cold-cache rounds."

### 20. Failed download offers a Retry that actually recovers

expected: |
  Start a cold-cache bot game, then kill the network mid-download (DevTools → Offline)
  until the gate shows its failed state. Bring the network back and press Retry: the
  download resumes from 0% and completes, Start becomes enabled, and the game plays
  normally. (The worker also retries once silently on its own, so you may need to drop
  the connection more than momentarily to reach the failed state.)
result: pass

### 21. failAllLeasesAndDropWorker marks 'failed' but never downgrades 'unsupported'

expected: terminal pre-ready error marks failed; an existing unsupported status is preserved
result: pass
source: automated
coverage_id: 213-04/D2

### 22. WebGPU-unavailable respawn and post-ready errors stay transparent to the store

expected: neither ever marks the asset store 'failed'
result: pass
source: automated
coverage_id: 213-04/D3

### 23. markEngineAssetsRetrying() clears 'failed' → 'idle' without touching progress or seen flags

expected: retry seam resets status only
result: pass
source: automated
coverage_id: 213-04/D4

### 24. 'unsupported' terminal state is an honest dead end with NO button

expected: points at the analysis board and importing games; no button of any kind; never the LoadError trailer copy
result: pass
source: automated
coverage_id: 213-04/D5

### 25. 'failed' terminal state renders a single Retry that clears the status and fires onRetry once

expected: Retry returns the gate to its downloading state
result: pass
source: automated
coverage_id: 213-04/D6

### 26. retryEngineWarm() calls queue.warm() always, pool.warm() only for blend>0

expected: warm fan-out matches the persona's blend
result: pass
source: automated
coverage_id: 213-04/D7
superseded: true
superseded_by: G-213-19b (213-06) removed the blend branch — retryEngineWarm() now fans out to BOTH providers unconditionally. Re-locked by 213-06's own tests.

### 27. Umami: engine-gate-shown / -started / -abandoned fire once each, correctly bucketed

expected: shown once per mount with bucketed assets; started once on Start with wait_bucket; abandoned once on unmount/pagehide and never after started
result: pass
source: automated
coverage_id: 213-04/D8

### 28. Sentry: exactly one captureException per mount per terminal state, zero on a clean run

expected: fixed non-interpolated messages with device context; clean idle→downloading→ready→Start produces zero Sentry calls
result: pass
source: automated
coverage_id: 213-04/D9

### 29. useFlawChessEngine.isReady true only once BOTH queue and pool report ready

expected: rejection captures once to Sentry and still flips isReady; no state update after unmount
result: pass
source: automated
coverage_id: 213-05/D1

### 30. EngineLinesSkeleton swaps pulsing rows for a real progress readout, no layout jump

expected: identical fixed-height container whether showing pulses or "{label} — {percent}%"
result: pass
source: automated
coverage_id: 213-05/D2
superseded: true
superseded_by: G-213-34 (213-07) deleted the per-card readouts entirely — EngineLinesSkeleton renders its original pulsing placeholders and reads no download state.

### 31. MovesByRatingChart skeleton shows the same readout while maia-model downloads

expected: MaiaChartSkeleton readout while downloading, original pulsing block otherwise; hook never behind a conditional return
result: pass
source: automated
coverage_id: 213-05/D3
superseded: true
superseded_by: G-213-34 (213-07) deleted the Maia chart's store read — the chart skeleton no longer shows any readout.

### 32. Standalone Stockfish worker reports progress under stockfish-wasm and marks ready on readyok

expected: useStockfishEngine wires the vendored glue's progressPort protocol
result: pass
source: automated
coverage_id: 213-05/D4

### 33. All three EngineLinesSkeleton mount sites in Analysis.tsx get live assetProgress

expected: desktop and mobile show identical readouts under the same store state, proven independent by mutation
result: pass
source: automated
coverage_id: 213-05/D5
superseded: true
superseded_by: G-213-34 (213-07) removed the assetProgress plumbing from all three Analysis.tsx mount sites.

### 34. Analysis board is never gated — stays fully interactive while engines download

expected: |
  Cold cache, Slow 4G, open the analysis board on any game. No gate Dialog or blocking
  overlay ever appears: you can move pieces, step through moves and use the board the
  whole time. The engine-lines panel and the Maia chart show a live download readout
  (label plus percent) in place of their pulsing skeletons, and the surrounding layout
  does not jump when the readout appears or disappears.
result: pass
note: "Superseded — the expectation itself was reversed by G-213-34 (user-locked): the analysis board IS now gated by the same modal. Replaced by test 35, which passed 2026-08-29. Recorded as pass per this file's superseded-checkpoint convention (tests 26/30/31/33)."
reported: "the separate progress bars on the analysis board are a mess and don't work properly. Gate the analysis board behind the same modal with the single progress bar."
observed: bars disagreed with each other; a readout stuck and never reached 100%; long stretches of downloading with no progress shown at all
severity: major
superseded: true
superseded_by: REVERSED by G-213-34 (213-07): the analysis board is now gated by the same non-dismissible modal as Bots. This is the issue that produced the reversal; see test 35 for its replacement.

### 35. G-213-34 cold-cache Slow-4G Network-tab script for the single analysis-board gate bar

expected: |
  Clear the cache, throttle DevTools to Slow 4G, open the analysis board on any game.
  The same non-dismissible EngineReadyGate as Bots appears (no X, no Esc, no
  click-outside) and the board is unreachable until you tap Start. Its ONE aggregate
  bar advances in step with the real Network tab: it never stalls at 0% while bytes are
  clearly flowing, never goes backwards, and never sticks below 100% once both
  transfers finish. Total transferred is ~53.0 MB and each asset is fetched exactly
  once. The "Download complete. Starting the engine..." readout appears before Start
  enables. No second progress surface appears anywhere on the page. Repeat on a narrow
  /mobile viewport — identical behaviour. Then reload with a warm cache: no gate at all.
result: pass
note: "Re-verified by the user 2026-08-29 after gap closure 213-08..12 (response: pass)."
history: |
  Round 1 (2026-08-28): FAILED — "The modal with the progress bar opens, but it moves
  extremly slowly ... stopped at 3% for a long time" → gaps G-213-35 / 35-c / 36 / 37,
  closed by plans 213-08..213-12 (all executed; 213-12 Task 4 blocking checkpoint
  APPROVED by the user, commit 197b1c37e; re-verification passed 39/39).
  Re-presented for final confirmation after gap closure.
source: human
why_human: |
  No automated check can observe real network timing or byte transfer against a live
  Network tab. This is 213-07-PLAN.md Task 2's own designated blocking human-check,
  deferred to end-of-phase UAT. It is the check that proves the UAT-reported defects
  (disagreeing bars, one stuck below 100%, silent stretches) died with the deleted
  readouts rather than living on in the store behind the single modal bar.
coverage_id: 213-07/G-213-34
tracked_in: .planning/WINDOWS.md entry 8 (kind: unrun-verify, status: open)

## Summary

total: 35
passed: 34
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

- gap_id: G-213-4
  truth: "The gate's progress readout tells the user how much is left and that the wait is one-time"
  status: resolved
  reason: "User reported: pass. However, it probably makes sense to add the MB indicator in parenthesis, e.g. Maia model — 31% (23.1 / 45.2 MB). Also add a line below the title to explain that this is a one-time download."
  severity: minor
  test: 4
  resolved_by: 6a658c1c6
  resolved_at: 2026-08-28

- gap_id: G-213-8
  truth: "The gate reaches 100% and enables Start once the Maia model download completes"
  status: resolved
  resolved_by: 6a658c1c6
  resolved_at: 2026-08-28
  resolution: "Three download multipliers fixed: the model buffer is transferred from the dying WebGPU worker into the wasm replacement instead of being re-downloaded; a terminal fetch failure is no longer misreported as webgpu-unavailable (which triggered a respawn re-running the same doomed download); and WebGPU is pinned off for the rest of the page session once it has failed, so ensureSpawned() can never re-probe and re-download. User re-verified 2026-08-28 on cleared caches in Brave and Chrome: exactly one download."
  reason: "User reported: When the dialog reaches \"Maia model — 100%\", the start button doesn't enable. In the browser footer, I see that the download keeps running, currently att 101,700 kB and counting. Follow-up: 'It seems to download the maia model more than once'; then 'Progress is now shown as Maia model - 53% (it was 100% before)'; then 'I think it's on a third download now, 144,600 kB and counting' - so at least THREE full model fetches, not two."
  severity: blocker
  test: 8
  root_cause: "On a WebGPU-capable browser the 45.7 MB Maia model is downloaded TWICE. maia-worker.js initSession() auto mode calls fetchModelBuffer() INSIDE the WebGPU try block (maia-worker.js:276). Any WebGPU failure - session create, op support, or the lazy Clip shader compile that the warmup analyze() exists to surface - returns ok:false; the host then terminates the worker and respawnPinnedToWasm() spawns a fresh one that re-runs the full init sequence, re-fetching the whole model from scratch. The ArrayBuffer from the first fetch dies with the terminated worker. The store itself is behaving correctly: resetEngineAssetForRefetch('maia-model') (WR-02) drops the bar back for the second pass, which is why the user saw 100% (fetch 1 complete) -> a pause while the WebGPU session created and the warmup ran -> 53% (fetch 2 in flight). Start is gated on status ready, which only arrives when the SECOND download finishes - so on any WebGPU-capable browser whose GPU session fails, the cold-start wait is ~2x the intended 45.7 MB."
  artifacts:
    - path: "frontend/src/lib/engine/workerPool.ts"
      issue: "ensureSpawned() (line 1118) spawns computePoolSize() workers - up to DESKTOP_POOL_MAX=4 - in one synchronous loop, each `new Worker(ENGINE_PATH)` at line 1051 instantiating its own copy of the 7.3 MB stockfish wasm; issued in the same tick, so the HTTP cache cannot dedupe them"
    - path: "frontend/src/hooks/useStockfishEngine.ts"
      issue: "line 360 constructs a FIFTH worker against the same ENGINE_PATH, mounted by Analysis.tsx:689 alongside the pool"
    - path: "frontend/src/lib/engine/engineAssetProgress.ts"
      issue: "one 'stockfish-wasm' id with a 7,295,411 B denominator and Math.max-monotonic loaded; five concurrent progressPort reporters collapse into one bar showing a single worker's progress, hiding ~29 MB"
    - path: "frontend/public/maia/maia-worker.js"
      issue: "initSession() fetches the model inside the WebGPU try block (line 276); the downloaded buffer is discarded with the terminated worker, so the wasm respawn re-downloads all 45.7 MB"

    - path: "frontend/src/lib/engine/maiaWorkerHost.ts"
      issue: "respawnPinnedToWasm() terminates the worker and spawns a bare replacement with no way to hand it the bytes already fetched"
  missing:

    - "Fetch the model bytes once and reuse them across the WebGPU->wasm fallback - e.g. fetch on the main thread / host and transfer the ArrayBuffer into the worker, or cache the response so the respawn's fetch is served from the HTTP cache instead of the network"
    - "Confirm the model response is cacheable (Cache-Control / Cache Storage) so the second fetch, if it remains, costs no network bytes"

- gap_id: G-213-19
  truth: "The gate explains what is happening between the last byte landing and Start enabling"
  status: resolved
  reason: "User reported: in brave, the progress stopped for a few seconds at Maia model - 100 % (45.7 / 45.7 MB), before enabling the start button. In that moment, it is not clear what is going on. In chrome, the start button enabled immediately."
  severity: minor
  test: 8
  root_cause: "The bytes are only half the wait. After the last chunk the worker still builds the ONNX session and runs the warmup inference before posting ready. That is near-instant on the WebGPU path (Chrome) but takes seconds on the wasm fallback (Brave, where WebGPU is unavailable). The gate had no copy for that phase, so it sat on 100% (45.7 / 45.7 MB) and read as a hang."
  artifacts:

    - path: "frontend/src/components/bots/EngineReadyGate.tsx"
      issue: "readout showed a completed byte count through the session-init gap"
  resolved_by: fd0e43ebd
  resolved_at: 2026-08-28
  resolution: "Readout switches to Download complete. Starting the engine... once every required byte is in but the worker has not reported ready. Gated on BYTES, not percent, so it never fires while chunks are still arriving (percent rounds to 100 early) and never fires for a blend>0 persona whose second asset is still downloading."

- gap_id: G-213-19b
  truth: "Engine asset download is one predictable bundle, not a blend-conditional split"
  status: resolved
  resolved_by: 213-06-PLAN.md
  resolved_at: 2026-08-28
  reason: "User reported: I think we should always download both models, Maia and Stockfish (53 MB together). I don't think splitting this makes sense and only increases complexity."
  severity: minor
  test: 19
  decision: "LOCKED 2026-08-28 — always download BOTH maia-model and stockfish-wasm regardless of persona blend. Accepted cost: blend-0 rungs wait for an extra 7.3 MB (45.7 -> 53.0 MB, ~16% longer). Bought: engineGateRequired() loses its blend branch, useBotGame loses the D-03 blend-0 pool.warm() skip, retryEngineWarm() loses its conditional fan-out."
  root_cause: ""
  artifacts: []
  missing: []
  debug_session: ""

- gap_id: G-213-34
  truth: "While engines download, the analysis board shows ONE trustworthy progress readout and stays reachable"
  status: resolved
  resolved_by: 213-07-PLAN.md
  resolved_at: 2026-08-28
  resolution_pending_human_check: "Test 35 is the designated cold-cache Slow-4G confirmation that the reported defects died with the deleted readouts."
  reason: "User reported: the separate progress bars on the analysis board are a mess and don't work properly. Gate the analysis board behind the same modal with the single progress bar. Observed defects: the readouts disagreed with each other; one stuck and never reached 100%; and there were long stretches of downloading with no progress shown at all."
  severity: major
  test: 34
  decision: "LOCKED 2026-08-28 (reaffirmed by the user after a dismissible-gate counter-proposal was raised and declined) — reverses the phase-213 'analysis board is never gated' decision outright. The analysis board mounts the SAME non-dismissible EngineReadyGate as Bots: one aggregate bar over both assets, no X, no Esc, no click-outside, board unreachable until Start. The per-card progress readouts are REMOVED entirely, not consolidated — one modal is the only place download progress is ever shown. Rationale accepted: one gate, one code path, zero conditionals. Known cost the user accepted: on a cold cache a user opening a game from their list cannot browse their own game (server data - move list, WDL stats, flaw markers, opening tree) until 53 MB has landed."
  root_cause: ""
  artifacts: []
  missing:

    - "Mount EngineReadyGate on the analysis board with the same non-dismissible contract as Bots, gated on BOTH assets"
    - "Strip the progress readout from EngineLinesSkeleton and MaiaChartSkeleton - they revert to their original pulsing placeholders. This removes the need for the 3 Analysis.tsx assetProgress props and the MovesByRatingChart store read."
    - "TrainReveal.tsx is the 4th EngineLinesSkeleton mount site - it inherits the reverted pulsing skeleton automatically and stays ungated (not raised by the user; flag if the train page should gate too)"
    - "The disagreeing / stuck / silent readouts need no separate diagnosis once the readouts are deleted - but if the SAME defect reaches the modal bar, it is a store bug, not a rendering bug. Verify the single bar against the Network tab on a cold cache before closing."
    - "Reverts shipped 213-05 work (D-02 EngineLinesSkeleton readout, D-03 MaiaChartSkeleton readout, D-05 three-mount-site assetProgress wiring) and their tests - checkpoints 30/31/33 pass today and will no longer apply"
  debug_session: ""

- gap_id: G-213-35
  truth: "The analysis-board gate's single aggregate bar tracks real network byte progress in step with the Network tab"
  status: resolved
  resolved_by: 213-08-PLAN.md, 213-09-PLAN.md, 213-10-PLAN.md
  resolved_at: 2026-08-29
  reason: "User reported: The modal with the progress bar opens, but it moves extremly slowly. In the dev tool bar, I see that data is downloaded much faster. Now it stopped at 3% for a long time. Something is not right here"
  severity: major
  test: 35
  observed: "Bar advances far slower than the real transfer shown in DevTools, then stalls at 3% for a long time while bytes keep flowing."
  cross_browser: "SUPERSEDED - Chrome reproduces it too on a second cold-cache try (user, 2026-08-28: \"extremely slow progress in the modal, and already 117MB downloaded in the dev tool footer\"). The first Chrome run only looked clean. The defect is browser-INDEPENDENT; the f16 branch is an aggravator on Brave, not the driver. Earlier note kept for the record: Brave FAILS, Chrome looked fine at the same Fast 4G throttle. Same page, same store, same throttle - so the driver is browser-dependent. Chrome's adapter has f16: warmup analyze() succeeds, ONE runtime is fetched (24.3 MB asyncify), no respawn, GPU inference; real transfer ~77 MB. Brave's adapter lacks f16: the 24.3 MB asyncify runtime is fetched, warmup throws, respawnPinnedToWasm() creates a second session that fetches the OTHER 13.5 MB runtime, then inference runs on wasm; real transfer ~91 MB, and the final 13.5 MB lands AFTER the bar already reads 100% (the model buffer is handed over, so initWasmOnlySession() reports 100% immediately)."
  environment: "Brave, DevTools throttling Fast 4G, cold cache. Console during the run: `[E:onnxruntime:, sequential_executor.cc:620 ExecuteKernel] Non-zero status code returned while running Cast node. Name:'elo_oppo_cast_to_/model/Clip_1' ... Program Cast requires f16 but the device does not support it` - i.e. the WebGPU adapter WAS found and the D-14 warmup analyze() caught an f16 shader failure, so this run took the full WebGPU-attempt-then-respawn path."
  root_cause: |
    CONFIRMED - the gate's denominator is structurally wrong: ~38 MB of the real cold-start
    download is invisible to the progress store, so the bar CANNOT track the Network tab.

    `engineAssetProgress.ts` counts exactly two ids totalling 53.0 MB
    (MAIA_MODEL_BYTES_FALLBACK 45,683,686 + STOCKFISH_WASM_BYTES_FALLBACK 7,295,411).
    Only bytes that flow through `fetchModelBuffer()`s streaming reader
    (maia-worker.js:180-208) or the Stockfish glue's `progressPort`
    (workerPool.ts:1067-1077) are ever reported.

    The onnxruntime-web RUNTIME binaries are fetched by ORT itself inside
    `ort.InferenceSession.create()` via `ort.env.wasm.wasmPaths = '/maia/'`
    (maia-worker.js:231, 291). They never touch `fetchModelBuffer`, so they post no
    `progress` message and have no store id:

      - frontend/public/maia/ort-wasm-simd-threaded.asyncify.wasm  24,254,953 B (WebGPU/JSEP build)
      - frontend/public/maia/ort-wasm-simd-threaded.wasm           13,479,978 B (wasm-only build)

    On this device BOTH are fetched, because the WebGPU attempt gets far enough to create a
    session (pulling the 24.3 MB asyncify build) before the warmup `analyze()` hits the f16
    Cast failure above, which triggers `respawnPinnedToWasm()` -> `initWasmOnlySession()` ->
    a second `InferenceSession.create()` that pulls the 13.5 MB wasm-only build.

    Real cold-start transfer on this run: 7.3 + 45.7 + 24.3 + 13.5 = ~90.8 MB.
    Tracked by the bar: 53.0 MB = 58%. The remaining 37.8 MB downloads with the bar
    showing nothing - which is exactly "data is downloaded much faster in the dev tool bar".
    The G-213-8 buffer handoff correctly stops the 45.7 MB MODEL being fetched twice, but it
    does nothing for the two different ORT runtime binaries, which are genuinely different files.

    CONFIRMED PRIMARY CAUSE - the SAME wasm is fetched up to FIVE times and the store counts
    it once. The observed 117 MB is the arithmetic.

    On the analysis board three independent Stockfish consumers each construct their own
    classic Worker against the same ENGINE_PATH ('/engine/stockfish-18-lite-single.js', whose
    .wasm is 7,295,411 B):
      - workerPool.ts:1118 `ensureSpawned()` loops `computePoolSize()` times (workerPool.ts:414
        -> DESKTOP_POOL_MAX 4, or MOBILE_POOL_SIZE 2) calling `createSlot()` -> `new Worker()`
        at line 1051, in ONE synchronous loop.
      - useStockfishEngine.ts:360 constructs a further standalone worker, mounted by
        Analysis.tsx:689.
    A 6+-core desktop therefore opens 4 pool workers + 1 standalone = 5 workers, each
    instantiating the full 7.3 MB wasm binary: up to 36.5 MB of Stockfish.

    The HTTP cache cannot dedupe them - all five requests are issued in the same tick, so none
    has a cached response yet when the others start. (DevTools "Disable cache", normally on
    while throttling, removes even the second-visit dedupe.)

    Meanwhile `engineAssetProgress.ts` has ONE 'stockfish-wasm' id and
    `reportEngineAssetProgress()` keeps `loaded` monotonic via Math.max, so five concurrent
    progressPort reporters collapse into a single bar showing the max of ONE worker's progress
    against a 7,295,411 B denominator. Four of the five downloads are invisible.

    Full cold-start accounting for the 117 MB run:
      5 x 7.3 MB Stockfish wasm                    = 36.5 MB  (7.3 counted, 29.2 invisible)
      1 x 45.7 MB Maia model                       = 45.7 MB  (counted)
      1 x ort-wasm-simd-threaded.asyncify.wasm     = 24.3 MB  (invisible - see below)
      app bundle / fonts / API                     ~  3-5 MB  (invisible)
      (+13.5 MB more on Brave, where f16 forces a second ORT runtime)
                                                   ~110-120 MB vs a 53.0 MB denominator
    The bar can represent at most ~45% of what the Network tab counts - exactly "extremely
    slow progress in the modal, and already 117MB downloaded".

    This also explains the 3% stall without guesswork: the five duplicate Stockfish fetches
    saturate the throttled pipe at the very moment the Maia model fetch starts, so the counted
    numerator (one Stockfish worker plus a barely-started model) crawls while ~29 MB of
    uncounted duplicate wasm goes over the wire.

    NOT INVESTIGATED (briefly called "ruled out" on the strength of the first Chrome run; that
    report has since been withdrawn, so it is open again - though it is no longer needed to
    explain any observed symptom) - per-chunk re-render storm. `reportEngineAssetProgress()`
    commits on EVERY chunk (~700 for a 45.7 MB body) and `Analysis.tsx:1115` subscribes at the
    top of the 3,600-line `Analysis()` component, so every chunk re-renders board/move list/
    charts. Worth tidying on its own merits, but do not let a fix plan bill it as the repair.
  artifacts:
    - path: "frontend/src/lib/engine/workerPool.ts"
      issue: "ensureSpawned() (line 1118) spawns computePoolSize() workers - up to DESKTOP_POOL_MAX=4 - in one synchronous loop, each `new Worker(ENGINE_PATH)` at line 1051 instantiating its own copy of the 7.3 MB stockfish wasm; issued in the same tick, so the HTTP cache cannot dedupe them"
    - path: "frontend/src/hooks/useStockfishEngine.ts"
      issue: "line 360 constructs a FIFTH worker against the same ENGINE_PATH, mounted by Analysis.tsx:689 alongside the pool"
    - path: "frontend/src/lib/engine/engineAssetProgress.ts"
      issue: "one 'stockfish-wasm' id with a 7,295,411 B denominator and Math.max-monotonic loaded; five concurrent progressPort reporters collapse into one bar showing a single worker's progress, hiding ~29 MB"
    - path: "frontend/public/maia/maia-worker.js"
      issue: "initSession()/initWasmOnlySession() set ort.env.wasm.wasmPaths and let ORT fetch the 24.3 MB asyncify and 13.5 MB wasm runtime binaries itself - neither is streamed through fetchModelBuffer(), so neither is counted"

    - path: "frontend/public/maia/maia-worker.js"
      issue: "the WebGPU path commits to the 24.3 MB asyncify runtime BEFORE the warmup analyze() can discover the device lacks f16; a pre-flight adapter feature probe (adapter.features.has('shader-f16')) would skip the WebGPU attempt entirely on this device and save the whole 24.3 MB"

    - path: "frontend/src/lib/engine/engineAssetProgress.ts"
      issue: "asset registry has only 'maia-model' and 'stockfish-wasm'; the ORT runtime binary has no id, so the percent denominator is ~58% of the real wait. Also commits per chunk with no coalescing."

    - path: "frontend/src/pages/Analysis.tsx"
      issue: "useEngineAssets() is called at line 1115 inside the 3,600-line Analysis() component, so every per-chunk store commit re-renders the whole analysis tree"
  missing:
    - "Stop fetching the same Stockfish wasm five times. Compile/instantiate it ONCE and share it across every worker (fetch the binary once and pass the ArrayBuffer, or a structured-cloneable WebAssembly.Module, to each worker), or serialise the spawns so the first response populates the HTTP cache before the rest are issued. Biggest single win: ~29 MB off every desktop cold start."
    - "Whatever the sharing mechanism, make the bar's denominator equal the bytes actually transferred - if N copies really must be fetched, count N of them rather than one."
    - "Count the ORT runtime binary. Either stream it in the worker (fetch it with the same reader used by fetchModelBuffer and hand the bytes to ORT via ort.env.wasm.wasmBinary) and register it as a third asset id, or otherwise make its bytes visible to the gate. Whatever the mechanism, the bar's denominator must equal the bytes the Network tab actually transfers."
    - "Stop downloading two different ORT runtimes on one page load. Probe adapter.features.has('shader-f16') (and any other feature the Maia graph needs) BEFORE importScripts(WEBGPU_RUNTIME_PATH), and go straight to the wasm-only path when it is absent - this device then fetches 13.5 MB instead of 24.3 + 13.5 MB."
    - "Decouple progress notification from the Analysis render tree: coalesce commits (only notify when the rounded aggregate percent changes, or rAF-batch them) and/or move the useEngineAssets() subscription out of Analysis() into the gate subtree so a chunk cannot re-render the board and charts."
    - "Re-run the test-35 script on BOTH Brave and Chrome, cold cache, and confirm the DevTools transferred total now matches the bar's denominator within a few MB - the acceptance check is the two numbers agreeing, not the bar merely looking smooth."
  debug_session: ""

- gap_id: G-213-35-c
  parent: G-213-35
  truth: "The gate's progress bar advances in step with the actual network transfer"
  status: resolved
  resolved_by: 213-10-PLAN.md
  resolved_at: 2026-08-29
  severity: blocker
  test: 35
  reason: "User re-ran test 35 in Brave after 213-08/213-09 landed. Byte accounting PASSED (66,537 kB transferred vs 66,459 kB expected for exactly one of each asset - within 78 kB, and ~24 MB below what a duplicate asyncify fetch would cost). But with every byte already transferred, the modal sat at 'Maia model - 75% (50.2 / 66.5 MB)' and crawled for minutes. User: 'It's been going on for minutes, something's not right here'."
  root_cause: "Two compounding causes, BOTH already named in G-213-35's own artifacts/missing lists and left unclosed by plans 08 and 09. (1) fetchModelBuffer() calls onProgress() once per stream chunk; reportEngineAssetProgress() calls commit() every time, notifying every listener with no coalescing - on the order of a thousand-plus notifications for the 45.7 MB model. (2) Analysis.tsx:1115 subscribes the entire 3,600-line Analysis() component to that store while using the result at exactly one place, line 3893, for a single boolean (engineGateOpen && engineAssets.status !== 'unsupported'). So every chunk re-renders the board, charts, engine lines and panels; the main thread falls minutes behind a queue it cannot drain. Bots.tsx is unaffected because it never calls useEngineAssets - it only mounts EngineReadyGate, which subscribes internally, so only a small modal re-renders."
  note: "Plan 213-09's Task 4 pre-authorised 'janky or stutters in steps' as NOT a failure and deferred the per-chunk re-render to a follow-up seed. That deferral was incorrect: the G-213-35 diagnosis lists this as required work, not optional, and the observed severity (minutes, not jank) independently fails Task 4's own 'no long stall while bytes flow' criterion."
  artifacts:
    - path: "frontend/src/lib/engine/engineAssetProgress.ts"
      issue: "commit() notifies every listener on every reportEngineAssetProgress call; no percent-change or time-based coalescing"
    - path: "frontend/src/pages/Analysis.tsx"
      issue: "line 1115 subscribes the whole page via useEngineAssets(); sole consumer is the line 3893 status boolean"
  missing:
    - "Coalesce store notifications: notify only when an asset's rounded percent changes, keeping every terminal mutator (pending/ready/failed/unsupported/refetch) on the unconditional synchronous path"
    - "Narrow the Analysis subscription to the status primitive so useSyncExternalStore bails out on byte-only updates"
    - "Re-run the test-35 script and require the bar to reach 100% within a second of the transfer stopping"
  planned_by: 213-10-PLAN.md

- gap_id: G-213-36
  truth: "Bot play works after the analysis board has already warmed the engine"
  status: resolved
  resolved_by: 213-11-PLAN.md
  resolved_at: 2026-08-29
  severity: blocker
  test: 35
  introduced_by: 213-09
  reason: "User re-ran on both Chrome and Brave after 213-10. Analysis-board download was 'perfect' on both. But starting a bot game after visiting the analysis board stalled: Sheldon the Snail (850) took 60 seconds for its first move, Cackle the Hyena (~1800) took 3 minutes; both moved normally afterwards. Console on both browsers: `Uncaught (in promise) DataCloneError: Failed to execute 'postMessage' on 'Worker': ArrayBuffer at index 0 is already detached. at constructWorker (maiaWorkerHost.ts:379:5) at maiaWorkerHost.ts:321:5`."
  root_cause: "Regression introduced by plan 213-09. ensureOrtRuntime() caches ONE promise resolving {backend, buffer}; constructWorker() pushes that buffer into postMessage's transfer list, which DETACHES it. Every later spawn resolves the same cached promise and gets the same detached ArrayBuffer, so postMessage throws. 213-09 chose 'transfer, not copy' reasoning that the only path needing the bytes twice was the WebGPU->wasm respawn (which needs a different build anyway) - that covered one respawn path and missed the ordinary one: navigating /analysis -> /bots releases the last lease, tears down module state, and the next analyze() spawns again into the detached buffer. The failure is silent rather than loud: new Worker() succeeds so the script loads, then postMessage throws inside a .then() as an unhandled rejection, so the worker never receives its init message, never posts ready, and dispatchNext() never runs. The move only appears once something else times out into a fallback - which is the 60s / 3min the user measured."
  artifacts:
    - path: "frontend/src/lib/engine/maiaWorkerHost.ts"
      issue: "constructWorker() line ~377 pushes the cached runtimeBuffer into the postMessage transfer list, detaching a value that ortRuntimeSource.ts hands to every subsequent spawn"
    - path: "frontend/src/lib/engine/ortRuntimeSource.ts"
      issue: "the cached single-flight promise resolves the same ArrayBuffer instance to every caller, with no protection against a caller detaching it"
  missing:
    - "Retain the fetched ArrayBuffer as a master inside ortRuntimeSource.ts and hand out a per-consumer copy (master.slice(0)), so the transferred value is always a copy the caller owns and the master never detaches"
    - "Apply the same protection to fetchWasmOnlyOrtRuntime, a cached second reader by construction"
    - "Audit the modelBuffer transfer at the same call site - one-shot today, identical shape"
    - "Do NOT fix by detecting detachment and re-fetching: that puts a network request on the second spawn and breaches the both-surfaces zero-refetch criterion under Disable cache"
  planned_by: 213-11-PLAN.md

- gap_id: G-213-37
  truth: "Starting a bot game after the engine is already warm downloads nothing"
  status: fixed
  severity: blocker
  test: 35
  reason: "User, testing with Fast 4G and DevTools 'Disable cache' ticked: 'I see that in the network tab, a lot of data is downloaded between the bot moves. I suspect that on game start, the maia model is re-downloaded.' User then handed the issue off to a fresh session rather than continuing UAT."
  root_cause_lead: "CONFIRMED BY CODE READING, not yet by measurement: the 45.7 MB Maia model is fetched ONLY inside the worker (frontend/public/maia/maia-worker.js:201, fetch(MODEL_PATH)). Nothing on the main thread retains it. The modelBuffer handoff added by G-213-8 covers exactly ONE path - the dying WebGPU worker passing its buffer to its wasm replacement (maiaWorkerHost.ts:457). Any OTHER fresh spawn re-fetches all 45.7 MB. Plan 213-11 gave the 13.5 MB ORT runtime retain-and-copy in ortRuntimeSource.ts but left the 3.4x-larger model with no equivalent."
  triage_result: "COMPLETED 2026-08-29 (session after handoff). Hypothesis 1 (spawn-per-move lifecycle defect): REJECTED - useBotGame's engine bring-up effect has [] deps (useBotGame.ts:1336), so pool and queue live for the whole game; no worker is torn down between moves. But a per-GAME respawn is guaranteed by design: BotsGame mounts with key={boot.nonce} (Bots.tsx:740/805) and unmounts on every new game / rematch, the effect cleanup calls queue.terminate(), the last lease releases, and maiaWorkerHost releases everything (worker.terminate() + resetModuleState, maiaWorkerHost.ts:640) - deliberately, to free the ~226 MB heap. The next mount warms a fresh worker whose ONLY source for the model is fetch(MODEL_PATH) inside maia-worker.js:201. With 'Disable cache' ticked that is a full 45.7 MB network fetch per game start. Dev-only amplifier: StrictMode (main.tsx:46) double-runs the [] effect - the first worker can begin streaming the model before cleanup terminates it, then the second mount fetches it all again; invisible in production but inflates every Network-tab reading taken in dev. Hypothesis 2 (test-setup amplification): PARTIALLY CONFIRMED - with cache enabled, production users would be served the respawn fetch from disk cache; the observed 45.7 MB/game is the cache-disabled worst case. 'Between the bot moves' is most plausibly the tail of the game-start fetch still streaming while the bot answered from the opening book, not a per-move respawn. USER MEASUREMENT 2026-08-29 confirming hypothesis 2: with DevTools cache ENABLED, bot games show no downloads between moves - the per-game respawn refetch is fully served by the HTTP disk cache, so production users on default browser settings were never hitting the network for it."
  decision: "USER DECISION 2026-08-29, locked as D-20 in 213-CONTEXT.md: fix via the Cache API (CacheStorage) as the SINGLE byte-ownership layer for all three engine assets (Maia model, ORT runtime, Stockfish wasm) - a unified from-scratch layer, NOT a fourth per-path patch. CacheStorage is disk-backed (no 45.7 MB main-thread retention, no FLAWCHESS-92 OOM regression), is available inside workers, and is NOT bypassed by DevTools 'Disable cache', so the both-surfaces zero-refetch criterion becomes honestly measurable."
  blocks: "213-10 Task 3 and 213-11 Task 3 checkpoints, both still unapproved. The /bots per-resource counts and the /analysis -> /bots zero-refetch number have never been reached in any run."
  handoff: ".planning/phases/213-first-run-engine-cold-start-ux/.continue-here.md"
  resolution: "FIXED 2026-08-29 by 213-12 (D-20): CacheStorage single byte-ownership layer (engineAssetCache.ts + worker-side read in maia-worker.js) for all three assets; the two in-memory patches (213-11 retained ORT master, G-213-8 modelBuffer handoff) retired. Task 4 blocking-human checkpoint (A second-game zero bytes, B /analysis -> /bots per-resource counts + zero new requests, C cache-enabled reading, D console clean, E gate asymmetry unchanged; Chrome + Brave) APPROVED by the user via /gsd-execute-phase 213 --gaps-only (commit 197b1c37e). This also covers the never-reached /bots per-resource counts and the zero-refetch hop that previously blocked 213-10/213-11 Task 3 checkpoints. Post-approval hardening: CR-01 truncation guard (59e27ca38) ensures a short or unverifiable body is never cached."

