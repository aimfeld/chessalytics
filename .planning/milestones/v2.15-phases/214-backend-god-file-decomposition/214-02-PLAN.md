---
phase: 214-backend-god-file-decomposition
plan: 02
type: execute
wave: 2
depends_on: ["214-01"]
files_modified:
  - app/services/tactic_detector.py
  - pyproject.toml
autonomous: true
requirements: []
user_setup: []

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`detect_tactic_motif` reads as parse-PV -> try-mate-tier -> collect-candidates -> select-winner, with each stage a named function."
    - "`uv run ruff check app/services/tactic_detector.py` is clean both with the project's ignore table and with it emptied — the file needs no complexity exemption any more."
    - "The tactic-tagger precision/recall report is byte-identical (modulo its Generated timestamp) before and after the split."
    - "All 36 tests in tests/services/test_tactic_detector.py pass unchanged."
  artifacts:
    - app/services/tactic_detector.py
    - pyproject.toml
  key_links:
    - "`detect_tactic_motif` -> `_dispatch_mate_tier` / `_collect_non_mate_candidates` / `_select_shallowest_candidate`"
    - "`_INT_TO_MOTIF` and `_parse_pv` stay importable from `app.services.tactic_detector` (pinned by test imports)"
    - "`scripts/tactic_tagger_report.py --check-goals` -> the byte-identity oracle for this file"
  prohibitions:
    - statement: "No behavior change: public signatures, return shapes, and the motif decision for every position are unchanged."
      status: flagged
      verification: manual
    - statement: "No test is deleted or weakened; the tests/ diff is additions only."
      status: flagged
      verification: manual
    - statement: "No new `# ty: ignore` line is added."
      status: flagged
      verification: manual
    - statement: "No Sentry capture site is lost (this file has zero; it must still have zero)."
      status: flagged
      verification: manual
    - statement: "`apply_game_filters()` remains the single game-filter path (not used by this file; must not be introduced)."
      status: flagged
      verification: manual
    - statement: "No frontend file is touched."
      status: flagged
      verification: manual
    - statement: "The tactic fixtures under fixtures/tagger/ and the GOALS table in scripts/tactic_tagger_report.py are not edited — the gate is not to be moved."
      status: flagged
      verification: manual
---

<objective>
Split `app/services/tactic_detector.py`'s dispatcher so it stops breaching the ruff
complexity rules, with the tactic-tagger precision/recall harness proving byte-identical
detection output.

Purpose: this file leads the phase because it has the strongest oracle — 36 unit tests plus a
~26 s fixture-driven precision/recall harness whose report is a content fingerprint of every
motif decision. If a behavior-preserving split method works anywhere, it is provable here
first, which is why the ROADMAP orders it first.

Output: `detect_tactic_motif` and `detect_clearance` within `C901 <= 15` / `PLR0912 <= 12`;
the file's `per-file-ignores` entry deleted from `pyproject.toml`; an identical harness report.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/214-backend-god-file-decomposition/214-PATTERNS.md

Read only the sections of
`.planning/phases/214-backend-god-file-decomposition/214-RESEARCH.md` named per task in
`<read_first>` — the full file is ~1,050 lines.

Do NOT `@`-load `.planning/PROJECT.md` (229 KB), `.planning/ROADMAP.md` (133 KB) or
`.planning/STATE.md` (302 KB).

**Phase contract (locked, from ROADMAP.md Phase 214):** zero behavior change — public
signatures, return shapes, emitted SQL, and Sentry capture sites unchanged. The existing tests
are the sole oracle and pass untouched; tests may be ADDED where a split exposes an untested
seam, never rewritten to fit the refactor. Split seams are "pipeline orchestrator -> one
function per stage" and "nested loops -> early continue / Counter accumulator", NOT "split to
fit a signature": no context dataclasses with fewer than three fields and one reader, no
handler bundles of unrelated callbacks. Renaming public entry points is out of scope.

**Merge discipline:** this plan shares exactly one file with its five wave-2 siblings —
`pyproject.toml`, where it deletes a single `per-file-ignores` line. Re-read `pyproject.toml`
immediately before that edit; a sibling plan may already have removed its own line. Each
wave-2 plan is its own squash-merge unit (214-RESEARCH.md "Ordering and Wave Structure",
Option A).
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Extract the mate tier from detect_tactic_motif, end to end through the harness</name>
  <precondition>Plan 214-01 is merged: `scripts/check_function_size.py` exists and `uv run ruff check . --config 'lint.per-file-ignores = {}'` reports C901/PLR0912 findings for this file.</precondition>
  <precondition>The dev PostgreSQL is running: `docker compose -f docker-compose.dev.yml -p flawchess-dev ps` shows `flawchess-dev-db-1` healthy.</precondition>
  <files>app/services/tactic_detector.py</files>
  <read_first>
    - `app/services/tactic_detector.py` lines 2413-2587 (`detect_tactic_motif`) and the `_sort_key` / `_parse_pv` / `_INT_TO_MOTIF` definitions it uses
    - `214-RESEARCH.md` sections "Pattern 2: Dispatcher/tiered-candidate pipeline" and "`tactic_detector.py` — precision/recall harness as the byte-identity oracle"
    - `214-PATTERNS.md` section "`app/services/tactic_detector.py` (service, dispatcher split)"
    - `.claude/skills/tactic-tagger-report/SKILL.md`
    - `CLAUDE.md` (function-size rules; "Don't split just to fit a signature")
  </read_first>
  <action>
Capture the oracle BEFORE touching the file:
`PYTHONPATH=. uv run python scripts/tactic_tagger_report.py --check-goals` — it prints
`Wrote <path> (N lines)` and exits 1. Exit 1 is the correct, unchanged behavior: 2 of 27 goal
dimensions (discovered-attack recall, fork recall) are pre-existing known gaps that this phase
does not touch. The gate is report CONTENT, not exit code. Copy the written report to
`.planning/phases/214-backend-god-file-decomposition/214-02-tactic-report-before.md` as the before-baseline (the verify commands below read
that exact path).

Then extract Tier 1 of `detect_tactic_motif` into
`_dispatch_mate_tier(...) -> tuple[...] | None` — it returns the winning tuple when the mate
tier fires and `None` to fall through to the non-mate tiers. Move the tier's early returns
into the helper; `detect_tactic_motif` keeps its guard clauses and now reads
"if a mate-tier result exists, return it".

Keep the extracted helper in `app/services/tactic_detector.py` — this file has no sibling
module and needs none. `_INT_TO_MOTIF` and `_parse_pv` are imported by name in
`tests/services/test_tactic_detector.py`; they stay exactly where they are.

Commit this task on its own before expanding.
  </action>
  <acceptance_criteria>
    - `app/services/tactic_detector.py` defines `_dispatch_mate_tier` with an explicit return type annotation, and `detect_tactic_motif` calls it instead of inlining the mate tier.
    - `grep -c "def _parse_pv\|_INT_TO_MOTIF = \|_INT_TO_MOTIF: " app/services/tactic_detector.py` is at least 2 — both names still defined in this module.
    - `uv run pytest -n auto tests/services/test_tactic_detector.py -q` reports 0 failures.
    - The post-change harness report differs from the scratch baseline only on the `**Generated:**` line.
    - The task is committed on its own (`git log -1 --stat` shows only `app/services/tactic_detector.py`).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -n auto tests/services/test_tactic_detector.py -q</automated>
    <fails_when>non-zero exit, or any "failed" or "error" count other than 0 in the summary line</fails_when>
    <automated>PYTHONPATH=. uv run python scripts/tactic_tagger_report.py --check-goals; diff --ignore-matching-lines='^\*\*Generated:' .planning/phases/214-backend-god-file-decomposition/214-02-tactic-report-before.md "$(ls -t reports/tactic-tagger/*.md | head -1)"</automated>
    <fails_when>`diff` prints any hunk line or exits non-zero — any difference outside the Generated timestamp means a motif decision changed</fails_when>
    <automated>uv run ruff format --check app/ tests/ scripts/ && uv run ty check app/ tests/ scripts/</automated>
    <fails_when>non-zero exit, or output naming a file as "would be reformatted", or output containing "error["</fails_when>
  </verify>
  <done>One tier of the dispatcher lives in its own function, the 36 unit tests pass, and the precision/recall report is unchanged — the split method is proven end to end on this file before any further extraction.</done>
  <reversibility rating="reversible">A single in-file function extraction; `git revert` of one commit restores the original dispatcher.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Finish the detect_tactic_motif split — candidate collection and winner selection</name>
  <files>app/services/tactic_detector.py</files>
  <read_first>
    - `app/services/tactic_detector.py` — the current `detect_tactic_motif` after task 1, plus the Tier 2-5 collection loops and `_sort_key`
    - `214-RESEARCH.md` section "Pattern 2: Dispatcher/tiered-candidate pipeline"
    - `214-PATTERNS.md` section "`app/services/tactic_detector.py` (service, dispatcher split)"
    - `CLAUDE.md` (function-size rules)
  </read_first>
  <action>
Extract the remaining two stages:
- `_collect_non_mate_candidates(...) -> list[...]` — the Tier 2/3/4/5 collection loops,
  returning the candidate list in the same order the inline code built it. Order matters:
  `min()` is stable, so a reordering silently changes which candidate wins a tie.
- `_select_shallowest_candidate(candidates) -> ... | None` — the `min(candidates,
  key=_sort_key)` call plus the empty-list early return. It is small on purpose: it names the
  depth-primary, tier/rank-tiebreak rule once so a reader does not re-derive it from a lambda.
  This is the one deliberate small extraction in this plan; do not extract further one-line
  helpers around it.

`detect_tactic_motif` then reads: parse PV -> `_dispatch_mate_tier` -> if None,
`_collect_non_mate_candidates` -> `_select_shallowest_candidate` -> unpack and return. Its
tuple return shape and element order are unchanged.

After this task, `C901` and `PLR0912` must no longer fire for `detect_tactic_motif`. Verify
with the ignore table emptied on the command line, since the file's baseline ignore entry is
still present until task 3.
  </action>
  <acceptance_criteria>
    - `_collect_non_mate_candidates` and `_select_shallowest_candidate` exist with explicit return type annotations, and `detect_tactic_motif`'s body is the four-step orchestration described in the action.
    - `detect_tactic_motif`'s return statement still yields the same 4-tuple element order as before the split (compare against `git show HEAD~2:app/services/tactic_detector.py`).
    - `uv run ruff check app/services/tactic_detector.py --config 'lint.per-file-ignores = {}' --output-format concise` reports no finding whose line maps to `detect_tactic_motif`.
    - `uv run pytest -n auto tests/services/test_tactic_detector.py -q` reports 0 failures.
    - The harness report still differs from the baseline only on the `**Generated:**` line.
    - No context dataclass was introduced to pass state between the three helpers.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -n auto tests/services/test_tactic_detector.py -q</automated>
    <fails_when>non-zero exit, or any "failed" or "error" count other than 0 in the summary line</fails_when>
    <automated>uv run ruff check app/services/tactic_detector.py --config 'lint.per-file-ignores = {}' --output-format concise</automated>
    <fails_when>output contains `C901` or a `PLR0912` finding on `detect_tactic_motif`'s line</fails_when>
    <automated>PYTHONPATH=. uv run python scripts/tactic_tagger_report.py --check-goals; diff --ignore-matching-lines='^\*\*Generated:' .planning/phases/214-backend-god-file-decomposition/214-02-tactic-report-before.md "$(ls -t reports/tactic-tagger/*.md | head -1)"</automated>
    <fails_when>`diff` prints any line or exits non-zero</fails_when>
  </verify>
  <done>The dispatcher is a three-stage pipeline with unchanged output, and its cyclomatic complexity and branch count are inside the configured thresholds.</done>
  <reversibility rating="reversible">In-file extraction only; revert restores the original function body.</reversibility>
</task>

<task type="auto">
  <name>Task 3: detect_clearance branch reduction, mutation proof, and ignore-entry deletion</name>
  <files>app/services/tactic_detector.py, pyproject.toml</files>
  <read_first>
    - `app/services/tactic_detector.py` lines 1968-2035 (`detect_clearance`, PLR0912 13 branches) and lines 603-668 (`detect_pin`) for the sibling detector shape
    - `214-RESEARCH.md` section "Test Oracle Per File" (the `tactic_detector.py` row and the mutation-test rule)
    - `pyproject.toml` `[tool.ruff.lint.per-file-ignores]` as it stands right now
    - `CLAUDE.md` ("nested loops -> invert with early continue/return or a Counter accumulator")
  </read_first>
  <action>
1. Bring `detect_clearance` inside `max-branches = 12`. Its branches are a chain of
   line/square eligibility tests; apply CLAUDE.md's own named seam — invert with early
   `continue`/`return` inside the loop, or extract a single predicate helper named for the
   condition it tests (e.g. `_clearance_line_is_usable(...) -> bool`). Do not extract five
   one-line helpers; one named predicate or an inverted loop is the whole fix.
2. Mutation proof (ROADMAP gate). Temporarily replace the body of `_dispatch_mate_tier` with
   an immediate `return None`, run `uv run pytest -n auto tests/services/test_tactic_detector.py -q`,
   and confirm it FAILS. Restore the body, confirm green again, and record both outcomes
   (which tests failed, and the restored pass) in the SUMMARY. This proves the extracted
   helper is actually covered rather than merely present.
3. Re-read `pyproject.toml`, then delete the single `per-file-ignores` entry for
   `app/services/tactic_detector.py`. Leave every other entry — including the five other
   in-scope files and the eight out-of-scope files — untouched.
  </action>
  <acceptance_criteria>
    - `uv run ruff check app/services/tactic_detector.py` exits 0 AND `uv run ruff check app/services/tactic_detector.py --config 'lint.per-file-ignores = {}'` exits 0 — the file is clean with and without the ignore table, so its exemption is provably dead.
    - `uv run ruff check .` exits 0 (deleting one entry did not break the project-wide gate).
    - `uv run python scripts/check_function_size.py app/services/tactic_detector.py --fail-over-depth 4 --fail-over-loc 200` exits 0.
    - `grep -v '^\s*#' app/services/tactic_detector.py | grep -c 'sentry_sdk\.capture_exception('` prints `0`, matching this file's pre-split count of 0.
    - `git diff --stat` for this plan shows `app/services/tactic_detector.py` and `pyproject.toml` only, and no new file.
    - `git diff -- tests/` for this plan is empty (no test touched at all) — this file's oracle needed no additions.
    - The SUMMARY records: the mutation-proof result, the complexipy before/after count for this file (16 functions over 15 at planning time), and any function still between 100 and 200 logic LOC with a one-line justification.
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff check app/services/tactic_detector.py && uv run ruff check app/services/tactic_detector.py --config 'lint.per-file-ignores = {}' && uv run ruff check .</automated>
    <fails_when>non-zero exit from any of the three, or any output line containing `C901`, `PLR0912`, or `PLR0915`</fails_when>
    <automated>uv run python scripts/check_function_size.py app/services/tactic_detector.py --fail-over-depth 4 --fail-over-loc 200</automated>
    <fails_when>non-zero exit, or any reported function with `max_nesting_depth` above 4 or `logic_loc` above 200</fails_when>
    <automated>uv run pytest -n auto tests/services/test_tactic_detector.py -q</automated>
    <fails_when>non-zero exit, or any "failed" or "error" count other than 0 in the summary line</fails_when>
    <automated>PYTHONPATH=. uv run python scripts/tactic_tagger_report.py --check-goals; diff --ignore-matching-lines='^\*\*Generated:' .planning/phases/214-backend-god-file-decomposition/214-02-tactic-report-before.md "$(ls -t reports/tactic-tagger/*.md | head -1)"</automated>
    <fails_when>`diff` prints any line or exits non-zero</fails_when>
    <automated>uv run ruff format --check app/ tests/ scripts/ && uv run ty check app/ tests/ scripts/ && uv run pytest -n auto -x -q</automated>
    <fails_when>non-zero exit, output naming a file as "would be reformatted", output containing "error[", or any "failed" count other than 0</fails_when>
  </verify>
  <done>`app/services/tactic_detector.py` carries no complexity exemption, its detection output is provably identical, and the mutation proof shows the new seam is covered by real tests.</done>
  <reversibility rating="reversible">Code extraction plus a one-line TOML deletion; reverting the plan's commits restores both.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Code moved, no trust boundary changes. `tactic_detector.py` is pure chess logic over an already-parsed PV with no I/O, no DB access, no request/response boundary, and no auth or crypto. No endpoint, schema, or input-validation surface is added, moved, or removed. |

ASVS level 1; blocking threshold: high.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-214-02-01 | Tampering | extracted helpers in `app/services/tactic_detector.py` | medium | mitigate | The one real risk of this phase is a split that silently changes behavior. Mitigated by three independent checks: 36 unit tests unchanged, the fixture-driven precision/recall report byte-compared (timestamp excluded), and a mutation proof that reverting an extracted helper turns the suite red. |
| T-214-02-02 | Repudiation | Sentry capture sites | low | accept | This file has zero `sentry_sdk.capture_exception(` sites before the split; the acceptance criterion asserts the count is still 0, so no diagnostic coverage can be lost here. |
</threat_model>

<verification>
- `uv run pytest -n auto tests/services/test_tactic_detector.py -q` green (36 tests).
- Tactic-tagger report byte-identical to the pre-split baseline apart from `**Generated:**`.
- `uv run ruff check app/services/tactic_detector.py` clean both with and without the ignore table.
- `uv run python scripts/check_function_size.py app/services/tactic_detector.py --fail-over-depth 4 --fail-over-loc 200` exits 0.
- `uv run ty check app/ tests/ scripts/` zero errors; `uv run ruff format --check app/ tests/ scripts/` clean.
- `uv run pytest -n auto -x` green.
- Mutation proof recorded in the SUMMARY.
</verification>

<success_criteria>
- `detect_tactic_motif` is a four-step orchestrator over three named stage helpers; `detect_clearance` is inside the branch limit.
- The file's `per-file-ignores` entry is gone and `ruff check .` is still green.
- Detection output is provably unchanged (unit tests + byte-identical harness report).
- Sentry capture count for the file is unchanged at 0; no test was touched.
</success_criteria>

## Artifacts this plan produces

**New symbols in `app/services/tactic_detector.py`:** `_dispatch_mate_tier()`,
`_collect_non_mate_candidates()`, `_select_shallowest_candidate()`, and (if the branch
reduction takes the predicate route rather than loop inversion) one named predicate helper for
`detect_clearance`.

**`pyproject.toml`:** the `[tool.ruff.lint.per-file-ignores]` entry for
`app/services/tactic_detector.py` is deleted.

**No new module files.** No test files added or modified.

<output>
Create `.planning/phases/214-backend-god-file-decomposition/214-02-SUMMARY.md` when done,
recording the mutation-proof result, the complexipy before/after count for this file, the
harness diff outcome, and any 100-200 logic-LOC survivors with a one-line justification each.
</output>
