---
phase: 214-backend-god-file-decomposition
plan: 03
type: execute
wave: 2
depends_on: ["214-01"]
files_modified:
  - app/services/endgame_service.py
  - pyproject.toml
autonomous: true
requirements: []
user_setup: []

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`_aggregate_endgame_stats` is a thin orchestrator — normalize row, accumulate, build per class, sort, return — and is under 200 logic LOC."
    - "`_aggregate_endgame_stats_by_tc` reuses the same normalize/accumulate/build helpers instead of duplicating them, and its nesting depth is at most 4."
    - "The file's only `sentry_sdk.capture_exception(` site still fires on the same per-row `endgame_class is None` branch."
    - "`uv run ruff check app/services/endgame_service.py` is clean both with the project's ignore table and with it emptied."
    - "All 372 tests across the file's four core test modules pass unchanged."
  artifacts:
    - app/services/endgame_service.py
    - pyproject.toml
  key_links:
    - "`_aggregate_endgame_stats` and `_aggregate_endgame_stats_by_tc` -> the shared `_normalize_endgame_row` / `_accumulate_endgame_rows` / `_build_category_stats` helpers"
    - "`_EndgameAccumulators` TypedDict -> the `app/services/stats_service.py::FilterParams` precedent CLAUDE.md cites"
    - "`_aggregate_per_tc_percentile` stays importable from `app.services.endgame_service` (pinned by a test import)"
    - "`count_endgame_games` stays a module attribute of `app.services.endgame_service` (patched at tests/test_endgame_service.py:1779)"
  prohibitions:
    - statement: "No behavior change: public signatures, return shapes, emitted SQL, and the single Sentry capture site are unchanged."
      status: flagged
      verification: manual
    - statement: "No test is deleted or weakened; the tests/ diff is additions only."
      status: flagged
      verification: manual
    - statement: "No new `# ty: ignore` line is added (this file already carries 15+ row-attribute suppressions; the count must not grow)."
      status: flagged
      verification: manual
    - statement: "No Sentry capture site is lost — the file's count stays at exactly 1."
      status: flagged
      verification: manual
    - statement: "No sibling module is created; every extracted helper stays in app/services/endgame_service.py."
      status: flagged
      verification: manual
    - statement: "No frontend file is touched."
      status: flagged
      verification: manual
---

<objective>
Turn `endgame_service.py`'s two worst aggregation functions into a shared
normalize/accumulate/build pipeline, and bring the per-TC card builder and the clock-row
iterator inside the ruff thresholds.

Purpose: this file holds the phase's single worst offenders —
`_aggregate_endgame_stats` (249 logic LOC, C901 25, PLR0912 30, PLR0915 145, cognitive
complexity 69) and `_aggregate_endgame_stats_by_tc` (nesting depth 5, cognitive complexity
70), which are the same accumulate-then-build shape one level apart. Splitting them once and
sharing the helpers removes the duplication that made them grow, and it is the only function
in the phase that breaches the 200-logic-LOC hard limit outright.

Output: four ruff-flagged functions inside their thresholds, no function in the file over 200
logic LOC or nesting depth 4, the file's `per-file-ignores` entry deleted, and 372 existing
tests passing untouched.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/214-backend-god-file-decomposition/214-PATTERNS.md

Read only the sections of
`.planning/phases/214-backend-god-file-decomposition/214-RESEARCH.md` named per task in
`<read_first>` — the full file is ~1,050 lines.

Do NOT `@`-load `.planning/PROJECT.md` (229 KB), `.planning/ROADMAP.md` (133 KB) or
`.planning/STATE.md` (302 KB).

**Phase contract (locked, from ROADMAP.md Phase 214):** zero behavior change — public
signatures, return shapes, emitted SQL, and Sentry capture sites unchanged. The existing tests
are the sole oracle and pass untouched; tests may be ADDED where a split exposes an untested
seam, never rewritten to fit the refactor. Split seams are "pipeline orchestrator -> one
function per stage" and "nested loops -> early continue / Counter accumulator", NOT "split to
fit a signature": no context dataclasses with fewer than three fields and one reader, no
handler bundles of unrelated callbacks. Renaming public entry points is out of scope.

**Planner decision — no sibling module in this phase.** The ROADMAP permits moving a cohesive
helper cluster into a sibling `.py` file, but no gate in this phase requires one, and four of
the six in-scope files have tests that patch names as module attributes (here:
`patch("app.services.endgame_service.count_endgame_games")` at
`tests/test_endgame_service.py:1779`). A module boundary is only free for dotted-path imports;
a bare-name call inside a moved function resolves against the NEW module's globals, so an
attribute patch on the origin module silently stops applying. Keep every extraction in
`app/services/endgame_service.py`. If you find a cluster where an in-file split is genuinely
not readable, stop and flag it rather than moving it.

**Merge discipline:** this plan shares exactly one file with its five wave-2 siblings —
`pyproject.toml`, where it deletes a single `per-file-ignores` line. Re-read `pyproject.toml`
immediately before that edit; a sibling plan may already have removed its own line. Each
wave-2 plan is its own squash-merge unit.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Extract the row-shape normalizer from _aggregate_endgame_stats</name>
  <precondition>Plan 214-01 is merged: `scripts/check_function_size.py` exists and `uv run ruff check . --config 'lint.per-file-ignores = {}'` reports C901/PLR0912/PLR0915 findings for this file.</precondition>
  <precondition>The dev PostgreSQL is running: `docker compose -f docker-compose.dev.yml -p flawchess-dev ps` shows `flawchess-dev-db-1` healthy.</precondition>
  <files>app/services/endgame_service.py</files>
  <read_first>
    - `app/services/endgame_service.py` lines 397-600 (`_aggregate_endgame_stats`'s accumulator declarations, the 6/8/9-tuple-vs-`Row` dispatch around lines 464-505, and the Sentry branch at 512-517)
    - `214-RESEARCH.md` section "Pattern 1: Accumulate-then-build pipeline" (worked example, recommended three-function split, exact line ranges)
    - `214-PATTERNS.md` section "`app/services/endgame_service.py` (service, CRUD/aggregation split)"
    - `app/services/stats_service.py` lines 55-72 (the `FilterParams` TypedDict shape to copy)
    - `CLAUDE.md` (function-size rules; "Don't split just to fit a signature"; TypedDicts for internal structured data)
  </read_first>
  <action>
Capture the oracle before touching anything: run the file's four-module test command and
record the passed count (372 at planning time).

Extract the tuple-length dispatch — the deepest nesting inside `_aggregate_endgame_stats` —
into `_normalize_endgame_row(row) -> _EndgameRow`, where `_EndgameRow` is a `NamedTuple`
carrying exactly the fields the per-row loop consumes. The dispatch handles three row shapes
(6-, 8- and 9-element tuples) plus a SQLAlchemy `Row`; keep all four branches and their field
ordering exactly as they are — a silently reordered field here produces plausible-looking but
wrong aggregates that no type checker will catch.

Leave the Sentry `set_context`/`capture_exception` pair where it is for now: it fires on the
per-row `endgame_class is None` branch, and it moves in task 2 together with that branch.

`_aggregate_per_tc_percentile` is imported by name in the tests — do not touch it.

Commit this task on its own before expanding.
  </action>
  <acceptance_criteria>
    - `_EndgameRow` (NamedTuple) and `_normalize_endgame_row` exist in `app/services/endgame_service.py`, with explicit type annotations, and `_aggregate_endgame_stats` calls the normalizer once per row.
    - All four row-shape branches survive; `git diff` shows the field ordering inside each branch unchanged.
    - `uv run pytest -n auto tests/test_endgame_service.py tests/services/test_endgame_service.py tests/test_aggregation_sanity.py tests/services/test_endgame_service_chip_decoupling.py -q` reports 0 failures and the same passed count as the pre-change baseline.
    - `grep -v '^\s*#' app/services/endgame_service.py | grep -c 'sentry_sdk\.capture_exception('` prints `1`.
    - The task is committed on its own (`git log -1 --stat` shows only `app/services/endgame_service.py`).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -n auto tests/test_endgame_service.py tests/services/test_endgame_service.py tests/test_aggregation_sanity.py tests/services/test_endgame_service_chip_decoupling.py -q</automated>
    <fails_when>non-zero exit, any "failed" or "error" count other than 0 in the summary line, or a passed count lower than the pre-change baseline</fails_when>
    <automated>grep -v '^\s*#' app/services/endgame_service.py | grep -c 'sentry_sdk\.capture_exception('</automated>
    <fails_when>prints any number other than `1`</fails_when>
    <automated>uv run ruff format --check app/ tests/ scripts/ && uv run ty check app/ tests/ scripts/</automated>
    <fails_when>non-zero exit, output naming a file as "would be reformatted", or output containing "error["</fails_when>
  </verify>
  <done>The row-shape dispatch is a named function reused by the whole pipeline, the 372-test oracle is green, and the split method is proven on the phase's largest file before the deeper extraction.</done>
  <reversibility rating="reversible">A single in-file extraction; reverting one commit restores the inline dispatch.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Accumulate/build split shared by both aggregate functions</name>
  <files>app/services/endgame_service.py</files>
  <read_first>
    - `app/services/endgame_service.py` lines 397-1039 (`_aggregate_endgame_stats` in full, and `_aggregate_endgame_stats_by_tc` immediately after it)
    - `214-RESEARCH.md` section "Pattern 1: Accumulate-then-build pipeline" (the 11 accumulators, the recommended helper boundaries, the Sentry placement rule)
    - `214-PATTERNS.md` sections "`app/services/endgame_service.py` (service, CRUD/aggregation split)" and "Sentry capture-site inventory"
    - `CLAUDE.md` (function-size rules; TypedDict-for-accumulators guidance)
  </read_first>
  <action>
Complete the three-function split RESEARCH specifies:

1. `_EndgameAccumulators` — a `TypedDict` with the 11 correlated per-key accumulators (`wdl`,
   `conv`, `recov`, `gaps_by_class`, `starts_by_class`, `ends_by_class`, `gaps_by_bucket`,
   `eval_sum_by_class`, `eval_sumsq_by_class`, `eval_n_by_class`, `last_played_at_by_class`).
   Copy the docstring shape of `app/services/stats_service.py::FilterParams`: state what the
   bundle is and cross-reference the function whose outputs it mirrors. Eleven correlated
   outputs of one row-scan pass is the sanctioned accumulator case, not the forbidden
   "context dataclass with fewer than three fields and one reader".
2. `_accumulate_endgame_rows(rows) -> _EndgameAccumulators` — the per-row loop. The Sentry
   `set_context` + `capture_exception` pair moves here WITH its `endgame_class is None`
   branch: never split a try/except or a guarded branch across a function boundary.
3. `_build_category_stats(endgame_class, accumulators) -> EndgameCategoryStats` — the
   per-class body, called once per key from a thin loop.

`_aggregate_endgame_stats` becomes: normalize -> accumulate -> build per class -> sort ->
return, preserving the existing sort key and the `(categories, dict(gaps_by_bucket))` return
shape.

Then make `_aggregate_endgame_stats_by_tc` call the SAME three helpers instead of carrying its
own copy of the loop. It differs only by an outer per-TC-bucket dispatch; that dispatch stays,
the inner work does not get duplicated. This is where its depth-5 nesting disappears.

Do not introduce a mutable context object threaded between the helpers — the accumulator
TypedDict is the only shared structure, and it flows one way.
  </action>
  <acceptance_criteria>
    - `_EndgameAccumulators` (TypedDict, 11 keys), `_accumulate_endgame_rows` and `_build_category_stats` exist with explicit type annotations.
    - `_aggregate_endgame_stats_by_tc` calls `_accumulate_endgame_rows` and `_build_category_stats`; `grep -c "for row in rows" app/services/endgame_service.py` did not increase relative to `git show HEAD~2` (no duplicated row loop).
    - `uv run ruff check app/services/endgame_service.py --config 'lint.per-file-ignores = {}' --output-format concise` reports no finding on `_aggregate_endgame_stats` or `_aggregate_endgame_stats_by_tc`.
    - `uv run python scripts/check_function_size.py app/services/endgame_service.py --fail-over-loc 200 --fail-over-depth 4 --json` shows `_aggregate_endgame_stats` under 200 logic LOC and `_aggregate_endgame_stats_by_tc` at depth 4 or less.
    - The Sentry pair sits inside `_accumulate_endgame_rows`, and the file's capture count is still exactly 1.
    - The four-module oracle reports 0 failures at the same passed count as the task-1 baseline.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -n auto tests/test_endgame_service.py tests/services/test_endgame_service.py tests/test_aggregation_sanity.py tests/services/test_endgame_service_chip_decoupling.py -q</automated>
    <fails_when>non-zero exit, any "failed" or "error" count other than 0 in the summary line, or a passed count lower than the task-1 baseline</fails_when>
    <automated>uv run ruff check app/services/endgame_service.py --config 'lint.per-file-ignores = {}' --output-format concise</automated>
    <fails_when>output contains a finding whose message names `_aggregate_endgame_stats` or `_aggregate_endgame_stats_by_tc`, or any `PLR0915` line for those two functions</fails_when>
    <automated>grep -v '^\s*#' app/services/endgame_service.py | grep -c 'sentry_sdk\.capture_exception('</automated>
    <fails_when>prints any number other than `1`</fails_when>
    <automated>uv run ty check app/ tests/ scripts/</automated>
    <fails_when>non-zero exit, or output containing "error["</fails_when>
  </verify>
  <done>Both aggregate functions share one normalize/accumulate/build pipeline, the 200-logic-LOC hard breach is gone, and the aggregates the endgame report renders are byte-for-byte the same as before.</done>
  <reversibility rating="reversible">In-file extraction and call-site rewiring; reverting the plan's commits restores both original functions.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Per-TC cards, clock-row branches, mutation proof, and ignore-entry deletion</name>
  <files>app/services/endgame_service.py, pyproject.toml</files>
  <read_first>
    - `app/services/endgame_service.py` lines 2596-2741 (`_compute_per_tc_metric_cards`, whose two stages are already labelled "Single pass: accumulate per-TC stats" and "Build cards in fixed TC order") and lines 2106-2266 (`_iterate_clock_rows`, PLR0912 15)
    - `214-RESEARCH.md` sections "Test Oracle Per File" (the endgame_service row and the mutation-test rule) and "Pitfall 1: Raw line count is not logic LOC"
    - `pyproject.toml` `[tool.ruff.lint.per-file-ignores]` as it stands right now
    - `CLAUDE.md` ("nested loops -> invert with early continue/return or a Counter accumulator")
  </read_first>
  <action>
1. `_compute_per_tc_metric_cards` is the same accumulate-then-build shape as task 2, one
   metric down: extract `_accumulate_per_tc_stats(...)` for the single pass and
   `_build_tc_metric_card(...)` for the per-TC card built under the
   `MIN_GAMES_PER_TC_CARD` gate. The fixed TC ordering of the output list must not change.
2. `_iterate_clock_rows` breaches only `PLR0912` (15 branches). Apply CLAUDE.md's named seam:
   invert the loop with early `continue` for the skip conditions, or extract one named
   predicate for the row-eligibility test. Do not split it into a helper per branch. Its D-04
   same-quintile counting (the covariance correction) must keep counting exactly the same
   rows.
3. Mutation proof (ROADMAP gate). Temporarily make `_build_category_stats` return a
   zero-filled record, run the four-module oracle, and confirm it FAILS. Restore, confirm
   green, and record which tests failed in the SUMMARY.
4. Re-read `pyproject.toml`, then delete the single `per-file-ignores` entry for
   `app/services/endgame_service.py`. Leave every other entry untouched.
5. For the SUMMARY: run `uv run python scripts/check_function_size.py
   app/services/endgame_service.py --json` and list every function still between 100 and 200
   logic LOC with a one-line justification — `get_endgame_overview` (~187 logic LOC at
   planning time) is a fetch-and-assemble orchestrator whose body is a sequence of awaited
   repository calls, and `_get_endgame_performance_from_rows` (~120) is a single statistical
   pass; neither is flagged by any ruff rule. That listing feeds ROADMAP success criterion 1.
  </action>
  <acceptance_criteria>
    - `_accumulate_per_tc_stats` and `_build_tc_metric_card` exist with explicit return type annotations, and the per-TC card list is emitted in the same fixed TC order as before.
    - `uv run ruff check app/services/endgame_service.py` exits 0 AND `uv run ruff check app/services/endgame_service.py --config 'lint.per-file-ignores = {}'` exits 0.
    - `uv run ruff check .` exits 0.
    - `uv run python scripts/check_function_size.py app/services/endgame_service.py --fail-over-depth 4 --fail-over-loc 200` exits 0.
    - `grep -v '^\s*#' app/services/endgame_service.py | grep -c 'sentry_sdk\.capture_exception('` prints `1`.
    - `git diff -- tests/` for this plan is empty.
    - `git diff --stat` for this plan lists `app/services/endgame_service.py` and `pyproject.toml` only, with no new file.
    - The SUMMARY records the mutation-proof result, the complexipy before/after count for this file (12 functions over 15 at planning time), and the 100-200 logic-LOC survivor listing with justifications.
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff check app/services/endgame_service.py && uv run ruff check app/services/endgame_service.py --config 'lint.per-file-ignores = {}' && uv run ruff check .</automated>
    <fails_when>non-zero exit from any of the three, or any output line containing `C901`, `PLR0912`, or `PLR0915`</fails_when>
    <automated>uv run python scripts/check_function_size.py app/services/endgame_service.py --fail-over-depth 4 --fail-over-loc 200</automated>
    <fails_when>non-zero exit, or any reported function with `max_nesting_depth` above 4 or `logic_loc` above 200</fails_when>
    <automated>uv run pytest -n auto tests/test_endgame_service.py tests/services/test_endgame_service.py tests/test_aggregation_sanity.py tests/services/test_endgame_service_chip_decoupling.py -q</automated>
    <fails_when>non-zero exit, any "failed" or "error" count other than 0, or a passed count lower than the task-1 baseline</fails_when>
    <automated>uv run ruff format --check app/ tests/ scripts/ && uv run ty check app/ tests/ scripts/ && uv run pytest -n auto -x -q</automated>
    <fails_when>non-zero exit, output naming a file as "would be reformatted", output containing "error[", or any "failed" count other than 0</fails_when>
  </verify>
  <done>Every ruff-flagged function in `endgame_service.py` is inside its threshold, no function breaches 200 logic LOC or depth 4, the file's exemption is deleted, and the endgame report's numbers are unchanged.</done>
  <reversibility rating="reversible">Code extraction plus a one-line TOML deletion.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Code moved, no trust boundary changes. `endgame_service.py` aggregates rows already fetched by the repository layer for the authenticated user; this plan adds no query, no endpoint, no request parameter, and no change to the `user_id` scoping already applied upstream. |

ASVS level 1; blocking threshold: high.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-214-03-01 | Tampering | `_normalize_endgame_row` / `_accumulate_endgame_rows` / `_build_category_stats` | medium | mitigate | A split that silently reorders a row field or drops an accumulator produces wrong-but-plausible statistics. Mitigated by the 372-test four-module oracle at an unchanged passed count, the explicit "field ordering unchanged" acceptance criterion, and a mutation proof that zero-filling `_build_category_stats` turns the suite red. |
| T-214-03-02 | Repudiation | the file's single `sentry_sdk.capture_exception(` site | medium | mitigate | A split can silently drop a capture and blind the error dashboard. Mitigated by a comment-filtered grep count asserted as exactly 1 after every task, plus the rule that the Sentry pair moves together with the `endgame_class is None` branch it guards. |
| T-214-03-03 | Information Disclosure | user-scoped aggregation | low | accept | No query is authored or modified here; `user_id` scoping stays entirely in the repository layer this plan does not touch. |
</threat_model>

<verification>
- Four-module oracle green at an unchanged passed count (372 at planning time).
- `uv run ruff check app/services/endgame_service.py` clean with and without the ignore table; `ruff check .` still exits 0.
- `uv run python scripts/check_function_size.py app/services/endgame_service.py --fail-over-depth 4 --fail-over-loc 200` exits 0.
- Sentry capture count for the file is exactly 1 after every task.
- `uv run ty check app/ tests/ scripts/` zero errors; `uv run ruff format --check app/ tests/ scripts/` clean; `uv run pytest -n auto -x` green.
- Mutation proof and the 100-200 logic-LOC survivor listing recorded in the SUMMARY.
</verification>

<success_criteria>
- The two worst aggregation functions share one pipeline; neither breaches C901, PLR0912, PLR0915, the 200-logic-LOC limit, or depth 4.
- `_compute_per_tc_metric_cards` and `_iterate_clock_rows` are inside their thresholds.
- The file's `per-file-ignores` entry is deleted and `ruff check .` is still green.
- No test was touched; the Sentry capture count is unchanged at 1.
</success_criteria>

## Artifacts this plan produces

**New symbols in `app/services/endgame_service.py`:** `_EndgameRow` (NamedTuple),
`_EndgameAccumulators` (TypedDict, 11 keys: `wdl`, `conv`, `recov`, `gaps_by_class`,
`starts_by_class`, `ends_by_class`, `gaps_by_bucket`, `eval_sum_by_class`,
`eval_sumsq_by_class`, `eval_n_by_class`, `last_played_at_by_class`),
`_normalize_endgame_row()`, `_accumulate_endgame_rows()`, `_build_category_stats()`,
`_accumulate_per_tc_stats()`, `_build_tc_metric_card()`, plus one named predicate helper if
the `_iterate_clock_rows` branch reduction takes the predicate route rather than loop
inversion.

**`pyproject.toml`:** the `[tool.ruff.lint.per-file-ignores]` entry for
`app/services/endgame_service.py` is deleted.

**No new module files.** No test files added or modified.

<output>
Create `.planning/phases/214-backend-god-file-decomposition/214-03-SUMMARY.md` when done,
recording the mutation-proof result, the complexipy before/after count for this file, and the
100-200 logic-LOC survivor listing with a one-line justification each.
</output>
