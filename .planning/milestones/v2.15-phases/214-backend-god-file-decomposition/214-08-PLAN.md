---
phase: 214-backend-god-file-decomposition
plan: 08
type: execute
wave: 3
depends_on: ["214-02", "214-03", "214-04", "214-05", "214-06", "214-07"]
files_modified:
  - .planning/codebase/CONCERNS.md
autonomous: true
requirements: []
user_setup: []

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`uv run ruff check .` passes with C901/PLR0912/PLR0915 enabled and none of the six in-scope files relies on a per-file-ignore any more."
    - "No function in the six in-scope files exceeds 200 logic LOC or nesting depth 4, apart from documented `allow-loc` exemptions."
    - "The full backend suite, ty and ruff are green, and the phase's aggregate `tests/` diff contains additions only."
    - "The CONCERNS.md 'Large God files' entry names only the four frontend files."
  artifacts:
    - .planning/codebase/CONCERNS.md
  key_links:
    - "the six in-scope files with an emptied ignore table -> ROADMAP success criterion 0"
    - "`scripts/check_function_size.py` over app/services and app/repositories -> ROADMAP success criterion 1"
    - "the aggregate `git diff -- tests/` across the phase -> ROADMAP success criterion 2"
    - "CONCERNS.md 'Large God files' -> ROADMAP success criterion 4"
  prohibitions:
    - statement: "No app/ source file is modified in this plan — it verifies and documents; if a gate fails, report it rather than patching the code here."
      status: flagged
      verification: manual
    - statement: "No test is deleted or weakened."
      status: flagged
      verification: manual
    - statement: "The CONCERNS.md entry is not deleted outright — the four frontend god files are still open debt and stay listed."
      status: flagged
      verification: manual
    - statement: "No frontend file is touched."
      status: flagged
      verification: manual
---

<objective>
Verify the phase's five success criteria against the merged result of plans 214-02..214-07 and
retire the backend half of the CONCERNS.md "Large God files" entry.

Purpose: each file plan proves its own file. Nothing so far proves the phase — that
`ruff check .` is green with zero in-scope exemptions left, that no function anywhere in the
six files still breaches the hard limits, and that the aggregate `tests/` diff really is
additions only. This plan is the one place those aggregate facts are measured and written
down, and it is the only correct moment to update the source concern that started the phase.

Output: a recorded phase-wide after-baseline (ruff, complexipy, `check_function_size.py`), and
a CONCERNS.md entry that names only the four frontend files.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/214-backend-god-file-decomposition/214-PATTERNS.md

Do NOT `@`-load `.planning/PROJECT.md` (229 KB), `.planning/ROADMAP.md` (133 KB) or
`.planning/STATE.md` (302 KB). The five success criteria are quoted in task 1.

**Ordering:** this plan runs after all six file plans are merged. If any of them has not
landed, stop and say which — a phase-wide baseline measured against a partial trunk is
worse than no baseline.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Measure the phase-wide after-baseline against all five success criteria</name>
  <precondition>Plans 214-02 through 214-07 are all merged into the working branch: `uv run ruff check .` exits 0 and `git log --oneline` shows a commit for each of the six in-scope files.</precondition>
  <precondition>The dev PostgreSQL is running: `docker compose -f docker-compose.dev.yml -p flawchess-dev ps` shows `flawchess-dev-db-1` healthy.</precondition>
  <files>(measurement only — no file is modified by this task)</files>
  <read_first>
    - `.planning/phases/214-backend-god-file-decomposition/214-01-SUMMARY.md` (the before-baselines to compare against)
    - the six file plans' SUMMARYs (`214-02-SUMMARY.md` … `214-07-SUMMARY.md`) — their per-file complexipy deltas, mutation proofs and survivor listings roll up into this plan's table
    - `CLAUDE.md` (the pre-merge gate command list)
  </read_first>
  <action>
Measure, do not fix. Record every number in the SUMMARY as an explicit before/after table
against the plan-01 baseline.

**Criterion 0 — ruff clean, no in-scope exemptions.**
Run `uv run ruff check .` (must exit 0), then run ruff over the six in-scope files with the
ignore table emptied on the command line:
`uv run ruff check app/services/endgame_service.py app/repositories/train_repository.py
app/services/eval_apply.py app/repositories/library_repository.py app/services/insights_llm.py
app/services/tactic_detector.py --config 'lint.per-file-ignores = {}' --output-format concise`
It must exit 0 — that is the direct proof the six files need no exemption, independent of what
the TOML happens to say. Also record `uv run ruff check . --config 'lint.per-file-ignores = {}'
--output-format concise | grep -cE 'C901|PLR0912|PLR0915'`, which should now be the plan-01
count minus every breach the six file plans fixed (the residue is the eight out-of-scope
files).

**Criterion 0b — complexipy before/after.**
`uv run complexipy app/ --max-complexity-allowed 15 --failed --sort desc` — record the app-wide
count against plan 01's baseline (97 at planning time) and the per-file counts for the six.

**Criterion 1 — size and depth.**
`uv run python scripts/check_function_size.py app/services app/repositories
--fail-over-depth 4 --fail-over-loc 200` must exit 0. Then run it with `--json` and build the
listing of every function in the six files between 100 and 200 logic LOC, with the one-line
justification each file plan's SUMMARY already recorded, plus any `allow_loc` exemption and its
reason. That listing is what ROADMAP criterion 1 asks to be recorded.

**Criterion 2 — full gate, tests additions-only.**
Run the CLAUDE.md pre-merge gate: `uv run ruff format --check app/ tests/ scripts/`,
`uv run ruff check .`, `uv run ty check app/ tests/ scripts/`,
`uv run --project analysis --with ty ty check analysis/`, `uv run pytest -n auto -x`. Then
verify the aggregate test diff for the whole phase is additions only:
`git diff --numstat <phase-base-commit>..HEAD -- tests/` — every row must show 0 deleted lines.
The only expected rows are the plan-01 script test and the plan-07 golden test plus its data
file.

**Criterion 3 — no new suppressions, growth from deliberate splits only.**
`git diff <phase-base-commit>..HEAD -- app/ scripts/ tests/ | grep -c '^+.*# ty: ignore'` must
be 0. Scope the diff to those three source trees: an unscoped diff also spans `.planning/`,
where this phase's plan documents quote the suppression syntax in prose and would make the
gate count its own planning text. Record
`git diff --stat <phase-base-commit>..HEAD` and confirm the only new files are
`scripts/check_function_size.py`, `tests/scripts/test_check_function_size.py` and
`tests/services/golden/insights_user_prompt.txt` — no in-scope module was split into a sibling
file this phase (that was a deliberate planner decision, recorded in each plan).

If ANY of these fails, stop and report exactly which criterion and which command — do not
patch app/ code from this plan.
  </action>
  <acceptance_criteria>
    - `uv run ruff check .` exits 0.
    - Ruff over the six in-scope files with an emptied ignore table exits 0.
    - `uv run python scripts/check_function_size.py app/services app/repositories --fail-over-depth 4 --fail-over-loc 200` exits 0.
    - The full pre-merge gate (ruff format check, ruff check, ty on app/tests/scripts, ty on analysis, full pytest) is green.
    - `git diff --numstat <phase-base-commit>..HEAD -- tests/` shows 0 in the deletions column on every row.
    - `git diff <phase-base-commit>..HEAD -- app/ scripts/ tests/ | grep -c '^+.*# ty: ignore'` prints `0`.
    - The SUMMARY contains a before/after table for ruff counts, complexipy app-wide and per-file counts, and the full 100-200 logic-LOC survivor listing with justifications and any `allow_loc` exemption.
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff check . && uv run ruff check app/services/endgame_service.py app/repositories/train_repository.py app/services/eval_apply.py app/repositories/library_repository.py app/services/insights_llm.py app/services/tactic_detector.py --config 'lint.per-file-ignores = {}' --output-format concise</automated>
    <fails_when>non-zero exit from either command, or any output line containing `C901`, `PLR0912`, or `PLR0915`</fails_when>
    <automated>uv run python scripts/check_function_size.py app/services app/repositories --fail-over-depth 4 --fail-over-loc 200</automated>
    <fails_when>non-zero exit, or any reported function with `max_nesting_depth` above 4 or a non-exempt `logic_loc` above 200</fails_when>
    <automated>uv run complexipy app/ --max-complexity-allowed 15 --failed --sort desc | tail -5</automated>
    <fails_when>the command cannot run (complexipy not installed), or the reported app-wide over-threshold count is higher than plan 01's recorded baseline</fails_when>
    <automated>uv run ruff format --check app/ tests/ scripts/ && uv run ty check app/ tests/ scripts/ && uv run --project analysis --with ty ty check analysis/ && uv run pytest -n auto -x -q</automated>
    <fails_when>non-zero exit, output naming a file as "would be reformatted", output containing "error[", or any "failed" or "error" count other than 0 in the pytest summary line</fails_when>
  </verify>
  <done>All five ROADMAP success criteria are measured against the merged trunk with recorded numbers, so the phase's claim rests on tool output rather than on six separate per-file assertions.</done>
  <reversibility rating="reversible">Measurement only; no file is modified.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Retire the backend half of the CONCERNS.md "Large God files" entry</name>
  <files>.planning/codebase/CONCERNS.md</files>
  <read_first>
    - `.planning/codebase/CONCERNS.md` lines 18-22 (the "Large God files" entry: Issue / Files / Impact / Fix approach)
    - this plan's task-1 measurements (the actual post-phase line counts for the six backend files)
    - `CLAUDE.md` (the function-size rule the entry cites)
  </read_first>
  <action>
Rewrite the "Large God files" entry so it describes only what is still true:

- **Issue**: reframe around the four frontend pages/hooks/modules. Drop the
  `endgame_service.py` examples (`_iterate_clock_rows`, `_get_endgame_performance_from_rows`) —
  they are no longer the immediate hard-limit-breaching candidates, and leaving them in makes
  the entry misleading to the next reader.
- **Files**: list only `frontend/src/pages/Analysis.tsx`, `frontend/src/hooks/useBotGame.ts`,
  `frontend/src/lib/engine/workerPool.ts` and `frontend/src/pages/Openings.tsx`, with their
  current line counts measured now (`wc -l`), not the values copied from the 2026-09-02 map.
- **Impact**: keep the substance, scoped to the frontend.
- **Fix approach**: point at the follow-up phase and note that the eslint `complexity` /
  `max-depth` gates are still to be enabled for the frontend, mirroring what Phase 214 did for
  the backend with ruff `C901`/`PLR0912`/`PLR0915`, `complexipy` and
  `scripts/check_function_size.py`.

Add one line recording that the backend god files were decomposed in Phase 214 (with the
date), so the entry's history is not lost — a concern that silently shrinks reads like an
oversight. Write that history line WITHOUT listing the backend module paths: name the phase and
the date only. The verification below greps the entry's `Files:` line for an `app/` path, and a
history line that re-lists the modules would make a correctly-narrowed entry look wrong.

Do not touch the neighbouring `# ty: ignore` suppressions concern. It cites
`train_repository.py:2670`, `eval_apply.py:785,801,…` and a cluster in `endgame_service.py`
whose line numbers this phase shifted; correcting those is not in this phase's scope, and
rewriting a concern nobody measured would be worse than a stale line number. If you want, note
in the SUMMARY that those line references are now stale so a future map refresh picks it up.
  </action>
  <acceptance_criteria>
    - The "Large God files" entry's Files line names exactly the four `frontend/` paths and no `app/` path.
    - `grep -A8 'Large "God files"' .planning/codebase/CONCERNS.md | grep '^- Files:' | grep -c 'app/'` prints `0` — the entry's Files line names no backend module.
    - The four frontend line counts in the entry match current `wc -l` output.
    - The entry retains an Issue, Files, Impact and Fix approach line (it is edited, not deleted), plus a line recording the Phase 214 backend decomposition and its date.
    - The neighbouring `# ty: ignore` suppressions concern is unchanged (`git diff` shows no hunk touching it).
    - `git diff --stat` for this plan lists `.planning/codebase/CONCERNS.md` only.
  </acceptance_criteria>
  <verify>
    <automated>grep -A8 'Large "God files"' .planning/codebase/CONCERNS.md | grep '^- Files:' | grep -c 'app/'</automated>
    <fails_when>prints any number other than `0`</fails_when>
    <automated>grep -A8 'Large "God files"' .planning/codebase/CONCERNS.md | grep -c 'frontend/src/pages/Analysis.tsx'</automated>
    <fails_when>prints `0` (the frontend files must still be listed — the entry is narrowed, not deleted)</fails_when>
    <automated>git diff --stat -- .planning/codebase/CONCERNS.md</automated>
    <fails_when>no output (the file was not modified), or output listing any file other than `.planning/codebase/CONCERNS.md`</fails_when>
  </verify>
  <done>The concern that motivated this phase now describes only the frontend debt that remains, with current line counts and a pointer to the follow-up phase.</done>
  <reversibility rating="reversible">A planning-document edit.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Code moved, no trust boundary changes — and this plan moves none: it runs verification commands and edits one planning document. No application code, configuration, dependency, endpoint, or schema is touched. |

ASVS level 1; blocking threshold: high.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-214-08-01 | Repudiation | the phase-wide after-baseline | medium | mitigate | A closeout that asserts success without measuring it would let a missed breach ship as "verified". Mitigated by making every criterion a runnable command with a stated failing direction, and by the instruction to stop and report rather than patch when one fails. |
| T-214-08-02 | Tampering | `.planning/codebase/CONCERNS.md` | low | accept | A documentation edit with no runtime effect; the acceptance criteria keep it from being deleted outright or from silently swallowing the adjacent suppressions concern. |
</threat_model>

<verification>
- `uv run ruff check .` exits 0; the six in-scope files are clean with an emptied ignore table.
- `uv run python scripts/check_function_size.py app/services app/repositories --fail-over-depth 4 --fail-over-loc 200` exits 0.
- complexipy app-wide count recorded and no higher than plan 01's baseline.
- Full pre-merge gate green (ruff format check, ruff check, ty on app/tests/scripts, ty on analysis, `pytest -n auto -x`).
- Aggregate `tests/` diff shows 0 deletions on every row; zero added `# ty: ignore` lines.
- CONCERNS.md "Large God files" names only the four frontend files.
</verification>

<success_criteria>
- All five ROADMAP success criteria (0, 1, 2, 3, 4) are measured and recorded with commands and numbers.
- The CONCERNS.md entry is narrowed to the frontend, with current line counts and a Phase 214 history line.
- No app/ source file changed in this plan.
</success_criteria>

## Artifacts this plan produces

**`.planning/codebase/CONCERNS.md`** — the "Large God files" entry rewritten to name only
`frontend/src/pages/Analysis.tsx`, `frontend/src/hooks/useBotGame.ts`,
`frontend/src/lib/engine/workerPool.ts` and `frontend/src/pages/Openings.tsx`, with current
line counts, a frontend-scoped fix approach naming the eslint `complexity` / `max-depth`
follow-up, and a line recording the Phase 214 backend decomposition.

**No new symbols, no new source files, no dependency change.**

<output>
Create `.planning/phases/214-backend-god-file-decomposition/214-08-SUMMARY.md` when done,
containing the phase-wide before/after table (ruff counts, complexipy app-wide and per-file,
`check_function_size.py` results), the complete 100-200 logic-LOC survivor listing with
justifications and any `allow_loc` exemption, the aggregate `tests/` diff verdict, and a note
about the now-stale line numbers in the neighbouring `# ty: ignore` concern.
</output>
