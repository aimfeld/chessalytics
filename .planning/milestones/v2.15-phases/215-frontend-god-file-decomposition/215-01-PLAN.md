---
phase: 215-frontend-god-file-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/eslint.config.js
  - frontend/eslint.config.sonarjs.mjs
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/knip.json
  - docs/dev-tooling.md
  - CLAUDE.md
  - frontend/CLAUDE.md
autonomous: true
requirements: [SC-0]
user_setup: []

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SC-0: `npm run lint` exits 0 from `frontend/` with `complexity`, `max-depth` and `max-statements` enabled at severity `error` (every pre-existing breach baselined in one dedicated override region that only ever shrinks)."
    - "With the baseline override defeated on the command line (`--rule` wins over a flat-config `files:` block), eslint reports the measured breach counts — proving the three rules and their thresholds are live in `eslint.config.js` rather than merely spelled correctly."
    - "The three rules are scoped to `**/*.{ts,tsx}` so the vendored bundles under `frontend/public/` are never linted by them."
    - "`npm run lint:cognitive` runs `sonarjs/cognitive-complexity` at threshold 15 over the same tree and is NOT part of `npm run lint` or CI."
    - "`npm run knip` still exits 0 after the new devDependency is added."
    - "docs/dev-tooling.md, the root CLAUDE.md function-size rule and frontend/CLAUDE.md all name the new frontend rules with runnable commands."
  artifacts:
    - frontend/eslint.config.js
    - frontend/eslint.config.sonarjs.mjs
    - frontend/package.json
    - docs/dev-tooling.md
    - CLAUDE.md
    - frontend/CLAUDE.md
  key_links:
    - "`eslint.config.js` `files: ['**/*.{ts,tsx}']` rules block -> `npm run lint` in `.github/workflows/ci.yml`"
    - "the Phase 215 baseline override region -> one path deleted by 215-06 (`src/pages/Analysis.tsx`) and one by 215-07 (`src/pages/Openings.tsx`)"
    - "`npm run lint:cognitive` -> the before/after cognitive-complexity counts recorded by 215-08"
    - "`--rule` on the CLI defeating a `files:` override block -> the SC-0/SC-1 proof command every later plan uses"
  prohibitions:
    - statement: "No file under frontend/src/ is modified in this plan — it touches lint configuration, package manifests and docs only."
      status: flagged
      verification: manual
    - statement: "No test is deleted or weakened; the `__tests__`/`*.test.*` diff for this plan is empty."
      status: flagged
      verification: manual
    - statement: "`max-lines-per-function` is NOT added to the enforced rule set — it counts the JSX return tree, which CLAUDE.md's logic-LOC rule explicitly excludes."
      status: flagged
      verification: manual
    - statement: "`npm run lint:cognitive` is NOT added to `npm run lint` nor to `.github/workflows/ci.yml` this phase."
      status: flagged
      verification: manual
    - statement: "None of the 28 pre-existing `react-hooks/*` warnings is fixed, and the rule severities for `react-hooks/*` are unchanged."
      status: flagged
      verification: manual
    - statement: "No backend file is touched (Phase 214 already did the backend)."
      status: flagged
      verification: manual
---

<objective>
Land the frontend complexity gate BEFORE any file is split, so every later plan is measured
rather than eyeballed.

Purpose: this is the frontend twin of 214-01. Plans 215-02..215-07 each prove their file with
a number, and two of them delete their own path from the baseline override region as proof of
completion. Success criteria 0 and 1 have no meaning until a tool measures them, and the
measurement command itself has a trap: `npm run lint` is `eslint .`, which also walks the
vendored engine bundles under `frontend/public/` (`stockfish-18-lite-single.js`,
`ort.wasm.min.js`, `maia-worker.js`, …). Those breach all three rules heavily, so the rules
must be scoped to `**/*.{ts,tsx}`.

Output: three eslint core rules enforced at `error` with every pre-existing breach baselined
and `npm run lint` green on day one; `eslint-plugin-sonarjs` behind a report-only
`npm run lint:cognitive`; all of it documented in `docs/dev-tooling.md`, the root `CLAUDE.md`
function-size rule and `frontend/CLAUDE.md`; the app-wide before-baselines recorded in this
plan's SUMMARY.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@frontend/CLAUDE.md
@.planning/phases/215-frontend-god-file-decomposition/215-PATTERNS.md

Read only the sections of
`.planning/phases/215-frontend-god-file-decomposition/215-RESEARCH.md` named per task in
`<read_first>` — the full file is ~925 lines.

Do NOT `@`-load `.planning/PROJECT.md`, `.planning/ROADMAP.md` (133 KB) or
`.planning/STATE.md` (312 KB). Everything from the phase contract you need is quoted below.

**Phase contract (locked, from ROADMAP.md Phase 215):** zero behavior change. Rendered DOM
(every `data-testid`, every `data-umami-event`), hook dependency arrays, the engine message
protocol and TanStack Query keys are unchanged. Existing vitest suites are the sole behavior
oracle and pass untouched; tests may be ADDED where a split exposes an untested seam, never
rewritten to fit the refactor. Split seams are the ones CLAUDE.md names — React components
extract data shaping into a `useXyzData` hook, desktop/mobile renderers split past ~40 LOC of
logic each, handler arrows become named functions, closure factories become one function per
stage — and NOT "split to fit a signature". Renaming the public entry points (`Analysis`,
`OpeningsPage`, `useBotGame`, `createWorkerPool`) is out of scope. Fixing any of the 28
pre-existing `react-hooks/*` warnings is out of scope: adding a dependency re-fires an effect,
which IS a behavior change.

**All frontend commands run from `frontend/`.** `npm run lint`, `npm run build` (`tsc -b` —
lint and vitest do NOT type-check), `npm run knip`, `npx vitest run <path>`.

**Security enforcement is inactive for this project** (`.planning/config.json` has no
`workflow.security_*` keys). The `<threat_model>` block below is recorded for continuity with
Phase 214, not because a gate demands it.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Enable complexity/max-depth/max-statements at `error` with a measured baseline, `npm run lint` green</name>
  <precondition>`npm run lint` from `frontend/` exits 0 on the current trunk (verified at planning time, 2026-09-03) — the baseline you are about to bake in is measured against a green tree.</precondition>
  <files>frontend/eslint.config.js</files>
  <read_first>
    - `frontend/eslint.config.js` (whole file, 57 lines) — the single `files: ['**/*.{ts,tsx}']` block with its `rules` object, and the three existing `files: [...] / rules: {...}` override blocks for `src/components/ui/**`, `src/components/filters/**`, `src/components/analysis/**`
    - `frontend/package.json` lines 6-17 (the `scripts` block; `"lint": "eslint ."`)
    - `215-RESEARCH.md` sections "Baseline — use this session's numbers, one discrepancy vs. the ROADMAP's '~35 files'" and "Architecture Patterns" items 1-3 (`max-statements` counts nested functions separately; `npm run lint` has no `--max-warnings`, so `warn` is a no-op; the `files: [...]` override block is this config's own established relaxation mechanism)
    - `215-PATTERNS.md` section "ESLint flat-config baseline override block (tooling plan)"
    - `CLAUDE.md` lines 156-163 (the function-size rule these rules enforce)
  </read_first>
  <action>
Measure FIRST, then write the config from what you measured.

**Step 1 — measure.** From `frontend/`, run the reconnaissance command (severity `warn` here on
purpose, so it does not exit non-zero and abort the survey):

`npx eslint --no-inline-config --rule 'complexity: ["warn", 15]' --rule 'max-depth: ["warn", 4]' --rule 'max-statements: ["warn", 100]' --rule 'max-lines-per-function: ["warn", {"max": 200, "skipBlankLines": true, "skipComments": true}]' -f json .`

and reduce the JSON twice: once over ALL linted paths, once restricted to paths ending in
`.ts`/`.tsx`. Planner measurements from 2026-09-03 (eslint 10.4.1), which yours should
reproduce:

| Rule | all linted paths | `.ts`/`.tsx` only |
|---|---|---|
| `complexity > 15` | 117 breaches / 58 files | **69 breaches / 52 files** |
| `max-depth > 4` | 27 / 4 | **6 / 1** (`src/lib/__tests__/reminderSlotState.test.ts`) |
| `max-statements > 100` | 4 / 3 | **1 / 1** (`src/pages/Analysis.tsx`) |
| `max-lines-per-function > 200` | — | **66 / 62** |

The gap between the two columns is six vendored bundles under `frontend/public/`
(`engine/stockfish-18-lite-single.js`, `maia/maia-worker.js`, `maia/ort-wasm-simd-threaded.mjs`,
`maia/ort-wasm-simd-threaded.asyncify.mjs`, `maia/ort.wasm.min.js`, `maia/ort.webgpu.min.js`).
`globalIgnores(['dist', 'dev-dist'])` does not cover `public/`, and `npm run lint` is
`eslint .`, so those files ARE linted today. Note the `.ts`/`.tsx` numbers differ from
215-RESEARCH.md's 68/51 because RESEARCH excluded test paths and `eslint .` does not — the one
extra file is `src/lib/engine/__tests__/treeCommon.test.ts`. Save both reductions verbatim for
the SUMMARY.

**Step 2 — enforce, scoped to TypeScript.** Add the three rules to the `rules` object of the
EXISTING `files: ['**/*.{ts,tsx}']` block (the one that already carries
`'react-hooks/set-state-in-effect': 'off'`), NOT as a new top-level block:
`complexity: ['error', 15]`, `'max-depth': ['error', 4]`, `'max-statements': ['error', 100]`.
Severity is `error` and not `warn`: `npm run lint` is `eslint .` with no `--max-warnings` flag,
so a `warn`-severity rule can never fail CI and the gate would be decorative. Scoping to
`**/*.{ts,tsx}` is what keeps the six vendored `public/` bundles out — do NOT add a
`globalIgnores` entry for `public/`, which would change what the rest of the config lints.

Do NOT add `max-lines-per-function` to the enforced set. It counts the returned JSX tree,
which CLAUDE.md's logic-LOC rule explicitly excludes, so `OpeningsPage`'s 585-line render would
fail it forever. It stays a reconnaissance rule invoked ad hoc by later plans and by 215-08.

**Step 3 — baseline what already breaches.** After the three existing `react-refresh` override
blocks, add a new region of override blocks, opened by a comment naming Phase 215 and stating
the contract: this region only ever shrinks, and a file's entry is deleted by the plan that
fixes it. Three blocks, following the exact shape of the existing overrides:

1. `files: [<the 52 measured `.ts`/`.tsx` paths with a `complexity` breach>]`, `rules: { complexity: 'off' }`. Sort the array so a future diff is readable, and put `src/pages/Analysis.tsx` and `src/pages/Openings.tsx` at the END under their own one-line comment naming the plans that delete them (215-06 and 215-07) — the other 50 are out of Phase 215 scope and stay until a future phase touches them.
2. `files: ['src/pages/Analysis.tsx']`, `rules: { 'max-statements': 'off' }` — the only `max-statements` breach in the TypeScript tree. 215-06 deletes this block.
3. `files: ['src/lib/__tests__/reminderSlotState.test.ts']`, `rules: { 'max-depth': 'off' }` — the only `max-depth` breach in the TypeScript tree. It is a test file and out of scope; RESEARCH's claim that `max-depth` "needs no baseline" was derived from a non-test-only survey and does not hold for `eslint .`.

`src/hooks/useBotGame.ts` and `src/lib/engine/workerPool.ts` get NO entry in any of the three
blocks: measured at threshold 15/100/4 they have zero breaches today (`useBotGame` sits at
cyclomatic complexity exactly 15, which does not exceed the max; `workerPool.ts`'s worst inner
function is `handleLine` at 14). Their plans prove their improvement with
`max-lines-per-function`, not with a deletion from this region. Adding a decorative entry for
them would make the region grow instead of shrink.

**Step 4 — prove the rules are live.** Confirm that a CLI `--rule` flag defeats a flat-config
`files:` override block, which is this phase's equivalent of Phase 214's "ignore table emptied"
proof (verified at planning time: a scratch config turning `complexity` off for
`src/pages/Analysis.tsx` reported 0 findings, and the same config plus
`--rule 'complexity: ["error", 15]'` reported 6). Every later plan and 215-08 depend on this
mechanism.
  </action>
  <acceptance_criteria>
    - `frontend/eslint.config.js`'s `files: ['**/*.{ts,tsx}']` block contains `complexity: ['error', 15]`, `'max-depth': ['error', 4]` and `'max-statements': ['error', 100]`.
    - The file contains no `max-lines-per-function` key anywhere.
    - The Phase 215 baseline region contains exactly three override blocks with the rule keys `complexity`, `max-statements` and `max-depth` set to `'off'`, and the `complexity` block's `files` array holds the 52 paths you measured.
    - `src/pages/Analysis.tsx` and `src/pages/Openings.tsx` appear in the `complexity` array under a comment naming plans 215-06 and 215-07.
    - Neither `src/hooks/useBotGame.ts` nor `src/lib/engine/workerPool.ts` appears anywhere in the baseline region: `grep -v '^\s*//' frontend/eslint.config.js | grep -c 'useBotGame.ts\|engine/workerPool.ts'` prints `0`.
    - `npm run lint` from `frontend/` exits 0.
    - `npx eslint --no-inline-config --rule 'complexity: ["error", 15]' src/pages/Analysis.tsx` reports 6 `complexity` findings (lines 549, 1640, 2321, 2390, 3169, 3338), proving the CLI override defeats the baseline block.
    - Both measurement reductions from step 1 are pasted verbatim into the plan SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run lint</automated>
    <fails_when>non-zero exit, or any output line naming rule `complexity`, `max-depth` or `max-statements`</fails_when>
    <automated>cd frontend && npx eslint --no-inline-config --rule 'complexity: ["error", 15]' -f json src/pages/Analysis.tsx | python3 -c "import json,sys; d=json.load(sys.stdin); n=sum(1 for f in d for m in f['messages'] if m['ruleId']=='complexity'); print(n)"</automated>
    <fails_when>prints `0` (the baseline block swallowed the CLI override, so no later plan can prove anything), or prints a number other than `6`</fails_when>
    <automated>cd frontend && grep -c "max-lines-per-function" eslint.config.js</automated>
    <fails_when>prints any number other than `0`</fails_when>
    <automated>cd frontend && grep -v '^\s*//' eslint.config.js | grep -c 'useBotGame.ts\|engine/workerPool.ts'</automated>
    <fails_when>prints any number other than `0`</fails_when>
    <automated>cd frontend && npx eslint --no-inline-config --rule 'complexity: ["error", 15]' --rule 'max-depth: ["error", 4]' --rule 'max-statements: ["error", 100]' src/hooks/useBotGame.ts src/lib/engine/workerPool.ts</automated>
    <fails_when>non-zero exit — these two files must be clean against the enforced rules today, which is why they get no baseline entry</fails_when>
  </verify>
  <done>The three complexity rules are enforced at `error` across the TypeScript tree, every pre-existing breach is baselined in one labelled region, `npm run lint` is green, and the CLI-override proof command that plans 215-02..215-08 all rely on is verified to work.</done>
  <reversibility rating="costly">The lint config flows into CI (`.github/workflows/ci.yml:141` runs `npm run lint`); reverting is a config revert, cheap in effort, but it un-gates every later plan's success criterion, so it is not a free undo.</reversibility>
</task>

<task type="auto">
  <name>Task 2: eslint-plugin-sonarjs behind a report-only `npm run lint:cognitive`</name>
  <files>frontend/package.json, frontend/package-lock.json, frontend/eslint.config.sonarjs.mjs, frontend/knip.json</files>
  <read_first>
    - `215-RESEARCH.md` sections "Package Legitimacy Audit" (the `[OK]` verdict, peer ranges, and the `"skip-dev": true` finding that keeps `audit-ci` clean) and "Architecture Patterns" item 4 (the rule's default threshold is already 15)
    - `frontend/package.json` (the `scripts` block and `devDependencies`)
    - `frontend/knip.json` (whole file — `project` is `src/**/*.{ts,tsx}` only, so a dependency imported from a root-level config file is invisible to knip)
    - `frontend/audit-ci.jsonc` (the `"skip-dev": true` line)
    - `215-PATTERNS.md` section "No Analog Found" (there is no codebase precedent for a second eslint config behind its own script; RESEARCH's verified scratch-config recipe is the starting point)
  </read_first>
  <action>
`eslint-plugin-sonarjs@4.2.0` is already audited `[OK]` in 215-RESEARCH.md's Package
Legitimacy Audit (SonarSource-maintained, 4.1M weekly downloads, `postinstall` null, peer
ranges verified against `eslint@10.4.1` and `typescript@~6.0.3`), so no human legitimacy
checkpoint is required. Confirm the npm page resolves to `github.com/SonarSource/SonarJS`
before installing, then:

1. `npm install --save-dev eslint-plugin-sonarjs@4.2.0` from `frontend/`.
2. Create `frontend/eslint.config.sonarjs.mjs`: import the default export of
   `./eslint.config.js`, spread it, and append one block
   `{ files: ['**/*.{ts,tsx}'], plugins: { sonarjs }, rules: { 'sonarjs/cognitive-complexity': ['error', 15] } }`.
   Open the file with a comment stating it is report-only, that it is deliberately not part of
   `npm run lint` or CI, and why 15 is the threshold (it is CLAUDE.md's own cognitive-complexity
   target and also the rule's own default). Severity is `error` so that a future decision to
   gate it needs a script change only, not a config change.
3. Add `"lint:cognitive": "eslint --config eslint.config.sonarjs.mjs ."` to `scripts`,
   immediately after `"lint"`. Do NOT chain it into `lint`, and do NOT touch
   `.github/workflows/ci.yml`.
4. Run `npm run knip`. `knip.json`'s `project` glob covers `src/**` only, so the new
   root-level config file is invisible to knip and the new devDependency will very likely be
   reported as unused, which would break the CI knip gate. If it is reported, add
   `eslint-plugin-sonarjs` to the existing `ignoreDependencies` array with the other
   config-only entries. If knip does resolve it, change nothing.
5. Record the app-wide cognitive-complexity baseline for the SUMMARY:
   `npm run lint:cognitive` reduced to a breach count and a file count (RESEARCH measured 41
   breaches across 34 non-test files; your number over the full `eslint .` scope will differ and
   yours is the one that counts), plus the per-file counts for the four in-scope files
   (RESEARCH: `Analysis.tsx` 4 — lines 549/1640/1782/2390; `useBotGame.ts` 0;
   `workerPool.ts` 1 — line 896 `handleLine`; `Openings.tsx` 1 — line 114).
6. Re-run `npx audit-ci --config audit-ci.jsonc` to confirm the install did not disturb the
   vulnerability gate (RESEARCH verified it cannot, because `audit-ci.jsonc` sets
   `"skip-dev": true`, but confirm rather than assume).
  </action>
  <acceptance_criteria>
    - `frontend/package.json` `devDependencies` lists `eslint-plugin-sonarjs` at `4.2.0` (or a `^4.2.0` range resolving to it in `package-lock.json`).
    - `frontend/package.json` `scripts` contains `"lint:cognitive"` and `"lint"` is still exactly `"eslint ."`.
    - `frontend/eslint.config.sonarjs.mjs` exists, spreads the base config, and sets `'sonarjs/cognitive-complexity': ['error', 15]`.
    - `npm run lint:cognitive` runs to completion and reports `sonarjs/cognitive-complexity` findings (it is expected to exit non-zero — it is a report, not a gate).
    - `npm run lint` still exits 0 and reports no `sonarjs/*` rule (the plugin is not wired into the main config).
    - `npm run knip` exits 0.
    - `npx audit-ci --config audit-ci.jsonc` exits 0.
    - `grep -c "lint:cognitive" .github/workflows/ci.yml` prints `0`.
    - The SUMMARY records the app-wide cognitive breach/file counts and the four per-file counts.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run lint:cognitive 2>&1 | grep -c "sonarjs/cognitive-complexity"</automated>
    <fails_when>prints `0` (the plugin did not load or the rule never fired — a report that reports nothing is not a working report)</fails_when>
    <automated>cd frontend && npm run lint 2>&1 | grep -c "sonarjs"</automated>
    <fails_when>prints any number other than `0` — the plugin must not leak into the gating lint run</fails_when>
    <automated>cd frontend && npm run lint && npm run knip</automated>
    <fails_when>non-zero exit from either, or knip output naming `eslint-plugin-sonarjs` as an unused dependency</fails_when>
    <automated>cd frontend && npx audit-ci --config audit-ci.jsonc</automated>
    <fails_when>non-zero exit, or output reporting a non-zero count in the high or critical column</fails_when>
    <automated>grep -c "lint:cognitive" .github/workflows/ci.yml</automated>
    <fails_when>prints any number other than `0`</fails_when>
  </verify>
  <done>Cognitive complexity is measurable on demand with one command, the before-baseline is recorded, and neither the gating lint run nor CI nor the vulnerability gate changed behavior.</done>
  <reversibility rating="reversible">One devDependency, one new config file, one npm script; `npm uninstall` plus deleting the file and script restores the prior state exactly.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Document the frontend gate in docs/dev-tooling.md and both CLAUDE.md files</name>
  <files>docs/dev-tooling.md, CLAUDE.md, frontend/CLAUDE.md</files>
  <read_first>
    - `docs/dev-tooling.md` lines 5-21 (the "Scripts" bullet list, ending with the `check_function_size.py`, "Ruff complexity rules" and `complexipy` bullets that Phase 214 added — the frontend entries join this list in the same register)
    - `CLAUDE.md` lines 156-163 (the "Keep functions small and shallow" bullet, in particular the "Backend measurement tools:" sub-bullet the new frontend line mirrors)
    - `frontend/CLAUDE.md` "Code Style & Safety" section (the knip bullet is the closest neighbour in tone and length)
    - `215-PATTERNS.md` section "Docs register for the tooling plan (backend twin)"
  </read_first>
  <action>
Three edits, each terse and in the register of the text it joins. No em-dash pile-ups; one per
paragraph at most.

1. `docs/dev-tooling.md` — add two bullets immediately after the `complexipy` bullet:
   - **Frontend complexity rules**: `complexity` (max 15), `max-depth` (max 4) and
     `max-statements` (max 100) are enabled at `error` inside `frontend/eslint.config.js`'s
     `**/*.{ts,tsx}` block, so `npm run lint` gates them in CI. State why they are scoped to
     TypeScript (`eslint .` also walks the vendored engine bundles under `frontend/public/`),
     why `max-statements` is the logic-LOC analog rather than `max-lines-per-function` (a JSX
     return tree is one statement, which honours CLAUDE.md's "excluding the returned JSX tree"
     carve-out), and that every pre-existing breach is baselined in one labelled override
     region that only ever shrinks. Give the reconnaissance command later readers will want:
     `npx eslint --no-inline-config --rule 'complexity: ["error", 15]' <path>` defeats the
     baseline region, which is how a file's real state is checked.
   - **`npm run lint:cognitive`**: `sonarjs/cognitive-complexity` at 15, report-only, not in
     `npm run lint` and not in CI, with its own config file `frontend/eslint.config.sonarjs.mjs`.
2. `CLAUDE.md` — add a "Frontend measurement tools:" sub-bullet directly under the existing
   "Backend measurement tools:" sub-bullet in the function-size rule, naming `npm run lint`
   (the three rules with their thresholds) and `npm run lint:cognitive`, and pointing at
   `docs/dev-tooling.md` for detail. Keep it to the guardrail; the detail lives in the
   reference doc per this repo's own split.
3. `frontend/CLAUDE.md` — add one bullet to "Code Style & Safety" stating that the three
   complexity rules gate `npm run lint`, that a new breach must be fixed rather than baselined
   (the override region is a historical baseline, not an escape hatch), and that
   `npm run lint:cognitive` is available as a report.
  </action>
  <acceptance_criteria>
    - `grep -c 'lint:cognitive' docs/dev-tooling.md CLAUDE.md frontend/CLAUDE.md` prints a non-zero count for each of the three filenames.
    - `grep -c 'max-statements' docs/dev-tooling.md` is at least 1 and the surrounding text explains the JSX-return-tree carve-out.
    - `CLAUDE.md` contains the string `Frontend measurement tools` inside the "Keep functions small and shallow" bullet, immediately after the "Backend measurement tools" sub-bullet.
    - `frontend/CLAUDE.md` gained exactly one new bullet under "Code Style & Safety" and no other section changed.
    - `git diff --stat` for this task lists only `docs/dev-tooling.md`, `CLAUDE.md` and `frontend/CLAUDE.md`.
    - `npm run lint`, `npm run build`, `npm run knip` and `npm test -- --run` are all green from `frontend/` (the docs edits must not disturb anything, and this is the plan's full-gate checkpoint).
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'lint:cognitive' docs/dev-tooling.md CLAUDE.md frontend/CLAUDE.md</automated>
    <fails_when>any of the three filenames reports a count of `0`</fails_when>
    <automated>grep -c 'Frontend measurement tools' CLAUDE.md</automated>
    <fails_when>prints `0`</fails_when>
    <automated>cd frontend && npm run lint && npm run build && npm run knip</automated>
    <fails_when>non-zero exit from any of the three, or `tsc` output containing `error TS`</fails_when>
    <automated>cd frontend && npm test -- --run 2>&1 | tail -5</automated>
    <fails_when>non-zero exit, or the summary line reporting any non-zero "failed" count</fails_when>
  </verify>
  <done>The frontend gate is discoverable from the same three places the backend gate is, so the next reader finds it without archaeology.</done>
  <reversibility rating="reversible">Three documentation edits; a revert restores them.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds no endpoint, request/response schema, auth or crypto code, and no runtime dependency. It changes linter configuration, adds a dev-only eslint plugin consumed by a report-only script, and edits documentation. No trust boundary is created, moved or removed. |

Security enforcement is inactive for this project; ASVS level 1 assumed, blocking threshold high.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-215-01-SC | Tampering | `npm install --save-dev eslint-plugin-sonarjs` | high | mitigate | Audited `[OK]` in 215-RESEARCH.md's Package Legitimacy Audit against the npm registry (65 published versions, 4,115,741 weekly downloads, repo `github.com/SonarSource/SonarJS`, `deprecated: false`, `postinstall: null`). Task 2 re-confirms the npm project page before install; the resolved version is pinned in `package-lock.json`; it is a devDependency consumed only by eslint and never reaches the shipped bundle. `npx audit-ci` is re-run as an acceptance criterion. |
| T-215-01-02 | Tampering | the `eslint.config.js` baseline override region | low | accept | A mis-scoped relaxation could hide real findings rather than create a vulnerability. Mitigated by the CLI-override proof command, which reports a file's true state regardless of the region; accepted as a tooling-only risk. |
</threat_model>

<verification>
- `npm run lint` exits 0 from `frontend/` with the three rules enabled at `error`.
- `npx eslint --no-inline-config --rule 'complexity: ["error", 15]' src/pages/Analysis.tsx` reports 6 findings, proving the rules are live and the CLI override defeats the baseline region.
- `src/hooks/useBotGame.ts` and `src/lib/engine/workerPool.ts` are clean against all three enforced rules and carry no baseline entry.
- `npm run lint:cognitive` reports `sonarjs/cognitive-complexity` findings; `npm run lint` reports none.
- `npm run build`, `npm run knip`, `npm test -- --run` and `npx audit-ci --config audit-ci.jsonc` all green.
- `docs/dev-tooling.md`, `CLAUDE.md` and `frontend/CLAUDE.md` all name the new rules and the cognitive script.
</verification>

<success_criteria>
- SC-0 groundwork: the three eslint complexity rules are enforced project-wide across `**/*.{ts,tsx}` and `npm run lint` is green with every pre-existing breach baselined in one labelled region.
- The region's `complexity` array carries `src/pages/Analysis.tsx` and `src/pages/Openings.tsx` under a comment naming the plans that delete them; `useBotGame.ts` and `workerPool.ts` are deliberately absent.
- `eslint-plugin-sonarjs` is a real devDependency behind a report-only `npm run lint:cognitive`, outside `npm run lint` and outside CI.
- All three tools are documented in `docs/dev-tooling.md` and referenced from both CLAUDE.md files.
- The SUMMARY records the app-wide before-baselines for all four rules plus the cognitive-complexity counts.
</success_criteria>

## Artifacts this plan produces

**New file `frontend/eslint.config.sonarjs.mjs`** — default-exports the base
`eslint.config.js` array spread with one appended block registering the `sonarjs` plugin and
setting `'sonarjs/cognitive-complexity': ['error', 15]`.

**`frontend/eslint.config.js` keys added** — inside the existing `files: ['**/*.{ts,tsx}']`
block: `complexity: ['error', 15]`, `'max-depth': ['error', 4]`,
`'max-statements': ['error', 100]`. Plus a new Phase 215 baseline region of three override
blocks: `{ files: [<52 paths>], rules: { complexity: 'off' } }`,
`{ files: ['src/pages/Analysis.tsx'], rules: { 'max-statements': 'off' } }`,
`{ files: ['src/lib/__tests__/reminderSlotState.test.ts'], rules: { 'max-depth': 'off' } }`.

**`frontend/package.json`** — new script `lint:cognitive`; new devDependency
`eslint-plugin-sonarjs@4.2.0`. **`frontend/package-lock.json`** — resolved tree.
**`frontend/knip.json`** — `eslint-plugin-sonarjs` added to `ignoreDependencies` if knip
reports it unused.

**`docs/dev-tooling.md`** — two new bullets in the "Scripts" list.
**`CLAUDE.md`** — a "Frontend measurement tools:" sub-bullet in the function-size rule.
**`frontend/CLAUDE.md`** — one new bullet in "Code Style & Safety".

**No new source symbol, no new component, no new hook.**

<output>
Create `.planning/phases/215-frontend-god-file-decomposition/215-01-SUMMARY.md` when done,
including both verbatim baseline reductions from task 1 (all-paths and `.ts`/`.tsx`-only), the
cognitive-complexity app-wide and per-file counts, and the `react-hooks/*` warning counts
(`exhaustive-deps` and `refs`) that plans 215-02..215-08 must leave unchanged.
</output>
