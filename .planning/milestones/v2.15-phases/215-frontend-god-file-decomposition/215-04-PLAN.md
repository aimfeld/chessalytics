---
phase: 215-frontend-god-file-decomposition
plan: 04
type: execute
wave: 2
depends_on: ["215-01"]
files_modified:
  - frontend/src/pages/Analysis.tsx
  - frontend/src/hooks/analysis/useAnalysisRouteParams.ts
  - frontend/src/hooks/analysis/useAnalysisEngineLines.ts
  - frontend/src/hooks/analysis/useAnalysisRouteSeeding.ts
autonomous: true
requirements: [SC-1, SC-2, SC-3]
user_setup: []

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SC-1: the arrow at the former `Analysis.tsx:1640` (`qualityBySanWithGem`, cyclomatic 16) no longer breaches cyclomatic complexity 15 in any file."
    - "SC-1: `Analysis()`'s statement count is strictly below the recorded pre-split 213 and its cyclomatic complexity strictly below the recorded pre-split 176 (both are still over the limits at the end of this plan; 215-06 lands them)."
    - "SC-2: all 85 tests in `src/pages/__tests__/Analysis.test.tsx` pass unchanged, and the app-wide `react-hooks/exhaustive-deps` plus `react-hooks/refs` warning count is exactly 28, unchanged in either direction."
    - "Every moved `useEffect`/`useMemo`/`useCallback` keeps its dependency array byte-identical, and the five suppression comments at the former lines 1057/1072/1083/1124/1154 move with the effects they guard — the total across `Analysis.tsx` plus the new hooks stays exactly 6."
    - "SC-3: the `data-testid` inventory of `Analysis.tsx` is byte-identical before and after (24 attributes, 0 `data-umami-event`), and no TypeScript suppression comment is added."
    - "The default export `Analysis` keeps its name and remains the default export of `@/pages/Analysis`."
  artifacts:
    - frontend/src/hooks/analysis/useAnalysisRouteParams.ts
    - frontend/src/hooks/analysis/useAnalysisEngineLines.ts
    - frontend/src/hooks/analysis/useAnalysisRouteSeeding.ts
    - frontend/src/pages/Analysis.tsx
  key_links:
    - "`useAnalysisRouteParams` -> `useAnalysisRouteSeeding` -> `useAnalysisBoard`'s imperative `loadMainLine`/`goToNode`/`insertPvLine`"
    - "`useAnalysisEngineLines` -> the move-quality UI (`EngineLines`, `MaiaHumanPanel`, quality bars)"
    - "the five `exhaustive-deps` suppression comments -> the app-wide 28-warning invariant measured by 215-08"
    - "`vi.mock('@/lib/engine/workerPool', () => ({ isLowPowerDevice: () => false }))` at Analysis.test.tsx:83 -> unchanged, because no engine import path moves in this plan"
  prohibitions:
    - statement: "No behavior change: what the page computes and renders is identical, including which engine line wins a tie and which move-quality badge is shown."
      status: flagged
      verification: manual
    - statement: "No test is deleted or weakened; the test diff for this plan is additions only, and may be empty."
      status: flagged
      verification: manual
    - statement: "None of the 28 pre-existing `react-hooks/*` warnings is fixed. Adding a missing dependency re-fires an effect, which IS a behavior change, and the count must not drop."
      status: flagged
      verification: manual
    - statement: "No render fragment or JSX is moved in this plan — the render split is 215-06's scope. This plan touches only the hook and data section, the former lines 549-1741."
      status: flagged
      verification: manual
    - statement: "The analysis board INTENTIONALLY shows opponent gems (`best_move_tier` is position-scoped, not user-scoped). A change that filters gem markers to the user's own plies is a regression, not a fix."
      status: flagged
      verification: manual
    - statement: "No entry is deleted from the Phase 215 baseline override region — `Analysis.tsx` still breaches `complexity` and `max-statements` at the end of this plan, and 215-06 owns both deletions."
      status: flagged
      verification: manual
---

<objective>
Extract the URL/route-parameter, engine-line-reconciliation and route-seeding clusters of
`Analysis()` into three sibling hooks — the first of three plans that together bring
`Analysis()` inside the enforced complexity and statement limits.

Purpose: `Analysis()` is 2,037 counted lines, 213 statements and cyclomatic complexity 176, by
far the largest target in the phase, and its 85 tests assert only through rendered DOM, so a
regression surfaces several hooks downstream of its cause. Splitting it in one plan would
exceed a single executor's context; three plans each read a bounded region and prove it with
the same oracle. This plan takes the region 549-1741, 215-05 takes the marker and arrow
clusters, 215-06 takes the render tree and lands both baseline deletions.

Output: `src/hooks/analysis/useAnalysisRouteParams.ts`, `useAnalysisEngineLines.ts` and
`useAnalysisRouteSeeding.ts`; the cyclomatic-16 `qualityBySanWithGem` memo inside the limit; a
recorded before/after statement and complexity count for `Analysis()`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@frontend/CLAUDE.md
@.planning/phases/215-frontend-god-file-decomposition/215-PATTERNS.md

Read only the sections of
`.planning/phases/215-frontend-god-file-decomposition/215-RESEARCH.md` named per task in
`<read_first>` — the full file is ~925 lines.

Do NOT `@`-load `.planning/PROJECT.md`, `.planning/ROADMAP.md` (133 KB) or
`.planning/STATE.md` (312 KB).

**Phase contract (locked, from ROADMAP.md Phase 215):** zero behavior change. Rendered DOM
(every `data-testid`, every `data-umami-event`), hook dependency arrays (including
intentionally missing deps — the suppression comment moves with the code), the engine message
protocol and TanStack Query keys are unchanged. Existing vitest suites are the sole behavior
oracle and pass untouched; tests may be ADDED where a split exposes an untested seam, never
rewritten to fit the refactor. Split seams are the ones CLAUDE.md names — for a React
component, extract data shaping into a `useXyzData` hook — and NOT "split to fit a signature":
no context object with fewer than three fields and one reader, no "handlers" hook bundling
unrelated callbacks by shared deps. Renaming `Analysis` is out of scope. Any "obvious" fix
spotted during the split is out of scope: capture it as a seed, do not implement it.

**Rules of hooks:** every extracted hook is called unconditionally, once per render, in the
same relative position its cluster occupied. Branch INSIDE the hook on an `enabled`-style flag;
never wrap the call site in a condition. `useStockfishGradingEngine` is already called twice
with an `enabled` flag rather than conditionally (lines 1400 and 2313) — that is the pattern.

**`noUncheckedIndexedAccess` is on.** `npm run lint` and vitest do NOT type-check;
`npm run build` (`tsc -b`) is the only step that catches a narrowing regression introduced by a
copy-paste split. Narrow explicitly; never add a type suppression.

**Line numbers refer to `Analysis.tsx` at the phase base commit.** They shift as you edit;
re-locate by symbol name, not by line, after the first task.

**All frontend commands run from `frontend/`.** `npm run lint`, `npm run build`,
`npm run knip`, `npx vitest run <path>`.

**Merge discipline:** this plan is its own squash-merge unit and is the first of three that
touch `Analysis.tsx`; 215-05 and 215-06 depend on it and merge after it. All wave-2 plans read
`frontend/eslint.config.js`'s Phase 215 baseline region, so wave 2 merges sequentially rather
than running in parallel worktrees.

**Security enforcement is inactive for this project** (`.planning/config.json` has no
`workflow.security_*` keys). The `<threat_model>` block is recorded for continuity with
Phase 214.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Extract useAnalysisRouteParams end to end through the 85-test oracle</name>
  <precondition>Plan 215-01 is merged: `npm run lint` exits 0 with the three complexity rules enabled and `npx eslint --no-inline-config --rule 'complexity: ["error", 15]' src/pages/Analysis.tsx` reports 6 findings (lines 549, 1640, 2321, 2390, 3169, 3338).</precondition>
  <files>frontend/src/hooks/analysis/useAnalysisRouteParams.ts, frontend/src/pages/Analysis.tsx</files>
  <read_first>
    - `frontend/src/pages/Analysis.tsx` lines 549-600 (`searchParams`/`navigate`, `lineParam`/`lineSans` 559, `fenParam`/`rootFenSeed` 568, `orientationParam`/`urlOrientation` 576, `gameIdRaw`/`plyRaw` 584, `gameId` 587, `initialPly` 592, `isGameMode` 597) and lines 999-1035 (`seededKey` 999, `navigatedInitialPlyKey` 1000, `pasteHandoffConsumed` 1006, `initialTactic` 1011, `initialAlignPly` 1021, `autoOrientation` 1034)
    - `frontend/src/pages/Analysis.tsx` lines 398-548 (the module-level pure helpers `fenToRootPly`, `forkPlyForOrientation`, `flawKey`, `formatPastedResultDate`, `bestSanFromPv`, `findFocusedFlaw`, `isNodeInsideSubtree`, `buildFocusedPvLine`) — they already live outside the component and do NOT move
    - `frontend/src/hooks/useFastForward.ts` lines 1-90 (the analog: JSDoc header explaining WHY the hook is separate, one typed options object in, a named result interface out, named module-level constants with derivation comments)
    - `frontend/src/pages/__tests__/Analysis.test.tsx` lines 1-100 (the provider stack, the mutable `engineState` mock object, the `matchMedia` definition at 336, and the `vi.mock('@/lib/engine/workerPool', () => ({ isLowPowerDevice: () => false }))` factory at line 83)
    - `215-RESEARCH.md` section "Seam map — `Analysis.tsx` (`Analysis()`, 549-4370)" (the nine-cluster table, and the note that `Analysis()` already composes eleven extracted hooks, so this phase extends an existing convention rather than inventing one)
    - `215-PATTERNS.md` sections "Extracted `useXyzData` hooks (Analysis.tsx clusters)" and "Extracted URL/effect-seeding hook (Analysis route-param cluster)"
    - `CLAUDE.md` lines 156-163 (function-size rule; "Don't split just to fit a signature")
  </read_first>
  <action>
Capture the before-baselines first, from `frontend/`:
- `npx eslint --no-inline-config --rule 'complexity: ["warn", 15]' --rule 'max-statements: ["warn", 100]' --rule 'max-lines-per-function: ["warn", {"max": 200, "skipBlankLines": true, "skipComments": true}]' src/pages/Analysis.tsx` — expect `Analysis` at complexity 176, 213 statements, 2037 lines, plus complexity findings at 1640 (16), 2321 (19), 2390 (27), 3169 (18), 3338 (19). Measured 2026-09-03.
- `grep -o 'data-testid="[^"]*"' src/pages/Analysis.tsx | sort > /tmp/analysis-testid-before.txt` — expect 24 attributes; confirm `grep -c 'data-umami-event' src/pages/Analysis.tsx` prints 0.
- `grep -c 'eslint-disable' src/pages/Analysis.tsx` — expect 6.
- `npx vitest run src/pages/__tests__/Analysis.test.tsx` — expect 85 passed.
All four go in the SUMMARY.

Create the directory `frontend/src/hooks/analysis/` and its first module,
`useAnalysisRouteParams.ts`. It owns the read-only derivation of everything the page learns
from the URL: `lineSans`, `rootFenSeed`, `urlOrientation`, `gameId`, `initialPly`,
`isGameMode`, `initialTactic`, `initialAlignPly` and `autoOrientation`. Signature: one typed
`UseAnalysisRouteParamsOptions` object in (it needs `gameData?.user_color` for
`autoOrientation`, plus whatever `initialAlignPly` reads), one named
`UseAnalysisRouteParamsResult` interface out. It calls `useSearchParams()` itself; `navigate`
stays in the component, because navigation is an action the page performs rather than a value
it derives.

Open the file with a JSDoc block in `useFastForward.ts`'s register: what it owns, WHY it is a
separate hook (URL parsing is a pure derivation with no board or engine state), and a
cross-reference to this phase.

`seededKey`, `navigatedInitialPlyKey` and `pasteHandoffConsumed` are seeding refs, not route
params — they stay in the component for now and move in task 3 with the effects that use them.

Destructure the hook's result into the same local names the rest of the body already uses, so
no downstream reference changes. Re-run the four baselines and confirm the testid inventory
diff is empty.

Commit this task on its own before expanding.
  </action>
  <acceptance_criteria>
    - `frontend/src/hooks/analysis/useAnalysisRouteParams.ts` exports `useAnalysisRouteParams`, `UseAnalysisRouteParamsOptions` and `UseAnalysisRouteParamsResult`, and opens with a JSDoc block naming why the hook is separate.
    - `Analysis.tsx` no longer declares `lineSans`, `rootFenSeed`, `urlOrientation`, `gameId`, `initialPly`, `isGameMode`, `initialTactic`, `initialAlignPly` or `autoOrientation` locally; each name is destructured from the hook's result.
    - `diff <(grep -o 'data-testid="[^"]*"' src/pages/Analysis.tsx | sort) /tmp/analysis-testid-before.txt` produces no output.
    - `npx vitest run src/pages/__tests__/Analysis.test.tsx` reports 85 passed, 0 failed.
    - `npm run lint`, `npm run build` and `npm run knip` are green.
    - `Analysis`'s statement count is strictly below 213 and its cyclomatic complexity strictly below 176 (both recorded in the SUMMARY).
    - `git diff -- 'frontend/src/**/__tests__/*' 'frontend/src/**/*.test.*'` is empty for this task.
    - The task is committed on its own; `git log -1 --stat` lists only the two files in `<files>`.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/__tests__/Analysis.test.tsx 2>&1 | tail -8</automated>
    <fails_when>non-zero exit, or the summary line does not contain "85 passed", or it reports any non-zero "failed" count</fails_when>
    <automated>cd frontend && diff <(grep -o 'data-testid="[^"]*"' src/pages/Analysis.tsx | sort) /tmp/analysis-testid-before.txt</automated>
    <fails_when>`diff` prints any hunk line or exits non-zero — a testid appeared or vanished, breaking the browser-automation contract</fails_when>
    <automated>cd frontend && npx eslint --no-inline-config --rule 'complexity: ["warn", 15]' --rule 'max-statements: ["warn", 100]' src/pages/Analysis.tsx 2>&1 | grep -cE "'Analysis' has a complexity of 176|'Analysis' has too many statements \(213\)"</automated>
    <fails_when>prints any number other than `0` — a surviving `176` or `213` means the extraction removed nothing from the component's own body</fails_when>
    <automated>cd frontend && npm run lint && npm run build && npm run knip</automated>
    <fails_when>non-zero exit from any of the three, `tsc` output containing "error TS", or knip naming an export of the new hook as unused</fails_when>
  </verify>
  <done>The `src/hooks/analysis/` directory exists with its first hook, the 85-test oracle is green, the rendered testid inventory is byte-identical, and the hook-extraction method is proven end to end on the lowest-risk cluster before the engine and seeding clusters move.</done>
  <reversibility rating="reversible">One new hook file and a call-site rewiring; reverting the commit restores the inline derivations.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Extract useAnalysisEngineLines — the reconciliation cluster and the cyclomatic-16 quality memo</name>
  <files>frontend/src/hooks/analysis/useAnalysisEngineLines.ts, frontend/src/pages/Analysis.tsx</files>
  <read_first>
    - `frontend/src/pages/Analysis.tsx` lines 1186-1290 (`engineAssetStatus` 1186, `rootPly` 1188, `currentPly` 1189, `canGoForward` 1191, `playedSan` 1201, `bestSan` 1208, `shownSans` 1222, `rawProbBySan` 1231, `flawChessDisplayedSans` 1240, `freeRunCommitted` 1255, `unionSans` 1267) and lines 1290-1742 (the effect at 1290, `evalLookup` 1412, `gradedCandidateUcis` 1422, `reconciledTieBreakUci` 1434, `reconciledBestUci` 1445, `reconciledBestSan` 1469, `reconciledStockfishLine` 1484, `reconciledBestEval` 1503, `reconciledRankedLines` 1524, `flawChessRankedLinesForVerdict` 1545, `reconciledPvLines` 1561, `qualityBySan` 1592, `qualityBySanWithGem` 1640, `engineTopLines` 1726)
    - `frontend/src/pages/Analysis.tsx` lines 1396-1412 (the first `useStockfishGradingEngine` call — decide whether it moves into the hook or stays and its result is passed in)
    - `frontend/src/hooks/useLiveMoveFlaw.ts` lines 1-40 (the analog for a `useMemo`-shaped pure transform hook: module-level `Record<...>` constant, small named result interface, no internal state or effects)
    - `215-RESEARCH.md` section "Seam map — `Analysis.tsx`", the "Engine-line reconciliation" row (named the single cleanest, highest-value extraction in the file), plus "Assumptions Log" row A2 and "Open Questions" item 2 (the cluster table is a starting hypothesis derived from a grep survey, not a line-by-line read — confirm boundaries by reading before committing to them, or a producer and its consumer land in different hooks)
    - `215-PATTERNS.md` section "Extracted transform hooks (gem-sweep / board-arrows)"
  </read_first>
  <action>
Create `frontend/src/hooks/analysis/useAnalysisEngineLines.ts` holding the engine-line
reconciliation cluster: the derived memos at 1191-1289 (`canGoForward`, `bestSan`, `shownSans`,
`rawProbBySan`, `flawChessDisplayedSans`, `unionSans`, plus the plain derivations `playedSan`
and `freeRunCommitted`) and the reconciliation memos at 1412-1741. One typed
`UseAnalysisEngineLinesOptions` object in — the Stockfish and FlawChess engine results, the
current `position`, node state, and the grading result — and one named
`UseAnalysisEngineLinesResult` interface out.

Confirm the cluster boundary by reading before you move anything. If `evalLookup` is produced
by the `useStockfishGradingEngine` call at 1396-1412, pass that result in as an option rather
than calling `useStockfishGradingEngine` a third time inside the hook: a third engine instance
would change engine behavior, which is exactly what this phase forbids.

`qualityBySanWithGem` (1640, cyclomatic 16) is the one function in this cluster that breaches
the enforced limit. Bring it inside 15 with CLAUDE.md's own named seam — invert a branch with
an early `return`/`continue` inside the loop, or extract one named predicate helper for the
condition it tests. Not five one-line helpers. Its output is a `Map<string, MoveQualityEval>`
consumed by the quality bars; the mapping from a SAN to its badge must not change, including
which moves carry a gem badge. The analysis board INTENTIONALLY shows opponent gems, because
`best_move_tier` is position-scoped; filtering them to the user's own plies is a regression.

Every `useMemo` moves with its dependency array byte-identical. There should be no suppression
comment in 1191-1741 (the five in this file's hook section are at 1057-1154 and belong to task
3); confirm the total across `Analysis.tsx` plus the new hooks is still 6.
  </action>
  <acceptance_criteria>
    - `frontend/src/hooks/analysis/useAnalysisEngineLines.ts` exports `useAnalysisEngineLines`, `UseAnalysisEngineLinesOptions` and `UseAnalysisEngineLinesResult`.
    - `npx eslint --no-inline-config --rule 'complexity: ["error", 15]' src/hooks/analysis/` exits 0 — `qualityBySanWithGem` is inside the limit in its new home.
    - `npx eslint --no-inline-config --rule 'complexity: ["warn", 15]' src/pages/Analysis.tsx` no longer reports a finding for the former `qualityBySanWithGem`; the remaining findings are `Analysis` itself plus the four arrows 215-05 and 215-06 own.
    - `npx vitest run src/pages/__tests__/Analysis.test.tsx` reports 85 passed.
    - The testid inventory diff against the task-1 baseline is empty.
    - `grep -c 'eslint-disable' src/pages/Analysis.tsx src/hooks/analysis/*.ts` sums to exactly 6.
    - `npm run build` exits 0 and the diff adds no TypeScript suppression comment.
    - No context object with fewer than three fields was introduced to pass state between the hook and the component.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/__tests__/Analysis.test.tsx 2>&1 | tail -8</automated>
    <fails_when>non-zero exit, or the summary line does not contain "85 passed"</fails_when>
    <automated>cd frontend && npx eslint --no-inline-config --rule 'complexity: ["error", 15]' --rule 'max-depth: ["error", 4]' --rule 'max-statements: ["error", 100]' src/hooks/analysis/</automated>
    <fails_when>non-zero exit, or any output line naming rule `complexity`, `max-depth` or `max-statements`</fails_when>
    <automated>cd frontend && diff <(grep -o 'data-testid="[^"]*"' src/pages/Analysis.tsx | sort) /tmp/analysis-testid-before.txt</automated>
    <fails_when>`diff` prints any hunk line or exits non-zero</fails_when>
    <automated>cd frontend && grep -c 'eslint-disable' src/pages/Analysis.tsx src/hooks/analysis/*.ts | awk -F: '{s+=$2} END {print s}'</automated>
    <fails_when>prints any number other than `6`</fails_when>
    <automated>cd frontend && npm run lint && npm run build</automated>
    <fails_when>non-zero exit from either, or `tsc` output containing "error TS"</fails_when>
    <automated>cd frontend && git diff -- src/pages/Analysis.tsx src/hooks/analysis/ | grep -cE '^\+.*@ts-(ignore|expect-error)'</automated>
    <fails_when>prints any number other than `0`</fails_when>
  </verify>
  <done>The engine-line reconciliation cluster lives in its own hook, the worst-offending memo in this region is inside the cyclomatic limit, and the 85-test oracle plus the testid inventory prove the rendered result is unchanged.</done>
  <reversibility rating="reversible">One new hook file plus call-site rewiring; reverting the commit restores the inline memos.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Extract useAnalysisRouteSeeding with its five suppression comments, then measure and mutation-prove</name>
  <files>frontend/src/hooks/analysis/useAnalysisRouteSeeding.ts, frontend/src/pages/Analysis.tsx</files>
  <read_first>
    - `frontend/src/pages/Analysis.tsx` lines 999-1010 (`seededKey`, `navigatedInitialPlyKey`, `pasteHandoffConsumed` — the seeding refs) and lines 1036-1161 (the six seeding effects at 1036, 1045, 1067, 1079, 1097, 1130, with the five `eslint-disable-next-line react-hooks/exhaustive-deps` comments sitting INSIDE the effect bodies on the line above the closing dependency array, at 1057, 1072, 1083, 1124 and 1154)
    - `frontend/src/pages/Analysis.tsx` lines 677-748 (the `useAnalysisBoard` destructuring that provides `loadMainLine`, `goToNode`, `insertPvLine`, `nodes`, `mainLine`, `currentNodeId` — the imperative surface these effects drive) and line 633 (`hasAutoFlipped` ref, used by the effect at 1036)
    - `frontend/src/pages/Analysis.tsx` lines 292-312 (`useAnalysisLayoutMode`, the in-file precedent for a plain non-exported hook with an effect and matching cleanup — the shape to promote)
    - `215-RESEARCH.md` sections "Behavior-Preservation Hazards / Rules-of-hooks ordering" and "Seam map — `Analysis.tsx`", the "URL/route param seeding" row
    - `215-PATTERNS.md` section "`useEffect`/`useMemo`/`useCallback` dependency-array + disable-comment atomicity"
  </read_first>
  <action>
1. Create `frontend/src/hooks/analysis/useAnalysisRouteSeeding.ts` holding the seeding refs
   (`seededKey`, `navigatedInitialPlyKey`, `pasteHandoffConsumed`, `hasAutoFlipped`) and the six
   effects at 1036-1160. These effects imperatively seed board state through `loadMainLine`,
   `goToNode` and `insertPvLine`, so the hook takes those callbacks plus the route params
   through its options object and returns nothing except whatever state the page still reads.

   This is the highest-regression-risk extraction in the plan: effect ORDER and effect TIMING
   are the behavior. Move the six effects in their existing order, keep each dependency array
   byte-identical, and move each `eslint-disable-next-line react-hooks/exhaustive-deps` comment
   as part of the effect body it sits in — same relative position, same wording. Do not add a
   dependency to silence a warning; the app-wide count of 28 is a hard invariant in both
   directions, and adding a dependency re-fires an effect, which is a behavior change.

   The effect at 1045 carries the Phase 210 / SEED-042 comment explaining why it seeds from the
   game's own `initial_fen` rather than the standard start (a custom-start game otherwise took
   the page down through the ErrorBoundary, Sentry FLAWCHESS-96). Move that comment verbatim; it
   is the reason the `?? STARTING_FEN` fallback exists.

2. Measure and record for the SUMMARY:
   - `npx eslint --no-inline-config --rule 'complexity: ["warn", 15]' --rule 'max-statements: ["warn", 100]' --rule 'max-lines-per-function: ["warn", {"max": 200, "skipBlankLines": true, "skipComments": true}]' src/pages/Analysis.tsx src/hooks/analysis/` — `Analysis`'s complexity, statements and lines after this plan, against the 176 / 213 / 2037 baseline, plus which of the five originally-flagged arrows remain (215-05 owns 2321, 2390 and 3169; 215-06 owns 3338).
   - The app-wide react-hooks warning count:
     `npx eslint --no-inline-config -f json . | python3 -c "..."` counting `react-hooks/exhaustive-deps` plus `react-hooks/refs` across `.ts`/`.tsx` paths. It must be exactly 28.
   - `npm run lint:cognitive` filtered to `src/pages/Analysis.tsx` and `src/hooks/analysis/`, against the pre-split baseline of 4 breaches in `Analysis.tsx` (lines 549, 1640, 1782, 2390).

3. Two-way mutation proof, one function per new hook module. For each of
   `useAnalysisRouteParams` (mutate the `isGameMode` derivation to a constant),
   `useAnalysisEngineLines` (mutate `reconciledBestUci` to return `null`) and
   `useAnalysisRouteSeeding` (mutate the game-mode seeding effect at the former line 1045 to
   return early): apply the mutation, run
   `npx vitest run src/pages/__tests__/Analysis.test.tsx`, confirm it FAILS and record which
   tests failed; restore and confirm 85 passed. Record all six outcomes in the SUMMARY. If a
   mutation leaves the suite GREEN, that seam is unguarded: ADD a test to the existing
   `Analysis.test.tsx` (never a parallel file, never a rewrite of an existing test), re-run the
   mutation and record the new result. RESEARCH recommends the mutation gate here even though
   the ROADMAP names it only for `workerPool.ts` and `Openings.tsx`, precisely because this
   file's tests assert only through rendered DOM.
  </action>
  <acceptance_criteria>
    - `frontend/src/hooks/analysis/useAnalysisRouteSeeding.ts` exports `useAnalysisRouteSeeding` plus its options interface, and contains exactly 5 `eslint-disable-next-line react-hooks/exhaustive-deps` comments.
    - `grep -c 'eslint-disable' src/pages/Analysis.tsx src/hooks/analysis/*.ts` sums to exactly 6 (the sixth is the one at the former line 2849, which stays in `Analysis.tsx` — 215-05 does not move it either).
    - The app-wide `react-hooks/exhaustive-deps` plus `react-hooks/refs` count across `.ts`/`.tsx` paths is exactly 28.
    - `npx vitest run src/pages/__tests__/Analysis.test.tsx` reports at least 85 passed, 0 failed.
    - The testid inventory diff against the task-1 baseline is empty.
    - `Analysis`'s recorded statement count and cyclomatic complexity are both strictly lower than after task 2, and all four numbers (before, after task 1, after task 2, after task 3) are in the SUMMARY.
    - The three two-way mutation proofs are recorded with the failing test names for each, plus the restored-green confirmation.
    - `npm run lint && npm run build && npm run knip && npm test -- --run` all green.
    - `git diff -- frontend/eslint.config.js` is empty — no baseline entry deleted in this plan.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/__tests__/Analysis.test.tsx 2>&1 | tail -8</automated>
    <fails_when>non-zero exit, or the summary line reporting any non-zero "failed" count, or a passed count below 85</fails_when>
    <automated>cd frontend && grep -c 'eslint-disable' src/pages/Analysis.tsx src/hooks/analysis/*.ts | awk -F: '{s+=$2} END {print s}'</automated>
    <fails_when>prints any number other than `6`</fails_when>
    <automated>cd frontend && npx eslint --no-inline-config -f json . 2>/dev/null | python3 -c "import json,sys; d=json.load(sys.stdin); print(sum(1 for f in d if f['filePath'].endswith(('.ts','.tsx')) for m in f['messages'] if m['ruleId'] in ('react-hooks/exhaustive-deps','react-hooks/refs')))"</automated>
    <fails_when>prints any number other than `28` — a warning was fixed (out of scope, a behavior change) or introduced</fails_when>
    <automated>cd frontend && diff <(grep -o 'data-testid="[^"]*"' src/pages/Analysis.tsx | sort) /tmp/analysis-testid-before.txt</automated>
    <fails_when>`diff` prints any hunk line or exits non-zero</fails_when>
    <automated>cd frontend && git diff -- eslint.config.js | wc -l</automated>
    <fails_when>prints any number other than `0`</fails_when>
    <automated>cd frontend && npm run lint && npm run build && npm run knip && npm test -- --run 2>&1 | tail -6</automated>
    <fails_when>non-zero exit from any step, `tsc` output containing "error TS", knip naming an unused export, or the vitest summary reporting a non-zero "failed" count</fails_when>
  </verify>
  <done>The six seeding effects live in their own hook with their five suppression comments and byte-identical dependency arrays, the app-wide react-hooks warning count is provably unchanged at 28, and a two-way mutation proof shows each of the plan's three new hooks is genuinely covered by the 85-test oracle.</done>
  <reversibility rating="reversible">One new hook file plus call-site rewiring; reverting the plan's three commits restores the original hook section.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL / query string → page state | `?line=`, `?fen=`, `?orientation=`, `?game_id=`, `?ply=` are attacker-controllable inputs already parsed by `parseAnalysisLineParam`/`parseAnalysisFenParam` and the numeric guards at lines 587-596. This plan moves that parsing into `useAnalysisRouteParams` unchanged — same parsers, same guards, same fallbacks. No new parameter is read and no validation is relaxed. |

Security enforcement is inactive for this project; ASVS level 1 assumed, blocking threshold high.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-215-04-01 | Tampering | `useAnalysisRouteParams` URL parsing | medium | mitigate | A moved parser that drops a guard would let a malformed `?fen=`/`?line=` reach the board and, per Sentry FLAWCHESS-96, take the page down through the ErrorBoundary. Mitigated by moving `parseAnalysisFenParam`/`parseAnalysisLineParam` calls and their numeric guards verbatim, and by the 85-test oracle plus `npm run build`'s narrowing checks. |
| T-215-04-02 | Tampering | `useAnalysisRouteSeeding` effect order and dependency arrays | medium | mitigate | Reordered or re-fired seeding effects change what the board loads. Mitigated by the byte-identical dependency-array rule, the exactly-6 suppression-comment count, the exactly-28 app-wide react-hooks warning invariant, and a two-way mutation proof on the game-mode seeding effect. |
| T-215-04-03 | Repudiation | rendered `data-testid` contract | low | mitigate | The browser-automation contract is asserted by a byte-level inventory diff after every task; accepted risk beyond that is nil because this plan moves no JSX. |
</threat_model>

<verification>
- `npx vitest run src/pages/__tests__/Analysis.test.tsx` reports 85 passed after every task.
- `data-testid` inventory of `Analysis.tsx` byte-identical to the pre-split capture; `data-umami-event` count stays 0.
- The suppression-comment total across `Analysis.tsx` plus `src/hooks/analysis/` is exactly 6.
- App-wide `react-hooks/exhaustive-deps` plus `react-hooks/refs` count is exactly 28.
- `complexity`/`max-depth`/`max-statements` with the CLI override exit 0 across `src/hooks/analysis/`.
- `Analysis`'s complexity and statement counts strictly decrease at each task and are recorded.
- `npm run lint`, `npm run build`, `npm run knip`, `npm test -- --run` all green.
- Three two-way mutation proofs recorded in the SUMMARY.
- `frontend/eslint.config.js` untouched by this plan.
</verification>

<success_criteria>
- Three named hooks exist under `src/hooks/analysis/`, each with a JSDoc header, a typed options object and a named result interface.
- The cyclomatic-16 `qualityBySanWithGem` memo is inside the limit; no new function anywhere breaches the enforced rules.
- `Analysis()`'s complexity and statement counts are measurably lower and recorded, with the remaining gap explicitly assigned to 215-05 and 215-06.
- The 85-test oracle passes, the testid inventory is identical, and the react-hooks warning count is unchanged at 28.
</success_criteria>

## Artifacts this plan produces

**New directory `frontend/src/hooks/analysis/`.**

**New file `frontend/src/hooks/analysis/useAnalysisRouteParams.ts`** — exports
`useAnalysisRouteParams()`, `UseAnalysisRouteParamsOptions`, `UseAnalysisRouteParamsResult`;
owns `lineSans`, `rootFenSeed`, `urlOrientation`, `gameId`, `initialPly`, `isGameMode`,
`initialTactic`, `initialAlignPly`, `autoOrientation`.

**New file `frontend/src/hooks/analysis/useAnalysisEngineLines.ts`** — exports
`useAnalysisEngineLines()`, `UseAnalysisEngineLinesOptions`, `UseAnalysisEngineLinesResult`;
owns `canGoForward`, `playedSan`, `bestSan`, `shownSans`, `rawProbBySan`,
`flawChessDisplayedSans`, `freeRunCommitted`, `unionSans`, `evalLookup`,
`gradedCandidateUcis`, `reconciledTieBreakUci`, `reconciledBestUci`, `reconciledBestSan`,
`reconciledStockfishLine`, `reconciledBestEval`, `reconciledRankedLines`,
`flawChessRankedLinesForVerdict`, `reconciledPvLines`, `qualityBySan`, `qualityBySanWithGem`,
`engineTopLines`, plus one named predicate helper if the complexity reduction takes that route.

**New file `frontend/src/hooks/analysis/useAnalysisRouteSeeding.ts`** — exports
`useAnalysisRouteSeeding()` plus its options interface; owns `seededKey`,
`navigatedInitialPlyKey`, `pasteHandoffConsumed`, `hasAutoFlipped` and the six seeding effects
with their five suppression comments.

**`frontend/src/pages/Analysis.tsx`** — `Analysis` keeps its name and default export; the
former lines 549-1741 collapse to three hook calls plus the state the render tree still reads.

**No new npm script, no eslint config change, no new test file** (unless a mutation proof
exposes an uncovered seam, in which case tests are ADDED to the existing `Analysis.test.tsx`).

<output>
Create `.planning/phases/215-frontend-god-file-decomposition/215-04-SUMMARY.md` when done,
recording `Analysis()`'s complexity/statements/lines at four points (before, after each task),
which of the five originally-flagged arrows remain and which plan owns each, the three two-way
mutation proofs with their failing test names, the `npm run lint:cognitive` before/after counts
for `src/pages/Analysis.tsx` and `src/hooks/analysis/`, the testid inventory verdict, and the
app-wide react-hooks warning count.
</output>
