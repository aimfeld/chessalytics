---
phase: 215-frontend-god-file-decomposition
plan: 05
type: execute
wave: 2
depends_on: ["215-04"]
files_modified:
  - frontend/src/pages/Analysis.tsx
  - frontend/src/hooks/analysis/useAnalysisBoardArrows.ts
  - frontend/src/hooks/analysis/useAnalysisGemMarkers.ts
  - frontend/src/hooks/analysis/useBoardStageSize.ts
autonomous: true
requirements: [SC-1, SC-2, SC-3]
user_setup: []

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SC-1: the three arrows at the former `Analysis.tsx:2321` (cyclomatic 19), `:2390` (`moveListMarkers`, 27 — the file's worst non-component offender) and `:3169` (`boardSquareMarkers`, 18) are all inside cyclomatic complexity 15 in their new homes."
    - "SC-1: after this plan the only remaining cyclomatic breaches in `Analysis.tsx` are `Analysis()` itself and the arrow at the former line 3338 (`playerBar`, 19), both of which 215-06 owns."
    - "SC-2: all 85 tests in `src/pages/__tests__/Analysis.test.tsx` pass unchanged, and the app-wide `react-hooks/exhaustive-deps` plus `react-hooks/refs` count is exactly 28."
    - "The gem/marker resolution produces the same `Map<NodeId, FlawMarkerEntry>` for the same inputs, including markers on OPPONENT plies — `best_move_tier` is position-scoped by design."
    - "SC-3: the `data-testid` inventory of `Analysis.tsx` is byte-identical to the pre-phase capture, `data-umami-event` count stays 0, and no TypeScript suppression comment is added."
    - "Every moved `useMemo`/`useEffect`/`useCallback` keeps its dependency array byte-identical; the suppression-comment total across `Analysis.tsx` plus `src/hooks/analysis/` stays exactly 6."
  artifacts:
    - frontend/src/hooks/analysis/useAnalysisBoardArrows.ts
    - frontend/src/hooks/analysis/useAnalysisGemMarkers.ts
    - frontend/src/hooks/analysis/useBoardStageSize.ts
    - frontend/src/pages/Analysis.tsx
  key_links:
    - "`useAnalysisGemMarkers` -> the `Map<NodeId, FlawMarkerEntry>` consumed by `VariationTree` and the move list"
    - "`useAnalysisBoardArrows` -> the `BoardArrow[]` and `SquareMarker[]` props passed to `ChessBoard`"
    - "`useGemSweep` and the second `useStockfishGradingEngine` call -> passed in as options, never re-instantiated inside an extracted hook"
    - "`useBoardStageSize` -> the board container width/height props and the `ResizeObserver` cleanup"
  prohibitions:
    - statement: "No behavior change: the same markers, arrows and board dimensions are produced for the same inputs."
      status: flagged
      verification: manual
    - statement: "The analysis board INTENTIONALLY shows opponent gems (`best_move_tier` is position-scoped, not user-scoped). A split that filters gem markers to the user's own plies is a regression, not a fix."
      status: flagged
      verification: manual
    - statement: "No extra `useStockfishGradingEngine` or `useGemSweep` instance is created. Both already exist in the component; an extracted hook receives their results as options."
      status: flagged
      verification: manual
    - statement: "No test is deleted or weakened; the test diff for this plan is additions only, and may be empty."
      status: flagged
      verification: manual
    - statement: "None of the 28 pre-existing `react-hooks/*` warnings is fixed; the count must not move in either direction."
      status: flagged
      verification: manual
    - statement: "No render fragment or JSX is moved in this plan — the render split is 215-06's scope."
      status: flagged
      verification: manual
    - statement: "No entry is deleted from the Phase 215 baseline override region; 215-06 owns both `Analysis.tsx` deletions."
      status: flagged
      verification: manual
---

<objective>
Extract the gem/marker resolution, board-arrow and board-sizing clusters of `Analysis()` into
three more sibling hooks, clearing the three worst arrow-level complexity breaches in the file.

Purpose: `moveListMarkers` (cyclomatic 27) is the highest-complexity function in the phase
outside a component body, and the gem-sweep effect beside it is 19. Together with
`boardSquareMarkers` (18) they are three of the six complexity findings in `Analysis.tsx`.
Clearing them here leaves 215-06 with only the render tree and `playerBar` to handle, which is
what makes a three-plan split of this file land inside each executor's context.

Output: `src/hooks/analysis/useAnalysisBoardArrows.ts`, `useAnalysisGemMarkers.ts` and
`useBoardStageSize.ts`; three fewer complexity findings; a recorded before/after statement and
complexity count for `Analysis()`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@frontend/CLAUDE.md
@.planning/phases/215-frontend-god-file-decomposition/215-PATTERNS.md

Read only the sections of
`.planning/phases/215-frontend-god-file-decomposition/215-RESEARCH.md` named per task in
`<read_first>` — the full file is ~925 lines. Also read
`.planning/phases/215-frontend-god-file-decomposition/215-04-SUMMARY.md` first: it records the
current complexity, statement and line numbers for `Analysis()` and which arrows remain.

Do NOT `@`-load `.planning/PROJECT.md`, `.planning/ROADMAP.md` (133 KB) or
`.planning/STATE.md` (312 KB).

**Phase contract (locked, from ROADMAP.md Phase 215):** zero behavior change. Rendered DOM
(every `data-testid`, every `data-umami-event`), hook dependency arrays (including
intentionally missing deps — the suppression comment moves with the code), the engine message
protocol and TanStack Query keys are unchanged. Existing vitest suites are the sole behavior
oracle and pass untouched; tests may be ADDED where a split exposes an untested seam, never
rewritten to fit the refactor. Split seams are the ones CLAUDE.md names — for a React
component, extract data shaping into a `useXyzData` hook — and NOT "split to fit a signature".
Renaming `Analysis` is out of scope. Any "obvious" fix spotted during the split is out of
scope: capture it as a seed, do not implement it.

**Rules of hooks:** every extracted hook is called unconditionally, once per render, in the
same relative position its cluster occupied. Branch INSIDE the hook on an `enabled`-style flag;
never wrap the call site in a condition.

**`noUncheckedIndexedAccess` is on.** The marker and arrow clusters manipulate
`Map<NodeId, ...>` and array structures, so `npm run build` (`tsc -b`) is the step that catches
a narrowing regression a copy-paste split introduces — `npm run lint` and vitest do not
type-check. Narrow explicitly; never add a type suppression.

**Line numbers refer to `Analysis.tsx` at the phase base commit.** 215-04 already shifted them;
re-locate by symbol name, not by line.

**All frontend commands run from `frontend/`.** `npm run lint`, `npm run build`,
`npm run knip`, `npx vitest run <path>`.

**Merge discipline:** this plan depends on 215-04 and must merge after it; 215-06 merges after
this one. All wave-2 plans read `frontend/eslint.config.js`'s Phase 215 baseline region, so
wave 2 merges sequentially rather than running in parallel worktrees.

**Security enforcement is inactive for this project** (`.planning/config.json` has no
`workflow.security_*` keys). The `<threat_model>` block is recorded for continuity with
Phase 214.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Extract useAnalysisBoardArrows end to end, clearing the cyclomatic-18 square-marker memo</name>
  <precondition>Plan 215-04 is merged: `src/hooks/analysis/` exists with `useAnalysisRouteParams.ts`, `useAnalysisEngineLines.ts` and `useAnalysisRouteSeeding.ts`, and `npx vitest run src/pages/__tests__/Analysis.test.tsx` reports 85 passed.</precondition>
  <files>frontend/src/hooks/analysis/useAnalysisBoardArrows.ts, frontend/src/pages/Analysis.tsx</files>
  <read_first>
    - `frontend/src/pages/Analysis.tsx` lines 2576-2740 (`sidelineNodeColors` 2576, `pvSidelineArrows` 2597, `qualityHoverArrows` 2645, `nextMoveArrow` 2669, `engineArrows` 2693) and lines 3169-3315 (`boardSquareMarkers` 3169 — cyclomatic 18, the arrow at 3233, `lastMoveTierColor` 3261)
    - `frontend/src/pages/Analysis.tsx` lines 244-266 (the arrow-width constants `QUALITY_HOVER_ARROW_WIDTH`, `NEXT_MOVE_ARROW_WIDTH`, `ARROW_COUNT`, `FLAWCHESS_ENGINE_ARROW_WIDTH`, `STOCKFISH_ENGINE_ARROW_WIDTH` — they move with the cluster if nothing else reads them, otherwise stay and are imported)
    - `frontend/src/hooks/useLiveMoveFlaw.ts` (whole file, small) — the analog: a `useMemo`-shaped pure transform hook returning a small typed interface with `squareMarkers` and a highlight color, exactly the shape this extraction produces
    - `frontend/src/components/board/ChessBoard` prop types for `BoardArrow` and `SquareMarker` (import site only — do not modify)
    - `215-RESEARCH.md` section "Seam map — `Analysis.tsx`", the "Board-arrow computation" row, and "Behavior-Preservation Hazards / `noUncheckedIndexedAccess`"
    - `215-PATTERNS.md` section "Extracted transform hooks (gem-sweep / board-arrows)"
  </read_first>
  <action>
Capture the before-baselines first, from `frontend/`:
- `npx eslint --no-inline-config --rule 'complexity: ["warn", 15]' --rule 'max-statements: ["warn", 100]' --rule 'max-lines-per-function: ["warn", {"max": 200, "skipBlankLines": true, "skipComments": true}]' src/pages/Analysis.tsx src/hooks/analysis/` — record `Analysis()`'s three numbers and the surviving arrow findings.
- `grep -o 'data-testid="[^"]*"' src/pages/Analysis.tsx | sort > /tmp/analysis-testid-before-05.txt` and confirm it matches 215-04's capture.
- `npx vitest run src/pages/__tests__/Analysis.test.tsx` — expect 85 passed.

Create `frontend/src/hooks/analysis/useAnalysisBoardArrows.ts` holding the board-overlay
derivation: `sidelineNodeColors`, `pvSidelineArrows`, `qualityHoverArrows`, `nextMoveArrow`,
`engineArrows`, `boardSquareMarkers` and `lastMoveTierColor`. One typed
`UseAnalysisBoardArrowsOptions` object in (the reconciled lines from
`useAnalysisEngineLines`, the game overlay, the live flaw, hovered quality moves, node state)
and one named `UseAnalysisBoardArrowsResult` interface out, carrying the `BoardArrow[]` and
`SquareMarker[]` values the chessboard receives as props.

`boardSquareMarkers` (3169, cyclomatic 18) is this cluster's breach. Bring it inside 15 with
CLAUDE.md's own seam — invert a branch with an early `continue`/`return`, or extract one named
helper for the marker-color decision it makes. Not five one-line helpers. The set of squares
marked and their colors must be identical for identical inputs.

Move the arrow-width constants only if this hook becomes their sole reader; otherwise import
them. Two copies of a tuning constant is worse than one import, and CLAUDE.md forbids magic
numbers either way.

Every `useMemo` moves with its dependency array byte-identical. Confirm the suppression-comment
total across `Analysis.tsx` plus `src/hooks/analysis/` is still exactly 6.

Commit this task on its own before expanding.
  </action>
  <acceptance_criteria>
    - `frontend/src/hooks/analysis/useAnalysisBoardArrows.ts` exports `useAnalysisBoardArrows`, `UseAnalysisBoardArrowsOptions` and `UseAnalysisBoardArrowsResult`, and opens with a JSDoc block naming why the hook is separate.
    - `npx eslint --no-inline-config --rule 'complexity: ["error", 15]' src/hooks/analysis/` exits 0 — `boardSquareMarkers` is inside the limit in its new home.
    - `npx eslint --no-inline-config --rule 'complexity: ["warn", 15]' src/pages/Analysis.tsx` no longer reports a finding for the former `boardSquareMarkers`.
    - `npx vitest run src/pages/__tests__/Analysis.test.tsx` reports 85 passed, 0 failed.
    - The testid inventory diff against `/tmp/analysis-testid-before-05.txt` is empty.
    - `grep -c 'eslint-disable' src/pages/Analysis.tsx src/hooks/analysis/*.ts` sums to exactly 6.
    - `npm run lint`, `npm run build` and `npm run knip` are green.
    - The task is committed on its own; `git log -1 --stat` lists only the two files in `<files>`.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/__tests__/Analysis.test.tsx 2>&1 | tail -8</automated>
    <fails_when>non-zero exit, or the summary line does not contain "85 passed", or it reports any non-zero "failed" count</fails_when>
    <automated>cd frontend && npx eslint --no-inline-config --rule 'complexity: ["error", 15]' --rule 'max-depth: ["error", 4]' --rule 'max-statements: ["error", 100]' src/hooks/analysis/</automated>
    <fails_when>non-zero exit, or any output line naming rule `complexity`, `max-depth` or `max-statements`</fails_when>
    <automated>cd frontend && diff <(grep -o 'data-testid="[^"]*"' src/pages/Analysis.tsx | sort) /tmp/analysis-testid-before-05.txt</automated>
    <fails_when>`diff` prints any hunk line or exits non-zero</fails_when>
    <automated>cd frontend && npm run lint && npm run build && npm run knip</automated>
    <fails_when>non-zero exit from any of the three, `tsc` output containing "error TS", or knip naming an export of the new hook as unused</fails_when>
  </verify>
  <done>The board-overlay derivation lives in its own hook, the cyclomatic-18 square-marker memo is inside the limit, and the 85-test oracle plus the testid inventory prove the rendered board is unchanged.</done>
  <reversibility rating="reversible">One new hook file plus call-site rewiring; reverting the commit restores the inline memos.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Extract useAnalysisGemMarkers — the cyclomatic-27 move-list marker memo and the gem-sweep resolution</name>
  <files>frontend/src/hooks/analysis/useAnalysisGemMarkers.ts, frontend/src/pages/Analysis.tsx</files>
  <read_first>
    - `frontend/src/pages/Analysis.tsx` lines 810-890 (`armedGameId` 810, the effect at 811, `sweepArmedForGame` 815, `storedTierByPly` 823, `storedBestGoodByPly` 857, `gameHasStoredBestMoveData` 881) — the stored-tier inputs the marker resolution reads
    - `frontend/src/pages/Analysis.tsx` lines 1782-1830 (`flawMarkerByNodeId` 1782 — cyclomatic 14, cognitive 16, a sonarjs-only finding invisible to the cyclomatic rule; `fastForwardStopPlies` 1820) and lines 2009-2160 (the effect at 2009, the arrow at 2074, `fenAtPly` 2125, `sweepCandidates` 2138, `pinnedEloForPly` 2146)
    - `frontend/src/pages/Analysis.tsx` lines 2240-2576 (the effect at 2242, `resolveMarkerFor` 2262, `parentGemCandidateSans` 2300, the second `useStockfishGradingEngine` call at 2313, the effect at 2321 — cyclomatic 19, `moveListMarkers` 2390 — cyclomatic 27, the arrows at 2442 and 2532)
    - `frontend/src/hooks/useGemSweep.ts` lines 1-40 (the JSDoc header explaining why THAT hook owns its own engine calls — the contrast this extraction must respect: `useAnalysisGemMarkers` does NOT own an engine, it reshapes already-resolved values)
    - `215-RESEARCH.md` section "Seam map — `Analysis.tsx`", the "Gem-sweep / marker resolution" row (`moveListMarkers`'s 27-complexity body is named the highest-value split target after the reconciliation cluster), plus "Assumptions Log" row A2 and "Open Questions" item 2 (expect the reconciliation and gem-sweep clusters to be more tightly coupled than the table suggests — both read `currentNodeId` and both feed `moveListMarkers`)
    - `215-PATTERNS.md` section "Extracted transform hooks (gem-sweep / board-arrows)"
  </read_first>
  <action>
Create `frontend/src/hooks/analysis/useAnalysisGemMarkers.ts` holding the gem/marker cluster:
`storedTierByPly`, `storedBestGoodByPly`, `sweepArmedForGame` and its `armedGameId` effect,
`flawMarkerByNodeId`, `fastForwardStopPlies`, `fenAtPly`, `sweepCandidates`, `pinnedEloForPly`,
`resolveMarkerFor`, `parentGemCandidateSans`, the resolution effects at 2009/2242/2321, and
`moveListMarkers`. One typed `UseAnalysisGemMarkersOptions` object in, one named
`UseAnalysisGemMarkersResult` interface out carrying the `Map<NodeId, FlawMarkerEntry>` the
move list and `VariationTree` consume.

Do NOT instantiate an engine inside the hook. `useGemSweep` and the second
`useStockfishGradingEngine` call (line 2313) already run in the component; pass their results
in as options. A third grading-engine instance would change engine behavior, which is exactly
what this phase forbids. If it turns out a cluster genuinely cannot be separated from the
engine call it drives, keep the call in the component and pass a narrower slice of its result.

Two functions in this cluster breach the enforced limit and one breaches only the cognitive
metric:
- `moveListMarkers` (2390, cyclomatic 27) — the worst offender in the file outside a component
  body. Bring it inside 15 with CLAUDE.md's own seams: invert the loop with early
  `continue`/`return`, or extract named per-tier resolution helpers (`_resolveGemEntry`-shaped),
  one per marker tier rather than one per condition. The resulting `Map` must be identical for
  identical inputs.
- the effect at 2321 (cyclomatic 19) — "when the parent grade completes, run C2 and RESOLVE the
  node". Reduce it by extracting the resolution body into a named function the effect calls;
  do not change when the effect fires or what it depends on.
- `flawMarkerByNodeId` (1782) passes the cyclomatic rule at 14 but is a sonarjs
  cognitive-complexity breach at 16. Record its cognitive number before and after; reducing it
  is desirable, not required, and must never come at the cost of a behavior change.

The analysis board INTENTIONALLY shows opponent gems: `best_move_tier` is position-scoped, not
user-scoped, and only badges, dots and cycling filter to the user via `isUserPly`. A split that
"fixes" the board to show only the user's gems is a regression. If you believe you have found
such a bug, capture it as a seed and leave the behavior alone.

Every `useMemo`/`useEffect`/`useCallback` moves with its dependency array byte-identical.
  </action>
  <acceptance_criteria>
    - `frontend/src/hooks/analysis/useAnalysisGemMarkers.ts` exports `useAnalysisGemMarkers`, `UseAnalysisGemMarkersOptions` and `UseAnalysisGemMarkersResult`.
    - `grep -c 'useStockfishGradingEngine\|useGemSweep' src/hooks/analysis/useAnalysisGemMarkers.ts` prints `0` — no engine is instantiated inside the extracted hook.
    - `npx eslint --no-inline-config --rule 'complexity: ["error", 15]' src/hooks/analysis/` exits 0 — both the 27 and the 19 are inside the limit in their new home.
    - `npx eslint --no-inline-config --rule 'complexity: ["warn", 15]' src/pages/Analysis.tsx` reports findings only for `Analysis` itself and the arrow at the former line 3338; the findings for the former 2321 and 2390 are gone.
    - `npx vitest run src/pages/__tests__/Analysis.test.tsx` reports 85 passed.
    - The testid inventory diff against `/tmp/analysis-testid-before-05.txt` is empty.
    - `npm run build` exits 0 and the diff adds no TypeScript suppression comment.
    - The SUMMARY records `flawMarkerByNodeId`'s cognitive-complexity number before and after.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/__tests__/Analysis.test.tsx 2>&1 | tail -8</automated>
    <fails_when>non-zero exit, or the summary line does not contain "85 passed"</fails_when>
    <automated>cd frontend && npx eslint --no-inline-config --rule 'complexity: ["error", 15]' --rule 'max-depth: ["error", 4]' --rule 'max-statements: ["error", 100]' src/hooks/analysis/</automated>
    <fails_when>non-zero exit, or any output line naming rule `complexity`, `max-depth` or `max-statements`</fails_when>
    <automated>cd frontend && npx eslint --no-inline-config --rule 'complexity: ["warn", 15]' src/pages/Analysis.tsx 2>&1 | grep -c 'complexity'</automated>
    <fails_when>prints a number greater than `2` — only `Analysis` itself and the former `playerBar` arrow may still be flagged</fails_when>
    <automated>cd frontend && grep -c 'useStockfishGradingEngine\|useGemSweep' src/hooks/analysis/useAnalysisGemMarkers.ts</automated>
    <fails_when>prints any number other than `0`</fails_when>
    <automated>cd frontend && diff <(grep -o 'data-testid="[^"]*"' src/pages/Analysis.tsx | sort) /tmp/analysis-testid-before-05.txt</automated>
    <fails_when>`diff` prints any hunk line or exits non-zero</fails_when>
    <automated>cd frontend && npm run lint && npm run build</automated>
    <fails_when>non-zero exit from either, or `tsc` output containing "error TS"</fails_when>
  </verify>
  <done>The gem/marker resolution lives in its own hook with both of its enforced-rule breaches cleared, no extra engine instance was created, and the rendered markers are unchanged.</done>
  <reversibility rating="reversible">One new hook file plus call-site rewiring; reverting the commit restores the inline cluster.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Extract useBoardStageSize, then measure and mutation-prove the plan's three hooks</name>
  <files>frontend/src/hooks/analysis/useBoardStageSize.ts, frontend/src/pages/Analysis.tsx</files>
  <read_first>
    - `frontend/src/pages/Analysis.tsx` lines 3041-3168 (`boardStageRef` 3041, the resize effect at 3049 with its arrow at 3052, and the `boardWidth`/`boardStageHeight` state it maintains)
    - `frontend/src/pages/Analysis.tsx` lines 172-243 (the board-sizing constants `MOBILE_BREAKPOINT_PX`, `BOARD_EVAL_BARS_ALLOWANCE_PX`, `DESKTOP_BOARD_SIZE_REDUCTION_PX`, `MOBILE_BOARD_MAX_WIDTH`, `MOBILE_BOARD_BLOCK_MAX_PX`, `BOARD_WIDTH_LOCK_MIN_PX`, `BOARD_HEIGHT_LOCK_MIN_PX`, `SIDE_COLUMN_WIDTH_PX`, `DESKTOP_GRID_GAP_PX`, `EVAL_SLIDER_SLACK_PX`, `DESKTOP_GRID_MAX_WIDTH_PX`) — several are also read by the render tree 215-06 owns, so check every reader before moving one
    - `frontend/src/pages/Analysis.tsx` lines 292-312 (`useAnalysisLayoutMode`, the in-file precedent: lazy `useState` initializer plus one effect with matching listener cleanup)
    - `frontend/src/hooks/useFitBoardToViewport.ts` and `frontend/src/hooks/useMiniBoardSize.ts` (existing board-sizing hooks — read them to confirm this extraction is not duplicating one of them; if either already does the job, say so in the SUMMARY and skip the new file rather than adding a third)
    - `frontend/src/pages/__tests__/Analysis.test.tsx` lines 330-340 and 510-540 (the `matchMedia` definitions the layout-mode and sizing paths rely on)
  </read_first>
  <action>
1. Read `useFitBoardToViewport.ts` and `useMiniBoardSize.ts` FIRST. If either already provides
   the `ResizeObserver`-driven container measurement this cluster needs, do not add a third
   hook: record that finding in the SUMMARY and leave the cluster inline. Only create
   `frontend/src/hooks/analysis/useBoardStageSize.ts` if it is genuinely new.

2. If new: it owns `boardStageRef`, the `boardWidth`/`boardStageHeight` state and the resize
   effect at 3049, returning the ref and both dimensions. Keep the effect's cleanup exactly as
   it is (a leaked `ResizeObserver` or listener is a real regression a passing test will not
   catch). Move a sizing constant only if this hook becomes its sole reader; grep every
   constant's readers first, because the render tree 215-06 owns reads several of them.

3. Measure and record for the SUMMARY:
   - `npx eslint --no-inline-config --rule 'complexity: ["warn", 15]' --rule 'max-statements: ["warn", 100]' --rule 'max-lines-per-function: ["warn", {"max": 200, "skipBlankLines": true, "skipComments": true}]' src/pages/Analysis.tsx src/hooks/analysis/` — `Analysis()`'s three numbers after this plan against the 176 / 213 / 2037 phase baseline and against 215-04's after-numbers, plus confirmation that the only surviving complexity findings are `Analysis` itself and the former `playerBar` arrow.
   - The app-wide `react-hooks/exhaustive-deps` plus `react-hooks/refs` count: exactly 28.
   - `npm run lint:cognitive` filtered to `src/pages/Analysis.tsx` and `src/hooks/analysis/`, against the phase baseline of 4 breaches in `Analysis.tsx` (lines 549, 1640, 1782, 2390).

4. Two-way mutation proof, one function per new hook module. For each of
   `useAnalysisBoardArrows` (mutate `boardSquareMarkers` to return an empty array),
   `useAnalysisGemMarkers` (mutate `moveListMarkers` to return an empty `Map`) and
   `useBoardStageSize` (mutate the measured width to a constant — skip this one if step 1
   concluded the hook is not needed): apply the mutation, run
   `npx vitest run src/pages/__tests__/Analysis.test.tsx`, confirm it FAILS and record which
   tests failed; restore and confirm 85 passed. Record every outcome in the SUMMARY. If a
   mutation leaves the suite GREEN, that seam is unguarded: ADD a test to the existing
   `Analysis.test.tsx` (never a parallel file, never a rewrite of an existing test), re-run the
   mutation and record the new result.
  </action>
  <acceptance_criteria>
    - Either `frontend/src/hooks/analysis/useBoardStageSize.ts` exists and exports `useBoardStageSize` plus its result interface, OR the SUMMARY records which existing hook already covers the cluster and why no new file was added.
    - `npx eslint --no-inline-config --rule 'complexity: ["warn", 15]' src/pages/Analysis.tsx` reports at most 2 findings: `Analysis` itself and the arrow at the former line 3338.
    - `npx eslint --no-inline-config --rule 'complexity: ["error", 15]' --rule 'max-depth: ["error", 4]' --rule 'max-statements: ["error", 100]' src/hooks/analysis/` exits 0.
    - The app-wide `react-hooks/exhaustive-deps` plus `react-hooks/refs` count across `.ts`/`.tsx` paths is exactly 28.
    - `npx vitest run src/pages/__tests__/Analysis.test.tsx` reports at least 85 passed, 0 failed.
    - The testid inventory diff against `/tmp/analysis-testid-before-05.txt` is empty and `grep -c 'data-umami-event' src/pages/Analysis.tsx` prints `0`.
    - Every mutation proof is recorded with its failing test names plus the restored-green confirmation.
    - `git diff -- frontend/eslint.config.js` is empty — no baseline entry deleted in this plan.
    - `npm run lint && npm run build && npm run knip && npm test -- --run` all green.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx eslint --no-inline-config --rule 'complexity: ["warn", 15]' src/pages/Analysis.tsx 2>&1 | grep -c 'complexity'</automated>
    <fails_when>prints a number greater than `2`</fails_when>
    <automated>cd frontend && npx eslint --no-inline-config --rule 'complexity: ["error", 15]' --rule 'max-depth: ["error", 4]' --rule 'max-statements: ["error", 100]' src/hooks/analysis/</automated>
    <fails_when>non-zero exit, or any output line naming rule `complexity`, `max-depth` or `max-statements`</fails_when>
    <automated>cd frontend && npx eslint --no-inline-config -f json . 2>/dev/null | python3 -c "import json,sys; d=json.load(sys.stdin); print(sum(1 for f in d if f['filePath'].endswith(('.ts','.tsx')) for m in f['messages'] if m['ruleId'] in ('react-hooks/exhaustive-deps','react-hooks/refs')))"</automated>
    <fails_when>prints any number other than `28`</fails_when>
    <automated>cd frontend && diff <(grep -o 'data-testid="[^"]*"' src/pages/Analysis.tsx | sort) /tmp/analysis-testid-before-05.txt && grep -c 'data-umami-event' src/pages/Analysis.tsx</automated>
    <fails_when>`diff` prints any hunk line, or the grep count prints any number other than `0`</fails_when>
    <automated>cd frontend && git diff -- eslint.config.js | wc -l</automated>
    <fails_when>prints any number other than `0`</fails_when>
    <automated>cd frontend && npm run lint && npm run build && npm run knip && npm test -- --run 2>&1 | tail -6</automated>
    <fails_when>non-zero exit from any step, `tsc` output containing "error TS", knip naming an unused export, or the vitest summary reporting a non-zero "failed" count</fails_when>
  </verify>
  <done>Every arrow-level complexity breach in `Analysis.tsx` is cleared, only the component body and one render helper remain for 215-06, and a two-way mutation proof shows each new hook is genuinely covered by the 85-test oracle.</done>
  <reversibility rating="reversible">One new hook file (or none) plus call-site rewiring; reverting the plan's three commits restores the inline clusters.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Code moved, no trust boundary changes. This plan reshapes already-fetched TanStack Query data and already-computed engine results into markers and arrows; no network call, endpoint, schema, auth path or user-input surface is created, moved or removed. |

Security enforcement is inactive for this project; ASVS level 1 assumed, blocking threshold high.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-215-05-01 | Tampering | `moveListMarkers` complexity reduction | medium | mitigate | Reducing a cyclomatic-27 body is the most likely place in the phase for a silent logic change, and the 85 tests assert only through rendered DOM. Mitigated by the byte-identical dependency-array rule, the testid inventory diff, and a two-way mutation proof on this exact function. |
| T-215-05-02 | Tampering | accidental user-scoping of gem markers | medium | mitigate | A well-meaning "fix" that filters opponent gems out of the board would be a regression. Mitigated by an explicit prohibition, an explicit note in the action, and the HUMAN-UAT smoke in 215-06 which checks the analysis board in library-game mode. |
| T-215-05-03 | Denial of Service | `ResizeObserver` cleanup in the extracted sizing hook | low | mitigate | A leaked observer degrades the tab over time and no passing test would catch it. Mitigated by the explicit instruction to move the cleanup unchanged and by reading `useAnalysisLayoutMode` as the listener-cleanup precedent. |
</threat_model>

<verification>
- `npx vitest run src/pages/__tests__/Analysis.test.tsx` reports 85 passed after every task.
- At most two `complexity` findings remain in `Analysis.tsx`: `Analysis` itself and the former `playerBar` arrow.
- `complexity`/`max-depth`/`max-statements` with the CLI override exit 0 across `src/hooks/analysis/`.
- `data-testid` inventory byte-identical; `data-umami-event` count 0.
- App-wide `react-hooks/*` warning count exactly 28.
- Suppression-comment total across `Analysis.tsx` plus `src/hooks/analysis/` exactly 6.
- `npm run lint`, `npm run build`, `npm run knip`, `npm test -- --run` all green.
- Mutation proofs recorded in the SUMMARY.
- `frontend/eslint.config.js` untouched by this plan.
</verification>

<success_criteria>
- Three more named hooks (or two, if the sizing cluster is already covered by an existing hook) exist under `src/hooks/analysis/`, each with a JSDoc header, a typed options object and a named result interface.
- The cyclomatic 27, 19 and 18 findings are cleared; no new function anywhere breaches the enforced rules.
- `Analysis()`'s complexity and statement counts are measurably lower and recorded, with the remaining gap explicitly assigned to 215-06.
- The 85-test oracle passes, the testid inventory is identical, and the react-hooks warning count is unchanged at 28.
</success_criteria>

## Artifacts this plan produces

**New file `frontend/src/hooks/analysis/useAnalysisBoardArrows.ts`** — exports
`useAnalysisBoardArrows()`, `UseAnalysisBoardArrowsOptions`, `UseAnalysisBoardArrowsResult`;
owns `sidelineNodeColors`, `pvSidelineArrows`, `qualityHoverArrows`, `nextMoveArrow`,
`engineArrows`, `boardSquareMarkers`, `lastMoveTierColor`, plus one named marker-color helper if
the complexity reduction takes that route.

**New file `frontend/src/hooks/analysis/useAnalysisGemMarkers.ts`** — exports
`useAnalysisGemMarkers()`, `UseAnalysisGemMarkersOptions`, `UseAnalysisGemMarkersResult`; owns
`storedTierByPly`, `storedBestGoodByPly`, `armedGameId`, `sweepArmedForGame`,
`flawMarkerByNodeId`, `fastForwardStopPlies`, `fenAtPly`, `sweepCandidates`, `pinnedEloForPly`,
`resolveMarkerFor`, `parentGemCandidateSans`, `moveListMarkers`, plus per-tier resolution
helpers if the complexity reduction takes that route.

**Conditionally new file `frontend/src/hooks/analysis/useBoardStageSize.ts`** — created only if
no existing hook already covers the cluster; would export `useBoardStageSize()` plus its result
interface, owning `boardStageRef`, `boardWidth`, `boardStageHeight` and the resize effect.

**`frontend/src/pages/Analysis.tsx`** — `Analysis` keeps its name and default export; three
more clusters collapse to hook calls.

**No new npm script, no eslint config change, no new test file** (unless a mutation proof
exposes an uncovered seam, in which case tests are ADDED to the existing `Analysis.test.tsx`).

<output>
Create `.planning/phases/215-frontend-god-file-decomposition/215-05-SUMMARY.md` when done,
recording `Analysis()`'s complexity/statements/lines before and after this plan, the surviving
complexity findings by line and which plan owns each, the mutation proofs with their failing
test names, `flawMarkerByNodeId`'s cognitive-complexity number before and after, the
`npm run lint:cognitive` counts for `src/pages/Analysis.tsx` and `src/hooks/analysis/`, the
testid inventory verdict, the app-wide react-hooks warning count, and whether
`useBoardStageSize.ts` was needed.
</output>

