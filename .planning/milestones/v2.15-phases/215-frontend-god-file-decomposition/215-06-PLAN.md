---
phase: 215-frontend-god-file-decomposition
plan: 06
type: execute
wave: 2
depends_on: ["215-05"]
files_modified:
  - frontend/src/pages/Analysis.tsx
  - frontend/src/components/analysis/AnalysisPlayerBar.tsx
  - frontend/src/components/analysis/AnalysisTabs.tsx
  - frontend/src/components/analysis/AnalysisBoardStage.tsx
  - frontend/src/components/analysis/AnalysisDesktopCards.tsx
  - frontend/eslint.config.js
autonomous: false
requirements: [SC-0, SC-1, SC-2, SC-3, SC-4]
user_setup: []

estimate:
  tokens: 105000
  raw_tokens: 105000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "SC-0: `src/pages/Analysis.tsx` no longer appears in either the `complexity` or the `max-statements` block of the Phase 215 baseline override region in `frontend/eslint.config.js`, and `npm run lint` still exits 0."
    - "SC-1: `Analysis()` is inside cyclomatic complexity 15 and 100 statements, down from 176 and 213, and the arrow at the former line 3338 (`playerBar`, 19) is inside 15 in its new home — so the whole file passes the enforced rules with the baseline defeated on the command line."
    - "SC-2: all 85 tests in `src/pages/__tests__/Analysis.test.tsx` pass unchanged, and the app-wide `react-hooks/exhaustive-deps` plus `react-hooks/refs` count is exactly 28."
    - "SC-3: the UNION `data-testid` inventory across `src/pages/Analysis.tsx` and `src/components/analysis/**` is byte-identical before and after the render split, and the `data-umami-event` inventory likewise."
    - "SC-4: the HUMAN-UAT smoke on Analysis in library-game, paste and tactic modes, at desktop and mobile widths, is recorded with its outcome."
    - "The default export `Analysis` keeps its name and remains the default export of `@/pages/Analysis`; no new route, prop contract or query key is introduced."
  artifacts:
    - frontend/src/components/analysis/AnalysisPlayerBar.tsx
    - frontend/src/components/analysis/AnalysisTabs.tsx
    - frontend/src/components/analysis/AnalysisBoardStage.tsx
    - frontend/src/components/analysis/AnalysisDesktopCards.tsx
    - frontend/src/pages/Analysis.tsx
    - frontend/eslint.config.js
  key_links:
    - "the JSX fragment consts inside `Analysis()` -> the `&&`/`?:` operators that make up most of its cyclomatic 176; moving them into components is what lands SC-1"
    - "`src/components/analysis/**` -> the existing `react-refresh/only-export-components: 'off'` override block, so a new file there needs no new override"
    - "`Analysis.tsx`'s two entries in the Phase 215 baseline region -> deleted by this plan, closing SC-0 for this file"
    - "the union testid inventory -> the browser-automation contract in frontend/CLAUDE.md"
  prohibitions:
    - statement: "No behavior change: the rendered DOM is identical in every layout mode, including which elements exist in the tree while visually hidden."
      status: flagged
      verification: manual
    - statement: "No `data-testid` or `data-umami-event` value is renamed, merged or dropped. Desktop and mobile variants that carry different testids keep BOTH strings; they are an intentional browser-automation contract, not accidental duplication."
      status: flagged
      verification: manual
    - statement: "A JSX fragment const is NOT converted into a function called from the same place merely to move its operators out of the component's own complexity count. Extract a real component with a typed props interface, or leave it."
      status: flagged
      verification: manual
    - statement: "No test is deleted or weakened; the test diff for this plan is additions only, and may be empty."
      status: flagged
      verification: manual
    - statement: "None of the 28 pre-existing `react-hooks/*` warnings is fixed; the count must not move in either direction."
      status: flagged
      verification: manual
    - statement: "The analysis board INTENTIONALLY shows opponent gems (`best_move_tier` is position-scoped). A render change that hides them is a regression, and the HUMAN-UAT smoke checks for it explicitly."
      status: flagged
      verification: manual
    - statement: "Only `src/pages/Analysis.tsx`'s two entries leave the baseline override region. Every other path in it stays, including `src/pages/Openings.tsx`, which 215-07 owns."
      status: flagged
      verification: manual
---

<objective>
Split `Analysis()`'s render tree into four sibling components so the component body finally
fits cyclomatic complexity 15 and 100 statements, then delete both of its baseline override
entries and smoke the page by hand.

Purpose: the first two Analysis plans took the hook and data section; what remains is where
most of the cyclomatic 176 actually lives. Roughly 25 render-fragment declarations sit between
the hooks and the 122-line JSX return, and the ones that are plain `const x = (<JSX/>)` rather
than arrow functions contribute every `&&` and `? :` inside them directly to `Analysis()`'s own
complexity count. Extracting them into real components with typed props is what closes SC-0 and
SC-1 for this file. This plan is also the first of the two that changes a render tree, so it
carries the phase's first HUMAN-UAT checkpoint.

Output: `AnalysisPlayerBar.tsx`, `AnalysisTabs.tsx`, `AnalysisBoardStage.tsx` and
`AnalysisDesktopCards.tsx` under `src/components/analysis/`; both `Analysis.tsx` entries gone
from the eslint baseline region with `npm run lint` still green; a recorded desktop and mobile
smoke.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@frontend/CLAUDE.md
@.planning/phases/215-frontend-god-file-decomposition/215-PATTERNS.md

Read only the sections of
`.planning/phases/215-frontend-god-file-decomposition/215-RESEARCH.md` named per task in
`<read_first>` — the full file is ~925 lines. Also read
`.planning/phases/215-frontend-god-file-decomposition/215-05-SUMMARY.md` first: it records
`Analysis()`'s current complexity, statement and line numbers and which findings survive.

Do NOT `@`-load `.planning/PROJECT.md`, `.planning/ROADMAP.md` (133 KB) or
`.planning/STATE.md` (312 KB).

**Phase contract (locked, from ROADMAP.md Phase 215):** zero behavior change. Rendered DOM
(every `data-testid`, every `data-umami-event`), hook dependency arrays, the engine message
protocol and TanStack Query keys are unchanged. Existing vitest suites are the sole behavior
oracle and pass untouched; tests may be ADDED where a split exposes an untested seam, never
rewritten to fit the refactor. Split seams are the ones CLAUDE.md names — split desktop/mobile
renderers past ~40 LOC of logic each — and NOT "split to fit a signature": no context object
with fewer than three fields and one reader, no handler bundle of unrelated callbacks.
Renaming `Analysis` is out of scope. Any "obvious" fix spotted during the split is out of
scope: capture it as a seed, do not implement it.

**`src/components/analysis/**` already carries a `react-refresh/only-export-components: 'off'`
override** (added in Phase 139 because the overlay files co-export arrow builders alongside
components). A new file there therefore needs no new override block. A new file anywhere else
would; keep every new component in that directory.

**`noUncheckedIndexedAccess` is on.** `npm run lint` and vitest do NOT type-check;
`npm run build` (`tsc -b`) is the only step that catches a narrowing regression a prop-threading
split introduces. Narrow explicitly; never add a type suppression.

**Line numbers refer to `Analysis.tsx` at the phase base commit.** 215-04 and 215-05 already
shifted them; re-locate by symbol name, not by line.

**All frontend commands run from `frontend/`.** `npm run lint`, `npm run build`,
`npm run knip`, `npx vitest run <path>`.

**Merge discipline:** this plan depends on 215-05 and merges after it. It is the only Analysis
plan that edits `frontend/eslint.config.js`; re-read that file immediately before the edit,
because a sibling wave-2 plan may already have removed its own path. All wave-2 plans merge
sequentially rather than running in parallel worktrees.

**Security enforcement is inactive for this project** (`.planning/config.json` has no
`workflow.security_*` keys). The `<threat_model>` block is recorded for continuity with
Phase 214.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Extract AnalysisPlayerBar end to end, clearing the cyclomatic-19 player-bar helper</name>
  <precondition>Plan 215-05 is merged: `npx eslint --no-inline-config --rule 'complexity: ["warn", 15]' src/pages/Analysis.tsx` reports at most 2 findings — `Analysis` itself and the arrow at the former line 3338.</precondition>
  <files>frontend/src/components/analysis/AnalysisPlayerBar.tsx, frontend/src/pages/Analysis.tsx</files>
  <read_first>
    - `frontend/src/pages/Analysis.tsx` lines 3316-3414 (`boardRow` 3316, `showPlayerBars` 3331, `playerBar` 3338 — an arrow taking `(color, rowPosition)` and cyclomatic 19, `evalBarCap` 3374, `evalBarSlot` 3384, `boardHeaderRow` 3391, `boardFooterRow` 3404)
    - `frontend/src/pages/Analysis.tsx` lines 4248-4370 (the JSX return — see exactly how `boardHeaderRow(playerBar(...))` and `boardFooterRow(...)` are invoked, and with which arguments)
    - `frontend/src/components/analysis/EvalBar.tsx` and one other existing file in `src/components/analysis/` (for the local props-interface and import conventions)
    - `frontend/src/pages/openings/StatsTab.tsx` lines 1-38 (the `type XxxProps = { ... }` convention: every piece of derived state and every `onXxx` callback listed explicitly, no default export of a non-component value)
    - `215-RESEARCH.md` section "Seam map — `Analysis.tsx`", the "`playerBar` render helper" row (it is called twice, top and bottom, which is exactly why it exists as a helper) and "Behavior-Preservation Hazards / `react-refresh/only-export-components`"
    - `frontend/CLAUDE.md` "Browser Automation Rules" (the testid naming convention and why every value must survive)
  </read_first>
  <action>
Capture the before-baselines first, from `frontend/`:
- The UNION testid and umami inventories across the page and the components directory:
  `grep -oh 'data-testid="[^"]*"' src/pages/Analysis.tsx src/components/analysis/*.tsx | sort > /tmp/analysis-testid-union-before.txt`
  `grep -oh 'data-umami-event="[^"]*"' src/pages/Analysis.tsx src/components/analysis/*.tsx | sort > /tmp/analysis-umami-union-before.txt`
  `Analysis.tsx` alone held 24 testids and 0 umami attributes at the phase base commit; the
  union adds whatever the existing components already carry. Every later check in this plan
  compares against the UNION, because markup is moving between these files.
- `npx eslint --no-inline-config --rule 'complexity: ["warn", 15]' --rule 'max-statements: ["warn", 100]' src/pages/Analysis.tsx` — record `Analysis()`'s two numbers.
- `npx vitest run src/pages/__tests__/Analysis.test.tsx` — expect 85 passed.

Create `frontend/src/components/analysis/AnalysisPlayerBar.tsx` exporting `PlayerBar` (from the
`playerBar` arrow, cyclomatic 19), plus `EvalBarCap`, `EvalBarSlot`, `BoardHeaderRow` and
`BoardFooterRow` as real components with `type XxxProps = { ... }` interfaces. In the page,
`boardHeaderRow(playerBar('white', 'top'))` becomes `<BoardHeaderRow player={<PlayerBar color="white" rowPosition="top" … />} …/>`
or the equivalent composition — the rendered element order and every attribute must stay the
same.

Bring `PlayerBar` inside cyclomatic 15 with CLAUDE.md's own seam: it branches on game mode
versus pasted headers versus clocks, so extract a small named sub-component or helper per
source of the displayed name/rating/clock, not one helper per condition.

Do NOT convert a JSX fragment const into a function purely to move its operators out of
`Analysis()`'s complexity count. That is metric-gaming, not a split: `<PlayerBar>` must be a
component with a typed props interface that a reader can understand on its own.

Move every `data-testid` and `data-umami-event` verbatim. If desktop and mobile variants carry
different testid strings, both strings survive; collapsing them changes the DOM contract.

Commit this task on its own before expanding.
  </action>
  <acceptance_criteria>
    - `frontend/src/components/analysis/AnalysisPlayerBar.tsx` exports `PlayerBar`, `EvalBarCap`, `EvalBarSlot`, `BoardHeaderRow` and `BoardFooterRow`, each with a `type XxxProps` interface, and exports no non-component value other than a type.
    - `npx eslint --no-inline-config --rule 'complexity: ["error", 15]' src/components/analysis/AnalysisPlayerBar.tsx` exits 0.
    - `diff <(grep -oh 'data-testid="[^"]*"' src/pages/Analysis.tsx src/components/analysis/*.tsx | sort) /tmp/analysis-testid-union-before.txt` produces no output; the same for the umami inventory.
    - `npx vitest run src/pages/__tests__/Analysis.test.tsx` reports 85 passed, 0 failed.
    - `npm run lint`, `npm run build` and `npm run knip` are green, and lint reports no `react-refresh/only-export-components` finding for the new file.
    - `Analysis()`'s complexity is strictly lower than the number recorded in 215-05's SUMMARY.
    - The task is committed on its own; `git log -1 --stat` lists only the two files in `<files>`.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/__tests__/Analysis.test.tsx 2>&1 | tail -8</automated>
    <fails_when>non-zero exit, or the summary line does not contain "85 passed", or it reports any non-zero "failed" count</fails_when>
    <automated>cd frontend && diff <(grep -oh 'data-testid="[^"]*"' src/pages/Analysis.tsx src/components/analysis/*.tsx | sort) /tmp/analysis-testid-union-before.txt && diff <(grep -oh 'data-umami-event="[^"]*"' src/pages/Analysis.tsx src/components/analysis/*.tsx | sort) /tmp/analysis-umami-union-before.txt</automated>
    <fails_when>either `diff` prints a hunk line or exits non-zero — an attribute value appeared, vanished or was renamed</fails_when>
    <automated>cd frontend && npx eslint --no-inline-config --rule 'complexity: ["error", 15]' src/components/analysis/AnalysisPlayerBar.tsx</automated>
    <fails_when>non-zero exit, or any output line naming rule `complexity`</fails_when>
    <automated>cd frontend && npm run lint && npm run build && npm run knip</automated>
    <fails_when>non-zero exit from any of the three, `tsc` output containing "error TS", knip naming an export of the new file as unused, or lint reporting `react-refresh/only-export-components`</fails_when>
  </verify>
  <done>The player-bar family is a set of real components with typed props, the cyclomatic-19 helper is inside the limit, and the union testid inventory proves the rendered DOM is byte-identical — so the render-extraction method is proven before the larger fragments move.</done>
  <reversibility rating="reversible">One new component file plus call-site rewiring; reverting the commit restores the inline helpers.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Extract AnalysisTabs — the engine, eval, moves and stats tab fragments</name>
  <files>frontend/src/components/analysis/AnalysisTabs.tsx, frontend/src/pages/Analysis.tsx</files>
  <read_first>
    - `frontend/src/pages/Analysis.tsx` lines 3414-3578 (`variationTree` 3414, `boardControls` 3443, `evalChart` 3474, `tagsPanel` 3526) and lines 3662-4000 (`eloSelector` 3662, `renderFlawChessCard` 3682, `humanTab` 3769, `flawChessTab` 3801, `mobileEngineLines` 3810, `evalTab` 3839, `moveListHeaderContent` 3858, `moveListKey` 3893, `movesTab` 3894, `statsTab` 3919, `analysisTabs` 3944)
    - `frontend/src/pages/Analysis.tsx` lines 326-397 (`EngineToggleHeader` and `FlawChessInfoTooltip` — two components ALREADY defined at module level in this file; they are the in-file precedent for what a fragment becomes, and they may move to the new file with the tabs if nothing else reads them)
    - `frontend/src/components/analysis/EngineLines.tsx`, `FlawChessEngineLines.tsx`, `MaiaHumanPanel.tsx`, `AnalysisTagsPanel.tsx`, `EloSelector.tsx` (the components these fragments already compose — read their prop types, do not modify them)
    - `frontend/src/pages/openings/StatsTab.tsx` lines 1-38 (the props-interface convention)
    - `215-RESEARCH.md` section "Seam map — `Analysis.tsx`" (the render rows) and `CLAUDE.md` lines 156-163 ("split desktop/mobile renderers past ~40 LOC of logic each"; "Don't split just to fit a signature")
  </read_first>
  <action>
Create `frontend/src/components/analysis/AnalysisTabs.tsx` and move the tab-panel family into
it as real components with `type XxxProps` interfaces: `HumanTab`, `FlawChessTab`,
`MobileEngineLines`, `EvalTab`, `MoveListHeaderContent`, `MovesTab`, `StatsTab`, the
`AnalysisTabs` container, plus the shared fragments they compose — `VariationTreePanel` (from
`variationTree`), `BoardControls` (from `boardControls`), `EvalChartPanel` (from `evalChart`),
`TagsPanel` (from `tagsPanel`), `EloSelectorPanel` (from `eloSelector`) and `FlawChessCard`
(from `renderFlawChessCard`).

If a fragment is read by both this file and the board-stage or cards file that task 3 creates,
put it here and import it there rather than duplicating it. Duplicated markup is exactly what
`frontend/CLAUDE.md`'s "always apply changes to mobile too — search for duplicated markup" rule
exists to prevent, and a split that copies rather than shares makes the page worse.

`moveListKey` (3893) is a plain string derivation, not markup — it stays in the page and is
passed in as a prop, unless every reader moves with the tabs.

Prop interfaces here will be large (a tab panel legitimately needs a dozen derived values and
several callbacks). That is not the "context object with fewer than three fields and one
reader" anti-pattern CLAUDE.md forbids: these are component props with one clear reader each
and a name that says what the component renders. Do NOT bundle unrelated callbacks into a
single `handlers` object to shorten a signature — that IS the forbidden shape.

`EngineToggleHeader` (326) and `FlawChessInfoTooltip` (369) are already module-level components
in `Analysis.tsx`. Move them into the new file only if the tabs become their sole reader;
otherwise leave them where they are.

Every `data-testid` and `data-umami-event` moves verbatim; re-run the union inventory diff after
the move.
  </action>
  <acceptance_criteria>
    - `frontend/src/components/analysis/AnalysisTabs.tsx` exports the tab and shared-fragment components named in the action, each with a `type XxxProps` interface, and exports no non-component value other than a type.
    - `Analysis.tsx` no longer declares `humanTab`, `flawChessTab`, `mobileEngineLines`, `evalTab`, `moveListHeaderContent`, `movesTab`, `statsTab`, `analysisTabs`, `variationTree`, `boardControls`, `evalChart`, `tagsPanel`, `eloSelector` or `renderFlawChessCard`.
    - Both union inventory diffs (testid and umami) against the task-1 captures produce no output.
    - `npx vitest run src/pages/__tests__/Analysis.test.tsx` reports 85 passed.
    - `npx eslint --no-inline-config --rule 'complexity: ["error", 15]' --rule 'max-depth: ["error", 4]' --rule 'max-statements: ["error", 100]' src/components/analysis/AnalysisTabs.tsx` exits 0.
    - `Analysis()`'s complexity and statement counts are both strictly lower than after task 1 (record both).
    - `npm run build` exits 0 and the diff adds no TypeScript suppression comment.
    - No markup fragment exists in two files; `grep -c 'data-testid="board-btn-flip"' src/components/analysis/*.tsx src/pages/Analysis.tsx` (or any other testid you moved) shows it in exactly one file.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/__tests__/Analysis.test.tsx 2>&1 | tail -8</automated>
    <fails_when>non-zero exit, or the summary line does not contain "85 passed"</fails_when>
    <automated>cd frontend && diff <(grep -oh 'data-testid="[^"]*"' src/pages/Analysis.tsx src/components/analysis/*.tsx | sort) /tmp/analysis-testid-union-before.txt && diff <(grep -oh 'data-umami-event="[^"]*"' src/pages/Analysis.tsx src/components/analysis/*.tsx | sort) /tmp/analysis-umami-union-before.txt</automated>
    <fails_when>either `diff` prints a hunk line or exits non-zero — note that a DUPLICATED fragment shows up here as an added line, so this also catches copy-instead-of-share</fails_when>
    <automated>cd frontend && npx eslint --no-inline-config --rule 'complexity: ["error", 15]' --rule 'max-depth: ["error", 4]' --rule 'max-statements: ["error", 100]' src/components/analysis/</automated>
    <fails_when>non-zero exit, or any output line naming rule `complexity`, `max-depth` or `max-statements`</fails_when>
    <automated>cd frontend && npm run lint && npm run build</automated>
    <fails_when>non-zero exit from either, `tsc` output containing "error TS", or lint reporting `react-refresh/only-export-components`</fails_when>
  </verify>
  <done>The tab-panel family is a set of real components with typed props, no fragment is duplicated across files, and the union testid inventory proves the rendered DOM is unchanged.</done>
  <reversibility rating="reversible">One new component file plus call-site rewiring; reverting the commit restores the inline fragments.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Extract the board stage and desktop cards, land SC-0/SC-1, delete both baseline entries</name>
  <files>frontend/src/components/analysis/AnalysisBoardStage.tsx, frontend/src/components/analysis/AnalysisDesktopCards.tsx, frontend/src/pages/Analysis.tsx, frontend/eslint.config.js</files>
  <read_first>
    - `frontend/src/pages/Analysis.tsx` lines 3316-3337 (`boardRow`, `showPlayerBars`) and 3579-3661 (`desktopBoardStage`), lines 4001-4093 (`stockfishCard` 4001, `movesCard` 4046, `desktopMaiaPanel` 4060, `pasteModalNode` 4085) and lines 4094-4370 (`closeEngineGate` 4094, `handleEngineGateRetry` 4105, and the JSX return at 4248)
    - `frontend/eslint.config.js` — the Phase 215 baseline override region exactly as it stands right now (a sibling wave-2 plan may already have removed its own path)
    - `frontend/src/components/analysis/PasteModal.tsx` (the component `pasteModalNode` composes)
    - `215-RESEARCH.md` section "Baseline — use this session's numbers" (the `Analysis` row: complexity 176, 213 statements, 2037 lines at the phase base commit)
    - `.planning/phases/215-frontend-god-file-decomposition/215-04-SUMMARY.md` and `215-05-SUMMARY.md` (the intermediate numbers this task's before/after table continues)
  </read_first>
  <action>
1. Create `frontend/src/components/analysis/AnalysisBoardStage.tsx` (`BoardRow` from `boardRow`,
   `DesktopBoardStage` from `desktopBoardStage`) and
   `frontend/src/components/analysis/AnalysisDesktopCards.tsx` (`StockfishCard`, `MovesCard`,
   `DesktopMaiaPanel`, `PasteModalNode`), same real-component-with-typed-props rule as task 2.
   `showPlayerBars`, `closeEngineGate` and `handleEngineGateRetry` are page-level state and
   callbacks: they stay in `Analysis.tsx` and are passed in as props.

2. Land SC-1. Measure with the baseline defeated on the command line:
   `npx eslint --no-inline-config --rule 'complexity: ["error", 15]' --rule 'max-depth: ["error", 4]' --rule 'max-statements: ["error", 100]' src/pages/Analysis.tsx src/hooks/analysis/ src/components/analysis/`
   It must exit 0. If `Analysis()` is still over on either metric, keep extracting from the
   remaining fragments — the JSX return itself (4248-4370) is the last candidate and can be
   split into a desktop and a mobile layout component, which is CLAUDE.md's own named seam.
   Do NOT reach the number by wrapping a fragment in an arrow function called from the same
   place; that moves the operators into a nested scope without making anything readable, and it
   is explicitly prohibited by this plan.

3. Land SC-0. Re-read `frontend/eslint.config.js`, then delete `src/pages/Analysis.tsx` from
   the `complexity` block's `files` array AND delete the whole
   `{ files: ['src/pages/Analysis.tsx'], rules: { 'max-statements': 'off' } }` block. Leave
   every other entry untouched, including `src/pages/Openings.tsx`, which 215-07 owns, and the
   `max-depth` block for `src/lib/__tests__/reminderSlotState.test.ts`. Then confirm
   `npm run lint` still exits 0 — that is the proof the exemption was dead rather than load-bearing.

4. Record the phase-relevant after-numbers for the SUMMARY: `Analysis()`'s complexity,
   statements and `max-lines-per-function` count at the phase base commit and after each of
   215-04, 215-05 and this plan; the surviving `max-lines-per-function` figure for
   `Analysis.tsx` and every new component and hook, with a one-line justification for anything
   between 100 and 200 counted lines; and `npm run lint:cognitive` filtered to
   `src/pages/Analysis.tsx`, `src/hooks/analysis/` and `src/components/analysis/`, against the
   phase baseline of 4 breaches in `Analysis.tsx` (lines 549, 1640, 1782, 2390).

5. Two-way mutation proof on the render split: temporarily make `AnalysisTabs`'s moves tab
   render `null`, run `npx vitest run src/pages/__tests__/Analysis.test.tsx`, confirm it FAILS
   and record which tests failed; restore and confirm 85 passed. Repeat for `PlayerBar`. If a
   mutation leaves the suite GREEN, ADD a test to the existing `Analysis.test.tsx` (never a
   parallel file, never a rewrite), re-run and record.
  </action>
  <acceptance_criteria>
    - `frontend/src/components/analysis/AnalysisBoardStage.tsx` and `AnalysisDesktopCards.tsx` exist and export the components named in the action, each with a `type XxxProps` interface.
    - `npx eslint --no-inline-config --rule 'complexity: ["error", 15]' --rule 'max-depth: ["error", 4]' --rule 'max-statements: ["error", 100]' src/pages/Analysis.tsx src/hooks/analysis/ src/components/analysis/` exits 0 — SC-1 for this file.
    - `grep -v '^\s*//' frontend/eslint.config.js | grep -c "src/pages/Analysis.tsx"` prints `0` — SC-0 for this file.
    - `grep -v '^\s*//' frontend/eslint.config.js | grep -c "src/pages/Openings.tsx"` prints at least `1` — 215-07's entry survived.
    - `npm run lint` exits 0 after the deletions.
    - Both union inventory diffs (testid and umami) against the task-1 captures produce no output.
    - `npx vitest run src/pages/__tests__/Analysis.test.tsx` reports at least 85 passed, 0 failed.
    - The app-wide `react-hooks/exhaustive-deps` plus `react-hooks/refs` count across `.ts`/`.tsx` paths is exactly 28.
    - The two mutation proofs are recorded with their failing test names plus the restored-green confirmation.
    - `npm run lint && npm run build && npm run knip && npm test -- --run` all green.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx eslint --no-inline-config --rule 'complexity: ["error", 15]' --rule 'max-depth: ["error", 4]' --rule 'max-statements: ["error", 100]' src/pages/Analysis.tsx src/hooks/analysis/ src/components/analysis/</automated>
    <fails_when>non-zero exit, or any output line naming rule `complexity`, `max-depth` or `max-statements`</fails_when>
    <automated>cd frontend && grep -v '^\s*//' eslint.config.js | grep -c "src/pages/Analysis.tsx"</automated>
    <fails_when>prints any number other than `0`</fails_when>
    <automated>cd frontend && grep -v '^\s*//' eslint.config.js | grep -c "src/pages/Openings.tsx"</automated>
    <fails_when>prints `0` — 215-07's baseline entry was deleted by mistake</fails_when>
    <automated>cd frontend && diff <(grep -oh 'data-testid="[^"]*"' src/pages/Analysis.tsx src/components/analysis/*.tsx | sort) /tmp/analysis-testid-union-before.txt && diff <(grep -oh 'data-umami-event="[^"]*"' src/pages/Analysis.tsx src/components/analysis/*.tsx | sort) /tmp/analysis-umami-union-before.txt</automated>
    <fails_when>either `diff` prints a hunk line or exits non-zero</fails_when>
    <automated>cd frontend && npx eslint --no-inline-config -f json . 2>/dev/null | python3 -c "import json,sys; d=json.load(sys.stdin); print(sum(1 for f in d if f['filePath'].endswith(('.ts','.tsx')) for m in f['messages'] if m['ruleId'] in ('react-hooks/exhaustive-deps','react-hooks/refs')))"</automated>
    <fails_when>prints any number other than `28`</fails_when>
    <automated>cd frontend && npm run lint && npm run build && npm run knip && npm test -- --run 2>&1 | tail -6</automated>
    <fails_when>non-zero exit from any step, `tsc` output containing "error TS", knip naming an unused export, or the vitest summary reporting a non-zero "failed" count</fails_when>
  </verify>
  <done>`Analysis()` is inside cyclomatic complexity 15 and 100 statements, both of its baseline override entries are gone with `npm run lint` still green, and the rendered DOM is provably byte-identical.</done>
  <reversibility rating="reversible">Two new component files plus a config deletion; reverting the plan's commits restores both.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: HUMAN-UAT smoke of the Analysis page, desktop and mobile</name>
  <read_first>
    - `.claude/skills/parallel-worktree/SKILL.md` (if a second dev server is convenient for the smoke; not required)
    - `215-RESEARCH.md` section "Locked Decisions", the "Visual HUMAN-UAT" bullet
  </read_first>
  <what-built>
`Analysis()`'s render tree split into four components under `src/components/analysis/`
(`AnalysisPlayerBar.tsx`, `AnalysisTabs.tsx`, `AnalysisBoardStage.tsx`,
`AnalysisDesktopCards.tsx`), with both of `Analysis.tsx`'s eslint baseline override entries
deleted. Automated checks cover the DOM contract (85 tests plus a byte-level testid and umami
inventory diff), not the visual layout, and this is the first plan in the phase that changed a
render tree. Start the dev stack before handing over:
`docker compose -f docker-compose.dev.yml -p flawchess-dev up -d`, the backend, and
`npm run dev` from `frontend/`.
  </what-built>
  <how-to-verify>
Open the Analysis page and confirm each item at a desktop width AND at a mobile width (narrow
the browser or use device emulation):

1. **Library-game mode** (`/analysis?game_id=<an imported game>`): board renders, both player
   bars show the right names, ratings and clocks in the right rows, the move list and variation
   tree populate, eval bars and the eval chart render, engine lines appear once the engine warms.
2. **Paste mode** (`/analysis`, then paste a PGN): the paste modal opens and closes, the pasted
   game loads, the pasted headers show in the player bars.
3. **Tactic mode** (open a flaw from Library so the page loads with a tactic focus): the tactic
   overlay renders and the focused line is highlighted.
4. **Gem markers**: gem and great-move markers appear on the board and in the move list,
   INCLUDING on opponent moves. The analysis board intentionally shows opponent gems because
   `best_move_tier` is position-scoped; if they are missing, that is a regression introduced by
   this phase, not a fix.
5. **Tabs**: every analysis tab (human/Maia, FlawChess, eval, moves, stats) opens and renders.
6. **Mobile specifics**: the mobile engine-lines block and the mobile board layout render, and
   nothing that used to be visible has disappeared behind a breakpoint.

Record the outcome per item, with the browser and both viewport widths used, in the plan
SUMMARY. If any item fails, do not patch from this task: report which item and what you saw.
  </how-to-verify>
  <resume-signal>Type "approved" once all six items pass at both widths (item 4 explicitly confirming opponent gem markers are still visible), or describe the item that failed and what you observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered DOM ↔ browser automation / analytics | `data-testid` and `data-umami-event` attributes are a contract consumed by external tooling. This plan moves markup between files, which is exactly where an attribute can be silently renamed or dropped. Mitigated by a union inventory diff after every task; no new attribute is introduced. |

Security enforcement is inactive for this project; ASVS level 1 assumed, blocking threshold high.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-215-06-01 | Tampering | the render-fragment extractions | high | mitigate | Moving ~25 markup fragments through typed prop interfaces is the phase's largest opportunity for a silent DOM change, and the 85 tests assert only through rendered output. Mitigated by the union testid/umami inventory diff after every task, two two-way mutation proofs, and a six-item HUMAN-UAT smoke at two widths. |
| T-215-06-02 | Repudiation | `data-umami-event` attributes | medium | mitigate | A dropped analytics attribute makes an outbound click invisible with no runtime error. Mitigated by the union umami inventory diff, which is run alongside the testid diff at every task. |
| T-215-06-03 | Tampering | deleting the two baseline override entries | low | accept | Deleting an exemption cannot hide a finding; it can only expose one. `npm run lint` exiting 0 afterwards is the proof the exemption was dead. Accepted. |
</threat_model>

<verification>
- `complexity`/`max-depth`/`max-statements` with the CLI override exit 0 across `src/pages/Analysis.tsx`, `src/hooks/analysis/` and `src/components/analysis/`.
- `src/pages/Analysis.tsx` appears nowhere in `frontend/eslint.config.js`; `src/pages/Openings.tsx` still does; `npm run lint` exits 0.
- Union `data-testid` and `data-umami-event` inventories byte-identical before and after.
- `npx vitest run src/pages/__tests__/Analysis.test.tsx` reports 85 passed.
- App-wide `react-hooks/*` warning count exactly 28.
- `npm run lint`, `npm run build`, `npm run knip`, `npm test -- --run` all green.
- Two two-way mutation proofs recorded.
- HUMAN-UAT smoke recorded with per-item verdicts at two viewport widths.
</verification>

<success_criteria>
- SC-0 for `Analysis.tsx`: both baseline override entries deleted, `npm run lint` green.
- SC-1 for `Analysis.tsx`: `Analysis()` inside complexity 15 and 100 statements; every function in the page, its hooks and its new components inside the enforced limits.
- SC-2: 85 tests pass, test diff additions only, react-hooks warning count unchanged at 28.
- SC-3: union testid and umami inventories identical; no TypeScript or lint suppression added.
- SC-4: HUMAN-UAT smoke recorded for Analysis at desktop and mobile widths.
</success_criteria>

## Artifacts this plan produces

**New file `frontend/src/components/analysis/AnalysisPlayerBar.tsx`** — exports `PlayerBar`,
`EvalBarCap`, `EvalBarSlot`, `BoardHeaderRow`, `BoardFooterRow` with their `Props` types.

**New file `frontend/src/components/analysis/AnalysisTabs.tsx`** — exports `HumanTab`,
`FlawChessTab`, `MobileEngineLines`, `EvalTab`, `MoveListHeaderContent`, `MovesTab`, `StatsTab`,
`AnalysisTabs`, `VariationTreePanel`, `BoardControls`, `EvalChartPanel`, `TagsPanel`,
`EloSelectorPanel`, `FlawChessCard` with their `Props` types.

**New file `frontend/src/components/analysis/AnalysisBoardStage.tsx`** — exports `BoardRow`,
`DesktopBoardStage` with their `Props` types.

**New file `frontend/src/components/analysis/AnalysisDesktopCards.tsx`** — exports
`StockfishCard`, `MovesCard`, `DesktopMaiaPanel`, `PasteModalNode` with their `Props` types.

**`frontend/eslint.config.js`** — `src/pages/Analysis.tsx` removed from the `complexity`
baseline array; the `max-statements` override block for it deleted entirely.

**`frontend/src/pages/Analysis.tsx`** — `Analysis` keeps its name and default export; the render
section becomes component composition.

**No new npm script, no new test file** (unless a mutation proof exposes an uncovered seam, in
which case tests are ADDED to the existing `Analysis.test.tsx`).

<output>
Create `.planning/phases/215-frontend-god-file-decomposition/215-06-SUMMARY.md` when done,
recording `Analysis()`'s complexity/statements/lines at four points (phase base, after 215-04,
after 215-05, after this plan), the surviving `max-lines-per-function` figures with one-line
justifications for anything between 100 and 200 counted lines, the two mutation proofs, the
`npm run lint:cognitive` counts for the page plus both new directories, the union testid and
umami inventory verdicts, the app-wide react-hooks warning count, the eslint baseline entries
deleted, and the six HUMAN-UAT smoke outcomes with the viewport widths used.
</output>

