---
phase: 215-frontend-god-file-decomposition
plan: 08
type: execute
wave: 3
depends_on: ["215-02", "215-03", "215-04", "215-05", "215-06", "215-07"]
files_modified:
  - .planning/codebase/CONCERNS.md
autonomous: true
requirements: [SC-0, SC-1, SC-2, SC-3, SC-4, SC-5]
user_setup: []

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "SC-0 (relaxed 2026-09-04, see ROADMAP Phase 215 success criteria): `npm run lint` exits 0 with `complexity`/`max-depth`/`max-statements` enabled; `useBotGame.ts` and `workerPool.ts` appear nowhere in `frontend/eslint.config.js`; `Analysis.tsx` (and `Openings.tsx` if 215-07 recorded a residual) appear only in the Phase 215 baseline region with a reasoned-residual comment whose numbers match the plan SUMMARY."
    - "SC-1 (relaxed 2026-09-04): with the baseline region defeated on the command line, no function in the four in-scope files or in any file this phase created exceeds nesting depth 4, and none exceeds cyclomatic complexity 15 or 100 statements EXCEPT the page-component bodies `Analysis()` and (if recorded) `OpeningsPage()`, whose measured residuals and justifications are copied from their SUMMARYs into this plan's table."
    - "SC-1: every remaining function in those files between 100 and 200 counted lines is listed with a one-line justification, and any function still over 200 carries a written reasoned exemption."
    - "SC-2: the full frontend gate (`lint`, `build`, `knip`, `test`) and the full backend pre-merge gate both pass; the aggregate test diff for the phase shows additions only; the app-wide `react-hooks/*` warning count is exactly 28, unchanged."
    - "SC-3: per file, the before/after `data-testid` and `data-umami-event` inventories are identical; the phase diff adds no TypeScript suppression comment and no lint suppression beyond the ones that moved with their code."
    - "SC-4: the HUMAN-UAT smoke outcomes for Analysis and Openings, desktop and mobile, are collected from the 215-06 and 215-07 SUMMARYs and recorded in one place."
    - "SC-5: the CONCERNS.md 'Large God files' entry no longer lists any of the four files this phase decomposed."
  artifacts:
    - .planning/codebase/CONCERNS.md
  key_links:
    - "the four in-scope files with the baseline region defeated -> ROADMAP success criteria 0 and 1"
    - "the aggregate `git diff` over `__tests__`/`*.test.*` across the phase -> ROADMAP success criterion 2"
    - "the per-file testid/umami inventories -> ROADMAP success criterion 3"
    - "the 215-06 and 215-07 HUMAN-UAT records -> ROADMAP success criterion 4"
    - "CONCERNS.md 'Large God files' -> ROADMAP success criterion 5"
  prohibitions:
    - statement: "No file under frontend/src/ or app/ is modified in this plan — it verifies and documents. If a gate fails, report exactly which criterion and which command rather than patching the code here."
      status: flagged
      verification: manual
    - statement: "No test is deleted or weakened."
      status: flagged
      verification: manual
    - statement: "The CONCERNS.md entry is not deleted outright — the next tier of large frontend components is still open debt and stays listed, now with the gate enforcing the limit on new code."
      status: flagged
      verification: manual
    - statement: "No neighbouring CONCERNS.md entry is edited. The `# ty: ignore` suppressions entry and every other concern stay byte-identical."
      status: flagged
      verification: manual
    - statement: "`frontend/eslint.config.js` is not edited in this plan. If a baseline entry is still present for an in-scope file, that is a finding to report, not to fix here."
      status: flagged
      verification: manual
---

<objective>
Verify the phase's six success criteria against the merged result of plans 215-02..215-07 and
narrow the CONCERNS.md "Large God files" entry to the frontend debt that actually remains.

Purpose: each file plan proves its own file. Nothing so far proves the phase — that
`npm run lint` is green with only the two recorded page-component residuals left (SC-0/SC-1
were relaxed for `Analysis()`/`OpeningsPage()` on 2026-09-04, see the ROADMAP success
criteria), that no other function anywhere in the four files still breaches the limits, that the aggregate test diff really is additions
only, and that the app-wide `react-hooks/*` warning count never moved. This plan is the one
place those aggregate facts are measured and written down, and it is the only correct moment to
update the source concern that started the phase.

Output: a recorded phase-wide after-baseline (eslint core rules, `max-lines-per-function`,
`lint:cognitive`, react-hooks counts, testid/umami inventories, aggregate diffs) and a
CONCERNS.md entry that names only the next tier.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@frontend/CLAUDE.md

Do NOT `@`-load `.planning/PROJECT.md`, `.planning/ROADMAP.md` (133 KB) or
`.planning/STATE.md` (312 KB). The six success criteria are quoted in task 1.

**Ordering:** this plan runs after all six file plans are merged. If any of them has not landed,
stop and say which — a phase-wide baseline measured against a partial trunk is worse than no
baseline.

**Security enforcement is inactive for this project** (`.planning/config.json` has no
`workflow.security_*` keys). The `<threat_model>` block is recorded for continuity with
Phase 214.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Measure the phase-wide after-baseline against all six success criteria</name>
  <precondition>Plans 215-02 through 215-07 are all merged into the working branch: `npm run lint` exits 0 from `frontend/` and `git log --oneline` shows commits for `workerPool.ts`, `useBotGame.ts`, `Analysis.tsx` (three plans) and `Openings.tsx`.</precondition>
  <precondition>The dev PostgreSQL is running for the backend half of the pre-merge gate: `docker compose -f docker-compose.dev.yml -p flawchess-dev ps` shows `flawchess-dev-db-1` healthy.</precondition>
  <files>(measurement only — no file is modified by this task)</files>
  <read_first>
    - `.planning/phases/215-frontend-god-file-decomposition/215-01-SUMMARY.md` (the before-baselines every after-number is compared against: complexity 69 breaches / 52 files, `max-depth` 6 / 1, `max-statements` 1 / 1, `max-lines-per-function` 66 / 62 over `.ts`/`.tsx`; the cognitive-complexity counts; the react-hooks counts)
    - the six file plans' SUMMARYs (`215-02-SUMMARY.md` … `215-07-SUMMARY.md`) — their per-file before/after numbers, mutation proofs, survivor listings and HUMAN-UAT records roll up into this plan's tables
    - `CLAUDE.md` (the pre-merge gate command list) and `frontend/CLAUDE.md` (the knip and browser-automation rules)
  </read_first>
  <action>
Measure, do not fix. Record every number in the SUMMARY as an explicit before/after table
against the 215-01 baseline. All frontend commands run from `frontend/`.

**Criterion 0 — lint green, no in-scope relaxations beyond the recorded residuals (relaxed 2026-09-04).**
`npm run lint` must exit 0. Then prove the four files carry no relaxation beyond the recorded page-component residuals, two ways:
- `grep -v '^\s*//' eslint.config.js | grep -cE "src/hooks/useBotGame.ts|src/lib/engine/workerPool.ts"` must print `0` (comment lines are filtered so a leftover explanatory comment naming a removed path cannot fail the gate).
- `grep -v '^\s*//' eslint.config.js | grep -cE "src/pages/Analysis.tsx|src/pages/Openings.tsx"` prints the number of recorded residual entries (2 for `Analysis.tsx`: `complexity` + `max-statements`; plus 1 for `Openings.tsx` only if 215-07 kept its entry), and each such entry carries a residual comment whose before/after numbers match the 215-06 / 215-07 SUMMARY. Any other in-scope entry is a finding.
- Run the enforced rules over all four files AND every file this phase created, with the
  baseline region defeated on the command line:
  `npx eslint --no-inline-config --rule 'complexity: ["error", 15]' --rule 'max-depth: ["error", 4]' --rule 'max-statements: ["error", 100]' src/pages/Analysis.tsx src/pages/Openings.tsx src/hooks/useBotGame*.ts src/lib/engine/workerPool*.ts src/hooks/analysis/ src/components/analysis/ src/pages/openings/`
  Every finding it prints must be one of the recorded residuals (`Analysis` complexity and
  statements; `OpeningsPage` complexity only if 215-07 recorded one); anything else is a
  finding. That is the direct proof, independent of what the config happens to say.
Also record the app-wide residue: rerun the 215-01 measurement command over `.` and reduce it
to per-rule breach and file counts for `.ts`/`.tsx` paths. `complexity` should be the 215-01
baseline of 69/52 minus everything the six file plans fixed; the residue is the out-of-scope
files.

**Criterion 0b — cognitive complexity and `max-lines-per-function` before/after.**
`npm run lint:cognitive`, reduced to a total breach count and a file count, against 215-01's
baseline; plus the per-file counts for the four in-scope files (215-01 recorded
`Analysis.tsx` 4, `useBotGame.ts` 0, `workerPool.ts` 1, `Openings.tsx` 1).
`npx eslint --no-inline-config --rule 'max-lines-per-function: ["warn", {"max": 200, "skipBlankLines": true, "skipComments": true}]' .` reduced the same way, against 215-01's 66/62.

**Criterion 1 — size, complexity and depth.**
The enforced-rule command above printing nothing but the recorded page-component residuals IS criterion 1's gate (relaxed 2026-09-04: `Analysis()` and, if recorded, `OpeningsPage()` may exceed complexity 15 / 100 statements with a written justification; nesting depth 4 has no exemption). Then build the listing
criterion 1 asks for: run `max-lines-per-function` at 100 over the four files and everything
this phase created, and list every function between 100 and 200 counted lines with the one-line
justification its own plan's SUMMARY already recorded. Add any function still OVER 200 with its
written reasoned exemption (215-03 may have one for `useBotGame`, 215-07 may have one for
`OpeningsPage`'s JSX return). A survivor with no written justification is a criterion-1 failure,
not a footnote.

**Criterion 2 — full gates, tests additions-only, react-hooks count unchanged.**
- Frontend: `npm run lint && npm run build && npm run knip && npm test -- --run`.
- Backend (unchanged by this phase, but the pre-merge gate covers both stacks):
  `uv run ruff format --check app/ tests/ scripts/ && uv run ruff check . && uv run ty check app/ tests/ scripts/ && uv run --project analysis --with ty ty check analysis/ && uv run pytest -n auto -x -q`.
- Aggregate test diff: `git diff --numstat <phase-base-commit>..HEAD -- 'frontend/src/**/__tests__/*' 'frontend/src/**/*.test.*'` — every row must show 0 in the deletions column. The only expected rows are `Openings.render.test.tsx` (new, from 215-07) and any test ADDED because a mutation proof exposed an uncovered seam.
- App-wide `react-hooks/exhaustive-deps` plus `react-hooks/refs` count over `.ts`/`.tsx` paths must be exactly 28, the same number 215-01 recorded.

**Criterion 3 — inventories identical, growth from deliberate extractions only, no new suppressions.**
- Per file, compare the union `data-testid` and `data-umami-event` inventories at the phase base
  commit against now:
  `git show <phase-base-commit>:frontend/src/pages/Analysis.tsx | grep -o 'data-testid="[^"]*"' | sort` versus
  `grep -oh 'data-testid="[^"]*"' frontend/src/pages/Analysis.tsx frontend/src/components/analysis/*.tsx | sort`,
  and the same shape for `Openings.tsx` against `frontend/src/pages/openings/*.tsx`.
  `useBotGame.ts` and `workerPool.ts` had 0 of each and must still have 0.
- The phase diff over `frontend/src/` must add zero TypeScript suppression comments; the exact
  runnable command is in this task's acceptance criteria and verify block.
- Suppression-comment accounting: the total across `frontend/src/pages/Analysis.tsx` plus
  `frontend/src/hooks/analysis/` must be exactly 6, matching the pre-phase count in
  `Analysis.tsx` alone. Scope every one of these greps to `frontend/src/`: an unscoped diff also
  spans `.planning/`, where this phase's own plan documents quote the suppression syntax in
  prose and would make the gate count its own planning text.
- `git diff --stat <phase-base-commit>..HEAD -- frontend/` — confirm the new files are exactly
  the ones the six plans' "Artifacts this plan produces" sections list, and nothing else.

**Criterion 4 — HUMAN-UAT.**
Collect the six-item smoke records from `215-06-SUMMARY.md` (Analysis: library-game, paste and
tactic modes; gem markers on opponent moves) and `215-07-SUMMARY.md` (Openings: desktop sidebar,
mobile drawer, bookmarks drawer, filter parity), with the viewport widths used, into one table.
If either is missing or records a failure, that is a criterion-4 failure to report.

If ANY of these fails, stop and report exactly which criterion and which command. Do not patch
frontend or backend code from this plan.
  </action>
  <acceptance_criteria>
    - `npm run lint` exits 0 from `frontend/`.
    - `grep -v '^\s*//' frontend/eslint.config.js | grep -cE "src/hooks/useBotGame.ts|src/lib/engine/workerPool.ts"` prints `0`, and every remaining in-scope entry (`Analysis.tsx`, optionally `Openings.tsx`) carries a residual comment whose numbers match its SUMMARY.
    - The enforced-rule command with the baseline defeated, over the four in-scope files and all directories this phase created, prints no finding other than the recorded page-component residuals (`Analysis` complexity/statements, and `OpeningsPage` complexity only if 215-07 recorded one).
    - The full frontend gate (`lint`, `build`, `knip`, `test`) and the full backend pre-merge gate are both green.
    - `git diff --numstat <phase-base-commit>..HEAD` over the frontend test globs shows 0 in the deletions column on every row.
    - The app-wide `react-hooks/exhaustive-deps` plus `react-hooks/refs` count is exactly 28.
    - `git diff <phase-base-commit>..HEAD -- frontend/src/ | grep -cE '^\+.*@ts-(ignore|expect-error)'` prints `0`.
    - The per-file testid and umami inventory comparisons against the phase base commit produce no differences.
    - The SUMMARY contains: a before/after table for all four eslint rules (app-wide and per in-scope file), the `lint:cognitive` before/after counts, the complete 100-to-200-counted-line survivor listing with justifications, any over-200 reasoned exemption, the aggregate test-diff verdict, and the collected HUMAN-UAT table.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run lint && grep -v '^\s*//' eslint.config.js | grep -cE "src/hooks/useBotGame.ts|src/lib/engine/workerPool.ts"</automated>
    <fails_when>non-zero exit from lint, or the grep printing any number other than `0`</fails_when>
    <automated>cd frontend && npx eslint --no-inline-config --rule 'complexity: ["error", 15]' --rule 'max-depth: ["error", 4]' --rule 'max-statements: ["error", 100]' src/pages/Analysis.tsx src/pages/Openings.tsx src/hooks/useBotGame*.ts src/lib/engine/workerPool*.ts src/hooks/analysis/ src/components/analysis/ src/pages/openings/ 2>&1 | grep -E "complexity|max-depth|max-statements" | grep -vE "Function '(Analysis|OpeningsPage)' has (a complexity|too many statements)"</automated>
    <fails_when>prints any line (a finding other than the recorded page-component residuals), or an `OpeningsPage` residual appears without 215-07 having recorded one</fails_when>
    <automated>cd frontend && npx eslint --no-inline-config -f json . 2>/dev/null | python3 -c "import json,sys; d=json.load(sys.stdin); print(sum(1 for f in d if f['filePath'].endswith(('.ts','.tsx')) for m in f['messages'] if m['ruleId'] in ('react-hooks/exhaustive-deps','react-hooks/refs')))"</automated>
    <fails_when>prints any number other than the 215-01 measured baseline (30 with `--no-inline-config`: 27 exhaustive-deps + 3 refs; 215-04 corrected the plan text's 28)</fails_when>
    <automated>cd frontend && npm run build && npm run knip && npm test -- --run 2>&1 | tail -6</automated>
    <fails_when>non-zero exit from any step, `tsc` output containing "error TS", knip naming an unused export or dependency, or the vitest summary reporting a non-zero "failed" count</fails_when>
    <automated>uv run ruff format --check app/ tests/ scripts/ && uv run ruff check . && uv run ty check app/ tests/ scripts/ && uv run --project analysis --with ty ty check analysis/ && uv run pytest -n auto -x -q 2>&1 | tail -5</automated>
    <fails_when>non-zero exit, output naming a file as "would be reformatted", output containing "error[", or the pytest summary reporting a non-zero "failed" or "error" count</fails_when>
    <automated>git diff --numstat $(git merge-base HEAD main)..HEAD -- 'frontend/src/**/__tests__/*' 'frontend/src/**/*.test.*' | awk '{s+=$2} END {print s+0}'</automated>
    <fails_when>prints any number other than `0` — a test line was deleted somewhere in the phase</fails_when>
    <automated>git diff $(git merge-base HEAD main)..HEAD -- frontend/src/ | grep -cE '^\+.*@ts-(ignore|expect-error)'</automated>
    <fails_when>prints any number other than `0`</fails_when>
  </verify>
  <done>All six ROADMAP success criteria are measured against the merged trunk with recorded numbers, so the phase's claim rests on tool output rather than on six separate per-file assertions.</done>
  <reversibility rating="reversible">Measurement only; no file is modified.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Narrow the CONCERNS.md "Large God files" entry to the next tier</name>
  <files>.planning/codebase/CONCERNS.md</files>
  <read_first>
    - `.planning/codebase/CONCERNS.md` lines 18-24 (the "Large God files" entry: Issue / Files / Impact / Fix approach / History, as Phase 214 left it — it currently lists exactly the four files this phase decomposed and its Fix approach still says a follow-up phase "should also enable eslint `complexity` / `max-depth` gates for the frontend", which this phase did)
    - this plan's task-1 measurements (the actual post-phase line counts and complexity numbers)
    - the phase's own out-of-scope list: `frontend/src/components/train/TrainReveal.tsx`, `frontend/src/components/library/EvalChart.tsx`, `frontend/src/components/results/LibraryGameCard.tsx`, `frontend/src/components/train/TrainSolveScreen.tsx`, `frontend/src/components/analysis/VariationTree.tsx`, `frontend/src/App.tsx`
    - `CLAUDE.md` lines 156-163 (the function-size rule the entry cites, now with a "Frontend measurement tools" line added by 215-01)
  </read_first>
  <action>
Rewrite the "Large God files" entry so it describes only what is still true.

- **Issue**: reframe around the next tier of large frontend components. Drop the claim that
  "there is no automated gate enforcing the limit on the frontend yet" — 215-01 added one.
  State instead that these files are baselined in the Phase 215 override region of
  `frontend/eslint.config.js` and shrink under CLAUDE.md's "refactor bloated code on sight"
  rule, with the gate preventing NEW breaches.
- **Files**: list the six next-tier paths with their line counts measured now (`wc -l`), not
  values copied from an older map. Do NOT list any of `frontend/src/pages/Analysis.tsx`,
  `frontend/src/hooks/useBotGame.ts`, `frontend/src/lib/engine/workerPool.ts` or
  `frontend/src/pages/Openings.tsx` — a verification below greps this line for them.
- **Impact**: keep the substance, scoped to the next tier.
- **Fix approach**: point at the "refactor on sight" rule plus the now-live gate, and note that
  a file's entry leaves the eslint baseline region when its complexity is fixed.
- **History**: append one line recording that the four frontend god files were decomposed in
  Phase 215 (with the date) and that the eslint `complexity`/`max-depth`/`max-statements` gate
  plus `npm run lint:cognitive` landed with it, alongside the existing Phase 214 line. Write
  that history line WITHOUT re-listing the four decomposed module paths: the verification greps
  the entry's `Files:` line for them, and a history line that re-lists them would make a
  correctly-narrowed entry look wrong.

Do not touch any neighbouring concern. The `# ty: ignore` suppressions entry below it is
unrelated to this phase; if you notice something stale there, note it in the SUMMARY for a
future map refresh rather than editing it here.
  </action>
  <acceptance_criteria>
    - The "Large God files" entry's `Files:` line names the six next-tier `frontend/` paths and none of the four decomposed files.
    - `grep -A10 'Large "God files"' .planning/codebase/CONCERNS.md | grep '^- Files:' | grep -cE 'Analysis.tsx|useBotGame.ts|workerPool.ts|Openings.tsx'` prints `0`.
    - `grep -A10 'Large "God files"' .planning/codebase/CONCERNS.md | grep -c 'TrainReveal.tsx'` is at least 1 — the entry is narrowed, not deleted.
    - The six line counts in the entry match current `wc -l` output.
    - The entry retains an Issue, Files, Impact, Fix approach and History line, and the History line records both Phase 214 and Phase 215 with their dates.
    - The neighbouring `# ty: ignore` suppressions concern is unchanged (`git diff` shows no hunk touching it).
    - `git diff --stat` for this plan lists `.planning/codebase/CONCERNS.md` only.
  </acceptance_criteria>
  <verify>
    <automated>grep -A10 'Large "God files"' .planning/codebase/CONCERNS.md | grep '^- Files:' | grep -cE 'Analysis.tsx|useBotGame.ts|workerPool.ts|Openings.tsx'</automated>
    <fails_when>prints any number other than `0`</fails_when>
    <automated>grep -A10 'Large "God files"' .planning/codebase/CONCERNS.md | grep -c 'TrainReveal.tsx'</automated>
    <fails_when>prints `0` — the next tier must still be listed; the entry is narrowed, not deleted</fails_when>
    <automated>grep -A12 'Large "God files"' .planning/codebase/CONCERNS.md | grep -c 'Phase 215'</automated>
    <fails_when>prints `0` — the history line recording this phase is missing</fails_when>
    <automated>git diff --stat -- .planning/codebase/CONCERNS.md</automated>
    <fails_when>no output (the file was not modified), or output listing any file other than `.planning/codebase/CONCERNS.md`</fails_when>
  </verify>
  <done>The concern that motivated this phase now describes only the next tier of frontend debt, with current line counts, the now-live gate named as the mechanism, and a history line recording both decomposition phases.</done>
  <reversibility rating="reversible">A planning-document edit.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan runs verification commands and edits one planning document. No application code, configuration, dependency, endpoint or schema is touched. |

Security enforcement is inactive for this project; ASVS level 1 assumed, blocking threshold high.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-215-08-01 | Repudiation | the phase-wide after-baseline | medium | mitigate | A closeout that asserts success without measuring it would let a missed breach ship as "verified". Mitigated by making every criterion a runnable command with a stated failing direction, and by the instruction to stop and report rather than patch when one fails. |
| T-215-08-02 | Tampering | `.planning/codebase/CONCERNS.md` | low | accept | A documentation edit with no runtime effect; the acceptance criteria keep it from being deleted outright or from silently swallowing the adjacent suppressions concern. |
</threat_model>

<verification>
- `npm run lint` exits 0; none of the four in-scope files appears in `frontend/eslint.config.js`.
- The enforced rules with the baseline defeated exit 0 over the four files and every directory this phase created.
- Full frontend gate and full backend pre-merge gate both green.
- Aggregate frontend test diff shows 0 deletions on every row; zero added TypeScript suppressions.
- App-wide `react-hooks/*` warning count exactly 28.
- Per-file testid and umami inventories identical to the phase base commit.
- The 100-to-200-counted-line survivor listing is complete with justifications, and any over-200 survivor carries a written exemption.
- HUMAN-UAT records for Analysis and Openings collected in one table.
- CONCERNS.md "Large God files" names only the next tier and records Phase 215 in its history.
</verification>

<success_criteria>
- All six ROADMAP success criteria (0, 1, 2, 3, 4, 5) are measured and recorded with commands and numbers.
- The CONCERNS.md entry is narrowed to the next tier with current line counts and a two-phase history line.
- No frontend or backend source file changed in this plan.
</success_criteria>

## Artifacts this plan produces

**`.planning/codebase/CONCERNS.md`** — the "Large God files" entry rewritten to name only
`frontend/src/components/train/TrainReveal.tsx`, `frontend/src/components/library/EvalChart.tsx`,
`frontend/src/components/results/LibraryGameCard.tsx`,
`frontend/src/components/train/TrainSolveScreen.tsx`,
`frontend/src/components/analysis/VariationTree.tsx` and `frontend/src/App.tsx`, with current
line counts, a fix approach naming the now-live eslint gate and the Phase 215 baseline override
region, and a history line recording both the Phase 214 backend and the Phase 215 frontend
decompositions.

**No new symbols, no new source files, no dependency change, no eslint config change.**

<output>
Create `.planning/phases/215-frontend-god-file-decomposition/215-08-SUMMARY.md` when done,
containing the phase-wide before/after table for all four eslint rules (app-wide and per
in-scope file), the `npm run lint:cognitive` before/after counts, the complete
100-to-200-counted-line survivor listing with one-line justifications, any over-200 reasoned
exemption, the aggregate test-diff verdict, the per-file testid and umami inventory verdicts,
the app-wide `react-hooks/*` count, the collected HUMAN-UAT table for Analysis and Openings with
viewport widths, and the full list of files this phase created.
</output>
