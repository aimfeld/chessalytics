---
phase: 216-audit-bugs-and-quick-wins
plan: 06
type: execute
wave: 4
depends_on: ["216-01", "216-04"]
files_modified:
  - app/services/lichess_client.py
  - app/services/user_benchmark_percentiles_service.py
  - app/routers/position_bookmarks.py
  - app/services/openings_service.py
  - app/services/chesscom_client.py
  - app/services/import_service.py
  - app/services/library_service.py
  - .github/workflows/ci.yml
  - CLAUDE.md
  - docs/dev-tooling.md
autonomous: true
requirements: []
user_setup: []

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 5
  confidence: low

must_haves:
  truths:
    - "SC-6: uv run python scripts/check_function_size.py app/ --fail-over-depth 4 --fail-over-loc 200 exits 0 with zero breaches."
    - "SC-6: all eight nesting-depth breaches listed in 216-CONTEXT.md D-13 are fixed by zero-behavior-change extraction, with no baseline file and no depth pragma."
    - "SC-6: no existing test file is rewritten to fit a refactor; tests may only be added, and every covering suite named in 216-RESEARCH.md passes unmodified."
    - "SC-6: the gate runs as a CI step in .github/workflows/ci.yml and as a line in the CLAUDE.md mandatory pre-merge gate block, scoped to app/ only."
    - "Spec-less probe fallback: skipped visibly — this phase has no requirement IDs and no SPEC.md/UI-SPEC.md/AI-SPEC.md, so no probe predicates were generated."
  artifacts:
    - app/services/lichess_client.py
    - app/services/user_benchmark_percentiles_service.py
    - app/routers/position_bookmarks.py
    - app/services/openings_service.py
    - app/services/chesscom_client.py
    - app/services/import_service.py
    - app/services/library_service.py
    - .github/workflows/ci.yml
    - CLAUDE.md
    - docs/dev-tooling.md
  key_links:
    - "extracted module-scope helpers -> called once per loop iteration -> the original function stays at the outer nesting level"
    - "scripts/check_function_size.py app/ --fail-over-depth 4 -> exit code -> new CI step -> job fails on a regression"
    - "CLAUDE.md pre-merge gate block -> the same command -> local squash-merge to main"
---

<objective>
Close F-09 / SEED-161 group 6: `scripts/check_function_size.py` exists but runs in neither
CI nor the pre-merge gate, and eight functions in `app/` breach the CLAUDE.md hard nesting
limit of 4. D-13 fixes all eight rather than baselining them, and D-14 then gates the rule
in both places.

Purpose: make the nesting rule real. A gate that is only documented is not a gate, and a
baseline file would have frozen the eight breaches in place indefinitely.
Output: seven refactored source files, a new CI step, a new pre-merge gate line, and a doc
note. No behavior change anywhere.

Spec-less probe fallback is skipped visibly: this phase carries no requirement IDs and no
SPEC/UI-SPEC/AI-SPEC exists, so no probe predicates were generated this run.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/216-audit-bugs-and-quick-wins/216-CONTEXT.md
@.planning/phases/216-audit-bugs-and-quick-wins/216-RESEARCH.md
@.planning/phases/216-audit-bugs-and-quick-wins/216-PATTERNS.md
@.planning/phases/216-audit-bugs-and-quick-wins/216-01-SUMMARY.md
@.planning/phases/216-audit-bugs-and-quick-wins/216-04-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this plan produces

New module-scope helper functions (names are the intended seams from 216-RESEARCH.md's
per-function table; adjust only if reading the real code shows a better seam, and record any
deviation in the SUMMARY):

| File | New helper | Replaces |
|------|-----------|----------|
| `app/services/lichess_client.py` | `_raise_for_status`, `_normalize_line` | the status-code branch chain and the per-NDJSON-line parse/normalize block |
| `app/services/user_benchmark_percentiles_service.py` | `_compute_and_upsert_cell` (shared by both stages) | the per-cell try/except body in `compute_stage_a` and `compute_stage_b` |
| `app/routers/position_bookmarks.py` | `_build_position_suggestion` | the per-position loop body including the FEN reconstruction try/except |
| `app/services/openings_service.py` | `_build_bookmark_time_series` | the per-bookmark loop body |
| `app/services/import_service.py` | `_resolve_lichess_since_ms` | the nested since-override / last-synced / tz-aware chain |
| `app/services/library_service.py` | `_build_tactic_by_ply_entry` | the per-row tactic-slot computation inside the `if positions:` branch |
| `app/services/chesscom_client.py` | one per-month fetch helper (name it after reading the real seam) | the per-month fetch loop body |

Other artifacts:

| Kind | Name | Notes |
|------|------|-------|
| New CI step | `Function-size gate` | test job, immediately after `Type check (ty, analysis project)` |
| New pre-merge gate line | in the `CLAUDE.md` pre-merge gate code block | the same command, scoped to `app/` |
| Doc note | `docs/dev-tooling.md` `scripts/check_function_size.py` bullet | records that the gate now runs in CI and pre-merge |

The negative greps below run against the gate script's captured output, never against source
files, so a source comment cannot self-invalidate them; the filenames are allowlisted:

<!-- planner-discipline-allow: lichess_client.py -->
<!-- planner-discipline-allow: user_benchmark_percentiles_service.py -->
<!-- planner-discipline-allow: position_bookmarks.py -->
<!-- planner-discipline-allow: openings_service.py -->
<!-- planner-discipline-allow: chesscom_client.py -->
<!-- planner-discipline-allow: import_service.py -->
<!-- planner-discipline-allow: library_service.py -->

<tasks>

<task type="tracer">
  <name>Task 1: Flatten fetch_lichess_games (depth 7, the worst) and prove the whole loop</name>
  <files>app/services/lichess_client.py</files>
  <read_first>
    - `app/services/lichess_client.py` lines 40-230 — read only this range, which covers the whole target function; do not read the entire file
    - `.planning/phases/216-audit-bugs-and-quick-wins/216-RESEARCH.md` "Per-function refactor seam" table, the `lichess_client.py:51 fetch_lichess_games` row — it names both seams and their exact line ranges
    - `CLAUDE.md` "Coding Guidelines" -> "Keep functions small and shallow", especially the "Don't split just to fit a signature" caveat and the named seams
    - `scripts/check_function_size.py` docstring — how depth is measured and why the `allow-loc` pragma never covers depth
  </read_first>
  <action>
    Establish the baseline first: run the gate and save its full output, so every later task
    can diff against a recorded starting state rather than a remembered one.

    Then take the two seams RESEARCH.md identified. Extract the HTTP status-code branch chain
    into a module-scope `_raise_for_status(response, username) -> None` that returns normally
    on success and raises the same exception types the inline code raises today, in the same
    order, for the same status codes. Extract the per-NDJSON-line parse and normalize block
    into a synchronous module-scope `_normalize_line(line, username, user_id)` returning the
    normalized game or `None` to signal skip, swallowing exactly the two exception types the
    inline code swallows today and no others. The async generator body then keeps only the
    blank-line skip, the helper call, the `None` check, the yield, and the fetched-game
    callback.

    This is a zero-behavior-change refactor. Preserve exception types, exception ordering,
    retry semantics, log calls, and any Sentry capture exactly as they are; a swallowed
    exception that used to be re-raised, or a retry that stops retrying, is a regression even
    if every test still passes. Add type annotations on both helpers, since the whole file
    must keep passing ty.

    Run the covering suites and the gate, and confirm the gate output no longer names this
    file. If depth is still above 4, take the second seam further rather than adding a pragma:
    D-13 rules out both a baseline file and a depth pragma.
  </action>
  <verify>
    <automated>uv run python scripts/check_function_size.py app/ --fail-over-depth 4 --fail-over-loc 200 > /tmp/216-06-gate.txt 2>&1; grep -c 'lichess_client.py' /tmp/216-06-gate.txt</automated>
    <fails_when>prints anything other than 0 (the file still has a breaching function)</fails_when>
    <automated>uv run pytest tests/test_lichess_client.py tests/test_import_service.py -x -q 2>&1 | tail -5</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored test</fails_when>
    <automated>git diff --numstat -- tests/ | awk '$2 != 0 {print "MODIFIED:" $3; f=1} END {exit f?1:0}'</automated>
    <fails_when>non-zero exit, or any MODIFIED: line is printed (a line was deleted from an existing test file, meaning a test was rewritten rather than added)</fails_when>
    <automated>uv run ty check app/ tests/ scripts/</automated>
    <fails_when>non-zero exit, or the output reports one or more diagnostics</fails_when>
  </verify>
  <acceptance_criteria>
    - The gate output contains no line naming `app/services/lichess_client.py`.
    - `tests/test_lichess_client.py` and `tests/test_import_service.py` pass, both unmodified.
    - `git diff --numstat -- tests/` shows a deleted-lines count of 0 for every existing test file.
    - Both new helpers are module-scope, annotated, and pass ty.
    - No `check-function-size` pragma was added anywhere.
    - The recorded baseline gate output (all eight breaches) is saved in the SUMMARY for the closing comparison.
  </acceptance_criteria>
  <done>The worst breach in the codebase is gone, and the extraction pattern the remaining tasks follow is proven against real tests.</done>
</task>

<task type="auto">
  <name>Task 2: compute_stage_a and compute_stage_b — one shared seam, two call sites</name>
  <files>app/services/user_benchmark_percentiles_service.py</files>
  <read_first>
    - `app/services/user_benchmark_percentiles_service.py` lines 415-560 — read only this range, covering both target functions
    - `.planning/phases/216-audit-bugs-and-quick-wins/216-RESEARCH.md` "Per-function refactor seam" table, both `user_benchmark_percentiles_service.py` rows. The `compute_stage_b` row carries an explicit warning: extracting the cell body alone drops it from 7 to about 5, which still breaches, because of the extra metric-family loop. The double loop itself must also be addressed.
    - `CLAUDE.md` "Don't split just to fit a signature" — if the two call sites end up needing materially different parameter sets, use two helpers rather than one over-parameterized one
  </read_first>
  <action>
    Extract the per-cell try/except body (compute the metric for one user and time-control
    anchor, re-raise cancellation, capture and continue on any other exception) into a
    module-scope helper called once per cell from both stages. Keep the Sentry context and
    the stage label intact: whichever stage a failure came from must still be
    distinguishable in Sentry after the refactor.

    For the second stage, one helper is explicitly not expected to be enough. Also flatten
    the metric-family and anchor double loop, either by iterating a single flattened product
    of the two sequences or by extracting the whole inner loop as one call per outer
    iteration. Choose after reading the code; record which you chose and why in the SUMMARY.

    Verify the actual post-refactor depth with the gate rather than assuming the same fix
    that cleared the first stage clears the second.

    Preserve ordering and short-circuit semantics exactly: these stages write percentile rows,
    so a reordered upsert or a swallowed cancellation is a real regression that the depth
    number will not reveal.
  </action>
  <verify>
    <automated>uv run python scripts/check_function_size.py app/ --fail-over-depth 4 --fail-over-loc 200 > /tmp/216-06-gate.txt 2>&1; grep -c 'user_benchmark_percentiles_service.py' /tmp/216-06-gate.txt</automated>
    <fails_when>prints anything other than 0 (either stage still breaches)</fails_when>
    <automated>uv run pytest tests/services/test_user_benchmark_percentiles_service.py tests/services/test_user_benchmark_percentiles_service_real_data.py tests/services/test_import_service_stage_a.py tests/services/test_eval_drain_stage_b.py tests/services/test_percentile_compute_gate.py -x -q 2>&1 | tail -5</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored test</fails_when>
    <automated>git diff --numstat -- tests/ | awk '$2 != 0 {print "MODIFIED:" $3; f=1} END {exit f?1:0}'</automated>
    <fails_when>non-zero exit, or any MODIFIED: line is printed</fails_when>
    <automated>uv run ty check app/ tests/ scripts/</automated>
    <fails_when>non-zero exit, or the output reports one or more diagnostics</fails_when>
  </verify>
  <acceptance_criteria>
    - The gate output contains no line naming `app/services/user_benchmark_percentiles_service.py`.
    - All five covering test files pass, unmodified.
    - The cancellation path still re-raises rather than being captured and swallowed.
    - The Sentry context still distinguishes which stage a cell failure came from.
    - The SUMMARY records which flattening approach was chosen for the second stage and why.
  </acceptance_criteria>
  <done>Both percentile stages are under the depth limit with their write ordering and error semantics intact.</done>
</task>

<task type="auto">
  <name>Task 3: get_suggestions and get_time_series — extract the per-item loop body</name>
  <files>app/routers/position_bookmarks.py, app/services/openings_service.py</files>
  <read_first>
    - `app/routers/position_bookmarks.py` lines 30-135 — read only this range, covering the target function including the FEN reconstruction try/except
    - `app/services/openings_service.py` lines 275-380 — read only this range, covering the target function
    - `.planning/phases/216-audit-bugs-and-quick-wins/216-RESEARCH.md` "Per-function refactor seam" table, the `position_bookmarks.py` and `openings_service.py` rows
    - `CLAUDE.md` "Architecture" -> Router convention, and "routers stay thin, with branching and aggregation pushed into the service layer"
  </read_first>
  <action>
    For the router handler, extract the per-position loop body, including the FEN
    reconstruction try/except, into a helper returning the suggestion object or `None` to
    signal skip. The outer colour and position loops stay in the handler. The helper's own
    try, loop and branch nest independently and must themselves stay under depth 4.

    Note the CLAUDE.md guidance that routers stay thin. If the natural home for the extracted
    helper is the corresponding service module rather than the router, put it there and say
    so in the SUMMARY. Do not, however, take that as licence to restructure the router more
    broadly: this phase is a depth fix, not a layering refactor.

    For the openings service, extract the per-bookmark loop body, including the nested row
    loop, the window check and the outcome branch chain, into a helper returning one
    bookmark's time series, called once per bookmark from the outer loop.

    Both are zero-behavior-change: identical outputs for identical inputs, including the
    skip-on-error behavior and the ordering of results.
  </action>
  <verify>
    <automated>uv run python scripts/check_function_size.py app/ --fail-over-depth 4 --fail-over-loc 200 > /tmp/216-06-gate.txt 2>&1; grep -c 'position_bookmarks.py\|openings_service.py' /tmp/216-06-gate.txt</automated>
    <fails_when>prints anything other than 0 (either function still breaches)</fails_when>
    <automated>uv run pytest tests/test_bookmarks_router.py tests/test_openings_time_series.py tests/test_aggregation_sanity.py -x -q 2>&1 | tail -5</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored test</fails_when>
    <automated>git diff --numstat -- tests/ | awk '$2 != 0 {print "MODIFIED:" $3; f=1} END {exit f?1:0}'</automated>
    <fails_when>non-zero exit, or any MODIFIED: line is printed</fails_when>
    <automated>uv run ty check app/ tests/ scripts/</automated>
    <fails_when>non-zero exit, or the output reports one or more diagnostics</fails_when>
  </verify>
  <acceptance_criteria>
    - The gate output names neither `app/routers/position_bookmarks.py` nor `app/services/openings_service.py`.
    - All three covering test files pass, unmodified.
    - The router still declares its prefix on the `APIRouter` and uses relative decorator paths, unchanged.
    - Each new helper is itself under depth 4 per the gate.
  </acceptance_criteria>
  <done>Both depth-5 breaches in the router and openings service are gone, with outputs and skip behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 4: The three import and library breaches</name>
  <files>app/services/chesscom_client.py, app/services/import_service.py, app/services/library_service.py</files>
  <read_first>
    - `app/services/chesscom_client.py` lines 370-470 — read only this range; RESEARCH.md notes the seam is in the per-month fetch loop body, which extends past its captured excerpt, so read to the end of the function
    - `app/services/import_service.py` lines 1000-1090 — read only this range, covering the target generator factory
    - `app/services/library_service.py` lines 470-620 — read only this range, covering the target card builder including the `if positions:` branch
    - `.planning/phases/216-audit-bugs-and-quick-wins/216-RESEARCH.md` "Per-function refactor seam" table, the three matching rows
    - `CLAUDE.md` "Critical Constraints" — the chess.com sequential-fetch and delay rules, and the lichess millisecond-timestamp rule, both of which the refactored code must preserve exactly
  </read_first>
  <action>
    Chess.com client: the early-return guards at the top already flatten the entry; the
    remaining depth is in the per-month fetch loop body. Extract "fetch and yield one month's
    games, handling the skip case for a missing archive" into a helper generator, mirroring
    the per-line extraction from Task 1. Preserve the sequential-with-delay fetch behavior and
    the User-Agent header handling exactly; parallelizing or dropping a delay here would be a
    behavior change disguised as a refactor.

    Import service: extract the nested since-override, previous-last-synced and
    timezone-awareness chain in the lichess branch into a helper returning the resolved
    millisecond timestamp or `None`, called once before the fetch loop. Lichess timestamps are
    milliseconds, not seconds; preserve the unit conversion exactly. Leave the chess.com
    branch's nested archive-skipped closure alone: a nested function is its own scope and does
    not contribute to the enclosing function's depth.

    Library service: extract the per-row tactic-slot computation inside the `if positions:`
    branch into a helper returning one entry, or `None` to skip, called from the row loop.
    Preserve visibility gating exactly as written; a tactic slot that becomes visible when it
    previously was not is a user-visible change.

    All three are zero-behavior-change extractions.
  </action>
  <verify>
    <automated>uv run python scripts/check_function_size.py app/ --fail-over-depth 4 --fail-over-loc 200 > /tmp/216-06-gate.txt 2>&1; grep -c 'chesscom_client.py\|import_service.py\|library_service.py' /tmp/216-06-gate.txt</automated>
    <fails_when>prints anything other than 0 (any of the three still breaches)</fails_when>
    <automated>uv run pytest tests/test_chesscom_client.py tests/test_import_service.py tests/services/test_import_service.py tests/services/test_library_service.py tests/routers/test_imports_readiness.py -x -q 2>&1 | tail -5</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored test</fails_when>
    <automated>git diff --numstat -- tests/ | awk '$2 != 0 {print "MODIFIED:" $3; f=1} END {exit f?1:0}'</automated>
    <fails_when>non-zero exit, or any MODIFIED: line is printed</fails_when>
    <automated>uv run python scripts/check_function_size.py app/ --fail-over-depth 4 --fail-over-loc 200</automated>
    <fails_when>non-zero exit, or the output lists any breach line instead of the no-breaches summary (this is the point where all eight must be clear)</fails_when>
  </verify>
  <acceptance_criteria>
    - The gate exits 0 and reports zero breaches across all of `app/`.
    - All five covering test files pass, unmodified.
    - The chess.com fetch is still sequential with its inter-request delay, and still sends the User-Agent header.
    - The lichess since value is still in milliseconds.
    - No `check-function-size` pragma exists anywhere in `app/`.
  </acceptance_criteria>
  <done>All eight D-13 breaches are fixed and the gate is clean for the first time.</done>
</task>

<task type="auto">
  <name>Task 5: Gate it in CI and in the pre-merge block, then run the full gate</name>
  <files>.github/workflows/ci.yml, CLAUDE.md, docs/dev-tooling.md</files>
  <read_first>
    - `.github/workflows/ci.yml` lines 82-100 — the ruff and ty steps, and the run-and-fail-on-nonzero-exit convention; the new step goes immediately after `Type check (ty, analysis project)`
    - `.github/workflows/ci.yml` as edited by plans 216-02 and 216-04 — confirm the `Caddyfile validate` step and the `astral-sh/setup-uv` step are present and place the new step relative to them deterministically
    - `CLAUDE.md` lines 62-75 — the mandatory pre-merge gate block, the exact bash fence the new line joins
    - `docs/dev-tooling.md` line 17 — the existing `scripts/check_function_size.py` bullet, which gains a sentence rather than a new bullet
  </read_first>
  <action>
    CI: add a step named `Function-size gate` to the test job, placed immediately after
    `Type check (ty, analysis project)` and before `Caddyfile validate`, running
    `uv run python scripts/check_function_size.py app/ --fail-over-depth 4 --fail-over-loc 200`.
    The tool's exit code gates the job, so no assertion wrapper is needed. Scope is `app/`
    only; `tests/` and `scripts/` are deliberately not gated (D-14).

    CLAUDE.md: add the identical command as a new line inside the pre-merge gate bash fence.
    Place it next to the ty lines, since it is a static check that belongs before the test
    run, and give it a short trailing comment naming what it enforces (nesting depth and
    logic LOC in `app/`). Do not restructure the block or reword the surrounding prose.

    docs/dev-tooling.md: extend the existing `scripts/check_function_size.py` bullet with one
    sentence stating that the gate now runs in CI and in the mandatory pre-merge block, that
    its scope is `app/`, and that the `allow-loc` pragma still covers LOC only and never
    depth. Do not add a second bullet.

    Then run the complete CLAUDE.md pre-merge gate, now including the new line, over the whole
    refactor. Use no em-dashes in the CLAUDE.md or docs prose.
  </action>
  <verify>
    <automated>uv run python -c "import yaml; d=yaml.safe_load(open('.github/workflows/ci.yml')); s=d['jobs']['test']['steps']; names=[x.get('name') for x in s]; i=names.index('Function-size gate'); j=names.index('Type check (ty, analysis project)'); assert i==j+1, (i,j,names); assert 'check_function_size.py app/ --fail-over-depth 4 --fail-over-loc 200' in s[i]['run'], s[i]; print('gate step ok')"</automated>
    <fails_when>non-zero exit, an AssertionError traceback, or no "gate step ok" line (the step is missing, misplaced, or carries a different command)</fails_when>
    <automated>sed -n '/^### Pre-merge gate/,/^```$/p' CLAUDE.md | grep -c 'check_function_size.py app/ --fail-over-depth 4 --fail-over-loc 200'</automated>
    <fails_when>prints anything other than 1 (the command is missing from the pre-merge gate block, or duplicated)</fails_when>
    <automated>uv run python scripts/check_function_size.py app/ --fail-over-depth 4 --fail-over-loc 200</automated>
    <fails_when>non-zero exit, or the output lists any breach line</fails_when>
    <automated>uv run ruff format --check app/ tests/ scripts/ analysis/ && uv run ruff check . && uv run ty check app/ tests/ scripts/ && uv run --project analysis --with ty ty check analysis/</automated>
    <fails_when>non-zero exit from any of the four, a file named as needing reformatting, or any lint error or type diagnostic</fails_when>
    <automated>uv run pytest -n auto -x 2>&1 | tail -6</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored test</fails_when>
    <automated>cd frontend && npm run lint && npm test -- --run 2>&1 | tail -6</automated>
    <fails_when>non-zero exit from either, or a failed test in the vitest summary</fails_when>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` parses and the `Function-size gate` step sits immediately after `Type check (ty, analysis project)` with the exact D-14 command.
    - The `CLAUDE.md` pre-merge gate bash fence contains the same command exactly once.
    - The `docs/dev-tooling.md` bullet for the script gained a sentence about CI and pre-merge enforcement, and no second bullet was added.
    - The gate exits 0 with zero breaches.
    - The full pre-merge gate, including the frontend half, passes.
    - `git diff --numstat -- tests/` still shows a deleted-lines count of 0 for every existing test file.
    - Neither the CLAUDE.md line nor the docs sentence contains an em-dash.
  </acceptance_criteria>
  <done>The rule is enforced in both places it needs to be, and the whole refactor is green against the gate that guards `main`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External chess platform APIs -> import pipeline | `lichess_client` and `chesscom_client` parse untrusted upstream payloads; the refactor moves that parsing into helpers |
| Backend service layer -> database writes | the percentile stages and library card builder write user-visible rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-216-06 | Tampering | error handling silently widened during extraction | high | mitigate | Every extraction task states that exception types, ordering and retry semantics are preserved, and the existing covering suites named in RESEARCH.md are the oracle. A per-task acceptance criterion forbids deleting a line from any existing test file, so a refactor cannot be made to pass by weakening its own test. |
| T-216-06B | Denial of service | a swallowed cancellation or a lost retry bound in the percentile stages | high | mitigate | Task 2 names cancellation re-raise as a hard requirement and Task 4 names the chess.com sequential-with-delay behavior, because both are throttling and shutdown controls whose loss the depth number would not reveal. |
| T-216-06C | Information disclosure | a widened tactic-visibility gate in the library card builder | medium | mitigate | Task 4 states that visibility gating is preserved exactly and that a newly visible slot is a user-visible change, with `tests/services/test_library_service.py` as the oracle. |
| T-216-06D | Repudiation | losing the stage label in Sentry context during the shared-helper extraction | medium | mitigate | Task 2 requires that a cell failure remain attributable to its originating stage after the refactor. |
| T-216-06E | Tampering | a future regression reintroducing a depth breach | medium | mitigate | D-14's CI step plus the pre-merge gate line make the rule fail closed, with no baseline file and no depth pragma to hide behind. |
</threat_model>

<verification>
- `uv run python scripts/check_function_size.py app/ --fail-over-depth 4 --fail-over-loc 200` exits 0 with zero breaches.
- Every covering test file named in 216-RESEARCH.md passes, all unmodified.
- `git diff --numstat -- tests/` shows zero deleted lines across existing test files.
- The full CLAUDE.md pre-merge gate, including the new line and the frontend half, is green.
- The `Function-size gate` CI step exists in the right position with the exact command.
</verification>

<success_criteria>
SC-6 is met when all eight D-13 breaches are fixed by zero-behavior-change extraction with
no baseline and no pragma, every covering suite passes unmodified, and the gate runs both as
a CI step and as a CLAUDE.md pre-merge line scoped to `app/`.
</success_criteria>

<output>
Create `.planning/phases/216-audit-bugs-and-quick-wins/216-06-SUMMARY.md` when done.
</output>
