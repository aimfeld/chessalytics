---
phase: 216-audit-bugs-and-quick-wins
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - app/core/ip_rate_limiter.py
  - tests/test_ip_rate_limiter.py
  - frontend/Dockerfile
  - frontend/src/lib/theme.ts
  - frontend/src/index.css
  - frontend/src/pages/Home.tsx
  - frontend/src/components/analysis/MaiaMoveQualityBar.tsx
  - alembic/env.py
  - .env.example
autonomous: false
requirements: []
user_setup: []

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 6
  confidence: low

must_haves:
  truths:
    - "SC-7: the shared sliding-window rate limiter deletes an IP key once its timestamp list is empty after pruning, so the map no longer grows without bound, and a test proves the deletion."
    - "SC-7: both frontend/Dockerfile base images are digest-pinned in the same image:tag@sha256 form the root Dockerfile already uses."
    - "SC-7: the four hard-coded colour literals outside the theme layer are replaced by named tokens with zero visual change, and the frontend gate stays green including knip."
    - "SC-7: alembic/env.py sets compare_type=True on both context.configure calls, and the ten no-op downgrade functions each document that they are deliberately irreversible."
    - "SC-7: the four Settings fields missing from .env.example are present, commented out, with types and defaults matching app/core/config.py."
    - "SC-7: the orphan worktree under .claude/worktrees is removed locally by the user; it is outside the repo tree and produces no commit."
    - "Spec-less probe fallback: skipped visibly — this phase has no requirement IDs and no SPEC.md/UI-SPEC.md/AI-SPEC.md, so no probe predicates were generated."
  artifacts:
    - app/core/ip_rate_limiter.py
    - tests/test_ip_rate_limiter.py
    - frontend/Dockerfile
    - frontend/src/lib/theme.ts
    - alembic/env.py
    - .env.example
  key_links:
    - "_SlidingWindowRateLimiter.is_allowed -> the one shared class that feedback_rate_limiter and reset_password_rate_limiter both import -> all three limiters fixed by one edit"
    - "frontend/Dockerfile FROM lines -> pinned manifest digests -> reproducible frontend image builds"
    - "single colour token source -> Home.tsx responsive Tailwind classes and MaiaMoveQualityBar.tsx inline style -> identical rendered colour"
    - "alembic/env.py compare_type=True -> alembic revision --autogenerate detects column type drift"
---

<objective>
Close F-16 to F-21 / SEED-161 group 7: six unrelated small defects that share only their
size. Unbounded rate-limiter key growth, unpinned frontend Docker bases, four colour
literals outside the theme layer, four undocumented Settings fields, autogenerate blind to
column type changes plus ten silently irreversible migrations, and one 97 MB orphan
worktree.

Purpose: clear the audit's long tail in one pass so none of it survives into the next
milestone.
Output: one backend behavior fix with a new test, plus five config, style and doc changes.

Spec-less probe fallback is skipped visibly: this phase carries no requirement IDs and no
SPEC/UI-SPEC/AI-SPEC exists, so no probe predicates were generated this run.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/216-audit-bugs-and-quick-wins/216-CONTEXT.md
@.planning/phases/216-audit-bugs-and-quick-wins/216-RESEARCH.md
@.planning/phases/216-audit-bugs-and-quick-wins/216-PATTERNS.md
@CLAUDE.md
@frontend/CLAUDE.md
</context>

## Artifacts this plan produces

| Kind | Name | Notes |
|------|------|-------|
| New file | `tests/test_ip_rate_limiter.py` | proves key eviction; no rate-limiter unit test exists today |
| Changed method | `_SlidingWindowRateLimiter.is_allowed` in `app/core/ip_rate_limiter.py` | the single shared implementation behind all three limiters |
| Digest pins | two `FROM` lines in `frontend/Dockerfile` | `node:24-alpine@sha256:...` and `caddy:2.11.4@sha256:...` |
| New colour token(s) | exported from `frontend/src/lib/theme.ts`, backed by a new custom property in `frontend/src/index.css` | one source of truth for the dark surface and the on-dark text colour |
| Changed config | `compare_type=True` on both `context.configure()` calls in `alembic/env.py` | |
| Doc comments | irreversibility note in ten `alembic/versions/*.py` `downgrade()` bodies | no functional change |
| New keys | four commented-out fields in `.env.example` | `BENCHMARK_HOMOGENIZE_EVAL_SOURCE`, `BENCHMARK_SELECTION_GATE_ENABLED`, `BEST_MOVE_BACKFILL_ENABLED`, `OAUTH_TUNNEL_ORIGINS` |

<tasks>

<task type="tracer">
  <name>Task 1: Rate-limiter key eviction, with a test that proves the deletion</name>
  <files>app/core/ip_rate_limiter.py, tests/test_ip_rate_limiter.py</files>
  <read_first>
    - `app/core/ip_rate_limiter.py` in full (about 50 lines) — the `_SlidingWindowRateLimiter` class, its `defaultdict[str, list[float]]` store, and the module-level `guest_create_limiter` singleton
    - `app/core/feedback_rate_limiter.py` and `app/core/reset_password_rate_limiter.py` — confirm both import the shared class and hold no limiter logic of their own, so one edit fixes all three
    - `tests/test_guest_auth.py` and `tests/test_password_reset.py` — the existing tests that exercise the limiters through the API; they are the regression oracle and must not be modified
    - `.planning/phases/216-audit-bugs-and-quick-wins/216-PATTERNS.md` "Rate limiter eviction fix" — the exact placement derivation
  </read_first>
  <action>
    In `_SlidingWindowRateLimiter.is_allowed`, after the line that prunes timestamps outside
    the window and before the count check, delete the key from the store when its pruned list
    is empty. An empty list can never satisfy the count check, so the accept path is
    unaffected, and the `defaultdict` transparently re-creates the entry on the subsequent
    append. Add a short comment at the fix site naming what leaked and why, per CLAUDE.md's
    comment-bug-fixes rule: without this, every IP that ever hit a limited endpoint stays in
    the map for the process lifetime.

    Edit only this one class. The other two limiter modules import it and need no change.

    Then create `tests/test_ip_rate_limiter.py` covering the shared class directly, on a
    fresh instance rather than the module singleton so no test leaks state into another:
    - allow up to the configured maximum, then reject the next call;
    - after the window elapses, the key's pruned list is empty and the key is gone from the internal store, then a subsequent call is allowed again and re-creates it;
    - a key that is currently within its limit is still present.

    Do not wait out a real window in the test. Construct the limiter with a tiny window, or
    drive time by seeding the internal timestamp list with values already older than the
    cutoff. Do not change the production window or maximum constants to make a test
    convenient.
  </action>
  <verify>
    <automated>uv run pytest tests/test_ip_rate_limiter.py -x -q 2>&1 | tail -5</automated>
    <fails_when>non-zero exit, the summary reporting any failed or errored test, or a passed count below 3</fails_when>
    <automated>uv run pytest tests/test_ip_rate_limiter.py tests/test_guest_auth.py tests/test_password_reset.py tests/test_guest_google_promotion.py -q 2>&1 | tail -5</automated>
    <fails_when>non-zero exit, or any failed or errored test (an existing limiter behavior regressed)</fails_when>
    <automated>uv run pytest tests/test_ip_rate_limiter.py -q --durations=3 2>&1 | tail -6</automated>
    <fails_when>the slowest listed duration exceeds 1 second (the test is sleeping out a real window instead of seeding timestamps)</fails_when>
    <automated>git diff --numstat -- tests/test_guest_auth.py tests/test_password_reset.py tests/test_guest_google_promotion.py</automated>
    <fails_when>any output is produced (an existing limiter test was modified rather than left as the oracle)</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/test_ip_rate_limiter.py` exists and contains a test asserting the key is absent from the limiter's internal store after its list prunes to empty.
    - Reverting the one-line eviction fix makes that test fail. State in the SUMMARY that this was checked, since a test that passes both with and without the fix proves nothing.
    - `app/core/feedback_rate_limiter.py` and `app/core/reset_password_rate_limiter.py` are unchanged in `git diff`.
    - The production maximum and window constants are unchanged.
    - The three existing limiter test files are unmodified and still pass.
    - A comment at the fix site explains what leaked.
  </acceptance_criteria>
  <done>All three limiters stop accumulating dead keys, and the deletion is proven by a test that fails without the fix.</done>
</task>

<task type="auto">
  <name>Task 2: Digest-pin the frontend Docker bases</name>
  <files>frontend/Dockerfile</files>
  <read_first>
    - `frontend/Dockerfile` in full (15 lines) — the two `FROM` lines and the build-arg wiring
    - `Dockerfile` lines 1-2 and 22 — the existing `image:tag@sha256:` convention, including the digest-pinned `ghcr.io/astral-sh/uv` copy source
    - `.planning/phases/216-audit-bugs-and-quick-wins/216-RESEARCH.md` "Digest pins" — the digests captured on 2026-09-04 and the instruction to re-pull before writing them
  </read_first>
  <action>
    Re-verify both digests before writing anything: `docker pull node:24-alpine` and
    `docker pull caddy:2.11.4`, and read the `Digest:` line each prints. Do not copy the
    values out of RESEARCH.md; `node:24-alpine` is a rolling tag that moves, and a stale
    digest would pin the build to an image that is no longer what the tag means.

    Rewrite both `FROM` lines in `frontend/Dockerfile` to the `image:tag@sha256:<digest>`
    form, keeping the tag as well as the digest exactly as the root `Dockerfile` does, and
    keeping the stage aliases (`AS builder`, `AS runtime`) unchanged. Change nothing else in
    the file: the `COPY` lines, the build args and the `npm ci` / `npm run build` steps stay
    as they are.

    Record both digests and the date you pulled them in the SUMMARY, so the next upgrader can
    tell how old the pin is.
  </action>
  <verify>
    <automated>grep -c '^FROM .*@sha256:' frontend/Dockerfile</automated>
    <fails_when>prints anything other than 2 (one or both base images are still unpinned)</fails_when>
    <automated>grep -E '^FROM ' frontend/Dockerfile | grep -c -E '^FROM (node:24-alpine|caddy:2\.11\.4)@sha256:[0-9a-f]{64} AS (builder|runtime)$'</automated>
    <fails_when>prints anything other than 2 (a tag, digest format or stage alias was altered)</fails_when>
    <automated>docker build -f frontend/Dockerfile -t flawchess-frontend-216-07 . 2>&1 | tail -5</automated>
    <fails_when>non-zero exit, or the output contains "manifest unknown", "not found" or any pull error (a digest was mistyped or does not exist for that tag)</fails_when>
  </verify>
  <acceptance_criteria>
    - Both `FROM` lines carry a 64-hex-character `@sha256:` digest alongside the existing tag.
    - The stage aliases `AS builder` and `AS runtime` are unchanged.
    - `git diff frontend/Dockerfile` shows only the two `FROM` lines changed.
    - A local build of the file succeeds against the pinned digests.
    - The SUMMARY records both digests and the pull date.
  </acceptance_criteria>
  <done>The frontend image builds from exactly the bases that were reviewed, not from whatever the tag points at today.</done>
</task>

<task type="auto">
  <name>Task 3: Hoist the four colour literals into the theme layer</name>
  <files>frontend/src/lib/theme.ts, frontend/src/index.css, frontend/src/pages/Home.tsx, frontend/src/components/analysis/MaiaMoveQualityBar.tsx</files>
  <read_first>
    - `frontend/src/pages/Home.tsx` lines 400-445 — the three literal usages, two of which are inside responsive Tailwind variant strings
    - `frontend/src/components/analysis/MaiaMoveQualityBar.tsx` lines 555-575 — the one usage, an inline style object with two literals on the same line
    - `frontend/src/lib/theme.ts` lines 90-100 and 580-590 — the existing token export style, including the near-black and near-white precedents
    - `frontend/src/index.css` around line 63 — the existing custom-property block and the `--charcoal` definition
    - `frontend/CLAUDE.md` "Code Style & Safety" — the theme.ts rule and the knip rule (a new export that nothing imports fails CI)
    - `.planning/phases/216-audit-bugs-and-quick-wins/216-RESEARCH.md` "Hex literal replacement" — specifically the warning that the existing charcoal custom property is a visually different colour and must not be substituted
  </read_first>
  <action>
    Read the four call sites first and note the exact current values; the goal is a pure
    hoist with zero rendered difference, not a colour change. Do not reuse the existing
    charcoal custom property: it is a visibly different colour, so substituting it would be a
    silent redesign.

    Two of the three usages on the home page sit inside responsive Tailwind variant strings,
    which cannot consume a TypeScript constant, and cannot become an inline style because a
    breakpoint variant has no inline-style equivalent. Resolve this with a single source of
    truth rather than two copies of the same value: define a new custom property in
    `frontend/src/index.css` next to the existing ones, reference it from the Tailwind
    arbitrary values on the home page, and export a `theme.ts` token whose value resolves to
    that same custom property for the inline style object in the quality bar. If you choose a
    different mechanism, the constraint stands: the literal must appear exactly once in the
    codebase after this task.

    The quality bar line carries two literals, a dark one and a white one. Both get tokens;
    the white one must keep its exact current value rather than being mapped onto the
    existing near-white token, which is a different value.

    Every new export must be imported somewhere, or knip fails the build.
  </action>
  <verify>
    <automated>grep -Eci '#1a1a1a|#ffffff' frontend/src/pages/Home.tsx frontend/src/components/analysis/MaiaMoveQualityBar.tsx</automated>
    <fails_when>either file reports a count other than 0 (a colour literal remains outside the theme layer)</fails_when>
    <automated>cd frontend && npm run lint && npm run knip</automated>
    <fails_when>non-zero exit from either, an eslint error, or a knip report of an unused export or unused dependency</fails_when>
    <automated>cd frontend && npm run build</automated>
    <fails_when>non-zero exit, or the output contains a line matching "error TS"</fails_when>
    <automated>cd frontend && npm test -- --run 2>&1 | tail -6</automated>
    <fails_when>non-zero exit, or the vitest summary reports any failed test file or failed test</fails_when>
  </verify>
  <acceptance_criteria>
    - Neither `frontend/src/pages/Home.tsx` nor `frontend/src/components/analysis/MaiaMoveQualityBar.tsx` contains a raw hex colour literal for these four sites.
    - The colour value appears exactly once in the codebase, in the theme layer.
    - The existing charcoal custom property is unchanged and was not substituted for the new one.
    - The rendered colours are byte-identical to before: the SUMMARY states how this was confirmed (value comparison at each of the four sites).
    - `npm run lint`, `npm run knip`, `npm run build` and `npm test -- --run` all pass.
  </acceptance_criteria>
  <done>The last four colour literals outside the theme layer are gone, with no visual change and no dead export.</done>
</task>

<task type="auto">
  <name>Task 4: Alembic type comparison and irreversible-downgrade documentation</name>
  <files>alembic/env.py</files>
  <precondition>The dev PostgreSQL container is running (`docker compose -f docker-compose.dev.yml -p flawchess-dev up -d`); `uv run alembic check` needs a reachable database and cannot be verified without one.</precondition>
  <read_first>
    - `alembic/env.py` lines 60-80 and 135-155 — both `context.configure()` calls, offline and online
    - `.planning/phases/216-audit-bugs-and-quick-wins/216-RESEARCH.md` "Migration housekeeping" — the confirmed list of exactly ten migrations with a no-op downgrade, reproduced independently by the researcher
    - Two or three of the listed migration files, to see the existing docstring style in this repo's migrations before writing ten comments
    - `CLAUDE.md` "Database design rules" — context for why these ten are data-repair migrations
  </read_first>
  <action>
    Add `compare_type=True` to both `context.configure()` calls in `alembic/env.py`. The
    online call is the one exercised by `alembic upgrade head` and
    `alembic revision --autogenerate`; the offline call gets it too so the two do not drift.

    Re-verify the no-op downgrade list before editing rather than trusting the captured list:
    scan `alembic/versions/` for files whose `downgrade()` body is only a bare pass, and
    confirm the count is ten. If the count differs, document the discrepancy in the SUMMARY
    and treat what you found as authoritative.

    Add a one-line docstring or comment inside each such `downgrade()` stating that the
    migration is deliberately irreversible and why, in one clause: a data-repair or cleanup
    migration cannot restore the values it overwrote. Do not add code to any `downgrade()`
    body and do not attempt to make any of them actually reversible.

    Then run `alembic check`. Enabling type comparison can surface pre-existing column type
    drift that was previously invisible. If it does, record every reported difference in the
    SUMMARY and stop there: fixing schema drift is a separate question and is not in this
    phase's scope. Flag it, do not migrate it.
  </action>
  <verify>
    <automated>grep -c 'compare_type=True' alembic/env.py</automated>
    <fails_when>prints anything other than 2 (one of the two configure calls was missed)</fails_when>
    <automated>uv run python -c "import ast,pathlib;ds=[(p.name,[f for f in ast.parse(p.read_text()).body if isinstance(f,ast.FunctionDef) and f.name=='downgrade']) for p in sorted(pathlib.Path('alembic/versions').glob('*.py'))];g=lambda f:[s for s in f.body if not(isinstance(s,ast.Expr) and isinstance(s.value,ast.Constant) and isinstance(s.value.value,str))];noop=[(n,ast.get_docstring(fs[0]) is not None) for n,fs in ds if fs and all(isinstance(s,ast.Pass) for s in g(fs[0]))];print('noop=%d documented=%d'%(len(noop),sum(1 for _,d in noop if d)));[print('UNDOCUMENTED',n) for n,d in noop if not d]"</automated>
    <fails_when>any UNDOCUMENTED line is printed, or the two counts on the "noop=" line differ from each other (a no-op downgrade still carries no docstring)</fails_when>
    <automated>uv run alembic check 2>&1 | tail -10</automated>
    <fails_when>non-zero exit for a reason other than detected schema drift, or the database is unreachable. A reported type or column difference is NOT a task failure: record every reported difference in the SUMMARY and leave it unfixed, since schema drift is out of this phase's scope.</fails_when>
    <automated>uv run alembic upgrade head 2>&1 | tail -5</automated>
    <fails_when>non-zero exit, or the output contains a traceback (env.py no longer loads with the new keyword argument)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'compare_type=True' alembic/env.py` prints 2.
    - The count of migrations with a bare-pass `downgrade()` is re-derived in this task, matches the ten reported by research (or the discrepancy is recorded), and every one of them carries a docstring.
    - No `downgrade()` body gained executable code.
    - `alembic upgrade head` still runs against the dev database.
    - Any newly surfaced type difference from `alembic check` is listed in the SUMMARY and explicitly left unfixed.
  </acceptance_criteria>
  <done>Autogenerate can see column type changes, and every deliberately irreversible migration says so in its own file.</done>
</task>

<task type="auto">
  <name>Task 5: Add the four missing Settings fields to .env.example</name>
  <files>.env.example</files>
  <precondition>The executing session can Read and Edit `.env.example`. Both the research session and the planning session were denied access to this path by a permission policy that matches `.env*`, example files included. Attempt a Read first; if it is denied, halt and ask the user for a one-time permission grant on this exact file rather than writing blind.</precondition>
  <read_first>
    - `.env.example` in full — needed to match its existing grouping, comment style and ordering; this is the whole reason for the precondition above
    - `app/core/config.py` lines 35-145 — the `Settings` class, specifically the four target fields with their annotated types and defaults
    - `.planning/phases/216-audit-bugs-and-quick-wins/216-RESEARCH.md` "`.env.example` gap" and assumption A5 — the absence claim comes from the seed and was never independently re-verified, so confirm it before adding anything
  </read_first>
  <action>
    Confirm the path is tracked (`git ls-files -- .env.example` returns it), then read the
    file. Before adding anything, grep it for each of the four field names: the claim that
    they are missing comes from the seed and was never re-verified, so a field that is
    already present must not be duplicated.

    Add the genuinely missing ones as commented-out entries, each with its default value
    taken from `app/core/config.py`, not invented: `OAUTH_TUNNEL_ORIGINS` (string, empty
    default), `BEST_MOVE_BACKFILL_ENABLED` (boolean, false default),
    `BENCHMARK_SELECTION_GATE_ENABLED` (boolean, false default) and
    `BENCHMARK_HOMOGENIZE_EVAL_SOURCE` (boolean, false default).

    Match the file's own conventions for comment prefix, spacing and grouping: place each new
    entry beside the block of related settings it belongs with rather than appending four
    orphan lines at the bottom. Give each a short comment saying what it switches, since a
    commented-out flag with no explanation is only marginally better than an absent one.

    Add no real secret values. This is an example file.
  </action>
  <verify>
    <automated>git ls-files -- .env.example</automated>
    <fails_when>prints nothing (the path is not tracked, so it is the wrong file)</fails_when>
    <automated>for k in BENCHMARK_HOMOGENIZE_EVAL_SOURCE BENCHMARK_SELECTION_GATE_ENABLED BEST_MOVE_BACKFILL_ENABLED OAUTH_TUNNEL_ORIGINS; do printf '%s=' "$k"; grep -c "$k" .env.example; done</automated>
    <fails_when>any of the four lines prints a count of 0 (the field is still absent) or a count above 1 (the field was duplicated)</fails_when>
    <automated>git diff --numstat -- .env.example</automated>
    <fails_when>the deleted-lines column is not 0 (an existing entry was removed rather than four being added)</fails_when>
  </verify>
  <acceptance_criteria>
    - `git ls-files -- .env.example` returns the path, and the file was read before being edited.
    - Each of the four field names appears exactly once in `.env.example`, commented out.
    - Each new entry's default matches `app/core/config.py` and carries a one-line explanatory comment.
    - The diff adds lines only; nothing existing was removed or reordered.
    - No real credential value was written.
  </acceptance_criteria>
  <done>A new contributor copying the example file sees every switch the application actually reads.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 6: Remove the orphan worktree</name>
  <read_first>
    - `.planning/phases/216-audit-bugs-and-quick-wins/216-CONTEXT.md` "Claude's Discretion" -> Housekeeping, the last bullet
    - `.planning/phases/216-audit-bugs-and-quick-wins/216-RESEARCH.md` "Runtime State Inventory", the OS-registered state row confirming the directory is absent from `git worktree list`
  </read_first>
  <action>Delete the orphan worktree directory under .claude/worktrees locally</action>
  <instructions>
    One 97 MB orphan worktree directory is sitting under `.claude/worktrees/`. It is not
    registered with git (`git worktree list` shows only the main repo and one other
    worktree), it is not tracked, and it is not covered by any commit, so no automated step
    in this phase can or should touch it.

    This is a local-only manual cleanup because it deletes files outside the repository tree.
    Run it yourself when you are ready:

    1. `git worktree list` and confirm `agent-a740fb7ec554451f9` does not appear.
    2. `du -sh .claude/worktrees/agent-a740fb7ec554451f9` to confirm the size before deleting.
    3. `rm -rf .claude/worktrees/agent-a740fb7ec554451f9`.
    4. `git status --porcelain` afterwards should be unchanged by the removal.

    There is nothing to commit.
  </instructions>
  <verification>`git status --porcelain` is unchanged by the removal, and `du -sh .claude/worktrees` no longer reports the orphan directory; the executor records the outcome and the reclaimed size in the plan SUMMARY.</verification>
  <resume-signal>Reply "done" once removed, or "skipped" to keep it</resume-signal>
  <acceptance_criteria>
    - The plan SUMMARY records whether the directory was removed and how much space it freed.
    - No commit in this phase references `.claude/worktrees`.
    - `git status --porcelain` is unaffected by the removal.
  </acceptance_criteria>
  <done>The orphan directory is gone (or consciously kept), with the outcome recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet -> rate-limited endpoints | Guest creation, feedback and password reset are all keyed on the client IP |
| Docker Hub -> frontend image build | Base image content enters the deployed artifact |
| Repository -> contributor environment | `.env.example` is the template a contributor copies |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-216-07 | Denial of service | unbounded key growth in `_SlidingWindowRateLimiter` | medium | mitigate | The key is deleted once its list prunes to empty, so memory is proportional to currently-active IPs rather than to every IP ever seen. A dedicated test asserts the deletion, and the acceptance criteria require confirming the test fails when the fix is reverted. |
| T-216-07B | Tampering | unpinned `node:24-alpine` and `caddy:2.11.4` frontend bases | medium | mitigate | Both bases are pinned to a manifest digest re-pulled at implementation time, matching the root `Dockerfile` convention, so the deployed frontend image is built from exactly the reviewed content. |
| T-216-07C | Information disclosure | writing a real secret into `.env.example` | high | mitigate | The task adds commented-out keys with defaults taken from `app/core/config.py` and explicitly forbids real values. The session-level permission policy that blocks `.env*` reads is treated as a precondition to satisfy rather than an obstacle to route around. |
| T-216-07D | Tampering | undetected column type drift in migrations | low | mitigate | `compare_type=True` on both configure calls makes autogenerate see type changes. Any drift it surfaces immediately is recorded and left unfixed, since acting on it silently would be an unplanned schema change. |
| T-216-07E | Repudiation | migrations that cannot be rolled back and do not say so | low | mitigate | Each of the ten no-op downgrades gains a docstring stating why it is irreversible, so an operator planning a rollback learns it from the file rather than from a failed attempt. |
</threat_model>

<verification>
- `uv run pytest tests/test_ip_rate_limiter.py tests/test_guest_auth.py tests/test_password_reset.py -x` passes, with the eviction test proven to fail when the fix is reverted.
- Both `frontend/Dockerfile` `FROM` lines carry a digest, and a local build succeeds.
- No hex colour literal remains at the four cited sites; `npm run lint`, `knip`, `build` and `test` are green.
- `grep -c 'compare_type=True' alembic/env.py` prints 2; every no-op downgrade has a docstring; `alembic upgrade head` still runs.
- All four Settings keys appear exactly once in `.env.example`.
- The orphan worktree outcome is recorded (HUMAN-UAT).
</verification>

<success_criteria>
SC-7 is met when all six housekeeping bullets are applied: key eviction with a proving test,
two digest pins, four literals hoisted with zero visual change, `compare_type=True` plus ten
documented downgrades, four `.env.example` keys, and the orphan worktree resolved.
</success_criteria>

<output>
Create `.planning/phases/216-audit-bugs-and-quick-wins/216-07-SUMMARY.md` when done.
</output>