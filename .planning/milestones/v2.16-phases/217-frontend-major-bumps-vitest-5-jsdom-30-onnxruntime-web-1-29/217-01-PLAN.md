---
phase: 217-frontend-major-bumps-vitest-5-jsdom-30-onnxruntime-web-1-29
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
autonomous: true
requirements: []
user_setup: []

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SC-1: `npm test -- --run` passes the full frontend suite with `vitest@5.x`, `@vitest/coverage-v8@5.x`, `@vitest/ui@5.x` and `jsdom@30.x` resolved, all four moved in ONE commit (D-01)."
    - "SC-1: the project-wide timeout config stays the only timeout source — `TEST_TIMEOUT_MS`/`HOOK_TIMEOUT_MS` in `frontend/vite.config.ts` and `ASYNC_UTIL_TIMEOUT_MS` in `frontend/src/vitest.setup.ts` are unchanged, and the count of per-file `}, NNNN)` literals across `frontend/src` stays at its measured pre-phase baseline of 8 (D-03)."
    - "SC-2: the `undici` entry is deleted from `frontend/package.json` `overrides`, `npm audit` reports no undici advisory afterwards, and the reason is recorded in the SUMMARY (D-02)."
    - "SC-5: the full CLAUDE.md pre-merge gate plus `npm run build` is green immediately before cluster 1's own squash-merge to `main`, which is wave 1 of two sequential waves (D-09, D-10)."
    - "D-04: a jsdom `HTMLMediaElement.play()` not-implemented warning is tolerated; a warning of that class is not a failure."
    - "D-05: if any package in the cluster cannot be made green it is pinned back to its current major with the reason recorded in the SUMMARY, rather than shipping a partially bumped cluster silently."
    - "Spec-less probe fallback: skipped visibly — this phase has no requirement IDs and no SPEC.md/UI-SPEC.md/AI-SPEC.md, so no probe predicates were generated."
  prohibitions:
    - "D-08: `scripts/package.json`'s `onnxruntime-node` pin is NOT touched in this phase — it moves with the Phase 218 parity spike."
    - "D-11: `typescript` stays on its current major; Renovate's `typescript-7.x` branch stays unmerged (no published `typescript-eslint` accepts TS >= 6.1)."
    - "D-03: no per-file vitest timeout literal is added to any test file, whatever vitest 5 changes about defaults."
    - "The frontend has no Prettier — never run `prettier --write` (it mass-reformats)."
    - "`onnxruntime-web` is NOT bumped in this plan; it belongs to wave 2 (D-09 bisectability)."
    - "The three remaining `overrides` majors (`fast-uri`, `js-yaml`, `@babel/plugin-transform-modules-systemjs`) stay untouched — deferred, out of scope."
  artifacts:
    - frontend/package.json
    - frontend/package-lock.json
  key_links:
    - "frontend/package.json devDependencies -> `npm install` -> frontend/package-lock.json -> `npm test -- --run` under the jsdom 30 environment"
    - "deleted `overrides.undici` -> jsdom@30's own `undici: ^8.9.0` range -> `npm ls undici` resolution -> `npm audit` clean"
    - "vite.config.ts `test.testTimeout`/`hookTimeout` + vitest.setup.ts `asyncUtilTimeout` -> the ONLY timeout ceiling the suite sees (no per-file literals added)"
    - "full CLAUDE.md pre-merge gate + `npm run build` -> local `git merge --squash` into `main` (no PR)"
---

<objective>
Cluster 1 of SEED-162: move `vitest`, `@vitest/coverage-v8`, `@vitest/ui` from 4.x to 5.x and
`jsdom` from 29.x to 30.x in a single commit, and resolve the `undici` override that was
explicitly deferred to "the jsdom 30 bump" by commit `bc737e2fb`. No product behavior change —
every file touched is dev tooling.

Purpose: land the test-toolchain major as one bisectable step, so that if vitest 5 (published
2026-09-03, one day before this phase was researched) turns out to be broken, exactly one commit
has to be reverted or one package pinned back.
Output: an edited `frontend/package.json`, a regenerated `frontend/package-lock.json`, a green
full gate, and one squash-merge commit on `main`.

Spec-less probe fallback is skipped visibly: this phase carries no requirement IDs and no
SPEC/UI-SPEC/AI-SPEC exists, so no probe predicates were generated this run.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/217-frontend-major-bumps-vitest-5-jsdom-30-onnxruntime-web-1-29/217-CONTEXT.md
@.planning/phases/217-frontend-major-bumps-vitest-5-jsdom-30-onnxruntime-web-1-29/217-RESEARCH.md
@.planning/phases/217-frontend-major-bumps-vitest-5-jsdom-30-onnxruntime-web-1-29/217-PATTERNS.md
@CLAUDE.md
@frontend/CLAUDE.md
</context>

## Artifacts this plan produces

| Kind | Name | Notes |
|------|------|-------|
| New symbols | none | modify-in-place only; no new file, function, constant, or export |
| Changed dependency ranges | `vitest`, `@vitest/coverage-v8`, `@vitest/ui` | `^4.1.7` -> `^5.0.0` in `frontend/package.json` devDependencies |
| Changed dependency range | `jsdom` | `^29.1.1` -> `^30.0.1` in `frontend/package.json` devDependencies |
| Deleted config entry | `overrides.undici` | the `"undici": "^7.29.0"` line is removed from `frontend/package.json` entirely |
| Regenerated file | `frontend/package-lock.json` | `npm install` output; no hand edits |
| Possible test edits | any test file that `clearMocks: true` newly exposes | unknown at plan time; triaged in Task 2, listed in the SUMMARY if they occur |

## Decision on gate scope (D-10, stated per the planner contract)

This plan runs the **full** CLAUDE.md pre-merge gate, backend commands included, even though no
backend file is touched. Reason: CLAUDE.md states the full gate runs right before each
squash-merge that integrates real work and that "a subset run is not acceptable at that point".
The backend commands are also the cheapest possible proof that a devDependency bump did not
perturb anything outside `frontend/`.

## Package legitimacy disposition (deviation recorded)

217-RESEARCH.md's Package Legitimacy Audit flags `vitest`, `undici` and `onnxruntime-web` as
`[SUS]` on the `too-new` signal only — every other identity signal (official GitHub repo, 99M+
weekly downloads, no postinstall script) is clean, and all four packages are **pre-existing
dependencies being version-bumped**, not newly introduced names. No blocking human legitimacy
checkpoint is inserted for that reason; instead Task 1 re-checks the registry identity of the
bumped packages as an automated verify. This is a deliberate, recorded deviation from the
default "[SUS] gets a blocking checkpoint" rule, not an oversight.

<tasks>

<task type="auto">
  <name>Task 1: Bump the four packages together and delete the undici override</name>
  <files>frontend/package.json, frontend/package-lock.json</files>
  <precondition>Local Node satisfies jsdom 30's `engines` floor `^22.22.2 || ^24.15.0 || >=26.0.0` — `node --version` printed `v24.19.0` on 2026-09-04. If it prints below `v24.15.0`, halt and report rather than installing.</precondition>
  <read_first>
    - `frontend/package.json` in full — the `dependencies` block (note `onnxruntime-web` is pinned exactly, and stays at its current value in this plan), the `devDependencies` block (`vitest`, `@vitest/coverage-v8`, `@vitest/ui`, `jsdom`, `typescript`, `@types/node`), and the `overrides` block (ten entries; only the `undici` line moves)
    - `frontend/vite.config.ts` lines 39-70 — the `TEST_TIMEOUT_MS`/`HOOK_TIMEOUT_MS` constants, the `test` block, and `optimizeDeps.exclude`. Read to confirm the current values; this plan does NOT edit this file (D-03).
    - `frontend/src/vitest.setup.ts` in full (18 lines) — `ASYNC_UTIL_TIMEOUT_MS`. Same: read-only for this plan.
    - `frontend/audit-ci.jsonc` in full — note `"skip-dev": true`, which is why `audit-ci` alone cannot see a dev-tree undici advisory and the verify below adds a targeted `npm audit` check.
    - `217-RESEARCH.md` sections "Standard Stack" (target versions and the peer-compat matrix), "Pattern 1: The `undici` Override", and "Pitfall 5: vitest 5.0.0 is one day old"
    - `217-PATTERNS.md` section "`frontend/package.json` + `package-lock.json`" — the `bc737e2fb` commit-message shape and the exact current values to modify
  </read_first>
  <action>
    In `frontend/package.json` devDependencies, set `vitest`, `@vitest/coverage-v8` and
    `@vitest/ui` to `^5.0.0` and `jsdom` to `^30.0.1`. All four move in this one commit per D-01:
    vitest's jsdom environment is jsdom's only test-runner consumer, so a split leaves a
    non-bisectable intermediate state.

    Delete the `"undici"` line from the `overrides` block entirely (D-02). The repo-grounded
    reason to record in the SUMMARY, verified against `frontend/package-lock.json` while planning:
    three dev-only consumers declare undici today — `jsdom@29.1.1` at `^7.25.0`, `shadcn@4.21.0`
    at `^7.27.2`, and `@dotenvx/dotenvx@1.75.1` at `^7.11.0` — while `jsdom@30.0.1` declares
    `^8.9.0`. Raising the override floor to the 8.x line would therefore force `shadcn` and
    `@dotenvx` across a major neither declares, which is exactly the anti-pattern `bc737e2fb`
    declined when it deferred this entry. Deleting lets jsdom 30 resolve 8.x naturally while the
    other two keep 7.x, and cannot bump anything ahead of jsdom. `npm audit` currently reports
    zero undici advisories in this tree, so the security floor the override existed for is no
    longer load-bearing.

    Run `npm install` from `frontend/` to regenerate `package-lock.json`. Never hand-edit the
    lockfile.

    Touch nothing else: not `typescript` (D-11), not `@types/node`, not `onnxruntime-web` (that is
    wave 2, D-09), not `scripts/package.json`'s `onnxruntime-node` (D-08), and not the other three
    `overrides` majors. Do not edit `vite.config.ts` or `vitest.setup.ts` (D-03).

    D-05 escape hatch, live for this cluster: if `npm install` itself cannot resolve a package, pin
    that one package back to its current major, record which and why in the SUMMARY, and continue
    with the rest rather than forcing it. Before debugging any install-time oddity, check
    `npm view vitest versions` for a 5.0.x patch published since 2026-09-03.
  </action>
  <verify>
    <automated>( cd frontend && npm ls vitest @vitest/coverage-v8 @vitest/ui jsdom )</automated>
    <fails_when>non-zero exit, or the output is missing any of the lines `vitest@5.`, `@vitest/coverage-v8@5.`, `@vitest/ui@5.`, `jsdom@30.`, or npm marks any of them `invalid`/`UNMET`</fails_when>
    <automated>grep -c '"undici"' frontend/package.json || true</automated>
    <fails_when>prints any number other than 0 (the override entry is still present)</fails_when>
    <automated>( cd frontend && npm ls undici )</automated>
    <fails_when>non-zero exit, or the `jsdom@30` branch of the tree shows an `undici@7.` child, or npm marks any undici entry `invalid`/`UNMET`</fails_when>
    <automated>( cd frontend && npm audit --json 2>/dev/null | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const v=JSON.parse(s).vulnerabilities||{};console.log('undici:', v.undici ? v.undici.severity : 'no advisory');});" )</automated>
    <fails_when>prints anything other than `undici: no advisory`</fails_when>
    <automated>( cd frontend && npm view vitest@5.0.0 repository.url && npm view jsdom@30.0.1 repository.url )</automated>
    <fails_when>non-zero exit, or the two printed URLs are not `github.com/vitest-dev/vitest` and `github.com/jsdom/jsdom` respectively (a registry identity change is a supply-chain signal, T-217-SC)</fails_when>
  </verify>
  <acceptance_criteria>
    - `frontend/package.json` devDependencies read `"vitest": "^5.0.0"`, `"@vitest/coverage-v8": "^5.0.0"`, `"@vitest/ui": "^5.0.0"`, `"jsdom": "^30.0.1"`.
    - `grep -c '"undici"' frontend/package.json` prints `0`.
    - `frontend/package.json` `dependencies.onnxruntime-web` is still the exact pin it was before this plan ran.
    - `frontend/vite.config.ts` and `frontend/src/vitest.setup.ts` are byte-identical to their pre-task state.
    - `frontend/package-lock.json` was regenerated by `npm install`, not edited by hand.
    - The SUMMARY records the undici decision (deleted, not raised) with the three-consumer reason above.
  </acceptance_criteria>
  <reversibility rating="reversible">A devDependency range change plus a regenerated lockfile; reverting is one `git revert` and one `npm install`.</reversibility>
  <done>All four packages resolve at their new majors, the undici override is gone, the tree resolves cleanly, and no advisory targets undici.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full suite under vitest 5 / jsdom 30 and triage what clearMocks exposes</name>
  <files>frontend/src (test files only, if triage requires an edit)</files>
  <read_first>
    - `217-RESEARCH.md` sections "Pattern 2: `clearMocks` Default Flip (Vitest 5)" and "Pitfall 3: `clearMocks` masking or unmasking test bugs" — 40 of 109 `vi.mock()`-using test files have no explicit `beforeEach` clear/reset, so they newly get automatic clearing between tests
    - `217-RESEARCH.md` "Code Examples -> Verified: jsdom 30's only breaking change" — the only jsdom 30 breaking change is the Node engines floor; no removed APIs, nothing about `HTMLMediaElement` or canvas
    - `frontend/vite.config.ts` lines 39-57 — the comment block explaining why the project-wide ceilings exist and why per-file `}, 15000)` band-aids were removed
    - `frontend/src/vitest.setup.ts` — `ASYNC_UTIL_TIMEOUT_MS`, the second and last timeout source
    - `frontend/CLAUDE.md` — frontend-only rules (no Prettier; testids; Sentry)
    - The eight pre-existing per-file timeout literals that form the baseline for the grep gate below: `frontend/src/hooks/__tests__/useGemSweep.test.ts`, `frontend/src/pages/__tests__/Train.solveLoop.test.tsx` (five matches, one of which is a comment), `frontend/src/pages/__tests__/Bots.test.tsx` (two matches)
  </read_first>
  <action>
    Run the full frontend suite. The script is `vitest run`, so `npm test -- --run` and `npm test`
    are equivalent; if vitest 5 rejects the `--run` flag next to the `run` subcommand, drop the
    flag, use `npm test`, and note it in the SUMMARY.

    Triage every failure on its merits:

    Failures whose assertion counts mock calls across sibling `it()` blocks in one file are the
    `clearMocks: true` default flip surfacing a pre-existing test-isolation gap. Fix them at the
    test — scope the state the test genuinely needs inside a single `it()`, or give that one file
    its own `beforeEach`. Do NOT add `clearMocks: false` to `vite.config.ts` to make them go away;
    that fights the new safer default across the whole future suite for a handful of files.

    A jsdom `Not implemented: HTMLMediaElement.prototype.play` warning is tolerated and is not a
    failure (D-04). A new warning of that same class is likewise not a failure.

    Never add a per-file numeric timeout literal, whatever vitest 5 changed about defaults (D-03).
    If a genuine timeout ceiling problem appears, raise `TEST_TIMEOUT_MS`/`HOOK_TIMEOUT_MS` in
    `frontend/vite.config.ts` or `ASYNC_UTIL_TIMEOUT_MS` in `frontend/src/vitest.setup.ts` — the
    two shared constants — and say so explicitly in the SUMMARY, since that is an exception to
    this plan's "config unchanged" expectation.

    D-05 escape hatch: if the suite cannot be made green in a way that is attributable to vitest 5
    itself rather than to this repo's tests, pin the offending package back to its current major,
    record the failure mode and the decision in the SUMMARY, and stop rather than shipping a
    partially bumped cluster. Check `npm view vitest versions` for a 5.0.x patch first — a failure
    mode that matches nothing in the official migration guide is more likely a fresh-release bug
    than a repo bug.

    Also note in the SUMMARY, without changing anything: `@vitest/ui@5` requires token-based auth
    for the `/__vitest__/` URL. Nothing in `package.json` or CI passes `--ui`, so this is a
    local-dev-only behavior change.
  </action>
  <verify>
    <automated>( cd frontend && npm test -- --run )</automated>
    <fails_when>non-zero exit, or the run summary reports any failed or errored test file, or reports that no test files were found</fails_when>
    <automated>grep -rn "}, [0-9]\{4,\})" frontend/src --include="*.test.ts" --include="*.test.tsx" | wc -l</automated>
    <fails_when>prints anything other than 8 — 8 is the measured pre-phase baseline of per-file timeout literals (7 real, 1 inside a comment); a higher number means a per-file timeout was re-added, which D-03 forbids</fails_when>
    <automated>grep -c 'TEST_TIMEOUT_MS = 20_000' frontend/vite.config.ts</automated>
    <fails_when>prints anything other than 1 (the shared test-timeout ceiling was changed without the SUMMARY exception this plan requires)</fails_when>
    <automated>grep -c 'ASYNC_UTIL_TIMEOUT_MS = 5000' frontend/src/vitest.setup.ts</automated>
    <fails_when>prints anything other than 1 (the shared testing-library ceiling was changed without the SUMMARY exception this plan requires)</fails_when>
  </verify>
  <acceptance_criteria>
    - The full suite exits 0 with zero failed test files under `vitest@5` and `jsdom@30`.
    - The per-file timeout literal count across `frontend/src` is exactly 8, the pre-phase baseline.
    - Both shared timeout constants still hold their pre-phase values, or the SUMMARY names the one that changed and why.
    - Every test file edited during triage is listed in the SUMMARY with the reason (which is always "this test relied on mock call history leaking between tests").
    - If D-05 was invoked, the SUMMARY names the pinned-back package, the exact failure mode, and the `npm view` check that was run first.
  </acceptance_criteria>
  <reversibility rating="reversible">Test-level fixes; each one is independently revertable and none touches production code.</reversibility>
  <done>The full frontend suite is green under the new majors, with no per-file timeout added and no shared timeout constant silently changed.</done>
</task>

<task type="auto">
  <name>Task 3: Full pre-merge gate, then cluster 1's own squash-merge to main</name>
  <files>frontend/package.json, frontend/package-lock.json</files>
  <precondition>`git status --porcelain` is empty (everything from Tasks 1-2 is committed) and `git branch --show-current` is not `main`. If either is untrue, halt and hand off rather than merging.</precondition>
  <read_first>
    - `CLAUDE.md` section "Pre-merge gate (MANDATORY before squash-merging to `main`)" — the exact seven commands, in order
    - `CLAUDE.md` section "Version Control" — `main` is the integration trunk, feature work merges back via local `git merge --squash` and the branch is deleted, never via a GitHub PR
    - `217-PATTERNS.md` section "Dependency-bump commit message shape" — the `bc737e2fb` / `d1693e05f` template: one-line summary of what moved old -> new, a paragraph on anything declined or deferred with the specific reason, then a `Gate:` line naming the commands run green
    - `frontend/audit-ci.jsonc` — `"allowlist": []` is the goal state; do not add an entry to make the gate pass
  </read_first>
  <action>
    Run the full CLAUDE.md pre-merge gate, backend commands included. CLAUDE.md is explicit that a
    subset run is not acceptable immediately before a squash-merge, and the backend half is also
    the cheapest proof that a devDependency bump perturbed nothing outside `frontend/`. Then add
    `npm run build` per D-10 — `npm test` and `npm run lint` do not type-check, because esbuild
    strips types, so the build is the only real frontend type check — and
    `npx audit-ci --config audit-ci.jsonc` from `frontend/`.

    If ruff or eslint rewrites any file, commit that separately with a `style(...)` or `chore(...)`
    prefix before merging. Never run `prettier --write`; the frontend has no Prettier and it would
    mass-reformat.

    Compose the squash commit message in the `bc737e2fb` shape: subject
    `chore(deps): bump vitest/@vitest-* to 5.x and jsdom to 30.x`, a body paragraph naming what
    moved from which version to which and recording the undici decision (deleted rather than
    raised, with the three-consumer reason from Task 1) plus anything D-05 pinned back, then a
    `Gate:` line naming the exact commands that ran green. Close with the project's standard
    commit trailers.

    Changelog decision (CLAUDE.md "Changelog is not optional"): this cluster adds NO entry to
    `CHANGELOG.md`. It changes test tooling only (vitest, jsdom, a dev-tree override); nothing a
    user can observe moves, and the Tier A dependency chores `bc737e2fb` / `d1693e05f` set the same
    precedent. State that decision in one line of the squash-merge commit body so the omission is
    recorded, not silent. Cluster 2 (217-02) DOES add an entry because it has a user-visible cost.

    Merge locally, no PR: from the phase branch, `git checkout main`, `git merge --squash <branch>`,
    commit with that message, then delete the phase branch. Deleting it matters — the next wave's
    executor gets a fresh branch cut from the new `main`, which is what keeps wave 2's diff
    reviewable on its own.

    Expect one piece of GSD machinery noise: after this task, the SUMMARY commit may land on a
    freshly recreated `gsd/phase-217` branch rather than on `main`. That is normal. Check
    `git branch --show-current` before reporting where anything landed, and never force a merge
    from a dirty tree or an unexpected branch — halt and hand off instead.
  </action>
  <verify>
    <automated>uv run ruff format --check app/ tests/ scripts/ analysis/ && uv run ruff check .</automated>
    <fails_when>non-zero exit from either command, or either names a file that would be reformatted or reports a remaining lint error</fails_when>
    <automated>uv run ty check app/ tests/ scripts/ && uv run --project analysis --with ty ty check analysis/</automated>
    <fails_when>non-zero exit, or either invocation reports one or more diagnostics</fails_when>
    <automated>uv run python scripts/check_function_size.py app/ --fail-over-depth 4 --fail-over-loc 200</automated>
    <fails_when>non-zero exit, or the output names any function over the depth or LOC limit</fails_when>
    <automated>uv run pytest -n auto -x 2>&1 | tail -6</automated>
    <fails_when>the summary line reports any failed or errored test, or reports that no tests ran</fails_when>
    <automated>( cd frontend && npm run lint && npm test -- --run && npm run build )</automated>
    <fails_when>non-zero exit from any of the three, or `tsc -b` reports any type error</fails_when>
    <automated>( cd frontend && npx audit-ci --config audit-ci.jsonc )</automated>
    <fails_when>non-zero exit, or the output reports any high or critical advisory in a production dependency</fails_when>
    <automated>git status --porcelain</automated>
    <fails_when>prints any line (the tree is dirty, so the squash-merge did not complete cleanly)</fails_when>
    <automated>git log -1 --pretty=%s</automated>
    <fails_when>the printed subject does not start with `chore(deps): ` (the squash commit was not created, or not in the `bc737e2fb` shape)</fails_when>
  </verify>
  <acceptance_criteria>
    - Every command in the CLAUDE.md pre-merge gate ran, in addition to `npm run build` and `npx audit-ci --config audit-ci.jsonc`, and all exited 0.
    - `frontend/audit-ci.jsonc` `allowlist` is still `[]` — no entry was added to make the gate pass.
    - `main` carries exactly one new commit for cluster 1, whose subject starts with `chore(deps): ` and whose body records the undici decision and any D-05 pin-back.
    - The phase branch used for wave 1 no longer exists after the merge.
    - The SUMMARY states which branch the executor was on when it finished, and the CI-resolved Node version is noted if a CI run was observed (jsdom 30 needs `>= 24.15.0`; `actions/setup-node` pins only the major).
  </acceptance_criteria>
  <reversibility rating="reversible">One squash commit on `main`; `git revert` plus `npm install` restores the previous toolchain exactly.</reversibility>
  <done>The full gate is green and cluster 1 is on `main` as a single revertable commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> `frontend/node_modules` | Untrusted third-party code enters the developer and CI machines at install time |
| `frontend/package.json` `overrides` -> the resolved dependency tree | A hand-written floor silently rewrites what every consumer gets, including consumers that never declared that range |
| dev toolchain -> shipped bundle | vitest, jsdom and undici are dev-only and must never reach `dist/`; `audit-ci` runs with `skip-dev: true` precisely on that assumption |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-217-SC | Tampering | `npm install` of `vitest@5`, `@vitest/coverage-v8@5`, `@vitest/ui@5`, `jsdom@30` | high | mitigate | All four are pre-existing dependencies being version-bumped, not new names. 217-RESEARCH.md's legitimacy audit resolved each to its official GitHub repo with 98M+ weekly downloads and no postinstall script; the `[SUS]` flags are `too-new` freshness only. Task 1 re-checks `repository.url` for `vitest@5.0.0` and `jsdom@30.0.1` as an automated verify, the lockfile is regenerated (never hand-edited) so CI's `npm ci` installs exactly what was resolved locally, and `npx audit-ci --config audit-ci.jsonc` runs in the Task 3 gate. |
| T-217-01 | Tampering | deleting `overrides.undici` reintroduces a vulnerable transitive | medium | mitigate | `npm audit` reports zero undici advisories in this tree today, and jsdom 30 pulls `undici ^8.9.0`, strictly newer than the deleted `^7.29.0` floor. Task 1 verifies with a targeted `npm audit --json` filter on `undici` after the install, because `audit-ci.jsonc` sets `skip-dev: true` and therefore cannot see a dev-tree advisory at all. If a live advisory appears, the override is re-added at whatever range clears it. |
| T-217-02 | Tampering | raising `overrides.undici` to the 8.x line instead of deleting it | medium | mitigate | Rejected by design: `shadcn@4.21.0` and `@dotenvx/dotenvx@1.75.1` declare `undici ^7.x`, so an `^8.9.0` floor would force two dev tools across an undeclared major through a mechanism that exists only as a security floor. D-02 forbids bumping ahead of jsdom; deletion cannot. |
| T-217-03 | Denial of service | a fresh-release regression in `vitest@5.0.0` (published one day before research) blocking the suite | low | accept | Accepted with a named exit: D-05's pin-back escape hatch, invoked with the reason recorded in the SUMMARY. The blast radius is dev tooling only; nothing user-facing ships from this plan. |
</threat_model>

<verification>
- `( cd frontend && npm ls vitest @vitest/coverage-v8 @vitest/ui jsdom )` shows all four at their new majors with no `invalid`/`UNMET` marker.
- `grep -c '"undici"' frontend/package.json` prints `0`, and the targeted `npm audit` filter prints `undici: no advisory`.
- `( cd frontend && npm test -- --run )` is green across the full suite.
- The per-file timeout literal count across `frontend/src` is exactly 8; both shared timeout constants are unchanged.
- The full CLAUDE.md pre-merge gate plus `npm run build` and `npx audit-ci --config audit-ci.jsonc` all exit 0.
- `main` carries one `chore(deps): ` squash commit for cluster 1 and the working tree is clean.
</verification>

<success_criteria>
SC-1, SC-2 and cluster 1's half of SC-5 are met when `vitest@5.x` / `@vitest/coverage-v8@5.x` /
`@vitest/ui@5.x` / `jsdom@30.x` resolve, the full suite passes under jsdom 30 with the shared
timeout config still the only timeout source, the `undici` override is resolved with the reason
recorded, and the whole cluster sits on `main` as one squash commit behind a green full gate.
</success_criteria>

<output>
Create `.planning/phases/217-frontend-major-bumps-vitest-5-jsdom-30-onnxruntime-web-1-29/217-01-SUMMARY.md` when done.
</output>
