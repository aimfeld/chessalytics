---
phase: 218-backend-onnxruntime-parity-spike-python-3-14-chain
plan: 02
type: execute
wave: 2
depends_on: ["218-01"]
files_modified:
  - pyproject.toml
  - uv.lock
  - scripts/package.json
  - scripts/package-lock.json
  - .python-version
  - analysis/pyproject.toml
  - analysis/uv.lock
  - Dockerfile
  - Dockerfile.worker
  - .github/workflows/ci.yml
  - CLAUDE.md
  - README.md
autonomous: true
requirements: []

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "SC-2: `onnxruntime` resolves at 1.29.0 in the `maia-inference` group and `onnxruntime-node` resolves at 1.29.0 in `scripts/`, both still pinned exactly rather than as a range."
    - "SC-2: the standing parity regression test actually RUNS (not skips) against the newly pinned runtime, in the same environment the CI job uses."
    - "SC-2: `.python-version`, `pyproject.toml` `requires-python`, `analysis/pyproject.toml` `requires-python`, every `FROM` stage in both Dockerfiles, and the CI `setup-python` step all name Python 3.14 — and they moved in ONE commit, because a partial bump means CI tests one interpreter while the images build another."
    - "SC-2: `ghcr.io/astral-sh/uv` is re-pinned to 0.12.x by index digest in both Dockerfiles, with the digest obtained from `docker buildx imagetools inspect` at execution time rather than copied from any document."
    - "SC-2: both `uv.lock` and `analysis/uv.lock` are regenerated so `uv lock --check` passes for both projects and `uv sync --locked` inside the images has something valid to install."
    - "SC-2: BOTH container images build locally — CI builds only the API image, so the worker image's 3.14 build is otherwise entirely unproven (RESEARCH.md Pitfall 3)."
    - "Every `FROM` line in both Dockerfiles is still digest-pinned; the re-pin re-derives digests, it never regresses to a floating tag."
    - "Spec-less probe fallback: skipped visibly — this phase has no requirement IDs and no SPEC.md/UI-SPEC.md/AI-SPEC.md exists, so no probe predicates were generated this run."
  prohibitions:
    - statement: "`Dockerfile.worker` is never moved to Python 3.14 in a commit that does not also move `Dockerfile`. Splitting the interpreter across the fleet buys nothing and costs a debugging dimension (SEED-162 cluster 4, ROADMAP success criterion 4)."
      status: enforced
      verification: "Task 2 verify gates on `git status --porcelain -- Dockerfile Dockerfile.worker` printing two lines before the commit; 218-03 re-checks it post-merge with `git show --stat` on the most recent commit touching the worker image."
    - statement: "The two Dockerfiles never carry different `python:` or `ghcr.io/astral-sh/uv:` references. Same tags, same digests, same commit."
      status: enforced
      verification: "Task 2 verify diffs the extracted image references between the two files and requires them identical, plus a count check so two empty extractions cannot pass."
    - statement: "No digest is copied out of prose — not from 218-RESEARCH.md, not from this plan, not from a chat message, not from a `docker pull` log line (which prints a per-platform manifest digest, not the index digest this pin format uses)."
      status: enforced
      verification: "Task 2's action names the exact `docker buildx imagetools inspect` command as the only source; the SUMMARY records the two digests it printed."
    - statement: "Nothing under `reports/quality-assessment/` is edited. Those are dated point-in-time snapshots; rewriting their Python version would falsify history."
      status: enforced
      verification: "Task 2 verify gates on `git status --porcelain -- reports/` being empty."
    - statement: "This plan does not run the full pre-merge gate, write a CHANGELOG entry, merge to `main`, or deploy. Those are 218-03, behind their own gate."
      status: enforced
      verification: "`files_modified` contains no `CHANGELOG.md`; 218-03 owns the release."
  artifacts:
    - pyproject.toml
    - uv.lock
    - scripts/package.json
    - scripts/package-lock.json
    - .python-version
    - analysis/pyproject.toml
    - analysis/uv.lock
    - Dockerfile
    - Dockerfile.worker
    - .github/workflows/ci.yml
    - CLAUDE.md
    - README.md
  key_links:
    - "`pyproject.toml` `maia-inference` pin -> `uv.lock` -> `uv sync --locked --group maia-inference` in `Dockerfile`'s builder stage AND in `.github/workflows/ci.yml` -> the FastAPI process's `InferenceSession`"
    - "`scripts/package.json` pin -> `scripts/package-lock.json` -> `requireFromScripts('onnxruntime-node')` -> `verify_value_head.mjs`"
    - "`.python-version` + `requires-python` -> `uv lock` resolution fork -> `uv sync --locked` inside both images -> whether a cp314 wheel exists for every locked package"
    - "`docker buildx imagetools inspect` index digest -> the `FROM`/`COPY --from=` pins in BOTH Dockerfiles -> reproducible builds on the API host and the remote worker fleet"
    - "`Dockerfile.worker` local build -> the ONLY proof the worker image survives the interpreter bump; CI has zero steps referencing that file"
---

<objective>
The PASS branch of the chain: raise both native onnxruntime pins to the measured-safe version, then
move the entire backend to Python 3.14 in one commit — every `requires-python`, every Dockerfile
stage across both images, CI, both lockfiles — and re-pin the `uv` base image by digest while the
Dockerfiles are open. Two commits, in that order, so a bisect can separate "the runtime pin moved"
from "the interpreter moved".

Purpose: onnxruntime 1.20.1 ships no cp314 wheel, so the language bump was never independently
schedulable — it is gated on the pin, and the pin was gated on 218-01's measurement. With that
answered, the remaining risk is entirely mechanical: a location missed, a lockfile not
regenerated, an image that CI never builds, or two Dockerfiles drifting apart.

Output: `onnxruntime` and `onnxruntime-node` at 1.29.0, Python 3.14 everywhere, `uv` re-pinned by
index digest in both images, both lockfiles regenerated, and both container images proven to build
locally.

Spec-less probe fallback is skipped visibly: this phase carries no requirement IDs and no
SPEC/UI-SPEC/AI-SPEC exists, so no probe predicates were generated this run.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/218-backend-onnxruntime-parity-spike-python-3-14-chain/218-RESEARCH.md
@.planning/phases/218-backend-onnxruntime-parity-spike-python-3-14-chain/218-PATTERNS.md
@.planning/phases/218-backend-onnxruntime-parity-spike-python-3-14-chain/218-01-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this plan produces

| Kind | Name | Notes |
|------|------|-------|
| Changed pin | `onnxruntime` in `pyproject.toml` `maia-inference` | raised to `1.29.0`, exact pin, no range; comment rewritten to carry both datapoints and both evidence paths |
| Changed pin | `onnxruntime-node` in `scripts/package.json` | raised to `1.29.0`; description rewritten the same way. In scope here per Phase 217 D-08, which deferred this bump TO Phase 218 |
| Regenerated lock | `uv.lock` | new `requires-python` floor + new onnxruntime resolution |
| Regenerated lock | `scripts/package-lock.json` | matches the raised Node pin |
| Regenerated lock | `analysis/uv.lock` | new `requires-python` floor for the standalone analysis project |
| Changed version | `.python-version` | `3.14` |
| Changed version | `requires-python` in `pyproject.toml` and `analysis/pyproject.toml` | `>=3.14` in both |
| Changed base images | `Dockerfile` (2 `FROM` stages), `Dockerfile.worker` (3 `FROM` stages) | `python:3.14-slim@sha256:<index digest>`, identical digest in all five |
| Changed base images | the `COPY --from=ghcr.io/astral-sh/uv:` line in both Dockerfiles | `0.12.x@sha256:<index digest>`, identical in both |
| Changed CI input | `python-version` in the `actions/setup-python@v7` step of `.github/workflows/ci.yml` | `"3.14"` |
| Changed docs | `CLAUDE.md` backend stack line, `README.md` badge / stack table / prerequisites | version-currency only; RESEARCH.md assumption A3 |
| New symbols | none | no new file, function, constant, endpoint or export |
| Not produced | any edit under `reports/quality-assessment/` | dated historical snapshots; see `prohibitions` |
| Not produced | a CHANGELOG entry, a merge, or a deploy | 218-03 owns those |

<!-- planner-discipline-allow: 3\.13 -->

## Version currency

218-RESEARCH.md's version numbers were re-verified at plan time (2026-09-05) against live
registries: `onnxruntime==1.29.0` publishes cp314 manylinux wheels for x86_64 and aarch64;
`onnxruntime-node@1.29.0` is the npm `latest` dist-tag (not a `-dev` prerelease); the newest `uv`
release is `0.12.10`; `uv python list` offers `cpython-3.14.3-linux-x86_64-gnu` for download. The
`uv` line moves fast — Task 2 re-checks it again at execution time rather than trusting this
paragraph.

<tasks>

<task type="auto">
  <name>Task 1: Raise both native onnxruntime pins to 1.29.0 and prove the parity gate runs against them</name>
  <precondition>218-01's checkpoint was answered `proceed`: `218-evidence-onnxruntime-1.29.0-python.txt` contains `PARITY GATE PASSED` with `exit_code=0`, and `218-evidence-onnxruntime-node-1.29.0.txt` contains `All value-head checks passed.` with `exit_code=0`. If either is absent, halt the entire plan — the FAIL branch ends the phase at 218-01 with no version file touched.</precondition>
  <precondition>The dev PostgreSQL container is running (`docker compose -f docker-compose.dev.yml -p flawchess-dev up -d`) — `tests/conftest.py` clones its test database from a migrated template and cannot run without it.</precondition>
  <reversibility rating="costly">Moves the pin that guards a production inference path in the FastAPI process and in the headless harnesses. Undo is one `git revert` while the work is only on the phase branch, but every downstream task in the phase assumes it, and 218-01's measurement would have to be re-read to justify re-doing it.</reversibility>
  <files>pyproject.toml, uv.lock, scripts/package.json, scripts/package-lock.json</files>
  <read_first>
    - `.planning/phases/218-backend-onnxruntime-parity-spike-python-3-14-chain/218-01-SUMMARY.md` — the recorded verdict and the measured drift numbers this task quotes
    - The two candidate evidence files named in the precondition
    - `pyproject.toml` lines 38-49 — the `maia-inference` group, kept out of `[project.dependencies]` on purpose so the lean worker image never pulls it, and its pin comment
    - `scripts/package.json` in full — one JSON object whose `description` carries the pin rationale
    - `scripts/maia_parity_spike.py` lines 55-70 as updated by 218-01 — both dated measurement paragraphs, which the pin comments now summarize
    - `tests/services/test_maia_parity.py` lines 1-35 — the `pytest.importorskip("onnxruntime")` pair at module scope, which is why a green suite and a suite that actually exercised parity are different claims (RESEARCH.md Pitfall 4)
    - `.github/workflows/ci.yml` line 49 — `uv sync --locked --group maia-inference`, the reason the parity test is NOT skipped in CI
  </read_first>
  <action>
    Raise the Python pin: in `pyproject.toml`'s `[dependency-groups] maia-inference`, set the pinned
    string to `"onnxruntime==1.29.0"`. Keep it an exact pin — no `>=`, no `~=`, no caret, no upper
    bound trick. Rewrite the comment above it so it still commands the same discipline for the NEXT
    bump (pin exactly, never a range; any future bump MUST re-run `scripts/maia_parity_spike.py`
    before merging) and now carries both datapoints: the original 2026-07-16 measurement at 1.20.1
    and 218-01's measurement at 1.29.0, each with its evidence path. The historical fact that
    versions between the old pin and this one crashed on the vendored model stays in the comment —
    it is the reason the exact-pin rule exists, and deleting it would strip the next reader of the
    rationale.

    Raise the Node pin: set `dependencies.onnxruntime-node` in `scripts/package.json` to `1.29.0`
    and rewrite its `description` the same way, keeping the sentence that any bump must survive
    `scripts/engine_disagreement_study/verify_value_head.mjs` first, and citing 218-01's Node
    evidence file. This bump is in scope for this phase: Phase 217's D-08 deferred it TO here
    precisely because it is the same native core answered by the same measurement.

    Regenerate both lockfiles: `uv lock` for the Python project, `npm --prefix scripts install` for
    the Node harness. Do not hand-edit either lockfile.

    Install and exercise: `uv sync --group maia-inference`, then run the standing parity regression
    test and confirm it RUNS rather than skips. A skipped module reports as success at the suite
    level and proves nothing (Pitfall 4) — read the per-test status, not just the exit code. Then
    re-run the Node harness against the now-committed install, so the harness result is attributable
    to the manifest rather than to 218-01's throwaway install.

    Touch no Python-version file in this task. `.python-version`, both `requires-python` values,
    both Dockerfiles and `ci.yml` belong to Task 2's single commit, and keeping them out of this one
    is what makes the two commits independently bisectable.
  </action>
  <verify>
    <automated>uv run --group maia-inference python -c "import onnxruntime; print(onnxruntime.__version__)"</automated>
    <fails_when>non-zero exit (including a native crash at import, which exits 139), or it prints any version other than `1.29.0`</fails_when>
    <automated>uv lock --check</automated>
    <fails_when>non-zero exit — `uv.lock` no longer matches `pyproject.toml`, so `uv sync --locked` in the images and in CI would fail</fails_when>
    <automated>( cd scripts && npm ls onnxruntime-node )</automated>
    <fails_when>non-zero exit, or the output lacks the line `onnxruntime-node@1.29.0`, or npm marks the entry `invalid` / `UNMET`</fails_when>
    <automated>uv run --group maia-inference pytest tests/services/test_maia_parity.py -v</automated>
    <fails_when>non-zero exit, or the output contains `SKIPPED` or `no tests ran` — the importorskip guard swallowed the module and the parity gate did not actually execute</fails_when>
    <automated>node --import ./scripts/lib/frontend-alias-hook.mjs scripts/engine_disagreement_study/verify_value_head.mjs</automated>
    <fails_when>non-zero exit, any line beginning `FAIL`, or a missing `All value-head checks passed.` line</fails_when>
    <automated>git status --porcelain -- .python-version Dockerfile Dockerfile.worker .github/workflows/ci.yml analysis/pyproject.toml analysis/uv.lock</automated>
    <fails_when>any line is printed — a Python-version file was modified in this commit instead of Task 2's, which destroys the two commits' bisectability</fails_when>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` contains the exact string `"onnxruntime==1.29.0"` and no range operator on that entry.
    - The `maia-inference` comment names both measurements with their dates and evidence paths, and still contains the exact-pin imperative and the requirement to re-run the spike before any future bump.
    - `scripts/package.json` parses as JSON and pins `onnxruntime-node` at `1.29.0`; its description names 218-01's Node evidence path and retains the `verify_value_head.mjs` requirement.
    - `uv lock --check` exits 0 and `npm ls onnxruntime-node` reports `onnxruntime-node@1.29.0`.
    - `pytest tests/services/test_maia_parity.py -v` shows the parametrized parity test PASSING, with zero `SKIPPED` lines.
    - `verify_value_head.mjs` prints five `PASS` lines and `All value-head checks passed.`, exit 0, from the manifest-driven install.
    - `git status --porcelain` over `.python-version`, both Dockerfiles, `ci.yml`, `analysis/pyproject.toml` and `analysis/uv.lock` prints nothing.
    - The SUMMARY records the parity test's PASSED/SKIPPED status verbatim, not just the suite exit code.
  </acceptance_criteria>
  <done>Both native onnxruntime pins sit at 1.29.0 with regenerated lockfiles, and the standing parity gate has been observed running (not skipping) against them.</done>
</task>

<task type="auto">
  <name>Task 2: Move the backend to Python 3.14 everywhere in one commit, with both base images re-pinned by index digest</name>
  <precondition>Task 1's commit exists on the phase branch: `uv lock --check` exits 0 and `onnxruntime.__version__` reports 1.29.0 — the interpreter bump cannot resolve while the old pin, which ships no cp314 wheel, is still in `pyproject.toml`.</precondition>
  <precondition>`docker` and `docker buildx` are available and can reach `docker.io` and `ghcr.io` (`docker buildx imagetools inspect python:3.14-slim` succeeds) — both digests must be derived live, and both images must actually build.</precondition>
  <reversibility rating="costly">Raises the language floor for every build path at once: the local venv, CI, the API image and the independently-deployed remote worker fleet image. Undo is a single `git revert` of this one commit while it is only on the phase branch; once 218-03 releases it, undo means a rollback deploy of the worker fleet as well as the API.</reversibility>
  <files>.python-version, pyproject.toml, uv.lock, analysis/pyproject.toml, analysis/uv.lock, Dockerfile, Dockerfile.worker, .github/workflows/ci.yml, CLAUDE.md, README.md</files>
  <read_first>
    - `218-RESEARCH.md` section "Where Python 3.13 appears (must-change vs docs-only)" — the authoritative per-file, per-line classification, including the four docs-only rows and the "DO NOT EDIT" row for `reports/quality-assessment/`
    - `218-RESEARCH.md` Pitfall 3 — `.github/workflows/ci.yml` has zero steps referencing `Dockerfile.worker`; CI builds only `flawchess:ci` from the root `Dockerfile`
    - `218-PATTERNS.md` section "`Dockerfile` and `Dockerfile.worker` — Python base image + `uv` digest re-pin" — the `<tag>@sha256:<index-digest>` pin format and the git-history precedent that introduced it
    - `Dockerfile` lines 1-2 and 22 — the builder and runtime stages, and the `uv sync --locked --no-dev --group maia-inference --group push` line that consumes the regenerated lock
    - `Dockerfile.worker` lines 18-20, 34 and 68 — the header comment declaring the builder stage identical to `./Dockerfile`'s, and the three stages (builder, stockfish, runtime)
    - `.github/workflows/ci.yml` lines 35-49 — the `actions/setup-python@v7` step and the `uv sync --locked --group maia-inference` step below it
    - `pyproject.toml` line 5 and `analysis/pyproject.toml` line 5 — `requires-python = ">=3.13"` in both
    - `uv.lock` lines 1-7 — `revision = 3`, the `requires-python` header, and the pre-existing `resolution-markers` fork at the 3.14 boundary, which is a `uv` default behavior and NOT evidence the tree already resolves under 3.14
    - `CLAUDE.md` line 24 (backend stack line) and `README.md` lines 20, 51, 221 (badge, stack table, prerequisites)
  </read_first>
  <action>
    One commit, every location. A partial bump is worse than none: CI would test one interpreter
    while the images build another.

    Set `.python-version` to `3.14`. Set `requires-python = ">=3.14"` in both `pyproject.toml` and
    `analysis/pyproject.toml`. Set `python-version: "3.14"` in the `actions/setup-python@v7` step of
    `.github/workflows/ci.yml` — the `uv sync --locked --group maia-inference` step below it needs
    no change, since the regenerated lock is what makes it resolve.

    Derive both image digests live, in this order, and record what they print:
    `docker buildx imagetools inspect python:3.14-slim` and
    `docker buildx imagetools inspect ghcr.io/astral-sh/uv:<latest 0.12.x tag>`. The top `Digest:`
    line each prints is the OCI image INDEX digest, which is exactly the format both Dockerfiles
    already use. Never take a digest from a `docker pull` log line (that is a per-platform manifest
    digest) and never copy one out of a document, this plan included. Re-check the newest 0.12.x tag
    first with
    `curl -s "https://api.github.com/repos/astral-sh/uv/releases?per_page=10" | grep '"tag_name"'`;
    at plan time the newest was `0.12.10`, and that line ships roughly weekly — if a newer 0.12.x
    exists, use it and say so in the SUMMARY.

    Rewrite all five `FROM` lines — two in `Dockerfile`, three in `Dockerfile.worker` — to
    `FROM python:3.14-slim@sha256:<the index digest you just printed>`, preserving each line's
    existing `AS <stage>` suffix. Rewrite the single `COPY --from=ghcr.io/astral-sh/uv:...` line in
    each file to the new tag and digest. All five Python references carry the identical digest; both
    `uv` references carry the identical digest. The worker file's own header comment declares its
    builder stage identical to the API image's — that coupling is the contract, and both files land
    in this same commit. Never a commit that moves one image's interpreter without the other.

    Regenerate both lockfiles under the new floor: `uv python install 3.14`, then `uv lock`, then
    `( cd analysis && uv lock )`. If a package fails to resolve, name the exact package and version
    in the SUMMARY and stop — that is a real blocker, not something to work around by loosening
    `requires-python`.

    Build both images locally: `docker build -t flawchess:314-test .` and
    `docker build -f Dockerfile.worker -t flawchess-worker:314-test .`. The second one matters most:
    CI has no step that builds the worker image at all, so this local build is the only thing
    standing between a broken interpreter bump and a remote worker box. If a build fails inside
    `uv sync --locked` with a lockfile-format complaint, the cause is a lock written by an older
    local `uv` than the one in the image — regenerate with the matching version
    (`uvx uv@<the pinned tag> lock`) rather than dropping `--locked`.

    Update the two docs sites for version currency: the backend stack line in `CLAUDE.md` and the
    badge, stack table and prerequisites in `README.md`. Leave everything under
    `reports/quality-assessment/` alone — those are dated point-in-time snapshots and editing them
    would falsify history.
  </action>
  <verify>
    <automated>grep -rn '3\.13' .python-version pyproject.toml analysis/pyproject.toml Dockerfile Dockerfile.worker .github/workflows/ci.yml</automated>
    <fails_when>any line is printed (grep exits 0) — a must-change file still names the outgoing interpreter minor. A clean run prints nothing and exits 1</fails_when>
    <automated>grep -c 'python:3.14-slim@sha256:' Dockerfile Dockerfile.worker</automated>
    <fails_when>the output is anything other than exactly `Dockerfile:2` and `Dockerfile.worker:3` — a stage was missed or a stage was duplicated</fails_when>
    <automated>for f in Dockerfile Dockerfile.worker; do a=$(grep -c '^FROM ' "$f"); b=$(grep -c '^FROM .*@sha256:' "$f"); if [ "$a" = "$b" ]; then echo "PINNED $f $a"; else echo "UNPINNED $f $a vs $b"; fi; done</automated>
    <fails_when>any line begins `UNPINNED`, or fewer than two lines print — a `FROM` regressed to a floating tag, dropping the supply-chain control the project already had</fails_when>
    <automated>diff <(grep -o 'python:3.14-slim@sha256:[0-9a-f]*' Dockerfile | sort -u) <(grep -o 'python:3.14-slim@sha256:[0-9a-f]*' Dockerfile.worker | sort -u)</automated>
    <fails_when>non-zero exit or any `&lt;` / `&gt;` line — the two images point at different Python base digests, which is exactly the divergence success criterion 4 forbids</fails_when>
    <automated>diff <(grep -o 'ghcr.io/astral-sh/uv:[^ ]*' Dockerfile | sort -u) <(grep -o 'ghcr.io/astral-sh/uv:[^ ]*' Dockerfile.worker | sort -u)</automated>
    <fails_when>non-zero exit or any differing line — the two images would install different `uv` builds; also fails if both sides are empty, which the preceding count check catches</fails_when>
    <automated>grep -n 'requires-python' pyproject.toml analysis/pyproject.toml uv.lock analysis/uv.lock</automated>
    <fails_when>fewer than four lines print, or any printed line does not contain `>=3.14` — a lockfile was not regenerated under the new floor</fails_when>
    <automated>uv lock --check</automated>
    <fails_when>non-zero exit — `uv.lock` does not match `pyproject.toml`</fails_when>
    <automated>( cd analysis && uv lock --check )</automated>
    <fails_when>non-zero exit — `analysis/uv.lock` does not match `analysis/pyproject.toml`</fails_when>
    <automated>grep -n 'python-version' .github/workflows/ci.yml</automated>
    <fails_when>the `actions/setup-python` input does not read `python-version: "3.14"`</fails_when>
    <automated>docker build -t flawchess:314-test .</automated>
    <fails_when>non-zero exit — typically `uv sync --locked` rejecting the regenerated lock, or a dependency with no cp314 wheel failing to build from sdist inside the builder stage</fails_when>
    <automated>docker build -f Dockerfile.worker -t flawchess-worker:314-test .</automated>
    <fails_when>non-zero exit. This is the only build check the worker image gets anywhere — CI never builds it, so a failure here would otherwise surface first on a remote worker box</fails_when>
    <automated>git status --porcelain -- Dockerfile Dockerfile.worker</automated>
    <fails_when>fewer than two lines print — one image's interpreter is moving without the other, which success criterion 4 forbids</fails_when>
    <automated>git status --porcelain -- reports/</automated>
    <fails_when>any line is printed — a dated historical quality-assessment snapshot was edited</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -rn '3\.13'` over `.python-version`, `pyproject.toml`, `analysis/pyproject.toml`, `Dockerfile`, `Dockerfile.worker` and `.github/workflows/ci.yml` prints nothing.
    - `.python-version` reads `3.14`; both `requires-python` values read `>=3.14`; the CI input reads `python-version: "3.14"`.
    - `Dockerfile` has exactly 2 and `Dockerfile.worker` exactly 3 `python:3.14-slim@sha256:` references, all carrying one identical digest, and every `FROM` line in both files is digest-pinned.
    - Both files carry one identical `ghcr.io/astral-sh/uv:0.12.x@sha256:` reference, and the SUMMARY records the exact tag used plus both digests as printed by `docker buildx imagetools inspect`.
    - `uv lock --check` exits 0 for the root project and for `analysis/`; both lockfile headers read `requires-python = ">=3.14"`.
    - `docker build .` and `docker build -f Dockerfile.worker .` both exit 0.
    - `git status --porcelain -- Dockerfile Dockerfile.worker` lists both files, and `git status --porcelain -- reports/` lists none.
    - All ten files in `<files>` land in ONE commit; the SUMMARY names that commit SHA.
  </acceptance_criteria>
  <done>Every must-change location names Python 3.14, both images are digest-pinned to the same two freshly-derived index digests, both lockfiles are regenerated, and both images build locally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker Hub / GHCR -> both Dockerfiles' `FROM` and `COPY --from=` lines | Third-party base images enter every build; the digest pin is the only thing making that entry reproducible |
| PyPI -> `uv.lock` -> `uv sync --locked` inside the images | Locked resolution is what CI and both images install; a stale or unregenerated lock silently changes what ships |
| npm registry -> `scripts/package-lock.json` | The Node harness's native addon |
| `main` -> the remote worker fleet | `Dockerfile.worker` is built and deployed manually on separate boxes outside this repo's CI; nothing in the pipeline validates it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-218-SC | Tampering | `uv lock` / `npm install` pulling `onnxruntime` 1.29.0 and `onnxruntime-node` 1.29.0 | high | mitigate | Both are pre-existing pinned dependencies undergoing a version bump; 218-RESEARCH.md's Package Legitimacy Audit resolved both to github.com/microsoft/onnxruntime with an `OK` verdict, zero `[ASSUMED]`/`[SUS]`/`[SLOP]` entries, so no blocking legitimacy checkpoint applies. Both stay exact pins rather than ranges, both lockfiles are regenerated (never hand-edited) so CI's `--locked` installs exactly what was resolved here, and the raise is gated behind 218-01's committed measurement rather than behind a version number in a document. |
| T-218-04 | Tampering | the `FROM` and `COPY --from=` lines in both Dockerfiles | high | mitigate | Every `FROM` stays digest-pinned — Task 2's verify counts `FROM` lines against digest-pinned `FROM` lines in both files and fails on any gap, so a re-pin cannot silently regress to a floating tag. Digests are derived live from `docker buildx imagetools inspect` (the OCI index digest, matching the existing pin format) and never copied from prose, and the two files are diffed against each other so the API image and the worker fleet image can never install different base bytes. ASVS V14 configuration/dependency management is the applicable control and it is preserved, not weakened. |
| T-218-05 | Denial of Service | the remote Stockfish worker fleet after an interpreter bump | high | mitigate | `.github/workflows/ci.yml` contains zero steps referencing `Dockerfile.worker`, so "pre-merge gate green" says nothing about that image. Task 2 requires a local `docker build -f Dockerfile.worker` before the commit is accepted, and gates the commit on both Dockerfiles moving together so the fleet's interpreter can never drift from the API's. |
| T-218-02 | Denial of Service | the Maia `InferenceSession` in the FastAPI process after the pin moves | high | mitigate | The pin only moves after 218-01's committed measurement, and Task 1 additionally requires the standing `tests/services/test_maia_parity.py` gate to be observed RUNNING (not skipped) against the new pin — `pytest.importorskip` makes a skipped module look like success at the suite level, so the per-test status is gated explicitly. |
| T-218-06 | Tampering | lockfile drift between the declared pins and what the images install | medium | mitigate | `uv lock --check` is gated for both projects, `npm ls` is gated for the Node harness, and both `uv.lock` headers are asserted to carry the new `requires-python` floor. The images and CI use `uv sync --locked`, so a drifted lock fails the build rather than resolving something else. |
| T-218-07 | Information disclosure | build-time secrets in the rebuilt images | low | accept | Neither Dockerfile gains a new `ARG`, `ENV`, `COPY` or `RUN` in this phase — only base-image references and the interpreter version change, and secrets already arrive at runtime via `.prod.env` rather than at build time. Re-auditing the images' secret handling is out of scope for a version bump. |
</threat_model>

<verification>
- Both native pins read 1.29.0, exact, with regenerated lockfiles that pass `uv lock --check` / `npm ls`.
- The parity regression test was observed PASSING, with zero `SKIPPED` lines, against the new pin.
- No `3.13` remains in any of the six must-change files.
- Both Dockerfiles carry identical, freshly-derived index digests for both base images, and every `FROM` is digest-pinned.
- Both lockfile headers read `requires-python = ">=3.14"`.
- `docker build .` and `docker build -f Dockerfile.worker .` both succeed locally.
- The interpreter bump is exactly one commit containing all ten files; `Dockerfile` and `Dockerfile.worker` moved together.
- Nothing under `reports/` was modified.
</verification>

<success_criteria>
- ROADMAP success criterion 2 is satisfied through "full pre-merge gate green" excluded — this plan delivers the pins, every 3.14 location, both digest re-pins and both local image builds; 218-03 delivers the gate, the changelog and the verified release.
- ROADMAP success criterion 4 is satisfied structurally: the worker image's interpreter moved in the same commit as the API image's, gated before the commit and re-checked after the merge in 218-03.
</success_criteria>

<output>
Create `.planning/phases/218-backend-onnxruntime-parity-spike-python-3-14-chain/218-02-SUMMARY.md` when done.
Record in it: the parity test's per-test PASSED/SKIPPED status verbatim, the exact `uv` tag chosen
and both index digests as printed by `docker buildx imagetools inspect`, both `docker build` exit
statuses, and the single SHA of the interpreter-bump commit.
</output>
