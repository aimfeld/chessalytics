---
phase: 218-backend-onnxruntime-parity-spike-python-3-14-chain
plan: 03
type: execute
wave: 3
depends_on: ["218-02"]
files_modified:
  - CHANGELOG.md
  - .planning/seeds/SEED-162-major-dependency-backlog.md
autonomous: false
requirements: []

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SC-2: the full CLAUDE.md pre-merge gate runs green on the Python 3.14 tree — every command, not a subset — before anything is integrated into `main`."
    - "SC-2: the parity regression test is observed RUNNING under Python 3.14, not skipped, because a `pytest.importorskip` skip reports as suite success and would hide exactly the regression this phase exists to prevent."
    - "SC-2: `CHANGELOG.md` carries a user-facing `## [Unreleased]` bullet for this phase — the project's changelog rule has no exception for dependency work."
    - "SC-2: the phase reaches `main` as a squash-merge, and local `main` equals `origin/main` before any release PR is cut, because the PR is built from `origin/main` and unpushed local commits are silently dropped from it."
    - "SC-2: a `bin/deploy.sh` release is verified on flawchess.com — the script's own `SERVER_SHA` assertion against `origin/production` is the proof, and it exits non-zero on a mismatch."
    - "SC-4 (post-merge re-check): the most recent commit touching `Dockerfile.worker` also touches `Dockerfile` — the interpreter never moved on the worker fleet alone."
    - "SEED-162 cluster 4 is closed with the shipped SHA, and `main` is forward-ported from `production` so the next release does not conflict."
    - "Spec-less probe fallback: skipped visibly — this phase has no requirement IDs and no SPEC.md/UI-SPEC.md/AI-SPEC.md exists, so no probe predicates were generated this run."
  prohibitions:
    - statement: "The release never happens by direct SSH. `bin/deploy.sh` (driven by the project's `deploy` skill) is the only sanctioned deploy path."
      status: enforced
      verification: "Task 3's action names the skill and the script as the only route; the SUMMARY records the deploy script's exit status and the verified server SHA."
    - statement: "A subset of the pre-merge gate is not acceptable at this point. This is the squash-merge that integrates real work into `main`, which is exactly where CLAUDE.md makes the full list mandatory."
      status: enforced
      verification: "Task 1 verify runs all seven gate commands, each with its own failing direction."
    - statement: "`gh pr checks --watch` is never treated as the CI gate — it exits 0 on FAILED checks. Use `gh run watch <run-id> --exit-status`."
      status: enforced
      verification: "Task 3's action states the substitution explicitly; the deploy skill's own CI-babysitting steps are followed rather than re-derived."
    - statement: "No released `CHANGELOG.md` section is edited. The entry goes under `## [Unreleased]`, appended to the existing `### Changed` group rather than opening a second one."
      status: enforced
      verification: "Task 1 verify asserts the new bullet sits above the first `## [v` heading."
  artifacts:
    - CHANGELOG.md
    - .planning/seeds/SEED-162-major-dependency-backlog.md
  key_links:
    - "phase branch -> `git merge --squash` -> `main` -> `git push origin main` -> the release PR's base, which is `origin/main` and not the local branch"
    - "`main` -> release PR -> `production` -> `bin/deploy.sh` -> `ssh flawchess git rev-parse HEAD` compared against `git rev-parse origin/production` -> the exit status that proves the deploy"
    - "`uv sync --group maia-inference` -> `tests/services/test_maia_parity.py` runs instead of skipping -> the pre-merge gate actually exercises the Maia path under 3.14"
    - "`production` -> forward-port merge back into `main` -> the next release PR is mergeable instead of reporting 'no checks'"
---

<objective>
Ship the chain: gate it, write it down, integrate it, release it, and prove it landed. The full
CLAUDE.md pre-merge gate on the Python 3.14 tree, a `## [Unreleased]` changelog bullet, one
squash-merge into `main`, a human go/no-go, then the project's own deploy flow through to a
verified server SHA on flawchess.com — followed by the forward-port and the SEED-162 close-out.

Purpose: the earlier plans changed what the project builds with. This one is where that becomes
real for users, and it is the only irreversible-in-practice step in the phase. Everything here is
already documented project procedure; the plan's job is to make sure none of it is skipped and
that the two checks which have historically been skipped — the full gate rather than a subset, and
"local `main` equals `origin/main`" — are gated rather than remembered.

Output: a green gate, a changelog bullet, a squash-merge on `main`, a verified production release,
a forward-ported `main`, and SEED-162 cluster 4 closed.

Spec-less probe fallback is skipped visibly: this phase carries no requirement IDs and no
SPEC/UI-SPEC/AI-SPEC exists, so no probe predicates were generated this run.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/218-backend-onnxruntime-parity-spike-python-3-14-chain/218-01-SUMMARY.md
@.planning/phases/218-backend-onnxruntime-parity-spike-python-3-14-chain/218-02-SUMMARY.md
@.planning/seeds/SEED-162-major-dependency-backlog.md
@CLAUDE.md
@docs/git-workflow.md
</context>

## Artifacts this plan produces

| Kind | Name | Notes |
|------|------|-------|
| New changelog bullet | one `### Changed` line under `## [Unreleased]` in `CHANGELOG.md` | Appended to the existing `### Changed` group; references `(Phase 218)`; terse and user-facing per `docs/git-workflow.md` |
| Changed status line | `.planning/seeds/SEED-162-major-dependency-backlog.md` | Cluster 4 closed, with the shipped commit SHA |
| New git object | one squash-merge commit on `main` | Integrates the phase branch; the phase's atomic commits stay on the branch's history for bisect until it is deleted |
| New git object | one release merge on `production` plus the forward-port merge back into `main` | Produced by the deploy skill's own flow, not hand-rolled here |
| New symbols | none | No source file changes at all in this plan — it is gate, prose, and process |
| Not produced | a milestone close, a `gh release create`, or a `CHANGELOG` version heading | Releases are cut at the milestone boundary; this phase ships a deploy, not a milestone |

<tasks>

<task type="auto">
  <name>Task 1: Run the full pre-merge gate on the 3.14 tree, write the changelog bullet, and squash-merge to main</name>
  <precondition>218-02's two commits exist on the phase branch and the dev PostgreSQL container is running (`docker compose -f docker-compose.dev.yml -p flawchess-dev up -d`) — the backend suite clones its test database from a migrated template and cannot run without it.</precondition>
  <precondition>The working tree's virtualenv has been re-synced under the new interpreter (`uv sync --group maia-inference` after the `.python-version` change) — otherwise `pytest.importorskip("onnxruntime")` silently skips the parity module and the gate proves nothing about the Maia path (RESEARCH.md Pitfall 4).</precondition>
  <reversibility rating="costly">The squash-merge puts the interpreter bump on the integration trunk. Reversible by `git revert` on `main` while `production` has not moved; after Task 3 the practical undo is a rollback deploy.</reversibility>
  <files>CHANGELOG.md</files>
  <read_first>
    - `CLAUDE.md` section "Pre-merge gate (MANDATORY before squash-merging to `main`)" — the seven commands, verbatim, and the rule that a subset is not acceptable at an integration point
    - `CLAUDE.md` section "Version Control" — local squash-merge (`git merge --squash <branch>`, then delete the branch), not a GitHub PR
    - `docs/git-workflow.md` lines 36-51 — the per-phase changelog rule, the `### Added` / `### Changed` / `### Fixed` grouping, and the prohibition on editing released sections
    - `CHANGELOG.md` `## [Unreleased]` block in full — it already has `### Fixed`, `### Security` and `### Changed` groups; the Phase 217 bullet under `### Changed` is the closest tonal analog for a dependency change
    - `.planning/phases/218-backend-onnxruntime-parity-spike-python-3-14-chain/218-02-SUMMARY.md` — the exact `uv` tag and digests, for the SUMMARY narrative
    - `tests/services/test_maia_parity.py` lines 18-21 — the two `importorskip` calls that make "green suite" and "green suite that exercised parity" different claims
  </read_first>
  <action>
    Write the changelog bullet first, so it is inside the gate rather than after it. Append one line
    to the EXISTING `### Changed` group under `## [Unreleased]` in `CHANGELOG.md` — do not open a
    second `### Changed` heading and do not touch any `## [v...]` section. Keep it terse and
    user-facing: what changed, not how. The substance is that the backend's native Maia runtime
    moved to onnxruntime 1.29.0 and the backend now runs on Python 3.14, with container images and
    CI updated to match. Mark it `Internal:` in the same style as the Phase 216 and Phase 217
    bullets already there, since no user-visible behavior changes, and end with `(Phase 218)`.

    Then run the full CLAUDE.md pre-merge gate. All of it, in order, resolving every output — this
    is the squash-merge that integrates real work, which is exactly the point where CLAUDE.md makes
    the complete list mandatory rather than a judgement call. If a formatting or lint step modifies
    files, commit that separately with a `style(...)` or `chore(...)` prefix before continuing; a CI
    formatter diff is always avoidable locally because the formatter is deterministic.

    Read the parity test's per-test status, not the suite exit code. Under `pytest -n auto` a
    skipped module shows as `s` and the run still exits 0. The dedicated parity command below exists
    to make that distinction explicit; treat a `SKIPPED` there as a gate failure, because it means
    the whole phase's central claim went unverified under the new interpreter.

    Squash-merge into `main` per CLAUDE.md — `git merge --squash <phase branch>` from `main`, one
    commit, then delete the branch. Push `main`. Two things to check after pushing, both recorded
    project traps: `git rev-list --left-right --count main...origin/main` must read `0 0`, because
    the release PR in Task 3 is cut from `origin/main` and any unpushed local commit would be
    silently excluded from the release; and under this project's `phase` branching strategy the GSD
    commit helper can check out a freshly recreated phase branch and commit there, so confirm
    `git branch --show-current` before reporting anything as pushed.

    Do not open the release PR, merge to `production`, or run the deploy script in this task — the
    human gate sits between.
  </action>
  <verify>
    <automated>uv run ruff format --check app/ tests/ scripts/ analysis/</automated>
    <fails_when>non-zero exit, or the output names any file as needing reformatting</fails_when>
    <automated>uv run ruff check .</automated>
    <fails_when>non-zero exit, or any diagnostic line is printed</fails_when>
    <automated>uv run ty check app/ tests/ scripts/</automated>
    <fails_when>non-zero exit, or the summary line reports more than zero errors</fails_when>
    <automated>uv run --project analysis --with ty ty check analysis/</automated>
    <fails_when>non-zero exit, or the summary line reports more than zero errors</fails_when>
    <automated>uv run python scripts/check_function_size.py app/ --fail-over-depth 4 --fail-over-loc 200</automated>
    <fails_when>non-zero exit, or any breach line is printed</fails_when>
    <automated>uv run --group maia-inference pytest tests/services/test_maia_parity.py tests/services/test_maia_engine.py -v</automated>
    <fails_when>non-zero exit, or the output contains `SKIPPED` or `no tests ran` — under Python 3.14 the onnxruntime import guard swallowed the modules, so the phase's central claim is unverified</fails_when>
    <automated>uv run --group maia-inference pytest -n auto -x</automated>
    <fails_when>non-zero exit, or the summary line reports any `failed` or `error` count above zero</fails_when>
    <automated>( cd frontend && npm run lint && npm test -- --run )</automated>
    <fails_when>non-zero exit from either command</fails_when>
    <automated>awk '/^## \[/{n++} n==1 && /Phase 218/{found=1} END{exit !found}' CHANGELOG.md</automated>
    <fails_when>non-zero exit — the Phase 218 bullet is missing, or it landed below the first `## [v...]` heading instead of inside `## [Unreleased]`</fails_when>
    <automated>git rev-list --left-right --count main...origin/main</automated>
    <fails_when>the output is anything other than `0	0` — a local commit is unpushed and would be silently dropped from the release PR, or `main` is behind and needs a fast-forward pull</fails_when>
    <automated>git log --format=%H -1 -- Dockerfile.worker | xargs -I{} git show --stat --format= {} -- Dockerfile Dockerfile.worker</automated>
    <fails_when>the output lists `Dockerfile.worker` without also listing `Dockerfile` — the most recent commit touching the worker image did not touch the API image, which is the divergence success criterion 4 forbids</fails_when>
    <automated>git branch --show-current</automated>
    <fails_when>the output is anything other than `main` — the GSD commit helper recreated the phase branch and the work reported as pushed is sitting somewhere else</fails_when>
  </verify>
  <acceptance_criteria>
    - All eight pre-merge gate commands above exit 0 with no diagnostics; any file the formatter or linter changed is committed separately with a `style(...)`/`chore(...)` prefix.
    - `pytest tests/services/test_maia_parity.py tests/services/test_maia_engine.py -v` shows PASSED with zero `SKIPPED` lines under Python 3.14.
    - `CHANGELOG.md` has exactly one new bullet, inside `## [Unreleased]`'s existing `### Changed` group, ending `(Phase 218)`, and no `## [v...]` section is modified.
    - The phase branch is squash-merged into `main` as a single commit and deleted; `git branch --show-current` reads `main`.
    - `git rev-list --left-right --count main...origin/main` reads `0	0`.
    - The most recent commit touching `Dockerfile.worker` also touches `Dockerfile`.
    - The SUMMARY records the squash-merge SHA and the parity tests' verbatim per-test status lines.
  </acceptance_criteria>
  <done>The full gate is green on the 3.14 tree with the parity tests observed running, the changelog bullet is written, and the phase is a single squash-merge commit on a pushed `main`.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Release now — promote `main` to `production` and run `bin/deploy.sh` — or hold the 3.14 bump on `main`?</decision>
  <context>
    Everything is on `main` and green: onnxruntime 1.29.0, Python 3.14 across both images, CI and
    both lockfiles, both container images built locally, the full CLAUDE.md pre-merge gate passed
    with the Maia parity tests observed running rather than skipped.

    Why a human decides here rather than the plan proceeding: a deploy is the least reversible step
    in this phase. Undoing anything before this point is a `git revert`; undoing after it is a
    rollback deploy of a running service. It also changes the interpreter under the API image, and
    the remote Stockfish worker fleet's image is built and deployed manually on separate boxes
    outside `bin/deploy.sh` — so the fleet does not move with this release and its rebuild is a
    follow-up the operator schedules.

    This release will also carry everything else sitting on `main`. Phase 217 (the browser Maia
    runtime moving to onnxruntime-web 1.29.0) has not been deployed, so shipping now also gives
    returning devices a one-time engine-asset re-download, roughly 14 MB on the WASM path and 26 MB
    on the WebGPU path, because the asset cache version was bumped. Three of Phase 217's four
    real-device UAT legs (iOS below 16.4, a low-memory device, a browser with a true WebGPU adapter)
    are still recorded as deferred for lack of hardware.

    Before answering, run `git log --oneline origin/production..origin/main` and skim what is
    actually about to ship. Note that the commit count there is inflated by squash-merge history —
    read the `git diff --stat` if you want the real size.
  </context>
  <options>
    <option id="release">
      <name>Release now</name>
      <pros>Closes SEED-162 cluster 4 end to end and satisfies success criterion 2's last clause. Ships Phase 217's frontend runtime bump at the same time, so the browser and backend Maia runtimes reach production on the same version. A bump validated only on a developer machine is a bump nobody has actually tested in the environment that matters.</pros>
      <cons>Ships the undeployed Phase 217 work and its one-time asset re-download alongside it, with three device-UAT legs still deferred. Undo becomes a rollback deploy rather than a revert.</cons>
    </option>
    <option id="hold">
      <name>Hold on `main`</name>
      <pros>Leaves the interpreter bump revertible with a single commit, and lets Phase 217's outstanding device legs be closed before either change reaches users.</pros>
      <cons>Leaves success criterion 2 unmet and the phase open. `main` accumulates further divergence from `production`, and the next release ships a larger, harder-to-attribute batch.</cons>
    </option>
  </options>
  <resume-signal>Reply `release` to run Task 3, or `hold` to close the phase with the deploy recorded as an explicit deferral (say so in the SUMMARY and in SEED-162's status line).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Run the release through the project's deploy flow, verify it on flawchess.com, forward-port, and close SEED-162 cluster 4</name>
  <precondition>The Task 2 checkpoint was answered `release`, the working tree is clean, and `git rev-parse --abbrev-ref HEAD` reads `main` — the deploy skill's own preflight halts on a dirty tree because it cannot tell in-progress work from finished work.</precondition>
  <reversibility rating="one-way">A deployed release is the phase's least reversible step: undoing it means a rollback deploy of a running service rather than a revert. The preceding checkpoint is the door.</reversibility>
  <files>.planning/seeds/SEED-162-major-dependency-backlog.md</files>
  <read_first>
    - `.claude/skills/deploy/SKILL.md` in full — the preflight, the PR, the CI-babysitting rules, the merge, `bin/deploy.sh`, the monitoring and the verification steps. Follow it; do not re-derive a release procedure
    - `.claude/skills/deploy/references/ci-fixes.md` if any CI check fails
    - `docs/git-workflow.md` lines 15-32 — the release shape and the forward-port rule
    - `bin/deploy.sh` lines 20-80 — `TARGET_SHA` from `origin/production`, and the `SERVER_SHA` comparison that is the script's own final assertion
    - `.planning/seeds/SEED-162-major-dependency-backlog.md` line 3 and the Cluster 4 section, as updated by 218-01
  </read_first>
  <action>
    Follow `.claude/skills/deploy/SKILL.md` end to end. It is a set-and-forget flow: open the
    `main` to `production` PR, fix whatever CI complains about, squash-merge once green, run
    `bin/deploy.sh`, monitor it, and verify. Do not invent a shortcut and never deploy by direct
    SSH — `bin/deploy.sh` is the only sanctioned path.

    Three project traps the skill already documents, restated because each has cost a release
    before. Do not run `gh pr checks --watch` as the gate: it exits 0 even when checks have FAILED —
    use `gh run watch <run-id> --exit-status` instead. If the PR reports "no checks", check
    `mergeable` first, because that is usually a missing forward-port of the previous release
    (fix with `git merge -s ours origin/production` on `main`, push) and only sometimes a post-push
    race. And do not probe for `.prod.env` from a shell; that path is permission-denied by design
    and `bin/deploy.sh` fails loudly at its `scp` step if the file is missing.

    The proof that the release landed is `bin/deploy.sh`'s own final assertion: it compares
    `ssh flawchess "cd /opt/flawchess && git rev-parse HEAD"` against `git rev-parse origin/production`
    and exits non-zero on a mismatch. Record both SHAs in the SUMMARY, not just "deploy succeeded".

    Forward-port immediately after the release, not at the end of the milestone: merge `production`
    back into `main` per `docs/git-workflow.md`. Back-to-back releases conflict on the next PR
    otherwise, and `bin/deploy.sh` skips the step whenever you are not on a clean `main` checkout —
    so verify it happened rather than assuming the script did it.

    Finally, close the seed: update `.planning/seeds/SEED-162-major-dependency-backlog.md`'s
    `**Status:**` line and Cluster 4 section to record cluster 4 as done, naming the deployed
    commit SHA and keeping the evidence paths from 218-01. Note in the same edit that the remote
    Stockfish worker fleet's image is deployed manually outside `bin/deploy.sh`, so the fleet's
    rebuild on the new interpreter is an operator follow-up rather than something this release
    performed.
  </action>
  <verify>
    <automated>git rev-list --left-right --count main...origin/main</automated>
    <fails_when>the output is anything other than `0	0` — the release PR is cut from `origin/main`, so an unpushed local commit would ship nothing and a behind-`main` would surprise later</fails_when>
    <automated>bin/deploy.sh</automated>
    <fails_when>non-zero exit. The script asserts the server's `git rev-parse HEAD` equals `origin/production` and prints `ERROR: server is at <sha>, expected <sha>.` on a mismatch; it also fails fast at its `scp` step when `.prod.env` is absent locally</fails_when>
    <automated>git rev-parse origin/production</automated>
    <fails_when>the printed SHA is not the SHA reported by the deploy script as the verified server HEAD</fails_when>
    <automated>git log --oneline origin/production..origin/main</automated>
    <fails_when>the output still lists the Phase 218 squash-merge commit — the release did not include this phase's work</fails_when>
    <automated>git merge-base --is-ancestor origin/production main; echo $?</automated>
    <fails_when>prints anything other than `0` — `production` was not forward-ported back into `main`, which will make the next release PR unmergeable and report "no checks"</fails_when>
    <automated>grep -n 'Cluster 4' .planning/seeds/SEED-162-major-dependency-backlog.md</automated>
    <fails_when>the cluster 4 section does not record a done status with the deployed SHA</fails_when>
    <human-check>
      Open https://flawchess.com in a browser and confirm the app loads, sign-in works, and an
      existing game opens on the analysis board with the engine panel reaching a ready state. This
      is human-attended because `bin/deploy.sh` proves the server is at the right commit and the
      health check passes, which is not the same claim as the app being usable — and the Maia
      runtime path this phase touched is exactly the one CI cannot exercise. Record pass/fail and
      anything odd in the SUMMARY.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `bin/deploy.sh` exits 0, and its verified `SERVER_SHA` equals `git rev-parse origin/production`; both SHAs appear in the SUMMARY.
    - `git log --oneline origin/production..origin/main` no longer lists the Phase 218 squash-merge commit.
    - `origin/production` is an ancestor of `main` (the forward-port happened), verified rather than assumed.
    - `.planning/seeds/SEED-162-major-dependency-backlog.md` records cluster 4 as done with the deployed SHA, keeps 218-01's evidence paths, and notes the worker-fleet image rebuild as an operator follow-up.
    - The browser smoke check on flawchess.com is recorded with an explicit pass/fail.
    - No milestone close, `CHANGELOG` version heading, or `gh release create` was performed — releases are cut at the milestone boundary, not per phase.
  </acceptance_criteria>
  <done>The phase is running on flawchess.com at a SHA verified by the deploy script's own assertion, `main` is forward-ported, and SEED-162 cluster 4 is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase branch -> `main` | The squash-merge is where unreviewed work becomes the integration trunk; the pre-merge gate is the only control on it |
| `origin/main` -> release PR -> `production` | The PR is built from the remote ref, not the local branch, so anything unpushed is silently excluded |
| `production` -> `bin/deploy.sh` -> the flawchess.com host | Deploy credentials and `.prod.env` cross into a live environment |
| the release -> real users | Behavior change reaches production without any further gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-218-08 | Tampering | the released commit differing from the reviewed one | high | mitigate | `git rev-list --left-right --count main...origin/main` is gated at `0 0` both before the merge and before the release, because the PR is cut from `origin/main`; a release shipped from a stale remote ref is the recorded 2026-06-23 failure this prevents. `bin/deploy.sh` then asserts the server's `git rev-parse HEAD` equals `origin/production` and exits non-zero otherwise, so the deployed bytes are pinned to a known SHA rather than to a successful-looking script run. |
| T-218-09 | Denial of Service | a green-looking gate that never exercised the Maia path under 3.14 | high | mitigate | `pytest.importorskip("onnxruntime")` makes an unsynced worktree report the parity modules as skipped while the suite exits 0. Task 1 runs those two modules as their own gated command whose failing direction includes the literal string `SKIPPED`, so a skip fails the task rather than passing it. |
| T-218-10 | Elevation of privilege | ad-hoc deploy paths bypassing the sanctioned script | high | mitigate | Direct SSH deploys are prohibited by CLAUDE.md and by this plan's `prohibitions`; the release runs through `.claude/skills/deploy/SKILL.md` and `bin/deploy.sh`, which centralize credential handling and carry the server-SHA assertion. `.prod.env` is deliberately unreadable from the shell and is not probed. |
| T-218-11 | Tampering | CI reported green when it was not | medium | mitigate | `gh pr checks --watch` exits 0 on FAILED checks; the plan mandates `gh run watch <run-id> --exit-status` instead, and treats a "no checks reported" PR as a mergeability question (usually a missing forward-port) rather than as a pass. |
| T-218-12 | Repudiation | no record of what shipped | low | accept | Accepted: the changelog bullet, the squash-merge SHA, the deployed SHA and the seed close-out are all recorded in the SUMMARY and in git. A signed-provenance chain for a self-hosted single-operator deploy is disproportionate here. |
</threat_model>

<verification>
- All eight pre-merge gate commands green on the Python 3.14 tree, with the two Maia modules observed PASSED and never SKIPPED.
- One new `## [Unreleased]` `### Changed` bullet ending `(Phase 218)`; no released section touched.
- Phase on `main` as one squash-merge; `main` equals `origin/main`; current branch is `main`.
- The most recent commit touching `Dockerfile.worker` also touches `Dockerfile`.
- `bin/deploy.sh` exits 0 with a verified server SHA matching `origin/production`; the phase commit no longer appears in `origin/production..origin/main`.
- `origin/production` is an ancestor of `main` (forward-port confirmed).
- SEED-162 cluster 4 closed with the deployed SHA and the evidence paths retained.
- Browser smoke check on flawchess.com recorded with an explicit pass/fail.
</verification>

<success_criteria>
- ROADMAP success criterion 2 is complete: pins raised, every 3.14 location updated, `uv` re-pinned by digest in both Dockerfiles (218-02), full pre-merge gate green, and a `bin/deploy.sh` release verified on flawchess.com (this plan).
- ROADMAP success criterion 4 is re-checked post-merge on the actual merged history, not only pre-commit.
- SEED-162 cluster 4 is closed end to end, with the measurement evidence that gated it still reachable from the pin comments.
</success_criteria>

<output>
Create `.planning/phases/218-backend-onnxruntime-parity-spike-python-3-14-chain/218-03-SUMMARY.md` when done.
Record in it: the verbatim per-test status of the two Maia modules under 3.14, the squash-merge SHA,
the deployed SHA and the server SHA the deploy script verified, the forward-port confirmation, the
browser smoke-check result, and an explicit note that the remote worker fleet's image rebuild
remains an operator follow-up outside `bin/deploy.sh`.
</output>
