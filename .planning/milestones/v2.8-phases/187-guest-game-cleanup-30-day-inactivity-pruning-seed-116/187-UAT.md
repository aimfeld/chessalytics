---
status: testing
phase: 187-guest-game-cleanup-30-day-inactivity-pruning-seed-116
source: [187-VERIFICATION.md]
started: 2026-07-24
updated: 2026-07-24
---

## Current Test

number: 1
name: D-06 backstop — large single-guest cascade prod WAL/lock safety
expected: |
  A single guest holding a GM-Hikaru-scale game_positions cascade (~5M rows)
  is purged in one transaction (D-06, accepted risk) without spiking WAL growth
  or holding locks long enough to visibly degrade concurrent API latency on the
  shared prod DB. If it does, the documented chunked/batched deletion fallback
  is enabled (no migration needed).
awaiting: post-deploy observation

## Tests

### 1. D-06 backstop — large single-guest cascade prod safety
expected: |
  After deploy, the FIRST cleanup tick may process a one-time backlog of
  already-30-days-inactive guests (not the steady daily trickle D-02 assumed).
  Watch the per-run summary log line ("Guest cleanup tick: scanned=N purged=M
  games_deleted=K failed=J") and Sentry after the first prod tick(s), and watch
  WAL growth / lock duration vs live API latency for any single large cascade.
  A single ~5M-row cascade delete stays within prod's max_wal_size=8GB budget
  and does not visibly degrade concurrent API traffic. If it does, switch
  _purge_guest to the documented chunked/batched deletion fallback.
result: [pending]
why_human: |
  Declared verification: backstop in 187-01-PLAN.md frontmatter — non-inferable
  from the codebase or the test DB; requires real prod-scale data under live
  traffic to observe. The exact deletion code path (delete_all_games_for_user +
  reset_backfill_cursors) already runs in prod today via DELETE /api/games, so
  this is a monitor-after-deploy check, not a pre-ship blocker.

## Summary

total: 1
passed: 0
issues: 0
pending: 1
skipped: 0
blocked: 0

## Gaps
