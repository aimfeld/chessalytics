---
phase: 189-pool-scheduler-backend
plan: 02
type: execute
wave: 2
depends_on: ["189-01"]
files_modified:
  - app/routers/imports.py
  - app/services/guest_cleanup_service.py
  - tests/test_imports_router.py
  - tests/test_guest_cleanup_service.py
autonomous: true
requirements: [POOL-09]

must_haves:
  truths:
    # goal-backward (POOL-09 / D-02 / D-03 / D-04)
    - "DELETE /api/games removes every drill_items and drill_solves row belonging to the deleted games via the database FK cascade, with no explicit delete statement added to the handler (D-03: game deletion cascades drill items away)."
    - "DELETE /api/games leaves the user's drill_sessions and train_settings rows untouched (D-04: session history is user progress, not game-derived data)."
    - "The guest 30-day purge removes a guest's drill_items and drill_solves and leaves no dangling references behind."
    - "POST /api/train/sessions returns 200 (not a 500) for a user whose games were all deleted, and reports zero puzzles."
    - "Both delete-all call sites carry an inline comment stating that drill_sessions and train_settings are deliberately preserved per D-04, so a future maintainer does not 'fix' the apparent gap."
    # edge probe: POOL-09
    - "Deleting one game removes exactly that game's drill rows: a second game's drill_items and drill_solves for the same user survive untouched."
    - "delete-all for a user with zero drill rows returns 200 and deletes nothing, raising no error."
    - "No ordering of the delete-all statements can remove a drill_sessions row: the table has no foreign key to games and no explicit delete targets it."
    - "Calling DELETE /api/games twice in a row returns 200 both times; the second call reports deleted_count 0 and leaves the drill tables in the same state as the first."
    - statement: "A session composition running concurrently with a delete-all either observes the pre-delete pool or an empty pool — it never returns a puzzle whose games row has already been removed."
      verification: backstop

  artifacts:
    - path: "tests/test_imports_router.py"
      provides: "Delete-all cascade coverage for the train tables"
      contains: "drill"
    - path: "tests/test_guest_cleanup_service.py"
      provides: "Guest-purge cascade coverage for the train tables"
      contains: "drill"

  key_links:
    - from: "app/repositories/game_repository.py"
      to: "drill_items / drill_solves"
      via: "DELETE FROM games triggers the ON DELETE CASCADE declared on game_id"
      pattern: "delete_all_games_for_user"
    - from: "app/routers/imports.py"
      to: "drill_sessions"
      via: "deliberately absent — no delete statement targets drill_sessions (D-04)"
      pattern: "D-04"
---

<objective>
Prove POOL-09 against the two real deletion call sites, and record why one of the four train tables
deliberately survives.

Purpose: D-02 and D-04 push the entire POOL-09 guarantee into FK design, which means the guarantee
is invisible in application code and one careless "cleanup" commit away from silently breaking. This
plan pins it with tests at both call sites and leaves a breadcrumb at each.

Output: cascade + survival tests added to the two existing delete-path test files, and a D-04
comment at each call site.
</objective>

<artifacts_this_phase_produces>
Every symbol Phase 189 creates. Nothing below exists in the codebase before this phase.

**Tables:** `drill_items`, `drill_sessions`, `drill_solves`, `train_settings`

**Files:** `app/models/drill_item.py`, `app/models/drill_session.py`, `app/models/drill_solve.py`,
`app/models/train_settings.py`, `app/services/train_scheduler.py`, `app/services/train_pool.py`,
`app/repositories/train_repository.py`, `app/schemas/train.py`, `app/routers/train.py`,
`alembic/versions/*_phase_189_train_tables.py`, `tests/services/test_train_scheduler.py`,
`tests/services/test_train_pool.py`, `tests/repositories/test_train_repository.py`,
`tests/routers/test_train.py`

**Classes / enums:** `DrillItem`, `DrillStatus`, `DrillSession`, `DrillSolve`, `DrillSource`,
`DrillGuess`, `TrainSettings`, `ItemState`, `TrainSettingsRow`, `TrainPuzzle`,
`TrainSessionResponse`, `TrainSettingsResponse`, `TrainSettingsUpdate`, `SolveRequest`,
`SolveResponse`, `PuzzleRevealResponse`

**Functions:** `local_today`, `next_scheduled_day`, `apply_result`, `session_window`,
`is_session_expired`, `pool_entry_stmt`, `herring_stmt`, `expected_score_sql`, `expected_score_for`,
`classify_puzzle_type`,
`full_fen_at_ply`, `compose_slots`, `get_settings`, `get_or_create_settings`, `upsert_settings`,
`open_session_for_user`, `expire_stale_sessions`, `compose_and_materialize_session`,
`load_session_puzzles`, `record_solve`, `reveal_for_puzzle`, `_reject_guest`

**Constants:** `WINNABILITY_FLOOR_ES`, `SHARP_GAP_ES`, `HERRING_SHARE`, `LADDER_DAYS`,
`MASTERY_STREAK_THRESHOLD`, `PARK_FAIL_THRESHOLD`, `DEFAULT_TIMEZONE`, `DEFAULT_WEEKDAY_MASK`,
`DEFAULT_PUZZLES_PER_SESSION`

**Endpoints:** `GET /api/train/settings`, `PUT /api/train/settings`, `POST /api/train/sessions`,
`POST /api/train/sessions/{session_id}/solve`,
`GET /api/train/sessions/{session_id}/puzzles/{position}/reveal`

**Model fields:** `drill_items`(user_id, game_id, ply, status, streak, due_date, fail_count,
ever_correct, created_at) · `drill_sessions`(id, user_id, session_date, status, puzzle_count,
expires_on, started_at, completed_at) · `drill_solves`(session_id, position, user_id, game_id, ply,
source, guess, correct_guess, correct_move, played_move, solved_at) ·
`train_settings`(user_id, timezone, weekday_mask, puzzles_per_session)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/189-pool-scheduler-backend/189-CONTEXT.md
@.planning/phases/189-pool-scheduler-backend/189-RESEARCH.md
@.planning/phases/189-pool-scheduler-backend/189-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin the delete-all cascade and the drill_sessions survival rule</name>
  <read_first>
    - app/routers/imports.py lines 455-480 (the DELETE /games handler and its existing explicit deletes)
    - app/repositories/game_repository.py `delete_all_games_for_user` (the bulk DELETE FROM games that fires the cascade)
    - tests/test_imports_router.py (existing delete-all test shape, register/login helper, seeding style)
    - app/models/drill_item.py, app/models/drill_session.py, app/models/drill_solve.py, app/models/train_settings.py (the FK targets written in 189-01)
    - .planning/phases/189-pool-scheduler-backend/189-RESEARCH.md "Pattern 7" (the verified interaction with both delete call sites)
  </read_first>
  <files>app/routers/imports.py, tests/test_imports_router.py</files>
  <action>
Add a comment block inside the `delete_all_games` handler in `app/routers/imports.py`, immediately
after the `delete_all_games_for_user` call, stating: `drill_items` and `drill_solves` are removed by
the database cascade on `games.id` and need no explicit delete here; `drill_sessions` and
`train_settings` are deliberately preserved because session history is user progress rather than
game-derived data (cite D-02 and D-04 by ID). Add NO new delete statement — adding one would violate
D-04.

Add these cases to `tests/test_imports_router.py`, reusing that file's existing register/login and
seeding helpers:

`test_delete_games_cascades_drill_rows` — register a user, seed two games, insert a `DrillItem` and
a `DrillSession` plus two `DrillSolve` rows (one per game) for that user, call `DELETE /api/games`,
then assert `drill_items` and `drill_solves` counts for that user are 0 while the `drill_sessions`
row and any `train_settings` row still exist.

`test_delete_single_game_leaves_sibling_drill_rows` — seed two games each with its own `DrillItem`,
delete only the first game directly through the session (a targeted `DELETE FROM games WHERE id = ?`,
not the endpoint), and assert the second game's `DrillItem` survives with its `streak` and
`due_date` unchanged.

`test_delete_games_no_drill_rows_is_noop` — register a user with games but no train rows at all, call
`DELETE /api/games`, assert 200 and that no exception is raised.

`test_delete_games_twice_is_idempotent` — call `DELETE /api/games` twice; assert 200 both times, that
the second response's `deleted_count` is 0, and that the drill-table counts after the second call
equal the counts after the first.

`test_compose_session_after_delete_all_returns_empty` — after `DELETE /api/games`, call
`POST /api/train/sessions` and assert 200 with an empty `puzzles` list (not a 500 from a dangling
reference).
  </action>
  <verify>
    <automated>uv run pytest tests/test_imports_router.py -q -k "drill or delete_games" && uv run ty check app/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_imports_router.py -q -k "drill or delete_games"` exits 0 with `test_delete_games_cascades_drill_rows`, `test_delete_single_game_leaves_sibling_drill_rows`, `test_delete_games_no_drill_rows_is_noop`, `test_delete_games_twice_is_idempotent` and `test_compose_session_after_delete_all_returns_empty` all collected and passing.
    - `test_delete_games_cascades_drill_rows` asserts a non-zero drill row count BEFORE the delete and 0 after, so it cannot pass vacuously.
    - `test_delete_games_cascades_drill_rows` asserts the user's `drill_sessions` row count is unchanged by the delete.
    - The `delete_all_games` handler in `app/routers/imports.py` gained a comment referencing `D-04` and gained no new `session.execute(delete(...))` statement (the count of `delete(` occurrences in that handler is unchanged from before this task).
    - `uv run ty check app/ tests/` exits 0.
  </acceptance_criteria>
  <done>The delete-all path is proven to cascade drill items and solves away while preserving session history, and the reason is recorded at the call site.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the guest-purge cascade</name>
  <read_first>
    - app/services/guest_cleanup_service.py lines 60-130 (`_purge_guest`, its explicit deletes and cursor reset)
    - tests/test_guest_cleanup_service.py (eligibility fixtures, the guest-seeding helper, the leaked-guest cleanup convention noted at the top of the file)
    - app/models/drill_item.py, app/models/drill_solve.py, app/models/drill_session.py (FK targets)
  </read_first>
  <files>app/services/guest_cleanup_service.py, tests/test_guest_cleanup_service.py</files>
  <action>
Add a comment in `_purge_guest` next to the existing explicit deletes stating that the train tables
need no handling here: `drill_items` and `drill_solves` ride the `games` cascade, `drill_sessions`
and `train_settings` are preserved by design, and guests never accumulate train rows at all because
the D-05 gate rejects them before any pool query (cite D-04 and D-05 by ID). Add no new delete
statement.

Add to `tests/test_guest_cleanup_service.py`:

`test_purge_guest_cascades_drill_rows` — seed an eligible guest (using the file's existing
`is_guest=True` + stale `last_activity` helper) with a game, a `DrillItem` and a `DrillSolve` under a
`DrillSession`, run the purge, and assert the guest's `drill_items` and `drill_solves` counts are 0
afterwards while the `drill_sessions` row survives. Assert the counts are non-zero before the purge.

`test_purge_guest_without_drill_rows_is_noop` — an eligible guest with games but no train rows purges
cleanly and returns the expected deleted-game count.

Follow this file's existing convention of cleaning up any non-guest rows the test creates in a
`finally` block so the shared per-run DB does not leak state into sibling tests.
  </action>
  <verify>
    <automated>uv run pytest tests/test_guest_cleanup_service.py -q && uv run ty check app/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_guest_cleanup_service.py -q` exits 0 with `test_purge_guest_cascades_drill_rows` and `test_purge_guest_without_drill_rows_is_noop` collected and passing.
    - `test_purge_guest_cascades_drill_rows` asserts drill row counts > 0 before the purge and == 0 after, and asserts the `drill_sessions` count is unchanged.
    - `_purge_guest` in `app/services/guest_cleanup_service.py` gained a comment referencing `D-04` and `D-05` and gained no new `delete(` statement.
    - `uv run pytest -n auto -q` exits 0 (the full suite is unaffected by the new seeding).
    - `uv run ty check app/ tests/` exits 0.
  </acceptance_criteria>
  <done>The guest 30-day purge is proven to leave no orphaned drill rows, and the "nothing to do here" decision is documented in the purge body.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user → own game data | `DELETE /api/games` acts on `current_active_user.id` only |
| scheduled job → guest rows | `_purge_guest` selects targets by eligibility query, not by request input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-189-06 | Denial of Service | dangling drill rows after a game wipe | medium | mitigate | FK `ondelete="CASCADE"` on `drill_items.game_id` and `drill_solves.game_id`, pinned by cascade tests at both real call sites |
| T-189-07 | Tampering | a future cleanup commit deleting `drill_sessions` | medium | mitigate | Explicit D-04 comment at both call sites plus a test asserting `drill_sessions` survives, so the removal fails CI rather than silently erasing streak history |
| T-189-08 | Elevation of Privilege | wrong-target guest purge | low | accept | Pre-existing WR-01 in-transaction eligibility re-check in `_purge_guest` is unchanged by this plan |
| T-189-SC | Tampering | dependency supply chain | low | accept | Zero packages installed in this phase (189-RESEARCH.md Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `uv run pytest tests/test_imports_router.py tests/test_guest_cleanup_service.py -q` is green.
- `uv run pytest -n auto -q` is green (no cross-test state leak from the new seeding).
- `uv run ruff check app/ tests/` and `uv run ty check app/ tests/` are clean.
</verification>

<success_criteria>
- Both real delete paths are covered by non-vacuous cascade tests (non-zero before, zero after).
- `drill_sessions` survival is asserted, not assumed.
- Composing a session after a full wipe returns 200 with zero puzzles.
</success_criteria>

<output>
Create `.planning/phases/189-pool-scheduler-backend/189-02-SUMMARY.md` when done.
</output>
