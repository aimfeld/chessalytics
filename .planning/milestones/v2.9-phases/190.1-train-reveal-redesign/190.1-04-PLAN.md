---
phase: 190.1-train-reveal-redesign
plan: 04
type: execute
wave: 4
depends_on: [190.1-02, 190.1-03]
files_modified:
  - frontend/src/lib/trainArrows.ts
  - frontend/src/lib/__tests__/trainArrows.test.ts
  - frontend/src/lib/theme.ts
  - frontend/src/components/train/TrainSolveScreen.tsx
  - frontend/src/components/train/__tests__/TrainSolveScreen.test.tsx
  - frontend/src/components/train/TrainReveal.tsx
  - frontend/src/components/train/__tests__/TrainReveal.test.tsx
  - frontend/src/hooks/useTrainSession.ts
  - frontend/src/hooks/__tests__/useTrainSession.test.ts
  - frontend/src/pages/__tests__/Train.solveLoop.test.tsx
autonomous: true
requirements: [SOLV-02, SOLV-05]

must_haves:
  truths:
    - "D-02: on reveal the board shows green arrows for the actual good moves — moves whose expected-score drop against the best is better than an inaccuracy, per the project's existing thresholds."
    - "D-02: the green arrow count follows puzzle type — exactly one for a sharp puzzle, up to three for a soft or herring puzzle."
    - "D-02: the user's played move gets its own visually distinct arrow, and the move played in the game gets a smaller white arrow in the analysis board's style."
    - "D-02: no arrow of any kind is drawn before the attempt is graded."
    - "D-04: the current session score is visible at the top-right of the progress bar throughout the loop and survives a resume."
  artifacts:
    - "frontend/src/lib/trainArrows.ts: buildTrainRevealArrows plus the per-puzzle-type arrow cap constants"
    - "frontend/src/lib/theme.ts: TRAIN_PLAYED_MOVE_ARROW"
    - "frontend/src/hooks/useTrainSession.ts: UseTrainSessionResult.sessionSolvedCount"
    - "frontend/src/components/train/TrainSolveScreen.tsx: the train-session-score element"
  key_links:
    - "GradeResult.goodMoveUcis (Plan 02) -> buildTrainRevealArrows -> ChessBoard arrows prop"
    - "TrainReveal's reveal query -> onGameMoveUciChange -> TrainSolveScreen -> the white game-move arrow"
    - "BoardArrow.layerKey / onTop are what let a coincident played/good/game arrow all survive dedupeArrowsByMove"
---

<objective>
Put the phase's two remaining board-and-chrome surfaces on screen: the reveal arrows that
show what was good and what was played, and the running session score on the progress bar.

CONTEXT.md numbers its locked decisions D-1..D-5; this plan writes them as D-01..D-05.

Locked decisions covered: D-02 (all four arrow bullets), D-04 (session score on the
progress bar).

Purpose: both changes live on the solve screen and both are pure consumption of data the
earlier plans already produce — the good-moves set from the grading engine and the
already-accumulated localStorage-backed score. Planned last so the arrow set is built from
a settled `GradeResult` shape rather than a moving one.

Output: a reveal board that answers "what should I have played" at a glance, and a progress
row that keeps the running score visible without a separate screen.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/190.1-train-reveal-redesign/190.1-CONTEXT.md
@.planning/phases/190.1-train-reveal-redesign/190.1-RESEARCH.md
@.planning/phases/190.1-train-reveal-redesign/190.1-PATTERNS.md
@.planning/phases/190.1-train-reveal-redesign/190.1-02-SUMMARY.md
@.planning/phases/190.1-train-reveal-redesign/190.1-03-SUMMARY.md
</context>

## Artifacts this phase produces

See the "Artifacts this phase produces" section in `190.1-01-PLAN.md` — it lists every
symbol Phase 190.1 creates across all five plans. This plan creates
`frontend/src/lib/trainArrows.ts` (`buildTrainRevealArrows`, `TRAIN_SHARP_GOOD_MOVE_ARROWS`,
`TRAIN_SOFT_GOOD_MOVE_ARROWS`, and the three arrow-width constants), the theme token
`TRAIN_PLAYED_MOVE_ARROW`, the hook field `UseTrainSessionResult.sessionSolvedCount`, the
`TrainReveal` prop `onGameMoveUciChange`, and the testid `train-session-score`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reveal board arrows — good moves, the played move, and the game move</name>
  <files>frontend/src/lib/trainArrows.ts, frontend/src/lib/__tests__/trainArrows.test.ts, frontend/src/lib/theme.ts, frontend/src/components/train/TrainSolveScreen.tsx, frontend/src/components/train/__tests__/TrainSolveScreen.test.tsx, frontend/src/components/train/TrainReveal.tsx, frontend/src/components/train/__tests__/TrainReveal.test.tsx</files>
  <read_first>
    - `frontend/src/components/board/ChessBoard.tsx` lines 17-57 — the `BoardArrow` interface, especially `onTop` and `layerKey` and their documented purpose
    - `frontend/src/components/board/arrowGeometry.ts` — `dedupeArrowsByMove` and how `layerKey` participates in the dedupe key
    - `frontend/src/pages/Analysis.tsx` lines 2276-2340 — the white next-move arrow and the multi-rank engine-arrow memo, the two precedents to follow
    - `frontend/src/pages/Analysis.tsx` lines 225-250 — the arrow width constants and their values
    - `frontend/src/lib/theme.ts` — `NEXT_MOVE_ARROW`, `BEST_MOVE_ARROW`, and the `TRAIN_*` token block added in Phase 190
    - `frontend/src/lib/arrowColor.ts` — `DARK_GREEN`, the established categorical good/green arrow color
    - `frontend/src/components/train/TrainSolveScreen.tsx` — the `ChessBoard` render, `lastPlayedUci`, the `GradeResult` state added in Plan 03, and where the reveal is mounted
    - `frontend/src/components/train/TrainReveal.tsx` — the reveal query and its `played_in_game_move_uci` read
    - `frontend/src/hooks/useTrainGradingEngine.ts` — `GradeResult.goodMoveUcis` ordering (rank order, rank 1 first)
  </read_first>
  <behavior>
    - Before a verdict exists, the built arrow list is empty regardless of what else is supplied.
    - For a sharp puzzle, exactly one green good-move arrow is built even when the good-moves set has more entries.
    - For a soft or herring puzzle, up to three green good-move arrows are built, in the order the good-moves set supplies them, and fewer when the set is shorter.
    - A played-move arrow is built whenever a played move UCI is supplied, using the train played-move token, and it survives alongside a green arrow on the same from-to pair.
    - A game-move arrow is built whenever a game-move UCI is supplied, using the white next-move token, the smaller width, and drawn on top.
    - A malformed or too-short UCI contributes no arrow and does not throw.
  </behavior>
  <action>
Extract the arrow construction into a new pure module `frontend/src/lib/trainArrows.ts` so
it is unit-testable without rendering a board, and so `TrainSolveScreen`'s own logic stays
small. Export `buildTrainRevealArrows` taking the puzzle type, the good-move UCI list, the
played-move UCI, the game-move UCI, and a flag for whether the verdict has landed; it
returns a `BoardArrow[]`.

Add named constants in that module for the per-puzzle-type green-arrow caps — one for a
sharp puzzle and three for soft and herring — and for the three arrow widths, following the
Analysis page's own width values: the good-move and played-move arrows use a normal engine-
arrow width and the game-move arrow uses the thinner next-move width. No bare numeric
literal may appear at a call site.

Colors: green good-move arrows use the existing `DARK_GREEN` export from
`@/lib/arrowColor`; the game-move arrow uses `NEXT_MOVE_ARROW` from `@/lib/theme` with
`onTop` true, matching the analysis board's played-move arrow; the played-move arrow uses a
NEW named token added to `theme.ts`, `TRAIN_PLAYED_MOVE_ARROW`, aliased to the existing
blue `BEST_MOVE_ARROW` value in the same aliasing style Phase 190 used for the train
verdict tokens. No color literal may be written in the arrow module or the component.

Give every arrow a distinct `layerKey` — an indexed key per green arrow and one each for
the played and game arrows — so a coincident from-to pair renders as concentric arrows
instead of collapsing in `dedupeArrowsByMove`. Return an empty array when the verdict has
not landed: no arrow of any kind may be visible during the guess or the attempt.

Wire it up. `TrainReveal` gains an optional `onGameMoveUciChange: (uci: string | null) =>
void` prop, called from an effect whenever the reveal query's `played_in_game_move_uci`
resolves (and with null on cleanup), so the board owner learns the game move without
lifting the reveal query. `TrainSolveScreen` holds that value in state, calls
`buildTrainRevealArrows` in a memo from the puzzle type on the solve verdict, the stored
`GradeResult`'s good-move list, `lastPlayedUci` and that state, and passes the result to
`ChessBoard`'s existing `arrows` prop. Clear the game-move state in the per-puzzle reset
effect.
  </action>
  <verify>
    <automated>( cd frontend && npx vitest run src/lib/__tests__/trainArrows.test.ts src/components/train )</automated>
  </verify>
  <acceptance_criteria>
    - A unit test asserts `buildTrainRevealArrows` returns an empty array when the verdict-landed flag is false, even with a full good-moves list, a played move and a game move supplied.
    - A unit test with a sharp puzzle type and four good moves asserts exactly one green arrow is returned and its color is the `DARK_GREEN` export.
    - A unit test with a soft puzzle type and five good moves asserts exactly three green arrows, in the supplied order; a second case with two good moves asserts exactly two.
    - A unit test asserts a herring puzzle type uses the same cap as soft.
    - A unit test asserts the game-move arrow carries the `NEXT_MOVE_ARROW` color, `onTop` true, and a width strictly smaller than the good-move arrow width.
    - A unit test asserts the played-move arrow carries `TRAIN_PLAYED_MOVE_ARROW` and that its color differs from both the green and the game-move color.
    - A unit test where the played move, a good move and the game move are all the same from-to pair asserts three arrows are returned with three distinct `layerKey` values.
    - A unit test asserts a three-character UCI and an empty string each contribute no arrow and do not throw.
    - A `TrainSolveScreen` test drives the full fake-Worker flow and asserts the `arrows` prop handed to the board is empty before the verdict and non-empty afterwards.
    - `( cd frontend && npm run lint && npx tsc -b )` is clean.
  </acceptance_criteria>
  <done>After solving, the board carries green arrows for the genuinely good moves capped by puzzle type, a distinct arrow for what the user played and a thin white one for what was played in the game — and nothing at all before the attempt is graded.</done>
</task>

<task type="auto">
  <name>Task 2: Running session score on the progress bar</name>
  <files>frontend/src/hooks/useTrainSession.ts, frontend/src/hooks/__tests__/useTrainSession.test.ts, frontend/src/components/train/TrainSolveScreen.tsx, frontend/src/components/train/__tests__/TrainSolveScreen.test.tsx, frontend/src/pages/__tests__/Train.solveLoop.test.tsx</files>
  <read_first>
    - `frontend/src/hooks/useTrainSession.ts` — the whole file: the module docstring's note on the localStorage-backed tally and its cross-device limitation, `sessionScore`, `solvedPositions`, and the solve mutation's `onSuccess`
    - `frontend/src/components/train/TrainSolveScreen.tsx` — the progress row (`train-progress` and `train-progress-bar`) and the frozen-count arithmetic around it
    - `frontend/src/lib/trainScore.ts` — `TRAIN_POINTS_PER_PUZZLE`
    - `frontend/src/hooks/__tests__/useTrainSession.test.ts` — the existing localStorage-seeding and resume tests
    - `.planning/phases/190.1-train-reveal-redesign/190.1-RESEARCH.md` — Open Question 3 on why `currentIndex` is the wrong denominator
  </read_first>
  <action>
Expose the count the score's denominator needs, then render it.

In `useTrainSession`, add `sessionSolvedCount` to `UseTrainSessionResult`, computed as the
session response's frozen solved count plus the size of the internally tracked solved-
positions set. That is the number of puzzles actually scored so far in the session, and it
updates on exactly the same tick as `sessionScore` (both in the solve mutation's success
path), unlike `currentIndex`, which only moves when Next is pressed and would therefore read
stale on the very screen the score is shown. Document the existing cross-device limitation
(the tally is device-local, the frozen count is not) in the field's doc comment rather than
silently papering over it.

In `TrainSolveScreen`, render the score at the top-right of the progress row that already
holds the "i of N" text: keep that text on the left and put the score on the right of the
same flex line, above the existing progress bar, under
`data-testid="train-session-score"`. The score reads the accumulated score, a separator,
and the maximum, which is `sessionSolvedCount` multiplied by the existing
`TRAIN_POINTS_PER_PUZZLE` constant — never an inlined multiplier — followed by the points
unit. Render the element only when `sessionSolvedCount` is greater than zero, so the first
puzzle of a fresh session does not show a zero-out-of-zero score. Use the same text size
and weight as the "i of N" text; the score is peer information, not a heading.

The progress row already renders identically on mobile and desktop — do not add a second
markup branch.
  </action>
  <verify>
    <automated>( cd frontend && npx vitest run src/hooks/__tests__/useTrainSession.test.ts src/components/train/__tests__/TrainSolveScreen.test.tsx src/pages/__tests__/Train.solveLoop.test.tsx )</automated>
  </verify>
  <acceptance_criteria>
    - A `useTrainSession` test asserts `sessionSolvedCount` is the session's frozen solved count before any solve, and increases by one after a successful solve mutation.
    - A `useTrainSession` test asserts a failed solve mutation does not increase `sessionSolvedCount`.
    - A `TrainSolveScreen` test asserts `train-session-score` is absent on the first puzzle of a fresh session before any solve.
    - A `TrainSolveScreen` test drives one puzzle to a correct-guess correct-move verdict and asserts `train-session-score` text contains the accumulated score and a maximum equal to one times `TRAIN_POINTS_PER_PUZZLE`.
    - A `Train.solveLoop` test seeds a resumed session (a non-zero frozen solved count plus a localStorage-stored tally) and asserts the rendered score and maximum reflect the resumed values rather than restarting from zero.
    - A test asserts `train-session-score` and `train-progress` are siblings in the same row, with `train-progress-bar` below them.
    - `( cd frontend && npm run lint && npx tsc -b )` is clean.
  </acceptance_criteria>
  <done>The running score is visible on the progress row throughout the loop and shows the resumed value after a reload rather than restarting at zero.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| grading-engine output -> rendered board arrows | Engine-derived move strings turned into SVG geometry |
| localStorage -> session score display | Client-writable storage feeding a displayed number |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-190.1-14 | Information Disclosure | reveal arrows drawn before the attempt | high | mitigate | `buildTrainRevealArrows` returns an empty array unless the verdict-landed flag is true, and the flag is derived from the server solve response, not from local grading state. Asserted by a dedicated unit test and by a solve-screen test checking the board's arrows prop is empty pre-verdict. |
| T-190.1-15 | Tampering | malformed engine move strings reaching arrow geometry | medium | mitigate | Short or malformed UCI values are skipped in the arrow builder and contribute nothing; asserted by a unit test. `dedupeArrowsByMove` and the geometry helpers are unchanged. |
| T-190.1-16 | Spoofing | localStorage-backed score display | low | accept | The tally is a display-only convenience already shipped in Phase 190 (190-04 D-03); nothing server-side trusts it and no reward depends on it. The denominator now comes from the server's frozen solved count plus in-session solves, which makes a tampered tally more obviously inconsistent rather than less. |
| T-190.1-17 | Denial of Service | unbounded arrow count | low | mitigate | The green-arrow count is capped by a named per-puzzle-type constant, and the played and game arrows are at most one each — at most five arrows regardless of engine output. |
| T-190.1-SC | Tampering | npm/pip/cargo installs | high | accept | No packages are installed in this plan. |
</threat_model>

<verification>
- `( cd frontend && npx vitest run src/lib src/hooks src/components/train src/pages/__tests__/Train.solveLoop.test.tsx )` — green
- `( cd frontend && npm run lint && npx tsc -b )` — clean
- `( cd frontend && npm test -- --run )` — full frontend suite green before the checkpoint plan
</verification>

<success_criteria>
The reveal board shows the good moves, the played move and the game move as distinguishable
arrows with a puzzle-type-correct green count and nothing before the attempt; the running
score sits at the top-right of the progress bar and survives a resume.
</success_criteria>

<output>
Create `.planning/phases/190.1-train-reveal-redesign/190.1-04-SUMMARY.md` when done
</output>
