---
phase: 190.1
slug: train-reveal-redesign
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-26
---

# Phase 190.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (frontend) + pytest 8.x with pytest-xdist (backend) |
| **Config file** | `frontend/vite.config.ts` / `pyproject.toml` |
| **Quick run command** | `( cd frontend && npx vitest run src/components/train src/hooks/__tests__/useTrainGradingEngine.test.ts src/pages/__tests__/Train.solveLoop.test.tsx )` |
| **Full suite command** | `( cd frontend && npm run lint && npm test -- --run && npx tsc -b )` plus `uv run pytest -n auto tests/test_train*.py` when backend reveal schema changes |
| **Estimated runtime** | ~60 seconds (quick), ~5 min (full) |

---

## Sampling Rate

- **After every task commit:** Run the quick train-scoped vitest command
- **After every plan wave:** Run the full frontend suite (+ backend train tests if reveal schema touched)
- **Before `/gsd-verify-work`:** Full suite must be green, including `npx tsc -b` (npm lint/test do NOT type-check)
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

*(Filled by the planner — one row per task.)*

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements (vitest + pytest already configured; all four train test files exist from Phase 190).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions | Status |
|----------|-------------|------------|-------------------|--------|
| Arrow rendering legibility (green/white overlap, sizes) | SC-1 | Visual judgement on a real board | Solve a soft puzzle in the browser; confirm ≤3 green arrows + played-move arrow + smaller white game-move arrow are distinguishable | ✅ Discharged — 190.1-05 checkpoint item 1 (desktop + phone width), see `190.1-05-SUMMARY.md` coverage D1 |
| Refutation-search spinner feel | SC-3 | Perceived latency | Reveal a puzzle; PLAYED IN GAME box should populate within ~2s with a spinner meanwhile | ✅ Discharged — 190.1-05 checkpoint item 6; UAT round 9 (`57a1ff1f`) additionally removes the second search entirely when the played move matches a mount-search rank, see `190.1-05-SUMMARY.md` coverage D6 |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** manual-only rows discharged at the 190.1-05 blocking checkpoint (2026-07-26).
The per-task verification map above was never filled by the planner, so `nyquist_compliant`
stays `false` and the frontmatter `status` stays `draft` — `/gsd-validate-phase` was not run
for this phase. Per-plan coverage instead lives in each SUMMARY's `coverage` block.
