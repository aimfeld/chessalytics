---
phase: 190.1-train-reveal-redesign
verified: 2026-07-26T16:00:00Z
status: passed
score: 5/5 phase success criteria verified (SOLV-05, SOLV-02 refinements)
behavior_unverified: 0
overrides_applied: 0
---

# Phase 190.1: Train Reveal Redesign Verification Report

**Phase Goal:** The post-solve reveal becomes a self-contained, engine-consistent study
surface: the board shows what was good and what was played via arrows, the verdict rows
state the answers actually given, and three steppable engine lines (your move, best move,
played-in-game refutation) replace the game card — all evals and lines sourced from the
client-side grading engine so no two numbers can disagree with the verdict.

**Verified:** 2026-07-26
**Status:** passed
**Re-verification:** No — initial verification

This phase carries no new requirement IDs; it refines the SOLV-05 / SOLV-02 reveal surfaces
delivered by Phase 190, per the roadmap insert of 2026-07-26.

## Goal Achievement

### Success Criteria

| # | Criterion | Status | Evidence |
|---|-----------|--------|----------|
| 1 | Board shows green arrows for the actual good moves (better than an inaccuracy per existing expected-score thresholds), count following puzzle type — exactly 1 sharp, up to 3 soft/herring — plus a played-move arrow and a smaller white game-move arrow | ✓ VERIFIED | `frontend/src/lib/trainArrows.ts` exports `TRAIN_SHARP_GOOD_MOVE_ARROWS = 1`, `TRAIN_SOFT_GOOD_MOVE_ARROWS = 3`, and three named widths with the game-move arrow deliberately thinner (`TRAIN_GAME_MOVE_ARROW_WIDTH = 0.18` vs `0.5`). Good moves come from the MultiPV mount search's `goodMoveUcis` (190.1-02), classified against the project's existing `liveFlaw` thresholds — no new magic number. `trainArrows.test.ts` covers verdict-not-landed→empty, per-type caps and ordering, color/width/onTop, coincident-triple distinct `layerKey`s, malformed-UCI no-throw. Legibility discharged live at 190.1-05 item 1. |
| 2 | Verdict rows state the answers given ("Guess: One critical move ✗", "Move: Nc3 ✓"), not bare marks | ✓ VERIFIED | `TrainReveal.tsx` renders the guess verdict as a header-style line and each line box's title as `"{role}: {SAN}"` via `sanFromPlayedUci`, with a correct/incorrect `mark` on the your-move box. Reworked live across UAT rounds 1 (`7550424e`) and 3 (`3a09b574`). Covered in `TrainReveal.test.tsx`. |
| 3 | Three steppable line boxes — your move, best move, played in game — each with an eval, all lines and evals from the client grading engine, never mixed with stored server evals | ✓ VERIFIED | `TrainReveal.tsx` `ROLE_LABELS` = `{your: 'Your move', best: 'Best move', game: 'Played in game'}` built by a role-merging box builder over `CANONICAL_ROLE_ORDER`. Server-side `best_move`/`best_move_san`/`pv` were **retired from the reveal response** in 190.1-03 (`235f0a41`), so no stored eval can reach the panel. **Mutual consistency** was the one live-found defect: UAT round 9 (`57a1ff1f`) now grades and displays a played move from its matching MultiPV mount-search rank, so all boxes share one search by construction; the rare out-of-ranks case falls back to the after-move search with a display clamp that can never read better than the best line. |
| 4 | The full game card is gone, replaced by an opponent + date footer; Analyze and Next sit on one line; Analyze deep-links to the position shown in the puzzle (one ply before the mistake) | ✓ VERIFIED | `grep -rn "GameCard" frontend/src/components/train/` → no matches. `train-reveal-footer` renders `"Game: <TC> · vs <opponent> (<elo>) · <date>"` (`3a09b574`). One-line row is `Solution / Analyze / Next` plus the shared mute toggle (rounds 3 and 4 expanded it beyond the two the criterion names). Deep link: `buildGameAnalysisUrl(puzzle.game_id, puzzle.ply > 0 ? puzzle.ply - 1 : null)` — the off-by-one is documented at the call site and pinned by `TrainSolveScreen.test.tsx` (`ply 20 → 19`, plus a `ply === 0 → no query param` case). |
| 5 | The current session score is visible at the top-right of the progress bar throughout the solve loop and survives a resume (localStorage-backed) | ✓ VERIFIED | `TrainSolveScreen.tsx` renders `train-session-score` as the `justify-between` sibling of `train-progress`, suppressed until at least one puzzle is scored. `useTrainSession.ts` keeps `sessionScore` localStorage-backed per session id with a best-effort write (private-browsing safe), and derives the denominator as `session.solved_count + solvedPositions.size` so a resumed session continues from the server tally rather than restarting at zero. `useTrainSession.test.ts` + `Train.solveLoop.test.tsx` cover the resumed-session case. |

**Score:** 5/5 criteria verified (0 present-but-behavior-unverified)

### Required Artifacts

| Artifact | Status | Details |
|----------|--------|---------|
| `frontend/src/lib/trainArrows.ts` | ✓ VERIFIED | Pure arrow/overlay builder; `buildTrainRevealOverlay`, `buildTrainStepArrows`, `buildTrainStepMarkers`, `classifyTrainMoveQuality`, named count/width/highlight constants |
| `frontend/src/components/train/TrainReveal.tsx` | ✓ VERIFIED | Verdict rows, role-merged line boxes, footer, reveal-time game-move search with a discriminated `idle/loading/ready/error` state |
| `frontend/src/components/train/TrainLineStepper.tsx` | ✓ VERIFIED | Shared stepper for all three boxes; mobile single-line scrolling token row, desktop wrapping block (round 5) |
| `frontend/src/components/train/TrainSolveScreen.tsx` | ✓ VERIFIED | Reveal-board arrows, session score, Solution/Analyze/Next row, points flash |
| `frontend/src/hooks/useTrainGradingEngine.ts` | ✓ VERIFIED | MultiPV mount search, kept after-move PV, `goodMoveUcis`; drops `lowerbound`/`upperbound` info lines so a trailing aspiration-fail line can no longer clobber a full PV (round 4) |
| `app/schemas/train.py` / reveal endpoint | ✓ VERIFIED | Stored `best_move`/`best_move_san`/`pv` retired from the reveal response (190.1-03) |
| `190.1-05-SUMMARY.md` | ✓ VERIFIED | Operator verdict recorded per numbered checkpoint item |

### Plan Completion

| Plan | Status | Commits |
|------|--------|---------|
| 190.1-01 played-in-game engine line (tracer) | ✓ Complete | `8cdfb276`, `6789646b`, `b24af087` |
| 190.1-02 MultiPV mount search | ✓ Complete | `f6675a55`, `dd41b24a`, `a39188af` |
| 190.1-03 reveal panel rebuild + answer-key slimming | ✓ Complete | `e4e76ff7`, `b2cf3aed`, `235f0a41`, `893fcf15` |
| 190.1-04 reveal arrows + session score | ✓ Complete | `20a2702d`, `8cad345b`, `1bf7cb8b` |
| 190.1-05 blocking human checkpoint | ✓ Complete | Task 1 gate `7c2d1bbd`; Task 2 UAT rounds 1–9 (`7550424e` … `57a1ff1f`) plus `f4d62fa4` |

## Verification Method

- **Automated:** full pre-merge gate per `CLAUDE.md` — `ruff format`, `ruff check`, `ty check`, `pytest -n auto`, frontend `npm run lint`, `npm test -- --run`, and `npx tsc -b` (the plan adds `tsc -b` because npm lint/test do not type-check).
- **Manual:** live operator UAT at desktop and phone width against the real dev backend and dev database, across sharp / soft-herring / played-the-best-move / clear-blunder puzzles. Ran as nine numbered fix-and-re-verify rounds rather than one sequential pass; see `190.1-05-SUMMARY.md` for the round trail and the per-item verdict table, including its recording caveat.
- **Static:** direct inspection of the shipped source for each criterion (symbols and evidence cited in the tables above).

## Gaps / Advisory

None blocking. Three notes for the record:

1. **Scope expanded during UAT, by operator request.** Reveal and stepping sounds with a shared mute toggle, the "Points +N" board flash and score-screen "Points x/y" wording, the Beta badge, and the start-page tagline all landed in rounds 4–8. None are in the phase's success criteria; all are live and tested.
2. **Phase 190's opt-in tactic-line stepper was removed from the reveal** in round 4 as confusing next to the three engine-line boxes. This narrows the SOLV-06 surface Phase 190 verified. Deliberate, recorded in `190.1-05-SUMMARY.md` under `key-decisions`.
3. **A backend behavior fix rode along** (`f4d62fa4`): completing a session no longer unlocks a same-day recompose, which was defeating D-10 pacing and could leave a dead Resume button. Phase 191's ad-hoc "train now" (SCHD-03) will need an explicit opt-out of this guard — documented in the compose docstring.

`190.1-VALIDATION.md` keeps `status: draft` / `nyquist_compliant: false`: its per-task
verification map was never filled by the planner, so `/gsd-validate-phase` was not run for
this phase. Per-plan coverage lives in each SUMMARY's `coverage` block instead, and both
manual-only rows are now marked discharged.

## Conclusion

**PASSED.** All five phase success criteria are verified in the shipped code and confirmed
live at both layouts. The phase's central claim — that no two numbers on the reveal can
disagree with the verdict — was the one thing UAT actually broke, and round 9 fixed it
structurally rather than cosmetically by sourcing every box from a single search.
