---
phase: 187-guest-game-cleanup-30-day-inactivity-pruning-seed-116
plan: 02
type: execute
wave: 2
depends_on:
  - 187-01
files_modified:
  - app/services/guest_cleanup_service.py
  - app/main.py
  - tests/test_guest_cleanup_service.py
  - tests/test_main_lifespan.py
autonomous: true
requirements: []   # No REQ-IDs mapped to this phase — contract is CONTEXT.md D-01..D-07 + SEED-116
user_setup: []

must_haves:
  truths:
    - "run_periodic_guest_cleanup sleeps _GUEST_CLEANUP_INTERVAL_SECONDS BEFORE its first tick, then calls cleanup_inactive_guests each tick, mirroring run_periodic_reaper (D-01/D-02)."
    - "A cleanup_inactive_guests exception does NOT break the loop — it is caught per tick, logged, Sentry-captured with set_tag('source','guest_cleanup'), and the loop continues (D-07)."
    - "The guest-cleanup task is spawned via asyncio.create_task in the app/main.py lifespan on startup and cancelled+awaited on shutdown, its CancelledError swallowed and any other shutdown exception logged (not propagated) — exactly like the existing 3 tasks (D-01)."
    - "tests/test_main_lifespan.py EXPECTED_TASKS is corrected to include the previously-missing 'full-eval-drain' AND the new 'guest-cleanup', with a stub/assert for the new task."
  artifacts:
    - "app/services/guest_cleanup_service.py (run_periodic_guest_cleanup added)"
    - "app/main.py (4th background task wired into lifespan)"
    - "tests/test_main_lifespan.py (extended)"
  key_links:
    - "app/main.py lifespan create_task/cancel-await wiring is what actually schedules the daily job — without it the service functions are dead code."
    - "run_periodic_guest_cleanup delegates to Plan 01's cleanup_inactive_guests — the loop shell adds only sleep + per-tick try/except."
---

<objective>
Wrap Plan 01's proven purge orchestration in the in-process periodic asyncio loop and wire it into
the FastAPI lifespan as the 4th background task — a byte-for-byte structural mirror of
run_periodic_reaper / the existing reaper/drain/full-drain wiring. This is horizontal expansion
around the already-verified core: it adds the daily schedule and clean startup/shutdown lifecycle,
no new deletion logic.

Purpose: D-01/D-02 — the cleanup must run automatically on a daily cadence with clean cancellation,
using this repo's only scheduling mechanism (in-process asyncio task, no external cron).
Output: `run_periodic_guest_cleanup` in guest_cleanup_service.py, the lifespan wiring in app/main.py,
loop-mechanics tests, and the corrected/extended test_main_lifespan.py.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/187-guest-game-cleanup-30-day-inactivity-pruning-seed-116/187-CONTEXT.md
@.planning/phases/187-guest-game-cleanup-30-day-inactivity-pruning-seed-116/187-RESEARCH.md
@.planning/phases/187-guest-game-cleanup-30-day-inactivity-pruning-seed-116/187-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

NEW symbols introduced by this plan:
- Function: `run_periodic_guest_cleanup() -> None` (in app/services/guest_cleanup_service.py)
- Lifespan wiring in app/main.py: `guest_cleanup_task = asyncio.create_task(run_periodic_guest_cleanup(), name="guest-cleanup")` + its cancel/await block
- New import in app/main.py: `from app.services.guest_cleanup_service import run_periodic_guest_cleanup`
- Test class `TestRunPeriodicGuestCleanup` in tests/test_guest_cleanup_service.py
- Corrected `EXPECTED_TASKS` + a guest-cleanup stub/assert in tests/test_main_lifespan.py
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: run_periodic_guest_cleanup loop + loop-mechanics tests</name>
  <files>app/services/guest_cleanup_service.py, tests/test_guest_cleanup_service.py</files>
  <read_first>
    - app/services/import_service.py (run_periodic_reaper, ~lines 332-359) — the verbatim loop shape
      to mirror: `while True: await asyncio.sleep(_INTERVAL); try: await do_work() except Exception:
      logger.exception(...) + sentry_sdk.set_tag("source", ...) + sentry_sdk.capture_exception()`.
      Sleep-before-first-tick (T=0 handled elsewhere / not at all here — daily job, no startup call).
    - app/services/guest_cleanup_service.py (Plan 01 output) — cleanup_inactive_guests is the do_work().
    - tests/test_import_service.py (TestRunPeriodicReaper: test_reaper_calls_cleanup_at_interval,
      test_reaper_survives_cleanup_exception) — the monkeypatch-`asyncio.sleep` + monkeypatch-inner-fn
      test shape to mirror (drive one iteration, assert the inner fn was called; make the inner fn
      raise once, assert the loop caught it and would continue).
  </read_first>
  <action>
    Add `run_periodic_guest_cleanup() -> None` to guest_cleanup_service.py as a structural 1:1 mirror
    of run_periodic_reaper: an infinite `while True` that first `await asyncio.sleep(_GUEST_CLEANUP_INTERVAL_SECONDS)`
    then in a try calls `await cleanup_inactive_guests()`, and in `except Exception` calls
    `logger.exception("Periodic guest cleanup failed")` then `sentry_sdk.set_tag("source", "guest_cleanup")`
    then `sentry_sdk.capture_exception()`. Docstring: notes it sleeps BEFORE the first tick and is wired
    into app/main.py lifespan (started on startup, cancelled+awaited on shutdown) alongside the other 3
    background tasks. In tests/test_guest_cleanup_service.py add `TestRunPeriodicGuestCleanup` mirroring
    TestRunPeriodicReaper: (a) monkeypatch `asyncio.sleep` to raise a sentinel after N calls (or use a
    call-counter that cancels the task) so the loop runs exactly one iteration, monkeypatch
    cleanup_inactive_guests to a spy, assert the spy was called once; (b) monkeypatch
    cleanup_inactive_guests to raise, drive one iteration, assert the exception was swallowed (loop did
    not propagate) and sentry_sdk.capture_exception was invoked. Keep these as fast unit tests — no DB.
  </action>
  <behavior>
    - run_periodic_guest_cleanup sleeps _GUEST_CLEANUP_INTERVAL_SECONDS, then calls cleanup_inactive_guests once per iteration.
    - A raise from cleanup_inactive_guests is caught, logged, and Sentry-captured; the loop does not propagate the exception.
  </behavior>
  <verify>
    <automated>uv run pytest tests/test_guest_cleanup_service.py::TestRunPeriodicGuestCleanup -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "def run_periodic_guest_cleanup" app/services/guest_cleanup_service.py` succeeds.
    - `grep -q "_GUEST_CLEANUP_INTERVAL_SECONDS" app/services/guest_cleanup_service.py` succeeds (interval used in the loop).
    - `uv run pytest tests/test_guest_cleanup_service.py::TestRunPeriodicGuestCleanup -x` exits 0.
    - `uv run ty check app/services/guest_cleanup_service.py` reports zero errors.
  </acceptance_criteria>
  <done>The periodic loop calls the orchestration each tick and survives exceptions; unit tests green; committed.</done>
  <reversibility rating="reversible">A thin loop wrapper over Plan 01's orchestration; interval is a named constant, trivially tunable.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Wire the 4th background task into the app/main.py lifespan + fix test_main_lifespan drift</name>
  <files>app/main.py, tests/test_main_lifespan.py</files>
  <read_first>
    - app/main.py (lifespan, ~lines 82-150) — the exact 3-task pattern: the import at line 32, the three
      `asyncio.create_task(..., name=...)` calls (~lines 106-114), the three `.cancel()` calls (~lines
      123-125), and the three per-task await-CancelledError/except-Exception/logger.exception blocks
      (~lines 126-144). Insert the 4th task LAST in each group (no ordering
      dependency with stop_engine/stop_maia — guest cleanup never touches the engine).
    - app/services/guest_cleanup_service.py (Plan 02 Task 1 output) — run_periodic_guest_cleanup to import.
    - tests/test_main_lifespan.py — EXPECTED_TASKS tuple (currently `("periodic-orphan-reaper", "eval-drain")`
      — stale, missing "full-eval-drain") and TestLifespanBackgroundTasks.test_both_background_tasks_spawned
      (the `_stub_reaper`/`_stub_drain` monkeypatch + `await asyncio.sleep(0)` + assert-called shape).
      Note run_eval_drain AND run_full_eval_drain both live in app.services.eval_drain and are imported
      into app.main; patch them at their `app.main.<symbol>` attribute path.
  </read_first>
  <action>
    In app/main.py: add `from app.services.guest_cleanup_service import run_periodic_guest_cleanup`
    alongside the existing import_service import. In the lifespan, after the full_drain_task create_task,
    add `guest_cleanup_task = asyncio.create_task(run_periodic_guest_cleanup(), name="guest-cleanup")`
    with a one-line comment citing Phase 187 / SEED-116 / D-01. In the finally block add
    `guest_cleanup_task.cancel()` alongside the other three cancels, and add a fourth
    `try: await guest_cleanup_task except asyncio.CancelledError: pass except Exception:
    logger.exception("Guest cleanup task raised on shutdown")` block placed after the full_drain_task
    block (before the `finally: stop_engine()/stop_maia()`). In tests/test_main_lifespan.py: correct
    EXPECTED_TASKS to `("periodic-orphan-reaper", "eval-drain", "full-eval-drain", "guest-cleanup")`
    (widen the type annotation from the 2-tuple to a variable-length tuple, e.g. `tuple[str, ...]`), and
    extend test_both_background_tasks_spawned to also monkeypatch `app.main.run_full_eval_drain` and
    `app.main.run_periodic_guest_cleanup` with recording stubs (mirroring `_stub_reaper`/`_stub_drain`)
    and assert all four stubs were called during startup. Do not weaken the existing reaper/drain asserts.
  </action>
  <verify>
    <automated>uv run pytest tests/test_main_lifespan.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "run_periodic_guest_cleanup" app/main.py` succeeds (import + create_task).
    - `grep -q 'name="guest-cleanup"' app/main.py` succeeds.
    - `grep -q "guest_cleanup_task.cancel()" app/main.py` succeeds.
    - `grep -q '"guest-cleanup"' tests/test_main_lifespan.py` AND `grep -q '"full-eval-drain"' tests/test_main_lifespan.py` both succeed.
    - `uv run pytest tests/test_main_lifespan.py -x` exits 0.
    - `uv run ty check app/main.py tests/test_main_lifespan.py` reports zero errors.
  </acceptance_criteria>
  <done>The guest-cleanup task is spawned+cancelled in the lifespan and the lifespan smoke test asserts all four named tasks; committed.</done>
  <reversibility rating="reversible">Additive lifespan wiring mirroring 3 existing tasks; removable by deleting the 4 added lines with no schema impact.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lifespan startup/shutdown → background task | The task is spawned/cancelled by the process lifecycle; a shutdown exception must not leak and abort clean engine teardown. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-187-06 | DoS (task leak / unclean shutdown) | lifespan cancel/await block | medium | mitigate | Mirror the existing 3-task cancel-then-await pattern exactly; CancelledError swallowed, any other shutdown exception logged (not propagated); test_main_lifespan asserts the task is spawned and the context exits without propagating. |
| T-187-07 | DoS (silent scheduler failure) | run_periodic_guest_cleanup loop | medium | mitigate | Per-tick try/except with logger.exception + sentry_sdk.capture_exception (set_tag source=guest_cleanup); TestRunPeriodicGuestCleanup proves the loop survives an exception. |
| T-187-08 | Repudiation (undetected drift) | test_main_lifespan EXPECTED_TASKS staleness | low | mitigate | Correct the pre-existing "full-eval-drain" omission and add "guest-cleanup" so the smoke test actually gates all four background tasks going forward. |
</threat_model>

<verification>
- `uv run pytest tests/test_guest_cleanup_service.py tests/test_main_lifespan.py -x` green.
- `uv run pytest -n auto` full backend suite green (per-wave gate).
- `uv run ty check app/ tests/` reports zero errors.
- `uv run ruff check app/main.py app/services/guest_cleanup_service.py tests/` clean.
</verification>

<success_criteria>
- run_periodic_guest_cleanup runs the daily cadence and survives per-tick exceptions (D-01/D-02/D-07).
- The 4th background task is spawned on startup and cancelled+awaited cleanly on shutdown (D-01).
- test_main_lifespan gates all four named tasks (drift fixed).
- Full suite + ty green.
</success_criteria>

<output>
Create `.planning/phases/187-guest-game-cleanup-30-day-inactivity-pruning-seed-116/187-02-SUMMARY.md` when done
</output>
