---
phase: 188-import-eval-pipeline-cleanup-seed-115
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/eval_queue_service.py
  - app/services/eval_drain.py
  - app/routers/eval_remote.py
  - app/models/game.py
  - scripts/backfill_eval.py
  - scripts/backfill_full_evals.py
  - scripts/backfill_best_move_pv.py
  - scripts/backfill_multipv.py
  - scripts/backfill_opening_eval_cache.py
  - scripts/snapshot_tactic_counts.py
  - scripts/backfill_accuracy_acpl.py
  - scripts/resweep_holed_games.py
  - alembic/versions/
  - CHANGELOG.md
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09]
tags: [eval-pipeline, cleanup, backfill, alembic, maintenance]

must_haves:
  truths:
    - "Dead tier-2 scheduling prose is removed from the eval-pipeline cluster and claim behavior is byte-identical (D-03)."
    - "resweep_holed_games is reframed as the permanent Path-C mid-game-hole re-arm tool; its runtime behavior is unchanged (D-01)."
    - "The 7 completed backfill scripts live under scripts/archive/; the 2 with pruned imports (backfill_best_move_pv.py, backfill_multipv.py) still import cleanly (D-04)."
    - "Exactly 3 re-export symbols are pruned from eval_drain.py; every remaining importer (tests + scripts/remote_eval_worker.py) still resolves (D-06)."
    - "eval_remote.py stale /lease + /submit 'live and deprecated' docstring claims are corrected while the still-live /flaw-blob-submit claim is preserved (D-05)."
    - "ix_games_bestmove_backfill_pending's predicate matches _claim_tier4_bestmove verbatim, and the model Index text is byte-identical to the migration's op.create_index text (D-07)."
    - "No worker-facing endpoint, submit semantics, tier-4/4b logic, timestamp column, or fenced item changed; the full backend suite and ty check are green (D-02, D-08, D-09)."
  artifacts:
    - "alembic/versions/<new>_realign_ix_games_bestmove_backfill_pending.py (down_revision f09f8dee4aee)"
    - "scripts/archive/backfill_eval.py, backfill_full_evals.py, backfill_best_move_pv.py, backfill_multipv.py, backfill_opening_eval_cache.py, snapshot_tactic_counts.py, backfill_accuracy_acpl.py"
    - "modified app/services/eval_queue_service.py, app/services/eval_drain.py, app/routers/eval_remote.py, app/models/game.py"
  key_links:
    - "model Index sa.text(...) predicate <-> migration op.create_index postgresql_where sa.text(...) (must be byte-identical)"
    - "_claim_tier4_bestmove Stage-1/Stage-2 WHERE predicate <-> ix_games_bestmove_backfill_pending index predicate"
    - "pruned symbols (_batch_update_best_move_rows, _batch_update_pv_rows, _batch_update_flaw_pv_lines) <-> their real owner app.services.eval_apply (archived scripts repointed here)"
    - "10 surviving eval_drain re-exports <-> their test + remote_eval_worker.py importers"
---

<objective>
Retire the completed historical-backfill machinery from the import/eval pipeline (SEED-115
base scope) with the two CONTEXT.md amendments locked: KEEP `resweep_holed_games` (reframed,
D-01) and KEEP tiers 4/4b as permanent safety nets with no submit-semantics/protocol change
(D-02). All nine decisions (D-01..D-09) are covered here.

This is a cognitive-load reduction with no user-facing behavior change and no remote-worker
protocol change. RESEARCH.md verified every cited location against the live tree; the surgery
is docstring/comment trims, 7 git-mv archivals, 3 re-export deletions (+2 script import
repoints), and one non-concurrent partial-index realignment migration.

Note on task shape: TRACER_MODE is on globally, but a tracer slice is not meaningful for a
deletion/documentation phase — there is no new end-to-end capability to prove. Tasks are
ordered by dependency coupling instead: (1) independent docstring surgery, (2) the coupled
re-export prune + script archival (must share one commit), (3) the coupled model+migration
index realignment (must share one commit).

Purpose: Match the code and its documentation to the pipeline's real post-Phase-119/149
shape, and stop a stale partial index from mis-serving `_claim_tier4_bestmove`.
Output: Trimmed docstrings, a scripts/archive/ of 7 scripts, a leaner eval_drain re-export
block, one Alembic migration, and a CHANGELOG entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/188-import-eval-pipeline-cleanup-seed-115/188-CONTEXT.md
@.planning/phases/188-import-eval-pipeline-cleanup-seed-115/188-RESEARCH.md
@.planning/phases/188-import-eval-pipeline-cleanup-seed-115/188-PATTERNS.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**Created:**
- `alembic/versions/<timestamp>_realign_ix_games_bestmove_backfill_pending.py` — new migration; `down_revision = "f09f8dee4aee"` (current head, confirmed).
- `scripts/archive/backfill_eval.py`
- `scripts/archive/backfill_full_evals.py`
- `scripts/archive/backfill_best_move_pv.py` (moved + 1-line import repoint)
- `scripts/archive/backfill_multipv.py` (moved + 1-line import repoint)
- `scripts/archive/backfill_opening_eval_cache.py`
- `scripts/archive/snapshot_tactic_counts.py`
- `scripts/archive/backfill_accuracy_acpl.py`

**Changed index predicate** (`ix_games_bestmove_backfill_pending`):
- From: `full_pv_completed_at IS NOT NULL AND best_moves_completed_at IS NULL AND lichess_evals_at IS NULL`
- To:   `full_pv_completed_at IS NOT NULL AND best_moves_completed_at IS NULL`
- Applied in BOTH `app/models/game.py` `Index(...)` and the migration `op.create_index(...)`, byte-identical.

**Pruned re-export symbols** (deleted from `app/services/eval_drain.py`'s `from app.services.eval_apply import (...)` block, exactly these 3):
- `_batch_update_best_move_rows`
- `_batch_update_pv_rows`
- `_batch_update_flaw_pv_lines`

**Removed (git mv out of `scripts/`):** the 7 backfill/snapshot scripts listed above.

**Docstring-only edits (no symbol change):** `resweep_holed_games` in `eval_drain.py`;
tier-2 scheduling prose in `eval_queue_service.py`; stale `/lease`+`/submit` claims in
`eval_remote.py`.

**Explicitly NOT changed (D-09 fence, D-02/D-08):** the 8 live worker endpoints
(`/atomic-lease`, `/entry-lease`, `/entry-submit`, `/flaw-blob-lease`, `/flaw-blob-submit`,
`/atomic-submit`, `/bestmove-lease`, `/bestmove-submit`), `_tier4b_minimal_drain_tick`,
tier-3/tier-4/tier-4b logic, Path-C hole tolerance, all five completion-timestamp columns,
`OPENING_CACHE_BACKFILL_SQL`, `apply_game_filters`, and the 3 active scripts
(`backfill_flaws.py`, `retag_flaws.py`, `reimport_games.py`).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Docstring surgery — reframe resweep (D-01), trim dead tier-2 prose (D-03), fix stale /lease+/submit claims (D-05)</name>
  <files>app/services/eval_drain.py, app/services/eval_queue_service.py, app/routers/eval_remote.py, scripts/resweep_holed_games.py</files>
  <read_first>
    - RESEARCH.md section "Verified Facts vs. Seed Drift" items 1 (tier-2 docstring table), 2 (five-way "tier 2" homonym warning), 4 (eval_remote.py stale-docstring exact locations), 6 (resweep reframe draft text)
    - PATTERNS.md sections "resweep_holed_games docstring rewrite (D-01)", "eval_remote.py — stale docstring fix (D-05)", "eval_queue_service.py — tier-2 docstring/comment trim (D-03)"
    - app/services/eval_drain.py (re-read resweep_holed_games + docstring around lines 1151-1289 to re-confirm current line numbers before editing — research warns of drift)
    - app/services/eval_apply.py (~700-790, apply_completion_decision Path C — the mechanism the new resweep docstring must cite by name)
    - app/services/eval_queue_service.py (module docstring ~1-14, docstrings/comments at ~232, ~790, ~842, ~864, ~957)
    - app/routers/eval_remote.py (module docstring ~1-8, docstrings at ~428-429 and ~1313-1316)
  </read_first>
  <action>
    Docstring/comment edits only — zero runtime-logic lines change in any of these files.

    D-01 (eval_drain.py resweep_holed_games): Rewrite ONLY the motivating paragraph
    (~lines 1156-1162) from the "pre-Phase-119 legacy, population gone" framing to the
    "permanent manual re-arm tool for Path-C mid-game holes (weak-worker failure mode)"
    framing per RESEARCH.md item 6's drafted text: name `apply_completion_decision` Path C as
    the mechanism that deliberately stamps completion markers when
    `current_attempts + 1 >= MAX_EVAL_ATTEMPTS` with `failed_ply_count > 0`, state there is no
    population-exhaustion date, and demote the old D-116-07 pre-Phase-119 story to a
    parenthetical historical note. KEEP unchanged the hole-definition body and the
    SEED-045/SEED-049 terminal-ply + game-ending-move exclusions (~1163-1193). Optionally add a
    one-line "still load-bearing, not legacy" note to scripts/resweep_holed_games.py's
    already-neutral module docstring for consistency.

    D-03 (eval_queue_service.py): There is NO code to delete — the middle-tier constant was
    removed in Phase 149-04 and the claim SQL is tier-agnostic (`ORDER BY ej.tier ASC`). Trim the
    stale scheduling prose at the RESEARCH-item-1-table locations: condense the ~12-line
    module-docstring paragraph about the retired middle tier to 1-2 lines noting it is fully
    retired (Phase 118 enqueue removal + Phase 149-04 constant removal), keeping the
    tier-agnostic-SQL forward-compat note; drop the stale middle-tier hop from the
    `_claim_queued_job` (~232), `claim_eval_job` (~790 ordering line), the ~842 comment, and the
    `release_job` (~957) docstrings so each reads the real ordering (tier-1 > tier-3 > tier-4).
    Leave the ~864 comment as-is (accurate). CRITICAL: scope every verification grep to the
    eval-pipeline cluster only — a repo-wide search for the middle-tier term hits 3 unrelated
    subsystems (import-readiness, endgame combos, tactic detector) per RESEARCH item 2. Do NOT
    touch eval_jobs.py, imports.py, endgame_service.py, or tactic_detector.py.

    D-05 (eval_remote.py): At ~428-429 (`atomic_lease_eval_game` docstring), remove the claim
    that the Gen-1 `/lease` and `/submit` endpoints stay live and deprecated for a mixed-fleet
    deploy — they were deleted in Phase 149-03 (the module docstring 20 lines above already says
    so). At ~1313-1314 (`atomic_submit_eval` docstring), the claim is MIXED: remove only the
    `/submit` half (deleted), but KEEP the `/flaw-blob-submit` half accurate — that endpoint is
    still live (`@router.post("/flaw-blob-submit", ...)` ~985, D-09-fenced). Leave the
    already-past-tense "Unlike the old /submit..." sentence (~1315-1316) unchanged. Optional
    (RESEARCH Open Question 1, planner's discretion — do it if the docstring block is already
    open): fix the stale three-tier ordering mentions at eval_remote.py ~17/~430 and
    eval_drain.py ~861; skip if it widens the diff awkwardly.

    Do NOT alter any endpoint body, route decorator, schema, or the `require_operator_token`
    auth dependency (~163-193) — D-08.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_eval_queue.py tests/services/test_eval_drain.py tests/services/test_full_eval_drain.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat` shows changes limited to eval_drain.py, eval_queue_service.py, eval_remote.py, and (optionally) scripts/resweep_holed_games.py.
    - `git diff app/routers/eval_remote.py` shows ONLY docstring lines changed (no line inside any `@router.post` handler body, no change to lines ~163-193).
    - Reframed resweep docstring names `apply_completion_decision`; `grep -c "SEED-049" app/services/eval_drain.py` is >= 1 (exclusion text preserved).
    - The `claim_eval_job` ordering docstring no longer lists a middle tier between tier-1 and tier-3 (read the edited docstring to confirm it now reads tier-1 > tier-3 > tier-4).
    - `grep -c "flaw-blob-submit" app/routers/eval_remote.py` is unchanged from baseline (>= 1).
    - `uv run ruff check app/services/eval_drain.py app/services/eval_queue_service.py app/routers/eval_remote.py` exits 0.
    - `uv run pytest tests/services/test_eval_queue.py tests/services/test_eval_drain.py tests/services/test_full_eval_drain.py -x` exits 0.
  </acceptance_criteria>
  <reversibility rating="reversible">Pure docstring/comment edits; a plain git revert restores prior text with zero data or schema impact.</reversibility>
  <done>resweep is reframed (D-01), dead middle-tier scheduling prose is trimmed cluster-locally (D-03), and the stale /lease+/submit claims are corrected while /flaw-blob-submit stays accurate (D-05); the eval-pipeline test subset is green.</done>
</task>

<task type="auto">
  <name>Task 2: Prune 3 eval_drain re-exports (D-06) + archive 7 backfill scripts with import repoints (D-04) — single coupled commit</name>
  <files>app/services/eval_drain.py, scripts/backfill_eval.py, scripts/backfill_full_evals.py, scripts/backfill_best_move_pv.py, scripts/backfill_multipv.py, scripts/backfill_opening_eval_cache.py, scripts/snapshot_tactic_counts.py, scripts/backfill_accuracy_acpl.py</files>
  <read_first>
    - RESEARCH.md item 3 (the 13-symbol re-export audit table, the "only 3 prunable" verdict, the exact import-repoint targets for the 2 scripts, and the "Scripts to smoke-check after archival" note)
    - PATTERNS.md sections "eval_drain.py — re-export block prune (D-06)" and "scripts/archive/*.py — archival convention"
    - app/services/eval_drain.py (re-read the `from app.services.eval_apply import (...)` block ~57-105 to confirm the exact current lines of the 3 prune targets before deleting)
    - scripts/backfill_best_move_pv.py (its `from app.services.eval_drain import ...` line ~122-125)
    - scripts/backfill_multipv.py (its `from app.services.eval_drain import ...` line ~62-65)
  </read_first>
  <action>
    Coupled work — the prune and the script repoints MUST land in the SAME commit, because the
    2 archived scripts import symbols the prune removes.

    D-06: In eval_drain.py's `from app.services.eval_apply import (...)` re-export block, delete
    EXACTLY these 3 lines: `_batch_update_best_move_rows`, `_batch_update_pv_rows`,
    `_batch_update_flaw_pv_lines` (each carries a `# noqa: F401` backward-compat tag). Do NOT
    remove any other symbol — the other 10 noqa lines stay (test-imported and/or used by
    scripts/remote_eval_worker.py, the ACTIVE non-archived worker driver, via `_walk_pv_boards`),
    even though several of their noqa comments under-claim their real importer set. Fix the
    trailing comma/formatting so ruff is happy.

    D-04: `git mv` all 7 scripts into scripts/archive/ (byte-identical move, per commit 1d74a8e8):
    backfill_eval.py, backfill_full_evals.py, backfill_best_move_pv.py, backfill_multipv.py,
    backfill_opening_eval_cache.py, snapshot_tactic_counts.py, backfill_accuracy_acpl.py. Then
    repoint the 2 scripts that lose imports (1-line edit each, same commit):
    - scripts/archive/backfill_best_move_pv.py: change its import of `_batch_update_best_move_rows,
      _batch_update_pv_rows` from `app.services.eval_drain` to `app.services.eval_apply`.
    - scripts/archive/backfill_multipv.py: change ONLY `_batch_update_flaw_pv_lines` to import from
      `app.services.eval_apply`; its other two imports (`_assemble_flaw_blobs_from_submit`,
      `_build_flaw_blob_lease_positions`) STAY pointed at `app.services.eval_drain` (not pruned).

    KEEP `OPENING_CACHE_BACKFILL_SQL` in eval_drain.py (D-04 — gate-equivalence tests use it). Do
    NOT touch backfill_flaws.py, retag_flaws.py, reimport_games.py (D-09 active tools).

    Verification ORDER matters (RESEARCH pitfall 2): run `--collect-only` on the affected test
    files FIRST — an over-prune surfaces as an ImportError at collection time, not a lint warning
    — then do the real run and the archived-script import smoke check.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_eval_drain.py tests/services/test_full_eval_drain.py tests/test_eval_worker_endpoints.py tests/test_remote_eval_worker.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/services/test_eval_drain.py tests/services/test_full_eval_drain.py tests/test_eval_worker_endpoints.py --collect-only -q` succeeds (no ImportError) — run this BEFORE the real run.
    - Exactly 3 symbols removed: `grep -c "_batch_update_best_move_rows\|_batch_update_pv_rows\|_batch_update_flaw_pv_lines" app/services/eval_drain.py` returns 0.
    - `grep -c "_walk_pv_boards" app/services/eval_drain.py` is >= 1 (a surviving re-export used by the active remote worker driver was NOT removed).
    - `grep -c "OPENING_CACHE_BACKFILL_SQL" app/services/eval_drain.py` is >= 1 (kept per D-04).
    - `git status` shows all 7 scripts renamed under scripts/archive/ (`git log --diff-filter=R` / `git status` reflect renames), and backfill_flaws.py, retag_flaws.py, reimport_games.py remain in scripts/.
    - `uv run python -c "import scripts.archive.backfill_best_move_pv, scripts.archive.backfill_multipv"` exits 0 (repointed imports resolve).
    - `uv run pytest tests/services/test_eval_drain.py tests/services/test_full_eval_drain.py tests/test_eval_worker_endpoints.py tests/test_remote_eval_worker.py -x` exits 0.
    - `uv run ruff check app/services/eval_drain.py scripts/archive/` exits 0.
  </acceptance_criteria>
  <reversibility rating="reversible">git mv renames and a 3-line import deletion; fully restorable by revert, no schema/data impact.</reversibility>
  <done>The 3 sole-use re-exports are pruned (D-06), the 7 completed scripts are archived with the 2 dependent ones repointed to eval_apply (D-04), OPENING_CACHE_BACKFILL_SQL and all 10 surviving re-exports stay, and the eval-pipeline + worker-endpoint tests are green.</done>
</task>

<task type="auto">
  <name>Task 3: Realign ix_games_bestmove_backfill_pending — model edit + non-concurrent Alembic migration (D-07), single coupled commit</name>
  <files>app/models/game.py, alembic/versions/</files>
  <read_first>
    - RESEARCH.md item 5 (current index text, current query predicate, target fix, migration precedent, byte-identical-text requirement, and the confirmed current head `f09f8dee4aee`)
    - PATTERNS.md sections "alembic/versions/<new>_realign_... (migration)" and "app/models/game.py — Index definition (model)" (they carry the exact upgrade()/downgrade() body to copy)
    - alembic/versions/20260716_171823_1eda5daba951_phase_174_07_lichess_best_move_backfill_.py (the exact-shape precedent migration to mirror, incl. its non-concurrent rationale docstring)
    - app/models/game.py (~94-104, the Index(...) call + its byte-identical-text drift-lesson comment)
    - app/services/eval_queue_service.py (`_claim_tier4_bestmove` Stage-1 EXISTS subquery + Stage-2 WHERE — the predicate the index must match verbatim)
  </read_first>
  <action>
    Coupled work — the model Index edit and the migration MUST land in the SAME commit; the
    `sa.text(...)` predicate string must be character-for-character identical between them (the
    drift-lesson comment at game.py:95-96 exists precisely to enforce this).

    D-07: In app/models/game.py's `ix_games_bestmove_backfill_pending` Index(...), drop the
    trailing ` AND lichess_evals_at IS NULL` clause so the `postgresql_where=sa.text(...)` reads
    exactly: `full_pv_completed_at IS NOT NULL AND best_moves_completed_at IS NULL`. This matches
    `_claim_tier4_bestmove`'s live predicate (quick 260719-fsz dropped the lichess_evals_at clause
    from the query; this realigns the index to the query, not the reverse). Keep the
    byte-identical-text drift-lesson comment.

    Hand-write the migration (do NOT rely on --autogenerate for a postgresql_where text-only diff;
    generate the scaffold with `uv run alembic revision -m "realign ix_games_bestmove_backfill_pending predicate"`
    then write the body). `down_revision = "f09f8dee4aee"` (confirmed current head). Mirror the
    174-07 precedent EXACTLY: non-concurrent `op.drop_index(...)` + `op.create_index("...", "games",
    ["user_id"], unique=False, postgresql_where=sa.text(...))`. `upgrade()` drops the old 3-clause
    index and creates the 2-clause form; `downgrade()` is the mirror image (drops the 2-clause,
    recreates the original 3-clause predicate — copy the 3-clause text verbatim from the current
    game.py before you edit it). Do NOT introduce `postgresql_concurrently=True` — migrations run
    pre-traffic at container startup (deploy/entrypoint.sh runs `alembic upgrade head` before
    uvicorn), and 5+ prior partial-index migrations on this table are all non-concurrent.

    The new create_index `postgresql_where` string must be byte-identical to the string you put in
    game.py's Index(...). Verify by eye and by the acceptance grep below.
  </action>
  <verify>
    <automated>uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head</automated>
  </verify>
  <acceptance_criteria>
    - `uv run alembic upgrade head` exits 0 against the running dev DB (Docker Postgres on localhost:5432 must be up).
    - `uv run alembic downgrade -1 && uv run alembic upgrade head` round-trips cleanly (downgrade symmetry proven).
    - The new migration's `down_revision` is `"f09f8dee4aee"` and `uv run alembic heads` shows a single head after upgrade.
    - Model predicate has no lichess clause: `grep -c "lichess_evals_at" app/models/game.py` returns 0 inside the ix_games_bestmove_backfill_pending Index block (confirm by reading the block; the string appears only in the migration's downgrade()).
    - The `postgresql_where` text in the migration's `op.create_index` for ix_games_bestmove_backfill_pending is byte-identical to app/models/game.py's Index `sa.text(...)` — both read `full_pv_completed_at IS NOT NULL AND best_moves_completed_at IS NULL`.
    - Live predicate matches the query: after upgrade, `SELECT indexdef FROM pg_indexes WHERE indexname = 'ix_games_bestmove_backfill_pending'` (via the flawchess-db MCP or psql) shows a WHERE of `(full_pv_completed_at IS NOT NULL AND best_moves_completed_at IS NULL)` with no lichess_evals_at term.
    - No `CONCURRENTLY` in the migration: `grep -c "concurrently" alembic/versions/*realign_ix_games_bestmove*` returns 0.
    - `uv run ty check app/models/game.py` exits 0.
  </acceptance_criteria>
  <reversibility rating="reversible">The migration ships a symmetric `downgrade()` that recreates the original 3-clause index; the op is a non-destructive index drop/recreate on a near-empty matching set, run pre-traffic. A rollback is one `alembic downgrade -1`.</reversibility>
  <done>The index predicate matches `_claim_tier4_bestmove` verbatim, model and migration texts are byte-identical, and `alembic upgrade head` + a downgrade/upgrade round-trip succeed on the dev DB (D-07).</done>
</task>

<task type="auto">
  <name>Task 4: CHANGELOG entry + full pre-merge backend gate</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (the current `## [Unreleased]` section and its `### Changed` / `### Removed` subsections, to append in the existing style)
    - CLAUDE.md "Changelog & Releases" (per-phase bullet convention: terse, user-facing-ish, reference the phase number)
  </read_first>
  <action>
    Add Phase 188 bullets under `## [Unreleased]` in CHANGELOG.md, grouped into the existing
    `### Changed` and `### Removed` subsections (create them under [Unreleased] if absent). Keep
    it terse and reference Phase 188. Suggested bullets:
    - Removed (### Removed): "Archived completed one-shot backfill scripts to scripts/archive/ and
      pruned the 3 now-unused eval_drain re-exports (Phase 188, SEED-115)."
    - Changed (### Changed): "Realigned the ix_games_bestmove_backfill_pending partial index with
      the current best-move claim query; reframed resweep_holed_games as the permanent Path-C
      hole re-arm tool and trimmed stale tier-2 / legacy-endpoint docstrings (Phase 188)."

    Then run the full backend pre-merge gate to prove D-02/D-08/D-09 (nothing behavioral changed):
    ruff format, ruff check --fix, ty check, and the FULL suite with -n auto. If ruff format/fix
    modifies files, that is expected — include those in the final commit. This is the phase gate;
    the executor should surface any failure rather than paper over it.
  </action>
  <verify>
    <automated>uv run ty check app/ tests/ && uv run pytest -n auto</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md `## [Unreleased]` has a Phase 188 bullet under both `### Changed` and `### Removed`.
    - `uv run ruff format app/ tests/ scripts/` leaves no diff on a second run (deterministic formatter applied).
    - `uv run ruff check app/ tests/ scripts/` exits 0.
    - `uv run ty check app/ tests/` exits 0 (zero errors, mandatory per CLAUDE.md).
    - `uv run pytest -n auto` exits 0 (full backend suite green — proves D-02/D-08/D-09: no behavioral change).
  </acceptance_criteria>
  <reversibility rating="reversible">CHANGELOG text + auto-formatting only; nothing functional.</reversibility>
  <done>CHANGELOG has the Phase 188 entry and the full backend pre-merge gate (ruff format/check, ty, full pytest) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy pipeline → prod DB | The new Alembic migration runs automatically at container startup (`deploy/entrypoint.sh` runs `alembic upgrade head` before uvicorn accepts traffic), altering a partial index on the ~1M-row `games` table. |
| repo import graph → live remote worker | `scripts/remote_eval_worker.py` (the active worker driver) imports from `eval_drain.py`; pruning the wrong re-export would break it. |

No client/browser, auth, input-validation, or SQL-injection surface is touched. `eval_remote.py`
edits are docstring-only; the `require_operator_token` HMAC dependency and every `@router.post`
handler body are untouched (D-08).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-188-01 | Denial of Service | Index-realignment migration on `games` (auto-runs on prod deploy) | low | accept | Non-concurrent `drop_index`/`create_index` per the 174-07 repo precedent is safe here: migrations run pre-traffic at container startup (never concurrent with live queries), and the matching row-set is near-empty (tier-4b backlog drained ~2026-07-23), so the rebuild is fast. Do NOT introduce `CONCURRENTLY` (Task 3). A symmetric `downgrade()` provides rollback. |
| T-188-02 | Tampering | eval_drain re-export prune breaking `scripts/remote_eval_worker.py` | medium | mitigate | Prune EXACTLY the 3 audited sole-use symbols (Task 2); the worker driver depends on `_walk_pv_boards`, which STAYS. Verified by `pytest --collect-only` (ImportError surfaces at collection) + `tests/test_remote_eval_worker.py` + an explicit archived-script import smoke check before commit. |
| T-188-03 | Tampering | Model/migration predicate text drift (silent phantom-diff on future autogenerate) | low | mitigate | Byte-identical `sa.text(...)` between model Index and migration `op.create_index`, enforced by the drift-lesson comment and an acceptance-grep equality check (Task 3). |
| T-188-SC | Tampering | Supply-chain (npm/pip/cargo installs) | n/a | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: not applicable) — no new dependency surface. |
</threat_model>

<verification>
## Phase-level checks

1. Full backend suite green: `uv run pytest -n auto` (Task 4) — the master proof that this
   deletion/documentation phase changed zero behavior (D-02, D-08, D-09).
2. `uv run ty check app/ tests/` exits 0 (mandatory, CLAUDE.md).
3. `uv run ruff check app/ tests/ scripts/` exits 0 after format.
4. Migration round-trips on the dev DB: `alembic upgrade head` → `downgrade -1` → `upgrade head` (Task 3).
5. Live index predicate (post-upgrade) matches `_claim_tier4_bestmove`'s WHERE verbatim, with no `lichess_evals_at` term.
6. Decision coverage: D-01 (Task 1), D-02/D-08/D-09 (fenced — proven by full-suite green + diff review), D-03 (Task 1), D-04 (Task 2), D-05 (Task 1), D-06 (Task 2), D-07 (Task 3).

## Fence review (D-02, D-08, D-09 — no-change decisions)

These are verified by absence-of-change, not by a task:
- `git diff` on `eval_remote.py` touches only docstring lines (no `@router.post` body, no auth dep).
- The 8 live worker endpoints, `_tier4b_minimal_drain_tick`, tier-3/4/4b logic, Path-C tolerance,
  the 5 timestamp columns, `OPENING_CACHE_BACKFILL_SQL`, `apply_game_filters`, and the 3 active
  scripts (`backfill_flaws.py`, `retag_flaws.py`, `reimport_games.py`) are all unmodified.
</verification>

<success_criteria>
- All nine CONTEXT.md decisions (D-01..D-09) covered: D-01/D-03/D-05 (Task 1), D-04/D-06 (Task 2), D-07 (Task 3), D-02/D-08/D-09 fenced and proven by green full-suite + diff review.
- `uv run pytest -n auto` and `uv run ty check app/ tests/` both exit 0.
- The migration applies + round-trips on the dev DB; model and migration predicate texts are byte-identical and match the claim query.
- 7 scripts archived; exactly 3 re-exports pruned; the 2 dependent scripts import cleanly.
- CHANGELOG.md `## [Unreleased]` has a Phase 188 entry.
</success_criteria>

<output>
Create `.planning/phases/188-import-eval-pipeline-cleanup-seed-115/188-01-SUMMARY.md` when done
</output>