---
phase: 190.1-train-reveal-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/repositories/train_repository.py
  - app/schemas/train.py
  - app/routers/train.py
  - tests/routers/test_train.py
  - frontend/src/types/train.ts
  - frontend/src/hooks/useTrainGradingEngine.ts
  - frontend/src/hooks/__tests__/useTrainGradingEngine.test.ts
  - frontend/src/components/train/TrainLineStepper.tsx
  - frontend/src/components/train/__tests__/TrainLineStepper.test.tsx
  - frontend/src/components/train/TrainReveal.tsx
  - frontend/src/components/train/__tests__/TrainReveal.test.tsx
  - frontend/src/components/train/TrainSolveScreen.tsx
  - frontend/src/components/train/__tests__/TrainSolveScreen.test.tsx
  - frontend/src/pages/__tests__/Train.solveLoop.test.tsx
autonomous: true
requirements: [SOLV-05]

must_haves:
  truths:
    - "D-01: the PLAYED IN GAME box's eval and line come from a live client-side Stockfish search on the position after the game move — never from a stored server eval."
    - "D-03: the reveal renders a steppable PLAYED IN GAME box whose header carries the role label and a white-POV eval, driving the shared board through the existing onFenChange contract."
    - "D-05: the reveal response carries the move played in the game as a UCI string, behind the same post-attempt 409 gate as every other answer-adjacent field."
    - "A reveal-time engine search that fails or times out leaves an honest error state in the box (never a hang, never a stored-eval substitute)."
    - "A search dispatched for puzzle N can never populate puzzle N+1's box (generation guard)."
  artifacts:
    - "app/schemas/train.py: PuzzleRevealResponse.played_in_game_move_uci"
    - "app/repositories/train_repository.py: RevealedPuzzle.played_in_game_move_uci + its parse_san derivation"
    - "frontend/src/types/train.ts: PuzzleRevealResponse.played_in_game_move_uci"
    - "frontend/src/hooks/useTrainGradingEngine.ts: TrainEngineLine, RawSearchResult.pv, startGameMoveSearch"
    - "frontend/src/components/train/TrainLineStepper.tsx: title + evalLabel props"
    - "frontend/src/components/train/TrainReveal.tsx: the train-line-box-game-move block"
  key_links:
    - "reveal GET (post-409-gate) -> played_in_game_move_uci -> startGameMoveSearch -> TrainEngineLine -> replayPvLine -> TrainLineStepper -> shared board"
    - "generationRef in useTrainGradingEngine is the single cancellation authority for BOTH the mount/grading searches and the new reveal-time search"
---

<objective>
Tracer slice: wire ONE new engine line — the PLAYED IN GAME box — end to end through
every layer this phase touches (Postgres-backed repository read -> Pydantic schema ->
FastAPI router -> TS mirror -> grading-engine hook -> reveal component -> shared board),
production quality, with a real runnable end-to-end check.

CONTEXT.md numbers its locked decisions D-1..D-5; this plan writes them as D-01..D-05.

Locked decisions covered: D-01 (client engine sources the line + eval), D-03 (steppable
line box with an eval header), D-05 (server reveal gains the game move as UCI).

Purpose: prove the whole vertical before widening it. If the client-search-per-reveal
architecture is wrong, it is wrong after one commit rather than after the MultiPV
rewrite and the panel rebuild are already committed on top of it.

Output: a reveal that shows a third steppable line — the move actually played in the
game plus the engine's continuation after it, with a live white-POV eval — while the
existing verdict rows, best-line stepper, tactic stepper and game card keep working
untouched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/190.1-train-reveal-redesign/190.1-CONTEXT.md
@.planning/phases/190.1-train-reveal-redesign/190.1-RESEARCH.md
@.planning/phases/190.1-train-reveal-redesign/190.1-PATTERNS.md
</context>

## Artifacts this phase produces

Symbols CREATED by Phase 190.1 (not pre-existing — exclude from drift verification):

**Backend**
- `RevealedPuzzle.played_in_game_move_uci: str | None` (Plan 01)
- `PuzzleRevealResponse.played_in_game_move_uci: str | None` (Plan 01)

**Frontend types**
- `PuzzleRevealResponse.played_in_game_move_uci: string | null` (Plan 01)

**`useTrainGradingEngine.ts`**
- `TrainEngineLine` interface — `{ moves: string[]; evalCp: number | null; evalMate: number | null }` (Plan 01)
- `RawSearchResult.pv: string[]` (Plan 01)
- `TrainGradingEngine.startGameMoveSearch(puzzleFen, gameMoveUci): Promise<TrainEngineLine>` (Plan 01)
- `TRAIN_GRADING_MULTIPV_WIDTH`, `TRAIN_GRADING_MOUNT_MOVETIME_MS` (Plan 02)
- `GradeResult.bestLine`, `GradeResult.playedLine`, `GradeResult.goodMoveUcis` (Plan 02)

**`TrainLineStepper.tsx`**
- `title?: string | null`, `evalLabel?: string | null` props (Plan 01)

**`TrainReveal.tsx`**
- props `gradingEngine`, `gradeResult`, `playedMoveUci`, `guess`, `onGameMoveUciChange` (Plans 01/03/04)
- testids `train-line-box-game-move`, `train-line-box-your-move`, `train-line-box-best-move`, `train-reveal-footer`, `btn-train-analyze`

**`frontend/src/lib/trainArrows.ts` (NEW FILE, Plan 04)**
- `buildTrainRevealArrows(...)` — the pure reveal-arrow builder
- `TRAIN_SHARP_GOOD_MOVE_ARROWS`, `TRAIN_SOFT_GOOD_MOVE_ARROWS`
- `TRAIN_GOOD_MOVE_ARROW_WIDTH`, `TRAIN_PLAYED_MOVE_ARROW_WIDTH`, `TRAIN_GAME_MOVE_ARROW_WIDTH`

**`TrainSolveScreen.tsx`**
- testid `train-session-score` (Plan 04)

**`theme.ts`**
- `TRAIN_PLAYED_MOVE_ARROW` (Plan 04)

**`useTrainSession.ts`**
- `UseTrainSessionResult.sessionSolvedCount: number` (Plan 04)

**`frontend/scripts/measure-train-movetime.mjs`**
- `--multipv=N` flag + the per-width stability table (Plan 02)

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "played in game" engine line — one path only</name>
  <files>app/repositories/train_repository.py, app/schemas/train.py, app/routers/train.py, tests/routers/test_train.py, frontend/src/types/train.ts, frontend/src/hooks/useTrainGradingEngine.ts, frontend/src/components/train/TrainLineStepper.tsx, frontend/src/components/train/TrainReveal.tsx, frontend/src/components/train/TrainSolveScreen.tsx</files>
  <read_first>
    - `app/repositories/train_repository.py` — read `reveal_for_puzzle` in full (the `RevealedPuzzle` dataclass, the `solve.solved_at is None -> "not_attempted"` gate, and the `best_move_san` derivation block that is the literal template for the new field)
    - `app/schemas/train.py` — the `PuzzleRevealResponse` model and its docstring describing the 409 gate
    - `app/routers/train.py` — the `reveal_puzzle` handler and its `PuzzleRevealResponse(...)` construction
    - `app/services/library_service.py` around line 135 — the house `board.parse_san(...).uci()` idiom with its try/except shape
    - `frontend/src/types/train.ts` — the hand-maintained TS mirror
    - `frontend/src/hooks/useTrainGradingEngine.ts` — read the WHOLE file; you must not disturb `dispatchNow` / `search` / `startGrading` / `abortGrading` / `gradeMove` semantics
    - `frontend/src/hooks/uciParser.ts` — `ParsedInfoLine.pv` is already parsed; nothing there changes
    - `frontend/src/components/train/TrainLineStepper.tsx` — the `motifLabel` heading block is the slot the new heading joins
    - `frontend/src/components/train/TrainReveal.tsx` — the reveal query's `enabled` gate and the existing best-line stepper block
    - `frontend/src/components/train/TrainSolveScreen.tsx` — where `TrainReveal` is rendered and which props it already receives
    - `frontend/src/components/analysis/EngineLines.tsx` — `replayPvLine` and `formatScore` (both already exported; import, never reimplement)
  </read_first>
  <action>
Wire ONE entry point (the move played in the game) through every layer, with no other
call sites and no batching.

Backend, inside `reveal_for_puzzle` and strictly AFTER the existing
`solve.solved_at is None` early return (D-05, T-189-17):
derive `played_in_game_move_uci` from the already-loaded `played_in_game_san` and the
already-reconstructed `fen` using `chess.Board(fen).parse_san(san).uci()`, wrapped in
`except (ValueError, chess.IllegalMoveError, AssertionError)` yielding `None` — the same
never-raise contract the adjacent `best_move_san` derivation uses. Add the field to the
`RevealedPuzzle` dataclass, to `PuzzleRevealResponse` as `played_in_game_move_uci: str | None`
(documented in the model docstring as sitting behind the same 409 gate), and to the router's
response construction. No migration, no new column, no change to the 409/404 branches.

Frontend type mirror: add `played_in_game_move_uci: string | null` to
`PuzzleRevealResponse` in `frontend/src/types/train.ts` next to `played_in_game_san`.

Grading-engine hook (`useTrainGradingEngine.ts`):
add `pv: string[]` to `RawSearchResult`; accumulate the current search's `multipv === 1`
`pv` alongside the existing `currentRawEvalRef` scalar (a sibling ref reset in the same
place `currentRawEvalRef` is reset inside `dispatchNow`), and include it in the object
`pending.resolve(...)` produces. Export a new `TrainEngineLine` interface with fields
`moves` (UCI strings), `evalCp`, `evalMate`. Add `startGameMoveSearch(puzzleFen: string,
gameMoveUci: string): Promise<TrainEngineLine>` to `TrainGradingEngine`: it captures
`generationRef.current` at call time, derives the post-game-move FEN with the existing
module-local `fenAfterUciMove`, rejects with an Error when that returns null, otherwise
calls the existing `search(afterFen, generation)` and resolves
`{ moves: [gameMoveUci, ...raw.pv], evalCp: raw.evalCp, evalMate: raw.evalMate }`. The
returned line's `moves` are rooted at `puzzleFen` — that invariant is shared by all three
reveal lines and must be stated in the interface's doc comment. Evals stay white-POV
(the existing `whitePovSign` normalisation in the bestmove handler already does this).
Do NOT introduce a second generation counter and do NOT send any MultiPV setoption in
this task.

`TrainLineStepper.tsx`: add two optional props, `title?: string | null` and
`evalLabel?: string | null`, rendered in the SAME heading slot the existing
`motifLabel != null && (...)` block uses, with testids `train-line-stepper-title` and
`train-line-stepper-eval`. Existing call sites that pass neither render exactly as today.

`TrainReveal.tsx`: accept a new required prop `gradingEngine: TrainGradingEngine`
(threaded from `TrainSolveScreen`, which already holds it). Add an effect that reads
`revealQuery.data?.played_in_game_move_uci`, and when it is non-null calls
`startGameMoveSearch(puzzle.fen, uci)`, storing the result in a local discriminated state
(`idle` | `loading` | `ready` | `error`) with a `cancelled` flag in the effect cleanup so a
late settle after unmount never sets state. Render the resulting box below the existing
best-line block: convert `TrainEngineLine.moves` to SAN with `replayPvLine(puzzle.fen,
moves)` (taking the leading run of non-null `san` values), render `TrainLineStepper` with
`title` set to the role label for the move played in the game, `evalLabel` set to
`formatScore(evalCp, evalMate)`, `startFen` = `puzzle.fen`, and the existing `onFenChange`.
Wrap the box in `data-testid="train-line-box-game-move"`. When the UCI is null but
`played_in_game_san` is present, render the box with the SAN as the title and no eval and
no stepper. When both are null, render no box at all. Leave every other part of
`TrainReveal` (verdict rows, outcome copy, comeback hint, best-line stepper, tactic
stepper, game card, Next) exactly as it is — later plans replace those.

`TrainSolveScreen.tsx`: pass `gradingEngine` down to `TrainReveal`. No other change.
  </action>
  <verify>
    <automated>uv run pytest tests/routers/test_train.py -q && ( cd frontend && npx vitest run src/components/train/__tests__/TrainReveal.test.tsx src/components/train/__tests__/TrainSolveScreen.test.tsx )</automated>
  </verify>
  <acceptance_criteria>
    - `tests/routers/test_train.py` contains a test asserting the reveal response for a solved puzzle whose stored `move_san` is a normal move returns `played_in_game_move_uci` as the 4-character UCI of that move, and a second test asserting a promotion `move_san` returns the 5-character UCI.
    - A test asserts the reveal endpoint still returns 409 before `solved_at` is set, and that the 409 body carries no `played_in_game_move_uci` key.
    - `frontend/src/types/train.ts`'s `PuzzleRevealResponse` declares `played_in_game_move_uci: string | null`.
    - `frontend/src/components/train/__tests__/TrainReveal.test.tsx` contains a test where `revealPuzzle` resolves a fixture with a non-null `played_in_game_move_uci`, a stub `gradingEngine.startGameMoveSearch` resolves a known `{ moves, evalCp }`, and the test asserts `train-line-box-game-move` exists and its `train-line-stepper-eval` text equals the `formatScore` output for that eval.
    - `frontend/src/components/train/__tests__/TrainSolveScreen.test.tsx` drives the existing fake Worker end to end (guess -> move -> grading -> solve response -> reveal) with a reveal fixture carrying `played_in_game_move_uci`, and asserts `train-line-box-game-move` appears with a non-empty eval label — proving the real hook, not a stub, produced it.
    - A `TrainLineStepper` test asserts that omitting both `title` and `evalLabel` renders neither `train-line-stepper-title` nor `train-line-stepper-eval`, and that supplying them renders both.
    - `( cd frontend && npx tsc -b )` exits 0.
  </acceptance_criteria>
  <done>Solving one puzzle in a real browser session shows a third steppable line labelled for the move played in the game, with a live eval from the client engine, and the backend reveal payload carries that move as UCI only after the attempt is recorded.</done>
  <reversibility rating="reversible">The new field is additive and nullable; the new hook method is a thin wrapper over the existing `search` primitive — both revert by deletion with no data migration.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Honest states and cancellation safety for the reveal-time search</name>
  <files>frontend/src/hooks/useTrainGradingEngine.ts, frontend/src/hooks/__tests__/useTrainGradingEngine.test.ts, frontend/src/components/train/TrainReveal.tsx, frontend/src/components/train/__tests__/TrainReveal.test.tsx, frontend/src/pages/__tests__/Train.solveLoop.test.tsx</files>
  <read_first>
    - `frontend/src/hooks/useTrainGradingEngine.ts` — the `gradeMove` timeout-race wrapper (the exact pattern to reuse), the `startGrading` generation guard, and `abortGrading`
    - `frontend/src/hooks/__tests__/useTrainGradingEngine.test.ts` — the `MockWorker` class, `driveInit`, and the fake-timer setup
    - `frontend/src/components/train/TrainReveal.tsx` — the state machine added in Task 1
    - `frontend/src/components/ui/load-error.tsx` — the single source of the mandated failure sentence
    - `.planning/phases/190.1-train-reveal-redesign/190.1-RESEARCH.md` — Pitfall 2 (a search dispatched from the child component must reuse the same generation)
  </read_first>
  <action>
Make the reveal-time search impossible to hang and impossible to leak across puzzles.

In `useTrainGradingEngine.ts`: race `startGameMoveSearch`'s underlying promise against
`TRAIN_GRADING_TIMEOUT_MS` using the same settle-once wrapper shape `gradeMove` already
uses (a `settled` boolean, a `setTimeout` rejecting with an Error, `clearTimeout` on the
inner settle) so the promise ALWAYS settles. Before resolving, compare the captured
generation against `generationRef.current` and reject with an Error naming the superseded
condition when they differ — a result computed for a previous puzzle must never resolve
to the caller. Reject immediately when `hasErrorRef.current` is already true.

In `TrainReveal.tsx`: render the three honest states of the box. While `loading`, render
the box header (role label plus the SAN from `played_in_game_san` when available) with an
inline spinner using the same `Loader2` + muted-text shape `TrainSolveScreen`'s grading
indicator uses, under `data-testid="train-game-line-loading"`. On `error`, render
`LoadError` with a resource name describing the engine line, under
`data-testid="train-game-line-error"`, and no eval and no stepper. Never fall back to any
value from the reveal payload other than the SAN label — no stored eval and no stored line
may appear in this box under any failure mode. The other reveal blocks must keep rendering
normally while this box is loading or errored.
  </action>
  <verify>
    <automated>( cd frontend && npx vitest run src/hooks/__tests__/useTrainGradingEngine.test.ts src/components/train/__tests__/TrainReveal.test.tsx src/pages/__tests__/Train.solveLoop.test.tsx )</automated>
  </verify>
  <acceptance_criteria>
    - A hook test drives `startGameMoveSearch`, advances fake timers past `TRAIN_GRADING_TIMEOUT_MS` without emitting `bestmove`, and asserts the returned promise rejects rather than staying pending.
    - A hook test starts a search, calls `abortGrading()` (or `startGrading` for a different FEN) while it is in flight, then emits `bestmove`, and asserts the first promise rejects and its payload is never observable to the caller — proving the generation guard.
    - A `TrainReveal` test with a `startGameMoveSearch` stub that never settles asserts `train-game-line-loading` is present and the verdict rows are still rendered.
    - A `TrainReveal` test with a rejecting `startGameMoveSearch` asserts `train-game-line-error` is present, that no `train-line-stepper-eval` exists inside `train-line-box-game-move`, and that the other reveal blocks still render.
    - `( cd frontend && npx vitest run src/components/train src/hooks/__tests__/useTrainGradingEngine.test.ts src/pages/__tests__/Train.solveLoop.test.tsx )` is fully green — no pre-existing Phase 190 train test is left failing.
  </acceptance_criteria>
  <done>The box always resolves to a line, a spinner, or a stated failure; a search started for one puzzle can never display in the next one.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> `GET /train/sessions/{id}/puzzles/{position}/reveal` | An authenticated user requesting answer-adjacent data for a puzzle they may not have attempted, or may not own |
| browser -> vendored Stockfish WASM Worker | Untrusted-in-principle engine output (arbitrary UCI text) parsed by `uciParser.ts` and rendered |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-190.1-01 | Information Disclosure | `reveal_for_puzzle` — new `played_in_game_move_uci` | high | mitigate | The derivation is placed strictly after the existing `solve.solved_at is None -> "not_attempted"` early return, so the new field sits behind the identical 409 gate as `best_move`/`pv` (T-189-17). Asserted by a test that the pre-attempt response is 409 and carries no such key. |
| T-190.1-02 | Information Disclosure | `TrainReveal` reveal-time search dispatch | high | mitigate | The search is driven exclusively off `revealQuery.data`, and that query is `enabled: verdict !== null && sessionId !== null` (T-190-16). No engine search on the game move can be dispatched before the solve POST has succeeded; asserted by the existing call-count gating tests plus the new loading-state test. |
| T-190.1-03 | Elevation of Privilege | `reveal_for_puzzle` IDOR surface | high | accept | Unchanged: the `DrillSolve` lookup is still scoped by `user_id` AND `session_id` (T-189-16). The new field is derived from rows already inside that scope; no new query and no new client-supplied identifier is introduced. |
| T-190.1-04 | Denial of Service | reveal-time engine search | medium | mitigate | The search is raced against `TRAIN_GRADING_TIMEOUT_MS` and the promise always settles; a wedged Worker yields a stated failure, never an unbounded spinner. |
| T-190.1-05 | Tampering | hand-mirrored TS type vs Pydantic schema | medium | mitigate | The field is ADDED on both sides in the same task/commit, and `npx tsc -b` is an acceptance gate — the removal direction (which `tsc` cannot catch) is deliberately deferred to Plan 03 where it is done as a single coordinated task. |
| T-190.1-SC | Tampering | npm/pip/cargo installs | high | accept | This phase installs no new packages on either stack (RESEARCH.md "Package Legitimacy Audit: not applicable"). No install task exists, so no legitimacy checkpoint applies. |

**Client-side grading is not a new leak.** The good-moves set and the best move are computed
client-side by the same Stockfish search that already grades the move (SOLV-03, the
pre-existing and accepted architecture: `TrainPuzzle` carries no answer key, and the client
IS the grader). Nothing in this phase moves answer-key data earlier in the timeline; the new
reveal-time search is dispatched strictly after the solve POST succeeds. Do not "fix" the
client-side grading architecture in this phase.
</threat_model>

<verification>
- `uv run pytest tests/routers/test_train.py -q` — green
- `( cd frontend && npx vitest run src/components/train src/hooks/__tests__/useTrainGradingEngine.test.ts src/pages/__tests__/Train.solveLoop.test.tsx )` — green
- `( cd frontend && npx tsc -b )` — exits 0
- `uv run ruff check app/ tests/ && uv run ty check app/ tests/` — clean
</verification>

<success_criteria>
A solved puzzle's reveal shows a steppable "played in game" line with a live white-POV eval
sourced from the client engine; the backend exposes the game move as UCI only post-attempt;
the search always settles and never crosses a puzzle boundary; every pre-existing Phase 190
train test still passes.
</success_criteria>

<output>
Create `.planning/phases/190.1-train-reveal-redesign/190.1-01-SUMMARY.md` when done
</output>
