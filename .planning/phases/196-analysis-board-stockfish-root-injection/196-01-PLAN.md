---
phase: 196-analysis-board-stockfish-root-injection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/engine/treeCommon.ts
  - frontend/src/lib/engine/mctsSearch.ts
  - frontend/src/lib/engine/fallbackExpectimax.ts
  - frontend/src/lib/engine/__tests__/treeCommon.test.ts
  - frontend/src/lib/engine/__tests__/mctsSearch.test.ts
  - frontend/src/lib/engine/__tests__/fallbackExpectimax.test.ts
autonomous: true
requirements: [INJECT-01, INJECT-02, INJECT-07]

must_haves:
  truths:
    - "An injected root UCI that Maia's 90%-mass cut dropped survives BOTH the mass cut and `applyRootCandidateHardCap` and appears in the search's `EngineSnapshot.rankedLines` — proven by a real `mctsSearch` run at `policyTemperature: 2` on a 20-legal-move root (INJECT-01)."
    - "With `injectedUcis` non-empty and `candidateMap.size > ROOT_CANDIDATE_HARD_CAP`, every injected UCI is present in `applyRootCandidateHardCap`'s output AND the output size never exceeds `ROOT_CANDIDATE_HARD_CAP`; with `injectedUcis` omitted or empty the output is entry-for-entry and order-for-order identical to the pre-phase function on the same input (INJECT-01)."
    - "When `injectedUcis.size >= ROOT_CANDIDATE_HARD_CAP` the organic slot count clamps at 0 (never negative) and the injected set itself is capped to `ROOT_CANDIDATE_HARD_CAP` by the same probability-descending / ascending-UCI comparator (INJECT-01 boundary)."
    - "An injected UCI's seeded prior is `effectivePolicy[uci] / keptTotal`, where `keptTotal` is the summed `effectivePolicy` mass of exactly the keys `truncateAndRenormalize` kept — the same scale organic priors carry at that temperature — and is `0` only when `effectivePolicy` has no entry for that UCI at all (INJECT-02)."
    - "A post-fix injected move with a real prior ranks ABOVE a known weaker organic candidate whose `rankScore` it beats, where the pre-fix prior-`0` seeding put it strictly last regardless of its `value` (INJECT-02, observable-ranking proof)."
    - "A UCI in `budget.extraRootMoves` that `truncateAndRenormalize` already kept is NOT re-seeded (the `!merged.has(uci)` guard stands), does NOT overwrite the organic prior, and is NOT counted against the cap's exemption budget (INJECT-06 adjacency probe)."
    - "`applyRootCandidateHardCap`'s tie-break stays probability-descending then ascending-UCI, and `keptTotal` is summed over `candidateMap`'s own insertion order, so two runs at the same inputs produce a bit-identical candidate set and order (ENGINE-07, INJECT-06 ordering probe)."
    - "`mctsSearch.ts` and `fallbackExpectimax.ts` carry the identical union-block expression and the identical `applyRootCandidateHardCap` call shape (only `leaf`/`node` naming differs), and both files' regression tests assert the same behaviour (ENGINE-06 parity)."
    - "Both `mctsSearch.ts` header blocks describe the inclusion guarantee as surviving BOTH the Maia mass cut and the root hard cap, and both cite INJECT-01 (INJECT-07)."
    - "`dispatchExpansion`'s statement sequence outside the union block and the `applyRootCandidateHardCap` argument list is unchanged: no edit to the `await providers.policy` / `await providers.grade` ordering, and no edit to `searchLoop`'s round barrier (Phase 198 DISPATCH-10 file-ownership constraint)."
    - statement: "Two RankedLines with exactly equal `rankScore` retain `buildRankedLines`' input order (ES2019 stable sort), so the verdict-row lookup's result is not order-dependent."
      verification: backstop
  artifacts:
    - "frontend/src/lib/engine/treeCommon.ts — `applyRootCandidateHardCap` with its new second parameter"
    - "frontend/src/lib/engine/mctsSearch.ts — fixed union block, updated cap call, corrected header blocks"
    - "frontend/src/lib/engine/fallbackExpectimax.ts — mirrored union block + cap call"
    - "frontend/src/lib/engine/__tests__/treeCommon.test.ts — first direct unit tests of `applyRootCandidateHardCap`"
    - "frontend/src/lib/engine/__tests__/mctsSearch.test.ts — INJECT-01 + INJECT-02 regression cases"
    - "frontend/src/lib/engine/__tests__/fallbackExpectimax.test.ts — mirrored INJECT-01 + INJECT-02 cases"
  key_links:
    - "`dispatchExpansion` union block → `applyRootCandidateHardCap(candidateMap, injectedUcis)` — the injected set must be the UCIs the union ACTUALLY added, not all of `budget.extraRootMoves`"
    - "`effectivePolicy` (still in scope after `truncateAndRenormalize`) → the injected prior's denominator `keptTotal`"
    - "`applyRootCandidateHardCap`'s shared-by-both-runners contract (`treeCommon.ts` Pitfall 3) → the same signature change lands in `mctsSearch.ts` and `fallbackExpectimax.ts` in the same commit"
  prohibitions:
    - statement: "The product must NOT distinguish injected from organic root candidates in ranking, selection, or display. If a mechanism needs to know a candidate was injected in order to decide what to show or how to rank it, that mechanism is wrong for this phase (D-01/D-02). The ONLY place injection provenance may be consulted is the hard-cap exemption itself, which exists solely to stop a silent drop."
      status: active
      verification: flagged-unverified
    - statement: "This phase must NOT change ranking, candidate selection, or playing strength for any position outside the analysis board's disagreement path. Bot play must stay byte-identical so Phase 199's combined calibration sweep keeps its attribution."
      status: active
      verification: flagged-unverified
---

<objective>
Fix the two prerequisite bugs that make `budget.extraRootMoves` a no-op exactly when it matters, in
BOTH `SearchRunner` implementations, and correct the header claim that let the regression hide.

Purpose: today an injected Stockfish move is seeded with prior `0` and is therefore the first entry
`applyRootCandidateHardCap` drops whenever the root exceeds `ROOT_CANDIDATE_HARD_CAP = 15` — so the
mechanism silently disappears on precisely the high-branching / high-temperature positions the
analysis board's Play-style slider produces. Nothing downstream in this phase can work until the
search core honours the injection.

Output: `applyRootCandidateHardCap` with an exemption parameter; a commensurate injected prior at both
union sites; the first direct unit tests of the cap; a T=2.0 high-branching end-to-end regression test
in both runners; an observable-ranking proof of the prior fix; and both `mctsSearch.ts` header blocks
describing what the code actually guarantees.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/196-analysis-board-stockfish-root-injection/196-CONTEXT.md
@.planning/phases/196-analysis-board-stockfish-root-injection/196-RESEARCH.md
@.planning/phases/196-analysis-board-stockfish-root-injection/196-PATTERNS.md
@.planning/phases/196-analysis-board-stockfish-root-injection/196-VALIDATION.md
</context>

<artifacts_this_phase_produces>
New or changed symbols introduced by THIS plan (the plan-review-convergence source-grounding pass
must treat these as newly created, not as drift):

| Symbol / path | Kind | Where |
|---|---|---|
| `applyRootCandidateHardCap(candidateMap, injectedUcis?)` | changed signature — new optional 2nd param `injectedUcis?: ReadonlySet<string>` | `frontend/src/lib/engine/treeCommon.ts` |
| `injectedUcis` (local) | new local `Set<string>` built inside the union block | `mctsSearch.ts` `dispatchExpansion`, `fallbackExpectimax.ts` `expandNode` |
| `keptTotal` (local) | new local number — the summed `effectivePolicy` mass of `candidateMap`'s kept keys | both union blocks |

Symbols this plan CONSUMES but does not create (pre-existing): `ROOT_CANDIDATE_HARD_CAP`,
`truncateAndRenormalize`, `applyPolicyTemperature`, `POLICY_MASS_THRESHOLD`, `rankScore`,
`buildRankedLines`, `SearchBudget.extraRootMoves`, `uniformPolicyFromLegalMoves`, `makeFixedPolicy`,
`makeFixedGrade`, `freshSignal`, `NEUTRAL_BUDGET_ELO`, `evalToExpectedScore`.
</artifacts_this_phase_produces>

<source_audit>
## Multi-Source Coverage Audit (whole phase, all four source types)

### GOAL — ROADMAP § Phase 196 `**Goal:**`
| Goal clause | Plan | Status |
|---|---|---|
| Give the FlawChess engine an opinion on Stockfish's preferred move when it falls outside Maia's 90%-mass set | 196-02 (wiring) + 196-01 (core) | COVERED |
| Inject the already-running free MultiPV=2 run's moves into the root | 196-02 | COVERED |
| Re-run once on settled disagreement | 196-02 | COVERED |
| Fix the hard cap silently dropping injected moves | 196-01 | COVERED |
| Fix injected moves seeded with a zero prior | 196-01 | COVERED |

### REQ — REQUIREMENTS.md phase_req_ids
| ID | Plan | Status |
|---|---|---|
| INJECT-01 | 196-01 | COVERED |
| INJECT-02 | 196-01 | COVERED |
| INJECT-03 | 196-02 | COVERED |
| INJECT-04 | 196-02 | COVERED |
| INJECT-05 | 196-03 | COVERED |
| INJECT-06 | 196-02 | COVERED |
| INJECT-07 | 196-01 | COVERED |

### RESEARCH — 196-RESEARCH.md features / constraints / pitfalls
| Item | Plan | Status |
|---|---|---|
| CORRECTED: `matchedFlawChessLineForSf` receives the top-2-sliced `reconciledRankedLines`; INJECT-06 needs an additive `Analysis.tsx` memo | 196-02 Task 2 | COVERED (supersedes CONTEXT.md's "zero component change" discretion note) |
| CORRECTED: INJECT-01's regression test lives in `mctsSearch.test.ts` / `fallbackExpectimax.test.ts`, not `treeCommon.test.ts` | 196-01 Tasks 1–2 | COVERED (both, plus new direct cap unit tests in `treeCommon.test.ts`) |
| Pitfall 1 — unstable `extraRootMoves` identity causes a search-restart storm | 196-02 Task 2 | COVERED (`NO_EXTRA_ROOT_MOVES` sentinel + reference-stability test) |
| Pitfall 2 — assuming the verdict lookup "just works" post-injection | 196-02 Tasks 2–3 | COVERED |
| Pitfall 3 — file-ownership collision with Phase 198 | 196-01 Task 1 | COVERED (diff bound + explicit boundary in `<action>`) |
| Pitfall 4 — INJECT-05's superseded cost framing | 196-03 Task 3 | COVERED (report must carry both numbers + the reframing note) |
| `WorkerPool.cacheStats()` counter recommendation | 196-03 Task 1 | COVERED (implemented via a shipped-code `createGradeCache` extraction — see 196-03's `<planner_correction>`) |
| Open Question 1 — reset `cacheStats()` between passes | 196-03 Tasks 2–3 | COVERED (resolved: reset after the baseline pass) |
| Open Question 2 — 5–10 curated disagreement positions | 196-03 Task 2 | COVERED (`MIN_DISAGREEMENT_POSITIONS`, default target 8) |
| Assumption A2 — the exact dropped-tail UCI (`h2h4`) is unverified | 196-01 Task 1 | COVERED (task verifies the sorted key order before asserting) |

### CONTEXT — 196-CONTEXT.md D-01..D-05 + discretion
| Decision | Plan | Status |
|---|---|---|
| D-01 no visit gate on the displayed practical score | 196-02 Task 3 | COVERED |
| D-02 no special-casing in ranking or selection | 196-01 (prohibition) + 196-02 | COVERED |
| D-03 visit-budget dilution is the feature; no ceiling, no dilution gate | all plans (nothing added) | COVERED by omission — explicitly recorded, not forgotten |
| D-04 no verdict-copy changes | 196-02 Task 3 | COVERED (asserted unchanged) |
| D-05 evidence is a committed scripted harness run under `reports/` | 196-03 | COVERED |
| Discretion: inject `pvLines[0..1].moves[0]`, trigger on "at least one is not already a root candidate" | 196-02 Task 2 | COVERED |
| Discretion: memoized, deduped, sorted, stable-identity `extraRootMoves`; fires exactly once; resets on FEN change | 196-02 Task 2 | COVERED |
| Discretion: injection requires `engineEnabled` | 196-02 Task 2 | COVERED |
| Discretion: INJECT-02 renormalize locally, do NOT switch to `rawMaiaProb` | 196-01 Task 1 | COVERED |
| Discretion: INJECT-01 exempt-then-union, mirrored into `fallbackExpectimax.ts` | 196-01 Tasks 1–2 | COVERED |
| Discretion: INJECT-07 names both mechanisms | 196-01 Task 3 | COVERED |
| Discretion: INJECT-05 restate the cost framing; report BOTH numbers; a low hit rate is the honest finding | 196-03 Task 3 | COVERED |
| Discretion: "INJECT-06 ships with zero component change" | 196-02 Task 2 | SUPERSEDED by RESEARCH.md's verified correction — `FlawChessAgreementVerdict.tsx` needs zero internal change (that half holds); `Analysis.tsx`'s prop wiring needs one additive memo + one prop line. This is within the discretion CONTEXT.md granted the planner, not a scope expansion. |

**Excluded (not gaps):** every item under CONTEXT.md `<deferred>` (SEED-114 bot-preset injection,
`rankScore` → `rawMaiaProb`, the ~1.11× `P_REF_ANCHORS` correction, a visit-share ceiling, a visit
floor on the displayed score, reconciling the two objective evals) and every item under
`<domain>` "Out of scope" (Phase 197/198/199 work, retuning `FLAWCHESS_ENGINE_MAX_NODES`,
`ROOT_CANDIDATE_HARD_CAP`, `ROOT_PRIOR_FLOOR`, `P_REF_ANCHORS`, revising
`docs/flawchess-engine-explained-2026-07-06.md` §2).

**No MISSING items.** All GOAL / REQ / RESEARCH / CONTEXT items are assigned to a plan.
</source_audit>

<flagged_assumptions>
The spec-less edge probe returned five `unclassified` rows (INJECT-01, INJECT-02, INJECT-03,
INJECT-04, INJECT-07). The probe is requirement-text-driven and category-blind to this domain, so
those rows carry no category to resolve against. Per the fallback protocol they stay `unresolved` and
are surfaced here rather than silently backstopped. For each, the planner authored a concrete truth
against what the requirement actually asserts — the flag records the interpretation applied, so a
reviewer can challenge the interpretation rather than discover a gap:

1. **INJECT-01 (unclassified)** — interpreted as the cap-exemption's own boundary contract. Authored
   as three truths in this plan (presence, size bound, no-injection byte-identity, and the
   `injectedUcis.size >= CAP` clamp). Residual risk: the probe may have intended something else
   entirely; no further category signal exists to check against.
2. **INJECT-02 (unclassified)** — interpreted as the prior's scale contract and its degenerate
   `effectivePolicy[uci]` absence case. Authored as two truths in this plan.
3. **INJECT-03 (unclassified)** — interpreted as the option's backward compatibility for existing
   callers. Authored as a truth in 196-02.
4. **INJECT-04 (unclassified)** — interpreted as the exactly-once / reset contract. Authored as four
   truths in 196-02.
5. **INJECT-07 (unclassified)** — interpreted as "both mechanisms are named". Authored as a truth in
   this plan with a mechanical `grep` criterion plus a `<human-check>` for prose accuracy, because a
   doc comment carries no runtime assertion.

Probe-item accounting: 12 items surfaced → 11 authored into `must_haves.truths` as `covered`, 1
authored as a `backstop` marker (equal-`rankScore` sort stability, this plan), 5 of the 12 additionally
flagged above as `unresolved`-by-category. No silent drops.
</flagged_assumptions>

<assumption_delta_decision>
**Detector signal:** one `pluralization` hit, term `"second"`, snippet `"… no-injection behaviour for
existing callers; the second does not. Prefer the first unless a reason emerge…"`.

**Primary noun (unchanged):** the root candidate set is keyed by UCI move string within a single
position's search. `Map<string, number>` remains the single source of truth for "what candidates does
this root have", and a UCI string remains the sole identity for a candidate regardless of how it
entered the map.

**Decision: `no-change`.** The detector's snippet is prose describing the second of two candidate
*implementation options* for the hard-cap fix (exempt-then-union vs cap-before-union) — not the
introduction of a second platform, auth method, tenant, region, or source of truth. No core identity
assumption loses its monopoly in this phase.

**Accepted debt:** none — nothing was added alongside an existing primary.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end — an out-of-mass injected move survives the mass cut AND the hard cap</name>

  <files>frontend/src/lib/engine/treeCommon.ts, frontend/src/lib/engine/mctsSearch.ts, frontend/src/lib/engine/fallbackExpectimax.ts, frontend/src/lib/engine/__tests__/mctsSearch.test.ts</files>

  <read_first>
    - `.planning/phases/196-analysis-board-stockfish-root-injection/196-PATTERNS.md` § "`frontend/src/lib/engine/treeCommon.ts` — `applyRootCandidateHardCap`", § "`frontend/src/lib/engine/mctsSearch.ts` — module header (INJECT-07) + `dispatchExpansion` union block", § "`frontend/src/lib/engine/fallbackExpectimax.ts` — the mirrored union block", and § "Test Templates → INJECT-01 regression test" — these carry the verbatim before/after excerpts and the fixture recipe.
    - `.planning/phases/196-analysis-board-stockfish-root-injection/196-RESEARCH.md` § "CONFIRMED: `applyRootCandidateHardCap` — the blocker", § "CONFIRMED: the union-block prior-zero bug", § "CONFIRMED: variables in scope at the union site (INJECT-02's exact fix)", § "CORRECTED: the INJECT-01 regression test's actual location", and § "Pitfall 3: File-ownership collision with Phase 198 (SEED-127)".
    - `frontend/src/lib/engine/treeCommon.ts` — the current `applyRootCandidateHardCap` (~:98-112) and its doc comment; also `SearchTreeNode.rawMaiaProb`'s doc comment, which already anticipates a Stockfish-injected candidate `policy()` never scored.
    - `frontend/src/lib/engine/mctsSearch.ts` — `dispatchExpansion` (~:408-440) in full, so the union block's surroundings are seen and left alone.
    - `frontend/src/lib/engine/fallbackExpectimax.ts` — `expandNode` (~:179-202).
    - `frontend/src/lib/engine/select.ts` — `truncateAndRenormalize` (~:44-60) and `POLICY_MASS_THRESHOLD = 0.9`, to confirm which keys survive the mass cut and in what tie-break order.
    - `frontend/src/lib/engine/policyTemperature.ts` — `ROOT_CANDIDATE_HARD_CAP = 15` and its doc comment.
    - `frontend/src/lib/engine/__tests__/mctsSearch.test.ts` — the fixture helpers `uniformPolicyFromLegalMoves` (~:117-125), `makeFixedPolicy`, `makeFixedGrade`, `freshSignal`, `NEUTRAL_BUDGET_ELO`, the existing `describe('mctsSearch — D-04 extraRootMoves', ...)` (~:358-387), and the existing extreme-flatness cap test (~:563-580).
  </read_first>

  <behavior>
    - `applyRootCandidateHardCap(map, undefined)` and `applyRootCandidateHardCap(map, new Set())` return exactly what today's one-argument call returns for the same `map` — same entries, same iteration order (this is the byte-identity guarantee for every existing no-injection caller).
    - `applyRootCandidateHardCap(map, new Set(['h2h4']))` with `map.size = 19` (18 organic + `h2h4`) returns a map of size `<= 15` that CONTAINS `h2h4`; the 15 slots are `h2h4` plus the 14 highest-probability organic entries (probability descending, ascending-UCI tie-break).
    - `applyRootCandidateHardCap(map, injected)` where `injected.size >= 15` returns exactly the top 15 of `injected` by the same comparator and zero organic entries — never a negative slice length, never a map larger than the cap.
    - A UCI present in BOTH `map` and `injected` consumes an organic slot, not an exemption slot — because the caller only ever passes the UCIs the union actually ADDED.
    - A real `mctsSearch` run at `policyTemperature: 2` over a uniform 20-legal-move root with `extraRootMoves: ['<a mass-cut-dropped UCI>']` produces `snapshot.rankedLines` that (a) has `length <= ROOT_CANDIDATE_HARD_CAP` and (b) contains a line whose `rootMove` is that UCI. Pre-fix, (b) fails.
  </behavior>

  <action>
    Land the two bug fixes and one end-to-end regression test. Concrete target state:

    **1. `treeCommon.ts` — `applyRootCandidateHardCap` gains a second parameter.** New signature:
    `applyRootCandidateHardCap(candidateMap: Map<string, number>, injectedUcis?: ReadonlySet<string>): Map<string, number>`.
    Semantics, in this exact order:
    (a) keep the existing `if (candidateMap.size <= ROOT_CANDIDATE_HARD_CAP) return candidateMap;`
    early return untouched — it is what makes the no-injection path byte-identical;
    (b) when `injectedUcis` is undefined or empty, fall through to today's body verbatim
    (sort by `b[1] - a[1] || (a[0] < b[0] ? -1 : 1)`, slice `ROOT_CANDIDATE_HARD_CAP`, `new Map`);
    (c) otherwise partition `candidateMap.entries()` into `injected` (key in `injectedUcis`) and
    `organic` (key not in `injectedUcis`); compute `organicSlots = Math.max(0, ROOT_CANDIDATE_HARD_CAP - injected.length)`;
    sort BOTH partitions with the SAME comparator used in (b); take `injected.slice(0, ROOT_CANDIDATE_HARD_CAP)`
    and `organic.slice(0, organicSlots)`; concatenate, re-sort the concatenation with that same
    comparator, and return `new Map(...)` of it. Re-sorting the concatenation is what keeps the
    returned iteration order canonical (ENGINE-07) rather than "injected first".
    Extract the comparator into a single module-local named function used by every sort call in this
    function, so the three sorts cannot drift apart.
    Update the function's doc comment: it currently asserts the cap is applied after the
    `extraRootMoves` union; extend it to state that injected UCIs are exempt (organic capped to
    `ROOT_CANDIDATE_HARD_CAP - injectedCount`), that the exemption set is the UCIs the union ADDED
    (never all of `budget.extraRootMoves`), and cite INJECT-01. Per CLAUDE.md's bug-fix-comment rule,
    say what broke: a prior-`0` injected entry sorted dead last and was the first entry the cap
    dropped.

    **2. `mctsSearch.ts` `dispatchExpansion` — the union block.** Replace `merged.set(uci, 0)` with a
    commensurate prior and record which UCIs were actually added. Concretely, inside the existing
    `if (leaf.isRoot && budget.extraRootMoves && budget.extraRootMoves.length > 0) { ... }` block:
    compute `keptTotal` by summing `effectivePolicy[k] ?? 0` over `candidateMap.keys()`; build a
    `const injectedUcis = new Set<string>()`; for each `uci` of `budget.extraRootMoves`, when
    `!merged.has(uci)`, set `merged.set(uci, keptTotal > 0 ? (effectivePolicy[uci] ?? 0) / keptTotal : 0)`
    and `injectedUcis.add(uci)`. Declare `injectedUcis` in a scope the cap call below can read (a
    `const injectedUcis = new Set<string>()` immediately BEFORE the `if (leaf.isRoot && budget.extraRootMoves ...)`
    block is the smallest such change). Then change the cap call from
    `applyRootCandidateHardCap(candidateMap)` to `applyRootCandidateHardCap(candidateMap, injectedUcis)`.
    Add a bug-fix comment at the seeding line citing INJECT-02 and `196-RESEARCH.md`: prior `0` made
    `rankScore = min(1, 0/pRef) * value = 0`, so an injected move was always ranked last and always
    the first cap casualty, regardless of quality.
    `effectivePolicy[uci] ?? 0` narrowing is required (`noUncheckedIndexedAccess`); do not use `!`.
    Note that `0` is a legitimate outcome only when Maia's policy has no entry for that UCI at all —
    the shipped `maskAndSoftmaxUci` path gives every legal move a positive probability, so the `?? 0`
    is defensive, not the expected case.

    **BOUNDARY — this is a hard constraint, not a preference.** Phase 198 (SEED-127) rewrites
    `dispatchExpansion`'s round loop and its policy/grade await sequencing, and its DISPATCH-10
    requirement is that THIS phase's union and exemption survive that rewrite unchanged in behaviour.
    Your diff inside `dispatchExpansion` is limited to: the union block's own body, the one-line
    `injectedUcis` declaration, and the cap call's argument list. Do NOT restructure the
    policy/grade await ordering, do NOT touch `searchLoop`'s round barrier or its buffer-then-apply
    sequence, and do NOT refactor the surrounding function for size or style even if it tempts you.

    **3. `fallbackExpectimax.ts` `expandNode` — the identical mirror.** Apply the SAME expression
    verbatim, with `node.` in place of `leaf.`. `applyRootCandidateHardCap` is deliberately shared by
    both `SearchRunner` implementations so the cap can never diverge (`treeCommon.ts` Pitfall 3), and
    ENGINE-06's independent-implementation guarantee requires parity even though this file has no
    production caller. Both files MUST change in this one commit.

    **4. `mctsSearch.test.ts` — the end-to-end regression `it()`.** Add it directly alongside the
    existing extreme-flatness cap test in `describe('mctsSearch — Phase 159 policy temperature', ...)`,
    reusing that test's exact `START_FEN` (`rnbqkbnr/pppppppp/8/8/8/8/PPPPPPPP/RNBQKBNR w KQkq - 0 1`),
    `uniformPolicyFromLegalMoves(START_FEN)`, `policyTemperature: 2`, `maxNodes: 1`, `maxPlies: 1`,
    `concurrency: 1`, `NEUTRAL_BUDGET_ELO`, `makeFixedPolicy`, `makeFixedGrade({})`, `freshSignal()`.
    Give the new test a name that also contains the substring `extreme-flatness` so the existing
    `-t "extreme-flatness"` filter selects both.
    **Do not hardcode the injected UCI from the plan text.** `196-RESEARCH.md` Assumption A2 flags
    that `h2h4`/`h2h3` are unverified. Derive it in the test itself: compute
    `truncateAndRenormalize(uniformPolicyFromLegalMoves(START_FEN))` and pick a UCI from
    `uniformPolicyFromLegalMoves`'s keys that the returned Map does NOT contain — assert that such a
    UCI exists (so the fixture's own premise is guarded), then use it as `extraRootMoves: [uci]`.
    Assert BOTH: `snapshot.rankedLines.length` is `<= ROOT_CANDIDATE_HARD_CAP`, AND
    `snapshot.rankedLines.some((l) => l.rootMove === uci)` is `true`. Comment the test with the
    pre-fix failure mode: 18 organic + 1 injected = 19 candidates, the injected one at prior `0`
    sorting dead last and guaranteed to be among the 4 the 15-cap cut.

    Before committing, confirm the test genuinely regresses: temporarily revert the cap's second
    argument at the `mctsSearch.ts` call site, observe the new assertion fail, restore it. Do not
    commit the reverted state.
  </action>

  <verify>
    <automated>cd frontend && npx vitest run -t "extreme-flatness" src/lib/engine/__tests__/mctsSearch.test.ts && npx vitest run src/lib/engine/__tests__/mctsSearch.test.ts src/lib/engine/__tests__/fallbackExpectimax.test.ts src/lib/engine/__tests__/treeCommon.test.ts src/lib/engine/__tests__/select.test.ts src/lib/engine/__tests__/policyTemperature.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `cd frontend && npx vitest run -t "extreme-flatness" src/lib/engine/__tests__/mctsSearch.test.ts` reports 2 passing tests (the pre-existing cap bound + the new injection regression).
    - `cd frontend && npx vitest run src/lib/engine/__tests__/mctsSearch.test.ts src/lib/engine/__tests__/fallbackExpectimax.test.ts src/lib/engine/__tests__/treeCommon.test.ts src/lib/engine/__tests__/select.test.ts src/lib/engine/__tests__/policyTemperature.test.ts` exits 0 with zero failures — no pre-existing engine test regressed.
    - `cd frontend && npx tsc -b` exits 0 (lint and vitest do not type-check; the changed `applyRootCandidateHardCap` signature has two call sites).
    - `cd frontend && npm run lint` exits 0.
    - `git show --numstat HEAD -- frontend/src/lib/engine/mctsSearch.ts` reports added + deleted `<= 24` lines for this commit (Pitfall 3's own warning sign is a diff larger than ~10-15 lines inside `dispatchExpansion`).
    - `grep -c 'applyRootCandidateHardCap(candidateMap, injectedUcis)' frontend/src/lib/engine/mctsSearch.ts frontend/src/lib/engine/fallbackExpectimax.ts` reports `1` for each file — the exemption argument is passed at both call sites, not just one.
    - `grep -c 'keptTotal' frontend/src/lib/engine/mctsSearch.ts frontend/src/lib/engine/fallbackExpectimax.ts` reports `>= 2` for each file (the accumulator and its use in the seeding expression) — the prior is computed, not a literal.
    - Source assertion: in both files the union block's `merged.set(...)` call for a newly-added UCI takes the computed renormalized prior as its second argument; no executable line in either union block passes a numeric literal as that prior.
    - `grep -c 'INJECT-02' frontend/src/lib/engine/mctsSearch.ts frontend/src/lib/engine/fallbackExpectimax.ts` reports `>= 1` for each file — the bug-fix comment is present at both seeding sites (CLAUDE.md bug-fix-comment rule).
    - Source assertion: `git show HEAD -- frontend/src/lib/engine/mctsSearch.ts` contains no changed line inside `searchLoop`, and no reordering of the `await providers.policy` / `await providers.grade` statements in `dispatchExpansion`.
    - Source assertion: the union-block expression in `fallbackExpectimax.ts` differs from `mctsSearch.ts`'s only by the `node`/`leaf` identifier.
    - Source assertion: `applyRootCandidateHardCap`'s body contains exactly one comparator definition, referenced by every `.sort(...)` call in the function.
  </acceptance_criteria>

  <reversibility rating="reversible">The exemption parameter is optional and additive; reverting means deleting the second argument at two call sites and the partition branch. The union-block prior is one expression at two sites (CONTEXT.md rates this `reversible`).</reversibility>

  <done>
    `applyRootCandidateHardCap` accepts an exemption set, both runners seed injected priors on the
    kept-mass scale and pass the union's own additions as the exemption set, and a real `mctsSearch`
    run at T=2.0 on a 20-legal-move root proves an out-of-mass injected UCI reaches `rankedLines`
    while the total stays within the cap. Committed with both runners changed together.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parity mirror, direct cap unit tests, and the observable prior-ranking proof</name>

  <files>frontend/src/lib/engine/__tests__/fallbackExpectimax.test.ts, frontend/src/lib/engine/__tests__/treeCommon.test.ts, frontend/src/lib/engine/__tests__/mctsSearch.test.ts</files>

  <read_first>
    - `.planning/phases/196-analysis-board-stockfish-root-injection/196-PATTERNS.md` § "Test Templates" — both the INJECT-01 template and the INJECT-02 template (`describe('mctsSearch — D-04 extraRootMoves', ...)` and the Phase 159 D-01 findability-ranking test's fixture shape).
    - `.planning/phases/196-analysis-board-stockfish-root-injection/196-RESEARCH.md` § "CORRECTED: the INJECT-01 regression test's actual location" — specifically the paragraph beginning "A second, separate test proves INJECT-02", which explains why presence alone is not a prior proof.
    - `frontend/src/lib/engine/__tests__/fallbackExpectimax.test.ts` — the mirrored extreme-flatness test (~:381-397) and its own `extraRootMoves` describe block, plus its local fixture helpers (they are file-local copies, not shared imports — check before assuming a helper exists).
    - `frontend/src/lib/engine/__tests__/treeCommon.test.ts` — its existing import list and describe structure (`applyRootCandidateHardCap` currently has zero references here; this task adds the first).
    - `frontend/src/lib/engine/__tests__/mctsSearch.test.ts` — the Phase 159 D-01 findability-ranking test (~:391-436) and its `FINDABILITY_FEN` / `FINDABILITY_POLICY` / `FINDABILITY_GRADES` / low-ELO fixture shape.
    - `frontend/src/lib/engine/findability.ts` — `rankScore` (note its saturating factor can only DEMOTE) and `pRefForElo` / `P_REF_ANCHORS`, so the ranking fixture's ELO is chosen to make the assertion deterministic rather than lucky.
  </read_first>

  <behavior>
    - `treeCommon.test.ts`: `applyRootCandidateHardCap(map)` and `applyRootCandidateHardCap(map, new Set())` produce identical `[...result.entries()]` arrays for a `map` of size 20 — including order.
    - `treeCommon.test.ts`: with `map` of size 19 and `injectedUcis = new Set([lowestProbUci])`, the result has size 15 and contains `lowestProbUci`; without the set, the result has size 15 and does NOT contain it.
    - `treeCommon.test.ts`: with `injectedUcis` of size 17 over a 25-entry map, the result has size exactly 15, contains only injected keys, and its keys are the 15 highest-probability injected keys with ascending-UCI tie-break.
    - `treeCommon.test.ts`: a UCI present in both `map` and `injectedUcis` does not inflate the result past 15 and does not change which organic entries survive relative to passing that UCI only in `map`.
    - `treeCommon.test.ts`: two calls with the same inputs produce the same key order (deterministic tie-break, ENGINE-07).
    - `fallbackExpectimax.test.ts`: the same injection regression as `mctsSearch.test.ts`'s new test, same assertions, over `fallbackExpectimax`.
    - `mctsSearch.test.ts` and `fallbackExpectimax.test.ts`: with a fixture where the injected move's post-fix renormalized prior beats a known weaker organic candidate's `rankScore`, `rankedLines.findIndex(l => l.rootMove === injectedUci)` is strictly less than `rankedLines.findIndex(l => l.rootMove === weakerOrganicUci)`. Pre-fix (prior `0`) the injected index is the last index.
  </behavior>

  <action>
    Expand the tracer's single proof into the full regression matrix. Three groups of cases:

    **Group A — `fallbackExpectimax.test.ts` parity.** Mirror Task 1's new injection regression `it()`
    into this file, alongside its own extreme-flatness test, with the same derived-dropped-UCI
    approach (do not hardcode the UCI) and the same two assertions. Name it so
    `-t "extreme-flatness"` selects it. ENGINE-06 requires the two implementations to agree
    observably, not merely to share a helper.

    **Group B — `treeCommon.test.ts` direct unit tests of `applyRootCandidateHardCap`.** This function
    has zero direct tests today (`196-RESEARCH.md` grep-confirmed), which is a large part of why the
    regression survived. Add a new `describe('applyRootCandidateHardCap — INJECT-01 exemption', ...)`
    covering, as separate `it()`s, exactly the five behaviours in `<behavior>` above. Build the
    candidate maps as plain `new Map<string, number>([...])` literals with distinct descending
    probabilities plus at least one exact tie pair (two keys at the same probability) so the
    ascending-UCI tie-break is actually exercised. Import `ROOT_CANDIDATE_HARD_CAP` rather than
    writing `15` — no magic numbers, and the tests must not silently pass if the constant is retuned
    later.

    **Group C — the INJECT-02 observable-ranking proof, in both runner test files.** `RankedLine`
    does not expose `.prior`, so assert an ordering consequence instead of the value. Build a fixture
    in the shape of the existing Phase 159 D-01 findability-ranking test: a policy where one organic
    candidate has a genuinely small kept-mass share and a mediocre grade (the "known weaker organic"),
    an injected UCI that `truncateAndRenormalize` drops but whose raw temperature-reshaped probability
    is non-trivial, and grades that give the injected move a clearly better `value`. Choose the ELO so
    `pRefForElo` makes the arithmetic unambiguous, and add a comment stating the intended
    `rankScore` comparison in prose so a future reader can re-derive why the ordering must hold.
    Assert the strict `findIndex` inequality from `<behavior>`. Add the same test to both
    `mctsSearch.test.ts` and `fallbackExpectimax.test.ts`, inside each file's existing
    `extraRootMoves` describe block so `-t "extraRootMoves"` selects them.

    For each of Groups A/B/C, confirm the test regresses before committing: revert the relevant fix
    (the cap's second argument for A/B, the prior expression for C), observe the failure, restore.
    A test that passes both pre- and post-fix is not a regression test and must be rewritten.

    Do not add any assertion that depends on knowing a candidate was injected beyond the exemption
    argument itself — D-02 forbids provenance-aware ranking, and a test that encodes it would lock in
    the wrong behaviour.
  </action>

  <verify>
    <automated>cd frontend && npx vitest run -t "extreme-flatness" src/lib/engine/__tests__/mctsSearch.test.ts src/lib/engine/__tests__/fallbackExpectimax.test.ts && npx vitest run -t "extraRootMoves" src/lib/engine/__tests__/mctsSearch.test.ts src/lib/engine/__tests__/fallbackExpectimax.test.ts && npx vitest run src/lib/engine/__tests__/treeCommon.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `cd frontend && npx vitest run -t "extreme-flatness" src/lib/engine/__tests__/mctsSearch.test.ts src/lib/engine/__tests__/fallbackExpectimax.test.ts` reports 4 passing tests (2 pre-existing bounds + 2 injection regressions).
    - `cd frontend && npx vitest run -t "extraRootMoves" src/lib/engine/__tests__/mctsSearch.test.ts src/lib/engine/__tests__/fallbackExpectimax.test.ts` exits 0 and includes the 2 new prior-ranking tests (one per file).
    - `cd frontend && npx vitest run src/lib/engine/__tests__/treeCommon.test.ts` exits 0 and reports at least 5 tests inside the new `applyRootCandidateHardCap — INJECT-01 exemption` describe block.
    - `grep -c 'ROOT_CANDIDATE_HARD_CAP' frontend/src/lib/engine/__tests__/treeCommon.test.ts` reports `>= 2` — the cap is imported and referenced, not written as a literal.
    - `grep -c '\b15\b' frontend/src/lib/engine/__tests__/treeCommon.test.ts` reports `0` inside the new describe block (verify by reading the block; the cap value is never hardcoded).
    - Behaviour assertion, recorded in the task's commit message: for each of Groups A, B and C, reverting the corresponding fix makes at least one new assertion fail. Name the reverted line and the failing assertion per group.
    - `cd frontend && npm run lint` exits 0 and `cd frontend && npx tsc -b` exits 0.
  </acceptance_criteria>

  <reversibility rating="reversible">Test-only additions.</reversibility>

  <done>
    Both runners carry an identical injection regression and an identical prior-ranking regression;
    `applyRootCandidateHardCap` has direct unit tests covering the no-injection byte-identity, the
    exemption, the over-cap injected-set clamp, the injected∩organic overlap, and the deterministic
    tie-break order; and each group has been shown to fail when its fix is reverted.
  </done>
</task>

<task type="auto">
  <name>Task 3: Correct both mctsSearch.ts header claims to describe the real inclusion guarantee</name>

  <files>frontend/src/lib/engine/mctsSearch.ts</files>

  <read_first>
    - `.planning/phases/196-analysis-board-stockfish-root-injection/196-PATTERNS.md` § "`frontend/src/lib/engine/mctsSearch.ts` — module header (INJECT-07)" — it quotes both header blocks verbatim, at `mctsSearch.ts:22-26` (module header, D-03/D-04 invariants) and `mctsSearch.ts:400-407` (the block immediately above `dispatchExpansion`).
    - `frontend/src/lib/engine/mctsSearch.ts` — read both header regions as they now stand after Task 1, so the corrected text describes the shipped behaviour rather than the plan's description of it.
    - `frontend/src/lib/engine/treeCommon.ts` — `applyRootCandidateHardCap`'s doc comment as Task 1 left it, so the two descriptions agree.
  </read_first>

  <action>
    Both header blocks currently frame the `extraRootMoves` union as giving inclusion unconditionally,
    naming only the Maia mass cut as the mechanism it survives. That framing is what let the hard-cap
    regression hide: the union did run after truncation, but the cap immediately downstream dropped
    the injected entry whenever the root exceeded `ROOT_CANDIDATE_HARD_CAP`.

    Rewrite both blocks so each states the guarantee and BOTH mechanisms it now survives:
    (1) the union happens AFTER `truncateAndRenormalize`, so Maia's 90% mass cut cannot drop an
    injected move; AND (2) `applyRootCandidateHardCap` receives the union's own added UCIs as an
    exemption set, capping the organic set to `ROOT_CANDIDATE_HARD_CAP - injectedCount` instead of
    letting the injected entries compete for the 15 slots — so the root hard cap cannot drop them
    either. Each block must cite `INJECT-01` so the correction is traceable to the fix that made the
    guarantee real, and must record that before Phase 196 the second mechanism was not honoured.
    Keep both blocks in the surrounding comment's existing voice and line width; do not reflow
    unrelated lines of either block, and do not touch the `Promise`-barrier sentence in the module
    header's Pattern 5 description (Phase 198 owns that region's prose).

    This is a comment-only change. No code, no test, no exported symbol changes.
  </action>

  <verify>
    <automated>cd frontend && test "$(grep -c 'INJECT-01' src/lib/engine/mctsSearch.ts)" -ge 2 && npx tsc -b && npm run lint</automated>
    <human-check>Read `git show HEAD -- frontend/src/lib/engine/mctsSearch.ts`. Confirm each of the two header blocks names the Maia mass cut AND the root hard cap as mechanisms the inclusion guarantee survives, and that neither block still asserts inclusion unconditionally. VALIDATION.md classifies INJECT-07 as manual-only: a doc comment carries no runtime assertion.</human-check>
  </verify>

  <acceptance_criteria>
    - `cd frontend && grep -c 'INJECT-01' src/lib/engine/mctsSearch.ts` reports `>= 2` — both header blocks cite the requirement.
    - `cd frontend && grep -c 'guaranteed inclusion' src/lib/engine/mctsSearch.ts` reports `0`.
    - `cd frontend && grep -c 'guarantees inclusion' src/lib/engine/mctsSearch.ts` reports `0`.
    - `cd frontend && grep -c 'ROOT_CANDIDATE_HARD_CAP\|applyRootCandidateHardCap' src/lib/engine/mctsSearch.ts` reports `>= 4` (the import, the call, and a mention in each of the two header blocks).
    - `git show --numstat HEAD -- frontend/src/lib/engine/mctsSearch.ts` shows changes confined to the two header comment regions — zero changed lines containing an executable statement.
    - `cd frontend && npx vitest run src/lib/engine/__tests__/mctsSearch.test.ts` exits 0 (a comment change must not alter behaviour).
  </acceptance_criteria>

  <reversibility rating="reversible">Comment-only.</reversibility>

  <done>
    Both `mctsSearch.ts` header blocks describe the inclusion guarantee as surviving the Maia mass cut
    AND the root hard cap, cite INJECT-01, and no longer assert inclusion unconditionally.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan edits pure in-memory client-side search-core math. Its only inputs are a UCI-keyed probability `Record` produced by the same-origin Maia ONNX worker and a `string[]` of UCIs produced by the same-origin Stockfish WASM worker — both already inside the browser's own trust domain. No new network call, no new user-input parse, no new persisted state, no server tier. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-196-01 | Tampering (data integrity, not adversarial) | `budget.extraRootMoves` reaching `dispatchExpansion` with a UCI from a superseded prior-position search | low | accept | Pre-existing containment is sufficient and verified: `treeCommon.ts`'s `applyUciMoveFen` (~:144-157) catches an illegal/malformed UCI at that FEN and returns null rather than throwing, so a stale UCI yields no child instead of a corrupt tree. Plan 196-02 additionally filters candidate UCIs through `Analysis.tsx`'s existing `bestSanFromPv(position, uci)` legality gate before they ever reach the budget. No new control added here. |
| T-196-02 | Tampering | An injected UCI absent from `effectivePolicy` producing a `NaN`/`Infinity` prior that would corrupt the cap's sort comparator | low | mitigate | The seeding expression narrows with `effectivePolicy[uci] ?? 0` and guards the denominator with `keptTotal > 0`, so the seeded value is always a finite number in `[0, 1]`. Enforced by the `treeCommon.test.ts` tie-break/determinism cases in Task 2. |
| T-196-03 | Denial of Service (self-inflicted, resource) | An oversized `extraRootMoves` inflating the root candidate count past the cap and multiplying per-search Stockfish grade cost | low | mitigate | `applyRootCandidateHardCap` clamps the total to `ROOT_CANDIDATE_HARD_CAP` even in the all-injected case (`organicSlots = Math.max(0, ...)`, injected itself sliced to the cap) — asserted by Task 2's over-cap unit test. |
| T-196-SC | Tampering (supply chain) | Package-manager installs | n/a | accept | No `npm`/`pip`/`cargo` install task exists in this phase — `196-RESEARCH.md` § Package Legitimacy Audit records "Not applicable — no external packages are installed by this phase." The gate has no surface to guard here; no `[ASSUMED]`/`[SUS]` legitimacy checkpoint is warranted. |

ASVS categories V2 (Authentication), V3 (Session Management), V4 (Access Control) and V6
(Cryptography) have no surface in this plan. V5 (Input Validation) is marginal and already covered by
pre-existing defence in depth (T-196-01) — this plan closes no V5 gap because none exists in its
scope. No threat reaches the `high` blocking threshold.
</threat_model>

<verification>
1. `cd frontend && npx vitest run src/lib/engine/__tests__/mctsSearch.test.ts src/lib/engine/__tests__/fallbackExpectimax.test.ts src/lib/engine/__tests__/treeCommon.test.ts src/lib/engine/__tests__/select.test.ts src/lib/engine/__tests__/policyTemperature.test.ts src/lib/engine/__tests__/findability.test.ts` — zero failures.
2. `cd frontend && npm test -- --run` — the full frontend suite is green (wave-merge sampling rate per 196-VALIDATION.md).
3. `cd frontend && npx tsc -b && npm run lint && npm run knip` — zero errors. `tsc -b` is required, not optional: `applyRootCandidateHardCap`'s signature changed and lint/vitest do not type-check.
4. `git log --oneline` shows one commit per task, in task order.
</verification>

<success_criteria>
- `applyRootCandidateHardCap` accepts an optional exemption set and is byte-identical for every
  existing no-injection caller (asserted, not assumed).
- Both `SearchRunner` implementations seed injected root priors on the kept-mass scale and pass the
  union's own additions to the cap, with identical expressions.
- A real `mctsSearch` run and a real `fallbackExpectimax` run each prove an out-of-mass injected UCI
  reaches `rankedLines` at `policyTemperature: 2` on a 20-legal-move root, within the cap.
- A separate observable-ranking test in each runner proves the prior is commensurate, and has been
  shown to fail with the prior reverted to `0`.
- Both `mctsSearch.ts` header blocks describe the guarantee accurately and cite INJECT-01.
- The `dispatchExpansion` diff stays within Pitfall 3's bound so Phase 198's rewrite can preserve it.
</success_criteria>

<output>
Create `.planning/phases/196-analysis-board-stockfish-root-injection/196-01-SUMMARY.md` when done.
Record in it: the actual dropped-tail UCI the test derived (settling `196-RESEARCH.md` Assumption A2),
the per-group revert-proof results from Task 2, and the final `numstat` for `mctsSearch.ts`.
</output>
