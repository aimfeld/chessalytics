---
phase: 198-mctssearch-continuous-dispatch
plan: 03
type: execute
wave: 3
depends_on: [198-02]
files_modified:
  - reports/data/engine-grading-depth-ab-<bot-stamp>.tsv
  - reports/data/engine-grading-depth-ab-<analysis-stamp>.tsv
autonomous: true
requirements: [DISPATCH-02]

must_haves:
  truths:
    - "A committed TSV records the post-ladder policy/grade wall split at the bot budget (50 nodes, concurrency 4) over 16 positions with the app-faithful Maia FIFO enabled."
    - "A committed TSV records the same split at the analysis budget (400 nodes, concurrency at the Stockfish pool size) over the same 16 positions with the FIFO enabled."
    - "Both TSVs' add-commits strictly postdate `reports/continuous-dispatch/accept-rule.md`'s add-commit."
    - "`maia_peak_inflight` reads 1 on every judged row — the measurement regime matches the app's single-inference-in-flight Maia serialisation, so the numbers describe a scheduling regime production actually runs."
    - "`maia_cpu_ms` is non-zero and less than `wall_ms` on every row — the instrument is live, not silently reporting a constant."
  artifacts:
    - path: "reports/data"
      provides: "the two committed re-baseline TSVs, one per budget"
  key_links:
    - from: "reports/data/engine-grading-depth-ab-*.tsv"
      to: "reports/continuous-dispatch/accept-rule.md"
      via: "the accept rule's §1 pins the exact command line and §2 names the exact columns these TSVs carry"
      pattern: "maia_cpu_ms|maia_peak_inflight|maia_fifo"
  prohibitions:
    - statement: "MUST NOT quote the modelled wall-clock reduction at only one budget, or without naming the position count, the budget, and whether the Maia FIFO was enabled."
      requirement_id: DISPATCH-02
      category: transparency
---

<objective>
D-15 step 3a — the post-ladder re-baseline measurement passes, at both budgets.

This is the first real measurement of the shipped `[14, 14]` / floor-10 grading ladder (U-02: its
1.4x claim is a prediction obtained by rescaling Stage A per-call costs, and `[14,14]`/10 was
never a Stage B candidate). It is also the first measurement of the policy/grade split at all —
U-03 recorded that the harness could not measure it before Plan 198-01 landed.

Purpose: produce the two numbers the exit decision in 198-04 rests on. No modelling, no verdict,
no narration happens here — this plan captures evidence and commits it.
Output: two committed TSVs under `reports/data/`.

**Execution constraint (load-bearing).** Both tasks are real-engine runs. Task 2 is a long run
(order of an hour). Run BOTH in the foreground, inline. Do not background either and do not
dispatch either to a subagent that returns while the child is still running — a backgrounded
harness child dies with the agent, which cost Phase 197 an entire wave with zero measurement.
Commit Task 1 before starting Task 2, so a Task-2 interruption never loses Task 1's result.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/198-mctssearch-continuous-dispatch/198-CONTEXT.md
@.planning/phases/198-mctssearch-continuous-dispatch/198-RESEARCH.md
@reports/continuous-dispatch/accept-rule.md
@reports/grading-ladder/report.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: bot-budget re-baseline pass (50 nodes, concurrency 4, FIFO on, 16 positions)</name>
  <precondition>`reports/continuous-dispatch/accept-rule.md` exists and its add-commit is already in git history — `git log --diff-filter=A --format=%H -- reports/continuous-dispatch/accept-rule.md` returns a SHA.</precondition>
  <read_first>
    - `reports/continuous-dispatch/accept-rule.md` § 1 "Run parameters" — the pinned command line. Run exactly that; do not improvise flags. If the pinned command does not run as written, that is a defect in the rule to be raised, not a flag to quietly change.
    - `scripts/engine-grading-depth-ab.mjs` `Usage:` header as widened by 198-01 — `--nodes`, `--depths`, `--procs`, `--openings`, `--fens`, `--maia-fifo`, `--out-dir`.
    - `frontend/src/lib/engine/botBudget.ts` — `FLAWCHESS_BOT_MAX_NODES` and `FLAWCHESS_BOT_CONCURRENCY`, and the D-19 warning: these values are read, never changed (D-10).
    - `frontend/src/lib/engine/gradingLadder.ts` lines 114 and 130 — `GRADING_DEPTH_LADDER` `[14, 14]` and `GRADING_DEPTH_FLOOR` 10, the ladder this pass baselines against.
    - `reports/grading-ladder/report.md` § "Provenance" — the commit-message discipline to copy: exact repository-relative TSV path plus the verbatim producing command.
  </read_first>
  <files>reports/data/engine-grading-depth-ab-<bot-stamp>.tsv</files>
  <action>
Run `scripts/engine-grading-depth-ab.mjs` in ladder mode at the bot budget exactly as the accept
rule's §1 pins it: node budget at `FLAWCHESS_BOT_MAX_NODES`, `--procs` at
`FLAWCHESS_BOT_CONCURRENCY`, `--maia-fifo` on, the position set widened to 16 total via
`--openings`, and `--out-dir reports/data`.

Run it in the FOREGROUND, inline. Expected order of a few minutes.

Before committing, sanity-check the emitted TSV against three invalidation conditions the accept
rule names: every row's `maia_fifo` reads `true`; every row's `maia_peak_inflight` reads 1; every
row's `maia_cpu_ms` is greater than zero and strictly less than that row's wall-clock column. A
row failing any of these is not averaged in — if any row fails, stop and raise it rather than
proceeding, because it means the measurement regime is not the one the rule judges.

Commit the TSV alone, with a commit message quoting the exact command line verbatim and naming
this as the bot-budget half of DISPATCH-02's re-baseline. Write no narration and compute no
verdict — 198-04 owns both.
  </action>
  <verify>
    <automated>ls -t reports/data/engine-grading-depth-ab-*.tsv | head -1 | xargs -I{} sh -c 'head -1 {} | tr "\t" "\n" | grep -qx maia_cpu_ms && head -1 {} | tr "\t" "\n" | grep -qx maia_peak_inflight && echo ok'</automated>
  </verify>
  <acceptance_criteria>
    - A new `reports/data/engine-grading-depth-ab-*.tsv` exists whose header row contains `maia_cpu_ms`, `maia_peak_inflight`, and `maia_fifo`.
    - It has at least 16 distinct values in its `position` column.
    - Every data row's `maia_fifo` column reads `true` and every `maia_peak_inflight` column reads 1.
    - Every data row's `maia_cpu_ms` is greater than 0 and strictly less than that row's `wall_ms`.
    - The TSV's add-commit postdates `reports/continuous-dispatch/accept-rule.md`'s add-commit: comparing `git log --diff-filter=A --format=%ct` for the two paths, the accept rule's value is the smaller.
    - The commit touched only files under `reports/data/`.
    - The commit message contains the verbatim producing command line.
  </acceptance_criteria>
  <done>The bot-budget policy/grade wall split is committed evidence, taken on a Maia-FIFO-faithful provider over 16 positions.</done>
</task>

<task type="auto">
  <name>Task 2: analysis-budget re-baseline pass (400 nodes, pool-size concurrency, FIFO on, 16 positions) — LONG, foreground only</name>
  <precondition>The bot-budget TSV from Task 1 is already committed — `git log --diff-filter=A --format=%H -- 'reports/data/engine-grading-depth-ab-*.tsv'` returns at least one SHA created in this phase.</precondition>
  <read_first>
    - `reports/continuous-dispatch/accept-rule.md` § 1 — the pinned analysis-budget command line.
    - `frontend/src/hooks/useFlawChessEngine.ts` line 277 — `concurrency: computePoolSize()`, the device-adaptive analysis-board value this pass mirrors via `--procs`.
    - `frontend/src/lib/engine/workerPool.ts` `computePoolSize()` (line 269) — what pool size the shipped analysis board resolves to, so the pass uses the same number rather than an arbitrary one.
    - `.planning/phases/198-mctssearch-continuous-dispatch/198-RESEARCH.md` § "Assumptions Log" rows A3 and A4 — the N = 16 justification and the derived (not measured) ~123-165 s per-search cost estimate this pass replaces with a measurement. Treat the estimate as planning input only.
    - The project memory note on backgrounded executor runs: a `gsd-executor` that backgrounds a long harness run and returns loses it entirely.
  </read_first>
  <files>reports/data/engine-grading-depth-ab-<analysis-stamp>.tsv</files>
  <action>
Run `scripts/engine-grading-depth-ab.mjs` in ladder mode at the analysis budget exactly as the
accept rule's §1 pins it: 400 nodes, `--procs` at the shipped `computePoolSize()` value,
`--maia-fifo` on, the same 16-position set as Task 1 (identical `--openings` / `--fens`
arguments — a different position set between the two budgets would make the two verdicts
incomparable), and `--out-dir reports/data`.

This run is on the order of an hour. Run it in the FOREGROUND, inline, from a process that
survives for its whole duration. Do NOT background it. Do NOT hand it to a subagent that returns
while it is still running. If the run must be interrupted, re-run it whole rather than stitching
partial output — a half-set TSV silently changes the position count the verdict is quoted at,
which the accept rule forbids.

Apply the same three invalidation checks as Task 1 before committing (`maia_fifo` true,
`maia_peak_inflight` 1, `maia_cpu_ms` in range). Commit the TSV alone with the verbatim command
line in the message. Write no narration and compute no verdict.
  </action>
  <verify>
    <automated>ls -t reports/data/engine-grading-depth-ab-*.tsv | head -2 | wc -l | grep -qx 2 && ls -t reports/data/engine-grading-depth-ab-*.tsv | head -1 | xargs -I{} awk -F'\t' 'NR==1{for(i=1;i<=NF;i++)h[$i]=i} NR>1{if($h["maia_peak_inflight"]!=1) bad=1} END{exit bad?1:0}' {} && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - A second new `reports/data/engine-grading-depth-ab-*.tsv` exists, distinct from Task 1's.
    - Its `nodes` / node-budget column reads 400 on every row, and Task 1's reads the bot budget on every row.
    - Both TSVs contain the SAME set of values in their `position` column (identical position sets across budgets): `comm -3 <(cut -f1 <bot.tsv> | tail -n +2 | sort -u) <(cut -f1 <analysis.tsv> | tail -n +2 | sort -u)` produces no output.
    - Every data row's `maia_fifo` reads `true`, `maia_peak_inflight` reads 1, and `maia_cpu_ms` is greater than 0 and strictly less than `wall_ms`.
    - Both TSVs' add-commits postdate the accept rule's add-commit.
    - The commit message contains the verbatim producing command line and the observed total wall time of the pass.
  </acceptance_criteria>
  <done>The analysis-budget policy/grade wall split is committed evidence over the same 16 positions, at the same FIFO regime, as the bot-budget pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → committed TSV under `reports/data/` | measurement output becomes the evidence the phase's exit decision rests on |
| operator CLI → Node harness | local operator-supplied flags and paths |

ASVS V2/V3/V4/V5/V6 are **N/A** for this plan (no auth, session, access-control, new external
input, or cryptography surface) — reason recorded in `198-RESEARCH.md` § Security Domain. This
plan writes no code at all; it runs two committed scripts and commits their output.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-198-04 | Repudiation | the two committed re-baseline TSVs | medium | mitigate | Each TSV carries `maia_fifo`, `maia_peak_inflight`, the node budget and concurrency on every row; each commit message carries the verbatim producing command and touches only `reports/data/`. |
| T-198-07 | Tampering | a partial/interrupted TSV silently changing the position count a verdict is quoted at | medium | mitigate | Acceptance criteria assert at least 16 distinct positions and an identical position set across the two budgets; the action forbids stitching partial output and requires a whole re-run on interruption. |
| T-198-08 | Spoofing | a row measured without the FIFO passing as a FIFO-faithful measurement | high | mitigate | `maia_fifo` and `maia_peak_inflight` are emitted per row and asserted per row (not per file) before commit; the accept rule states a row above peak 1 invalidates that row rather than being averaged in. |
| T-198-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase (`198-RESEARCH.md` § Package Legitimacy Audit: not applicable). |
</threat_model>

## Artifacts this phase produces (scoped to this plan)

New files:
- `reports/data/engine-grading-depth-ab-<bot-stamp>.tsv` — bot budget, FIFO on, 16 positions
- `reports/data/engine-grading-depth-ab-<analysis-stamp>.tsv` — analysis budget, FIFO on, same 16 positions

No new code symbols, flags, or columns — this plan consumes 198-01's.

<verification>
- Two new TSVs under `reports/data/`, one per budget, identical position sets.
- Every row: `maia_fifo` true, `maia_peak_inflight` 1, `maia_cpu_ms` in `(0, wall_ms)`.
- Both add-commits postdate the accept rule's add-commit.
</verification>

<success_criteria>
- The policy/grade wall split is measured, at both budgets, on a provider that mirrors the app's Maia serialisation.
- No verdict, model, or narration has been produced yet — that is 198-04's job, and separating them is what keeps the rule mechanical.
</success_criteria>

<output>
Create `.planning/phases/198-mctssearch-continuous-dispatch/198-03-SUMMARY.md` when done.
Record both resolved TSV filenames, the observed wall time of each pass, and the per-row
`maia_cpu_ms` / `maia_peak_inflight` sanity-check results.
</output>
