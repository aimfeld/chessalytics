---
phase: 199-bot-re-calibration-sweep-strength-curve-refit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - reports/bot-parity-199/accept-rule.md
  - scripts/calibration_parity_verdict.py
autonomous: true
requirements: [RECAL-01, RECAL-02, RECAL-03]
must_haves:
  truths:
    - "The parity thresholds exist as named module-level constants in scripts/calibration_parity_verdict.py and as prose in reports/bot-parity-199/accept-rule.md, both committed to git before any sweep game is played (D-03)."
    - "The Maia-family and SF-family pooled shifts are computed and threshold-checked completely separately and are never averaged or summed into one number (D-03, mirroring fit_all_bot_cells' never-merge discipline)."
    - "The null-control validity gate is evaluated first: if the blend-0 cell's shift exceeds its own tolerance in either family, the verdict is VOID and no parity claim is made regardless of the exposed cells."
    - "The shape guard fails parity when any single exposed cell's new point estimate falls outside its committed CI in BOTH families simultaneously; one family alone does not fail it."
    - "scripts/calibration_parity_verdict.py --self-test passes against synthetic fixture numbers before the script is ever pointed at real data."
    - statement: "A malformed or truncated input (missing a required cell, a CI whose width is zero, a cells TSV missing a required column) raises loudly rather than producing a silent verdict."
      verification: backstop
  artifacts:
    - reports/bot-parity-199/accept-rule.md
    - scripts/calibration_parity_verdict.py
  key_links:
    - "reports/data/bot-curves-internal-scale.json cells[] (bot_elo, bot_blend, rating_vs_maia, ci_vs_maia, rating_vs_sf, ci_vs_sf) is the committed comparison target; the script must key on (bot_elo, bot_blend) exactly."
    - "SE-from-CI-width uses NORMAL_95_Z = 1.959963985, mirrored verbatim from scripts/calibration_anchor_fit.py:83-85 so both sides of the comparison use one definition of a standard error."
  prohibitions:
    - "Threshold values must NOT be CLI flags, environment variables, or config-file entries — only module-level constants plus the committed accept-rule doc, so there is no accidental post-hoc edit path (D-03)."
    - "Do NOT import numpy or scipy — stdlib only, matching scripts/calibration_anchor_fit.py and scripts/calibration_persona_fit.py."
    - "Do NOT read, write, or regenerate reports/data/bot-strength-lookup.json, frontend/src/generated/botStrengthCurves.ts, reports/data/persona-calibration.json or frontend/src/generated/personaCalibration.ts."
---

<objective>
Fix the parity threshold in git before the operator can see a single data point. D-03 makes this a
one-way door: once results are visible the threshold can no longer be honestly set, so the
pre-registration must be committed first, with its arithmetic derived from the committed CIs rather
than asserted.

Purpose: a measurement phase whose accept rule is written after the data is a story, not a result.

Output: `reports/bot-parity-199/accept-rule.md` (the human-readable pre-registration) and
`scripts/calibration_parity_verdict.py` (the machine-readable one, with the thresholds as constants
and a `--self-test` proving the arithmetic on synthetic numbers).
</objective>

<pre_registration>
## The pre-registered thresholds — derivation (A-04)

Recomputed this session from the committed bootstrap CIs in
`reports/data/bot-curves-internal-scale.json`, not copied from RESEARCH.md. Formula mirrors
`scripts/calibration_anchor_fit.py:83-85` and `:669-700`
(`se = (ci_hi - ci_lo) / (2 * NORMAL_95_Z)`, `weight = 1/se^2`), with
`se_shift = sqrt(se_new^2 + se_old^2) ~= sqrt(2) * se_old` under the symmetric-uncertainty
assumption (the new run has not happened yet; carried as an open assumption in 199-01-PLAN.md).

| Cell | Role | se maia (old) | se sf (old) | committed CI maia | committed CI sf |
|---|---|---|---|---|---|
| 1100 / 0.00 | null control | 59.7 | 53.6 | [864, 1098] | ±105.0 |
| 1300 / 0.05 | exposed | 46.7 | 42.5 | ±91.5 | ±83.4 |
| 1900 / 0.05 | exposed | 58.8 | 38.0 | ±115.2 | ±74.4 |
| 1500 / 0.50 | exposed | 84.9 | 37.9 | ±166.4 | ±74.2 |
| 2300 / 0.50 | exposed | 75.7 | 27.8 | ±148.4 | ±54.5 |

Pooled over the **4 exposed cells only** (the null control is a validity gate, never pooled):
- Maia family: `se_pooled = 43.40` -> **95% half-width ±85.1**
- SF family: `se_pooled = 24.86` -> **95% half-width ±48.7**

Single-cell shift resolution for the null control: **±165.3 (Maia)**, **±148.6 (SF)**.

This confirms RESEARCH.md's refinement and **rejects CONTEXT.md D-03's flat ±50 in both families**:
±50 holds for SF but is materially optimistic for Maia, whose per-cell CIs are far wider (the
1500/0.5 cell alone is ±166). Adopting the per-family split is exactly the "refine BEFORE the run"
invitation D-03's discretion note makes.

**Adopted constants (final; not editable after the run starts, per D-03):**

| Constant | Value | Meaning |
|---|---|---|
| `NORMAL_95_Z` | `1.959963985` | Mirrored from `calibration_anchor_fit.py:85` |
| `PARITY_POOLED_THRESHOLD_MAIA_ELO` | `85.0` | Parity HOLDS in the Maia family if the inverse-variance-pooled shift across the 4 exposed cells has absolute value at or below this |
| `PARITY_POOLED_THRESHOLD_SF_ELO` | `50.0` | Same, SF family |
| `NULL_CONTROL_MAX_SHIFT_MAIA_ELO` | `165.0` | Validity gate: the blend-0 null control's Maia-family shift must not exceed this |
| `NULL_CONTROL_MAX_SHIFT_SF_ELO` | `149.0` | Same, SF family |

**The claim this phase can support** is "no systematic shift larger than ~85 internal ELO in the
Maia family and ~50 in the SF family" — **not** "unchanged". Anything finer requires the full
15-cell sweep, which is out of scope (D-01).
</pre_registration>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/199-bot-re-calibration-sweep-strength-curve-refit/199-CONTEXT.md
@.planning/phases/199-bot-re-calibration-sweep-strength-curve-refit/199-RESEARCH.md
@.planning/phases/199-bot-re-calibration-sweep-strength-curve-refit/199-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit the pre-registration document</name>
  <files>reports/bot-parity-199/accept-rule.md</files>
  <read_first>
    - `reports/grading-ladder/accept-rule.md` — the sibling pre-registration document from Phase 195;
      mirror its shape (a contract stated before the measurement, with an explicit exit branch).
    - `.planning/phases/199-.../199-CONTEXT.md` decisions D-03, D-04, D-05, and amendment A-04 —
      the source of every claim this document records.
    - This plan's `<pre_registration>` block — the derivation table and adopted constants are
      already settled; transcribe them, do not re-derive.
  </read_first>
  <action>
    Create `reports/bot-parity-199/accept-rule.md` as the committed, human-readable pre-registration.
    Mirror `reports/grading-ladder/accept-rule.md`'s shape: a header block naming the phase, the date
    and the decision IDs it discharges, then the contract.

    State, in this order: (1) that this is a parity check and not a refit, per D-01; (2) the 5 cells
    and each one's role, per D-05 — `1100/0.00` null control, `1300/0.05` the measured non-monotone
    dip, `1900/0.05` light's top end, `1500/0.5` deep's low end, `2300/0.5` the cell all four
    rung-1800 personas share; (3) the three criteria in evaluation order — **validity gate first**
    (null-control shift within `NULL_CONTROL_MAX_SHIFT_MAIA_ELO` 165.0 and
    `NULL_CONTROL_MAX_SHIFT_SF_ELO` 149.0, else the verdict is VOID), then **primary** (per-family
    inverse-variance-pooled shift across the 4 exposed cells within
    `PARITY_POOLED_THRESHOLD_MAIA_ELO` 85.0 and `PARITY_POOLED_THRESHOLD_SF_ELO` 50.0), then **shape
    guard** (fail if any single exposed cell's new point estimate falls outside its committed CI in
    both families simultaneously); (4) the derivation table from this plan's `<pre_registration>`
    block verbatim, so a reader can check the arithmetic against
    `reports/data/bot-curves-internal-scale.json` themselves; (5) the stated resolution limit and the
    explicit sentence that the supportable claim is "no systematic shift larger than ~85 Maia / ~50
    SF", not "unchanged"; (6) the D-04 exit branch — on parity failure this phase STOPS AND REPORTS,
    delivering the measurement plus a written recommendation, with the revert-vs-refit call left to a
    separate operator decision made with data in hand.

    Add a closing note recording that A-04 supersedes D-03's flat ±50: the SF threshold keeps D-03's
    number, the Maia threshold is widened to 85.0 because the committed Maia CIs are roughly twice as
    wide, and that leaving Maia at 50.0 would have produced a false-fail rate the run cannot support.

    Add an explicit immutability line: these numbers are fixed as of this commit and are not editable
    once the sweep starts.
  </action>
  <verify>
    <automated>test -f reports/bot-parity-199/accept-rule.md && grep -q '85.0' reports/bot-parity-199/accept-rule.md && grep -q '165.0' reports/bot-parity-199/accept-rule.md && git log --oneline -1 -- reports/bot-parity-199/accept-rule.md</automated>
  </verify>
  <acceptance_criteria>
    - `reports/bot-parity-199/accept-rule.md` exists and is committed (`git log` for the path returns
      a commit).
    - All five adopted constant values appear in the document: `85.0`, `50.0`, `165.0`, `149.0`,
      `1.959963985`.
    - The document states the three criteria in evaluation order with the validity gate first.
    - The document contains the D-04 exit branch stating that parity failure stops the phase and
      reports, rather than reverting or escalating to a refit.
    - The document contains the derivation table with all 5 cells and both per-family pooled
      half-widths (85.1 and 48.7 as derived, alongside the 85.0/50.0 adopted values).
    - The document contains an immutability statement.
  </acceptance_criteria>
  <reversibility rating="costly">
    Revising the contract is still legitimate at this point, because no data exists yet — but it costs
    a re-derivation and a re-commit. The genuine one-way door is crossed at launch, not here: the
    `checkpoint:decision` in 199-04-PLAN.md task 2 is the last moment at which a revision is honest,
    and it offers `revise-first` explicitly for that reason.
  </reversibility>
  <done>
    The parity contract is in git, with its arithmetic checkable against the committed CIs, before any
    data exists.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The parity verdict script with pre-registered constants and a self-test</name>
  <files>scripts/calibration_parity_verdict.py</files>
  <behavior>
    - Given synthetic old/new (rating, ci_lo, ci_hi) tuples for 4 exposed cells whose true pooled
      shift is 0 by construction, the computed pooled shift is ~0 and the verdict is HOLDS in both
      families.
    - Given the same fixture shifted by +200 in every cell, the pooled shift is ~+200 and the verdict
      is FAILS in both families.
    - Given a fixture that HOLDS on the pooled criterion but where one exposed cell's new estimate
      lands outside its committed CI in both families, the shape guard fires and the verdict is FAILS.
    - Given the same single-cell excursion in only ONE family, the shape guard does NOT fire.
    - Given a null-control shift exceeding 165.0 (Maia) or 149.0 (SF), the verdict is VOID and no
      HOLDS/FAILS claim is emitted for the exposed cells.
    - Given an input missing a required cell key, or a CI whose width is zero, the script raises
      ValueError rather than emitting a verdict.
  </behavior>
  <read_first>
    - `scripts/calibration_anchor_fit.py` lines 80-90 (`NORMAL_95_Z`) and lines 669-700
      (`combine_preset_g_preset`) — the SE-from-CI-width and inverse-variance-pooling formulas to
      mirror verbatim, not re-derive.
    - `scripts/calibration_persona_fit.py` lines 130-160 (by-name header indexing of a `-cells.tsv`),
      lines 349-421 (the `--self-test` fixture convention, including the fail-loud-on-malformed
      case), and lines 424-460 (the argparse shape with `--self-test` as an early-return branch).
    - `reports/data/bot-curves-internal-scale.json` — the committed comparison target; note the
      top-level `cells` list and each cell's `bot_elo`, `bot_blend`, `rating_vs_maia`, `ci_vs_maia`,
      `rating_vs_sf`, `ci_vs_sf` keys.
    - This plan's `<pre_registration>` block — the adopted constant values.
    - `.planning/phases/199-.../199-PATTERNS.md` section "New: D-03 pooled-shift / parity-verdict
      script" — the module header and constant-naming pattern.
  </read_first>
  <action>
    Create `scripts/calibration_parity_verdict.py`. Name it `_verdict`, not `_check`, to avoid
    confusion with the unrelated existing `scripts/lib/calibration-parity.check.mjs`, which pins
    bot-move selection parity (CAL-02), a different meaning of the word.

    Follow the `calibration_persona_fit.py` module skeleton: `from __future__ import annotations`, a
    docstring stating stdlib-only (`argparse`, `csv`, `json`, `math`, `pathlib`, `sys` — no numpy, no
    scipy) and noting this is a `scripts/` research tool exempt from the Sentry capture rules that
    apply to `app/services` and `app/routers`, with usage examples for both `--self-test` and the
    real invocation.

    Declare the five pre-registered values as module-level constants with the exact names and values
    from this plan's `<pre_registration>` block: `NORMAL_95_Z`, `PARITY_POOLED_THRESHOLD_MAIA_ELO`,
    `PARITY_POOLED_THRESHOLD_SF_ELO`, `NULL_CONTROL_MAX_SHIFT_MAIA_ELO`,
    `NULL_CONTROL_MAX_SHIFT_SF_ELO`. Give each a short comment naming D-03/A-04 and pointing at
    `reports/bot-parity-199/accept-rule.md`. They must not be reachable from any CLI flag, env var or
    config file (per CLAUDE.md's no-magic-numbers rule these are named constants, and per D-03 they
    are deliberately not parameterised).

    Implement `_se_from_ci(ci_lo, ci_hi)` returning `(ci_hi - ci_lo) / (2.0 * NORMAL_95_Z)`, raising
    `ValueError` on a non-positive width. Implement a pooling helper taking a sequence of
    `(shift, se_shift)` pairs and returning the inverse-variance-weighted mean plus the pooled
    standard error, with the unweighted-mean fallback `combine_preset_g_preset` uses when every
    weight is degenerate. Per-cell, `shift = rating_new - rating_old` and
    `se_shift = math.hypot(se_new, se_old)`.

    Implement `parity_verdict(...)` returning a structured result with, per family and evaluated in
    this order: the null-control shift and its gate outcome; the pooled shift, pooled SE and
    threshold comparison over the 4 exposed cells; the per-cell shape-guard evaluation. Compute and
    report the Maia family and the SF family in completely separate code paths and never average,
    sum or otherwise merge them before the threshold check — mirror `fit_all_bot_cells`' never-merge
    discipline. Emit one of exactly three overall verdict strings: void, holds, fails. Void wins over
    everything; fails wins over holds.

    Wire the real inputs: `--old-json` (default `reports/data/bot-curves-internal-scale.json`),
    `--new-cells-tsv` accepted repeatedly (one per cell out-dir, since each cell is a separate
    invocation with its own aggregate), and `--out-json` (default
    `reports/data/bot-parity-199-verdict.json`). Extract columns from each `-cells.tsv` by NAME, not
    position, matching `calibration_persona_fit.py:140`'s `header.index` pattern. Fit the new
    per-cell ratings and CIs by calling the EXISTING `calibration_anchor_fit` functions
    (`fit_bot_cell_rating` and `bootstrap_bot_cell_ci`) unmodified — do not write a second fitter.
    Raise `ValueError` on a missing required column, a missing expected cell key, or a cell present
    in the new data but absent from the old JSON.

    Add `--self-test` as an early-return argparse branch checked before any real-file argument is
    required, covering exactly the six cases in this task's `<behavior>` block with hardcoded,
    obviously-synthetic fixture numbers carrying an inline comment stating they are NOT the real
    internal scale. Print a single success line naming the module on pass.

    Keep every function inside CLAUDE.md's limits: nesting depth at most 3, and split the
    per-family evaluation into its own helper rather than nesting two family loops inside
    `parity_verdict`.
  </action>
  <verify>
    <automated>uv run python scripts/calibration_parity_verdict.py --self-test</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python scripts/calibration_parity_verdict.py --self-test` exits 0 and prints a success
      line naming the module.
    - `--self-test` runs without any `--old-json` or `--new-cells-tsv` argument (early-return branch).
    - `grep -nE '^(NORMAL_95_Z|PARITY_POOLED_THRESHOLD_(MAIA|SF)_ELO|NULL_CONTROL_MAX_SHIFT_(MAIA|SF)_ELO)' scripts/calibration_parity_verdict.py`
      returns exactly 5 lines with the values `1.959963985`, `85.0`, `50.0`, `165.0`, `149.0`.
    - The five constants are not reachable from any runtime input. With comment lines stripped
      (`grep -v '^\s*#' scripts/calibration_parity_verdict.py`), none of the five constant names
      appears on a line that also contains `add_argument`, `getenv`, `environ`, or an assignment from
      an `args.` attribute. Equivalently: each constant name is assigned exactly once, at module
      level, from a numeric literal.
    - `grep -nE '^\s*(import|from)\s+(numpy|scipy)' scripts/calibration_parity_verdict.py` returns
      nothing.
    - Temporarily raising `PARITY_POOLED_THRESHOLD_SF_ELO` to `500.0` makes the "+200 shift" self-test
      case fail; revert after confirming. Temporarily deleting the null-control gate makes the VOID
      case fail; revert after confirming.
    - The script imports `fit_bot_cell_rating` and `bootstrap_bot_cell_ci` from
      `calibration_anchor_fit` rather than defining its own fitter
      (`grep -n 'from calibration_anchor_fit import\|import calibration_anchor_fit' scripts/calibration_parity_verdict.py`
      returns at least one line).
    - `uv run ruff format --check scripts/calibration_parity_verdict.py` and
      `uv run ruff check scripts/calibration_parity_verdict.py` both pass.
    - The script has not read or written any of the four shipping calibration artifacts:
      `git status --porcelain reports/data/bot-strength-lookup.json frontend/src/generated/botStrengthCurves.ts reports/data/persona-calibration.json frontend/src/generated/personaCalibration.ts`
      is empty.
  </acceptance_criteria>
  <reversibility rating="costly">
    The surrounding code is freely reversible. The constant VALUES become one-way only once the sweep
    starts producing rows; 199-04-PLAN.md task 2's `checkpoint:decision` is the gate that closes that
    door, and it is where the one-way rating properly sits.
  </reversibility>
  <done>
    The verdict machine exists, its arithmetic is proven on synthetic fixtures, and its thresholds are
    in git with no runtime edit path — all before any real data exists.
  </done>
</task>

</tasks>

<threat_model>
Assessed in 199-01-PLAN.md's `<threat_model>` for the whole phase: offline research tooling only, no
web tier, no user data, no packages introduced. The one threat touching this plan is T-199-03
(information disclosure via committed artifacts) — mitigated because this plan commits only a
prose document and a stdlib-only script, neither carrying secrets or raw logs.
</threat_model>

<verification>
- `uv run python scripts/calibration_parity_verdict.py --self-test` exits 0.
- `reports/bot-parity-199/accept-rule.md` is committed and contains all five threshold values.
- The four shipping calibration artifacts are unchanged.
- `git log --oneline -- reports/bot-parity-199/accept-rule.md scripts/calibration_parity_verdict.py`
  shows both landed before any `reports/data/sweep-199-*` path exists in the tree.
</verification>

<success_criteria>
The parity threshold is pre-registered in two committed forms — prose and constants — its arithmetic
is re-derived from the committed CIs rather than copied, the two anchor families are evaluated
separately, and the verdict machine passes a synthetic self-test before ever touching real data.
</success_criteria>

<output>
Create `.planning/phases/199-bot-re-calibration-sweep-strength-curve-refit/199-02-SUMMARY.md` when done.
</output>
