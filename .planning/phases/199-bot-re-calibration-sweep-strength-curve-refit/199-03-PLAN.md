---
phase: 199-bot-re-calibration-sweep-strength-curve-refit
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/preset-supervisor.sh
  - bin/run_persona_calibration_sweep.sh
autonomous: true
requirements: [RECAL-01, RECAL-03, RECAL-04]
must_haves:
  truths:
    - "bin/preset-supervisor.sh threads a caller-supplied anchor set into the harness invocation, so a pinned-bracket run keeps the automatic resume-on-crash loop instead of forfeiting it (D-02 and RECAL-04 satisfied together)."
    - "The anchor set is threaded into the RESUME relaunch as well as the cold launch, because calibration-harness.mjs:1477 refuses to resume a ledger whose anchor is absent from the current anchor pool."
    - "With no anchor override set, bin/preset-supervisor.sh produces a harness command line identical to its pre-phase behaviour (the default 10-anchor pool)."
    - "bin/run_persona_calibration_sweep.sh accepts a caller-supplied data directory, so the 199 persona spot-check writes to a fresh tree instead of silently no-opping against the stale 2026-07-22/23 aggregate already present in reports/data/persona-sweep-attacker-1600 and reports/data/persona-sweep-wall-1800 (P-06)."
    - statement: "With no data-directory override set, bin/run_persona_calibration_sweep.sh writes under reports/data exactly as before, so a future full 24-persona sweep is unaffected."
      verification: backstop
    - statement: "A pinned anchor token that is not a valid anchor spec fails loudly at harness parse time rather than being silently dropped, leaving the run on the default pool."
      verification: backstop
  artifacts:
    - bin/preset-supervisor.sh
    - bin/run_persona_calibration_sweep.sh
  key_links:
    - "preset-supervisor.sh launch() is the single nohup harness invocation used for BOTH cold start and resume; threading anchors there covers both paths in one edit."
    - "run_persona_calibration_sweep.sh launch_persona() -> PRESET_SUPERVISOR_DIR export -> preset-supervisor.sh DIR -> cells_present() skip check; the data-dir override must flow through this whole chain."
  prohibitions:
    - "Do NOT change the default behaviour of either script when the new environment variables are unset — a future full sweep must be unaffected."
    - "Do NOT remove or weaken the duplicate-run guard in run_persona_calibration_sweep.sh lines 87-94; the 199 persona pass is sequenced after the curve cells precisely because of it (P-07)."
    - "Do NOT delete, move or overwrite the existing reports/data/persona-sweep-*/ or reports/data/sweep-{human,light,deep}/ directories — they are the pre-195 timing baseline plan 05 parses."
    - "Do NOT modify frontend/src/lib/engine/gradingLadder.ts or any frontend file."
---

<objective>
Close the one real tooling gap RESEARCH.md identified: D-02's pinned brackets and RECAL-04's
resume-on-crash guarantee currently pull in opposite directions, because neither
`bin/preset-supervisor.sh` nor `bin/run_bot_curves_sweep.sh` exposes `--anchors`. Add the smallest
env-var seam that gives both, and fix a second, unflagged collision that would otherwise have made
the persona spot-check silently produce zero games.

Purpose: 4 of the 5 cells are blend>0 — exactly the population that dies ~5-6h in with the
onnxruntime-web wasm out-of-bounds fault. Invoking the bare harness per cell to get pinned brackets
would trade RECAL-04 away for D-02. This plan refuses that trade.

Output: `PRESET_SUPERVISOR_ANCHORS` on the supervisor and `PERSONA_SWEEP_DATA_DIR` on the persona
runbook, both defaulting to today's exact behaviour.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/199-bot-re-calibration-sweep-strength-curve-refit/199-CONTEXT.md
@.planning/phases/199-bot-re-calibration-sweep-strength-curve-refit/199-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread a pinned anchor set through the crash supervisor</name>
  <files>bin/preset-supervisor.sh</files>
  <read_first>
    - `bin/preset-supervisor.sh` in full (91 lines) — in particular the env-override block at lines
      21-23 and 36-37 (`PRESET_SUPERVISOR_DIR`, `PRESET_SUPERVISOR_GAMES`), the existing
      extensibility seam to mirror, and `launch()` at lines 50-66, whose `resume_args=()` array
      pattern (lines 58-59) is the exact idiom for conditionally adding a flag.
    - `scripts/calibration-harness.mjs` lines 344-362 (`parseAnchorSpec`) — the fail-loud token
      validation that makes a typo in the anchor list an immediate error rather than a silent
      fallback to the default pool.
    - `scripts/calibration-harness.mjs` lines 1470-1485 — the resume guards. A differing seed and an
      anchor absent from the current pool are both hard refusals. This is why the anchors must be
      present on the resume relaunch, not only on the cold start.
    - `.planning/phases/199-bot-re-calibration-sweep-strength-curve-refit/199-RESEARCH.md` section
      "Exact CLI invocations for the 5 curve cells (D-02, verified brackets)" — the per-cell anchor
      sets, and the finding that neither existing runbook script exposes an anchors flag.
  </read_first>
  <action>
    Add a third optional env override to `bin/preset-supervisor.sh` named `PRESET_SUPERVISOR_ANCHORS`,
    holding a comma-separated anchor token list. Declare it alongside `DIR` and `GAMES_PER_CELL`
    (currently lines 36-37) with an empty default, and document it in the "Env overrides" header block
    at lines 21-23 in the same one-line-per-variable style as the two existing entries.

    In `launch()`, build a bash array exactly the way `resume_args` is built at lines 58-59: declare a
    local array, and only when the variable is non-empty populate it with the anchors flag and its
    value. Expand that array inside the existing `nohup node` invocation. When the variable is unset
    or empty the array expands to nothing, the harness receives no anchors flag, the default
    10-anchor pool applies, and the command line matches today's exactly.

    Because `launch()` is the single code path used for both the cold start and every crash-resume
    relaunch, threading the anchors there covers both automatically — which is required, since the
    harness refuses to resume a ledger whose recorded anchor is not in the current anchor pool. Add a
    short comment at the array declaration recording that reason, so a future reader does not
    "simplify" the anchors onto the cold-start branch only and reintroduce the refusal at the worst
    possible moment, five hours into a run.

    Update the header `Usage:` block with a pinned-bracket example: exporting `PRESET_SUPERVISOR_DIR`
    and `PRESET_SUPERVISOR_ANCHORS`, then calling the script with a name, a blend and a single elo
    value.

    Do not change `--stockfish-procs 4`, `--seed 1`, the fast-crash guard, `MIN_HEALTHY_SECS`,
    `FAST_FAIL_LIMIT`, `find_ledger` or `cells_present`.
  </action>
  <verify>
    <automated>bash -n bin/preset-supervisor.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n bin/preset-supervisor.sh` exits 0.
    - Supervised pinned-bracket smoke run: with `PRESET_SUPERVISOR_DIR` set to a scratch dir,
      `PRESET_SUPERVISOR_ANCHORS` set to `maia700,maia1100` and `PRESET_SUPERVISOR_GAMES` set to `1`,
      invoking the script with name `199-smoke`, blend `0` and elo `1100` runs to completion and the
      supervisor logs its COMPLETE line. Blend 0 means no search, so this finishes in well under a
      minute.
    - In that smoke run's raw ledger, the distinct values of the `anchor` column are exactly
      `maia700` and `maia1100` — no third anchor appears. This is the proof that the pool was pinned
      and the locate pass had nothing else to pick from.
    - Re-invoking the same command against the same scratch dir exits 0 immediately via
      `cells_present` rather than replaying games.
    - Unset-default parity: with `PRESET_SUPERVISOR_ANCHORS` unset, the harness command line built by
      `launch()` contains no anchors flag. Verify by adding a temporary `echo` of the assembled
      command (or by `bash -x` tracing a run that is killed immediately after launch), then removing
      the temporary instrumentation.
    - Fail-loud on a bad token: setting `PRESET_SUPERVISOR_ANCHORS` to a nonsense token such as
      `maia999` makes the harness exit non-zero with a parse error naming that token, and the
      supervisor's fast-crash guard aborts after its limit rather than hot-looping. Confirm the abort
      message appears, then clean up the scratch dir.
    - `git diff HEAD -- bin/preset-supervisor.sh` shows changes only in the header comment block, the
      variable declarations near line 36, and inside `launch()`. `MIN_HEALTHY_SECS`,
      `FAST_FAIL_LIMIT`, `find_ledger`, `cells_present` and the watch loop are untouched.
    - All scratch directories created during verification are removed before commit.
  </acceptance_criteria>
  <reversibility rating="reversible">
    A purely additive env-var seam with an empty default; deleting it restores the prior file exactly.
  </reversibility>
  <done>
    A pinned-bracket cell can be launched under the crash supervisor, and a resumed relaunch presents
    the same anchor pool the harness demands.
  </done>
</task>

<task type="auto">
  <name>Task 2: Give the persona sweep a caller-supplied data directory</name>
  <files>bin/run_persona_calibration_sweep.sh</files>
  <read_first>
    - `bin/run_persona_calibration_sweep.sh` in full — specifically the defaults block at lines 55-64
      (`DATA_DIR`, `COMBINED_TSV`), the duplicate-run guard at lines 87-94, `launch_persona()` at
      lines 172-188 (where `outdir` is derived and `PRESET_SUPERVISOR_DIR` is exported), and the
      combine-plus-fit tail at lines 225-255 (which overwrites `reports/data/persona-calibration.json`
      and regenerates `frontend/src/generated/personaCalibration.ts`).
    - `bin/preset-supervisor.sh` line 48 (`cells_present`) and line 74 — the skip-on-complete branch
      that makes a re-run against a directory holding a prior aggregate exit 0 with zero games played.
    - The existing contents of `reports/data/persona-sweep-attacker-1600/` and
      `reports/data/persona-sweep-wall-1800/` — both already hold a 2026-07-22/23 aggregate, which is
      exactly what would trigger that skip.
  </read_first>
  <action>
    Make `DATA_DIR` (currently the hardcoded literal at line 59) honour an optional
    `PERSONA_SWEEP_DATA_DIR` environment variable, defaulting to the current value so every existing
    invocation behaves identically. Use the same `${VAR:-default}` idiom `bin/preset-supervisor.sh`
    already uses for its own overrides, and document the new variable in the header comment block
    next to the existing usage examples.

    Confirm the override flows through the whole chain rather than only the top-level variable:
    `COMBINED_TSV` is derived from `DATA_DIR`, `launch_persona()` derives each persona's out-dir from
    `DATA_DIR`, and that out-dir is exported as `PRESET_SUPERVISOR_DIR` into the supervisor, which
    uses it for its log, its ledger discovery and its completion check. Read each of those sites and
    make sure none of them re-hardcodes the base path.

    Add a comment at the `DATA_DIR` declaration recording WHY the override exists: the per-persona
    out-dir name is derived from the persona id alone, so a re-measurement of a persona that has
    already been swept would land on the prior run's directory, and the supervisor's completion check
    would treat the stale aggregate as "already done" and exit without playing a game. Name Phase 199
    as the first caller.

    Leave the duplicate-run guard, the rolling queue, the longest-first scheduling, the style
    threading via `CALIBRATION_HARNESS_STYLE`, the persona-tuple dump from `ALL_PERSONA_CELLS`, and
    the combine-plus-fit tail all unchanged. In particular do not alter the fit step: Phase 199
    suppresses it with the script's existing no-fit flag rather than by editing it, so that the
    RECAL-03 artifacts stay untouched by construction.
  </action>
  <verify>
    <automated>bash -n bin/run_persona_calibration_sweep.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n bin/run_persona_calibration_sweep.sh` exits 0.
    - `grep -n 'PERSONA_SWEEP_DATA_DIR' bin/run_persona_calibration_sweep.sh` returns the declaration
      plus its header-comment documentation line.
    - Dry-check of the derivation chain: `grep -n 'DATA_DIR' bin/run_persona_calibration_sweep.sh`
      shows `COMBINED_TSV` and `launch_persona`'s out-dir both derived from it, and
      `grep -n 'reports/data' bin/run_persona_calibration_sweep.sh` shows the literal appearing only
      as the default value of `DATA_DIR`, nowhere else in a path construction.
    - Override smoke: with `PERSONA_SWEEP_DATA_DIR` pointed at a scratch directory and the personas
      filter set to `attacker-1600`, the script creates the persona out-dir under the scratch tree,
      not under `reports/data/persona-sweep-attacker-1600`. Interrupt the run once the out-dir and its
      log appear; the point is to prove the path, not to play the games. Confirm the pre-existing
      `reports/data/persona-sweep-attacker-1600/` directory is unmodified afterwards
      (`git status --porcelain --ignored reports/data/persona-sweep-attacker-1600/` shows no new or
      changed file, and its file mtimes are unchanged).
    - Default parity: with `PERSONA_SWEEP_DATA_DIR` unset, the resolved `DATA_DIR` is the original
      literal. Confirm by sourcing the defaults block in a subshell, or by a `bash -x` trace of an
      immediately-interrupted run.
    - The duplicate-run guard block is byte-identical: `git diff HEAD -- bin/run_persona_calibration_sweep.sh`
      shows no change within lines 87-94's guard, nor in the combine-plus-fit tail.
    - All scratch directories created during verification are removed before commit.
  </acceptance_criteria>
  <reversibility rating="reversible">
    A one-line default-value change plus comments; reverting restores the prior behaviour exactly.
  </reversibility>
  <done>
    The 199 persona spot-check can write to a fresh tree, so the supervisor's completion check cannot
    mistake the 2026-07-23 run for this one.
  </done>
</task>

</tasks>

<threat_model>
Assessed for the whole phase in 199-01-PLAN.md's `<threat_model>`: offline research and measurement
tooling only, no web tier, no user data, no packages introduced, so the STRIDE register is
deliberately minimal rather than padded. The threat touching this plan is T-199-02 (tampering via a
resume against a mismatched anchor pool), and task 1 is precisely its mitigation: threading the
anchors through the shared `launch()` path means a crash-resume re-presents the same pool the harness
requires, instead of tripping the refusal mid-run.
</threat_model>

<verification>
- `bash -n bin/preset-supervisor.sh` and `bash -n bin/run_persona_calibration_sweep.sh` both exit 0.
- A supervised pinned-bracket smoke run completes with exactly the two pinned anchors in its ledger.
- With both new environment variables unset, neither script's behaviour differs from `HEAD~`.
- `git status --porcelain frontend/` is empty.
- `git status --porcelain reports/data/bot-strength-lookup.json frontend/src/generated/botStrengthCurves.ts reports/data/persona-calibration.json frontend/src/generated/personaCalibration.ts`
  is empty.
- The pre-195 baseline directories still exist untouched: `reports/data/sweep-human/run.log`,
  `reports/data/sweep-light/run.log`, `reports/data/sweep-deep/run.log`,
  `reports/data/persona-sweep-attacker-1600/run.log`, `reports/data/persona-sweep-wall-1800/run.log`.
</verification>

<success_criteria>
Pinned brackets and automatic crash recovery hold at the same time (D-02 plus RECAL-04), the persona
spot-check has somewhere fresh to write, and both scripts behave exactly as before when the new
environment variables are unset.
</success_criteria>

<output>
Create `.planning/phases/199-bot-re-calibration-sweep-strength-curve-refit/199-03-SUMMARY.md` when done.
</output>