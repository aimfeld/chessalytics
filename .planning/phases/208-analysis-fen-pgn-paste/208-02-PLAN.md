---
phase: 208-analysis-fen-pgn-paste
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/schemas/normalization.py
  - app/repositories/query_utils.py
  - app/services/normalization.py
  - tests/repositories/test_query_utils.py
  - tests/repositories/test_pasted_platform_exclusion.py
  - tests/services/test_normalize_pasted_game.py
autonomous: true
requirements: [PASTE-05]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

assumption_delta_decision:
  noun: "platform — the `Platform` Literal in app/schemas/normalization.py and the games.platform column"
  decision: no-change
  rationale: >-
    `platform` now genuinely carries two meanings — WHERE a game was played, and WHETHER it
    counts toward analytics — and `'pgn'` is the first value that is unconditionally excluded.
    The generalized noun is analytics eligibility, and `DEFAULT_EXCLUDED_PLATFORMS` already IS
    that seam: a single derived set over platform values, not a competing source of truth.
    Promoting eligibility to its own column would require a migration (an explicit phase
    non-goal), would be derived state that drifts on every insert, and is the per-game
    "count this in my stats" opt-in the seed already reasoned through and rejected.
    Accepted debt: eligibility is a function of platform, so a future platform whose
    eligibility varies PER ROW (e.g. "pasted, but it really is my game — count it") cannot be
    expressed and would force the promote. Task 1 adds
    `tests/repositories/test_query_utils.py::test_every_platform_has_an_analytics_disposition`,
    which goes red if a fifth platform value is added without deciding its eligibility.

must_haves:
  truths:
    # --- Goal-backward (SC-5 / PASTE-05) ---
    - "A platform='pgn' game is absent from every query built through apply_game_filters() when no explicit platform list is passed"
    - "Removing 'pgn' from DEFAULT_EXCLUDED_PLATFORMS makes tests/repositories/test_pasted_platform_exclusion.py fail — the exclusion proof is red-if-removed, not a symbol-presence check"
    - "'pgn' is added in exactly one place; no per-router or per-repository platform check is introduced anywhere in app/"
    - "Every value in the backend Platform Literal is either in DEFAULT_EXCLUDED_PLATFORMS or in ANALYTICS_INCLUDED_PLATFORMS, and the two sets are disjoint"
    - "Passing an explicit platform list equal to ANALYTICS_INCLUDED_PLATFORMS returns exactly the same rows as passing platform=None — the equivalence the Library opt-in in Plan 04 depends on"
    # --- D-16 hash determinism ---
    - "pasted_game_identity_hash() returns the same 64-character hex digest for the same game re-pasted with different [White]/[Black]/[Event]/[Site]/[Date]/[Result] header spellings — headers do not participate in the hash"
    - "pasted_game_identity_hash() returns different digests for the same SAN sequence played from two different root positions"
    - "The canonical root FEN feeding the hash uses only piece placement, side to move, and castling rights — halfmove clock, fullmove number and the en-passant field are dropped, so two sources emitting the same position with different counters or ep conventions hash identically"
    - "The digest is 64 hex characters, which fits games.platform_game_id String(100)"
    # --- D-07 untimed ---
    - "A normalized pasted game carries time_control_str, time_control_bucket, time_control_seconds, base_time_seconds and increment_seconds all None, and rated=False"
    # --- Normalization correctness ---
    - "normalize_pasted_game derives the starting side to move from game.board().turn, so a [SetUp]/[FEN] Black-to-move root is handled correctly (WR-02) — White-moves-first is never assumed"
    - "normalize_pasted_game never stores an unrecognized [Termination] header string in termination_raw; termination is derived from the final board state using the closed vocabulary, and termination_raw mirrors it (the CR-02 String(50) overflow cannot recur)"
    - "normalize_pasted_game applies no [%clk]-for-both-colors gate — a clockless pasted PGN normalizes successfully"
    - "played_at is parsed from the PGN's [UTCDate]/[UTCTime] pair when present, else [Date]; an unknown-date header of the ????.??.?? form yields None rather than today's date"
    # --- Edge probe: PASTE-05 (unclassified row → see flagged assumptions in this plan's Assumptions section) ---
    - "normalize_pasted_game returns None (never raises) for: unparseable text, zero mainline moves, a mainline longer than MAX_PASTED_PGN_PLIES, and a game whose result cannot be determined from either the [Result] header or the final board"
    - "No Sentry event is captured for an expected validation failure (unparseable or resultless input); Sentry capture is reserved for a genuinely unexpected parse exception, matching normalize_flawchess_game's split"
  prohibitions:
    - statement: "MUST NOT let the 'pasted games never count toward your stats' promise erode by exception: eligibility lives in exactly one seam, and any future surface that reads games without going through apply_game_filters() is a silent breach the user cannot see. The invariant test exists so adding a platform value forces an explicit eligibility decision rather than inheriting one by omission"
      category: values
      status: resolved
      verification: judgment
  artifacts:
    - path: "app/repositories/query_utils.py"
      provides: "The single analytics-exclusion seam extended with 'pgn' plus an opt-in escape"
      contains: "DEFAULT_EXCLUDED_PLATFORMS"
    - path: "app/services/normalization.py"
      provides: "normalize_pasted_game, pasted_game_identity_hash, canonical_root_fen, parse_pgn_played_at"
      exports: ["normalize_pasted_game", "pasted_game_identity_hash", "canonical_root_fen", "parse_pgn_played_at", "MAX_PASTED_PGN_PLIES"]
    - path: "tests/repositories/test_pasted_platform_exclusion.py"
      provides: "The red-if-removed end-to-end exclusion proof SC-5 requires"
  key_links:
    - from: "app/services/normalization.py"
      to: "app/schemas/normalization.py"
      via: "normalize_pasted_game returns a NormalizedGame with platform='pgn', which the Platform Literal must admit"
      pattern: "platform=\"pgn\""
    - from: "app/repositories/query_utils.py"
      to: "app/models/game.py"
      via: "Game.platform.notin_(DEFAULT_EXCLUDED_PLATFORMS) is the one applied exclusion"
      pattern: "notin_\\(DEFAULT_EXCLUDED_PLATFORMS"
---

<objective>
Make `platform='pgn'` a first-class, always-excluded platform value, and build the pure
server-side PGN normalization the save path will consume: a deterministic identity hash
(D-16), a canonical root FEN, PGN date parsing, and a `normalize_pasted_game()` that drops
the `[%clk]` gate `normalize_flawchess_game()` enforces.

Purpose: this is the phase's persistence-independent foundation. Everything here is pure
functions plus one central constant, so it can be built and proven in parallel with the
frontend tracer (Plan 01) without either being able to invalidate the other.

Output: the exclusion seam with a red-if-removed proof, an eligibility invariant test that
forces a future platform value to declare itself, and a tested `normalize_pasted_game()`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/208-analysis-fen-pgn-paste/208-CONTEXT.md
@.planning/phases/208-analysis-fen-pgn-paste/208-PATTERNS.md
@.planning/seeds/SEED-144-analysis-fen-pgn-paste.md
@CLAUDE.md
</context>

## Artifacts this phase produces

See the "Artifacts this phase produces" section of `208-01-PLAN.md` for the complete
phase-wide list. The symbols this plan creates are:

- `app/schemas/normalization.py` — `"pgn"` added to the `Platform` Literal
- `app/repositories/query_utils.py` — `"pgn"` in `DEFAULT_EXCLUDED_PLATFORMS`; new
  `ANALYTICS_INCLUDED_PLATFORMS` constant; new keyword-only `include_pasted` parameter on
  `apply_game_filters()`
- `app/services/normalization.py` — `normalize_pasted_game()`,
  `pasted_game_identity_hash()`, `canonical_root_fen()`, `parse_pgn_played_at()`,
  `MAX_PASTED_PGN_PLIES`, `_PASTE_PLATFORM_VALUE`
- `tests/repositories/test_pasted_platform_exclusion.py`,
  `tests/services/test_normalize_pasted_game.py`
- New platform literal value: `'pgn'`

## Assumptions surfaced (never silently dropped)

The deterministic edge probe returned four `unclassified` rows, which stay **`unresolved`**
and are recorded here as explicit planner assumptions rather than auto-resolved:

1. **PASTE-01 (`unclassified`)** — the probe could not classify "bare FEN loads as a free-play
   root, writes nothing, does not touch the URL". Planner assumption: the observable form of
   this requirement is the three goal-backward truths authored in `208-01-PLAN.md`
   (position on board / zero network requests / `location.search` unchanged). No additional
   shape-axis edge is assumed to exist.
2. **PASTE-05 (`unclassified`)** — "pasted games never appear in any `apply_game_filters()`
   consumer". Planner assumption: the requirement's edge surface is exhaustively covered by
   the red-if-removed test plus the eligibility invariant test in this plan; no per-consumer
   enumeration is assumed necessary because `apply_game_filters` is the single seam.
3. **PASTE-06 (`unclassified`)** — "re-pasting reuses the existing row". Planner assumption:
   covered by the idempotency and concurrency truths authored in `208-03-PLAN.md`.
4. **PASTE-07 (`unclassified`)** — "saved-but-unanalyzed is marked eval-ineligible". Planner
   assumption, stated explicitly because it reinterprets the criterion: because D-04 + D-08
   make persistence and tier-1 enqueue happen in the same request, the
   "saved and never analyzed" state does not exist by construction. The executable form of
   PASTE-07 is therefore the invariant "every persisted `platform='pgn'` row has an eval job
   or already-complete evals", authored in `208-03-PLAN.md`. If a reviewer intended a
   literal "ineligible" marking (e.g. stamping `evals_completed_at` at insert), that is a
   different design and this assumption is where it should be challenged.

<tasks>

<task type="auto">
  <name>Task 1: Add 'pgn' to the one exclusion seam, with an opt-in escape and two proofs</name>
  <reversibility rating="costly">The literal string `'pgn'` becomes the `games.platform` value of every persisted row and the token every exclusion and opt-in filters on; changing it later needs an UPDATE over existing rows plus coordinated edits in the schema Literal, the exclusion tuple and the Library opt-in. No `checkpoint:decision` is inserted: the value is already chosen by the human in SEED-144 D-05, ROADMAP Phase 208 locked constraint 4, and CONTEXT.md D-05/D-11/D-12 — gating it would re-ask a door already walked through.</reversibility>
  <files>app/schemas/normalization.py, app/repositories/query_utils.py, tests/repositories/test_query_utils.py, tests/repositories/test_pasted_platform_exclusion.py</files>
  <read_first>
    - `app/repositories/query_utils.py` lines 1-60 — the module docstring, the ply-parity single-source block, and the `DEFAULT_EXCLUDED_PLATFORMS` block with its "this is the ONE central seam — do not scatter per-router platform checks" comment
    - `app/repositories/query_utils.py` lines 200-270 — the full `apply_game_filters` signature and body, especially the `if platform is not None: ... else: notin_(DEFAULT_EXCLUDED_PLATFORMS)` branch and its D-02 comment
    - `app/schemas/normalization.py` lines 1-60 — the `Platform`/`GameResult`/`Color`/`Termination`/`TimeControlBucket` Literals and the `NormalizedGame` field list
    - `tests/repositories/test_query_utils.py` — the existing `DEFAULT_EXCLUDED_PLATFORMS` assertions (roughly lines 20-60) and any existing `apply_game_filters`-level flawchess-exclusion integration test, whose fixture shape is the template for the new pgn test
    - `tests/conftest.py` — the per-run database fixture contract, so the new integration test uses the project's session fixtures rather than inventing its own
    - `app/services/library_service.py` around line 868 — read the `library_platform` substitution ONLY to confirm it is left untouched by this task (D-11 forbids `"pgn"` joining that default list; Plan 04 adds a conditional opt-in instead)
  </read_first>
  <action>
    In `app/schemas/normalization.py`, widen `Platform` to
    `Literal["chess.com", "lichess", "flawchess", "pgn"]`. Add a one-line comment on the
    Literal noting that `flawchess` and `pgn` are analytics-excluded values and that
    `query_utils.DEFAULT_EXCLUDED_PLATFORMS` is where that is decided.

    In `app/repositories/query_utils.py`:
    - Change `DEFAULT_EXCLUDED_PLATFORMS = ("flawchess",)` to `("flawchess", "pgn")` and extend
      the existing comment block with the D-05/D-11 rationale in the same voice as the Phase 167
      text already there: pasted games are frequently NOT the user's own (a broadcast game, a
      friend's PGN, a scouted opponent), so they must never reach an analytics population, and a
      pasted game of your own does not count either — import it from the platform you played it
      on. Keep the "ONE central seam — do not scatter per-router platform checks" sentence intact.
    - Add `ANALYTICS_INCLUDED_PLATFORMS: tuple[str, ...] = ("chess.com", "lichess")` directly
      beneath it, with a comment stating that every value of the `Platform` Literal must appear in
      exactly one of the two tuples and that the invariant test enforces it.
    - Do NOT add an opt-in parameter to `apply_game_filters`. The Library's "show pasted games"
      escape (D-11/D-12) is expressed by passing an explicit `platform` list, which the existing
      `if platform is not None` branch already honors; Plan 04 resolves that list once in
      `library_service`. Add a sentence to the `apply_game_filters` docstring recording that
      `ANALYTICS_INCLUDED_PLATFORMS` is the explicit-list equivalent of the `platform is None`
      default, and that the invariant test is what keeps the two readings equal — the `notin_`
      default admits any future platform value, while an explicit `in_` list excludes it, so the
      two only agree while every platform member has a declared disposition.

    In `tests/repositories/test_query_utils.py`, extend the existing constant assertions with
    `assert "pgn" in DEFAULT_EXCLUDED_PLATFORMS`, and add
    `test_every_platform_has_an_analytics_disposition`: read the `Platform` Literal's members with
    `typing.get_args`, assert the union of `ANALYTICS_INCLUDED_PLATFORMS` and
    `DEFAULT_EXCLUDED_PLATFORMS` equals that member set exactly, and assert the two tuples are
    disjoint. Give the test a docstring saying it goes red when a new platform value is added
    without an explicit eligibility decision, and naming the assumption-delta record in this plan.

    Create `tests/repositories/test_pasted_platform_exclusion.py` — the red-if-removed proof SC-5
    demands. Mirror the fixture shape of whichever existing test proves `flawchess` exclusion end
    to end. Insert, for one user, three `Game` rows: one `platform='chess.com'`, one
    `platform='flawchess'`, one `platform='pgn'`. Then:
    - Build a select through `apply_game_filters` with `platform=None` and assert only the
      chess.com row is returned. This is the assertion that goes red if `"pgn"` is dropped from
      the tuple.
    - Build the same select with an explicit `platform=list(ANALYTICS_INCLUDED_PLATFORMS)` and
      assert it returns exactly the same rows as the `platform=None` default. This pins the
      equivalence Plan 04's Library opt-in relies on.
    - Build the same select with an explicit `platform=[*ANALYTICS_INCLUDED_PLATFORMS, "pgn"]` and
      assert the chess.com and pgn rows are returned and the flawchess row is not.
    - Build the same select with an explicit `platform=["pgn"]` and assert only the pgn row is
      returned.
    Clean up every inserted non-guest `Game` row in a `finally` block — the tier-3 eval lottery is
    global and random, and a leaked needs-engine row flakes unrelated lottery tests.
  </action>
  <verify>
    <automated>uv run pytest tests/repositories/test_query_utils.py tests/repositories/test_pasted_platform_exclusion.py -x && uv run ty check app/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'DEFAULT_EXCLUDED_PLATFORMS = ("flawchess", "pgn")' app/repositories/query_utils.py` is 1
    - `grep -c '"pgn"' app/schemas/normalization.py` is at least 1 and the `Platform` Literal has 4 members
    - `grep -rn 'DEFAULT_EXCLUDED_PLATFORMS' app/ | grep -v 'query_utils.py' | wc -l` is 0 — the constant is defined and applied in one module only
    - `grep -rEc "platform.*(==|!=|in_|notin_).*['\"]pgn['\"]" app/routers/ app/repositories/ | grep -v ':0' | wc -l` is 0 — no per-router or per-repository pgn check was scattered
    - `uv run pytest tests/repositories/test_pasted_platform_exclusion.py -x` exits 0
    - Reverting the tuple to `("flawchess",)` and re-running `uv run pytest tests/repositories/test_pasted_platform_exclusion.py` FAILS; restore the tuple afterwards. Record the observed failure in the SUMMARY — a symbol-presence check does not satisfy this criterion.
    - `uv run pytest tests/repositories/test_query_utils.py::test_every_platform_has_an_analytics_disposition -x` exits 0
    - `uv run ty check app/ tests/` reports zero errors
  </acceptance_criteria>
  <done>`platform='pgn'` is excluded from every default analytics population through the single seam, an opt-in escape exists for the Library alone, and both the red-if-removed proof and the eligibility invariant test pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The D-16 identity hash, canonical root FEN, and PGN date parsing</name>
  <reversibility rating="costly">The hash input set is baked into every persisted row's identity. Changing it later corrupts nothing, but every previously-pasted game silently stops deduping against a re-paste and duplicates accumulate. Rating carried through verbatim from CONTEXT.md D-16, which records "costly" and delegates the canonicalization details to planning; no `checkpoint:decision` is inserted because the human explicitly delegated this decision rather than reserving it.</reversibility>
  <files>app/services/normalization.py, tests/services/test_normalize_pasted_game.py</files>
  <read_first>
    - `app/services/normalization.py` lines 560-713 — `normalize_flawchess_game` in full: the try/except parse split, the `game is None` and empty-`nodes` early returns, the `game.board().turn` WR-02 derivation, the CR-02 board-derived termination fallback, and the `NormalizedGame` construction
    - `app/services/normalization.py` — the module's import block, `_VALID_GAME_RESULTS`, `_clock_presence_by_color`, `_FLAWCHESS_TERMINATION_HEADER_MAP`, `parse_time_control`, `parse_base_and_increment`, `find_opening`, `_normalize_tc_str`
    - `app/schemas/normalization.py` — the full `NormalizedGame` field list and which fields are required versus defaulted
    - `app/models/game.py` lines 44-52 and the `platform_game_id` / `played_at` / `termination_raw` column declarations — `platform_game_id` is `String(100)`, the unique constraint is `(user_id, platform, platform_game_id)`, `played_at` is nullable
    - `.planning/phases/208-analysis-fen-pgn-paste/208-CONTEXT.md` § D-16 and § Claude's Discretion — the root-FEN canonicalization guidance
  </read_first>
  <behavior>
    - `canonical_root_fen("rnbqkbnr/pppppppp/8/8/8/8/PPPPPPPP/RNBQKBNR w KQkq - 0 1")` and the same
      position written with `e3` in the en-passant field and different counters produce the SAME string.
    - `canonical_root_fen` output contains exactly three space-separated fields.
    - `pasted_game_identity_hash` over the same movetext with wildly different header spellings
      (`"Noël, Studer"` vs `"IM Studer Noel 2438 (SUI)"`, different Event/Site/Date/Result) is identical.
    - `pasted_game_identity_hash` over the same SAN sequence from two different `[SetUp]` roots differs.
    - `pasted_game_identity_hash` returns a 64-character lowercase hex string.
    - `parse_pgn_played_at` on `[UTCDate "2024.03.15"] [UTCTime "18:22:01"]` yields a tz-aware UTC datetime
      for that instant; on `[Date "2024.03.15"]` alone yields midnight UTC on that date; on
      `[Date "????.??.??"]` or a missing header yields `None`.
  </behavior>
  <action>
    Add to `app/services/normalization.py`, in the same module-level style as its existing helpers:

    `MAX_PASTED_PGN_PLIES = 1000` — a named bound (no magic numbers) on the mainline length a pasted
    game may carry. Each ply becomes a `game_positions` row, so this is the write-amplification
    ceiling; 1000 plies is roughly twice the longest recorded serious game.

    `canonical_root_fen(fen: str) -> str` — split the FEN on whitespace and return the first THREE
    fields joined by single spaces: piece placement, side to move, castling rights. Document why the
    remaining fields are dropped: the halfmove clock and fullmove number are counters, not identity,
    and the en-passant field is inconsistently produced in the wild (some producers emit the ep square
    on every pawn double-step, others only when a capture is actually available), so two sources
    publishing the same `[SetUp]` position can emit different FEN strings for an identical position and
    would otherwise defeat D-16's dedup. State the accepted consequence: a root where an en-passant
    capture is genuinely available hashes identically to the same root without it. The FULL, unmodified
    FEN is still what the board and the stored PGN use — only the identity hash sees the canonical form.

    `pasted_game_identity_hash(root_fen: str, sans: Sequence[str]) -> str` — build the input string as
    `canonical_root_fen(root_fen)`, then `"|"`, then the SAN moves joined by single spaces, and return
    `hashlib.sha256(...).hexdigest()` over its UTF-8 encoding. Headers are deliberately excluded
    (D-16): the seed's corpus evidence records one player spelled twelve ways, so any
    header-inclusive hash means the same game re-pasted from a different source produces a second row,
    defeating the whole point. `user_color` is deliberately excluded too (D-18). Move numbers,
    annotation glyphs and comments never enter the input because the SAN list comes from
    python-chess's own `node.san()`, which is a function of the position. Document the accepted
    residual collision: two genuinely different short games with identical moves from identical roots
    map to one row. 64 hex characters fit `platform_game_id`'s `String(100)`, and the unique constraint
    is per-user, so two users pasting the same game never collide with each other.

    `parse_pgn_played_at(headers) -> datetime.datetime | None` — prefer `UTCDate` (+ `UTCTime` when
    present and parseable), else `Date`. Parse with the PGN `%Y.%m.%d` and `%H:%M:%S` formats. Return a
    timezone-aware UTC datetime, or `None` when the header is absent, empty, or carries the
    conventional unknown markers (any component containing `?`). Never fall back to the current time:
    a pasted historical game stamped with today's date sorts wrongly in the Library and misrepresents
    the game.

    Import `hashlib` at module level. Write `tests/services/test_normalize_pasted_game.py` covering
    every bullet of this task's `<behavior>` block, one test per bullet, as pure unit tests with no DB.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_normalize_pasted_game.py -x && uv run ruff check app/ tests/ && uv run ty check app/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - `app/services/normalization.py` contains `def canonical_root_fen(`, `def pasted_game_identity_hash(`, `def parse_pgn_played_at(` and `MAX_PASTED_PGN_PLIES = 1000`
    - `uv run python -c "from app.services.normalization import canonical_root_fen as c; assert c('rnbqkbnr/pppppppp/8/8/8/8/PPPPPPPP/RNBQKBNR w KQkq - 0 1') == c('rnbqkbnr/pppppppp/8/8/8/8/PPPPPPPP/RNBQKBNR w KQkq e3 7 42'), 'counters/ep must not affect identity'; print('ok')"` prints ok
    - `uv run python -c "from app.services.normalization import pasted_game_identity_hash as h; d=h('rnbqkbnr/pppppppp/8/8/8/8/PPPPPPPP/RNBQKBNR w KQkq - 0 1',['e4','e5']); assert len(d)==64 and d==d.lower() and all(ch in '0123456789abcdef' for ch in d); print('ok')"` prints ok
    - `tests/services/test_normalize_pasted_game.py` contains a test asserting two PGNs with identical movetext but different `White`/`Black`/`Event`/`Site`/`Date`/`Result` headers produce the same digest
    - `tests/services/test_normalize_pasted_game.py` contains a test asserting the same SAN sequence from two different `[SetUp]` roots produces different digests
    - `uv run pytest tests/services/test_normalize_pasted_game.py -x` exits 0
    - `uv run ruff check app/ tests/` and `uv run ty check app/ tests/` both report zero problems
  </acceptance_criteria>
  <done>The identity hash is deterministic, header-independent, root-sensitive, and unit-proven; canonical root FEN and PGN date parsing are separately tested pure functions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: normalize_pasted_game — the clock-gate-free normalization variant</name>
  <files>app/services/normalization.py, tests/services/test_normalize_pasted_game.py</files>
  <read_first>
    - `app/services/normalization.py` as extended by Task 2, plus `normalize_flawchess_game` lines 560-713 again if not still in context
    - `app/schemas/normalization.py` — `NormalizedGame`, `Color`, `GameResult`, `Termination`
    - `app/models/game.py` — the `result`, `user_color`, `termination`, `termination_raw`, `played_at`, `time_control_*`, `rated`, `is_computer_game` column declarations and their nullability
  </read_first>
  <behavior>
    - Given plain movetext with no clock data at all, `normalize_pasted_game` returns a `NormalizedGame`
      (the `[%clk]`-for-both-colors gate that `normalize_flawchess_game` enforces is absent).
    - The returned object has `platform="pgn"`, `platform_game_id` equal to
      `pasted_game_identity_hash(root_fen, sans)`, `platform_url=None`, `rated=False`,
      `is_computer_game=False`, and every time-control field `None`.
    - Given a PGN with `[Result "1-0"]`, `result` is `"1-0"`; given `[Result "*"]` with a final
      board that is checkmate, the result is derived from the board; given `[Result "*"]` with a
      non-terminal final board, the function returns `None`.
    - Given a `[Termination "some unrecognised 60-character string ..."]`, `termination_raw` is the
      short board-derived value, never the header string.
    - Given a `[SetUp "1"]`/`[FEN ...]` Black-to-move root, the mainline SAN and the hash are computed
      from that root, not from the standard start.
    - Given more than `MAX_PASTED_PGN_PLIES` mainline plies, returns `None`.
    - Given unparseable text or zero mainline moves, returns `None` and captures no Sentry event.
  </behavior>
  <action>
    Add `normalize_pasted_game(pgn_text: str, user_id: int, user_color: Color) ->
    NormalizedGame | None` to `app/services/normalization.py`, derived from
    `normalize_flawchess_game` with these deliberate divergences:

    - DROP the `[%clk]`-for-both-colors gate entirely — both the per-node clock-presence list and
      the clock-presence-by-color helper call that consumes it. A pasted PGN is untimed by
      definition (D-07), so no clock check applies. Keep the `game.board().turn` derivation as the source of the starting side to
      move even though the clock gate is gone — it is what makes a `[SetUp]` Black-to-move root
      correct (WR-02) and it feeds the root FEN used by the hash.
    - Set every time-control field to `None`: `time_control_str`, `time_control_bucket`,
      `time_control_seconds`, `base_time_seconds`, `increment_seconds`. Do not call
      `parse_time_control` or `parse_base_and_increment`. `rated=False`, `is_computer_game=False`
      (a pasted game is between two humans as far as we can tell; `flawchess` bot games are the
      only `is_computer_game=True` non-platform source).
    - Result: take the `[Result]` header when it is one of the `_VALID_GAME_RESULTS`. Otherwise
      (missing, `*`, or garbage) derive it from the final board — checkmate yields the winner's
      score, and stalemate / insufficient material / fifty-move / threefold yields the draw score.
      When neither path determines a result, return `None`; `games.result` is `NOT NULL` over a
      three-value enum and there is no honest value for an unfinished broadcast game. This is an
      expected validation outcome, not a bug: no Sentry capture.
    - Termination: always use the board-derived closed-vocabulary fallback and set
      `termination_raw` to that same value. Do NOT look up a `[Termination]` header map and never
      store an unrecognized header string verbatim — that is the CR-02 defect (a crafted header
      longer than 50 characters reached the INSERT and crashed the flush with an unhandled
      Postgres DataError instead of a validation failure), and arbitrary pasted PGN is exactly the
      untrusted source that reproduces it.
    - `platform_game_id`: the Task 2 hash over the root FEN and the mainline SAN list.
    - `played_at`: `parse_pgn_played_at(game.headers)`. Never fall back to the current wall-clock
      time the way the bot-game normalizer does — a pasted historical game must carry its own date.
    - Usernames: the `[White]`/`[Black]` headers, falling back to a named constant for an absent
      header (`NormalizedGame.white_username`/`black_username` are non-optional). Ratings: the
      `[WhiteElo]`/`[BlackElo]` headers parsed to int, `None` when absent or unparseable.
    - Opening: reuse `find_opening(pgn_text)` exactly as `normalize_flawchess_game` does.
    - Bound the mainline at `MAX_PASTED_PGN_PLIES` and return `None` above it.
    - Keep the existing try/except split verbatim in shape: a genuinely unexpected parse exception
      gets `sentry_sdk.set_context` plus `capture_exception` (never embedding variable data in the
      message string), while every expected validation outcome returns `None` with no capture.
      Use `set_context` with `user_id` and the ply count, not a formatted message.
    - `ply_count` and `result_fen` are NOT `NormalizedGame` fields — `_flush_batch`'s Stage 5
      re-parses the PGN and bulk-updates both columns plus the Zobrist `game_positions` rows for
      every newly inserted game, pasted games included. Add no code for them.

    Extend `tests/services/test_normalize_pasted_game.py` with one test per `<behavior>` bullet.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_normalize_pasted_game.py -x && uv run ruff check app/ tests/ && uv run ty check app/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - `app/services/normalization.py` contains `def normalize_pasted_game(`
    - `grep -n 'def normalize_pasted_game' -A 200 app/services/normalization.py | grep -c '_clock_presence_by_color'` is 0 — the clock gate is absent from the new function
    - `grep -n 'def normalize_pasted_game' -A 200 app/services/normalization.py | grep -c 'datetime.now'` is 0
    - `grep -n 'def normalize_pasted_game' -A 200 app/services/normalization.py | grep -c '_FLAWCHESS_TERMINATION_HEADER_MAP'` is 0 — no header-sourced termination string
    - `tests/services/test_normalize_pasted_game.py` contains a test asserting a clockless plain-movetext PGN normalizes to a non-None `NormalizedGame`
    - `tests/services/test_normalize_pasted_game.py` contains a test asserting a `[Termination]` header longer than 50 characters does not appear in `termination_raw` and that `len(result.termination_raw) <= 50`
    - `tests/services/test_normalize_pasted_game.py` contains a test asserting `[Result "*"]` with a non-terminal final board returns `None`
    - `tests/services/test_normalize_pasted_game.py` contains a test asserting a mainline of `MAX_PASTED_PGN_PLIES + 1` plies returns `None`
    - `uv run pytest tests/services/test_normalize_pasted_game.py -x` exits 0
    - `uv run ty check app/ tests/` reports zero errors
  </acceptance_criteria>
  <done>A clockless, untimed, arbitrary PGN normalizes into a `platform='pgn'` `NormalizedGame` with a deterministic identity, an honest result, a bounded termination string, and a header-sourced date — or returns `None` on every expected invalid input without a Sentry event.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request body → `normalize_pasted_game` | Arbitrary attacker-controlled PGN text reaches python-chess and, through it, the ORM. |
| PGN header strings → `games` columns | Header values flow into bounded columns (`termination_raw` is `String(50)`, `platform_game_id` is `String(100)`). |
| `apply_game_filters` → every analytics repository | A single missed exclusion silently pollutes every WDL, endgame and insight surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-208-05 | Tampering | `DEFAULT_EXCLUDED_PLATFORMS` bypass | high | mitigate | `tests/repositories/test_pasted_platform_exclusion.py` asserts through `apply_game_filters` (not against the constant) and is required to fail when `'pgn'` is removed; a zero-match grep proves no per-router platform check was scattered. |
| T-208-06 | Tampering | a future platform value silently inheriting analytics eligibility | high | mitigate | `test_every_platform_has_an_analytics_disposition` compares `typing.get_args(Platform)` against the union of the two disposition tuples and fails on any unassigned member. |
| T-208-07 | Denial of Service | unbounded mainline → one `game_positions` row per ply | high | mitigate | `MAX_PASTED_PGN_PLIES = 1000` rejects the game before normalization returns, so write amplification is bounded before `_flush_batch` ever runs. The request-schema byte bound added in Plan 03 is the outer bound. |
| T-208-08 | Denial of Service | crafted `[Termination]` header overflowing `String(50)` into an unhandled DataError | medium | mitigate | The board-derived closed-vocabulary fallback is used unconditionally; no header string is ever stored in `termination_raw`. Asserted by a length test on an over-long header fixture. |
| T-208-09 | Information Disclosure | Sentry noise / PII leakage from expected parse failures | low | mitigate | Expected validation outcomes return `None` with no capture. The one genuine-exception capture uses `set_context` with `user_id` and ply count, never a formatted message and never the PGN body. |
| T-208-SC | Tampering | npm/pip/cargo installs | high | accept | Gate does not fire — this plan installs no package. `hashlib` and `datetime` are stdlib; `python-chess` and `sentry-sdk` are existing dependencies. Recorded, not skipped. |
</threat_model>

<verification>
- `uv run pytest tests/repositories/test_query_utils.py tests/repositories/test_pasted_platform_exclusion.py tests/services/test_normalize_pasted_game.py -x` exits 0.
- `uv run ruff format app/ tests/ && uv run ruff check app/ tests/ --fix && uv run ty check app/ tests/` all clean.
- The red-if-removed check is performed by hand (revert the tuple, observe the failure, restore) and its output recorded in the SUMMARY.
</verification>

<success_criteria>
- PASTE-05 satisfied at the seam level with a proof that goes red if the exclusion is removed.
- `Platform` admits `'pgn'` and every member has an explicit analytics disposition enforced by a test.
- `normalize_pasted_game` produces a deterministic, untimed, honestly-terminated `NormalizedGame` from arbitrary PGN, or `None` without a Sentry event on every expected invalid input.
</success_criteria>

<output>
Create `.planning/phases/208-analysis-fen-pgn-paste/208-02-SUMMARY.md` when done.
</output>
