---
phase: 216-audit-bugs-and-quick-wins
plan: 02
type: execute
wave: 2
depends_on: ["216-01"]
files_modified:
  - deploy/Caddyfile
  - .github/workflows/ci.yml
autonomous: false
requirements: []
user_setup:
  - service: sentry
    why: "CSP violation reports need the project's security-header endpoint, which embeds a DSN public key that exists nowhere in this repo (RESEARCH.md open question 2)"
    env_vars: []
    dashboard_config:
      - task: "Copy the security-header report URL (form: https://<org-ingest-host>/api/4511084868272208/security/?sentry_key=<public-key>)"
        location: "Sentry -> Settings -> Projects -> flawchess -> Client Keys (DSN) (or the Security Headers settings page), region de.sentry.io"

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SC-2: the flawchess.com site block sets HSTS (max-age 31536000, includeSubDomains, no preload), X-Content-Type-Options, Referrer-Policy, Permissions-Policy and Content-Security-Policy-Report-Only on every response, including /api/* and the SPA fallback."
    - "SC-2: the CSP is report-only and reports to the Sentry security endpoint via both report-uri and a report-to group backed by a Reporting-Endpoints header (D-07)."
    - "SC-2: no COOP or COEP header is added anywhere, and the existing CI guard that forbids them still passes (D-08)."
    - "SC-2: .github/workflows/ci.yml runs caddy validate on deploy/Caddyfile so a syntax error fails before merge, and the deploy job's health step asserts the five headers on flawchess.com."
    - "Spec-less probe fallback: skipped visibly — this phase has no requirement IDs and no SPEC.md/UI-SPEC.md/AI-SPEC.md, so no probe predicates were generated."
  artifacts:
    - deploy/Caddyfile
    - .github/workflows/ci.yml
  key_links:
    - "site-level unconditioned header block -> Caddy default directive order runs header before handle -> headers land on @static, /api/* and the SPA fallback alike"
    - "CSP report-uri / Reporting-Endpoints -> Sentry security endpoint -> violations become Sentry issues"
    - "deploy job Health check step -> curl -I https://flawchess.com/ -> per-header grep assertions"
---

<objective>
Fix F-02 / SEED-161 group 2: flawchess.com currently sends no HSTS, CSP,
`X-Content-Type-Options`, `Referrer-Policy` or `Permissions-Policy`. Ship all five from a
single unconditioned `header` block on the site (D-06, D-07), keep COOP/COEP out (D-08),
and assert the result in CI two ways (D-09).

Purpose: close the highest-severity remaining edge-config gap from the 2026-09-04 audit
without risking a day-one breakage, by shipping the CSP report-only.
Output: an edited `deploy/Caddyfile` site block and two `.github/workflows/ci.yml` changes.

Spec-less probe fallback is skipped visibly: this phase carries no requirement IDs and no
SPEC/UI-SPEC/AI-SPEC exists, so no probe predicates were generated this run.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/216-audit-bugs-and-quick-wins/216-CONTEXT.md
@.planning/phases/216-audit-bugs-and-quick-wins/216-RESEARCH.md
@.planning/phases/216-audit-bugs-and-quick-wins/216-PATTERNS.md
@.planning/phases/216-audit-bugs-and-quick-wins/216-01-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this plan produces

| Kind | Name | Notes |
|------|------|-------|
| New Caddyfile block | unconditioned site-level `header { }` inside `flawchess.com { }` | 5 security headers + `Reporting-Endpoints` |
| New CI step | `Caddyfile validate` (test job) | runs the pinned `caddy:2.11.4` image against `deploy/Caddyfile` |
| Changed CI step | `Health check` (deploy job) | loop now `break`s on success, then asserts the five headers with `curl -I` |
| New CSP reporting group name | `csp-endpoint` | referenced by the CSP `report-to` directive and defined by `Reporting-Endpoints` |

<!-- planner-discipline-allow: preload -->

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Obtain the Sentry security-report endpoint URL</name>
  <read_first>
    - `.planning/phases/216-audit-bugs-and-quick-wins/216-RESEARCH.md` "Pattern 4: Sentry security report endpoint" — the URL form and why it cannot be derived from the repo
    - `CLAUDE.md` "Error Handling & Sentry" — org `flawchess`, project id `4511084868272208`, region de.sentry.io
  </read_first>
  <action>Paste the Sentry security-header report URL for the flawchess project</action>
  <instructions>
    I cannot obtain this value myself. The CSP needs a reporting endpoint whose DSN public
    key and numeric org-ingest host exist nowhere in this repository: they live only inside
    `VITE_SENTRY_DSN` / `SENTRY_DSN`, which are injected at build and run time. Guessing or
    hand-deriving the numeric org ID would produce a silently dead endpoint.

    Open Sentry (region de.sentry.io), go to Settings -> Projects -> flawchess -> Client
    Keys (DSN), or the project's Security Headers settings page, and paste the
    security-report URL verbatim. It has the form
    `https://<org-ingest-host>/api/4511084868272208/security/?sentry_key=<public-key>`.

    One note before you paste: this value gets committed to a public repository. That is
    expected and safe. The same public key already ships inside the frontend bundle as part
    of `VITE_SENTRY_DSN`; a DSN public key is an ingest-only credential and grants no read
    access to the Sentry project.
  </instructions>
  <verification>The pasted string's path segment is `/api/4511084868272208/security/` and it carries a `sentry_key=` query parameter; the executor stores it for Task 2 and records in the SUMMARY that it came from the Sentry UI, not from repo contents.</verification>
  <resume-signal>Paste the URL, or reply "skip" to halt the plan (Task 2 cannot write a complete CSP without it)</resume-signal>
  <acceptance_criteria>
    - A URL string is supplied whose path segment is `/api/4511084868272208/security/` and which carries a `sentry_key=` query parameter.
    - The plan SUMMARY records that the value was pasted from the Sentry Client Keys page.
    - No attempt was made to reconstruct the value from repo contents or from an env file.
  </acceptance_criteria>
  <done>The exact report endpoint is in hand, so Task 2 can write a complete CSP rather than a stub.</done>
</task>

<task type="tracer">
  <name>Task 2: Site-level security header block — one response path, all five headers</name>
  <files>deploy/Caddyfile</files>
  <read_first>
    - `deploy/Caddyfile` lines 29-75 — the `flawchess.com { }` site body: `encode gzip`, the access-log rationale paragraph, and the site `log` block. The new block goes unconditioned in this body, before the `log` block.
    - `deploy/Caddyfile` lines 76-131 — the `handle @static` block, to confirm the new block is NOT nested there (nesting it would silently narrow the fix to static assets, RESEARCH.md Pitfall 2)
    - `.planning/phases/216-audit-bugs-and-quick-wins/216-RESEARCH.md` "Pattern 2" (directive ordering) and "Pattern 3" (the full per-directive CSP derivation table with evidence)
    - `frontend/index.html` — confirm the Umami script origin and the Google Fonts link/preconnect origins still match the CSP table before writing it
  </read_first>
  <action>
    Add one unconditioned `header { }` block directly in the `flawchess.com { }` site body
    (not inside any `handle`), placed after `encode gzip` and before the access-log comment
    paragraph. Precede it with a comment paragraph in this file's documented-rationale style
    naming SEED-161 group 2 / F-02, stating that the block is unconditioned on purpose
    because Caddy's default directive order runs `header` before every `handle`, so one
    block covers static assets, `/api/*` and the SPA fallback alike.

    Set exactly these fields (D-06, D-07):
    - `Strict-Transport-Security "max-age=31536000; includeSubDomains"` — one year, subdomains included, no preload token. Add a one-line comment that analytics.flawchess.com is on this same Caddy and already HTTPS.
    - `X-Content-Type-Options nosniff`
    - `Referrer-Policy strict-origin-when-cross-origin`
    - `Permissions-Policy "camera=(), microphone=(), geolocation=()"`
    - `Content-Security-Policy-Report-Only` with the policy assembled from RESEARCH.md Pattern 3's directive table: `default-src 'self'`; `script-src 'self' https://analytics.flawchess.com 'wasm-unsafe-eval'`; `style-src 'self' 'unsafe-inline' https://fonts.googleapis.com`; `font-src 'self' https://fonts.gstatic.com`; `img-src 'self' data: blob:`; `connect-src 'self' https://*.ingest.de.sentry.io https://analytics.flawchess.com blob:`; `worker-src 'self' blob:`; `frame-ancestors 'none'`; `base-uri 'self'`; `form-action 'self'`; then `report-uri <the URL from Task 1>` and `report-to csp-endpoint`.
    - `Reporting-Endpoints` set to `csp-endpoint="<the same URL from Task 1>"`, which is what makes the `report-to` directive resolvable (D-07 asks for both mechanisms).

    Ship report-only only: do not add a `Content-Security-Policy` enforcing header. Do not
    add COOP or COEP in any form; D-08 forbids both and an existing CI guard fails the build
    if either appears.

    Do not touch the existing `header @nocache` / `header @immutable` /
    `header @vendored_runtime` Cache-Control lines: different field names, no conflict, and
    they live inside `handle @static` where they belong.

    Do not run `caddy fmt`; this file is deliberately 4-space indented.
  </action>
  <verify>
    <automated>docker run --rm -v "$PWD/deploy/Caddyfile:/etc/caddy/Caddyfile:ro" caddy:2.11.4 caddy validate --config /etc/caddy/Caddyfile --adapter caddyfile</automated>
    <fails_when>non-zero exit, or the output does not end with the line "Valid configuration"</fails_when>
    <automated>docker run --rm -v "$PWD/deploy/Caddyfile:/etc/caddy/Caddyfile:ro" caddy:2.11.4 caddy adapt --config /etc/caddy/Caddyfile --adapter caddyfile > /tmp/216-02-adapt.json; for h in Strict-Transport-Security X-Content-Type-Options Referrer-Policy Permissions-Policy Content-Security-Policy-Report-Only Reporting-Endpoints; do printf '%s=' "$h"; grep -c "$h" /tmp/216-02-adapt.json; done</automated>
    <fails_when>any of the six lines prints a count of 0, or the adapt command exits non-zero</fails_when>
    <automated>grep -ci 'cross-origin-opener-policy\|cross-origin-embedder-policy' /tmp/216-02-adapt.json</automated>
    <fails_when>prints anything other than 0 (D-08 forbids COOP/COEP)</fails_when>
    <automated>grep -c 'sentry_key=' deploy/Caddyfile</automated>
    <fails_when>prints 0 (the CSP carries no real report endpoint) or prints more than 2 (the URL was pasted into more places than the report-uri directive and the Reporting-Endpoints header)</fails_when>
  </verify>
  <acceptance_criteria>
    - `caddy validate` exits 0 with "Valid configuration".
    - The adapted JSON contains all six header field names listed above.
    - The adapted JSON contains zero case-insensitive matches for `cross-origin-opener-policy` or `cross-origin-embedder-policy`.
    - `grep -c 'Content-Security-Policy "' deploy/Caddyfile` prints 0, i.e. only the report-only variant is set.
    - `grep -c 'preload' deploy/Caddyfile` prints 0 (D-06 ships HSTS without the preload token).
    - The new `header` block is a direct child of the `flawchess.com` site body: `awk` or manual inspection confirms it is not inside `handle @static`, `handle /api/*` or the SPA-fallback `handle`.
    - `git diff deploy/Caddyfile` shows no change to any `Cache-Control` line.
  </acceptance_criteria>
  <reversibility rating="costly">HSTS `includeSubDomains` with a one-year max-age is cached by browsers for up to a year, so a future plain-HTTP subdomain would be unreachable until expiry. Flagged only, not gated: D-06 already decided this and analytics.flawchess.com is already HTTPS on the same Caddy.</reversibility>
  <done>Every response the site emits carries the five headers, the CSP reports to Sentry and enforces nothing, and the config still validates.</done>
</task>

<task type="auto">
  <name>Task 3: CI assertions — caddy validate pre-merge, header assertion post-deploy</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - `.github/workflows/ci.yml` lines 1-12 — the `on:` triggers. This workflow runs on pull_request to main/production and on workflow_dispatch only, so it does NOT run on this feature branch; both steps below are verified locally by running their commands, not by watching a CI run.
    - `.github/workflows/ci.yml` lines 82-100 — the `Lint (ruff)` / `Format check (ruff)` / `Type check (ty)` steps, the run-and-fail-on-nonzero-exit convention the new validate step follows
    - `.github/workflows/ci.yml` lines 152-183 — the `No COOP/COEP header guard + WASM MIME check` step. Leave it exactly as is; reuse its `curl -sf -I ... | grep -qi <name>` idiom for the new assertion.
    - `.github/workflows/ci.yml` lines 239-252 — the `Health check` step. Note the loop currently calls `exit 0` on the first successful curl, which would skip anything appended after it.
  </read_first>
  <action>
    Change 1, test job: add a step named `Caddyfile validate`, placed immediately after
    `Type check (ty, analysis project)`, whose `run:` is the same docker invocation this
    plan verifies with locally: `docker run --rm -v "$PWD/deploy/Caddyfile:/etc/caddy/Caddyfile:ro" caddy:2.11.4 caddy validate --config /etc/caddy/Caddyfile --adapter caddyfile`.
    The tool's own exit code gates the job, so no extra assertion wrapper is needed. Do not
    add a `caddy fmt --check` variant: this Caddyfile is deliberately 4-space indented and
    Caddy already reports it as unformatted, so a fmt gate would fail on day one for
    reasons unrelated to this phase.

    Change 2, deploy job: extend the existing `Health check` step (D-09b). Replace the
    in-loop `exit 0` with `break` so control reaches the code after the loop, keep the
    post-loop failure path exiting 1 when all 36 attempts are exhausted, then append a
    header assertion: capture `curl -sf -I https://flawchess.com/` once into a variable,
    echo it for the build log, and for each of the five header names
    (`strict-transport-security`, `x-content-type-options`, `referrer-policy`,
    `permissions-policy`, `content-security-policy-report-only`) grep the captured output
    case-insensitively, print a PASS/FAIL line per header, and exit 1 if any is missing.
    Use a `FAIL=0` accumulator so one run reports every missing header rather than only the
    first, matching the style of the existing COOP/COEP guard step.

    Do not touch the `No COOP/COEP header guard + WASM MIME check` step, and do not move the
    header assertion into it: that step curls the local vite preview server, which cannot
    see Caddy-set headers.
  </action>
  <verify>
    <automated>uv run python -c "import yaml; d=yaml.safe_load(open('.github/workflows/ci.yml')); names=[s.get('name') for j in d['jobs'].values() for s in j['steps']]; assert 'Caddyfile validate' in names, names; assert 'No COOP/COEP header guard + WASM MIME check' in names, names; print('steps ok')"</automated>
    <fails_when>non-zero exit, an AssertionError traceback, or the absence of the line "steps ok" (the YAML no longer parses, or a required step name is missing)</fails_when>
    <automated>docker run --rm -v "$PWD/deploy/Caddyfile:/etc/caddy/Caddyfile:ro" caddy:2.11.4 caddy validate --config /etc/caddy/Caddyfile --adapter caddyfile</automated>
    <fails_when>non-zero exit, or the output does not end with "Valid configuration" (the exact command the new CI step will run does not pass locally)</fails_when>
    <automated>HDRS=$(printf 'HTTP/2 200\nstrict-transport-security: max-age=31536000; includeSubDomains\nx-content-type-options: nosniff\nreferrer-policy: strict-origin-when-cross-origin\npermissions-policy: camera=(), microphone=(), geolocation=()\ncontent-security-policy-report-only: default-src self\n'); F=0; for h in strict-transport-security x-content-type-options referrer-policy permissions-policy content-security-policy-report-only; do echo "$HDRS" | grep -qi "$h" || F=1; done; echo "present-case=$F"; HDRS2=$(echo "$HDRS" | grep -vi referrer-policy); F2=0; for h in strict-transport-security x-content-type-options referrer-policy permissions-policy content-security-policy-report-only; do echo "$HDRS2" | grep -qi "$h" || F2=1; done; echo "missing-case=$F2"</automated>
    <fails_when>the output is not exactly the two lines "present-case=0" and "missing-case=1" (the grep patterns the CI step uses either miss a header that is present or fail to notice one that is absent)</fails_when>
    <automated>sed -n '/name: Health check/,/^      - name:/p' .github/workflows/ci.yml | grep -c 'exit 0'</automated>
    <fails_when>prints anything other than 0 (the loop still short-circuits before the header assertion can run)</fails_when>
    <human-check>
      Post-deploy UAT for SC-2, collected at end of phase rather than executed here.
      Caddy-set headers are only observable against flawchess.com, and this phase ends at
      squash-merge to `main`.

      After the next `/deploy`: `curl -I https://flawchess.com/` must show
      `strict-transport-security`, `x-content-type-options`, `referrer-policy`,
      `permissions-policy` and `content-security-policy-report-only`, and must NOT show a
      COOP or COEP header. Repeat against `https://flawchess.com/api/health` to confirm the
      unconditioned block really covers the API path and not just the SPA shell.

      Then watch Sentry for incoming CSP violation reports over the following days. The
      policy is report-only, so a violation is a signal to tighten or widen a directive,
      never an outage.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` parses as YAML and contains a step named `Caddyfile validate` in the test job.
    - The `Caddyfile validate` step's command, run locally, exits 0 against the committed `deploy/Caddyfile`.
    - The `Health check` step body contains no `exit 0`; success leaves the loop with `break`.
    - The `Health check` step greps all five header names case-insensitively and exits 1 when any is absent.
    - The step named `No COOP/COEP header guard + WASM MIME check` is unchanged: `git diff` shows no lines touched inside it.
    - No step invokes `caddy fmt`.
  </acceptance_criteria>
  <done>A malformed Caddyfile fails before merge, and a deploy that silently loses a security header fails the release.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> flawchess.com | Untrusted document context; CSP and the four static headers are the only client-side controls the edge can impose |
| Browser -> Sentry ingest | CSP violation reports leave the browser directly to a third-party ingest host |
| Public git repo -> reader | The Caddyfile is public, so anything written into it is public |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-216-02 | Tampering | HSTS scope on `flawchess.com` | medium | accept | `includeSubDomains` with a one-year max-age is browser-cached and would make a future plain-HTTP subdomain unreachable until expiry. Accepted per D-06: analytics.flawchess.com is the only subdomain, is on the same Caddy, and is already HTTPS. The `preload` token is deliberately omitted so the entry is not baked into browser preload lists. |
| T-216-02B | Information disclosure | CSP `report-uri` / `Reporting-Endpoints` value in a public repo | low | accept | A Sentry DSN public key is an ingest-only credential and already ships inside the frontend bundle via `VITE_SENTRY_DSN`. Committing it grants no read access. Task 1 states this explicitly so the human checkpoint is not mistaken for a secret leak. |
| T-216-02C | Denial of service (self-inflicted) | CSP breaking the Maia/Stockfish workers or Umami | high | mitigate | The policy ships as `Content-Security-Policy-Report-Only` with no enforcing counterpart, so a wrong directive produces a Sentry report instead of a broken page. Directives are derived from RESEARCH.md Pattern 3's evidence table (`wasm-unsafe-eval`, `worker-src blob:`, the Umami and Google Fonts origins), not guessed. |
| T-216-02D | Elevation of privilege | camera / microphone / geolocation access by injected script | medium | mitigate | `Permissions-Policy "camera=(), microphone=(), geolocation=()"` denies all three at the edge for every response. |
| T-216-02E | Tampering | clickjacking of flawchess.com | medium | mitigate | `frame-ancestors 'none'` in the CSP. Report-only for now, so this is a detection control this phase and an enforcement control when the CSP flips (explicitly deferred in CONTEXT.md). |
| T-216-02F | Spoofing | Google OAuth breakage from an over-broad policy | low | accept | Verified in RESEARCH.md: the OAuth handoff is a top-level `window.location.href` navigation, which CSP does not govern, and D-08 keeps COOP/COEP out precisely because they break it. The existing CI guard remains and still fails the build if either header appears. |
</threat_model>

<verification>
- `caddy validate` exits 0; `caddy adapt` output contains all six header field names and zero COOP/COEP matches.
- `.github/workflows/ci.yml` parses; the `Caddyfile validate` step's command passes locally; the `Health check` step no longer short-circuits.
- The header-grep patterns are exercised locally in both directions (all present, one missing).
- Post-deploy (out of phase, recorded in Task 4): `curl -I` against `/` and `/api/health`.
</verification>

<success_criteria>
SC-2 is met when the site block sets all five headers unconditionally, the CSP is
report-only and points at the real Sentry endpoint by both mechanisms, no COOP/COEP header
exists and the existing guard step is untouched, CI validates the Caddyfile pre-merge, the
deploy job asserts the headers, and the post-deploy check is recorded.
</success_criteria>

<output>
Create `.planning/phases/216-audit-bugs-and-quick-wins/216-02-SUMMARY.md` when done.
</output>
