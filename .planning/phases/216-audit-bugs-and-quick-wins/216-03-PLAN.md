---
phase: 216-audit-bugs-and-quick-wins
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - uv.lock
  - frontend/package-lock.json
  - frontend/package.json
autonomous: false
requirements: []
user_setup:
  - service: github
    why: "Renovate has never run because the GitHub App was never installed on flawchess/flawchess (D-01); renovate.json itself is already correct"
    env_vars: []
    dashboard_config:
      - task: "Install the Renovate GitHub App on the flawchess/flawchess repository and confirm the Dependency Dashboard issue opens"
        location: "github.com/apps/renovate -> Configure -> flawchess/flawchess"

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "SC-3: the Python lockfile is caught up within existing semver ranges and the locked anthropic and complexipy versions have NOT crossed a major boundary (D-04 executed without violating D-05)."
    - "SC-3: the frontend lockfile is caught up within existing ranges, and typescript, vitest, @vitest/coverage-v8, @vitest/ui, jsdom, @types/node and onnxruntime-web stay on their current majors."
    - "SC-3: the full CLAUDE.md pre-merge gate plus a frontend production build pass on the caught-up dependency set."
    - "SC-3: renovate.json is unchanged, no .github/dependabot.yml for version updates is added, the README renovate badge stays, and the Renovate GitHub App is installed by the user (HUMAN-UAT) after the catch-up lands."
    - "Spec-less probe fallback: skipped visibly — this phase has no requirement IDs and no SPEC.md/UI-SPEC.md/AI-SPEC.md, so no probe predicates were generated."
  artifacts:
    - uv.lock
    - frontend/package-lock.json
  key_links:
    - "uv lock --upgrade-package <name> (scoped) -> uv.lock -> uv sync --locked --group maia-inference -> backend test suite"
    - "npm update -> frontend/package-lock.json -> npm run build (tsc -b + vite) -> npm test"
    - "catch-up landed first -> Renovate App installed second -> Renovate opens PRs only for the remaining majors, not a 27-package backlog"
---

<objective>
Fix F-03 / SEED-161 group 3: `renovate.json` landed 2026-04-20 but the GitHub App was never
installed, so no version-update automation has ever run and the dependency set has drifted.
Land the in-range catch-up first (D-04), then install the app (D-01), so Renovate starts
from a current baseline and its first PR batch is only the deliberately-deferred majors.

Purpose: restore working dependency automation without taking any major-version bump in
this phase (D-05).
Output: an updated `uv.lock` and `frontend/package-lock.json`, a green full gate, and the
Renovate App installed on `flawchess/flawchess`.

Spec-less probe fallback is skipped visibly: this phase carries no requirement IDs and no
SPEC/UI-SPEC/AI-SPEC exists, so no probe predicates were generated this run.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/216-audit-bugs-and-quick-wins/216-CONTEXT.md
@.planning/phases/216-audit-bugs-and-quick-wins/216-RESEARCH.md
@CLAUDE.md
</context>

## Artifacts this plan produces

| Kind | Name | Notes |
|------|------|-------|
| Regenerated lockfile | `uv.lock` | scoped per-package upgrades only |
| Regenerated lockfile | `frontend/package-lock.json` | `npm update`, ranges unchanged |
| Recorded baseline | locked versions of `anthropic` and `complexipy` before and after | goes in the plan SUMMARY as the D-05 audit trail |
| Recorded baseline | the excluded-major list actually observed at run time | `npm outdated` + `uv lock --upgrade --dry-run` output, in the SUMMARY |
| External state | Renovate GitHub App installed on `flawchess/flawchess` | plus the Dependency Dashboard issue it opens |

<!-- planner-discipline-allow: .github/dependabot.yml -->

<tasks>

<task type="tracer">
  <name>Task 1: Scoped Python catch-up — prove the mechanism on one package, then the rest</name>
  <files>uv.lock</files>
  <read_first>
    - `.planning/phases/216-audit-bugs-and-quick-wins/216-RESEARCH.md` "Pitfall 1: Bare `uv lock --upgrade` silently violates D-05" and the full in-range diff captured there
    - `.planning/phases/216-audit-bugs-and-quick-wins/216-RESEARCH.md` "Assumptions Log" row A3 — a scoped upgrade can still cascade transitively, so the exclusion check runs after every lock, not once at the end
    - `pyproject.toml` — `pydantic-ai-slim[anthropic,google]>=2.31,<3.0` (which is why `anthropic` has no direct pin) and `complexipy>=7.0.1` (unbounded above)
    - `CLAUDE.md` "Commands" — `uv sync` installs from the lockfile; the maia-inference group is a separate `--group`
  </read_first>
  <action>
    Record the baseline first: capture the currently locked versions of `anthropic` and
    `complexipy` from `uv.lock` and write both into the plan SUMMARY. These two are the
    D-05 tripwires for the whole task.

    Then take the thin slice end to end before touching anything else: pick one clearly safe
    package from the dry-run diff (`sqlalchemy` is a good choice, it is a direct dependency
    with real test coverage), run `uv lock --upgrade-package sqlalchemy`, re-read the two
    tripwire versions, run `uv sync --locked --group maia-inference`, and run one fast test
    file. This proves the scoped-upgrade mechanism, the lockfile round-trip and the
    exclusion check all work before you widen the blast radius.

    Then expand: regenerate the candidate list at run time with `uv lock --upgrade --dry-run`
    (do not trust RESEARCH.md's captured list, transitive resolutions shift), remove
    `anthropic` and `complexipy` from it, and issue a single `uv lock` invocation carrying
    one `--upgrade-package <name>` flag per remaining package. Never run a bare
    `uv lock --upgrade`.

    After the lock, re-read the tripwire versions from `uv.lock` and halt immediately if
    either crossed a major boundary; if a transitive cascade moved one of them, drop the
    package that pulled it from the upgrade set and re-lock rather than hand-editing
    `uv.lock`.

    Finish with `uv sync --locked --group maia-inference`.
  </action>
  <verify>
    <automated>uv run python -c "import re,sys; t=open('uv.lock').read(); f=lambda n: re.search(r'name = \"%s\"\nversion = \"([^\"]+)\"' % n, t).group(1); a=f('anthropic'); c=f('complexipy'); print('anthropic',a,'complexipy',c); sys.exit(0 if a.startswith('0.') and c.startswith('7.') else 1)"</automated>
    <fails_when>non-zero exit, or the printed anthropic version does not start with "0." or the printed complexipy version does not start with "7." (a D-05 major was taken)</fails_when>
    <automated>uv sync --locked --group maia-inference</automated>
    <fails_when>non-zero exit, or the output reports that the lockfile is out of date with pyproject.toml</fails_when>
    <automated>uv run pytest tests/test_health.py tests/test_guest_auth.py -x -q 2>&1 | tail -5</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored test. If tests/test_health.py does not exist yet (plan 216-05 owns it), run tests/test_guest_auth.py alone and treat a missing-file error on the other path as expected.</fails_when>
    <automated>uv run ty check app/ tests/ scripts/</automated>
    <fails_when>non-zero exit, or the output reports one or more diagnostics</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv.lock` has changed and `git diff --stat uv.lock` is non-empty.
    - The locked `anthropic` version still starts with `0.` and the locked `complexipy` version still starts with `7.`.
    - No bare `uv lock --upgrade` was run: the shell history for this task shows only `--upgrade-package`-scoped invocations and `--dry-run` probes.
    - `uv sync --locked --group maia-inference` succeeds against the new lockfile.
    - `uv run ty check app/ tests/ scripts/` reports zero diagnostics.
    - The plan SUMMARY records the before and after versions of both tripwire packages.
  </acceptance_criteria>
  <done>The Python dependency set is current within its declared ranges, with the two excluded majors provably untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend in-range catch-up</name>
  <files>frontend/package-lock.json, frontend/package.json</files>
  <read_first>
    - `.planning/phases/216-audit-bugs-and-quick-wins/216-RESEARCH.md` "npm side is clean by contrast" — the `npm outdated` Wanted column already excludes every D-05 npm major
    - `frontend/package.json` — the declared ranges, to confirm which packages are exact-pinned (`onnxruntime-web`) versus caret-ranged
    - `CLAUDE.md` "Commands" -> Frontend, and the pre-merge gate block
    - `frontend/CLAUDE.md` — frontend-scoped rules that apply to any resulting code change
  </read_first>
  <action>
    Capture `npm outdated` output from `frontend/` into the plan SUMMARY first: the Wanted
    column is the D-05 evidence that `npm update` cannot cross a major.

    Run `npm update` in `frontend/`. Then inspect `git diff frontend/package.json`: if npm
    rewrote any declared range, verify by hand that each rewritten range still caps below
    the D-05 majors (typescript 7, vitest 5, jsdom 30, @types/node 26, onnxruntime-web 1.29)
    and revert any range that does not.

    `onnxruntime-web` must stay exactly where it is regardless of what npm suggests: the
    package is also vendored under `frontend/public/maia/`, so bumping the npm entry alone
    would desynchronize the two copies.

    Then run the frontend gate: `npm run lint`, `npm run build` (this is the step that
    actually type-checks, since vitest strips types and never will), and `npm test -- --run`.
  </action>
  <verify>
    <automated>cd frontend && npm run lint</automated>
    <fails_when>non-zero exit, or eslint reports any error (warnings within the existing baseline are acceptable, new errors are not)</fails_when>
    <automated>cd frontend && npm run build</automated>
    <fails_when>non-zero exit, or the output contains a TypeScript diagnostic line matching "error TS"</fails_when>
    <automated>cd frontend && npm test -- --run 2>&1 | tail -8</automated>
    <fails_when>non-zero exit, or the vitest summary reports any failed test file or failed test</fails_when>
    <automated>cd frontend && node -e "const p=require('./package.json'); const d={...p.dependencies,...p.devDependencies}; console.log(JSON.stringify({ts:d.typescript,vitest:d.vitest,jsdom:d.jsdom,types_node:d['@types/node'],ort:d['onnxruntime-web']}))"</automated>
    <fails_when>the printed typescript range allows 7.x, vitest allows 5.x, jsdom allows 30.x, @types/node allows 26.x, or the onnxruntime-web value differs from its pre-task value (any of these means a D-05 major was taken)</fails_when>
  </verify>
  <acceptance_criteria>
    - `frontend/package-lock.json` has changed; `git diff --stat` is non-empty.
    - The declared ranges for `typescript`, `vitest`, `@vitest/coverage-v8`, `@vitest/ui`, `jsdom` and `@types/node` still cap below their next major.
    - `onnxruntime-web`'s entry in `frontend/package.json` is byte-identical to its pre-task value.
    - `npm run lint`, `npm run build` and `npm test -- --run` all pass.
    - The plan SUMMARY contains the `npm outdated` table captured before the update.
  </acceptance_criteria>
  <done>The frontend dependency set is current within its declared ranges and the app still type-checks, lints and tests clean.</done>
</task>

<task type="auto">
  <name>Task 3: Full pre-merge gate on the caught-up dependency set</name>
  <files>uv.lock, frontend/package-lock.json</files>
  <read_first>
    - `CLAUDE.md` "Pre-merge gate (MANDATORY before squash-merging to `main`)" — the exact six commands and the rule that a modifying step gets a `style(...)`/`chore(...)` commit
    - `.planning/phases/216-audit-bugs-and-quick-wins/216-CONTEXT.md` D-04 — the gate must include `npm run build`, since `npm test` does not type-check
  </read_first>
  <action>
    Run the full CLAUDE.md pre-merge gate against the caught-up dependency set, in order:
    `uv run ruff format app/ tests/ scripts/ analysis/`, `uv run ruff check . --fix`,
    `uv run ty check app/ tests/ scripts/`,
    `uv run --project analysis --with ty ty check analysis/`, `uv run pytest -n auto -x`,
    and `( cd frontend && npm run lint && npm test -- --run )`, plus
    `( cd frontend && npm run build )` per D-04.

    A dependency bump is exactly the situation the gate exists for: a new `ruff` version can
    reformat files and a new `ty` version can surface new diagnostics. If a step modifies
    files, commit those separately with a `style(...)` or `chore(...)` prefix as CLAUDE.md
    requires, and do not fold them into the lockfile commit.

    Note in the SUMMARY any new lint or type diagnostic introduced purely by a tool version
    bump, with how it was resolved. Do not silence a new diagnostic with an ignore comment
    without saying so explicitly.
  </action>
  <verify>
    <automated>uv run ruff format --check app/ tests/ scripts/ analysis/</automated>
    <fails_when>non-zero exit, or the output names one or more files that would be reformatted</fails_when>
    <automated>uv run ruff check .</automated>
    <fails_when>non-zero exit, or the output reports one or more remaining lint errors</fails_when>
    <automated>uv run ty check app/ tests/ scripts/ && uv run --project analysis --with ty ty check analysis/</automated>
    <fails_when>non-zero exit from either invocation, or either reports one or more diagnostics</fails_when>
    <automated>uv run pytest -n auto -x 2>&1 | tail -6</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored test</fails_when>
    <automated>cd frontend && npm run lint && npm run build && npm test -- --run 2>&1 | tail -6</automated>
    <fails_when>non-zero exit from any of the three, an "error TS" line in the build output, or a failed test in the vitest summary</fails_when>
  </verify>
  <acceptance_criteria>
    - All six pre-merge gate commands plus the frontend build pass with zero errors.
    - `git status --porcelain` shows no unstaged formatting changes after the gate.
    - Any file the gate rewrote is committed under a `style(...)` or `chore(...)` prefix, separate from the lockfile commit.
    - The plan SUMMARY lists every new diagnostic caused by a tool version bump and how it was resolved.
  </acceptance_criteria>
  <done>The caught-up dependency set is proven green against the same gate that guards `main`.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 4: Install the Renovate GitHub App</name>
  <read_first>
    - `renovate.json` — confirm it is unchanged by this plan; `prConcurrentLimit: 5` and `prHourlyLimit: 2` stay as they are (D-03)
    - `README.md` line 18 — the renovate badge, which stays (D-01)
    - `.planning/phases/216-audit-bugs-and-quick-wins/216-CONTEXT.md` D-01, D-02, D-03
  </read_first>
  <action>Install the Renovate GitHub App on flawchess/flawchess and confirm its Dependency Dashboard issue opens</action>
  <instructions>
    The catch-up above has landed, so Renovate will now start from a current baseline. This
    step is a GitHub UI action that only you can perform.

    1. Open github.com/apps/renovate, choose Configure, select the `flawchess/flawchess`
       repository, and grant access.
    2. Wait for Renovate's first run and confirm a "Dependency Dashboard" issue is opened on
       the repo. That issue is the visible signal that the app is running; D-02 deliberately
       adds no CI staleness guard and no config validator.
    3. Do not enable automerge anywhere (D-03): every Renovate PR is reviewed by hand,
       matching the local squash-merge workflow.

    Expect the first PR batch to be the D-05 majors: typescript 6 to 7, vitest and its two
    companions 4 to 5, jsdom 29 to 30, @types/node 24 to 26, onnxruntime-web 1.27 to 1.29
    (which also needs re-vendoring under `frontend/public/maia/`, so do not merge it
    casually), anthropic 0.122 to 1.3, complexipy 7 to 8. Those are out of scope here and
    are reviewed in their own PRs.
  </instructions>
  <verification>The executor records the reported issue number in the plan SUMMARY and confirms `git diff renovate.json` is empty and that no version-update Dependabot config file was added.</verification>
  <resume-signal>Reply with the Dependency Dashboard issue number or URL, or "skip" if you are deferring the install</resume-signal>
  <acceptance_criteria>
    - The Dependency Dashboard issue number or URL is recorded in the plan SUMMARY.
    - `git diff renovate.json` is empty.
    - `.github/dependabot.yml` does not exist (Dependabot keeps doing security PRs only, D-01).
    - The README renovate badge line is unchanged.
    - Automerge is not enabled (D-03).
  </acceptance_criteria>
  <done>Dependency automation is actually running, with a visible dashboard and a hand-review workflow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public package registries -> this repo | PyPI and npm content enters the build through `uv.lock` and `package-lock.json` |
| Renovate GitHub App -> this repo | A third-party app gains write access to open PRs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-216-05 | Tampering | `uv.lock` / `frontend/package-lock.json` supply chain | high | mitigate | No new package names are introduced; this is an in-range version bump of already-vetted, already-locked packages, so the package-legitimacy gate does not apply (RESEARCH.md "Package Legitimacy Audit": no `[ASSUMED]`, `[SUS]` or `[SLOP]` dispositions). Both lockfile diffs are reviewed before commit, and CI's existing `pip-audit` and `audit-ci` steps run against the result on the next release PR. |
| T-216-05B | Tampering | unintended major-version bump crossing a breaking change | high | mitigate | Bare `uv lock --upgrade` is prohibited; scoped `--upgrade-package` invocations only, with an explicit post-lock assertion on the `anthropic` and `complexipy` versions and a range assertion on the six npm majors. |
| T-216-05C | Elevation of privilege | Renovate GitHub App write access | medium | accept | Renovate is the already-chosen tool (D-01) and its config is already committed. Automerge stays off (D-03), so the app can open PRs but cannot land code; every change still passes a human review and the local squash-merge gate. |
| T-216-05D | Tampering | `onnxruntime-web` npm/vendored copy divergence | medium | mitigate | The npm entry is pinned exactly and asserted byte-identical after the update, because the runtime actually loads the vendored copy under `frontend/public/maia/`; bumping only one side would ship mismatched wasm and glue. |
</threat_model>

<verification>
- Locked `anthropic` still on 0.x and `complexipy` still on 7.x after every lock invocation.
- `uv sync --locked --group maia-inference` succeeds.
- Full CLAUDE.md pre-merge gate plus `npm run build` all green.
- `renovate.json` unchanged, no `.github/dependabot.yml`, README badge unchanged.
- Renovate Dependency Dashboard issue exists (HUMAN-UAT, recorded in the SUMMARY).
</verification>

<success_criteria>
SC-3 is met when both lockfiles are caught up within their existing ranges with no D-05
major taken, the full gate plus a frontend build are green, `renovate.json` and the README
badge are untouched, and the Renovate App is installed with its Dependency Dashboard issue
open.
</success_criteria>

<output>
Create `.planning/phases/216-audit-bugs-and-quick-wins/216-03-SUMMARY.md` when done.
</output>
