---
phase: 218-backend-onnxruntime-parity-spike-python-3-14-chain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/218-backend-onnxruntime-parity-spike-python-3-14-chain/218-evidence-onnxruntime-1.20.1-control.txt
  - .planning/phases/218-backend-onnxruntime-parity-spike-python-3-14-chain/218-evidence-onnxruntime-1.29.0-python.txt
  - .planning/phases/218-backend-onnxruntime-parity-spike-python-3-14-chain/218-evidence-onnxruntime-node-1.29.0.txt
  - scripts/maia_parity_spike.py
  - pyproject.toml
  - scripts/package.json
  - .planning/seeds/SEED-162-major-dependency-backlog.md
autonomous: false
requirements: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SC-1: the onnxruntime 1.29.0 run of `scripts/maia_parity_spike.py` is committed as a readable evidence file, so anyone can see the pass/fail and the measured max per-ply drift without re-running anything."
    - "SC-1: a control run of the SAME script at the currently pinned 1.20.1, in the SAME isolated environment, is committed alongside it — that is what makes the 1.29.0 number a diff against the 1.20.1 baseline rather than an unanchored number."
    - "SC-1: the committed baseline is the `PARITY_EPSILON` comment block inside `scripts/maia_parity_spike.py` (there is no separate baseline file), and it now carries a second dated measurement paragraph naming the 1.29.0 result and the evidence path."
    - "SC-1: `pyproject.toml`'s `maia-inference` pin comment records the 1.29.0 result and points at the evidence file, pass or fail."
    - "SC-1 (scope correction): `scripts/package.json`'s `onnxruntime-node` pin rationale carries the same verdict from the same native core, measured by `verify_value_head.mjs` — Phase 217's D-08 deferred that bump TO this phase, it is not out of scope."
    - "SC-3: the exit code recorded in each evidence file is the interpreter's own, captured via `${PIPESTATUS[0]}` — a segfault (exit 139) is a result to record, not an error to retry away."
    - "SEED-162 cluster 4 carries the measurement outcome and the evidence path in its status line."
    - "The go/no-go between this plan and 218-02 is a human decision made against committed evidence, not an executor inference."
    - "Spec-less probe fallback: skipped visibly — this phase has no requirement IDs and no SPEC.md/UI-SPEC.md/AI-SPEC.md exists, so no probe predicates were generated this run."
  prohibitions:
    - statement: "No pinned version string moves in this plan. `pyproject.toml` still pins onnxruntime at 1.20.1 and `scripts/package.json` still pins onnxruntime-node at 1.21.1 when this plan ends, whatever the measurement said. Raising them is 218-02's job, behind the checkpoint."
      status: enforced
      verification: "Task 2 verify asserts the two pinned strings are unchanged; a moved pin fails the task."
    - statement: "`PARITY_EPSILON` is never loosened, and the fixture corpus is never edited, to make a run pass. The script's own D-02 rule."
      status: enforced
      verification: "Task 2 verify asserts `PARITY_EPSILON: float = 0.010` is still present verbatim and that the pyproject/spike diffs are comment-only."
    - statement: "The measurement never installs into the project environment. `uv.lock`, `pyproject.toml`, `scripts/package.json` and `scripts/package-lock.json` are byte-identical to HEAD when the measurement finishes (RESEARCH.md Pitfall 2)."
      status: enforced
      verification: "Task 1 verify runs `git status --porcelain` scoped to every version file and requires empty output."
    - statement: "Phase 217's clean onnxruntime-web 1.29.0 result is not treated as evidence about the native CPU build. Different compiled artifact, different code path (RESEARCH.md Pitfall 5)."
      status: enforced
      verification: "The native measurement is actually run; no task cites Phase 217 as a reason to shorten it."
  artifacts:
    - .planning/phases/218-backend-onnxruntime-parity-spike-python-3-14-chain/218-evidence-onnxruntime-1.20.1-control.txt
    - .planning/phases/218-backend-onnxruntime-parity-spike-python-3-14-chain/218-evidence-onnxruntime-1.29.0-python.txt
    - .planning/phases/218-backend-onnxruntime-parity-spike-python-3-14-chain/218-evidence-onnxruntime-node-1.29.0.txt
    - scripts/maia_parity_spike.py
    - pyproject.toml
    - scripts/package.json
    - .planning/seeds/SEED-162-major-dependency-backlog.md
  key_links:
    - "`uv run --no-project --with onnxruntime==<version>` -> ephemeral venv -> `scripts/maia_parity_spike.py` -> evidence file; the `--no-project` flag is the link that keeps `uv.lock` out of the measurement"
    - "`scripts/maia_parity_spike.py` `PARITY_EPSILON` comment block -> the only committed 1.20.1 baseline -> the second dated paragraph appended here"
    - "`npm --prefix scripts install --no-save onnxruntime-node@1.29.0` -> `scripts/node_modules` -> `requireFromScripts('onnxruntime-node')` in `scripts/lib/node-engine-providers.mjs` -> `verify_value_head.mjs` -> evidence file"
    - "evidence file paths -> quoted in the `pyproject.toml` pin comment, the `scripts/package.json` description, the spike docstring and SEED-162 -> the next person bumping either pin finds the second datapoint"
---

<objective>
Answer the one question SEED-162 cluster 4 is blocked on, with committed evidence: does the
**native** onnxruntime core still segfault or diverge on the vendored `maia3_simplified.onnx`
at 1.29.0? Measure it in both languages that consume that core — Python (`onnxruntime`, the
`maia-inference` group) and Node (`onnxruntime-node`, the headless harness) — in throwaway
environments that cannot touch a lockfile, commit the raw output, write the verdict into every
pin-rationale site that will be read by the next person who tries this, and stop at a human
go/no-go.

Purpose: this is a measurement, not a bump. The whole phase is a chain whose first link is
falsifiable; if it fails, nothing below it should have been touched. Every design choice in this
plan exists to keep the FAIL path leaving a clean tree (`git diff` shows comments and evidence,
nothing else) while still recording a real second datapoint.

Output: three evidence files, four updated pin-rationale/status sites, and a recorded PASS or
FAIL verdict.

Spec-less probe fallback is skipped visibly: this phase carries no requirement IDs and no
SPEC/UI-SPEC/AI-SPEC exists, so no probe predicates were generated this run.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/218-backend-onnxruntime-parity-spike-python-3-14-chain/218-RESEARCH.md
@.planning/phases/218-backend-onnxruntime-parity-spike-python-3-14-chain/218-PATTERNS.md
@.planning/seeds/SEED-162-major-dependency-backlog.md
@CLAUDE.md
</context>

## Artifacts this plan produces

| Kind | Name | Notes |
|------|------|-------|
| New file | `.planning/phases/218-backend-onnxruntime-parity-spike-python-3-14-chain/218-evidence-onnxruntime-1.20.1-control.txt` | Raw stdout+stderr of the spike at the currently pinned version, run in the same isolated shape as the candidate — the anchor that makes the candidate number a diff |
| New file | `.planning/phases/218-backend-onnxruntime-parity-spike-python-3-14-chain/218-evidence-onnxruntime-1.29.0-python.txt` | Raw stdout+stderr of the spike at 1.29.0. This file IS success criterion 1 |
| New file | `.planning/phases/218-backend-onnxruntime-parity-spike-python-3-14-chain/218-evidence-onnxruntime-node-1.29.0.txt` | Raw stdout+stderr of `verify_value_head.mjs` against `onnxruntime-node@1.29.0` |
| Changed comment | `PARITY_EPSILON` block in `scripts/maia_parity_spike.py` | Second dated measurement paragraph appended below the 2026-07-16 one. The constant's value is unchanged |
| Changed comment | `maia-inference` pin comment in `pyproject.toml` | Records the 1.29.0 result + evidence path. The pinned string `"onnxruntime==1.20.1"` is unchanged in this plan |
| Changed comment | `description` field in `scripts/package.json` | Same verdict for the Node side. The pinned `1.21.1` is unchanged in this plan |
| Changed status line | `.planning/seeds/SEED-162-major-dependency-backlog.md` | Cluster 4 outcome + evidence path |
| New symbols | none | No new function, class, constant, endpoint or export anywhere in this plan |
| Not produced | a `baseline.json` or any separate baseline artifact | RESEARCH.md Pitfall 1: no such file exists and none is created. The baseline is a comment block |
| Not produced | any version-string change | See the `prohibitions` block |

## API coverage

Not applicable, and deliberately not fabricated: the deterministic detector
(`api-coverage.cjs`) returned `detected: false` for this phase's scope, and the phase integrates
no external API — it bumps a local ML runtime pin and a language runtime. No `COVERAGE.md` is
written.

<assumption_delta_decision>
**Noun:** "parity measurement datapoint" for the native onnxruntime pin.

**Signal:** `assumption-delta scan 218` flagged the ROADMAP phrase "the pin comment gains the
**second** datapoint" — a singular-to-plural transition on a noun that is currently modeled as
exactly one thing (the single dated paragraph inside the `PARITY_EPSILON` comment block, measured
2026-07-16 at 1.20.1).

**Decision: add-alongside.** The 2026-07-16 paragraph stays exactly as written and the new
measurement is appended below it as a second dated paragraph in the same block. It is not
rewritten, replaced, or collapsed into a "latest measurement" singleton.

**Rationale:** the value of the block is the *series*. A future bump attempt needs to see that
1.20.1 measured 0.003844 in July 2026 AND what 1.29.0 measured now, because the shape of the
delta (numeric drift vs. a hard segfault vs. a tier flip) is what tells the next person whether
to try an intermediate version or to re-export the model (RESEARCH.md Open Question 2). Promoting
the block to a structured multi-row artifact is not warranted at n=2 and would fight the file's
existing shape.
</assumption_delta_decision>

<tasks>

<task type="tracer">
  <name>Task 1: Measure the native onnxruntime core at 1.29.0 end-to-end, in both languages, without touching a lockfile</name>
  <precondition>`frontend/node_modules` and `scripts/node_modules` are installed at their committed versions (`npm ci` in `frontend/`, `npm --prefix scripts ci`) — the Node harness resolves `chess.js` from `frontend/node_modules` through the `@/` alias hook and `onnxruntime-node` from `scripts/node_modules`; halt if either directory is absent.</precondition>
  <reversibility rating="reversible">Produces three untracked-then-committed text files and mutates only gitignored install mirrors; `git clean` plus `npm --prefix scripts ci` restores the tree exactly.</reversibility>
  <files>.planning/phases/218-backend-onnxruntime-parity-spike-python-3-14-chain/218-evidence-onnxruntime-1.20.1-control.txt, .planning/phases/218-backend-onnxruntime-parity-spike-python-3-14-chain/218-evidence-onnxruntime-1.29.0-python.txt, .planning/phases/218-backend-onnxruntime-parity-spike-python-3-14-chain/218-evidence-onnxruntime-node-1.29.0.txt</files>
  <read_first>
    - `scripts/maia_parity_spike.py` in full — the module docstring's exit-code contract and its "do NOT loosen PARITY_EPSILON, do NOT re-export the model" rule; the `PARITY_EPSILON` comment block at lines 55-70 (this IS the committed baseline, per RESEARCH.md Pitfall 1 — there is no baseline file to find); `main()`, which prints a per-ply table, a `measured max per-ply drift:` line and either `PARITY GATE PASSED` or `PARITY GATE FAILED`
    - `app/services/maia_encoding.py` lines 37-40 — its only imports are `math`, `collections.abc` and `chess`, which is why the isolated invocation below needs exactly `numpy` + `chess` on top of onnxruntime
    - `pyproject.toml` lines 38-49 — the `[dependency-groups] maia-inference` block and its pin comment
    - `scripts/package.json` — the `description` field, which carries the `onnxruntime-node` pin rationale and names `verify_value_head.mjs` as the required gate for any bump
    - `scripts/engine_disagreement_study/verify_value_head.mjs` — the five hard checks, the `PASS`/`FAIL` line prefix, and `process.exit(1)` on any failure
    - `scripts/lib/node-engine-providers.mjs` `createMaiaSession` — `requireFromScripts('onnxruntime-node')` and the `frontend/public/maia/maia3_simplified.onnx` path it resolves
    - `218-RESEARCH.md` sections "Installation", Pitfall 1, Pitfall 2 and Pitfall 5
  </read_first>
  <action>
    Three runs, each in a throwaway environment, each captured to its own committed evidence file.
    Give every evidence file a two-line header before the captured output: the exact command that
    produced it, and the run date. After the run, append a single line `exit_code=<N>` taken from
    `${PIPESTATUS[0]}` — `tee` swallows the real status, and the status is load-bearing here
    because a native crash exits 139 (SIGSEGV) with no summary line printed at all. A crashed run
    is a result to record, not an error to retry.

    Run A, the control, at the version currently pinned:
    `uv run --no-project --with onnxruntime==1.20.1 --with numpy --with chess python scripts/maia_parity_spike.py`
    piped through `tee` into `218-evidence-onnxruntime-1.20.1-control.txt`.
    `--no-project` is load-bearing: it builds an ephemeral overlay and never writes `uv.lock` or
    `pyproject.toml` (Pitfall 2). The script inserts the repo root into `sys.path` itself, so
    `app.services.maia_encoding` imports fine outside the project environment; `numpy` and `chess`
    are the only other third-party imports on that path. This invocation shape was validated at
    plan time — without the onnxruntime override it reaches the script's own "onnxruntime not
    installed" branch and exits 2, proving every other import resolves.

    Run B, the candidate: the same command with `onnxruntime==1.29.0`, into
    `218-evidence-onnxruntime-1.29.0-python.txt`.

    Run C, the Node side of the same native core:
    `npm --prefix scripts install --no-save onnxruntime-node@1.29.0`, then
    `node --import ./scripts/lib/frontend-alias-hook.mjs scripts/engine_disagreement_study/verify_value_head.mjs`
    piped into `218-evidence-onnxruntime-node-1.29.0.txt`. `--no-save` is the Node equivalent of
    `--no-project`: it swaps the installed copy under the gitignored `scripts/node_modules` without
    rewriting the manifest. Afterwards restore the committed state with `npm --prefix scripts ci`,
    and if npm touched either manifest anyway, `git checkout -- scripts/package.json scripts/package-lock.json`.

    Read the control before you read the candidate. If run A does not itself pass the gate, the
    measurement rig is broken (wrong model bytes, a corrupted corpus, a machine-specific numpy
    interaction) and run B's number means nothing — stop and report that, rather than reporting a
    candidate result measured against a broken control.

    For run B, record which of the three distinguishable outcomes occurred, because they imply
    different follow-ups (RESEARCH.md Open Question 2): a native crash with no summary line; a
    completed run whose max per-ply drift exceeds the tolerance; or a completed run where a ply
    lands in a different gem/great/neither tier than the corpus expects. Put that classification
    in the SUMMARY in words, not just the raw file.

    Do not edit the tolerance constant, the fixture corpus, or the vendored model to influence any
    of these runs. Do not shorten this task on the grounds that the browser-side runtime already
    moved to 1.29.0 in Phase 217 — that is a different compiled artifact on a different code path
    (Pitfall 5).
  </action>
  <verify>
    <automated>ls -l .planning/phases/218-backend-onnxruntime-parity-spike-python-3-14-chain/218-evidence-onnxruntime-1.20.1-control.txt .planning/phases/218-backend-onnxruntime-parity-spike-python-3-14-chain/218-evidence-onnxruntime-1.29.0-python.txt .planning/phases/218-backend-onnxruntime-parity-spike-python-3-14-chain/218-evidence-onnxruntime-node-1.29.0.txt</automated>
    <fails_when>non-zero exit (an evidence file is missing), or any listed size is 0 (the redirect captured nothing)</fails_when>
    <automated>grep -H '^exit_code=' .planning/phases/218-backend-onnxruntime-parity-spike-python-3-14-chain/218-evidence-onnxruntime-1.20.1-control.txt .planning/phases/218-backend-onnxruntime-parity-spike-python-3-14-chain/218-evidence-onnxruntime-1.29.0-python.txt .planning/phases/218-backend-onnxruntime-parity-spike-python-3-14-chain/218-evidence-onnxruntime-node-1.29.0.txt</automated>
    <fails_when>fewer than three lines are printed — an evidence file has no recorded interpreter exit status, so a SIGSEGV run would be indistinguishable from a clean one</fails_when>
    <automated>grep -E 'PARITY GATE|measured max per-ply drift' .planning/phases/218-backend-onnxruntime-parity-spike-python-3-14-chain/218-evidence-onnxruntime-1.20.1-control.txt</automated>
    <fails_when>the output lacks the line `PARITY GATE PASSED`, or lacks a `measured max per-ply drift:` line — the control did not reproduce the committed baseline, so the candidate run is unanchored and must not be reported as a verdict</fails_when>
    <automated>git status --porcelain -- pyproject.toml uv.lock scripts/package.json scripts/package-lock.json .python-version Dockerfile Dockerfile.worker .github/workflows/ci.yml analysis/pyproject.toml analysis/uv.lock</automated>
    <fails_when>any line is printed — the measurement mutated a version file, which the FAIL path of success criterion 3 forbids outright</fails_when>
    <automated>git diff --exit-code -- scripts/package.json scripts/package-lock.json</automated>
    <fails_when>non-zero exit — the `--no-save` install or the restore step left the Node manifests modified</fails_when>
  </verify>
  <acceptance_criteria>
    - All three evidence files exist, are non-empty, and each opens with a header naming the exact command that produced it and the run date.
    - Each evidence file contains exactly one `exit_code=` line, sourced from `${PIPESTATUS[0]}` rather than from the `tee` pipeline's status.
    - The control file contains `PARITY GATE PASSED`, a `measured max per-ply drift:` line, and `exit_code=0`.
    - The candidate Python file contains either `PARITY GATE PASSED` or `PARITY GATE FAILED` or ends mid-table with a non-zero `exit_code=` recording a crash; all three are acceptable outcomes to commit, and all three are reported verbatim.
    - The Node evidence file contains either `All value-head checks passed.` with `exit_code=0`, or at least one line beginning `FAIL` / a non-zero `exit_code=`.
    - `git status --porcelain` scoped to `pyproject.toml`, `uv.lock`, `scripts/package.json`, `scripts/package-lock.json`, `.python-version`, `Dockerfile`, `Dockerfile.worker`, `.github/workflows/ci.yml`, `analysis/pyproject.toml`, `analysis/uv.lock` prints nothing.
    - `PARITY_EPSILON: float = 0.010` is still present verbatim in `scripts/maia_parity_spike.py` and `tests/fixtures/maia_parity/corpus.json` is unmodified.
    - The SUMMARY names the candidate outcome class in words: crash, tolerance exceeded, or tier flip — or a clean pass.
  </acceptance_criteria>
  <done>Both native runtimes have been exercised at 1.29.0 against the vendored model, their raw output is committed with recorded exit statuses, an anchoring control run is committed beside them, and no version file was touched.</done>
</task>

<task type="auto">
  <name>Task 2: Write the verdict into every pin-rationale site and the seed, without moving a pin</name>
  <reversibility rating="reversible">Comment and prose edits in four files; `git revert` restores them and no build input changes.</reversibility>
  <files>scripts/maia_parity_spike.py, pyproject.toml, scripts/package.json, .planning/seeds/SEED-162-major-dependency-backlog.md</files>
  <read_first>
    - `scripts/maia_parity_spike.py` lines 55-70 — the exact wording and indentation of the existing 2026-07-16 measurement paragraph, which the new paragraph mirrors
    - `218-PATTERNS.md` section "`scripts/maia_parity_spike.py` — pin-comment / baseline-docstring update", which carries the append template
    - `pyproject.toml` lines 43-49 — the pin comment's imperative voice ("PIN EXACTLY … never a range", "Any future bump MUST re-run …") which must survive the rewrite
    - `scripts/package.json` — the `description` string in full; it is a single JSON string, so the edit must keep it valid JSON on one line
    - `.planning/seeds/SEED-162-major-dependency-backlog.md` line 3 (the `**Status:**` line) and the "Cluster 4" section, for the phrasing convention already used to schedule clusters
    - The three evidence files written by Task 1, for the exact numbers to quote
  </read_first>
  <action>
    Record the same verdict in four places, quoting the measured numbers and the evidence paths so
    the next person to touch either pin finds them without re-running anything.

    In `scripts/maia_parity_spike.py`, append a second dated paragraph directly below the existing
    2026-07-16 one, inside the same comment block above the tolerance constant, matching its
    indentation and voice. It must contain the literal phrase `onnxruntime 1.29.0`, the measured
    max per-ply drift (or the crash signal, if the run died), the pass/fail word, and the relative
    path of the candidate evidence file. Do not replace or reword the 2026-07-16 paragraph and do
    not change the constant's value — see this plan's `assumption_delta_decision`.

    In `pyproject.toml`'s `maia-inference` group comment, add the same result and the same evidence
    path. Keep every imperative already there: the exact-pin instruction, the pointer to the
    verified segfault repro note, and the requirement that any future bump re-runs the spike first.
    On a PASS, the comment says the measurement passed and that 218-02 raises the pin; on a FAIL,
    it says 1.29.0 was measured and rejected, and names what failed. Either way the pinned string
    stays `"onnxruntime==1.20.1"` in this plan.

    In `scripts/package.json`'s `description`, do the same for the Node side: the 1.29.0 result
    from `verify_value_head.mjs`, the evidence path, and the retained sentence that any bump must
    survive that harness first. Keep the file valid JSON. The pinned `onnxruntime-node` value stays
    `1.21.1` in this plan. Note for the reader of that description: Phase 217's D-08 deferred this
    bump TO Phase 218 — it is in scope here, and 218-02 raises it on a PASS.

    In `.planning/seeds/SEED-162-major-dependency-backlog.md`, update the `**Status:**` line and add
    one result line to the Cluster 4 section: on a FAIL, cluster 4 is "deferred — onnxruntime 1.29
    still segfaults / diverges" with the evidence path (this satisfies success criterion 3 in full,
    and the phase ends here); on a PASS, cluster 4 is "spike passed <date>, evidence at <path>,
    version bump proceeding in 218-02" and 218-03 closes it after the release.

    Every edit in this task is a comment, a docstring, a JSON description string or planning prose.
    No pinned version, no dependency list entry, and no `requires-python` value changes here.
  </action>
  <verify>
    <automated>grep -c '"onnxruntime==1.20.1"' pyproject.toml</automated>
    <fails_when>prints anything other than 1 — the Python pin moved, which belongs to 218-02 and only behind the checkpoint</fails_when>
    <automated>python3 -c "import json;d=json.load(open('scripts/package.json'));print(d['dependencies']['onnxruntime-node'])"</automated>
    <fails_when>non-zero exit (the description edit broke the JSON), or it prints anything other than `1.21.1`</fails_when>
    <automated>grep -c 'PARITY_EPSILON: float = 0.010' scripts/maia_parity_spike.py</automated>
    <fails_when>prints anything other than 1 — the tolerance was altered, which the script's own D-02 rule forbids</fails_when>
    <automated>grep -c 'onnxruntime 1.29.0' scripts/maia_parity_spike.py</automated>
    <fails_when>prints 0 — the second dated measurement paragraph was not appended to the baseline comment block</fails_when>
    <automated>grep -Hc '218-evidence-onnxruntime' scripts/maia_parity_spike.py pyproject.toml scripts/package.json .planning/seeds/SEED-162-major-dependency-backlog.md</automated>
    <fails_when>any of the four lines ends in `:0` — that file does not point a reader at the committed evidence</fails_when>
    <automated>uv run ruff format --check scripts/maia_parity_spike.py</automated>
    <fails_when>non-zero exit, or the output names the file as needing reformatting</fails_when>
    <automated>uv run ruff check scripts/maia_parity_spike.py</automated>
    <fails_when>non-zero exit, or any diagnostic is printed (a long comment line trips the line-length rule)</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/maia_parity_spike.py` contains both the 2026-07-16 paragraph and a new dated paragraph naming `onnxruntime 1.29.0`, its measured drift or crash signal, its pass/fail word, and the evidence path.
    - `PARITY_EPSILON: float = 0.010` is unchanged.
    - `pyproject.toml` still contains `"onnxruntime==1.20.1"` exactly once, and its `maia-inference` comment names the 1.29.0 result, the evidence path, and still carries the exact-pin and re-run-the-spike imperatives.
    - `scripts/package.json` parses as JSON, still pins `onnxruntime-node` at `1.21.1`, and its description names the 1.29.0 Node result and the evidence path.
    - `.planning/seeds/SEED-162-major-dependency-backlog.md` `**Status:**` line and Cluster 4 section state the outcome and cite the evidence path.
    - `ruff format --check` and `ruff check` are both clean on the edited script.
    - No file outside the four listed in `<files>` is modified by this task.
  </acceptance_criteria>
  <done>All four pin-rationale and status sites carry the same verdict with the same evidence paths, and every pinned version string is still exactly what it was at phase start.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Does the phase proceed to the Python 3.14 chain (218-02 and 218-03), or stop here with 3.14 recorded as deferred?</decision>
  <context>
    This is the hard gate the whole phase is built around. Task 1 measured the native onnxruntime
    core at 1.29.0 in both Python and Node; Task 2 wrote the verdict into the pin comments and the
    seed. The evidence is committed at:

    - `.planning/phases/218-backend-onnxruntime-parity-spike-python-3-14-chain/218-evidence-onnxruntime-1.20.1-control.txt`
    - `.planning/phases/218-backend-onnxruntime-parity-spike-python-3-14-chain/218-evidence-onnxruntime-1.29.0-python.txt`
    - `.planning/phases/218-backend-onnxruntime-parity-spike-python-3-14-chain/218-evidence-onnxruntime-node-1.29.0.txt`

    Why a human decides: Python 3.14 is not independently schedulable — onnxruntime 1.20.1 ships
    no cp314 wheel, so the language bump is gated entirely on moving off that pin. A partial or
    ambiguous result (say, the Python side clean but the Node harness failing one check) is a
    judgement call about what "the segfault is gone and outputs match" means, and an executor
    should not make it by inference. Everything downstream — both container images, CI, the worker
    fleet, and a production deploy — rides on this call.

    Read the control file first: if the control did not itself pass, neither answer below applies
    and the rig needs fixing before anything is decided.
  </context>
  <options>
    <option id="proceed">
      <name>PASS — proceed to 218-02</name>
      <pros>Both evidence files show a clean pass (`PARITY GATE PASSED` with `exit_code=0`, and `All value-head checks passed.` with `exit_code=0`). Unblocks the pin raise and Python 3.14 everywhere, with cp314 wheels confirmed available for onnxruntime 1.29.0 and every other locked backend dependency.</pros>
      <cons>Commits the project to a language-runtime major and two re-pinned base images across the API image, the worker fleet image and CI. Reversible by `git revert` while the work is only on `main`; costlier once released.</cons>
    </option>
    <option id="defer">
      <name>FAIL — stop here, 3.14 deferred</name>
      <pros>Satisfies success criterion 3 exactly: no version file changed, the second datapoint is recorded in the pin comments, and SEED-162 cluster 4 is marked deferred with the evidence path. The phase ends clean and bisectable.</pros>
      <cons>Python 3.14 stays blocked until a future attempt; the failure mode recorded in Task 1 determines whether that retry should try an intermediate onnxruntime version (crash) or needs a model re-export (tolerance or tier failure), which is a much larger undertaking.</cons>
    </option>
  </options>
  <resume-signal>Reply `proceed` to run 218-02 and 218-03, or `defer` to close the phase here. If the control run did not pass, reply `defer` and describe the rig problem instead.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI -> ephemeral `uv` overlay environment | A third-party native shared library enters the build machine at measurement time and is loaded into a Python process |
| npm registry -> gitignored `scripts/node_modules` | The same native core enters as a Node addon, executed by the headless harness |
| ephemeral environment -> the tracked working tree | The moment a measurement could leak into a lockfile or a manifest; this plan's whole isolation design exists on this boundary |
| committed evidence file -> future maintainer's bump decision | A text file becomes the standing justification for a pin that guards a production inference path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-218-SC | Tampering | `uv run --with onnxruntime==1.29.0` and `npm install --no-save onnxruntime-node@1.29.0` | high | mitigate | Both are pre-existing pinned dependencies undergoing a version bump, not new introductions. 218-RESEARCH.md's Package Legitimacy Audit resolved both to github.com/microsoft/onnxruntime with an `OK` verdict and no `[ASSUMED]`/`[SUS]`/`[SLOP]` entries, so no blocking legitimacy checkpoint is required. Both are installed at an exact version, never a range, and the 1.29.0 copies land only in throwaway locations in this plan — nothing installed here reaches a lockfile or a committed manifest. |
| T-218-01 | Tampering | the tracked working tree during measurement | high | mitigate | `--no-project` (Python) and `--no-save` (Node) keep the manifests out of the install path, `npm --prefix scripts ci` restores the Node tree afterwards, and Task 1's verify gates on `git status --porcelain` over all ten version files plus `git diff --exit-code` on the two Node manifests. RESEARCH.md Pitfall 2 is the recorded failure this mitigates: a `uv add` here would leave a half-bumped tree that is easy to commit by accident on the FAIL path. |
| T-218-02 | Denial of Service | the vendored `maia3_simplified.onnx` load path in the FastAPI process | high | mitigate | The measurement runs before any pin moves, and the script verifies the model's SHA-256 before loading it, so parity is never measured against different bytes. A crash is captured as a recorded exit status rather than swallowed, and the standing `tests/services/test_maia_parity.py` regression test (CI-synced) remains the gate for anything downstream. |
| T-218-03 | Repudiation | the committed evidence files | low | accept | The files are hand-redirected stdout and could in principle be edited after the fact. Accepted: each file records the exact command and date in its header, the runs are cheap to reproduce, and the control run in the same environment makes a doctored candidate number visibly inconsistent. Cryptographic attestation of a local measurement is disproportionate for a two-command reproducible check. |
</threat_model>

<verification>
- The three evidence files exist, are non-empty, carry a command+date header and an `exit_code=` line.
- The control run passed the gate; the candidate run's outcome is recorded verbatim and classified in words.
- `git status --porcelain` over the ten version files is empty at the end of Task 1.
- The two pinned version strings are unchanged at the end of Task 2 (`"onnxruntime==1.20.1"`, `onnxruntime-node` `1.21.1`), and `PARITY_EPSILON` is still `0.010`.
- All four rationale/status sites cite an evidence path.
- `ruff format --check` and `ruff check` are clean on the edited script; `scripts/package.json` parses as JSON.
- The human checkpoint has been answered `proceed` or `defer`, and the SUMMARY records which.
</verification>

<success_criteria>
- ROADMAP success criterion 1 is fully satisfied by this plan alone, on either branch: the 1.29.0 spike output is committed as evidence, anchored by a same-environment 1.20.1 control, and `pyproject.toml`'s pin comment carries the result.
- ROADMAP success criterion 3 is fully satisfied by this plan on the FAIL branch: no version file changed, SEED-162 cluster 4 reads "deferred" with the evidence path, and the phase ends here.
- On the PASS branch, 218-02 is unblocked and its precondition can be checked mechanically against the committed evidence files.
</success_criteria>

<output>
Create `.planning/phases/218-backend-onnxruntime-parity-spike-python-3-14-chain/218-01-SUMMARY.md` when done.
Record in it: the control and candidate max per-ply drift numbers side by side, the candidate
outcome classified in words (clean pass / crash / tolerance exceeded / tier flip), the Node
harness result, the checkpoint answer, and — if `defer` — an explicit statement that no version
file was touched.
</output>

