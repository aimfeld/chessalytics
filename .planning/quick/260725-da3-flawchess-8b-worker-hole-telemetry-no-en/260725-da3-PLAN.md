---
phase: quick-260725-da3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/worker_heartbeat.py
  - alembic/versions/<new>_add_worker_hole_counters.py
  - app/repositories/worker_heartbeat_repository.py
  - app/services/eval_apply.py
  - app/services/engine.py
  - app/routers/eval_remote.py
  - tests/test_worker_heartbeats.py
  - tests/services/test_engine_nodes.py
  - tests/test_eval_worker_endpoints.py
  - CHANGELOG.md
autonomous: true
requirements: [ITEM-1, ITEM-2, ITEM-3]

must_haves:
  truths:
    - "Every atomic-submit with residual holes increments that worker's holes_submitted by failed_ply_count, on EVERY attempt (Path A/B/C alike), not only at the MAX_EVAL_ATTEMPTS cap."
    - "holes_submitted/plies_leased accumulate across multiple submits from the same worker_id (they are accumulators, never last-value)."
    - "A hole-free atomic-submit leaves holes_submitted unchanged while still advancing plies_leased."
    - "ITEM 2 (if shipped): an asyncio.TimeoutError inside _acquire_and_analyse returns None WITHOUT restarting the Stockfish worker; EngineError / EngineTerminatedError still restart it."
    - "ITEM 2 (if shipped): a cancelled analyse() leaves the UCI protocol usable — a second analyse() on the SAME protocol object returns a real score."
    - "The Path-C Sentry event carries worker_id + last_ip while its message string stays byte-identical, so all ~1602 existing events keep grouping into FLAWCHESS-8B."
    - "The eval_drain lane's cap path still uses logger.warning and fires zero Sentry events (FLAWCHESS-5V)."
  artifacts:
    - "alembic/versions/<rev>_add_worker_hole_counters.py — adds holes_submitted + plies_leased with server_default='0'"
    - "tests/test_worker_heartbeats.py — multi-submit accumulation test for the two new counters"
    - "tests/services/test_engine_nodes.py — timeout-does-not-restart test + real-engine post-cancellation reuse test"
    - "tests/test_eval_worker_endpoints.py — Path-C event carries worker_id/last_ip with an unchanged message string"
  key_links:
    - "eval_remote._apply_atomic_submit -> apply_full_eval(heartbeat_n_evals=len(body.evals)) -> upsert_worker_heartbeat(n_plies_leased=...) — the counter input must be genuinely seeded, not merely routed."
    - "apply_full_eval's failed_ply_count (computed at its top) -> upsert_worker_heartbeat(n_holes=...) inside the SAME write_session/commit."
    - "eval_remote._apply_atomic_submit's worker_id/last_ip -> bound callback -> _report_path_c_capacity_reached -> sentry_sdk tag/context."
---

<objective>
Close the FLAWCHESS-8B attribution blind spot and cut hole VOLUME at the source.

Three independent items from the brief (already investigated — do not re-derive the prod
measurements):

1. Per-worker hole counters on `worker_heartbeats` (passive telemetry, the real attribution
   mechanism). Task 1 is the end-to-end slice: model -> migration -> repository ->
   service wiring -> real HTTP atomic-submit -> DB assertion.
2. Stop restarting the Stockfish process on `asyncio.TimeoutError` — GATED on a mandatory
   python-chess cancellation-safety verification. Ships only if safe.
3. `worker_id` / `last_ip` on the Path-C Sentry event, message string byte-identical.

Purpose: make "is it one slow worker?" answerable from the DB, and stop amplifying one slow
position into a block of holes.
Output: two new heartbeat columns + migration, one engine exception-handling split (gated),
one enriched Sentry event, and regression tests for each shipped item — each proven by
reverting the fix and observing the test fail.

Out of scope (from the brief, do not drift): retrying a timed-out position at a reduced node
budget; changing `_NODES_TIMEOUT_S` / `_NODES_BUDGET` / `MAX_EVAL_ATTEMPTS` / the Path A/B/C
decision tree; `scripts/resweep_holed_games.py`; deploying (the user runs `/deploy`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260725-da3-flawchess-8b-worker-hole-telemetry-no-en/260725-da3-BRIEF.md
@CLAUDE.md

Current code (line numbers verified 2026-07-25):
- `app/models/worker_heartbeat.py` — 43 lines; `submit_count` / `evals_submitted` at L41-42 are
  the pattern to copy (`BigInteger`, `nullable=False`, `default=0`).
- `app/repositories/worker_heartbeat_repository.py` — `upsert_worker_heartbeat`; accumulator
  pattern in the `on_conflict_do_update` `set_` at L74-75; SAVEPOINT + swallow at L78-83.
- `app/services/eval_apply.py` — `apply_full_eval` L2357; `failed_ply_count` computed L2437;
  `upsert_worker_heartbeat` call L2504-2513 (only the atomic lane sets `record_heartbeat=True`).
- `app/routers/eval_remote.py` — `_report_path_c_capacity_reached` L1031-1049;
  `_apply_atomic_submit` L1052 with `worker_id`/`last_ip` params L1055-1056 and the
  `apply_full_eval(...)` call L1265-1287; `worker_id_label` L392.
- `app/services/engine.py` — `_acquire_and_analyse` L465; the shared except tuple L505-514;
  `_restart_worker` L439.
- `app/services/eval_drain.py` — `_log_path_c_capacity_reached` L641-658 (logger.warning; the
  FLAWCHESS-5V lane that must NOT change).
- `alembic` head is `e872c9deb514`. `uv run alembic check` is currently clean.

Existing tests that already cover adjacent behavior (read before writing new ones):
- `tests/test_worker_heartbeats.py` — the whole ASGI + session-patch harness for heartbeat
  assertions (`_make_client`, `_get_worker_heartbeat`, `_delete_worker_heartbeat`,
  `_patch_router_session`, `_insert_game`, `_insert_game_positions`, `_BLUNDER_SUBMIT_EVALS_142`,
  `_SIX_PLY_PGN_142`). Reuse it; do not build a second harness.
- `tests/test_eval_worker_endpoints.py::...::test_atomic_submit_holed_batch_at_cap_stamps_with_sentry_warning`
  (L2842-2960) — the canonical Path-C test; the hole is made by nulling `holed_evals[6]`.
- `tests/services/test_engine_nodes.py::TestEvaluateNodesWithMock::test_evaluate_nodes_timeout_returns_none`
  (L116-159) — currently asserts `restart_called` on timeout. ITEM 2 INVERTS this test.
- `tests/services/test_full_eval_drain.py::test_cap_reached_stamps_and_logs` (L3404) — already
  asserts the drain lane uses `logger.warning` and fires zero Sentry events. ITEM 3 must keep
  it green; do NOT write a duplicate of it.
- `alembic/versions/20260526_000000_add_n_games_to_user_benchmark_percentiles.py` — precedent for
  a permanent `server_default="0"` on a new NOT NULL counter column.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (ITEM 1): per-worker hole counters on worker_heartbeats</name>
  <precondition>Dev PostgreSQL is up: `docker compose -f docker-compose.dev.yml -p flawchess-dev up -d`.</precondition>
  <files>app/models/worker_heartbeat.py, alembic/versions/&lt;new&gt;_add_worker_hole_counters.py, app/repositories/worker_heartbeat_repository.py, app/services/eval_apply.py, tests/test_worker_heartbeats.py</files>
  <behavior>
    - Two holed atomic-submits from the same worker_id (each UNDER the retry cap, i.e. Path B):
      holes_submitted goes 1 then 2 (accumulation, not last-value); plies_leased goes
      len(evals) then 2*len(evals).
    - A third, hole-free atomic-submit from the same worker_id: holes_submitted STAYS 2;
      plies_leased advances to 3*len(evals).
    - The pre-existing three-lane accumulation test still passes: entry-submit and
      flaw-blob-submit never move either new counter (both default to 0).
  </behavior>
  <action>
Add two accumulating BigInteger counters to the fleet heartbeat registry, fed only by the
atomic-submit lane. Passive telemetry only — no gate, no authz decision, no behavior change.

1. `app/models/worker_heartbeat.py`: add `holes_submitted` and `plies_leased`, copying the
   `submit_count` / `evals_submitted` declaration at L41-42 exactly (`Mapped[int]`,
   `mapped_column(BigInteger, nullable=False, default=0)`). Extend the class docstring with one
   sentence: both counters are fed ONLY by the atomic-submit lane so a per-worker hole RATE
   (holes_submitted / plies_leased) is meaningful, unlike `evals_submitted` which mixes all
   three submit lanes. Keep the existing "passive telemetry (Phase 149 PRUNE-06)" framing and
   the "`worker_id` is advisory, never used for authz (T-123-03)" note intact.

2. Migration: `uv run alembic revision --autogenerate -m "add worker hole counters"`
   (down_revision will resolve to `e872c9deb514`). Autogenerate will emit both `add_column`s;
   hand-edit each to `server_default="0"` so the ~120 existing prod rows backfill atomically at
   upgrade. Keep the server_default permanently (`compare_server_default` is not enabled in
   `alembic/env.py`, so it causes no autogenerate drift — same as the
   `add_n_games_to_user_benchmark_percentiles` precedent). Write a real docstring naming quick
   task 260725-da3 / FLAWCHESS-8B and why the columns exist. `downgrade()` drops both columns.
   Then `uv run alembic upgrade head` against the dev DB.

3. `app/repositories/worker_heartbeat_repository.py`: add two keyword-only params
   `n_holes: int = 0` and `n_plies_leased: int = 0`. Default 0 is load-bearing — the
   entry-submit and flaw-blob-submit call sites stay untouched and can never inflate the
   counters. Add both to the `pg_insert(...).values(...)` and to the `on_conflict_do_update`
   `set_` with the SAME accumulate expression shape as L74-75
   (`WorkerHeartbeat.holes_submitted + stmt.excluded.holes_submitted`). Extend the docstring:
   these two accumulate like submit_count/evals_submitted and are atomic-lane-only. Leave the
   SAVEPOINT/swallow behavior exactly as is.

4. `app/services/eval_apply.py`: the ONLY correct seam is inside `apply_full_eval`'s existing
   `if record_heartbeat:` block (L2504) — `failed_ply_count` is already computed above it at
   L2437, and this call is inside the caller's single write_session/commit. Pass
   `n_holes=failed_ply_count` and `n_plies_leased=heartbeat_n_evals`. Do NOT add a second
   `upsert_worker_heartbeat` call in the router after `apply_full_eval` returns — that would
   double-count `submit_count` and land outside the atomic write ordering. Note in the comment
   that this deliberately counts holes on EVERY attempt (Path A/B/C alike), not just the Path-C
   cap path, because per-attempt telemetry is what makes a slow worker visible before it burns
   all 3 attempts. Add NO new params to `apply_full_eval` — `heartbeat_n_evals`
   (`len(body.evals)`, set by `_apply_atomic_submit` at L1285) is the submitted-ply count, and
   the drain lane leaves `record_heartbeat=False` so it is unaffected.

5. `tests/test_worker_heartbeats.py`: add one test implementing the `<behavior>` block above,
   reusing the module's existing harness and `_SIX_PLY_PGN_142` / `_BLUNDER_SUBMIT_EVALS_142`.
   Build each holed payload the way the Path-C test does (copy the eval dicts, then set
   `eval_cp`/`eval_mate` of index 6 to None) but leave `full_eval_attempts` at its default 0 so
   the submit takes Path B — that is the point: the counters must move on a non-cap attempt.
   Use a DISTINCT `full_hash` base per game (e.g. 930000/931000/932000) to avoid any
   cross-game dedup interaction. Extend `_get_worker_heartbeat` to return the two new columns.
   Clean up games + the heartbeat row in `finally`, as the neighbouring tests do.
  </action>
  <verify>
    <automated>uv run alembic upgrade head && uv run alembic check && uv run pytest tests/test_worker_heartbeats.py -x -q</automated>
    <automated>MUTATION PROOF (mandatory): change the `n_holes=failed_ply_count` argument in `apply_full_eval` to `n_holes=0`, re-run `uv run pytest tests/test_worker_heartbeats.py -x -q`, and confirm the new test FAILS on the holes_submitted assertion. Restore the real argument and confirm it passes again. Then repeat for `n_plies_leased` (set it to 0) and confirm the plies_leased assertion fails. Record both observed failure messages in the SUMMARY. A symbol-presence or grep check is NOT acceptable proof.</automated>
  </verify>
  <done>Both columns exist in the migrated schema with server_default '0'; `alembic check` reports no new upgrade operations; the new test proves accumulation across multiple submits AND that a hole-free submit leaves holes_submitted unchanged; the pre-existing three-lane heartbeat test still passes; both mutation proofs observed and recorded. Commit: `feat(260725-da3): per-worker hole counters on worker_heartbeats`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (ITEM 2): stop restarting Stockfish on a timeout — GATED on cancellation safety</name>
  <files>app/services/engine.py, tests/services/test_engine_nodes.py</files>
  <behavior>
    - A timed-out `analyse` returns None and does NOT call `_restart_worker`.
    - A `chess.engine.EngineError` still returns None and DOES call `_restart_worker`.
    - Real engine (skipped when the Stockfish binary is absent): after one `analyse` is
      cancelled by `asyncio.wait_for`, a SECOND `analyse` on the SAME protocol object returns a
      usable score — i.e. the UCI protocol is not desynced by the cancellation.
  </behavior>
  <action>
STEP A — MANDATORY CORRECTNESS GATE. Do this BEFORE editing `engine.py`. The brief forbids
assuming; a cancelled `analyse` that can desync the UCI protocol makes this item UNSAFE.

A1. Read the installed python-chess 1.11.2 source at
    `.venv/lib/python3.13/site-packages/chess/engine.py` and record what the cancellation path
    actually does, citing line numbers:
    - `Protocol.analyse` (L1100-1135): the result is awaited inside `with analysis:`.
    - `AnalysisResult.__exit__` (L2836) -> `AnalysisResult.stop()` (L2761).
    - `UciAnalysisCommand.cancel` (L1766): `self.engine.send_line("stop")`.
    - `Protocol.communicate` (L986-1021): `cancel_if_cancelled` on the result future ->
      `BaseCommand._cancel` (L1276); a NEW command is queued via `previous_command_finished`
      and only starts after the previous command's `finished` future resolves.
    - `UciAnalysisCommand._bestmove` (L1758-1763) -> `set_finished()`.
    Write down, in the SUMMARY, whether that chain means the next `analyse()` on the same slot
    is serialized behind the stale `bestmove`.

A2. Prove it empirically with the real binary (present locally at `~/.local/stockfish/sf`).
    Write this as the permanent regression test rather than a throwaway script: a new test in
    `tests/services/test_engine_nodes.py` under the existing `@skip_if_no_stockfish` class
    (reuse `skip_if_no_stockfish` / `_STOCKFISH_PATH`, and `_engine_popen_kwargs` if it is
    importable). Shape: `chess.engine.popen_uci(_STOCKFISH_PATH)` -> configure Hash/Threads the
    way `EnginePool.start` does -> `await asyncio.wait_for(protocol.analyse(board,
    Limit(nodes=_NODES_BUDGET)), timeout=0.05)` inside `pytest.raises((asyncio.TimeoutError,
    TimeoutError))` -> WITHOUT any restart, `await asyncio.wait_for(protocol.analyse(board,
    Limit(depth=6)), timeout=10.0)` on the SAME protocol -> assert the returned InfoDict has a
    usable `score`. Quit the engine in a `finally`. Keep it fast (small second budget).

A3. Decide:
    - If A2 passes -> proceed to STEP B.
    - If A2 hangs, raises, or returns a garbage/None score -> ITEM 2 IS BLOCKED. Do NOT edit
      `engine.py`. Do NOT change the existing timeout test. Delete or `pytest.mark.xfail` the
      probe test with a comment, report ITEM 2 as **blocked with evidence** (paste the observed
      failure + the A1 citations) in the SUMMARY, and ship only Tasks 1 and 3.

STEP B — apply the split (only if A3 says safe).

B1. `app/services/engine.py:_acquire_and_analyse` (L505-514): split the single except tuple into
    two handlers. `except asyncio.TimeoutError:` returns None WITHOUT calling
    `_restart_worker` (the slot still returns to the queue via the existing `finally`).
    `except (chess.engine.EngineError, chess.engine.EngineTerminatedError):` keeps the
    `await self._restart_worker(idx)` call. Put the timeout handler first.

B2. Add the CLAUDE.md bug-fix comment at the fix site: a timeout means the BOX is slow, not that
    the engine is broken; restarting spawned a fresh Stockfish process on an already-slow box
    and plausibly amplified one slow position into a block of holes (FLAWCHESS-8B — measured
    median 25 holes/game, not the 1-2 terminal-only holes the old comments assumed). Note the
    cancellation-safety basis from A1 in one clause so a future reader does not "restore" the
    restart. Keep it terse: this is a comment, not an essay.

B3. Fix the now-stale prose: `_acquire_and_analyse`'s docstring (L485 "timeout, or engine
    crash/restart") and `_restart_worker`'s docstring (L440 "after timeout / crash"). Read
    `app/services/engine.py` for any other docstring/comment that still claims a timeout
    restarts the worker and correct those too.

B4. Update `tests/services/test_engine_nodes.py::TestEvaluateNodesWithMock::test_evaluate_nodes_timeout_returns_none`
    (L116-159): invert the assertion to `assert not restart_called` with a message naming the
    new contract, rename the test to reflect it (e.g. `..._timeout_returns_none_without_restart`),
    and rewrite the docstring. Leave `test_evaluate_nodes_engine_error_returns_none` asserting
    `restart_called` — that contrast IS the test for the split.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_engine_nodes.py tests/services/test_engine.py tests/services/test_engine_pv.py -x -q</automated>
    <automated>MUTATION PROOF (mandatory, only if ITEM 2 ships): re-merge `asyncio.TimeoutError` back into the EngineError except tuple, re-run `uv run pytest tests/services/test_engine_nodes.py -x -q`, and confirm the inverted timeout test FAILS. Restore the split and confirm green. Record the observed failure message in the SUMMARY.</automated>
  </verify>
  <done>Either (a) the A1 citations + a passing real-engine post-cancellation reuse test are recorded, the except tuple is split, the timeout path no longer restarts, `EngineError`/`EngineTerminatedError` still do, stale docstrings are corrected, the inverted test passes, and the mutation proof is recorded — commit `fix(260725-da3): do not restart Stockfish on analyse timeout`; or (b) ITEM 2 is reported BLOCKED with the observed evidence, `app/services/engine.py` is untouched, and Tasks 1 and 3 still ship.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (ITEM 3): worker_id / last_ip on the Path-C Sentry event</name>
  <files>app/routers/eval_remote.py, tests/test_eval_worker_endpoints.py</files>
  <behavior>
    - A Path-C atomic-submit sent with `X-Worker-Id: <id>` produces a Sentry event whose message
      string is byte-identical to
      "atomic-submit: stamping complete after MAX_EVAL_ATTEMPTS with residual holes".
    - That event carries the worker identity: `worker_id` as a Sentry tag, and both `worker_id`
      and `last_ip` reachable from the event's structured data.
    - The pre-existing `eval` context keys (`game_id`, `hole_count`, `attempts`) are unchanged.
    - `tests/services/test_full_eval_drain.py::test_cap_reached_stamps_and_logs` stays green:
      the drain lane still uses `logger.warning` and fires zero Sentry events.
  </behavior>
  <action>
1. `app/routers/eval_remote.py:_report_path_c_capacity_reached` (L1031): add two keyword-only
   params `worker_id: str` and `last_ip: str | None`. Bind them at the call site (L1276) with
   `functools.partial(_report_path_c_capacity_reached, worker_id=worker_id, last_ip=last_ip)` —
   `_apply_atomic_submit` already has both values as parameters (L1055-1056). Do NOT widen the
   shared `on_path_c_capacity_reached: Callable[[int, int, int, str], None]` signature in
   `app/services/eval_apply.py` — the drain lane has no worker identity to supply. If `ty`
   rejects the partial against that Callable type, use a small local closure inside
   `_apply_atomic_submit` instead; do not add a `# ty: ignore`.

2. Inside the reporter: add `worker_id` and `last_ip` to the existing
   `sentry_sdk.set_context("eval", {...})` dict (keep `game_id` / `hole_count` / `attempts`
   exactly as they are), and add `sentry_sdk.set_tag("worker_id", worker_id)` so the dimension
   is filterable in Sentry search alongside the existing `source` tag. The
   `sentry_sdk.capture_message(...)` string MUST NOT change by a single byte — all ~1602
   existing events must keep grouping into FLAWCHESS-8B. Never interpolate a variable into the
   message (CLAUDE.md Sentry grouping rule).

3. Add a comment noting the identity names only the LAST of the up-to-3 attempters (each
   attempt can come from a different worker), so a future reader does not over-trust it for
   attribution; Task 1's per-worker counters are the real attribution mechanism. Keep the
   existing note that `worker_id` is advisory/self-reported (T-123-03) applicable — this is
   telemetry, never an authz input.

4. Extend the existing Path-C test
   `tests/test_eval_worker_endpoints.py::...::test_atomic_submit_holed_batch_at_cap_stamps_with_sentry_warning`
   (L2842) rather than duplicating its ~100-line setup: send an `X-Worker-Id` header, add a
   `set_tag` recorder alongside the existing `set_context` / `capture_message` monkeypatches,
   assert the captured cap message equals the exact expected literal string (a `==` against a
   module-level constant in the test — this is the grouping guard), assert a
   `("worker_id", <that id>)` tag was set, and assert `worker_id` + `last_ip` are present in the
   `eval` context with the expected values (`last_ip` is `_DEFAULT_CLIENT_ADDR[0]`-style
   `"127.0.0.1"` under the ASGI transport). Keep every existing assertion in that test intact.

5. Do NOT write a new drain-lane test — `tests/services/test_full_eval_drain.py::test_cap_reached_stamps_and_logs`
   already asserts `logger.warning` + zero Sentry events on that path. Run it and cite it.
   `app/services/eval_drain.py` must not be modified by this task at all.
  </action>
  <verify>
    <automated>uv run pytest tests/test_eval_worker_endpoints.py -k "cap" -x -q && uv run pytest "tests/services/test_full_eval_drain.py::TestHoleAwareCompletionGate::test_cap_reached_stamps_and_logs" -x -q</automated>
    <automated>MUTATION PROOF (mandatory, two mutations): (a) drop the `worker_id=` binding from the partial (or pass a hardcoded placeholder) and confirm the extended Path-C test FAILS on the tag/context assertion; (b) temporarily convert `eval_drain._log_path_c_capacity_reached` to a `sentry_sdk.capture_message` call and confirm `test_cap_reached_stamps_and_logs` FAILS on its zero-Sentry guard. Restore both, confirm green, and record both observed failure messages in the SUMMARY.</automated>
  </verify>
  <done>The Path-C event carries a `worker_id` tag plus `worker_id`/`last_ip` context; the message string is asserted equal to an exact literal and is unchanged; `eval_apply.apply_completion_decision`'s shared callback signature is unchanged; `eval_drain.py` is untouched and its logger.warning guard test is green; both mutation proofs recorded. Commit: `feat(260725-da3): worker identity on the Path-C capacity Sentry event`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote worker -> `/api/eval/remote/atomic-submit` | Operator-token-authenticated but otherwise untrusted: `X-Worker-Id`, `last_ip`-adjacent headers, eval payloads, and hole counts all originate outside the server. |
| server -> Sentry | Worker-supplied identity leaves the trust boundary into an external SaaS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-da3-01 | Spoofing | `X-Worker-Id` -> `holes_submitted`/`plies_leased`, Sentry `worker_id` tag | low | accept | `worker_id` is self-reported/advisory and already truncated to 16 chars by `worker_id_label`; a spoofing worker can only pollute its own telemetry row. Explicitly never an authz/gate input (T-123-03) — Task 1 and Task 3 both keep the "passive telemetry only" contract, and the counters feed no decision. |
| T-da3-02 | Denial of Service | new counter writes inside the atomic-submit write transaction | low | mitigate | No new statement is issued: the two counters ride the EXISTING `upsert_worker_heartbeat` UPSERT (Task 1 explicitly forbids a second upsert call). The repository's SAVEPOINT-and-swallow keeps a telemetry failure from aborting real eval work. |
| T-da3-03 | Information disclosure | `last_ip` in the Sentry event | low | accept | `last_ip` is an operator-owned worker-machine IP (local box + Hetzner), not an end-user IP — the same negligible-GDPR-surface rationale already recorded on the `worker_heartbeats.last_ip` column comment (D-06). No end-user data is added to the event. |
| T-da3-04 | Tampering | ITEM 2's skipped worker restart leaving a desynced UCI protocol | high | mitigate | Task 2 STEP A is a blocking correctness gate: source-level citation PLUS a real-engine post-cancellation reuse test. If the protocol can desync, ITEM 2 does not ship at all. |

No new packages are installed, so the package-legitimacy gate does not apply.
</threat_model>

<verification>
## FINAL STEP (mandatory, after Task 3 — do not skip, do not run a subset)

Backend-only pre-merge gate per CLAUDE.md and the brief. Dev DB must be up
(`docker compose -f docker-compose.dev.yml -p flawchess-dev up -d`). The per-run test-DB
template auto-refreshes on the new Alembic head, so no manual template rebuild is needed after
Task 1's migration.

```bash
uv run ruff format app/ tests/
uv run ruff check app/ tests/ --fix
uv run ty check app/ tests/        # zero errors required
uv run pytest -n auto -x
```

Frontend lint/tests are NOT needed (no frontend change). If a formatter or autofix step modifies
files, commit that separately with a `style(260725-da3):` / `chore(260725-da3):` prefix.

## Mutation-proof matrix (all shipped items — record the observed failure output in the SUMMARY)

| Item | Mutation | Test that must FAIL |
|------|----------|---------------------|
| 1 | `n_holes=0` in `apply_full_eval`'s heartbeat call | new accumulation test (holes assertion) |
| 1 | `n_plies_leased=0` | new accumulation test (plies_leased assertion) |
| 2 | re-merge `asyncio.TimeoutError` into the EngineError except tuple | inverted `..._timeout_returns_none_without_restart` |
| 3 | drop the `worker_id=` binding from the partial | extended Path-C test (tag/context assertion) |
| 3 | make `eval_drain._log_path_c_capacity_reached` call Sentry | `test_cap_reached_stamps_and_logs` |

Symbol-presence, import-presence, or grep checks are NOT acceptable proof for any row.

## CHANGELOG

If ITEM 2 ships, add exactly ONE terse bullet under `## [Unreleased]` -> `### Fixed` in
`CHANGELOG.md`: a slow analysis worker no longer restarts its engine on a search timeout, which
was turning one slow position into a block of missing evaluations. Items 1 and 3 are internal
telemetry and get no changelog entry (CLAUDE.md quick-task exemption). If ITEM 2 is blocked,
skip the CHANGELOG entirely.

## Do not

- Do not deploy. The user runs `/deploy` separately.
- Do not run `bin/reset_db.sh`.
- Do not touch `_NODES_TIMEOUT_S`, `_NODES_BUDGET`, `MAX_EVAL_ATTEMPTS`, the Path A/B/C tree,
  `scripts/resweep_holed_games.py`, or `scripts/remote_eval_worker.py`.
- Do not convert `eval_drain._log_path_c_capacity_reached` to a Sentry call (FLAWCHESS-5V) —
  except transiently, as the Task 3 mutation proof, and restore it immediately.
</verification>

<success_criteria>
- `worker_heartbeats.holes_submitted` / `plies_leased` exist with `server_default '0'`;
  `uv run alembic check` reports no new upgrade operations.
- A holed atomic-submit moves both counters on EVERY attempt (proven on a Path-B, non-cap
  submit); they accumulate across submits; a hole-free submit leaves `holes_submitted` unchanged.
- ITEM 2 either ships with source-level + real-engine cancellation-safety evidence recorded, or
  is reported BLOCKED with the observed evidence and `app/services/engine.py` untouched.
- The Path-C Sentry event carries `worker_id` (tag) + `worker_id`/`last_ip` (context) with a
  byte-identical message string; the shared `on_path_c_capacity_reached` signature is unchanged;
  the drain lane still uses `logger.warning` with zero Sentry events.
- Every shipped fix has a mutation proof recorded (revert -> observe the specific test fail ->
  restore -> green).
- Full backend pre-merge gate green: ruff format, ruff check --fix, `ty` zero errors,
  `uv run pytest -n auto -x`.
- 3 commits (or 2 if ITEM 2 is blocked), each scoped `(260725-da3)`, plus any formatter commit.
</success_criteria>

<output>
Create `.planning/quick/260725-da3-flawchess-8b-worker-hole-telemetry-no-en/260725-da3-SUMMARY.md` when done.

The SUMMARY must contain:
- The ITEM 2 gate outcome: the python-chess line citations from STEP A1, the real-engine probe
  result from A2, and the SHIP or BLOCKED decision.
- The mutation-proof matrix with the actual failure output observed for each row.
- Anything deferred (the reduced-node-budget retry is already out of scope per the brief).
</output>
