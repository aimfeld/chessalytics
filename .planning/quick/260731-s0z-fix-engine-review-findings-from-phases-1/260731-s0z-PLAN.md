---
quick_id: 260731-s0z
phase: quick-260731-s0z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/hooks/useMaiaEngine.ts
  - frontend/src/hooks/__tests__/useMaiaEngine.test.ts
  - frontend/src/pages/Analysis.tsx
  - frontend/src/pages/__tests__/Analysis.test.tsx
  - frontend/src/lib/engine/workerPool.ts
  - frontend/src/lib/engine/__tests__/workerPool.test.ts
  - frontend/src/hooks/useStockfishEngine.ts
  - frontend/src/hooks/__tests__/useStockfishEngine.test.ts
  - frontend/src/hooks/useFlawChessEngine.ts
  - frontend/src/hooks/__tests__/useFlawChessEngine.test.ts
  - frontend/src/lib/maiaEncoding.ts
  - frontend/src/lib/__tests__/maiaEncoding.test.ts
  - CHANGELOG.md
autonomous: true
requirements: [FIX-1, FIX-2, FIX-3, FIX-4, FIX-5, FIX-6, FIX-7]

must_haves:
  truths:
    - "FIX-1: toggling Maia off while an inference is in flight and back on leaves the hook usable — a later navigation to an uncached FEN issues a real analyze() instead of being dropped at the single-in-flight gate; the spinner (isAnalyzing) and isReady do not survive the disable."
    - "FIX-2: turning the Stockfish switch (or the FlawChess switch) off after an injection latched returns extraRootMoves to the shared NO_EXTRA_ROOT_MOVES sentinel instead of freezing the stale array into every subsequent position's search budget."
    - "FIX-3: a grade() call issued after every pool slot has died resolves with an empty Map instead of hanging forever (useBotGame.ts's signal-less grade() call cannot wedge)."
    - "FIX-4: aborting (or stopAll-ing) an in-flight request on a worker that never sends the terminating bestmove no longer silently loses the slot — a bounded stop-watchdog marks it dead, reports one static Sentry event, and drains pending requests when it was the last live slot."
    - "FIX-5: neither useStockfishEngine nor useFlawChessEngine commits the superseded search's results after its own FEN-change clear — the rapid-navigation window (up to RAPID_STEP_DEBOUNCE_MS) that let old info lines / snapshots / bestmoves land while currentFen already pointed at the new position is closed."
    - "FIX-6: useStockfishEngine commits at most one pvLines snapshot per PV_COMMIT_THROTTLE_MS during a search (first info line still paints immediately, final bestmove flushes unconditionally) instead of re-rendering Analysis.tsx on all ~20-40 info lines."
    - "FIX-7: one Maia inference builds its legal-move context once for the FEN instead of 42 times (21 rungs x 2 movegens), and the batched output is numerically identical to the per-rung maskAndSoftmax / maskAndSoftmaxUci path."
  artifacts:
    - "frontend/src/lib/engine/workerPool.ts: exported STOP_BESTMOVE_WATCHDOG_TIMEOUT_MS + a stop-watchdog arm/fire pair, and a noLiveSlotRemains() guard inside grade()"
    - "frontend/src/hooks/useStockfishEngine.ts: PV_COMMIT_THROTTLE_MS + a trailing-throttled pvLines commit + an immediate stop of the superseded search in the FEN-change effect"
    - "frontend/src/lib/maiaEncoding.ts: buildPolicyMoveContext(fen) + softmaxPolicyByContext(policy, ctx) returning BOTH the SAN-keyed and UCI-keyed distributions from one softmax pass"
    - "Regression tests that fail without each fix, in the five existing test files listed in files_modified"
  key_links:
    - "FIX-2 must preserve the load-bearing identity contract at Analysis.tsx:1109-1118 — every no-op branch returns the SAME NO_EXTRA_ROOT_MOVES reference, because that value feeds useFlawChessEngine's search-restart deps; a fresh [] would restart the search on every render."
    - "FIX-4 changes semantics two existing tests pin (workerPool.test.ts:613 abort, :628 stopAll both assert NO Sentry capture after GRADING_WATCHDOG_TIMEOUT_MS). Both must be rewritten to the re-arm semantics, not deleted. terminate() (test :642) is unaffected — it clears the watchdog and kills the worker."
    - "FIX-4's bestmove stopPending branch (workerPool.ts:551-560) already calls clearSlotWatchdog — that is what disarms the re-armed stop-watchdog on the healthy path. Verify it, do not duplicate it."
    - "FIX-6's trailing timer reads pvMapRef at fire time, so every path that supersedes or ends a search must clear it (FEN change, analyze()'s stop branch, both bestmove branches, worker cleanup) or a stale snapshot lands after the clear — this is the same failure FIX-5 fixes, arriving by a different route."
    - "FIX-7 must keep maskAndSoftmax and maskAndSoftmaxUci as the independent reference implementations the parity tests compare against. Do NOT reimplement either on top of the new batched path (that makes the parity test self-referential) and do NOT delete maskAndSoftmax (knip counts test-file imports as usage — verified: knip is green today and does not report test files as unused)."
---

<objective>
Fix 5 verified bugs and land 2 perf quick-wins found in a post-ship review of phases
194-198 (the frontend engine stack). All seven were verified against the working tree
at `main` @ `dbd1b3f8`; the file/line references in the tasks below are from that tree.

Purpose: three of these (FIX-1, FIX-3, FIX-4) are permanent-wedge bugs — a hook that
never analyzes again, a promise that never settles, a worker slot silently lost — and
two (FIX-2, FIX-5) silently feed one position's data into another position's search or
display. FIX-6/FIX-7 are cheap CPU wins on the same files.

Output: four atomic commits, one per task, each with a regression test that fails
without its fix, plus a CHANGELOG `### Fixed` bullet.

Explicitly out of scope (deliberately deferred findings — do not touch):
`fallbackExpectimax`, `treeCommon`, `maiaQueue`, anything under `reports/`. No
refactoring beyond the seven fixes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

Sources being fixed (read the cited regions, not the whole 3.8k-line page):
@frontend/src/hooks/useMaiaEngine.ts
@frontend/src/hooks/useStockfishEngine.ts
@frontend/src/hooks/useFlawChessEngine.ts
@frontend/src/lib/engine/workerPool.ts
@frontend/src/lib/maiaEncoding.ts
</context>

<verified_facts>
Established by reading the tree before planning — do NOT re-derive:

1. **FIX-2 affects BOTH switches.** `Analysis.tsx:645` passes `fen: engineEnabled ? position : null`
   and `:871` passes `fen: flawChessEnabled ? position : null`. Each hook's FEN effect
   commits `setCurrentFen(fen)`, so a disabled side pins its `currentFen` to `null`
   forever, the step-2b guard at `:1149` returns on every run, and the step-4 sentinel
   reset at `:1167` is unreachable. `flawChessEnabled` is already in the effect deps;
   `engineEnabled` is NOT.
2. **FIX-4 blast radius is exactly two tests.** `workerPool.test.ts:613` (abort) and
   `:628` (stopAll) assert no Sentry capture after `GRADING_WATCHDOG_TIMEOUT_MS`; both
   will now see the stop-watchdog fire at 10s. `:642` (terminate) and `:655` (onerror)
   are unaffected.
3. **FIX-6 does not break the existing single-info-line tests.** `lastPvCommitAt`
   starts at 0 and every one of those tests feeds its first info line at fake t >= 200,
   so `sinceLast > 150` and the first commit stays immediate.
4. **FIX-5's stockfish half is behavior-preserving for existing tests.** For a settled
   navigation `analyze()` already sends the `stop` in the same effect flush; moving it
   into the FEN effect changes no observable message sequence in
   `useStockfishEngine.test.ts:263` or `:309` (the `state === 'thinking'` guard keeps
   the FLAWCHESS-7V "stopping" case a no-op). The real uncovered window is the RAPID
   (debounced) path, where nothing stops the old search for up to 150ms.
5. **FIX-7 enumeration order is safe.** Verified on the installed chess.js 1.4.0:
   `chess.moves({verbose:true})` and the private `chess['_moves']({legal:true})`
   enumerate legal moves in IDENTICAL order (checked on the start position, a
   4-lane promotion FEN, and an en-passant FEN). A context built from the verbose
   call therefore reproduces both existing functions' softmax sums in the same
   summation order.
6. **knip is green today and does not flag test-only exports.** Verified by running
   `npm run knip`: it emits nothing, and it would report every `__tests__` file as
   unused if test files were not entry points. `maskAndSoftmax` losing its production
   call site in FIX-7 is therefore not a knip failure.
</verified_facts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FIX-1 + FIX-2 — Maia lease-cleanup wedge and the frozen extraRootMoves latch</name>
  <files>frontend/src/hooks/useMaiaEngine.ts, frontend/src/hooks/__tests__/useMaiaEngine.test.ts, frontend/src/pages/Analysis.tsx, frontend/src/pages/__tests__/Analysis.test.tsx</files>
  <read_first>frontend/src/hooks/useMaiaEngine.ts (lines 199-360), frontend/src/pages/Analysis.tsx (lines 1109-1187), frontend/src/pages/__tests__/Analysis.test.tsx (lines 30-70, 200-240, 1973-2150)</read_first>
  <behavior>
    FIX-1 (useMaiaEngine):
    - Disabling the hook mid-inference and re-enabling it leaves a later navigation to
      an UNCACHED FEN able to issue a fresh `lease.analyze()` on the new lease.
    - While disabled, `isAnalyzing` is false and `isReady` is false.
    FIX-2 (Analysis.tsx):
    - With an injection latched, flipping `btn-analysis-engine-toggle` off returns
      `extraRootMoves` to `[]` (the shared sentinel), and navigating to another
      position while off keeps it at `[]`.
    - Same for `btn-analysis-flawchess-toggle`.
    - Re-render with the switch off does not change the sentinel's identity.
  </behavior>
  <action>
FIX-1 — `useMaiaEngine.ts`, the worker-lease lifecycle effect's cleanup (currently lines
348-352, `disposed = true; lease.release(); leaseRef.current = null;`). The cleanup nulls
the lease but leaves `pendingFenRef` and `isAnalyzing` set, and leaves `isReady`/
`isReadyRef` true while disabled. Because the in-flight request's rejection handler
(line 261) bails on the `leaseRef.current !== lease` check BEFORE it clears
`pendingFenRef` (line 262), the ref stays non-null forever and every later `analyze()`
returns at the single-in-flight gate (line 228). Extend the cleanup to also reset
`pendingFenRef.current` to null, `setIsAnalyzing(false)`, `setIsReady(false)` and
`isReadyRef.current` to false — i.e. the same four-field reset the `onFatal` callback
(lines 324-331) already performs, for the same reason. Add a comment at the fix site
naming what broke: cleanup released the lease without clearing the in-flight bookkeeping,
so the drop-and-reissue gate stayed permanently closed after a mid-inference disable.
Mirror the precedent in `useStockfishEngine.ts`'s worker cleanup (lines 361-376), which
already resets `isReady`/`isReadyRef`/state on teardown for the analogous reason.

Note in the comment that `useGemSweep.ts` flips this hook's `enabled` dynamically
(`engineEnabled = effectiveEnabled && hasWork`), so it inherits the same fix; it reads
only `perElo`/`resultFen`/`hasFailed`, never `isReady`/`isAnalyzing`, so the added
resets cannot regress the sweep.

FIX-2 — `Analysis.tsx`, the injection effect (lines 1119-1187). Insert a new step
between step (1) latch-reset and step (2) latch-check: when either engine side is
disabled (`!engineEnabled || !flawChessEnabled`), clear `injectedForPositionRef.current`
to null, reset `extraRootMoves` through the SAME identity-preserving updater step (4)
uses (`prev === NO_EXTRA_ROOT_MOVES ? prev : NO_EXTRA_ROOT_MOVES`), and return. Placing
it before the latch check is load-bearing: a latched position must still reset. Clearing
the latch is intentional so that re-enabling can inject afresh; nothing can latch while
disabled because the new branch returns before step (3). Add `engineEnabled` to the
dependency array (`flawChessEnabled` is already there).

Comment at the fix site: with a side disabled its hook receives `fen: null`, so that
hook's `currentFen` is pinned null, the step-2b staleness guard returns on every run,
and the previously latched array kept feeding every subsequent position's search budget.
Keep the existing step numbering readable (label the new block 2a) and do NOT weaken the
2b guard itself — it is the WR-01 fix and still required for the both-enabled case.
  </action>
  <verify>
    <automated>cd frontend && npm test -- src/hooks/__tests__/useMaiaEngine.test.ts src/pages/__tests__/Analysis.test.tsx</automated>
  </verify>
  <done>
Both fixes committed as one commit with these regression tests added:

`useMaiaEngine.test.ts` (new test near the existing `onFatal` block at line 447; reuse
the `FakeLease`/`stubHost`/`driveReady`/`analyzeMessages` harness): advance fake timers
200ms, render with `fen: TEST_FEN, enabled: true`, drive ready, advance so the analyze
is in flight; capture that call; rerender with `enabled: false`; reject the captured
in-flight call (its `reject`); assert `isAnalyzing` is false and `isReady` is false;
rerender with `enabled: true`, drive ready on the NEW `currentLease`, advance timers,
then rerender with `fen: TEST_FEN_2` (uncached) and advance; assert the new lease
received an analyze for `TEST_FEN_2`. Confirm RED first by stashing only the
`useMaiaEngine.ts` change: without it the new lease sees zero analyze calls.

`Analysis.test.tsx` (new tests inside the Phase 196 describe block, after the test at
line 2119): (a) latch an injection exactly as the test at line 2062 does, click
`btn-analysis-engine-toggle`, assert `lastFlawChessCall()?.extraRootMoves` equals `[]`;
then with `clientWidthSpy` mocked at 400 navigate one move (square clicks) and assert it
is still `[]`. (b) the same for `btn-analysis-flawchess-toggle`. (c) assert identity
stability while off: two consecutive reads of `extraRootMoves` across an unrelated
re-render return the same reference. Confirm RED: without the `Analysis.tsx` change the
first assertion sees `['e2e4','g1f3']`.

Commit message: `fix(quick-260731-s0z): clear Maia in-flight state on disable; reset extraRootMoves when an engine side is off`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FIX-3 + FIX-4 — workerPool grade() hang on a fully dead pool, and the wedged 'stopping' slot</name>
  <files>frontend/src/lib/engine/workerPool.ts, frontend/src/lib/engine/__tests__/workerPool.test.ts</files>
  <read_first>frontend/src/lib/engine/workerPool.ts (lines 39-130, 447-500, 549-600, 647-760), frontend/src/lib/engine/__tests__/workerPool.test.ts (lines 34-110, 540-660, 1560-1640)</read_first>
  <behavior>
    - A fresh signal-less `grade()` issued after every slot has died (via `onerror` or a
      watchdog fire) resolves with an empty Map rather than never settling.
    - Aborting an in-flight request on a worker that sends nothing back marks the slot
      dead within `STOP_BESTMOVE_WATCHDOG_TIMEOUT_MS`, reports exactly one static Sentry
      event, and never re-dispatches to that slot.
    - A healthy `stop` that answers with `bestmove` before that bound produces NO Sentry
      event (the stop-watchdog is disarmed on the normal path).
    - When the stop-watchdog kills the LAST live slot, still-pending requests drain empty.
  </behavior>
  <action>
FIX-3 — `grade()` (lines 647-729). Its only zero-capacity guard is `slots.length === 0`
(line 675). Once every slot is `dead` (set by `worker.onerror` at 608-623 or by
`fireWatchdog` at 470-488) a NEW request is enqueued into a queue nothing services:
`dispatchNext` skips non-`isReady` slots and `drainPending` only runs at a death
transition. `useBotGame.ts:1460` calls `.grade(fen, [uci])` with no signal, so this
hangs unconditionally. Immediately after the existing `slots.length === 0` guard, add a
`noLiveSlotRemains()` check that resolves with a new empty Map. Comment: the existing
guard only covers "no slot was ever constructed"; this covers "every constructed slot has
since died", which was reachable and unrecoverable. Order matters — `noLiveSlotRemains()`
requires `slots.length > 0`, which the preceding guard has just established, and both
must sit AFTER `ensureSpawned()` and AFTER the `gradeCache.read()` hit path (a cached
answer must still be served by a dead pool).

FIX-4 — the abort path's in-flight branch (lines 713-721) and the same shape inside
`stopAll()` (lines 731-744). Both call `clearSlotWatchdog` and park the slot in
`'stopping'`, whose only exit is a `bestmove` that a genuinely hung worker never sends:
the slot is lost permanently, is NOT marked `dead` (so it also blinds
`noLiveSlotRemains()` and FIX-3's new guard), and nothing reaches Sentry. Add:

- An exported constant `STOP_BESTMOVE_WATCHDOG_TIMEOUT_MS` of `10_000` in the tunable
  constants block next to `GRADING_WATCHDOG_TIMEOUT_MS`, with a doc comment explaining
  that a `stop` must produce `bestmove` near-immediately (the search polls the stop flag
  between nodes), so this bound is an order of magnitude tighter than the 60s grading
  watchdog, and that a false positive costs one permanently dead slot — Sentry-visible,
  and the constant is the tuning knob if that is ever observed.
- `armStopWatchdog(slot)`: clears any existing timer via `clearSlotWatchdog`, then sets
  `slot.watchdogTimer` to a timeout firing `fireStopWatchdog(slot)`. Reusing
  `watchdogTimer` (rather than adding a second field) is deliberate — the two bounds are
  mutually exclusive by slot state, and every existing exit path already clears that one
  field.
- `fireStopWatchdog(slot)`: nulls `slot.watchdogTimer`, captures a STATIC Error
  (`'Stockfish worker pool: stop-bestmove watchdog timeout'`, no interpolated FEN/UCI —
  CLAUDE.md Sentry grouping rule) with the `source: 'stockfish-worker-pool'` tag, sets
  `isReady = false` and `dead = true`, defensively settles and nulls `slot.current`, and
  runs the same `if (noLiveSlotRemains()) drainPending()` tail `fireWatchdog` uses. Note
  in a comment that it deliberately mirrors `fireWatchdog` rather than sharing a body:
  the two differ in whether a `stop` still needs sending and in the Sentry message, and
  reusing `dead` (instead of a new lifecycle state) is the same choice D-06 already made.
- Replace the bare `clearSlotWatchdog(slot)` call in the abort in-flight branch and in
  `stopAll()`'s thinking branch with `armStopWatchdog(slot)`.

Then verify (do not duplicate) that the `bestmove` handler's `stopPending` branch (lines
551-560) clears the re-armed timer — it calls `clearSlotWatchdog` first, so the healthy
path disarms it. Same for `terminate()` (line 756). Add a short comment at the
`stopPending` branch recording that it is now also the disarm point for the stop-watchdog.
Leave `slot.stopPending` alone in `fireStopWatchdog` — `dead` is the dispatch gate, and a
late `bestmove` on a dead slot is already harmless.
  </action>
  <verify>
    <automated>cd frontend && npm test -- src/lib/engine/__tests__/workerPool.test.ts</automated>
  </verify>
  <done>
One commit containing both fixes and these test changes (import
`STOP_BESTMOVE_WATCHDOG_TIMEOUT_MS` alongside the existing `GRADING_WATCHDOG_TIMEOUT_MS`
import):

1. NEW (FIX-3): spawn the pool with one `grade()`, `simulateError()` every created
   worker, await that first promise empty, then issue a FRESH signal-less
   `grade(TEST_FEN_2, ['d7d5'])`. Assert settlement WITHOUT relying on the suite
   timeout: attach a `.then` that flips a local `settled` flag, `await
   vi.advanceTimersByTimeAsync(GRADING_WATCHDOG_TIMEOUT_MS)`, assert `settled` is true,
   then assert the resolved value equals an empty Map. (Without the fix the flag stays
   false and the test fails fast instead of hanging.)
2. REWRITE the test at line 613 ('an abort of an in-flight request clears its
   watchdog...') to the re-arm semantics, keeping its intent visible in the name: after
   `controller.abort()` and awaiting the promise, assert no Sentry capture yet; advance
   past `STOP_BESTMOVE_WATCHDOG_TIMEOUT_MS`; assert exactly one capture whose message is
   the static stop-watchdog string with the `stockfish-worker-pool` tag; then assert the
   dead slot is never re-dispatched to, using the pattern already at lines 564-575 (a
   later `grade()` is serviced by a DIFFERENT worker and the dead one never receives
   `position fen TEST_FEN_2`).
3. REWRITE the `stopAll()` test at line 628 the same way (same failure shape, same
   assertions).
4. NEW: keep a healthy-path guard — abort an in-flight request, then have the worker
   send its terminating `bestmove` BEFORE the stop bound, advance past
   `STOP_BESTMOVE_WATCHDOG_TIMEOUT_MS`, assert zero Sentry captures.
5. NEW (FIX-3 + FIX-4 composed): dispatch one request per slot sharing ONE
   `AbortController` (4 slots under `stubDesktopSizing`), `driveInit` each worker,
   `controller.abort()`, advance past the stop bound so every slot dies, then assert a
   fresh signal-less `grade()` resolves empty using the `settled`-flag pattern from (1).
6. Leave the `terminate()` test (line 642) and the `onerror` tests (655, 1613, 1628)
   unchanged — they must still pass untouched.

Commit message: `fix(quick-260731-s0z): resolve grade() empty on a fully dead pool; bound the stopping-slot wait with a stop-bestmove watchdog`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: FIX-5 + FIX-6 — stale post-clear commits in both engine hooks, and per-info-line re-render throttling</name>
  <files>frontend/src/hooks/useStockfishEngine.ts, frontend/src/hooks/__tests__/useStockfishEngine.test.ts, frontend/src/hooks/useFlawChessEngine.ts, frontend/src/hooks/__tests__/useFlawChessEngine.test.ts</files>
  <read_first>frontend/src/hooks/useStockfishEngine.ts (lines 84-145, 146-260, 262-380), frontend/src/hooks/useFlawChessEngine.ts (lines 142-330), frontend/src/hooks/__tests__/useStockfishEngine.test.ts (lines 44-60, 193-260, 263-352), frontend/src/hooks/__tests__/useFlawChessEngine.test.ts (lines 25-160)</read_first>
  <behavior>
    FIX-5 (useStockfishEngine): after a RAPID (debounced) FEN change, an info line from
    the OLD search does not commit into `pvLines`/`evalCp`, and the old search's
    `bestmove` is discarded; exactly one search ends up running for the new position.
    FIX-5 (useFlawChessEngine): a RAPID FEN change aborts the previous run's signal
    immediately (not up to `RAPID_STEP_DEBOUNCE_MS` later) and the previous run's pending
    trailing snapshot commit never lands after the clear.
    FIX-6 (useStockfishEngine): the FIRST info line of a search commits immediately; two
    more inside the throttle window produce ONE trailing commit carrying the latest data;
    a non-stale `bestmove` commits immediately without waiting on the throttle.
  </behavior>
  <action>
FIX-5, stockfish half — `useStockfishEngine.ts` FEN-change effect (lines 159-192). Today
nothing stops the superseded search on the debounced path, so for up to
`RAPID_STEP_DEBOUNCE_MS` the OLD search's info lines and possibly its `bestmove` commit
into state while `currentFen` already equals the new position: the documented `currentFen`
contract (lines 63-73) is violated, the previous position's arrow flashes, and a
`bestmove` landing in that window can feed the Analysis injection effect a pairing its
WR-01 guard cannot see (the guard checks `currentFen === position`, which holds). In the
effect, before the state clears, read `workerRef.current` and — only when the worker
exists, `isReadyRef.current` is true, and `stateRef.current === 'thinking'` — post
`'stop'`, set `stopPendingRef.current = true` and `stateRef.current = 'stopping'`. This is
exactly what `analyze()`'s busy branch (lines 211-218) does; reuse that existing Layer B
discard machinery and add NO new re-analyze path — the `bestmove` + `stopPending` handler
(lines 333-345) already re-analyzes `currentFenRef.current`, and `analyze()`'s
`'stopping'` early-return (lines 220-229, FLAWCHESS-7V) absorbs the debounced call that
arrives in the meantime. Comment the accepted interleaving: if the stale `bestmove` lands
before the debounce fires, the discard path starts the new search and the later debounced
`analyze()` stops-and-restarts that same position once — wasteful but correct, and never a
search on the wrong position. Guarding on `'thinking'` (never `'stopping'`) is required:
a second `stop` while one is in flight is the FLAWCHESS-7V hazard shape.

FIX-6 — same file, `commitPvSnapshot` (lines 274-294) currently runs on every info line
(~20-40 per search), each one re-rendering the 3.8k-line Analysis page. Apply the trailing
throttle `useFlawChessEngine.ts` already uses (lines 225-242): immediate commit when the
last commit is older than the window, otherwise exactly one rescheduled trailing commit.
Add `PV_COMMIT_THROTTLE_MS = 150` to the constants block with a comment that it is
numerically equal to `RAPID_STEP_DEBOUNCE_MS` but a DISTINCT mechanism (one throttles
output, the other debounces input — do not conflate them; same warning
`useFlawChessEngine.ts` carries at lines 27-33). Add `pvCommitTimerRef` and
`lastPvCommitAtRef` to the refs block, and a stable empty-dep `clearPendingPvCommit`
callback declared ABOVE the FEN-change effect so every caller can reach it. Keep the
existing snapshot builder as the immediate commit (rename it to make the pair obvious,
e.g. `commitPvSnapshotNow`) and wrap it in a throttled entry point that the info-line
branch calls. Let the trailing callback re-read `pvMapRef` at fire time rather than
capturing a snapshot object.

Establish and comment this invariant: the pending trailing commit is cleared by every
path that supersedes or ends the current search — the FEN-change effect, `analyze()`'s
`'thinking'` stop branch, the `bestmove` `stopPending` discard branch, the final
`bestmove` (which clears then commits immediately, an unconditional flush so the final
snapshot is never delayed or dropped), and the worker-lifecycle cleanup. Without that, a
trailing timer can fire between the stop and `analyze()`'s `pvMapRef.current.clear()` and
commit the previous position's lines — the same defect FIX-5 fixes, arriving by a
different route.

FIX-5, flawchess half — `useFlawChessEngine.ts` FEN-change effect (lines 191-216). The old
`mctsSearch` is not aborted until the debounced search effect (lines 246-259) or the
abort-on-disable cleanup (lines 325-327) runs, which on the rapid path is up to
`RAPID_STEP_DEBOUNCE_MS` later; and the onSnapshot throttle's `pendingTimerRef` is only
cleared inside that same search effect (lines 268-271), so a trailing commit of the OLD
search's snapshot can land after `setSnapshot(INITIAL_SNAPSHOT)`. In the FEN effect, call
`abortControllerRef.current?.abort()` and clear `pendingTimerRef` (nulling the handle).
Keep it minimal: do NOT add a `pool.stopAll()` call here (ABORT-01 already forwards the
signal into `providers.grade`, and an extra stop would now also arm stop-watchdogs from
Task 2), and leave `lastCommitAtRef` to the search-trigger effect that already resets it.
Comment what broke and why the abort belongs in the FEN effect rather than only in the
debounced one.

Do not change `isSearching` semantics in either hook — the pre-existing behavior where a
`fen: null` transition leaves `isSearching` true is out of scope for this task.
  </action>
  <verify>
    <automated>cd frontend && npm test -- src/hooks/__tests__/useStockfishEngine.test.ts src/hooks/__tests__/useFlawChessEngine.test.ts</automated>
  </verify>
  <done>
One commit with all three fixes and these tests; every existing test in both files still
passes untouched.

`useStockfishEngine.test.ts`:
- NEW (FIX-5): `vi.advanceTimersByTime(200)`, render with `TEST_FEN`, `driveInit`, so the
  settled path sends the `go` immediately. Feed one exact info line and assert `evalCp`
  painted. Then `rerender({ fen: TEST_FEN_2 })` WITHOUT advancing timers (so `sinceLast`
  is 0 and the RAPID path is taken), assert `'stop'` was sent, feed another info line
  from the old search, and assert `pvLines` is empty and `evalCp` is null while
  `currentFen` is `TEST_FEN_2`. Then feed the old `bestmove` and assert `pvLines` is
  still empty and that `position fen TEST_FEN_2` is subsequently sent exactly once with
  exactly two `go ` commands total. Confirm RED: without the fix the post-change info
  line commits and `pvLines` has one entry.
- NEW (FIX-6): after the first immediate commit, feed a second info line with a different
  score and assert `evalCp` still shows the FIRST value; advance
  `PV_COMMIT_THROTTLE_MS`; assert it now shows the second. Then feed a third info line
  (throttled, pending) followed immediately by `bestmove` and assert the third value is
  visible with no timer advance (unconditional flush) and `isAnalyzing` is false.

`useFlawChessEngine.test.ts` (reuse the existing `mockMctsSearch`/`makeSnapshot` harness):
- NEW (FIX-5): `vi.advanceTimersByTime(200)`, render with `TEST_FEN` (one `mctsSearch`
  call), capture `onSnapshot` (arg index 3) and the signal (arg index 4). Fire
  `onSnapshot(s1)` (immediate commit) then `onSnapshot(s2)` (schedules the trailing
  commit). `rerender({ fen: TEST_FEN_2 })` WITHOUT advancing timers (RAPID path) and
  assert the first signal is already `aborted`. Then advance 150ms (which also fires the
  FEN debounce) and assert `rankedLines` is EMPTY — not `s2.rankedLines` — and that a
  second `mctsSearch` was issued for `TEST_FEN_2`. Confirm RED: without the fix the
  signal is not aborted at the assertion point and `s2` commits after the clear.

Commit message: `fix(quick-260731-s0z): stop superseded searches on FEN change and throttle Stockfish pv commits`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: FIX-7 — hoist the rung-invariant movegen out of the 21-rung Maia result build; final gate</name>
  <files>frontend/src/lib/maiaEncoding.ts, frontend/src/lib/__tests__/maiaEncoding.test.ts, frontend/src/hooks/useMaiaEngine.ts, CHANGELOG.md</files>
  <read_first>frontend/src/lib/maiaEncoding.ts (lines 204-330), frontend/src/hooks/useMaiaEngine.ts (lines 115-142), frontend/src/lib/__tests__/maiaEncoding.test.ts (lines 130-230)</read_first>
  <behavior>
    - `buildPolicyMoveContext(fen)` is called ONCE per `buildMaiaResult` call, and the
      per-rung work is a pure softmax over precomputed indices.
    - For every rung, the batched SAN map equals `maskAndSoftmax(policy, fen)` and the
      batched UCI map equals `maskAndSoftmaxUci(policy, fen)`, key-for-key and
      value-for-value at the file's established `toBeCloseTo(..., 10)` precision.
    - Castling, en passant, all four underpromotion lanes, and black-to-move mirroring
      behave identically to the existing per-rung path.
  </behavior>
  <action>
`useMaiaEngine.ts`'s `buildMaiaResult` (lines 132-142) calls both `maskAndSoftmaxUci` and
`maskAndSoftmax` per ELO rung: 21 rungs means 42 `new Chess(fen)` constructions and 42
full legal-move generations (21 of them producing SAN, the expensive path JANK-01
avoided elsewhere) for ONE FEN whose legal-move set, vocab indices and UCI/SAN keys are
rung-INVARIANT. Only the logits differ per rung.

In `maiaEncoding.ts`, add next to the existing masking functions:
- An exported `PolicyMoveContext` interface holding three parallel same-length arrays in
  legal-move enumeration order: the flat policy-vocab index per move, the UCI key, and
  the SAN key. Document that the arrays are index-aligned and that the enumeration order
  is the contract.
- `buildPolicyMoveContext(fen)`: one `new Chess(fen)`, one `chess.moves({ verbose: true })`,
  and one pass filling the three arrays. Apply the SAME black-to-move `mirrorSquare`
  translation on `from`/`to` before `moveVocabIndex` that both existing functions apply,
  and build the UCI key as from + to + optional promotion (identical to
  `maskAndSoftmaxUci`'s form). Document that the verbose call is used deliberately
  because SAN is needed once anyway, and that chess.js 1.4.0 enumerates
  `moves({verbose:true})` and the private `_moves({legal:true})` in identical order
  (verified) so the softmax summation order — and therefore the arithmetic — matches
  both reference implementations.
- `softmaxPolicyByContext(policy, ctx)`: gathers scores from `policy` at the context's
  indices (same `?? Number.NEGATIVE_INFINITY` fallback), runs ONE numerically-stable
  softmax (max, `exp(s - max)`, sum reduced in array order — identical arithmetic to the
  existing functions), and returns both the SAN-keyed and UCI-keyed records from that
  single pass. Handle the empty-legal-move (terminal position) case exactly as today:
  both records empty, no NaN.

You MAY extract a shared private softmax-over-scores helper used by all three functions
provided the operation order is preserved exactly; otherwise leave the existing two
functions byte-identical. `maskAndSoftmax` and `maskAndSoftmaxUci` MUST remain exported
and unchanged in behavior — they are the independent reference implementations the parity
tests compare against, and reimplementing either on top of the new path would make those
tests self-referential. `maskAndSoftmax` loses its only production call site here; that
is not a knip failure (verified) so do not delete it and do not add a knip ignore.

Then rewrite `buildMaiaResult` to build the context once and run one
`softmaxPolicyByContext` per rung, feeding the UCI map to `setCachedPolicy(fen, elo, ...)`
and the SAN map to `moveProbabilities`. Keep the existing doc comment's load-bearing
points intact (the write-through is keyed on the RESULT's own `msg.fen`, never the hook's
current prop — 163-REVIEW WR-03) and extend it with the hoist rationale.

Finally: append a `### Fixed` bullet group to `CHANGELOG.md`'s `## [Unreleased]` section
covering this task set in terse user-facing terms (the Maia panel no longer stops
updating after toggling it off mid-analysis; the analysis board no longer reuses one
position's engine hints on later positions; the engine recovers instead of hanging when
its Stockfish workers die or stop responding; the board no longer briefly shows the
previous position's engine line), referencing `quick 260731-s0z`. No `### Changed`
entry is needed for FIX-6/FIX-7 — they are internal CPU wins with no visible behavior
change; mention them only if you judge the smoother live-eval painting user-visible.
  </action>
  <verify>
    <automated>cd frontend && npm test -- src/lib/__tests__/maiaEncoding.test.ts src/hooks/__tests__/useMaiaEngine.test.ts src/lib/engine/__tests__/maiaQueue.test.ts</automated>
    <automated>cd frontend && npm run lint && npx tsc -b && npm run knip && npm test</automated>
  </verify>
  <done>
One commit containing the encoding change, the hook rewrite, the tests and the CHANGELOG
bullet.

Tests added to `maiaEncoding.test.ts` in a new `describe('buildPolicyMoveContext / softmaxPolicyByContext')`
block, reusing the existing `nonUniformPolicy` generator and the `PROMOTION_FEN` /
`CASTLE_FEN` / `EN_PASSANT_FEN` / `BLACK_TO_MOVE_FEN` fixtures:
- For each of those FENs plus `START_FEN`: build the context once, run
  `softmaxPolicyByContext`, and assert the SAN map matches `maskAndSoftmax(policy, fen)`
  and the UCI map matches `maskAndSoftmaxUci(policy, fen)` — sorted key sets equal and
  every value `toBeCloseTo(..., 10)` (the precision this file already uses).
- One test on a deliberately tricky FEN with a promotion available AND en passant
  available AND black to move, asserting the same two-way parity — this is the case that
  exercises the mirroring, the underpromotion lanes and the en-passant move together.
- One test proving the batching is real: assert that a single context reused across
  several DIFFERENT policy tensors still reproduces the per-rung reference output for
  each of them (the rung-invariance claim the whole fix rests on).
- One test that a checkmate/stalemate FEN yields empty maps with no NaN.

`useMaiaEngine.test.ts`: the existing CACHE-05 write-through tests (lines 407-443) must
pass unchanged — they are the proof the UCI-keyed cache write still receives UCI keys per
rung after the rewrite. Do not weaken their regex.

Full frontend gate green: `npm run lint`, `npx tsc -b`, `npm run knip`, and the whole
`npm test` suite (all four tasks' tests together).

Commit message: `perf(quick-260731-s0z): build the Maia legal-move context once per FEN instead of per ELO rung`.
  </done>
</task>

</tasks>

<security>
No new trust boundary, no network surface, no user input path, no dependency added: all
seven changes are client-side control-flow/lifecycle fixes inside already-shipped
modules. One security-adjacent note worth keeping honest: FIX-4 adds a Sentry capture on
a new code path — its Error message must stay STATIC with no FEN/UCI/position
interpolation (CLAUDE.md Sentry grouping rule), which the Task 2 acceptance criteria
assert directly.
</security>

<verification>
Frontend-only change — no backend gate (`ruff`/`ty`/`pytest` are not involved).

Per task: the affected test files pass (each task's `<verify>` block). Final task also
runs the full frontend gate:
```
cd frontend && npm run lint
cd frontend && npx tsc -b     # npm run lint and npm test do NOT type-check
cd frontend && npm run knip   # CI fails on dead exports
cd frontend && npm test       # whole suite
```

Each fix must be proven by REVERTING it and watching the new test fail (see the project
memory on mutation-testing gap closures — grep/symbol presence is not proof). Note the
two tests in Task 2 that are UPDATED rather than added: their old expectation is the
thing being changed, so for those the RED proof is the reverse direction — with FIX-4
reverted the NEW expectation (a capture after the stop bound) must fail.

Not covered by the automated suites (mock Workers, jsdom):
1. FIX-4's false-positive risk — a real slot going dead because a healthy worker took
   >10s to answer a `stop`. Watch Sentry for `Stockfish worker pool: stop-bestmove
   watchdog timeout` after deploy; a stream of these means the constant is too tight,
   not that workers are hanging.
2. FIX-6's actual re-render reduction on `/analysis` (React DevTools profiler during a
   live search) — the throttle's behavior is unit-tested, the render-count win is not.
3. FIX-7's wall-clock win per Maia inference (~42 movegens removed) — the parity is
   unit-tested, the timing is not measured here and no timing claim should be made in
   the SUMMARY beyond the movegen count.
</verification>

<success_criteria>
- FIX-1: a Maia disable mid-inference followed by re-enable and navigation issues a real
  analyze; `isAnalyzing`/`isReady` do not survive the disable.
- FIX-2: `extraRootMoves` returns to the shared sentinel when either engine switch is
  off, with the sentinel's identity contract intact and `engineEnabled` in the deps.
- FIX-3: `grade()` on a fully dead pool resolves empty.
- FIX-4: a never-answering `stop` marks the slot dead inside
  `STOP_BESTMOVE_WATCHDOG_TIMEOUT_MS` with one static Sentry event, drains pending
  requests when it was the last live slot, and the healthy `stop` path still reports
  nothing.
- FIX-5: no stale info line / snapshot / bestmove commits after either hook's FEN-change
  clear, on the rapid path as well as the settled one.
- FIX-6: at most one `pvLines` commit per `PV_COMMIT_THROTTLE_MS`, first paint still
  immediate, final bestmove flushed unconditionally.
- FIX-7: one legal-move context per FEN, numerically identical to the per-rung path on
  castling, en passant, all four promotion lanes and black-to-move.
- Every bug fix carries a comment at the fix site explaining what broke (CLAUDE.md) and a
  regression test that fails when the fix is reverted.
- No magic numbers: `STOP_BESTMOVE_WATCHDOG_TIMEOUT_MS` and `PV_COMMIT_THROTTLE_MS` are
  named, documented constants.
- Four atomic commits; full frontend gate green (`lint`, `tsc -b`, `knip`, `test`);
  no changes to `fallbackExpectimax`, `treeCommon`, `maiaQueue` or `reports/`.
</success_criteria>

<output>
Commit each task separately. Write
`.planning/quick/260731-s0z-fix-engine-review-findings-from-phases-1/260731-s0z-SUMMARY.md`
when done, recording per fix: what broke, the one-line mechanism of the fix, and the test
that pins it.
</output>
