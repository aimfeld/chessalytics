---
phase: quick
plan: 260803-iv6
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/train/TrainSolveScreen.tsx
  - frontend/src/components/train/TrainReveal.tsx
  - frontend/src/lib/trainGuessLabels.ts
  - frontend/src/components/train/TrainScheduleSettings.tsx
  - frontend/src/components/train/__tests__/TrainSolveScreen.test.tsx
  - frontend/src/components/train/__tests__/TrainReveal.test.tsx
  - frontend/src/components/train/__tests__/TrainScheduleSettings.test.tsx
autonomous: true
requirements: []

estimate:
  tokens: 84000
  raw_tokens: 65000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "The Train solution screen shows a live Stockfish eval bar to the right of the board, in the same position and styling as the analysis board's Stockfish bar"
    - "The eval bar tracks the board while stepping a line inside a reveal card"
    - "The eval bar tracks the board in free-move (exploration) mode"
    - "No eval bar is shown before the user has committed a guess and played a move (it would leak the position's evaluation)"
    - "The board does not change size when the reveal opens (the bar's column is reserved from the start)"
    - "When a 'Flaw fixed!' banner is shown, it is the FIRST card in the reveal panel, above the Your-move box and the guess card, on both mobile and desktop"
    - "The guess-feedback card shows one prose sentence stating what the guess verdict means, using the exact wording locked below"
    - "The heading above the Train schedule QR code reads 'Reminders work better with FlawChess on your phone'"
  artifacts:
    - path: "frontend/src/components/train/TrainSolveScreen.tsx"
      provides: "Live Stockfish eval bar beside the Train board, fed by useStockfishEngine when not exploring and by the free-play engine while exploring"
    - path: "frontend/src/components/train/TrainReveal.tsx"
      provides: "Flaw-fixed banner hoisted to the top of the reveal panel; guess-card prose feedback line"
    - path: "frontend/src/lib/trainGuessLabels.ts"
      provides: "guessFeedbackProse() — single source of truth for the five guess-verdict sentences"
    - path: "frontend/src/components/train/TrainScheduleSettings.tsx"
      provides: "Reworded phone-section heading above the QR code"
  key_links:
    - "TrainSolveScreen.displayFen -> EvalBar: the SAME FEN the ChessBoard renders drives the bar in all three states (pristine solution, stepped line, free play)"
    - "useStockfishEngine is disabled while freePlay.isExploring so exactly one FEN-driven Stockfish worker exists at a time (free play already owns one)"
    - "SolveResponse.puzzle_type !== 'herring' is the played-game predicate for the prose — already on the client, no backend change"
---

<objective>
Four independent frontend changes to the Train UI:
1. A live Stockfish eval bar beside the board, matching the analysis board.
2. The "Flaw fixed!" banner moved to the top of the reveal card stack.
3. Prose feedback in the guess-feedback card.
4. Reworded heading above the QR code in the Train schedule card (added mid-flight by the user).

Purpose: the solution screen states verdicts but never shows the position's evaluation, so the user cannot see what the lines are actually worth while stepping them; the mastery celebration is buried three cards down; and the guess card's header states the guess but never says WHY it was right or wrong.
Output: updated `TrainSolveScreen.tsx` (eval bar), `TrainReveal.tsx` (banner order + prose), a new `guessFeedbackProse()` in `lib/trainGuessLabels.ts`, and unit tests for all three.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@frontend/src/components/train/TrainSolveScreen.tsx
@frontend/src/components/train/TrainReveal.tsx
@frontend/src/components/analysis/EvalBar.tsx
@frontend/src/hooks/useStockfishEngine.ts
@frontend/src/hooks/useTrainFreePlay.ts
@frontend/src/lib/trainGuessLabels.ts
@frontend/src/types/train.ts
</context>

<interface_context>
Facts verified in the codebase at plan time. Do not re-derive them; do check them still hold.

**`EvalBar`** (`frontend/src/components/analysis/EvalBar.tsx`):
`{ evalCp: number | null; evalMate: number | null; depth: number; flipped?: boolean; className?: string; whiteFraction?: number; testId?: string; accentColor?: string }`.
`evalCp`/`evalMate` are WHITE-POV. A mate only fills the bar when `depth >= 8`. `null`/`null` reads as the 0.5 midpoint. Its root already carries `w-5`; callers override with `className="h-full w-full"`.

**How `Analysis.tsx` positions the Stockfish bar** (the styling to mirror) — desktop tree, ~line 3177:
```
<div className="flex flex-row items-center gap-2">
  ...board...
  <div className="w-5 shrink-0" style={{ height: boardWidth }}>
    <EvalBar ... accentColor={STOCKFISH_ACCENT} className="h-full w-full" />
  </div>
</div>
```
The Stockfish bar is the RIGHT-hand bar, tinted `STOCKFISH_ACCENT` (blue) from `@/lib/theme`. Analysis subtracts the bars' horizontal chrome (`2 x 20px bar + 2 x 8px gap`) from the board's own width; Train has one bar, so `20 + 8 = 28px`.

**`useStockfishEngine({ fen, enabled })` -> `StockfishEngineState`** (exported interface):
`{ evalCp, evalMate, pvLines, depth, isAnalyzing, isReady, currentFen }`. `fen: null` keeps it idle; `enabled: false` never creates the Worker and tears down an existing one. MultiPV=2, `movetime 1500`, adaptive debounce — all already tuned. Do NOT write a new engine wrapper.

**`useTrainFreePlay`** already runs its own `useStockfishEngine({ fen, enabled: isExploring })`. Its `TrainFreePlayState.pvLines` is staleness-guarded (empty unless the lines belong to the shown position); `pvLines[0]` is a `PvLine` (`{ multipv, depth, moves, evalCp, evalMate }`, WHITE-POV) from `@/hooks/uciParser`.

**`TrainSolveScreen` state already present** (no new state needed for the FEN):
- `displayFen = freePlay.isExploring ? (freePlay.fen ?? boardFen) : boardFen` (~line 511) — the exact FEN the `ChessBoard` renders, in all three states. Line stepping reaches it via `onFenChange={setBoardFen}`.
- `showResultRow` (~line 534) — the gate the reveal panel itself uses.
- `flipped`, `boardMaxWidthPx`, `boardRef`, `columnRef`.
- No early `return` exists before line 792, so a new hook call anywhere after `showResultRow` is hook-order-safe.

**`terminalPositionEval(fen)`** from `@/lib/liveFlaw` returns `{ cp, mate } | null` (WHITE-POV, `null` for a non-terminal position). Already imported by `useTrainFreePlay`.

**`SolveResponse`** (`@/types/train`) carries `correct_guess: boolean` and `puzzle_type: 'sharp' | 'soft' | 'herring'`. `TrainReveal` already gates its game footer on `verdict.puzzle_type !== 'herring'`. **No backend or API change is needed for any of the three bullets** — the "came from a played game vs. a red herring" signal is already on the solve response.

**Test harness**: `TrainSolveScreen.test.tsx` installs a global `FakeWorker` that answers the UCI handshake and emits `info`/`bestmove` lines, and mocks `ChessBoard`. A new `useStockfishEngine` instance therefore runs for real against that fake. `TrainReveal.test.tsx` has `makeVerdict()` / `makeGradeResult()` / `makeReveal()` factories.
</interface_context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Live Stockfish eval bar beside the Train board</name>
  <files>frontend/src/components/train/TrainSolveScreen.tsx, frontend/src/components/train/__tests__/TrainSolveScreen.test.tsx</files>
  <read_first>
    - `frontend/src/components/train/TrainSolveScreen.tsx` lines 100-160 (constants), 495-540 (`displayFen`, `showResultRow`), 792-870 (board row render).
    - `frontend/src/pages/Analysis.tsx` around lines 2762-2776 and 3176-3192 (the Stockfish bar node and its desktop placement) — the styling reference.
    - `frontend/src/components/analysis/EvalBar.tsx` (whole file, ~130 lines).
  </read_first>
  <behavior>
    - Before a verdict lands (guess pending, or move played but still grading), no element with `data-testid="train-eval-bar"` exists.
    - Once the reveal opens, `train-eval-bar` exists and its `aria-label` reflects a real engine evaluation.
    - The bar's numbers follow `displayFen`: stepping a reveal line changes the board FEN and the bar re-evaluates the new FEN.
    - While `freePlay.isExploring`, the bar reads the free-play engine's top line instead of running a second search.
    - A checkmate/stalemate position on the board fills (or centres) the bar rather than sitting at the neutral midpoint.
  </behavior>
  <action>
Add a single live eval bar to the right of the Train board, mirroring the analysis board's Stockfish bar.

Module-level additions in `TrainSolveScreen.tsx` (all named constants, no bare numbers in JSX):
- `TRAIN_EVAL_BAR_CHROME_PX = 28` — the horizontal space the bar column plus its gutter takes out of the board row (20px `w-5` bar + 8px `gap-2`). Document that it is the single-bar counterpart of the analysis page's two-bar chrome constant.
- `TRAIN_TERMINAL_EVAL_DEPTH = 99` — synthetic depth for a terminal position, so a decisive mate clears EvalBar's own `depth >= 8` mate gate instead of collapsing to the midpoint. Same device the analysis page uses for its terminal eval.
- A module-level pure helper (NOT exported — knip) that returns `{ evalCp, evalMate, depth }` from `(fen, isExploring, freePlayTop, engine)`, in this precedence: terminal position first (via `terminalPositionEval` from `@/lib/liveFlaw`, mapped to `TRAIN_TERMINAL_EVAL_DEPTH`), then the free-play top `PvLine` when exploring, then the standalone engine's `evalCp`/`evalMate`/`depth`. Keep it flat (guard-clause returns, no nesting past depth 2) so the component body stays shallow per CLAUDE.md.

Inside the component, after `showResultRow` is computed:
- `const showEvalBar = showResultRow;` — the SAME gate the reveal panel uses. Comment WHY it cannot be earlier: an engine evaluation visible while the guess is still open hands the user the answer to the "one critical move vs several fine moves" question the puzzle is asking.
- `const evalBarFen = showEvalBar && !freePlay.isExploring ? displayFen : null;`
- `const evalBarEngine = useStockfishEngine({ fen: evalBarFen, enabled: evalBarFen !== null });` — reuse the existing hook, do not write a new wrapper. Comment that disabling it during exploration is deliberate: `useTrainFreePlay` already owns a FEN-driven Stockfish worker for the explored position, so this gate keeps exactly one such worker alive at a time (the session-scoped grading worker is a third, but is idle once the verdict has landed).
- Read the free-play top line as `freePlay.pvLines[0] ?? null` (`noUncheckedIndexedAccess`), then call the helper to get the bar's reading.

Board row restructure (the `<div ref={boardRef} className="relative w-full">` block):
- Turn `boardRef`'s div into the flex row: `className="flex w-full flex-row items-stretch gap-2"`.
- Move `relative` onto a new inner wrapper holding the `ChessBoard` and the existing "Points: +N" flash: `className="relative min-w-0 flex-1"`. The flash must stay centred on the BOARD, not on board-plus-bar, so it has to live inside this inner wrapper.
- Pass `maxWidth={boardMaxWidthPx - TRAIN_EVAL_BAR_CHROME_PX}` to `ChessBoard`. Leave the column's own `maxWidth`/`--train-col-max` at `boardMaxWidthPx` so the progress bar and the Solution/Analyze/Next row keep spanning the full column (board + bar), exactly as the analysis page does.
- Render the bar's `<div className="w-5 shrink-0">` slot UNCONDITIONALLY, and mount `<EvalBar>` inside it only when `showEvalBar`. Comment that the always-present slot is what keeps the board from resizing mid-puzzle when the reveal opens.
- `<EvalBar evalCp=... evalMate=... depth=... flipped={flipped} accentColor={STOCKFISH_ACCENT} testId="train-eval-bar" className="h-full w-full" />`. `STOCKFISH_ACCENT` comes from `@/lib/theme` — do not hard-code the blue.

`boardRef` still measures board height for `useFitBoardToViewport` because the bar has no intrinsic height and `items-stretch` sizes it from the board; leave the hook call untouched.

There is one render tree here (mobile/desktop differ only by `max-lg:`/`lg:` utilities), so this covers both viewports — verify there is no second copy of the board row before finishing.

Tests in `TrainSolveScreen.test.tsx` (write them before the implementation is finished):
- The bar is absent while the guess buttons are on screen, and present after a graded solve lands.
- The bar's `aria-label` shows a non-neutral evaluation once the FakeWorker's info lines arrive (assert it is not the `0.00` neutral reading, or assert the fill fraction moved off 50%).
- Stepping/board-FEN change: assert the bar survives a `setBoardFen` transition (drive it the way the existing stepping test drives the board) and that the mocked `ChessBoard`'s `position` and the bar are consistent.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/train/__tests__/TrainSolveScreen.test.tsx</automated>
    <automated>cd frontend && grep -n 'testId="train-eval-bar"' src/components/train/TrainSolveScreen.tsx</automated>
    <automated>cd frontend && grep -c 'useStockfishEngine' src/components/train/TrainSolveScreen.tsx  # exactly 2: the import and the one call site</automated>
  </verify>
  <done>A blue-framed vertical eval bar sits immediately right of the Train board once the reveal opens, tracks the board through line stepping and free play, is absent before the move is graded, and the board width does not change when the reveal opens.</done>
</task>

<task type="auto">
  <name>Task 2: Hoist the "Flaw fixed!" banner to the top of the reveal panel</name>
  <files>frontend/src/components/train/TrainReveal.tsx, frontend/src/components/train/__tests__/TrainReveal.test.tsx</files>
  <read_first>
    - `frontend/src/components/train/TrainReveal.tsx` lines 1091-1180 (the panel's render order: Your-move box, guess card, banner).
  </read_first>
  <action>
In `TrainReveal`'s returned panel, move the `{showFlawFixedBanner && <TrainFlawFixedBanner fen={puzzle.fen} />}` line so it is the FIRST child of the panel root — above the Your-move line box and above the guess card, keeping its own numbered section comment with it and renumbering the surrounding section comments so they still read in render order.

Keep three properties intact:
- The banner stays OUTSIDE the `isFreePlayActive` gate and outside the `isExploring` swap (it is pinned throughout exploration, per the existing D-10 note).
- `showFlawFixedBanner`'s condition is unchanged (`puzzle_type !== 'herring' && item_status === 'mastered'`).
- The panel root's `lg:mt-[46px]` top-alignment offset stays on the root div, so the first card still lines up with the top of the board on desktop.

One render tree serves both viewports here, so no mobile/desktop duplicate exists.

Add a test asserting DOM order: with a mastered non-herring verdict, `train-flaw-fixed-banner` precedes both `train-line-box-your-move` and `train-verdict-guess` (compare with `compareDocumentPosition` and `Node.DOCUMENT_POSITION_FOLLOWING`, not by index into a hand-built list).
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/train/__tests__/TrainReveal.test.tsx</automated>
  </verify>
  <done>On a mastered spaced-repetition item the "Flaw fixed!" card renders above every other reveal card on both viewports; a herring or non-mastered item still renders no banner at all.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prose feedback in the guess-feedback card</name>
  <files>frontend/src/lib/trainGuessLabels.ts, frontend/src/components/train/TrainReveal.tsx, frontend/src/components/train/__tests__/TrainReveal.test.tsx</files>
  <read_first>
    - `frontend/src/lib/trainGuessLabels.ts` (whole file, ~16 lines).
    - `frontend/src/components/train/TrainReveal.tsx` lines 1135-1172 (the guess card: header, `showAlsoFine` body).
  </read_first>
  <behavior>
    - guess `critical`, `correct_guess: false` -> the sentence about several moves being fine here.
    - guess `critical`, `correct_guess: true` -> the sentence about having identified the one critical move.
    - guess `several`, `correct_guess: false` -> the sentence about one move being clearly better than the alternatives.
    - guess `several`, `correct_guess: true`, `puzzle_type: 'herring'` -> the "Indeed, ..." sentence.
    - guess `several`, `correct_guess: true`, `puzzle_type: 'sharp'` or `'soft'` -> the "You handled this fine in your game." sentence.
    - `guess === null` -> no prose element rendered at all.
  </behavior>
  <action>
Add an exported pure function to `frontend/src/lib/trainGuessLabels.ts` — it already owns the guess vocabulary, so the sentences belong beside the labels rather than inline in the component:

```
export function guessFeedbackProse(
  guess: Guess,
  correctGuess: boolean,
  fromPlayedGame: boolean,
): string
```

The five return strings must be reproduced EXACTLY as the user wrote them, character for character (straight apostrophes are fine; the strings below contain none):
- `critical` + wrong: Several moves are fine here.
- `critical` + correct: You identified the one critical move.
- `several` + wrong: One move is clearly better than the alternatives.
- `several` + correct + NOT from a played game: Indeed, several moves are fine here.
- `several` + correct + from a played game: You handled this fine in your game.

Use guard-clause returns (max nesting depth 2). Do not add a sixth variant, do not reword, do not append qualifiers.

In `TrainReveal`, derive
`const guessProse = guess !== null ? guessFeedbackProse(guess, verdict.correct_guess, verdict.puzzle_type !== 'herring') : null;`
next to the existing `showAlsoFine` derivation. `verdict.puzzle_type !== 'herring'` is the "from a played game" predicate — the same one the game footer at the bottom of this component already uses; there is no extra API field to fetch.

Render the prose as the FIRST line of the guess card's `CardBody`, above the existing "Also fine" line. The body currently only mounts when `showAlsoFine` — change its condition so it mounts when either the prose or the Also-fine list exists, keeping the existing `<p className="text-sm" data-testid="train-reveal-also-fine">` element and its testid untouched. Give the prose `<p className="text-sm" data-testid="train-verdict-guess-prose">` — `text-sm` is the project floor, do not go smaller.

Do NOT change the guess card's spotlight wiring: `alsoFineSpotlightHandlers`, the `cursor-pointer` class and the `data-spotlight` attribute must all stay gated on `showAlsoFine` alone, so a card that only carries prose remains genuinely inert and does not advertise a hover affordance it has not got.

Tests: a `describe` over `guessFeedbackProse` covering all five combinations by exact string equality, plus a `TrainReveal` render test per branch reading `train-verdict-guess-prose`'s `textContent`, plus one asserting no prose element when `guess` is null.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/train/__tests__/TrainReveal.test.tsx</automated>
    <automated>cd frontend && grep -c 'You handled this fine in your game.' src/lib/trainGuessLabels.ts</automated>
    <automated>cd frontend && grep -c 'One move is clearly better than the alternatives.' src/lib/trainGuessLabels.ts</automated>
  </verify>
  <done>Each of the five guess/verdict/source combinations renders its exact locked sentence in the guess card body; a null guess renders none; the card's spotlight behaviour is unchanged.</done>
</task>

<task type="auto">
  <name>Task 4: Reword the QR-code heading in the Train schedule card</name>
  <files>frontend/src/components/train/TrainScheduleSettings.tsx, frontend/src/components/train/__tests__/TrainScheduleSettings.test.tsx</files>
  <read_first>
    - `frontend/src/components/train/TrainScheduleSettings.tsx` lines 410-425 (the `showQr` / `showPhoneSection` branch comment) and line 511 (the heading itself).
  </read_first>
  <action>
Replace the phone-section heading text at `TrainScheduleSettings.tsx:511`:

- from: `Use FlawChess on your phone`
- to:   `Reminders work better with FlawChess on your phone`

Copy change only. Do NOT touch the surrounding `<p className="mb-1 text-sm font-semibold">` classes, the `showQr` / `showMobileInstallButton` / `showPhoneSection` conditions, or the QR/install-button branches. `text-sm` stays.

Also update the explanatory comment above `showQr` (~line 415) that quotes the old string — it names the section by its heading, so leaving the stale quote there would misdescribe the code.

Update the four assertions in `TrainScheduleSettings.test.tsx` (lines ~417, ~449, ~484, ~500) that match on the old string so they assert the new one. Do not weaken them to substring/regex matches — keep exact-text assertions so the copy stays pinned.

Then grep the whole `frontend/src` tree for any other occurrence of the old string (e.g. an install prompt or PWA component reusing the same copy) and report — but only change this heading unless another occurrence is literally the same QR-code heading.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/train/__tests__/TrainScheduleSettings.test.tsx</automated>
    <automated>cd frontend && grep -rc 'Reminders work better with FlawChess on your phone' src/components/train/TrainScheduleSettings.tsx</automated>
    <automated>cd frontend && ! grep -rn "'Use FlawChess on your phone'\|>Use FlawChess on your phone<" src/ </automated>
  </verify>
  <done>The line above the Train QR code reads "Reminders work better with FlawChess on your phone"; no stale copy of the old string remains in src/ or its tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Client-side rendering only. No new network call, no new API field, no user-supplied input parsed. FENs rendered by the eval bar already flow through `chess.js` validation in the existing board/free-play paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-iv6-01 | Information disclosure | Train eval bar | medium | mitigate | The bar is gated on `showResultRow` — the same gate as the reveal panel. A live evaluation shown before the guess is committed would disclose the answer to the puzzle's own question. Asserted by a test that the bar is absent while the guess buttons are on screen. |
| T-iv6-02 | Denial of service (self) | Stockfish workers | low | mitigate | The new `useStockfishEngine` instance is disabled while `freePlay.isExploring`, so at most one FEN-driven Stockfish worker runs at a time alongside the (idle-at-reveal-time) session grading worker. |
| T-iv6-SC | Tampering | npm installs | n/a | accept | No package is installed by this plan. If that changes, stop and run the package legitimacy gate first. |
</threat_model>

<verification>
Run the full frontend gate from `frontend/`:

```
npm run lint
npm test -- --run
npx tsc -b            # lint+test do NOT type-check (esbuild strips types)
npm run knip          # CI fails on dead exports / unused deps
```

Manual check in the dev app (`/train`, dev clock available if a session is needed):
1. Start a puzzle — confirm no eval bar while the guess buttons are up, and that the board is the same size as after the reveal opens (no resize jump).
2. Commit a guess, play a move — the bar appears right of the board and settles on a real evaluation.
3. Step through a reveal card's line — the bar follows each ply.
4. Drop a move to enter free play — the bar keeps following, and only one search is in flight.
5. Flip the board — the bar's orientation follows it.
6. Check the same flow at a 375px viewport.
</verification>

<success_criteria>
- Live Stockfish eval bar right of the Train board, styled like the analysis board's (`w-5`, `gap-2`, `STOCKFISH_ACCENT`), correct on the solution screen, during line stepping, and in free-move mode.
- No eval bar before the move is graded; board width constant across the reveal transition.
- "Flaw fixed!" banner is the first card in the reveal panel when shown.
- All five guess-prose sentences render verbatim in the guess card body.
- The Train schedule QR heading reads "Reminders work better with FlawChess on your phone".
- `npm run lint`, `npm test -- --run`, `npx tsc -b` and `npm run knip` all pass.
</success_criteria>

<output>
Create `.planning/quick/260803-iv6-puzzle-solution-screen-flaw-fixed-cards-/260803-iv6-SUMMARY.md` when done.
</output>
