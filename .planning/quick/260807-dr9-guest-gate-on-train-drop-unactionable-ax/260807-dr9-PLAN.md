---
phase: quick-260807-dr9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/Train.tsx
  - frontend/src/components/train/TrainGuestGate.tsx
  - frontend/src/pages/__tests__/Train.guestGate.test.tsx
  - frontend/src/pages/__tests__/Train.solveLoop.test.tsx
  - frontend/src/instrument.ts
  - frontend/src/__tests__/instrument.beforeSend.test.ts
  - CHANGELOG.md
autonomous: true
requirements: [DR9-01, DR9-02, DR9-03, DR9-04, DR9-05, DR9-06]

estimate:
  tokens: 48000
  raw_tokens: 30000
  tasks: 2
  confidence: low   # no calibration samples for quick tasks in this repo

must_haves:
  truths:
    - "A guest who reaches /train fires ZERO /train/* requests: no POST /train/sessions, no GET /train/progress, no GET /train/settings — so FLAWCHESS-64's guaranteed 403 can no longer be produced (D-03, D-04)."
    - "A guest on /train sees a sign-up CTA screen whose copy says Train requires a free account, with a PRIMARY (`variant=\"default\"`) button — never `TrainStartScreen` (D-04, D-06)."
    - "Pressing that button calls `logoutForPromotion()` and THEN sets `window.location.href = '/login?tab=register'` — the same promote-in-place pair as `Import.tsx`'s guest promo (D-06)."
    - "Non-guest behavior is unchanged: `startSession()` still fires exactly once per page mount, and the pre-existing `Train.solveLoop.test.tsx` end-to-end tracer still passes (D-03)."
    - "The Train nav item stays visible to guests — `App.tsx` is NOT modified (D-05)."
    - "`is_guest` is read from `useUserProfile()`, never from `useAuth().user` (which is always null and carries no `is_guest`) (D-02)."
    - "`sentryBeforeSend` returns null (drops the event) for an axios error whose `code` is `ERR_NETWORK` or `ERR_CANCELED` when the page is unloading, or `navigator.onLine === false`, or `document.visibilityState === 'hidden'` (D-07)."
    - "`sentryBeforeSend` still RETURNS the event, fingerprinted `api-network-error`, for an `ERR_NETWORK` axios error while the page is foreground and online — the only variant that could signal a real Caddy/host outage (D-08)."
    - "`ECONNABORTED` -> `api-timeout`, 500 -> `api-server-error`, and the 401 drop are all byte-identical in behavior to today (D-08)."
    - "`instrument.ts` touches `window`/`navigator`/`document` only behind `typeof` guards, so the module still evaluates in the prerender environment (D-09)."
    - "Exactly two commits land: the first contains the literal `Fixes FLAWCHESS-64`, the second the literal `Fixes FLAWCHESS-24`, and each is independently green (D-10)."
    - "No file under `app/`, `tests/`, or `alembic/` is modified — the backend `_reject_guest` gate is correct and stays (D-01)."
    - "Reverting the guest gate in `Train.tsx` makes the new guest test FAIL, and reverting the drop branch in `instrument.ts` makes the new beforeSend test FAIL — proven by an actual revert, not by grep or symbol presence (D-11)."
  artifacts:
    - frontend/src/pages/Train.tsx
    - frontend/src/components/train/TrainGuestGate.tsx
    - frontend/src/pages/__tests__/Train.guestGate.test.tsx
    - frontend/src/instrument.ts
    - frontend/src/__tests__/instrument.beforeSend.test.ts
    - CHANGELOG.md
  key_links:
    - "Train.tsx -> useUserProfile() -> canTrain -> the mount effect that calls startSession() (the single 403 source, D-03)"
    - "Train.tsx -> isGuest -> <TrainGuestGate /> INSTEAD of <TrainStartScreen /> (blocks the progress/settings 403s, D-04)"
    - "TrainGuestGate -> useAuth().logoutForPromotion -> window.location.href='/login?tab=register' (promote-in-place, D-06)"
    - "instrument.ts pagehide listener -> module-level isUnloading -> sentryBeforeSend drop branch (D-07)"
---

<objective>
Two independent frontend-only Sentry fixes, landing as two atomic commits.

**FIX 1 — FLAWCHESS-64** (AxiosError 403, 53 events since 2026-06-15): every `/train/*`
handler 403s guests via `_reject_guest` (`app/routers/train.py:54`, Phase 189 D-05 — correct,
and it stays). The frontend lets guests through anyway: `/train` is in `NAV_ITEMS`/
`BOTTOM_NAV_ITEMS` unconditionally (`App.tsx:77,85`) and its route guard is
`ImportRequiredRoute` (`App.tsx:686`), which checks games + tier1 but NOT `is_guest` — and
105 of 304 prod guest accounts have imported games, so they pass it. `TrainPage` then fires
`startSession()` unconditionally on mount (`pages/Train.tsx:68`) → `POST /train/sessions` →
guaranteed 403 → `MutationCache.onError` (`lib/queryClient.ts`) reports it. `TrainStartScreen`
adds two more: `GET /train/progress` (via `useTrainProgress`) and `GET /train/settings` (via
`TrainScheduleSettings`).

**FIX 2 — FLAWCHESS-24** (AxiosError: Network Error, 55 events, fingerprint
`api-network-error`): axios XHR `onerror` means no HTTP response at all. Two
self-inflicted/environmental populations dominate: (a) our own hard navigations aborting
in-flight XHRs — 8 `window.location.href` sites (`api/client.ts:98` 401 redirect,
`hooks/useAuth.ts:167` logout, `auth/LoginForm.tsx:65` + `RegisterForm.tsx:138` Google OAuth,
`Welcome.tsx:115`, `Import.tsx:378`, `EvalCoverageBadge.tsx:113`) plus `main.tsx:21` reload;
(b) iOS Mobile Safari backgrounding / flaky mobile connectivity (9 of the last 15 events).
Confirmed NOT backend-caused: a 5xx returns a response and fingerprints `api-server-error`
instead, and the 2026-07-23..25 event cluster had no production deploys.

**Decision map — every decision below is LOCKED by the user. Do not revisit or redesign.**
- **D-01** Frontend only. No backend changes; `_reject_guest` is correct and stays.
- **D-02** Read `is_guest` from `useUserProfile()`, NOT from `useAuth()` (whose `user` is
  always null and carries no `is_guest`).
- **D-03** Do NOT call `startSession()` while the profile is null (still loading) or
  `profile.is_guest` is true.
- **D-04** For guests render a sign-up CTA screen INSTEAD of `TrainStartScreen` — rendering
  `TrainStartScreen` at all re-triggers the progress/settings 403s.
- **D-05** Keep the Train nav item visible for guests. The CTA is the conversion moment.
  `App.tsx` is not touched.
- **D-06** CTA = a PRIMARY button (`Button variant="default"`, per CLAUDE.md) that promotes
  the guest account in place, mirroring `pages/Import.tsx:374-386`: `logoutForPromotion()`
  from `useAuth`, then `window.location.href = '/login?tab=register'`. Copy says **Train
  requires a free account**. Layout follows `components/library/NoEngineAnalysisFlawsState.tsx`
  (heading + explainer + button) but with the primary variant, not `brand-outline`.
- **D-07** In `instrument.ts`: a module-level `isUnloading` flag set from a `'pagehide'`
  listener; `sentryBeforeSend` DROPS axios errors whose `code` is `ERR_NETWORK` or
  `ERR_CANCELED` when `isUnloading || navigator.onLine === false ||
  document.visibilityState === 'hidden'`.
- **D-08** KEEP sending (with the existing `api-network-error` fingerprint) when the page is
  foreground and online — that variant is the only one that could signal a real Caddy/host
  outage, and it must stay visible because such an outage never reaches the backend's own Sentry.
- **D-09** Guard the browser API access so the module stays safe in the prerender/SSR
  environment (`useAuth.ts:44` is the existing `typeof`-guard precedent).
- **D-10** Two atomic commits. First message contains `Fixes FLAWCHESS-64`, second contains
  `Fixes FLAWCHESS-24`, so Sentry auto-closes them on merge.
- **D-11** Tests: the guest branch of `Train.tsx` (guest → no `POST /train/sessions` and the
  CTA renders; non-guest → session composed exactly as today) and, if it can be done without
  tripping knip, the beforeSend drop/keep matrix.
- **D-12** react-router 8 is in use — import from `'react-router'`, never `'react-router-dom'`
  (migrated in `c56eb5cbb`).

Repo rules that apply to the new UI (CLAUDE.md): `data-testid` on the button AND the
container, `aria-label` on the button, semantic HTML (real `<button>`/`<h2>`), `text-sm`
minimum font size, mobile friendly.

Purpose: stop manufacturing two Sentry issues that are 100% frontend-caused, and turn the
guest 403 into the conversion moment it should have been.
Output: one new small component, a gate in `Train.tsx`, a drop branch in `instrument.ts`,
two new test files, one updated test file, a CHANGELOG bullet, two commits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@frontend/src/pages/Train.tsx
@frontend/src/instrument.ts
@frontend/src/components/library/NoEngineAnalysisFlawsState.tsx
@frontend/src/hooks/useUserProfile.ts
@frontend/src/pages/__tests__/Train.solveLoop.test.tsx
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Guest gate + sign-up CTA on /train (FLAWCHESS-64)</name>
  <files>
frontend/src/components/train/TrainGuestGate.tsx (new),
frontend/src/pages/Train.tsx,
frontend/src/pages/__tests__/Train.guestGate.test.tsx (new),
frontend/src/pages/__tests__/Train.solveLoop.test.tsx,
CHANGELOG.md
  </files>

  <read_first>
frontend/src/pages/Train.tsx (whole file — 193 lines; the mount effect at 66-71 is the
FLAWCHESS-64 source), frontend/src/components/library/NoEngineAnalysisFlawsState.tsx (the
D-06 layout precedent), frontend/src/pages/Import.tsx lines 370-390 (the D-06
`logoutForPromotion()` + `window.location.href` pair), frontend/src/hooks/useAuth.ts lines
170-183 (`logoutForPromotion` semantics), frontend/src/pages/__tests__/Train.solveLoop.test.tsx
lines 145-245 (its `@/api/client` mock spreads `...actual`, so `apiClient` stays the REAL
axios instance — that is why this file must gain a `useUserProfile` mock, see below),
frontend/src/hooks/__tests__/useEvalCoverage.test.tsx lines 32-47 (the repo's
`Object.defineProperty(window, 'location', ...)` precedent).
  </read_first>

  <behavior>
    - Guest (`profile.is_guest === true`) lands on /train: `trainApi.composeOrResumeSession`
      is called ZERO times, `train-guest-gate` renders, `train-start-screen` does NOT render.
    - Guest presses `btn-signup-for-train`: `logoutForPromotion()` is called exactly once and
      `window.location.href` becomes `/login?tab=register`.
    - Non-guest (`is_guest === false`): `composeOrResumeSession` is called exactly once and
      `train-guest-gate` does NOT render.
    - Profile unresolved (query still loading, or errored): `composeOrResumeSession` is called
      ZERO times — the guest status is unknown, so no /train/* request may be issued.
  </behavior>

  <action>
Per D-04/D-06, create `frontend/src/components/train/TrainGuestGate.tsx` — a small named-export
component (the codebase convention for `components/train/*`). It must live in its own file, NOT
inline in `Train.tsx`: it is the only thing that calls `useAuth()`, and keeping that call out of
`Train.tsx` means the provider-free render in the existing `Train.solveLoop.test.tsx` (which
wraps only `MemoryRouter` + `QueryClientProvider`) does not start throwing "useAuth must be used
within an AuthProvider".

Shape it on `NoEngineAnalysisFlawsState.tsx`'s guest branch (D-06): a centered flex column
container carrying `data-testid="train-guest-gate"` with the same class recipe
(`flex min-h-[40vh] flex-col items-center justify-center gap-4 px-4 py-12 text-center`), a
lucide `Dumbbell` icon marked `aria-hidden="true"` (matching the Train nav icon; the precedent
uses `Cpu`), an `<h2 className="text-xl font-semibold text-foreground">` whose copy states that
Train requires a free account, a `<p className="text-sm text-muted-foreground max-w-sm">`
explainer saying a free account turns the user's own blunders into daily spaced-repetition
puzzles and that their imported games carry over, and a `Button` with `variant="default"` (the
PRIMARY brand-brown CTA per D-06 and CLAUDE.md — explicitly NOT `brand-outline`, which is what
the `NoEngineAnalysisFlawsState` precedent uses), `data-testid="btn-signup-for-train"`, an
`aria-label`, and label text `Sign up free`. Its `onClick` calls `logoutForPromotion()` from
`useAuth()` and THEN assigns `window.location.href = '/login?tab=register'` — that exact
ordering and that exact URL, mirroring `Import.tsx:378` (D-06). Do not use `useNavigate` here:
the promotion path deliberately hard-navigates so the register page mounts with the cleared
auth state and the `promote_intent` sessionStorage flag `logoutForPromotion` just set. No
`text-xs` anywhere (CLAUDE.md font floor). Add a short docstring naming FLAWCHESS-64 and why
this screen exists instead of `TrainStartScreen`.

In `frontend/src/pages/Train.tsx`, add the gate. Read the profile with
`const { data: profile, isError: profileError } = useUserProfile()` — from `useUserProfile`,
never `useAuth().user` (D-02; `Analysis.tsx:789` carries the same warning comment). Derive
`const isGuest = profile?.is_guest === true;` and `const canTrain = profile != null && !isGuest;`.

Replace the unconditional mount effect (currently lines 66-71) with a once-per-mount effect
guarded by both `canTrain` and a `useRef` latch, so the "runs once per page mount" contract in
the file's existing comment survives the fact that the effect now has real dependencies: return
early when the latch is already set or `canTrain` is false, otherwise set the latch and call
`startSession()`. Keep `startSession` in the dependency array now that the effect is no longer
a bare mount effect (the latch, not an empty dep array, is what enforces once-only) and drop
the `eslint-disable-next-line react-hooks/exhaustive-deps` if the rule is satisfied. Per
CLAUDE.md's "comment bug fixes" rule, leave a comment at this site recording that every
`/train/*` endpoint 403s guests via `_reject_guest`, so firing this status read for a guest —
or before the profile resolves — is a guaranteed 403 that reaches Sentry as FLAWCHESS-64.

Pass `enabled: canTrain` to `useTrainGradingEngine` instead of the hardcoded `enabled: true`,
so a guest does not spin up the page-level Stockfish Worker (`useTrainGradingEngine.ts:495`
returns before `new Worker(ENGINE_PATH)` when disabled) for a screen that can never grade a
move. This is safe against that hook's "enabled toggled at the SESSION boundary" contract
because `ImportRequiredRoute` blocks rendering until the profile query settles, so `canTrain`
is already final at mount in production.

Add the render branches immediately before the existing `return (`, AFTER every hook call (the
two effects and all `useState` calls stay above them, so no hook runs conditionally). Branch
order: `isGuest` first — return the page container `<div className="px-4 py-6 md:px-6"
data-testid="train-page">` wrapping `<TrainGuestGate />` and nothing else (D-04: reaching
`TrainStartScreen` is what re-fires the `/train/progress` and `/train/settings` 403s). Then
`profile == null` — return the same container with a single `text-sm text-muted-foreground`
paragraph: when `profileError` is true, the CLAUDE.md-mandated error copy ("Failed to load your
profile. Something went wrong. Please try again in a moment.") under
`data-testid="train-profile-error"`; otherwise a loading line under
`data-testid="train-profile-loading"`. The existing `return (...)` becomes the non-guest,
profile-resolved path and is otherwise untouched. Do NOT touch `App.tsx` — the nav entry stays
visible to guests (D-05).

Write `frontend/src/pages/__tests__/Train.guestGate.test.tsx` covering the four
`<behavior>` cases. Open it with the `// @vitest-environment jsdom` pragma (this repo has no
vitest config file — every test file declares its own environment) and a docstring naming
FLAWCHESS-64. Mock `@/hooks/useUserProfile` with a module-level mutable holder so each test
sets `{ data, isError }` before rendering; mock `@/api/client` the same way
`Train.solveLoop.test.tsx` does (`vi.importActual` spread + a `trainApi` object of `vi.fn()`s)
so `composeOrResumeSession` call counts are assertable; mock `@/hooks/useAuth` to return a
`logoutForPromotion` spy so no `AuthProvider` is needed. Stub `globalThis.ResizeObserver` and
`globalThis.Worker` with the same no-op shapes the sibling test uses (the non-guest control
case mounts the real grading engine). Render via a dynamic
`(await import('@/pages/Train')).default` inside `MemoryRouter` + `QueryClientProvider`
(`retry: false` on queries and mutations), mirroring the sibling's `renderTrainPage` helper.
For the zero-call assertions, let the microtask queue and effects flush first (await a
`waitFor` on the CTA/loading testid appearing) and only then assert
`expect(composeOrResumeSession).not.toHaveBeenCalled()` — an assertion made before the effects
run would pass even against the un-gated code and prove nothing. For the navigation assertion,
swap `window.location` with `Object.defineProperty(window, 'location', { configurable: true,
value: { ...originalLocation, href: '' } })` in `beforeEach` and restore it in `afterAll`,
exactly as `useEvalCoverage.test.tsx` does, then assert the href and the
`logoutForPromotion` call count.

Update `frontend/src/pages/__tests__/Train.solveLoop.test.tsx`: it renders `TrainPage` with an
`@/api/client` mock that spreads `...actual`, so `apiClient` is the real axios instance and
`useUserProfile` would never resolve under jsdom — leaving `profile` undefined and the newly
gated `startSession()` permanently unfired, which would break the whole tracer. Add a
`vi.mock('@/hooks/useUserProfile', ...)` returning a settled non-guest profile
(`{ data: { ...is_guest: false }, isError: false }`; the object needs only the fields
`Train.tsx` reads, cast if `UserProfile` requires more) with a one-line comment explaining that
the page is now profile-gated. Do not otherwise change that file's assertions.

Add ONE bullet to `CHANGELOG.md` under `## [Unreleased]` → `### Fixed`, in the existing terse
user-facing voice: guests who open Train now get a sign-up prompt instead of a page that
silently fails. No mention of Sentry issue IDs or internals in the changelog copy.
  </action>

  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/pages/__tests__/Train.guestGate.test.tsx src/pages/__tests__/Train.solveLoop.test.tsx</automated>
    <automated>cd frontend &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npm run knip</automated>
    <automated>test "$(git status --porcelain -- app tests alembic frontend/src/App.tsx frontend/package.json | wc -l)" -eq 0</automated>
  </verify>

  <done>
The four `<behavior>` cases pass; `npm run lint`, `npm run build` (tsc -b) and `npm run knip`
are clean; `git status` shows changes only in the five listed files (no `app/`, `tests/`,
`alembic/`, or `App.tsx` edits). Reverting the `canTrain` guard in the mount effect makes the
guest zero-call test FAIL, and reverting the `isGuest` render branch makes the CTA test FAIL —
verified by actually reverting each, then restoring. Committed as a single commit whose message
contains the literal `Fixes FLAWCHESS-64`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drop unactionable axios network-error noise in beforeSend (FLAWCHESS-24)</name>
  <files>
frontend/src/instrument.ts,
frontend/src/__tests__/instrument.beforeSend.test.ts (new)
  </files>

  <read_first>
frontend/src/instrument.ts (whole file — 53 lines), frontend/src/hooks/useAuth.ts line 44 (the
D-09 `typeof`-guard precedent), frontend/src/main.tsx line 1 (instrument is imported before
everything else, which is why the module must not assume a DOM).
  </read_first>

  <behavior>
    Matrix over an axios-like error (`isAxiosError: true`) with no `response`:
    - code `ERR_NETWORK`, page unloading (a `pagehide` event has fired) -> beforeSend returns null.
    - code `ERR_NETWORK`, `navigator.onLine === false` -> returns null.
    - code `ERR_NETWORK`, `document.visibilityState === 'hidden'` -> returns null.
    - code `ERR_CANCELED`, any one of those three conditions -> returns null.
    - code `ERR_NETWORK`, foreground + online + not unloading -> returns the event, with
      `fingerprint === ['api-network-error']`.
    - code `ERR_CANCELED`, foreground + online -> returns the event (no fingerprint added).
    - `response.status === 401` -> returns null (unchanged).
    - `response.status === 500` -> `fingerprint === ['api-server-error']` (unchanged).
    - code `ECONNABORTED` -> `fingerprint === ['api-timeout']`, even while hidden/offline
      (a timeout means the request was actually attempted; only ERR_NETWORK/ERR_CANCELED are
      suppressible).
    - a non-axios error -> returned untouched.
  </behavior>

  <action>
In `frontend/src/instrument.ts`, add a module-level `let isUnloading = false;` and register a
`'pagehide'` listener that sets it true, per D-07. Guard the registration behind
`typeof window !== 'undefined'` (D-09 — the module is imported first by `main.tsx` and must
still evaluate in the prerender environment; `useAuth.ts:44` is the existing precedent for this
guard style). Use `pagehide` specifically, not `beforeunload`/`unload`: it is the only one that
fires reliably on iOS Safari, which is population (b) of this issue.

Add a small named helper next to it that answers whether the environment is currently in a
state where a response-less XHR failure is expected rather than informative: true when
`isUnloading`, or when `navigator.onLine === false`, or when
`document.visibilityState === 'hidden'` — each browser global read behind its own `typeof`
guard so an undefined global reads as "not suppressible" (fail-open to reporting, never
fail-open to dropping). Name the two suppressible axios codes as a module-level constant array
or a pair of named constants rather than repeating string literals inline (CLAUDE.md: no magic
values).

In `sentryBeforeSend`, inside the existing `isAxiosLikeError(error)` branch, keep the 401 drop
first (unchanged), then add the new drop: when `error.code` is one of the suppressible codes AND
the helper says the environment is suppressible, return null. Everything after it — the 500,
`ECONNABORTED` and `ERR_NETWORK` fingerprint chain — stays exactly as it is, so a foreground,
online `ERR_NETWORK` still ships with the `api-network-error` fingerprint (D-08). Comment the
new branch with what it drops and why (CLAUDE.md "comment bug fixes"): axios XHR `onerror`
means no HTTP response at all, and the two dominant populations are our own 8
`window.location.href` hard navigations aborting in-flight XHRs plus iOS Safari backgrounding —
neither is actionable — while the foreground+online variant is deliberately kept because a real
Caddy/host outage never reaches the backend's own Sentry.

Widen `sentryBeforeSend`'s declared return type to `Sentry.ErrorEvent | null` and drop the two
`null as unknown as Sentry.ErrorEvent` casts (Sentry's `beforeSend` option accepts
`ErrorEvent | null`). If `tsc -b` rejects the widened type against this `@sentry/react`
version, revert to the existing cast style rather than fighting it — the cast is cosmetic and
the drop behavior is what matters.

Export `sentryBeforeSend` so it is testable. Knip is configured with `ignoreExportsUsedInFile:
true` and its vitest plugin treats test files as entry points (that is why the repo's ~40 test
files are not reported as unused), so an export consumed only by a test is expected to pass —
but `npm run knip` is in this task's verify precisely to confirm it. If knip DOES flag it,
fall back to moving the pure predicate + the `isUnloading` flag into a new
`frontend/src/lib/sentryNoise.ts` imported by `instrument.ts`, and test that module instead;
do not delete the test to satisfy knip.

Write `frontend/src/__tests__/instrument.beforeSend.test.ts` covering the full `<behavior>`
matrix. Open it with `// @vitest-environment jsdom` and a docstring naming FLAWCHESS-24. Mock
`@sentry/react` with a stub exposing `init: vi.fn()` and `browserTracingIntegration: vi.fn()`
so importing the module does not really initialize Sentry. Because `isUnloading` is
module-level state, call `vi.resetModules()` in `beforeEach` and load the module per test with
`const { sentryBeforeSend } = await import('@/instrument')` — otherwise the unloading test
poisons every later case. Drive the unloading case by dispatching a real
`window.dispatchEvent(new Event('pagehide'))` (this exercises the actual listener, not a
reimplementation of it). Drive the other two via
`Object.defineProperty(navigator, 'onLine', { configurable: true, value: false })` and
`Object.defineProperty(document, 'visibilityState', { configurable: true, value: 'hidden' })`,
restoring both in `afterEach`. Build the axios-like errors as plain objects with
`isAxiosError: true` and the relevant `code`/`response`, cast at the call site — do not import
axios into this test (the module under test deliberately duck-types for exactly this reason).
Assert `toBeNull()` for drops and `fingerprint` equality for keeps.

No CHANGELOG entry for this task: dropping unactionable Sentry noise is internal, not
user-facing (CLAUDE.md's per-phase changelog rule).
  </action>

  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/__tests__/instrument.beforeSend.test.ts</automated>
    <automated>cd frontend &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npm run knip</automated>
    <automated>cd frontend &amp;&amp; grep -vE '^\s*(//|\*|/\*)' src/instrument.ts | grep -c 'ERR_CANCELED' | grep -qvx 0</automated>
  </verify>

  <done>
Every case in the `<behavior>` matrix passes; `npm run lint`, `npm run build` and
`npm run knip` are clean. Reverting the new drop branch makes the three drop tests FAIL while
the keep tests still pass — verified by actually reverting, then restoring. `git status` shows
changes only in `frontend/src/instrument.ts` and the new test file. Committed as a single
commit whose message contains the literal `Fixes FLAWCHESS-24`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guest browser → `/train/*` API | Untrusted client decides whether to call; the server's `_reject_guest` remains the actual authority |
| browser error events → Sentry ingest | Client-side filter decides what telemetry is retained |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-dr9-01 | Elevation of Privilege | `Train.tsx` guest gate | medium | mitigate | The gate is a UX affordance ONLY. `_reject_guest` (`app/routers/train.py:54`) stays untouched and remains the enforcement point (D-01), so a guest who forges past the client still gets 403 |
| T-dr9-02 | Information Disclosure | `TrainGuestGate` promotion flow | low | accept | `logoutForPromotion()` clears `auth_token` and sets `promote_intent` before navigating — the same already-shipped path as `Import.tsx:378`; no new credential handling is introduced |
| T-dr9-03 | Repudiation | `sentryBeforeSend` drop branch | medium | mitigate | Dropping is scoped to exactly two axios codes AND three environmental conditions; the foreground+online `ERR_NETWORK` variant — the only one that could evidence a real Caddy/host outage invisible to the backend's own Sentry — is explicitly kept (D-08) and asserted by test |
| T-dr9-04 | Tampering | npm/pip/cargo installs | high | mitigate | No new dependency is added by either task; `package.json` is not modified |
</threat_model>

<verification>
**Frontend gate (the constraint's exact command, run from the repo root before each commit):**

```
cd frontend && npm run lint && npm test -- --run && npm run build && npm run knip
```

`npm run build` is load-bearing here, not ceremony: `npm run lint` + `npm test` do NOT
type-check (esbuild strips types), and this change touches shared types (`UserProfile` in
`Train.tsx`, the widened `beforeSend` return type).

**Backend is out of scope (D-01).** Do not run the backend suite and do not modify any file
under `app/`, `tests/`, or `alembic/`. Confirm with `git status` before each commit.

**Revert-proof check (D-11, mandatory — by actually reverting, never by grep):**
1. Remove the `canTrain` guard from `Train.tsx`'s mount effect → the guest "zero /train/*
   requests" test MUST fail. Restore.
2. Replace the `isGuest` render branch with the normal `TrainStartScreen` path → the
   `train-guest-gate` CTA test MUST fail. Restore.
3. Delete the new suppressible-code drop branch in `sentryBeforeSend` → all three drop tests
   MUST fail while the foreground+online keep tests still pass. Restore.
Record the observed failures in the SUMMARY. A fix that survives its own revert is not proven.

**Commit discipline (D-10) — two atomic commits, in this order:**
1. Task 1 files only, message containing the literal `Fixes FLAWCHESS-64`.
2. Task 2 files only, message containing the literal `Fixes FLAWCHESS-24`.
Each commit must be independently green against the frontend gate — do not stage both fixes
together and split afterwards. Verify with `git log --oneline -2` and
`git show --stat HEAD~1 HEAD`.

**Read-the-diff checks (no runtime needed):**
- `App.tsx` is unmodified — the Train nav item is still unconditional for guests (D-05).
- `is_guest` is read from `useUserProfile()`; `useAuth()` appears only inside
  `TrainGuestGate.tsx`, and only for `logoutForPromotion` (D-02).
- Every `react-router` import in new code is from `'react-router'`, not `'react-router-dom'` (D-12).
- No `text-xs` (or any sub-`text-sm` size) in `TrainGuestGate.tsx` (CLAUDE.md font floor).
- The CTA button is `variant="default"`, not `brand-outline` (D-06).
- `instrument.ts` reads `window`/`navigator`/`document` only behind `typeof` guards (D-09).
- No new entry in `frontend/package.json` dependencies.
</verification>

<success_criteria>
- A guest reaching `/train` issues zero `/train/*` requests and sees a primary-button sign-up
  CTA stating that Train requires a free account; the button promotes the guest account in
  place via `logoutForPromotion()` + `window.location.href = '/login?tab=register'`.
- The Train nav item is still visible to guests; `App.tsx` is unchanged.
- Non-guest Train behavior is byte-identical in effect: `startSession()` fires exactly once per
  page mount and the existing solve-loop tracer test passes.
- `sentryBeforeSend` drops `ERR_NETWORK`/`ERR_CANCELED` axios errors while unloading, offline,
  or hidden, and still reports + fingerprints the foreground+online `ERR_NETWORK` variant.
- 401 drops, 500 → `api-server-error`, and `ECONNABORTED` → `api-timeout` are unchanged.
- Every new assertion fails when its own fix is reverted.
- `npm run lint`, `npm test -- --run`, `npm run build` and `npm run knip` all pass in
  `frontend/`.
- Exactly two commits, carrying `Fixes FLAWCHESS-64` and `Fixes FLAWCHESS-24` respectively,
  with no backend files touched.
</success_criteria>

<output>
Create `.planning/quick/260807-dr9-guest-gate-on-train-drop-unactionable-ax/260807-dr9-SUMMARY.md` when done.
</output>
