---
quick_id: 260905-p0t
description: SEED-163 group 2 — analytics defaults flip to Human + Rated, Library keeps FlawChess-native games via one apply_game_filters bypass flag, plus disclosure hint and a named empty state
mode: quick
date: 2026-09-05
type: execute
autonomous: true
requirements: [SEED-163-2a, SEED-163-2b, SEED-163-2c, SEED-163-2d]
files_modified:
  - app/repositories/query_utils.py
  - app/repositories/library_repository.py
  - tests/repositories/test_native_games_bypass.py
  - frontend/src/components/filters/FilterPanel.tsx
  - frontend/src/pages/library/FlawsTab.tsx
  - frontend/src/hooks/useGameCount.ts
  - frontend/src/components/ui/no-human-rated-games-state.tsx
  - frontend/src/pages/Openings.tsx
  - frontend/src/pages/openings/GamesTab.tsx
  - frontend/src/pages/Endgames.tsx
  - frontend/src/pages/GlobalStats.tsx
  - CHANGELOG.md
  - .planning/seeds/SEED-163-analytics-population-excludes-bot-games.md
estimate:
  tokens: 130000
  raw_tokens: 65000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A fresh page load of Openings / Endgames / Stats analyses rated games against human opponents only, with the Opponent and Rated controls visibly reading Human and Rated."
    - "The modified-filters dot stays off on a fresh load (FILTER_DOT_FIELDS compares against the new DEFAULT_FILTERS)."
    - "A user who plays a FlawChess bot game finds it in the Library Games tab on a fresh load, without touching any filter."
    - "A pasted PGN revealed by the Library Pasted chip still appears in the Library Games and Flaws lists under the new Rated default."
    - "A flawchess game with is_computer_game=True, rated=False does NOT appear in any analytics query under opponent_type='human', rated=True."
    - "The Library filter panel states in one line that FlawChess bot games and pasted games are always shown there; no other filter panel says this."
    - "A user with imported games whose default population is empty sees a named empty state that reports their total game count and points at the Opponent and Rated controls."
    - "The Library Flaws tab's unfiltered probe stays filter-independent (it still answers 'do you have ANY engine-analyzed games')."
  artifacts:
    - "app/repositories/query_utils.py with one new keyword-only bypass flag on apply_game_filters"
    - "tests/repositories/test_native_games_bypass.py (red if the flag's OR wrapper is removed)"
    - "frontend/src/hooks/useGameCount.ts (shared ['gameCount'] query)"
    - "frontend/src/components/ui/no-human-rated-games-state.tsx (component + predicate)"
    - "CHANGELOG.md [Unreleased] → Changed bullet"
  key_links:
    - "DEFAULT_FILTERS (FilterPanel.tsx) → useFilterStore module state → every analytics hook's wire params"
    - "library_repository.query_filtered_games / query_flaws → apply_game_filters(native_games_bypass_opponent_and_rated=True)"
    - "LibraryFilterPanel showPastedChip → FilterPanel platform block → disclosure hint"
    - "useGameCount + areFiltersEqual(filters, DEFAULT_FILTERS, FILTER_DOT_FIELDS) → NoHumanRatedGamesState on all three analytics pages"
---

# Quick task 260905-p0t: analytics default Human + Rated (SEED-163 group 2)

## Scope

**In scope:** SEED-163 section 2 only — 2a (frontend default flip), 2b (Library-only
server-side exemption), 2c (disclosure hint), 2d (empty state).

**Out of scope (locked by the seed):** any change to `DEFAULT_EXCLUDED_PLATFORMS`; a
second filter store or per-field untouched tracking; a bot-games list outside the
Library; decoupling the Openings matching-games list from the WDL population.
Section 1 shipped as quick task `260905-mgx` — do not touch
`app/repositories/openings_repository.py`.

**Hard rule:** do NOT run `bin/reset_db.sh`. Work against the existing dev DB. The
pytest session clones its own database from a migrated template (`tests/conftest.py`),
so no manual DB preparation is needed.

## Grounded facts (verified at planning time, 2026-09-05)

| Fact | Location |
|---|---|
| `DEFAULT_FILTERS.rated = null`, `.opponentType = 'both'` | `frontend/src/components/filters/FilterPanel.tsx:87-102` (rated at :91, opponentType at :92) |
| `FILTER_DOT_FIELDS` includes `rated` + `opponentType`, compares against `DEFAULT_FILTERS` | `FilterPanel.tsx:118-131` |
| Store is module memory seeded from `DEFAULT_FILTERS`, no localStorage | `frontend/src/hooks/useFilterStore.ts:5` |
| `apply_game_filters` opponent+rated predicates | `app/repositories/query_utils.py:287-292` |
| Library **Games** tab query | `app/repositories/library_repository.py::query_filtered_games` (:1760), calls `apply_game_filters` at :1806 |
| Library **Flaws** tab query | `app/repositories/library_repository.py::query_flaws` (:1079), calls `apply_game_filters` at :1193 |
| Both are called **only** from `library_service` (`:964` games, `:1453` flaws) — no other production caller | verified by grep |
| `_filtered_games_base` (:1703) is used by the Library *analytics* counters only, NOT by `query_filtered_games` | verified by grep |
| `resolve_library_platforms` — games surface base includes `flawchess`; analytics surface base does not | `app/services/library_service.py:866-902` |
| FlawChess bot games: `platform="flawchess", rated=False, is_computer_game=True` | `app/services/normalization.py:717-718` |
| Pasted games: `platform="pgn", rated=False, is_computer_game=False` | `app/services/normalization.py:1082-1083` |
| `showPastedChip` slot (platform chip grid) | `FilterPanel.tsx:503-536`; only caller sets it at `LibraryFilterPanel.tsx:117` |
| Openings total-game-count query `/users/games/count` (counts ALL platforms) | `frontend/src/pages/Openings.tsx:210-218`; backend `app/routers/users.py:205`, `game_repository.count_games_for_user` (no platform filter) |
| Openings empty states | `frontend/src/pages/openings/GamesTab.tsx:46-64` (`hasNoGames` / `filtersMatchNothing`, both computed in `Openings.tsx:455-457`) |
| Endgames empty states + `statsData.total_games` ("games matching current filters") | `frontend/src/pages/Endgames.tsx:702-720`; type at `frontend/src/types/endgames.ts:69` |
| GlobalStats has **no** page-level empty state; `content` starts at `frontend/src/pages/GlobalStats.tsx:129` | verified |
| Library Flaws unfiltered probe hard-codes only `opponentType: 'both'` | `frontend/src/pages/library/FlawsTab.tsx:46` |
| Regression-test template (same shape, `db_session` rollback fixture, `ensure_test_user`) | `tests/repositories/test_pasted_platform_exclusion.py` |
| Test-module user IDs in use: 92400, 92401, 92402, 92403, 92500, 92600 | grep over `tests/` |

## MUTABLE-SCOPE AUTHORITY

Edit scope is authorized only for the files listed in `files_modified`, plus any
existing test file whose assertions encode the old defaults and which the verification
commands prove red. If a fourth production file turns out to need editing, stop and
report rather than widening scope silently.

<tasks>

<task type="auto">
  <name>Task 1: Library-only native-game bypass in apply_game_filters (2b)</name>
  <files>app/repositories/query_utils.py, app/repositories/library_repository.py, tests/repositories/test_native_games_bypass.py</files>
  <read_first>
    - app/repositories/query_utils.py:152-300 (signature, docstring, platform/rated/opponent predicates)
    - app/repositories/library_repository.py:1079-1200 (query_flaws) and :1760-1825 (query_filtered_games)
    - tests/repositories/test_pasted_platform_exclusion.py (whole file — the structural template)
  </read_first>
  <action>
Add ONE keyword-only flag to `apply_game_filters`:
`native_games_bypass_opponent_and_rated: bool = False`, placed with the other
keyword-only params after the `*`.

Replace the three independent predicates at query_utils.py:287-292 (`rated`,
`opponent_type == "human"`, `opponent_type == "bot"`) with a single composed
predicate block: build the list of active opponent/rated conditions first, AND them
together, and when the flag is set wrap the result as
`or_(Game.platform.in_(DEFAULT_EXCLUDED_PLATFORMS), <anded_predicates>)` before
appending it to the statement. When the flag is False the emitted SQL must be
identical to today's (one `.where()` per active predicate is fine; the observable
row set is what matters). When no opponent/rated predicate is active, emit nothing
at all — do NOT emit a bare platform OR-clause that would widen an unfiltered query.
Import `or_` and `and_` from `sqlalchemy` alongside the existing `case, literal`.

Extend the `apply_game_filters` docstring Args block with an entry for the new flag,
in the same voice as the surrounding entries. State: only the Library Games and Flaws
surfaces set it; it makes rows whose `platform` is in `DEFAULT_EXCLUDED_PLATFORMS`
ignore the Opponent and Rated filters unconditionally (including an explicit user
selection), because those games are stored `rated=False` and, for `flawchess`,
`is_computer_game=True`, so the new Human+Rated analytics defaults would otherwise
hide a game the user just finished from the only place it is browsable. Note that
those rows remain governed by the Platform filter and, for `pgn`, by the Library
`pasted` control (D-14), and that this extends Phase 167 D-03's "always here"
guarantee from the platform axis to the opponent/rated axes.

Set the flag at exactly TWO call sites, hard-coded `True` (do not thread a parameter
through the service or router — each function serves exactly one Library surface and
has no other production caller):
  - `library_repository.query_filtered_games` → its `apply_game_filters(...)` call
  - `library_repository.query_flaws` → its `game_filter_stmt = apply_game_filters(...)` call
At each site add a short comment naming SEED-163 2b and why the Library is the
exception. Do NOT set it in `_filtered_games_base` (that feeds the Library flaw-stats
and comparison analytics counters, which are analytics surfaces). Do NOT add any
predicate block in `app/services/library_service.py` — CLAUDE.md's single-seam rule
applies, and `resolve_library_platforms` stays exactly as it is.

Write `tests/repositories/test_native_games_bypass.py`, structured after
`tests/repositories/test_pasted_platform_exclusion.py` (module docstring explaining
the red-if-reverted contract, `_TEST_USER_ID = 92404`, `ensure_test_user`, the
rollback-scoped `db_session` fixture, `pytestmark = pytest.mark.asyncio`). Seed three
games for one user: a `chess.com` human rated game, a `flawchess` game with
`rated=False, is_computer_game=True`, and a `pgn` game with `rated=False,
is_computer_game=False`. Assert, all under `opponent_type="human", rated=True`:
  1. `apply_game_filters(..., platform=list(LIBRARY_GAMES_BASE_PLATFORMS), native_games_bypass_opponent_and_rated=True)` returns the chess.com AND flawchess ids.
  2. The same call with the flag left at its default returns only the chess.com id (this is the assertion that goes red if the OR wrapper is removed).
  3. `apply_game_filters(..., platform=None, ...)` — the analytics default — returns only the chess.com id whether or not the flag is set, proving the bypass cannot leak a native game into an analytics population.
  4. With `platform=["chess.com", "lichess", "pgn"]` and the flag set, the pgn id is returned (the Library Pasted-chip path under the new Rated default).
  5. With the flag set and `opponent_type="bot"`, the flawchess id is still returned (the bypass is unconditional, not "only at the human default").
Import `LIBRARY_GAMES_BASE_PLATFORMS` from `app.services.library_service` for case 1.

Do NOT run `bin/reset_db.sh`.
  </action>
  <verify>
    <automated>uv run pytest tests/repositories/test_native_games_bypass.py -x -q</automated>
    <automated>uv run pytest tests/repositories/test_query_utils.py tests/repositories/test_pasted_platform_exclusion.py tests/test_library_repository.py tests/repositories/test_library_repository.py tests/services/test_library_service.py tests/services/test_library_include_pasted.py tests/test_openings_repository.py -q</automated>
    <automated>uv run ruff format app/ tests/ && uv run ruff check . --fix && uv run ty check app/ tests/ scripts/</automated>
    <automated>uv run python scripts/check_function_size.py app/ --fail-over-depth 4 --fail-over-loc 200</automated>
  </verify>
  <done>
`apply_game_filters` carries exactly one new keyword-only flag; the two Library query
functions set it and nothing else does; `test_native_games_bypass.py` passes and case 2
fails if the OR wrapper is reverted; the existing Library / query_utils / openings
suites pass; ruff, ty and the function-size gate are clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Analytics defaults flip to Human + Rated, plus the Library disclosure hint (2a + 2c)</name>
  <files>frontend/src/components/filters/FilterPanel.tsx, frontend/src/pages/library/FlawsTab.tsx</files>
  <read_first>
    - frontend/src/components/filters/FilterPanel.tsx:87-131 (DEFAULT_FILTERS, resetFilterState, FILTER_DOT_FIELDS) and :503-536 (platform block + pasted chip)
    - frontend/src/components/filters/LibraryFilterPanel.tsx:101-119 (the showPastedChip call site and its containment comment)
    - frontend/src/pages/library/FlawsTab.tsx:43-47 and :221-227 (the unfiltered probe and its stated purpose)
  </read_first>
  <action>
**2a — default flip.** In `DEFAULT_FILTERS` set `rated: true` and
`opponentType: 'human'`. Update the two inline comments on those lines to say what the
default now means (rated games only; human opponents only) and add a short block
comment above `DEFAULT_FILTERS` recording SEED-163 2a: the analytics default population
is now Human + Rated so it matches the percentile benchmark cohort
(`g.rated AND NOT g.is_computer_game`), the store is module memory so this reaches
every user on next load with no migration, and the filter stays visible and reversible.
Change nothing else in `FilterPanel.tsx`'s filter logic — `FILTER_DOT_FIELDS`,
`resetFilterState` and `areFiltersEqual` all compare against `DEFAULT_FILTERS`, so the
modified-dot and Reset follow automatically. The `FilterState` type comment on
`opponentType` currently describes the old `both` default; correct it.

**Probe fix (load-bearing).** `FlawsTab.tsx:46` builds `UNFILTERED_PROBE_FILTERS` as
`{ ...DEFAULT_FILTERS, opponentType: 'both' }` and its comment declares it
filter-independent. With `rated: true` in the base it silently becomes rated-only,
which would show the "no engine-analysed games" message to a user whose analysed games
are unrated. Add `rated: null` to that override and extend the comment to say the
override must neutralise every narrowing default, naming both fields.

**2c — disclosure hint.** In `FilterPanel.tsx`, inside the Platform block and gated by
the same `showPastedChip` flag, render a one-line hint immediately below the chip grid
with `data-testid="filter-native-games-hint"` and exactly this copy:
`FlawChess bot games and pasted games are always shown here.`
Style it as muted helper text consistent with the panel's existing
`text-sm text-muted-foreground` labels (a smaller `text-xs` variant is fine); use
Tailwind classes only, no new component. Add a comment tying it to SEED-163 2c and to
the existing `showPastedChip` containment guarantee: `LibraryFilterPanel` is the only
caller that sets the flag, so Openings, Endgames and GlobalStats never render the hint,
and the same component instance covers the desktop sidebar and the mobile drawer.
Do not add a prop, and do not add store state.

**Existing tests.** Run the frontend suite and repair any assertion that encoded the
old defaults. Known candidates to inspect (they consume `DEFAULT_FILTERS` but were not
observed to assert `rated`/`opponent_type` values): `hooks/__tests__/useLibrary.test.ts`,
`hooks/__tests__/useLibraryGames.test.tsx`, `hooks/__tests__/useEndgameOverview.test.tsx`,
`components/filters/__tests__/PastedChip.test.tsx`. When repairing, change the
expectation to the new default rather than pinning the old value into the test.

Add coverage in `components/filters/__tests__/PastedChip.test.tsx` (it already owns the
`showPastedChip` containment contract): assert `DEFAULT_FILTERS.rated` is `true` and
`DEFAULT_FILTERS.opponentType` is `'human'`; assert the hint testid renders when
`LibraryFilterPanel` is used; assert it is absent from a bare `FilterPanel` with
`visibleFilters={['platform']}`.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run</automated>
    <automated>cd frontend && npm run lint</automated>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>
`DEFAULT_FILTERS` reads `rated: true` / `opponentType: 'human'`; the Library probe
explicitly neutralises both; the hint renders under `LibraryFilterPanel` only and its
absence from a plain `FilterPanel` is asserted; lint, the full vitest run, and the
tsc build via `npm run build` are all green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Named empty state for a filtered-to-zero analytics population, changelog, seed status (2d)</name>
  <files>frontend/src/hooks/useGameCount.ts, frontend/src/components/ui/no-human-rated-games-state.tsx, frontend/src/pages/Openings.tsx, frontend/src/pages/openings/GamesTab.tsx, frontend/src/pages/Endgames.tsx, frontend/src/pages/GlobalStats.tsx, CHANGELOG.md, .planning/seeds/SEED-163-analytics-population-excludes-bot-games.md</files>
  <read_first>
    - frontend/src/components/ui/empty-state.tsx (whole file — the `layout="page"` treatment to reuse)
    - frontend/src/pages/Openings.tsx:205-220 (inline gameCount query) and :450-460 (hasNoGames / filtersMatchNothing) and :495-510 (GamesTab props)
    - frontend/src/pages/openings/GamesTab.tsx:1-70
    - frontend/src/pages/Endgames.tsx:700-722
    - frontend/src/pages/GlobalStats.tsx:110-135 and :190-205
  </read_first>
  <action>
**Shared count hook.** Create `frontend/src/hooks/useGameCount.ts` exporting
`useGameCount()`: the exact query currently inlined at `Openings.tsx:210-217`
(`queryKey: ['gameCount']`, `apiClient.get<{ count: number }>('/users/games/count')`,
`staleTime: 30_000`). Keeping the same query key means the three pages share one cached
request. Document that this counts ALL of the user's imported games including
`flawchess` and `pgn` rows, which is deliberate: a user whose only games are bot games
must read "You have N games", not "You have 0 games". Replace the inline query in
`Openings.tsx` with the hook and drop the now-unused `useQuery` / `apiClient` imports.

**Empty-state module.** Create
`frontend/src/components/ui/no-human-rated-games-state.tsx` exporting two things:

  1. `shouldShowNoHumanRatedGames(filters: FilterState, totalGames: number | null): boolean`
     — returns true only when `totalGames !== null && totalGames > 0` AND
     `areFiltersEqual(filters, DEFAULT_FILTERS, FILTER_DOT_FIELDS)`. Document the
     reason for the second condition: the copy asserts a cause, so it may only be
     shown when the new Human+Rated defaults are the ONLY thing narrowing the
     population. If the user has also set a time control, platform or date filter,
     the existing generic "try adjusting your filters" state stays correct and this
     one must not claim otherwise.
  2. `NoHumanRatedGamesState({ totalGames }: { totalGames: number })` — renders
     `EmptyState` with `layout="page"`,
     `data-testid="empty-no-human-rated-games"`,
     title `No rated games against humans`, and subtitle
     `You have {totalGames} games, but none are rated games against humans. Change the Opponent or Rated filter to include them.`
     Interpolate `totalGames`; no other copy variants, no action button.

Import `DEFAULT_FILTERS`, `FILTER_DOT_FIELDS`, `areFiltersEqual` and the `FilterState`
type from `@/components/filters/FilterPanel` (one-directional — `FilterPanel` must not
import this module).

**Wire three surfaces.**
  - `Openings.tsx`: compute `noHumanRatedGames = shouldShowNoHumanRatedGames(filters, gameCount)` next to the existing `hasNoGames` / `filtersMatchNothing` derivations and pass it to `GamesTab` as a new prop, mirroring how `filtersMatchNothing` is already threaded.
  - `openings/GamesTab.tsx`: accept the new boolean prop and, inside the existing `filtersMatchNothing` branch, render `NoHumanRatedGamesState` when it is true, otherwise keep the current "No games matched" state. `gameCount` is already a prop; pass it through (fall back to `0` for the `number` type).
  - `Endgames.tsx`: in the overview branch chain, insert a case ahead of the current `statsData && statsData.categories.length === 0` branch: when `statsData` is present, `statsData.total_games === 0`, and `shouldShowNoHumanRatedGames(appliedFilters, gameCount)` holds, render `NoHumanRatedGamesState`. `total_games` is documented as "games matching current filters", which is the correct signal here; `categories.length === 0` is not (a user can have games but no endgames).
  - `GlobalStats.tsx`: add `useGameCount()`, and turn the `content` expression into a three-way conditional — loading, then the new empty state when `globalStats` has loaded with both `by_time_control` and `by_color` empty AND `shouldShowNoHumanRatedGames(filters, gameCount)` holds, then the existing sections block unchanged.

**Tests.** Add `frontend/src/components/ui/__tests__/no-human-rated-games-state.test.tsx`
covering the predicate and the component: true at the defaults with a positive count;
false when `totalGames` is 0 or null; false when any `FILTER_DOT_FIELDS` field diverges
from the default (use a time-control override as the case); and that the rendered node
carries the testid and reports the supplied count.

**Docs.** Append to `CHANGELOG.md` under `## [Unreleased]`, in a `### Changed` section
(create it below the existing `### Fixed` if absent), one user-facing bullet covering
the whole group: the Openings, Endgames and Stats pages now default to rated games
against human opponents so they match the population the percentile benchmarks compare
against, the Opponent and Rated filters still switch it back, and the Library keeps
showing FlawChess practice-bot games and pasted PGNs regardless of those two filters.
Follow the file's existing voice; use em-dashes sparingly.

Update `.planning/seeds/SEED-163-analytics-population-excludes-bot-games.md`: set
`status: complete`, bump `updated: 2026-09-05`, and mark section 2's heading as SHIPPED
with the quick-task id `260905-p0t`, mirroring how section 1 records `260905-mgx`.
Leave the design body intact as provenance.

Do NOT run `bin/reset_db.sh`.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run</automated>
    <automated>cd frontend && npm run lint</automated>
    <automated>cd frontend && npm run build</automated>
    <automated>uv run pytest -n auto -x</automated>
    <automated>grep -n "empty-no-human-rated-games" frontend/src/pages/openings/GamesTab.tsx frontend/src/pages/Endgames.tsx frontend/src/pages/GlobalStats.tsx frontend/src/components/ui/no-human-rated-games-state.tsx | grep -c . </automated>
  </verify>
  <done>
One shared `useGameCount` hook backs all three pages; the named empty state renders on
Openings Games, Endgames overview and GlobalStats only when the defaults alone empty
the population; its unit tests pass; the CHANGELOG has an `[Unreleased] → Changed`
bullet; SEED-163 is marked complete with this quick-task id; the full backend suite and
the three frontend gates are green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `/library/*`, `/openings/*`, `/endgames/*`, `/stats/*` | Authenticated user supplies filter query params; all reads stay scoped by `user_id` |

No new boundary is introduced: the change adds one server-side predicate modifier and
three client-side render branches. No new package is installed, so the package-legitimacy
gate does not apply.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-p0t-01 | Information disclosure | `apply_game_filters` bypass flag | medium | mitigate | The OR wrapper touches only the opponent/rated predicates. The `Game.user_id == user_id` scope is applied by every caller *outside* `apply_game_filters` and is untouched, and the platform predicate stays ANDed. Test case 3 in `test_native_games_bypass.py` pins that `platform=None` (the analytics default) never returns a native game with the flag set. |
| T-p0t-02 | Information disclosure | `query_flaws` bypass at the Flaws tab | low | mitigate | `get_library_flaws` resolves platforms with `surface="analytics"`, so `flawchess` is never in the list and the bypass can only reach `pgn` rows the user explicitly opted into with the Pasted chip. Test case 4 pins that path. |
| T-p0t-03 | Tampering | client-side default flip | low | accept | Defaults are a UI convenience; the backend routers already default `opponent_type="human"` and treat `rated` as an optional filter, so a client sending any combination gets no more data than its own user scope allows. |
</threat_model>

<verification>
Run the full pre-merge gate before integrating (CLAUDE.md), from the repo root:

```
uv run ruff format app/ tests/ scripts/ analysis/
uv run ruff check . --fix
uv run ty check app/ tests/ scripts/
uv run --project analysis --with ty ty check analysis/
uv run python scripts/check_function_size.py app/ --fail-over-depth 4 --fail-over-loc 200
uv run pytest -n auto -x
( cd frontend && npm run lint && npm test -- --run && npm run build )
```

`npm run build` is mandatory, not optional: lint and vitest run through esbuild and do
not type-check, so a broken prop or type only surfaces there.

If a formatter or autofix step modifies files, commit that with a `style(...)` /
`chore(...)` prefix.
</verification>

<success_criteria>
- A fresh browser load of Openings, Endgames and Stats shows Opponent = Human and Rated = Rated with the modified-filters dot OFF.
- Switching Opponent back to Both or Rated back to All restores the previous population on every analytics page.
- The Library Games tab shows a FlawChess bot game and (with the Pasted chip on) a pasted PGN on a fresh load, with no filter changes.
- The Library filter panel carries the disclosure line; Openings / Endgames / GlobalStats panels do not.
- Reverting the OR wrapper in `apply_game_filters` turns `test_native_games_bypass.py` red.
- Full pre-merge gate green.
</success_criteria>

<output>
Write `.planning/quick/260905-p0t-seed-163-group-2-analytics-default-human/260905-p0t-SUMMARY.md` when done.
</output>
</content>
</invoke>
