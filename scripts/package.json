{
  "name": "flawchess-scripts",
  "private": true,
  "description": "Native deps for the Node analysis harness only, NOT the app. onnxruntime-node is PINNED at 1.29.0: >=1.22 segfaulted loading the vendored maia3_simplified.onnx at older versions, the exact same pin rationale as pyproject.toml's onnxruntime==1.29.0 (see scripts/maia_parity_spike.py). Any bump must survive `node scripts/engine_disagreement_study/verify_value_head.mjs` first (that gate explicitly requests the native backend). History: 1.21.1 was the last known-safe version prior to Phase 218. Phase 218 measured onnxruntime-node 1.29.0 (2026-09-05): PASS, no crash, all value-head checks passed. Evidence: .planning/phases/218-backend-onnxruntime-parity-spike-python-3-14-chain/218-evidence-onnxruntime-node-1.29.0.txt. Pin raised to 1.29.0 to unblock the Python 3.14 chain (218-02).",
  "dependencies": {
    "onnxruntime-node": "1.29.0"
  }
}
